# 진단 명령어 2026-03-18: Plan A 실행 → Goal2 재진단 → 3중 수정 → 무한회전 수정

> 이 문서는 2026-03-18 세션에서 사용한 모든 명령어, 수정 내역, 스크립트, 빌드/배포 과정을 기록합니다.
> Phase 1 = Plan A 실행 (방안B-2 제거, Nav2 파라미터 복원)
> Phase 2 = Goal2 데드존 재진단 + 3중 수정 (MIN_DZ 0.25, velocity_smoother, forward_sampling)
> Phase 3 = 무한회전 진단 + 방안B 감속 감지 추가

---

## 목차
1. [Phase 1: 드라이버 수정 — 방안B-2 제거 + 방안B 재활성화](#1-phase-1-드라이버-수정--방안b-2-제거--방안b-재활성화)
2. [Phase 1: Nav2 파라미터 복원](#2-phase-1-nav2-파라미터-복원)
3. [Phase 1: 빌드 및 배포 검증](#3-phase-1-빌드-및-배포-검증)
4. [Phase 2: Goal2 진단 — 테스트 스크립트 실행 및 cmd_vel 관찰](#4-phase-2-goal2-진단--테스트-스크립트-실행-및-cmd_vel-관찰)
5. [Phase 2: velocity_smoother 토픽 흐름 분석](#5-phase-2-velocity_smoother-토픽-흐름-분석)
6. [Phase 2: 3중 수정 적용](#6-phase-2-3중-수정-적용)
7. [Phase 2: 빌드 및 배포 검증](#7-phase-2-빌드-및-배포-검증)
8. [Phase 3: 무한회전 진단 및 방안B 감속 감지 수정](#8-phase-3-무한회전-진단-및-방안b-감속-감지-수정)
9. [재시작 및 테스트 명령어](#9-재시작-및-테스트-명령어)

---

## 1. Phase 1: 드라이버 수정 — 방안B-2 제거 + 방안B 재활성화

### 1.1 수정 대상 파일
```
jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```

### 1.2 삭제: __init__의 방안B-2 상태변수

삭제된 코드 (9줄):
```python
# [2026-03-17 방안B-2] Anti-hunting angular velocity filter state
# MCU 데드존(0/72/144) + PID 적분 윈드업으로 인한
# 헌팅→오버슈트→발산 방지용 상태 변수
# ※ 방안A(펌웨어) 적용 후 아래 초기화 및 _anti_hunting_filter() 삭제할 것
self._ah_last_angular = 0.0
self._ah_last_time = time.time()
self._ah_reversal_count = 0
self._ah_last_reversal_time = 0.0
self._ah_brake_until = 0.0
```

### 1.3 삭제: _anti_hunting_filter() 메서드 전체

약 55줄의 메서드 전체 삭제 (방안A 펌웨어 적용으로 불필요).

### 1.4 삭제: cmd_vel_callback의 방안B-2 주석 호출

삭제된 코드 (3줄, 이미 주석 상태였음):
```python
# # [2026-03-17 방안B-2] Anti-hunting filter: 방향전환 감쇠 + rate limiting
# # ※ 방안A(펌웨어) 적용 후 아래 1줄 삭제할 것
# angular_corrected = self._anti_hunting_filter(angular_corrected)
```

### 1.5 재활성화+수정: 방안B (최소 회전속도)

변경 전 (주석상태, 0.25):
```python
# [2026-03-16 방안B] MCU 데드존 우회: 최소 회전 속도 보장
# MCU Motor_Ignore_Dead_Zone에서 작은 PID 출력이 0 또는 역방향으로 매핑됨
# ※ 방안A(펌웨어 Motor_Ignore_Dead_Zone 비례매핑) 적용 후 아래 블록 제거할 것
# ── 방안B 시작 ──
# MIN_ANGULAR_DEADZONE = 0.25
# if abs(angular_corrected) > 0.01 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
#     angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
# # ── 방안B 끝 ──
```

변경 후 (Phase 1 — 활성화, **0.15**, 주석 갱신):
```python
# [방안B] 모터 물리적 데드존 우회: 최소 회전 속도 보장
# 방안A(펌웨어) 적용 후에도 ESC 서보 중립(108) 근처는 토크 미발생
# Nav2/velocity_smoother가 작은 angular 출력 시 ESC 물리 데드존에 빠짐
# ── 방안B 시작 ──
MIN_ANGULAR_DEADZONE = 0.15  # rad/s (scaled) — ESC 물리 데드존 탈출 최소값
if abs(angular_corrected) > 0.005 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
    angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
# ── 방안B 끝 ──
```

> ⚠️ 이 0.15 값은 Phase 2에서 **0.25**로 상향됨 (Section 6.1 참조)

---

## 2. Phase 1: Nav2 파라미터 복원

### 2.1 수정 대상 파일
```
jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```

### 2.2 복원된 파라미터

```yaml
# 변경: 0.7 → 1.0
max_vel_theta: 1.0           # 각속도 최대값 (방안A 펌웨어 적용으로 1.0 복원)

# 변경: 0.5 → 0.8
acc_lim_theta: 0.8           # 각가속도 (방안A 펌웨어 적용으로 0.8 복원)

# 변경: -0.5 → -0.8
decel_lim_theta: -0.8        # 각감속도 (방안A 펌웨어 적용으로 -0.8 복원)

# 변경: 15.0 → 5.0
Twirling.scale: 5.0          # 경로 추종 중 불필요한 스핀 방지 (방안A 적용으로 5.0 복원)

# 변경: 5.0 → 1.0
Oscillation.scale: 1.0       # 진동 페널티 (방안A 적용으로 1.0 복원)
```

---

## 3. Phase 1: 빌드 및 배포 검증

### 3.1 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
source /opt/ros/humble/setup.bash
colcon build --packages-select jupiter_bringup jupiter_nav_VO --symlink-install
```

### 3.2 방안B-2 완전 삭제 확인
```bash
grep -c "_anti_hunting_filter\|_ah_last" \
  /home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
# 결과: 0 (정상 — 방안B-2 흔적 없음)
```

### 3.3 방안B 재활성화 확인
```bash
grep -n "MIN_ANGULAR_DEADZONE" \
  /home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
# 결과:
# 240:            MIN_ANGULAR_DEADZONE = 0.15  # rad/s (scaled) — ESC 물리 데드존 탈출 최소값
# 241:            if abs(angular_corrected) > 0.005 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
# 242:                angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
```

### 3.4 Nav2 파라미터 복원 확인
```bash
grep -n "max_vel_theta\|acc_lim_theta\|decel_lim_theta\|Twirling.scale\|Oscillation.scale" \
  /home/jetson/jupiter_ws_ros2/install/jupiter_nav_VO/share/jupiter_nav_VO/config/nav2_params_fused.yaml
# 결과:
# 124:      max_vel_theta: 1.0
# 130:      acc_lim_theta: 0.8
# 133:      decel_lim_theta: -0.8
# 156:      Twirling.scale: 5.0
# 159:      Oscillation.scale: 1.0
```

---

## 4. Phase 2: Goal2 진단 — 테스트 스크립트 실행 및 cmd_vel 관찰

### 4.1 테스트 스크립트 전체 (`scripts/test_deadzone_bypass.py`)

```python
#!/usr/bin/env python3
"""방안B 데드존 우회 검증 + Nav2 Goal 테스트"""
import rclpy
from rclpy.node import Node
from geometry_msgs.msg import Twist, PoseStamped
from nav_msgs.msg import Odometry
from action_msgs.msg import GoalStatus
from rclpy.action import ActionClient
from nav2_msgs.action import NavigateToPose
import math, time, threading, sys

class DeadzoneAndGoalTest(Node):
    def __init__(self):
        super().__init__('deadzone_goal_test')
        self.pub = self.create_publisher(Twist, '/cmd_vel', 10)
        self.yaw_vals = []
        self.odom_msgs = []
        self.vslam_count = 0
        
        self.create_subscription(Odometry, '/odom', self.odom_cb, 10)
        self.create_subscription(Odometry, '/visual_slam/tracking/odometry_adapted',
                                 self.vslam_cb, 10)
        
        self.nav_client = ActionClient(self, NavigateToPose, 'navigate_to_pose')
        
    def odom_cb(self, msg):
        q = msg.pose.pose.orientation
        siny = 2.0 * (q.w * q.z + q.x * q.y)
        cosy = 1.0 - 2.0 * (q.y * q.y + q.z * q.z)
        yaw = math.atan2(siny, cosy)
        self.yaw_vals.append((time.time(), math.degrees(yaw)))
        self.odom_msgs.append(msg)
        
    def vslam_cb(self, msg):
        self.vslam_count += 1

    def test_deadzone(self):
        """Part 1: 작은 회전 명령으로 데드존 우회 확인"""
        print("\n" + "="*60)
        print("Part 1: 데드존 우회 테스트 (angular.z = 0.1 rad/s, 3초)")
        print("="*60)
        
        time.sleep(1.0)
        yaw_start = self.yaw_vals[-1][1] if self.yaw_vals else 0.0
        
        twist = Twist()
        twist.angular.z = 0.1  # 방안B 없으면 데드존에 갇힘
        
        print(f"Initial yaw: {yaw_start:.2f} deg")
        print(f"Sending angular.z = 0.1 rad/s ...")
        
        t0 = time.time()
        while time.time() - t0 < 3.0:
            self.pub.publish(twist)
            time.sleep(0.05)
        
        # 정지
        twist.angular.z = 0.0
        for _ in range(20):
            self.pub.publish(twist)
            time.sleep(0.05)
        time.sleep(1.0)
        
        yaw_end = self.yaw_vals[-1][1] if self.yaw_vals else 0.0
        yaw_delta = yaw_end - yaw_start
        if yaw_delta > 180: yaw_delta -= 360
        if yaw_delta < -180: yaw_delta += 360
        
        print(f"Final yaw: {yaw_end:.2f} deg")
        print(f"Yaw change: {yaw_delta:.2f} deg")
        
        if abs(yaw_delta) > 3.0:
            print(">>> PASS: 로봇 회전됨 — 방안B 데드존 우회 작동")
            return True
        else:
            print(">>> FAIL: 로봇 미움직임 — 데드존 문제 지속")
            return False

    def test_medium_rotation(self):
        """Part 2: 중간 회전 명령 (angular.z = 0.5 rad/s, 이전에 못 움직이던 값)"""
        print("\n" + "="*60)
        print("Part 2: 중간 회전 테스트 (angular.z = 0.5 rad/s, 3초)")
        print("="*60)
        
        time.sleep(1.0)
        yaw_start = self.yaw_vals[-1][1] if self.yaw_vals else 0.0
        
        twist = Twist()
        twist.angular.z = 0.5
        
        print(f"Initial yaw: {yaw_start:.2f} deg")
        print(f"Sending angular.z = 0.5 rad/s ...")
        
        t0 = time.time()
        while time.time() - t0 < 3.0:
            self.pub.publish(twist)
            time.sleep(0.05)
        
        twist.angular.z = 0.0
        for _ in range(20):
            self.pub.publish(twist)
            time.sleep(0.05)
        time.sleep(1.0)
        
        yaw_end = self.yaw_vals[-1][1] if self.yaw_vals else 0.0
        yaw_delta = yaw_end - yaw_start
        if yaw_delta > 180: yaw_delta -= 360
        if yaw_delta < -180: yaw_delta += 360
        
        print(f"Final yaw: {yaw_end:.2f} deg")
        print(f"Yaw change: {yaw_delta:.2f} deg")
        
        if abs(yaw_delta) > 5.0:
            print(">>> PASS: 로봇 회전됨")
        else:
            print(">>> FAIL: 로봇 미움직임")
        return yaw_delta

    def send_nav_goal(self, x, y, yaw_deg, timeout=60.0):
        """Nav2 goal 전송 및 대기"""
        if not self.nav_client.wait_for_server(timeout_sec=5.0):
            print("Nav2 action server not available!")
            return None, 0.0
        
        goal = NavigateToPose.Goal()
        goal.pose.header.frame_id = 'map'
        goal.pose.header.stamp = self.get_clock().now().to_msg()
        goal.pose.pose.position.x = x
        goal.pose.pose.position.y = y
        yaw_rad = math.radians(yaw_deg)
        goal.pose.pose.orientation.z = math.sin(yaw_rad / 2.0)
        goal.pose.pose.orientation.w = math.cos(yaw_rad / 2.0)
        
        t0 = time.time()
        future = self.nav_client.send_goal_async(goal)
        
        # 배경 spin에서 처리하므로 future.done() 폴링
        while not future.done():
            time.sleep(0.05)
            if time.time() - t0 > 10.0:
                print("Goal send timeout!")
                return 'SEND_TIMEOUT', time.time() - t0
        
        goal_handle = future.result()
        
        if not goal_handle or not goal_handle.accepted:
            print("Goal rejected!")
            return 'REJECTED', 0.0
        
        result_future = goal_handle.get_result_async()
        
        while not result_future.done():
            time.sleep(0.1)
            if time.time() - t0 > timeout:
                goal_handle.cancel_goal_async()
                time.sleep(1.0)
                return 'TIMEOUT', time.time() - t0
        
        result = result_future.result()
        elapsed = time.time() - t0
        
        status = result.status
        if status == GoalStatus.STATUS_SUCCEEDED:
            return 'SUCCEEDED', elapsed
        elif status == GoalStatus.STATUS_ABORTED:
            return 'ABORTED', elapsed
        elif status == GoalStatus.STATUS_CANCELED:
            return 'CANCELED', elapsed
        else:
            return f'STATUS_{status}', elapsed

    def test_nav_goals(self):
        """Part 3: Nav2 Goal1 (0.5m 전진) + Goal2 (복귀)"""
        print("\n" + "="*60)
        print("Part 3: Nav2 Goal 테스트")
        print("="*60)
        
        # 현재 위치 기록
        time.sleep(0.5)
        if not self.odom_msgs:
            print("No odom data!")
            return
        
        cur = self.odom_msgs[-1].pose.pose
        cur_x = cur.position.x
        cur_y = cur.position.y
        q = cur.orientation
        cur_yaw = math.degrees(math.atan2(
            2.0 * (q.w * q.z + q.x * q.y),
            1.0 - 2.0 * (q.y * q.y + q.z * q.z)))
        
        print(f"Current pose: x={cur_x:.3f}, y={cur_y:.3f}, yaw={cur_yaw:.1f} deg")
        
        # Goal1: 0.5m 전진
        goal1_x = cur_x + 0.5 * math.cos(math.radians(cur_yaw))
        goal1_y = cur_y + 0.5 * math.sin(math.radians(cur_yaw))
        
        print(f"\n--- Goal1: 0.5m forward ({goal1_x:.3f}, {goal1_y:.3f}, {cur_yaw:.1f}) ---")
        
        vslam_before = self.vslam_count
        yaw_before_g1 = [v[1] for v in self.yaw_vals[-1:]] if self.yaw_vals else [0]
        
        status1, time1 = self.send_nav_goal(goal1_x, goal1_y, cur_yaw, timeout=30.0)
        vslam_g1 = self.vslam_count - vslam_before
        
        print(f"Goal1: {status1} in {time1:.1f}s (VSLAM adapted: {vslam_g1})")
        
        time.sleep(2.0)
        
        # Goal2: 원래 위치 복귀
        print(f"\n--- Goal2: Return to ({cur_x:.3f}, {cur_y:.3f}, {cur_yaw:.1f}) ---")
        
        vslam_before = self.vslam_count
        yaw_vals_g2_start = len(self.yaw_vals)
        
        status2, time2 = self.send_nav_goal(cur_x, cur_y, cur_yaw, timeout=60.0)
        
        vslam_g2 = self.vslam_count - vslam_before
        
        # Goal2 중 yaw 분석
        yaw_during_g2 = [v[1] for v in self.yaw_vals[yaw_vals_g2_start:]]
        if yaw_during_g2:
            yaw_min = min(yaw_during_g2)
            yaw_max = max(yaw_during_g2)
            yaw_span = yaw_max - yaw_min
        else:
            yaw_span = 0
        
        # 최종 위치
        if self.odom_msgs:
            final = self.odom_msgs[-1].pose.pose
            dist = math.sqrt((final.position.x - cur_x)**2 + (final.position.y - cur_y)**2)
        else:
            dist = -1
        
        print(f"Goal2: {status2} in {time2:.1f}s (VSLAM adapted: {vslam_g2})")
        print(f"  Yaw span during Goal2: {yaw_span:.1f} deg")
        print(f"  Final distance to start: {dist:.3f}m")
        
        # 결과 요약
        print("\n" + "="*60)
        print("결과 요약")
        print("="*60)
        print(f"Goal1: {status1} {time1:.1f}s | VSLAM: {vslam_g1}")
        print(f"Goal2: {status2} {time2:.1f}s | VSLAM: {vslam_g2} | yaw_span: {yaw_span:.1f}°")
        
        if status2 == 'SUCCEEDED':
            print(">>> Goal2 SUCCESS — 데드존 우회 효과 확인!")
        elif yaw_span < 90:
            print(">>> Goal2 실패했지만 yaw_span < 90° — 회전 통제됨 (이전 359°)")
        else:
            print(f">>> Goal2 실패, yaw_span={yaw_span:.1f}° — 여전히 회전 문제")


def main():
    rclpy.init()
    node = DeadzoneAndGoalTest()
    
    spin_thread = threading.Thread(target=rclpy.spin, args=(node,), daemon=True)
    spin_thread.start()
    
    try:
        # Part 1: 데드존 우회 확인
        dz_ok = node.test_deadzone()
        
        # Part 2: 중간 회전
        node.test_medium_rotation()
        
        if '--skip-nav' not in sys.argv:
            # Part 3: Nav2 Goal
            node.test_nav_goals()
        else:
            print("\n[Nav2 test skipped (--skip-nav)]")
            
    except KeyboardInterrupt:
        print("\nInterrupted")
    finally:
        # 정지 명령
        twist = Twist()
        for _ in range(10):
            node.pub.publish(twist)
            time.sleep(0.05)
        node.destroy_node()
        rclpy.shutdown()

if __name__ == '__main__':
    main()
```

### 4.2 테스트 실행 명령

```bash
cd /home/jetson/jupiter_ws_ros2
source install/setup.bash
python3 scripts/test_deadzone_bypass.py
```

### 4.3 테스트 결과 (수정 전)

```
============================================================
Part 1: 데드존 우회 테스트 (angular.z = 0.1 rad/s, 3초)
============================================================
Initial yaw: ...
Sending angular.z = 0.1 rad/s ...
Final yaw: ...
Yaw change: ...
>>> PASS: 로봇 회전됨 — 방안B 데드존 우회 작동

============================================================
Part 2: 중간 회전 테스트 (angular.z = 0.5 rad/s, 3초)
============================================================
(생략)

============================================================
Part 3: Nav2 Goal 테스트
============================================================
Current pose: x=0.010, y=0.535, yaw=109.9 deg

--- Goal1: 0.5m forward (0.010, 0.535, 109.9) ---
Goal1: SUCCEEDED in 2.7s (VSLAM adapted: 0)

--- Goal2: Return to (0.180, 0.065, 109.9) ---
Goal2: TIMEOUT in 61.1s (VSLAM adapted: 0)
  Yaw span during Goal2: 3.4 deg
  Final distance to start: 0.484m

============================================================
결과 요약
============================================================
Goal1: SUCCEEDED 2.7s | VSLAM: 0
Goal2: TIMEOUT 61.1s | VSLAM: 0 | yaw_span: 3.4°
>>> Goal2 실패했지만 yaw_span < 90° — 회전 통제됨 (이전 359°)
```

### 4.4 cmd_vel 모니터링 (Goal2 실행 중)

```bash
ros2 topic echo /cmd_vel
```

결과 (Goal2 실행 중 관찰된 패턴):
```yaml
---
linear:
  x: 0.0
  y: 0.0
  z: 0.0
angular:
  x: 0.0
  y: 0.0
  z: -0.052631578947368474
---
linear:
  x: 0.0
  y: 0.0
  z: 0.0
angular:
  x: 0.0
  y: 0.0
  z: -0.052631578947368474
---
# ... (같은 값 14회 반복) ...
---
linear:
  x: 0.0
  y: 0.0
  z: 0.0
angular:
  x: 0.0
  y: 0.0
  z: 0.05263157894736836
---
# ... (같은 값 21회 반복) ...
---
linear:
  x: 0.0
  y: 0.0
  z: 0.0
angular:
  x: 0.0
  y: 0.0
  z: -0.052631578947368474
---
# ... (같은 값 10회 반복, 이후 방향전환 반복) ...
```

**관찰 사항**:
- angular.z = ±0.052631578947368474 (**정확히 1/19**)
- linear.x = 0.0 (순수 제자리 회전 시도)
- 약 1.5초마다 부호 전환 (DWB가 방향을 바꿈)
- 로봇은 좌우로 움찔거릴 뿐 실제 회전 안 됨

---

## 5. Phase 2: velocity_smoother 토픽 흐름 분석

### 5.1 navigation_launch.py 분석

Nav2의 cmd_vel 토픽 흐름을 확인하기 위해 launch 파일을 분석.

```bash
cat /opt/ros/humble/share/nav2_bringup/launch/navigation_launch.py
```

핵심 부분 (controller_server → velocity_smoother → /cmd_vel):
```python
# navigation_launch.py (lines 114-120)
Node(
    package='nav2_controller',
    executable='controller_server',
    output='screen',
    respawn=use_respawn,
    respawn_delay=2.0,
    parameters=[configured_params],
    arguments=['--ros-args', '--log-level', log_level],
    remappings=remappings + [('cmd_vel', 'cmd_vel_nav')]),
    # ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    # controller_server의 cmd_vel → /cmd_vel_nav으로 remap

# navigation_launch.py (lines 175-184)
Node(
    package='nav2_velocity_smoother',
    executable='velocity_smoother',
    name='velocity_smoother',
    output='screen',
    respawn=use_respawn,
    respawn_delay=2.0,
    parameters=[configured_params],
    arguments=['--ros-args', '--log-level', log_level],
    remappings=remappings +
            [('cmd_vel', 'cmd_vel_nav'), ('cmd_vel_smoothed', 'cmd_vel')]),
    # ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    # 입력: cmd_vel → /cmd_vel_nav (controller_server 출력을 구독)
    # 출력: cmd_vel_smoothed → /cmd_vel (드라이버가 최종 구독)
```

**확인된 토픽 흐름**:
```
controller_server(DWB) → /cmd_vel_nav → velocity_smoother → /cmd_vel → jupiter_driver
```

### 5.2 velocity_smoother 설정 존재 여부 확인

```bash
grep -n "velocity_smoother" \
  /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
# 결과: (수정 전) — 일치하는 줄 없음!
```

**발견**: `nav2_params_fused.yaml`에 `velocity_smoother:` 섹션이 없었음
- velocity_smoother 노드는 `navigation_launch.py`에 의해 **반드시 실행됨** (lifecycle_nodes에 포함)
- 설정이 없으므로 **기본값**으로 실행: `max_accel: [2.5, 0.0, 3.2]`
- DWB `acc_lim_theta=0.8`과 불일치 (smoother는 3.2까지 허용)

### 5.3 lifecycle_nodes 확인

```bash
grep -n "lifecycle_nodes" /opt/ros/humble/share/nav2_bringup/launch/navigation_launch.py
```

결과:
```python
lifecycle_nodes = ['controller_server',
                   'smoother_server',
                   'planner_server',
                   'behavior_server',
                   'bt_navigator',
                   'waypoint_follower',
                   'velocity_smoother']
#                  ^^^^^^^^^^^^^^^^^^ — 필수 실행 노드
```

### 5.4 Foxglove와 test_deadzone_bypass.py 차이 분석

```bash
# DWB 샘플링 수학 확인
grep -n "vtheta_samples" \
  /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
# 결과:
# 137:      vtheta_samples: 20
```

```
DWB 최소 non-zero angular 샘플:
  max_vel_theta / (vtheta_samples - 1) = 1.0 / 19 = 0.052631... rad/s ✓

DWB 가속 제한에서 feasible 여부:
  acc_lim_theta / controller_frequency = 0.8 / 10 = 0.08 rad/s/cycle
  첫 샘플 0.0526 < 0.08 → feasible ✓
  둘째 샘플 0.1053 > 0.08 → infeasible ✗ (정지에서 1 cycle에 도달 불가)
  → DWB는 정지 상태에서 0 또는 ±0.0526만 출력 가능
```

```
Foxglove에서 정상인 이유:
  - 목표까지 경로가 충분히 김 (> forward_sampling_distance)
  - RotationShimController 정상 작동 → 0.8 rad/s 일관 방향 회전
  - DWB의 최소 샘플 문제가 발생하지 않음

test_deadzone_bypass.py Goal2에서 실패하는 이유:
  - Goal2 = 0.5m 복귀 (forward_sampling_distance=0.5와 같음)
  - RotationShim이 0.5m 앞을 보면 경로 끝 도달 → 방향 모호
  - DWB가 직접 처리 → ±0.0526 교대 → 물리 데드존 → 헌팅
```

---

## 6. Phase 2: 3중 수정 적용

### 6.1 수정 1: MIN_ANGULAR_DEADZONE 0.15 → 0.25

**파일**: `jupiter_driver_compensated.py`

변경 전 (Phase 1):
```python
# [방안B] 모터 물리적 데드존 우회: 최소 회전 속도 보장
# 방안A(펌웨어) 적용 후에도 ESC 서보 중립(108) 근처는 토크 미발생
# Nav2/velocity_smoother가 작은 angular 출력 시 ESC 물리 데드존에 빠짐
# ── 방안B 시작 ──
MIN_ANGULAR_DEADZONE = 0.15  # rad/s (scaled) — ESC 물리 데드존 탈출 최소값
if abs(angular_corrected) > 0.005 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
    angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
# ── 방안B 끝 ──
```

변경 후 (Phase 2 — 최종):
```python
# [방안B] 모터 물리적 데드존 우회: 최소 회전 속도 보장
# DWB acc_lim_theta에 의한 가속 제한으로 cmd_vel이 1/(vtheta_samples-1)=0.0526만 발생 가능
# 0.0526*0.353=0.019 → ESC≈108(중립) → 물리적 토크 미발생 → PID 윈드업 → 헌팅
# ── 방안B 시작 ──
MIN_ANGULAR_DEADZONE = 0.25  # rad/s (scaled) — DWB 최소샘플 0.0526 대응 (0.15에서 ESC 미작동 확인)
if abs(angular_corrected) > 0.005 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
    angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
# ── 방안B 끝 ──
```

### 6.2 수정 2: velocity_smoother 설정 신규 추가

**파일**: `nav2_params_fused.yaml` (파일 끝 — waypoint_follower 위에 추가)

추가된 전체 블록:
```yaml
# ==============================================================
# Velocity Smoother
# ==============================================================
# navigation_launch.py가 기본 생성하는 velocity_smoother 노드의 파라미터
# controller_server → /cmd_vel_nav → velocity_smoother → /cmd_vel → driver
# 이 설정 없으면 기본값(max_accel:[2.5,0,3.2]) 사용 — 로봇 특성과 불일치 [2026-03-18]
velocity_smoother:
  ros__parameters:
    use_sim_time: False
    smoothing_frequency: 20.0
    scale_velocities: false
    feedback: "OPEN_LOOP"
    max_velocity: [0.35, 0.0, 1.0]       # 로봇 최대 속도 (max_vel_x, 0, max_vel_theta)
    min_velocity: [-0.35, 0.0, -1.0]
    max_accel: [0.5, 0.0, 0.8]           # DWB acc_lim_x/theta와 일치
    max_decel: [-0.5, 0.0, -0.8]         # DWB decel_lim과 일치
    odom_topic: "odom"
    odom_duration: 0.1
    deadband_velocity: [0.0, 0.0, 0.0]   # 드라이버 방안B가 처리하므로 smoother 단에서는 0
    velocity_timeout: 1.0
```

### 6.3 수정 3: forward_sampling_distance 0.5 → 0.3

**파일**: `nav2_params_fused.yaml` RotationShimController 섹션

변경 전:
```yaml
forward_sampling_distance: 0.5
```

변경 후:
```yaml
forward_sampling_distance: 0.3     # ⭐ 0.5→0.3: 짧은 경로(0.5m 복귀)에서 RotationShim 활성화 보장 [2026-03-18]
```

### 6.4 RotationShimController 현재 설정 전체 (수정 후)

```yaml
    FollowPath:
      plugin: "nav2_rotation_shim_controller::RotationShimController"
      primary_controller: "dwb_core::DWBLocalPlanner"
      angular_dist_threshold: 0.785      # 45° — 경로 방향과 헤딩 차이가 이 이상이면 제자리 회전
      forward_sampling_distance: 0.3     # ⭐ 0.5→0.3: 짧은 경로(0.5m 복귀)에서 RotationShim 활성화 보장 [2026-03-18]
      rotate_to_heading_angular_vel: 0.8 # ⭐ 정지 회전 고정 속도
      max_angular_accel: 5.0             # ⭐ 1.6→5.0
      simulate_ahead_time: 1.0           # 충돌 검사 시뮬레이션 시간
```

---

## 7. Phase 2: 빌드 및 배포 검증

### 7.1 빌드

```bash
cd /home/jetson/jupiter_ws_ros2
source /opt/ros/humble/setup.bash
colcon build --packages-select jupiter_bringup jupiter_nav_VO --symlink-install
```

결과:
```
Starting >>> jupiter_bringup
Finished <<< jupiter_bringup [4.13s]
Starting >>> jupiter_nav_VO
Finished <<< jupiter_nav_VO [3.88s]
Summary: 2 packages finished [8.43s]
```

### 7.2 방안B 값 검증 (0.25)

```bash
grep -n "MIN_ANGULAR_DEADZONE" \
  /home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```

결과:
```
236:            MIN_ANGULAR_DEADZONE = 0.25  # rad/s (scaled) — DWB 최소샘플 0.0526 대응 (0.15에서 ESC 미작동 확인)
237:            if abs(angular_corrected) > 0.005 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
238:                angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
```

### 7.3 velocity_smoother 설정 검증

```bash
grep -n "velocity_smoother:" \
  /home/jetson/jupiter_ws_ros2/install/jupiter_nav_VO/share/jupiter_nav_VO/config/nav2_params_fused.yaml
```

결과:
```
294:velocity_smoother:
```

### 7.4 forward_sampling_distance 검증

```bash
grep -n "forward_sampling_distance" \
  /home/jetson/jupiter_ws_ros2/install/jupiter_nav_VO/share/jupiter_nav_VO/config/nav2_params_fused.yaml
```

결과:
```
113:      forward_sampling_distance: 0.3     # ⭐ 0.5→0.3: 짧은 경로(0.5m 복귀)에서 RotationShim 활성화 보장 [2026-03-18]
```

---

## 8. Phase 3: 무한회전 진단 및 방안B 감속 감지 수정

### 8.1 증상

3중 수정 적용 후 Nav2 주행 테스트에서 회전을 시작하면 **멈추지 못하고 무한히 회전**.

### 8.2 원인 분석

**방안B (MIN_ANGULAR_DEADZONE=0.25)가 velocity_smoother 감속을 차단:**

```
방안B 부스트 범위:
  scaled: 0.005 ~ 0.25
  raw cmd_vel 기준: 0.014 ~ 0.708 (÷ ANGULAR_SCALE_FACTOR 0.353)

velocity_smoother 감속 프로파일 (0.8→0.0, max_decel=-0.8, 20Hz):
  raw:     0.80  0.76  0.72  ...  0.60  0.40  0.20  0.08  0.00
  scaled:  0.282 0.268 0.254 ...  0.212 0.141 0.071 0.028 0.000
  방안B:    NO    NO    NO   ...  BOOST BOOST BOOST BOOST  NO
  MCU:     0.282 0.268 0.254 ...  0.25  0.25  0.25  0.25  0.00
                                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                          ~0.87초간 감속 대신 0.25 유지 → 오버슈트 → 무한회전
```

**무한회전 사이클:**
1. RotationShim: +0.8 → smoother → driver (0.282) → MCU → 로봇 회전
2. 목표 헤딩 도달 → RotationShim OFF → DWB 전환 → smoother 감속 시작
3. 감속 중: raw 0.7~0.04 → scaled 0.247~0.014 → 방안B가 0.25로 부스트
4. ~0.87초간 0.25 유지 → 로봇 계속 회전 → 목표 헤딩 오버슈트
5. 오버슈트 >45° → RotationShim 재활성화 → +0.8 → Step 1로 복귀 → **무한 루프**

### 8.3 수정: 감속 감지 기반 방안B

**원리**: angular 명령 크기가 이전 값보다 0.01 이상 감소 = 감속 → 부스트 금지

**`__init__` 추가:**
```python
# [방안B] 감속 감지용 이전 angular 명령 저장
self._prev_angular_cmd = 0.0
```

**`cmd_vel_callback` 방안B 블록 (최종):**
```python
# [방안B] 모터 물리적 데드존 우회: 최소 회전 속도 보장
# DWB 최소 non-zero 샘플 0.0526 → ×0.353=0.019 → ESC 미작동 → 부스트 필요
# 단, velocity_smoother 감속 중(0.8→0)에는 중간값이 스케일 후 0.25 미만이 되어
# 부스트되면 감속 불가→오버슈트→RotationShim 재활성화→무한회전 유발
# 따라서 angular 크기가 감소 중(감속)이면 부스트 금지
# ── 방안B 시작 ──
MIN_ANGULAR_DEADZONE = 0.25
is_decelerating = abs(angular) < abs(self._prev_angular_cmd) - 0.01
self._prev_angular_cmd = angular

if not is_decelerating:
    if abs(angular_corrected) > 0.005 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
        angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
# ── 방안B 끝 ──
```

### 8.4 빌드

```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_bringup --symlink-install
```

결과:
```
Starting >>> jupiter_bringup
Finished <<< jupiter_bringup [4.14s]
Summary: 1 package finished [4.53s]
```

### 8.5 빌드 검증

```bash
grep -n "is_decelerating\|_prev_angular_cmd" \
  /home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```

결과:
```
182:        self._prev_angular_cmd = 0.0
246:            is_decelerating = abs(angular) < abs(self._prev_angular_cmd) - 0.01
247:            self._prev_angular_cmd = angular
249:            if not is_decelerating:
```

---

## 9. 재시작 및 테스트 명령어

### 9.1 시스템 재시작

```bash
# 터미널 1: 센서 퓨전 + VSLAM
source /home/jetson/jupiter_ws_ros2/install/setup.bash
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py

# 터미널 2: Nav2 네비게이션 (velocity_smoother 포함)
source /home/jetson/jupiter_ws_ros2/install/setup.bash
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```

### 9.2 velocity_smoother 파라미터 적용 확인

```bash
ros2 param get /velocity_smoother max_accel
# 기대값: [0.5, 0.0, 0.8]

ros2 param get /velocity_smoother max_velocity
# 기대값: [0.35, 0.0, 1.0]
```

### 9.3 토픽 흐름 확인

```bash
# controller_server → cmd_vel_nav 확인
ros2 topic echo /cmd_vel_nav --field angular.z

# velocity_smoother → cmd_vel 확인
ros2 topic echo /cmd_vel --field angular.z
```

### 9.4 Nav2 Goal 테스트 (감속 감지 검증)

```bash
cd /home/jetson/jupiter_ws_ros2
python3 scripts/test_deadzone_bypass.py
```

기대 결과:
- Goal1: SUCCEEDED (0.5m 전진)
- Goal2: SUCCEEDED (0.5m 복귀) — forward_sampling=0.3에 의해 RotationShim 활성화

### 9.5 수동 cmd_vel 모니터링

```bash
# Goal2 실행 중 angular.z 관찰
ros2 topic echo /cmd_vel --field angular.z

# cmd_vel_nav (smoother 전) vs cmd_vel (smoother 후) 비교
ros2 topic echo /cmd_vel_nav --field angular.z &
ros2 topic echo /cmd_vel --field angular.z
```
