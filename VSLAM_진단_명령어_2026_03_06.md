# VSLAM 진단 및 수정 — 실행 명령어 전체 기록 (2026-03-06)

> 5차 테스트에서 VSLAM 커버리지가 43% (184초 세션 중 80초만 동작)로 확인되어,
> cuVSLAM 추적 소실의 원인을 조사하고 수정한 과정의 모든 명령어를 기록함.

---

## Phase 1: VSLAM 상태 확인 (호스트에서)

### 명령 1: VSLAM 관련 토픽 목록 확인
```bash
ros2 topic list | grep visual_slam
```
- **목적**: cuVSLAM이 발행하는 토픽들이 ROS2 네트워크에 등록되어 있는지 확인
- **결과**:
  ```
  /visual_slam/tracking/odometry
  /visual_slam/tracking/odometry_adapted
  /visual_slam/status
  ...
  ```
  → 토픽 자체는 등록되어 있음 (노드가 한 번은 실행되었다는 의미)

### 명령 2: VSLAM odometry 발행 주파수 확인
```bash
ros2 topic hz /visual_slam/tracking/odometry --window 10
```
- **목적**: cuVSLAM이 실시간으로 odometry를 발행하고 있는지, 발행 주파수 확인
- **결과**: 출력 없음 (데이터 수신 불가) → VSLAM 중단 상태

### 명령 3: VSLAM odometry 메시지 단일 수신 시도
```bash
timeout 5 ros2 topic echo /visual_slam/tracking/odometry --once
```
- **목적**: 5초 내에 한 건이라도 VSLAM 데이터가 오는지 확인
- **결과**: EXIT=124 (타임아웃, 데이터 없음) → VSLAM 완전 중단 확인

### 명령 4: 호스트에서 VSLAM/카메라 노드 검색
```bash
ros2 node list | grep -E "visual_slam|camera"
```
- **목적**: visual_slam_node, camera/camera 노드가 호스트 ROS2 그래프에 보이는지 확인
- **결과**: `/base_to_camera_tf`와 `/vslam_covariance_adapter`만 보임
  → Docker 내 VSLAM 노드가 호스트에서 DDS로 발견되지 않음

### 명령 5: 전체 노드 목록 확인
```bash
ros2 node list
```
- **목적**: 호스트에 등록된 모든 ROS2 노드 확인
- **결과**: 19개 노드 (EKF, SLAM toolbox, RF2O 등 호스트측 노드만 보임)
  → Docker 내 visual_slam_node, camera/camera 미표시

---

## Phase 2: Docker 컨테이너 상태 확인

### 명령 6: Docker 컨테이너 실행 상태 확인
```bash
docker ps
```
- **목적**: isaac_ros_dev 컨테이너가 실행 중인지 확인
- **결과**: `isaac_ros_dev-aarch64-container Up 5 hours` → 컨테이너 정상 실행 중

### 명령 7: Docker 내 노드 목록 (setup.bash 없이)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c 'ros2 node list'
```
- **목적**: Docker 내 ROS2 노드 목록 확인 (빠른 시도)
- **결과**: setup.bash 미소싱으로 일부 노드만 표시 → 정확한 결과 아님

### 명령 8: Docker 내 노드 목록 (setup.bash 소싱)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 node list'
```
- **목적**: Docker 내 정확한 노드 목록 확인
- **결과**:
  ```
  /visual_slam_node          ← cuVSLAM 노드 ALIVE
  /camera/camera             ← RealSense 노드 ALIVE
  /imu_tf_publisher          ← IMU TF 발행 노드 ALIVE
  /visual_slam_launch_container
  /launch_ros_224562
  ```
  → **노드들은 살아있지만 데이터를 발행하지 않는 상태**

---

## Phase 3: 카메라 스트림 개별 확인

### 명령 9: VSLAM odometry (Docker 내에서)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 3 ros2 topic echo /visual_slam/tracking/odometry --once'
```
- **목적**: Docker 내에서 직접 VSLAM odometry 확인 (DDS 문제 배제)
- **결과**: EXIT=124 → Docker 내부에서도 데이터 없음

### 명령 10: VSLAM status (Docker 내에서)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 3 ros2 topic echo /visual_slam/status --once'
```
- **목적**: cuVSLAM 추적 상태(vo_state) 확인
- **결과**: EXIT=124 → status도 발행되지 않음

### 명령 11: 카메라 IR 이미지 스트림 (Docker 내에서)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 3 ros2 topic echo /camera/infra1/camera_info --once'
```
- **목적**: RealSense IR 카메라가 이미지를 발행하는지 확인
- **결과**: EXIT=124 → **카메라 스트림 자체가 중단됨**

### 명령 12: 카메라 IMU 스트림 (Docker 내에서)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 3 ros2 topic echo /camera/imu --once'
```
- **목적**: RealSense IMU 데이터 발행 확인
- **결과**: EXIT=124 → IMU도 중단
- **종합 진단**: 카메라 노드 alive but USB 파이프라인 hang → 모든 스트림 중단

---

## Phase 4: 하드웨어 및 커널 진단

### 명령 13: USB 디바이스 확인
```bash
lsusb | grep -i "intel\|realsense"
```
- **목적**: RealSense D455F가 USB 버스에 물리적으로 인식되는지 확인
- **결과**: `Bus 002 Device 003: ID 8086:0b5c Intel Corp. Intel(R) RealSense(TM) Depth Camera 455f`
  → 물리적 연결 정상 (USB 3.2)

### 명령 14: Docker 컨테이너 로그 분석
```bash
docker logs --tail 100 isaac_ros_dev-aarch64-container
```
- **목적**: cuVSLAM 초기화 로그, 에러 메시지, frame jitter 경고 확인
- **결과**:
  - cuVSLAM 정상 초기화: `cuVSLAM v12.6`, `CUVSLAM_CreateTracker(): 4.56s`, `tracker was successfully initialized`
  - **24건 frame jitter 경고**: `Delta between current and previous frame [XX ms] is above threshold [22.000000 ms]`
    - 시작 5초 이내에 24건 밀집 발생
    - 이후 t=263, 343, 388에서 3건 산발
  - crash/segfault 없음
  - **핵심**: 카메라 프레임 전달이 USB 병목으로 불안정 → hang

### 명령 15: 커널 USB 에러 로그 확인
```bash
sudo dmesg | grep -iE "usb|uvc"
```
- **목적**: USB disconnect, firmware crash, UVC 드라이버 에러 확인
- **결과**: 정상 (부팅 시 인식 로그만 있고, disconnect/error 없음)
  - `uvcvideo 2-1.4-3:1.5: Unknown video format` (RealSense 정상 동작, 무시 가능)

---

## Phase 5: 노드 상세 정보 확인

### 명령 16: visual_slam_node 상세 정보
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 node info /visual_slam_node'
```
- **목적**: cuVSLAM 노드의 구독/발행 토픽 목록 확인
- **결과**:
  - Subscribers: `camera/infra1/image_rect_raw`, `camera/infra2/image_rect_raw`, `camera/imu`
  - Publishers: `visual_slam/tracking/odometry`, `visual_slam/status` 등
  → 토픽 구조 정상, 카메라 데이터 미수신이 원인

### 명령 17: camera/camera 노드 상세 정보
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 node info /camera/camera'
```
- **목적**: RealSense 카메라 노드의 발행 토픽 확인
- **결과**: 모든 토픽(infra1, infra2, imu 등) 등록되어 있으나 실제 데이터 없음
  → **노드 alive, USB 스트리밍 hang 확인**

---

## Phase 6: 런치 파일 분석 및 수정

### 명령 18: 런치 파일 읽기
```bash
# 로컬 파일 시스템에서 읽기 (Docker 마운트 경로)
cat /home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_visual_slam_realsense.launch.py
```
- **목적**: 현재 카메라 FPS, jitter 임계값 등 파라미터 확인
- **결과**: `depth_module.profile: '640x360x90'`, `image_jitter_threshold_ms: 22.00`
  → 90fps가 Jetson USB 3.2에서 대역폭 병목 원인으로 지목

### 수정 19: 런치 파일 파라미터 변경
```python
# 수정 전:
'depth_module.profile': '640x360x90',
'image_jitter_threshold_ms': 22.00,

# 수정 후:
'depth_module.profile': '640x360x60',  # ⭐ 90→60fps: USB 대역폭 33% 감소
'image_jitter_threshold_ms': 34.00,    # ⭐ 22→34ms: 60fps(16.7ms)의 2x 임계값
```
- **목적**: USB 대역폭 압박 해소로 스트리밍 hang 방지
- **근거**: 90fps → USB ~41.5 MB/s 지속 전송 → Jetson USB 3.2에서 hang 유발

---

## Phase 7: Docker 내 빌드 및 프로세스 관리

### 명령 20: Docker 내 colcon 빌드
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && cd /workspaces/isaac_ros-dev && \
   colcon build --packages-select isaac_ros_visual_slam --symlink-install'
```
- **목적**: 수정된 런치 파일을 Docker 내 install 디렉토리에 반영
- **결과**: 빌드 성공

### 명령 21: Docker 내 기존 VSLAM 프로세스 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'ps aux | grep -E "visual_slam|realsense|launch" | grep -v grep'
```
- **목적**: 기존에 실행 중인 VSLAM/카메라 프로세스 PID 확인
- **결과**: PID 224562 (launch), 224574 (component_container), 224576 (realsense2_camera_node)

### 명령 22: 기존 VSLAM 프로세스 종료
```bash
docker exec isaac_ros_dev-aarch64-container kill -9 224562 224574 224576
```
- **목적**: 카메라를 점유 중인 이전 프로세스 강제 종료
- **결과**: 프로세스 종료 확인 (ps aux로 재확인)
- **⚠️ 부작용**: SIGKILL(-9)로 종료 시 USB 디바이스 핸들이 정상 해제되지 않아,
  이후 새 프로세스가 카메라를 열 때 "failed to set power state" 에러 발생 가능

### 명령 23: RealSense USB 디바이스 리셋 (1차)
```bash
sudo python3 /home/jetson/reset_realsense.py
```
- **목적**: USB 디바이스 레벨에서 RealSense 초기화 (USBDEVFS_RESET ioctl)
- **결과**: `Successfully reset /dev/bus/usb/002/003`

---

## Phase 8: 1차 VSLAM 재시작 (DDS 기본 설정)

### 명령 24: VSLAM 재시작 (백그라운드)
```bash
docker exec -d isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py \
   > /tmp/vslam_launch.log 2>&1'
```
- **목적**: 수정된 60fps 설정으로 cuVSLAM 재시작
- **결과**: 시작 성공

### 명령 25: 시작 로그 확인
```bash
docker exec isaac_ros_dev-aarch64-container cat /tmp/vslam_launch.log | tail -40
```
- **목적**: 60fps 적용 확인, jitter 경고 수 확인
- **결과**:
  - `Open profile: stream_type: Infra(1), Format: Y8, Width: 640, Height: 360, FPS: 60` ← 60fps 적용 확인
  - `RealSense Node Is Up!`
  - `cuVSLAM tracker was successfully initialized.`
  - jitter 경고: **4건** (시작 직후만, 기존 90fps에서 24건 대비 대폭 감소)

### 명령 26: Docker 내 VSLAM 60Hz 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 5 ros2 topic echo /visual_slam/tracking/odometry --once --no-arr'
```
- **목적**: Docker 내에서 VSLAM odometry 데이터 확인
- **결과**: **정상 수신** — position/orientation/twist 모두 유효
  → 60fps 설정으로 VSLAM 안정 동작 확인

### 명령 27: VSLAM 발행 주파수 확인 (Docker 내)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 8 ros2 topic hz /visual_slam/tracking/odometry'
```
- **목적**: VSLAM 발행 주파수 정밀 측정
- **결과**: `average rate: 59.862` → **~60Hz 안정**

---

## Phase 9: 호스트↔Docker DDS 통신 검증

> **⚠️ 오진 정정**: Phase 9~10에서 DDS 통신 실패를 SHM 문제로 진단했으나,
> 실제 원인은 Phase 7에서 `kill -9`로 DDS participant를 강제 종료하여
> SHM 잠금 파일이 해제되지 않고 DDS discovery 캐시에 stale entry가 남아서 발생한 것.
> 정상 시작/종료(Ctrl+C) 시에는 `FASTRTPS_DEFAULT_PROFILES_FILE` 없이도 Docker↔Host 통신 정상 동작.

### 명령 28: 호스트에서 VSLAM 데이터 수신 시도
```bash
timeout 8 ros2 topic echo /visual_slam/tracking/odometry --once --no-arr
```
- **목적**: Docker에서 발행하는 VSLAM 데이터가 호스트에서 수신되는지 확인
- **결과**: EXIT=124 (타임아웃) → 호스트에서 수신 불가!

### 명령 29: DDS 통신 기본 테스트
```bash
docker exec -d isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && \
   ros2 topic pub /dds_test std_msgs/msg/String "data: hello_from_docker" --rate 10' \
  && sleep 2 && timeout 5 ros2 topic echo /dds_test --once
```
- **목적**: Docker→Host DDS 통신 자체가 동작하는지 간단한 테스트
- **결과**: EXIT=124 → **DDS 통신 자체가 불통**

### 명령 30: Docker 네트워크/IPC 모드 확인
```bash
docker inspect isaac_ros_dev-aarch64-container --format '{{.HostConfig.NetworkMode}}'
docker inspect isaac_ros_dev-aarch64-container --format '{{.HostConfig.IpcMode}}'
docker inspect isaac_ros_dev-aarch64-container --format '{{.HostConfig.PidMode}}'
```
- **목적**: Docker 컨테이너가 host 네트워크/IPC를 사용하는지 확인
- **결과**: 모두 `host` → 네트워크 격리 문제 아님

### 명령 31: ROS_DOMAIN_ID 및 DDS 미들웨어 확인
```bash
echo "Host: ROS_DOMAIN_ID=$ROS_DOMAIN_ID"
docker exec isaac_ros_dev-aarch64-container bash -c 'echo "Docker: ROS_DOMAIN_ID=$ROS_DOMAIN_ID"'
echo "Host DDS: $RMW_IMPLEMENTATION"
docker exec isaac_ros_dev-aarch64-container bash -c 'echo "Docker DDS: $RMW_IMPLEMENTATION"'
```
- **목적**: Host/Docker 간 DOMAIN_ID와 DDS 구현체 일치 확인
- **결과**: 둘 다 `ROS_DOMAIN_ID=0`, `RMW_IMPLEMENTATION=rmw_fastrtps_cpp` → 일치

### 명령 32: FastRTPS SHM 프로필 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'echo FASTRTPS=$FASTRTPS_DEFAULT_PROFILES_FILE && \
   cat /usr/local/share/middleware_profiles/rtps_udp_profile.xml'
```
- **목적**: FastRTPS의 전송 방식 설정 확인
- **결과**: `FASTRTPS_DEFAULT_PROFILES_FILE` 미설정 → SHM(공유메모리) 기본 사용
- **❌ 오진**: SHM이 실패 원인이라고 판단했으나, 실제로는 `kill -9`로 인한 DDS stale state가 원인

---

## Phase 10: 2차 VSLAM 재시작

> **❌ 불필요한 변경**: UDP 전용 프로필 지정은 불필요했음. 정상 시작 시 SHM+UDP 혼합으로 동작 확인됨.

### 명령 33: 1차 VSLAM 프로세스 종료
```bash
docker exec isaac_ros_dev-aarch64-container kill -9 <PIDs>
```
- **목적**: DDS SHM 모드로 시작된 1차 VSLAM 프로세스 종료

### 명령 34: RealSense USB 리셋 (2차)
```bash
sudo python3 /home/jetson/reset_realsense.py
```
- **목적**: 이전 프로세스 강제 종료 후 USB 디바이스 상태 초기화
- **결과**: `Successfully reset /dev/bus/usb/002/003`

### 명령 35: VSLAM 재시작 (UDP 전용 FastRTPS)
```bash
docker exec -d isaac_ros_dev-aarch64-container bash -c \
  'export FASTRTPS_DEFAULT_PROFILES_FILE=/usr/local/share/middleware_profiles/rtps_udp_profile.xml && \
   source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py \
   > /tmp/vslam_launch2.log 2>&1'
```
- **목적**: DDS 통신 문제 해결을 위해 UDP 전용으로 시도
- **❌ 오진**: `FASTRTPS_DEFAULT_PROFILES_FILE`는 실제로는 불필요함.
  DDS 통신 실패는 `kill -9`로 인한 stale SHM 잠금 파일이 원인이었고,
  정상 종료(Ctrl+C) 후 재시작하면 기본 SHM+UDP 혼합으로 정상 동작함.
- **결과**: 시작 성공 (UDP 전용이든 기본이든 동일하게 동작)

### 명령 36: 시작 로그 확인 (2차)
```bash
docker exec isaac_ros_dev-aarch64-container cat /tmp/vslam_launch2.log | tail -20
```
- **결과**:
  - `Infra(1), Format: Y8, Width: 640, Height: 360, FPS: 60` ← 60fps 확인
  - `cuVSLAM tracker was successfully initialized.`
  - jitter 경고 **4건** (108ms, 38ms, 38ms, 39ms — 시작 직후만)
  - `Motion Module force pause` 경고 1건 (RealSense FW 내부, 무해)

### 명령 37: 호스트에서 VSLAM 수신 확인
```bash
timeout 8 ros2 topic echo /visual_slam/tracking/odometry --once --no-arr
```
- **목적**: Docker→Host VSLAM 데이터 수신 확인
- **결과**: **정상 수신!**
  ```
  [RTPS_TRANSPORT_SHM Error] Failed init_port fastrtps_port7411  ← SHM 경고 (무해, UDP fallback 동작)
  pose:
    position: x=-0.043, y=-0.008, z=0.004
    orientation: w=0.9999...
  ```
  → Docker→Host DDS 통신 OK
- **참고**: 이후 `FASTRTPS_DEFAULT_PROFILES_FILE` 없이 정상 시작한 경우에도 동일하게 수신 확인됨

### 명령 38: VSLAM 발행 주파수 확인 (호스트에서)
```bash
timeout 5 ros2 topic echo /visual_slam/tracking/odometry --field header.stamp | head -20
```
- **목적**: 호스트에서 수신되는 VSLAM 메시지의 타임스탬프 간격 확인
- **결과**: ~16.7ms 간격 → **60Hz** 안정 수신

### 명령 39: 카메라 IR 이미지 주파수 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 5 ros2 topic hz /camera/infra1/image_rect_raw'
```
- **목적**: 카메라 IR 스트림 안정성 확인
- **결과**: `average rate: 59.855` → 60Hz 안정

### 명령 40: jitter 경고 총 횟수 확인
```bash
docker exec isaac_ros_dev-aarch64-container grep -c "above threshold" /tmp/vslam_launch2.log
```
- **목적**: 10분+ 가동 후 총 jitter 경고 수 비교
- **결과**:
  | 설정 | 시작 시 | 운영 중 | 총 |
  |------|---------|---------|-----|
  | 90fps, 22ms | 24건 | 3건+ | 27건+ |
  | 60fps, 34ms | 4건 | **0건** | **4건** |

---

## Phase 11: 카메라 에러 발생 및 원인 분석

### 증상: "failed to set power state" 에러
```
[WARN] [camera.camera]: Device 1/1 failed with exception: failed to set power state
[ERROR] [camera.camera]: The requested device with  is NOT found. Will Try again.
```
- **발생 시점**: 사용자가 Docker 내에서 수동으로 VSLAM 재시작 시도 시
- **원인**: 명령 35에서 `docker exec -d`로 시작한 VSLAM 프로세스(PID 283125, 283173, 283194)가
  **백그라운드에서 아직 실행 중**이면서 RealSense USB 디바이스를 점유 중.
  두 번째 realsense2_camera_node가 같은 카메라 디바이스에 접근하려 하면 USB 전원 설정 실패.

### 해결: 잔류 프로세스 종료 후 USB 리셋
```bash
# 잔류 프로세스 확인
docker exec isaac_ros_dev-aarch64-container ps aux | grep -E "visual_slam|realsense" | grep -v grep

# 잔류 프로세스 종료
docker exec isaac_ros_dev-aarch64-container kill <PIDs>

# USB 디바이스 리셋
sudo python3 /home/jetson/reset_realsense.py
```

---

## 수정 요약

| 항목 | 수정 전 | 수정 후 |
|------|---------|---------|
| `depth_module.profile` | `640x360x90` | `640x360x60` |
| `image_jitter_threshold_ms` | `22.00` | `34.00` |

**파일 위치**: `isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_visual_slam_realsense.launch.py`

---

## 근본 원인 분석

```
RealSense D455F: 640×360 @ 90fps (Stereo IR × 2채널)
  → USB 3.2 지속 전송: ~41.5 MB/s (640×360×1B × 2ch × 90fps)
  → Jetson Orin USB 3.2 컨트롤러 대역폭 한계
  → 프레임 전달 지터 증가 (22ms 임계값 초과 24건)
  → 카메라 드라이버 내부 큐 포화
  → USB 스트리밍 파이프라인 hang (노드 alive, 데이터 0)
  → cuVSLAM 추적 소실 (세션의 57%)
```

**60fps 전환 후**: USB 대역폭 ~27.6 MB/s (-33%), jitter 경고 93% 감소, 10분+ 안정 가동
