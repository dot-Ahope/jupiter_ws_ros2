# 명령어 로그 2026-04-24: nvblox people detection 전환 + Nav2 공존 튜닝

> 진단 및 수정에 사용한 명령어와 스크립트/설정 파일을 요약 없이 전부 기록한다.

---

## 0. 오늘 사용한 터미널 명령어 원문

### 0.1 오늘 날짜 / 기존 문서 확인

```bash
ls -la /home/jetson/jupiter_ws_ros2/진단_명령어_2026_04_24.md /home/jetson/jupiter_ws_ros2/진행과정_2026_04_24.md 2>&1; date +%Y_%m_%d
```
목적: 오늘 자 진단/진행 문서의 존재 확인 및 날짜 획득.
결과: 두 파일 모두 부재. 오늘 날짜 `2026_04_24`.

### 0.2 공식 nvblox 예제 파일 위치 탐색

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
echo "== 공식 nvblox config 위치 =="
ls /opt/ros/humble/share/nvblox_ros/config/ 2>&1
echo
echo "== 공식 specializations =="
ls /opt/ros/humble/share/nvblox_ros/config/specializations/ 2>&1
echo
echo "== nvblox_examples_bringup 공식 launch / config =="
find /opt/ros/humble/share/nvblox_examples_bringup -type f \( -name "*.launch.py" -o -name "*.yaml" -o -name "*.rviz" \) 2>&1
'
```
목적: `/opt/ros/humble/share` 아래 공식 파일 탐색.
결과: 해당 경로 부재 (apt 설치 대신 `/workspaces/isaac_ros-dev/install/` 사용).

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
find / -path /proc -prune -o -type d \( -name "nvblox_ros" -o -name "nvblox_examples_bringup" -o -name "nvblox_examples" \) -print 2>/dev/null | head -40
'
```
목적: 컨테이너 전체에서 nvblox 패키지 루트 탐색.
결과 토막:
```
/workspaces/isaac_ros-dev/install/nvblox_ros
/workspaces/isaac_ros-dev/install/nvblox_examples_bringup
/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup
```

### 0.3 공식 config / launch 파일 열람

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
find /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup -type f \( -name "*.py" -o -name "*.yaml" -o -name "*.rviz" \) 2>&1
'
```
목적: 공식 예제 패키지 파일 전수 목록.
결과: `config/nvblox/nvblox_base.yaml`, `specializations/nvblox_segmentation.yaml`, `nvblox_detection.yaml`, `nvblox_realsense.yaml`, `launch/perception/{segmentation,detection,nvblox}.launch.py`, `launch/realsense_example.launch.py` 모두 확인.

### 0.4 ShuffleSeg → Vanilla → PeopleNet 모델 파일 존재 확인

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
echo "== isaac_ros_assets models/peoplesemsegnet 디렉토리 =="
ls -la /workspaces/isaac_ros-dev/isaac_ros_assets/models/peoplesemsegnet/ 2>&1
echo
echo "== Vanilla 모델 파일 =="
find /workspaces/isaac_ros-dev/isaac_ros_assets/models/peoplesemsegnet -type f \( -name "*.plan" -o -name "*.onnx" -o -name "*.etlt" \) 2>&1
echo
echo "== ISAAC_ROS_WS =="
echo "ISAAC_ROS_WS=$ISAAC_ROS_WS"
'
```
목적: PeopleSemSegNet Vanilla TRT engine 확인.
결과:
```
/workspaces/isaac_ros-dev/isaac_ros_assets/models/peoplesemsegnet/
  deployable_quantized_vanilla_unet_onnx_v2.0/1/model.plan
  optimized_deployable_shuffleseg_unet_amr_v1.0/1/model.plan
ISAAC_ROS_WS=/workspaces/isaac_ros-dev
```

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
echo "== peoplenet 모델 디렉토리 =="
ls -la /workspaces/isaac_ros-dev/isaac_ros_assets/models/peoplenet/ 2>&1
echo
echo "== rsu_rs_480_640_mask 내부 =="
find /workspaces/isaac_ros-dev/isaac_ros_assets/models/peoplenet -type f 2>&1
echo
echo "== PEOPLENET 상수 =="
grep -n "PEOPLENET_INPUT" /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_ros_python_utils/nvblox_ros_python_utils/nvblox_constants.py 2>&1
echo
echo "== SEMSEGNET 상수 =="
grep -n "SEMSEGNET_INPUT" /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_ros_python_utils/nvblox_ros_python_utils/nvblox_constants.py 2>&1
'
```
목적: PeopleNet DetectNet_v2 TRT engine 존재 확인 및 입력 해상도 상수 추출.
결과:
```
/workspaces/isaac_ros-dev/isaac_ros_assets/models/peoplenet/
  rsu_rs_480_640_mask/1/model.plan  (존재 확인)
PEOPLENET_INPUT_IMAGE_WIDTH = 480
PEOPLENET_INPUT_IMAGE_HEIGHT = 640
SEMSEGNET_INPUT_IMAGE_WIDTH = 960
SEMSEGNET_INPUT_IMAGE_HEIGHT = 544
```

### 0.5 URDF base_link 수직 오프셋 확인

```bash
grep -nE 'base_link|base_footprint|link name|joint|origin xyz|camera_link' /home/jetson/jupiter_ws_ros2/src/jupiter_description/urdf/jupiter_simple.urdf 2>&1 | head -40
```
목적: `base_footprint → base_link` 높이 오프셋 (nvblox slice height 계산의 기준).
결과 토막:
```
37:  <joint name="base_joint" type="fixed">
38:    <parent link="base_footprint"/>
39:    <child link="base_link"/>
40:    <origin xyz="0.0 0.0 0.08" rpy="0 0 0"/>  ← base_link 는 base_footprint 위 8cm
```

### 0.6 Nav2 costmap plugin / 로봇 치수 확인

```bash
grep -nE 'nvblox|plugin|obstacle_layer|static_layer|inflation_layer|voxel_layer|observation|combined_occupancy|dynamic_occupancy|local_costmap|global_costmap' /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml 2>&1 | head -60
```
목적: local/global costmap 에 연결된 layer 구성과 nvblox slice 구독 경로 파악.
결과 토막:
```
221:      plugins: ["obstacle_layer", "nvblox_layer", "inflation_layer"]
241:      nvblox_layer:
242:        plugin: "nvblox::nav2::NvbloxCostmapLayer"
245:        nvblox_map_slice_topic: "/nvblox_node/combined_map_slice"
279:      plugins: ["static_layer", "nvblox_layer", "inflation_layer"]
```

```bash
grep -nE "robot_radius|footprint|inflation_radius|cost_scaling_factor|lethal_cost|max_obstacle|NvbloxCostmapLayer|inscribed" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml 2>&1
```
목적: 실제 inflation 파라미터와 footprint 확인.
결과 토막:
```
220: footprint: "[ [0.18, 0.1225], [0.18, -0.1225], [-0.18, -0.1225], [-0.18, 0.1225] ]"
247: max_obstacle_distance: 1.0
251: cost_scaling_factor: 5.0
252: inflation_radius: 0.30
# + nvblox_layer 에 inflation_distance: 0.5  ← 이중 inflation 의 주 원인 발견
```

### 0.7 URDF 바퀴/링크 치수 확인

```bash
grep -nE "origin xyz|geometry|box size|cylinder radius|radius|length" /home/jetson/jupiter_ws_ros2/src/jupiter_description/urdf/jupiter_simple.urdf | head -30
```
목적: 로봇 본체 박스 / 바퀴 반경 / 바퀴 간격.
결과 토막:
```
box size="0.20 0.15 0.08"   ← base_link 본체 (0.20m x 0.15m x 0.08m)
cylinder radius="0.035"      ← 바퀴 반경
origin xyz="0 0.0855 0"      ← 좌측 바퀴 위치
origin xyz="0 -0.0855 0"     ← 우측 바퀴 위치 (바퀴 중심 간격 0.171m)
```

### 0.8 공식 vs 우리 rate / bounds / slice 파라미터 grep

```bash
grep -nE 'voxel_size|tick_period_ms|integrate_depth_rate|integrate_color_rate|update_esdf_rate|publish_layer_rate|decay_tsdf_rate|decay_dynamic|maximum_input_queue|connected_mask_component|workspace_bounds_type|workspace_bounds_max_height_m|workspace_bounds_min_height_m|map_clearing_radius|map_clearing_frame_id' /home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml
```
목적: 우리 nvblox 설정 현재값 전수 확인 (공식 기본값과 차분 대조용).
결과 요지: `voxel_size 0.08`, `update_esdf_rate_hz 5→10`, `maximum_input_queue_length 4→8`, `workspace_bounds_min_height_m -0.2` (바닥 포함) 등 여러 항목 차분 확인.

### 0.9 yaml / launch / rviz 최종 snapshot 검증

```bash
grep -nE 'esdf_slice_height|esdf_slice_min_height|esdf_slice_max_height|workspace_bounds_min_height_m|workspace_bounds_max_height_m' /home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml
```
목적: 최종 z-범위 설정 검증.
결과:
```
workspace_bounds_min_height_m: -0.2
workspace_bounds_max_height_m: 2.5
static_mapper.esdf_slice_min_height: 0.10
static_mapper.esdf_slice_max_height: 1.2
dynamic_mapper.esdf_slice_min_height: 0.10
dynamic_mapper.esdf_slice_max_height: 1.8
```

```bash
grep -nE "peoplenet|peoplesemsegnet|engine_file|input_binding|output_binding|preprocess/input|postprocess/people|sigmoid|argmax|segmentation_resize|interleaved_to_planar|detection/people_mask|segmentation/people_mask" /home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py
```
목적: Detection pipeline 전환 완료 여부 검증.
결과:
```
56: 'peoplenet', 'rsu_rs_480_640_mask', '1', 'model.plan'
140: 'input_binding_names': ['preprocess/input_1:0']
142: 'output_binding_names': ['postprocess/people_mask']
162: 'network_output_type': 'sigmoid'
175: '/camera0/detection/people_mask'
198: ('camera_0/mask/image', '/camera0/detection/people_mask')
```

```bash
grep -nE "inflation_distance|inflation_radius|cost_scaling_factor|max_obstacle_distance|convert_to_binary" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```
목적: Nav2 inflation 설정 최종 검증.
결과:
```
nvblox_layer.inflation_distance: 0.0
inflation_layer.cost_scaling_factor: 10.0
inflation_layer.inflation_radius: 0.30
(local/global 양쪽 동일)
```

### 0.10 적용 후 재빌드 + 재기동 (표준 절차)

```bash
docker exec -it isaac_ros_dev-aarch64-container pkill -INT -f component_container_mt
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'cd /workspaces/isaac_ros-dev && source /opt/ros/humble/setup.bash && \
   colcon build --packages-select jupiter_nvblox --symlink-install'
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch jupiter_nvblox jupiter_nvblox.launch.py run_rviz:=true'
```
목적: nvblox launch/config/rviz 수정 후 재기동.
결과: symlink-install 이므로 config/rviz 변경은 재빌드 생략 가능. launch 수정은 `colcon build` 필요.

```bash
# Nav2 호스트 재기동 (nvblox 와 별개)
pkill -INT -f "nav2_fused_navigation\|bt_navigator\|controller_server\|planner_server"
source /home/jetson/jupiter_ws_ros2/install/setup.bash
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```
목적: nav2_params_fused.yaml 변경 반영.
결과: nvblox 는 slice 만 발행하면 NvbloxCostmapLayer 가 새 파라미터로 자동 재구독.

---

## 1. 분석에 사용한 설정 / 스크립트 파일

### 1.0 오늘 사용/열람한 스크립트 및 파일 전체 경로

- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py` ← 수정 (Vanilla → Detection 전환 2회)
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml` ← 수정 (rate/bounds/slice/inflation 다수)
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz` ← 수정 (Nav2+SLAM 통합 재설계, Detection Mask 이름)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` ← 수정 (inflation 설정)
- `/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/config/nvblox/nvblox_base.yaml` (열람) — 공식 base 값 비교 기준
- `/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/config/nvblox/specializations/nvblox_segmentation.yaml` (열람)
- `/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/config/nvblox/specializations/nvblox_detection.yaml` (열람)
- `/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/config/nvblox/specializations/nvblox_realsense.yaml` (열람)
- `/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/launch/perception/segmentation.launch.py` (열람) — ShuffleSeg/Vanilla 분기 참조
- `/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/launch/perception/detection.launch.py` (열람) — PeopleNet pipeline 참조
- `/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/launch/perception/nvblox.launch.py` (열람) — remapping 경로 참조
- `/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/launch/realsense_example.launch.py` (열람)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_description/urdf/jupiter_simple.urdf` (열람) — base_link 0.08m 오프셋, footprint 치수

### 1.1 수정된 파일 1 — `jupiter_nvblox.launch.py`

경로: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py`

변경 항목 (2단계 전환):

**1단계 — ShuffleSeg → Vanilla UNet**:
- `model_base`: `peoplesemsegnet/optimized_deployable_shuffleseg_unet_amr_v1.0` → `peoplesemsegnet/deployable_quantized_vanilla_unet_onnx_v2.0`
- `TensorRTNode.input_binding_names`: `['input_2']` → `['input_1:0']`
- `ImageToTensorNode.remappings.tensor`: 중간 이름 `image_to_tensor_output` 도입
- `InterleavedToPlanarNode` 신규 추가 (NHWC → NCHW 변환, `input_tensor_shape: [544, 960, 3]`, `num_blocks: 40`)

**2단계 — Vanilla → PeopleNet Detection** (최종):
- `model_base`: `peoplesemsegnet/deployable_quantized_vanilla_unet_onnx_v2.0` → `peoplenet/rsu_rs_480_640_mask/1/model.plan`
- Resize 노드 제거 (DetectNet 은 D455 원본 640×480 직접 입력)
- InterleavedToPlanar 제거 (ONNX preprocess 스테이지가 내부 layout 변환)
- `TensorRTNode.input_binding_names`: `['input_1:0']` → `['preprocess/input_1:0']`
- `TensorRTNode.output_binding_names`: `['argmax_1']` → `['postprocess/people_mask']`
- `TensorRTNode.output_tensor_names`: `['output_tensor']` → `['people_mask']`
- `UNetDecoderNode.network_output_type`: `'argmax'` → `'sigmoid'`
- `UNetDecoderNode.mask_width/height`: 960/544 → 480/640
- UNetDecoder 노드 이름: `unet_decoder_node` → `people_decoder_node`
- mask 출력 토픽: `/camera0/segmentation/people_mask` → `/camera0/detection/people_mask`
- `ImageToTensorNode` 에 `input_qos: 'SENSOR_DATA'` 추가
- nvblox remap:
  - `camera_0/color/image`: `/camera0/segmentation/image_resized` → `/camera0/color/image_raw`
  - `camera_0/color/camera_info`: `/camera0/segmentation/camera_info_resized` → `/camera0/color/camera_info`
  - `camera_0/mask/image`: `/camera0/segmentation/people_mask` → `/camera0/detection/people_mask`
  - `camera_0/mask/camera_info`: `/camera0/segmentation/camera_info_resized` → `/camera0/color/camera_info`
- `composable_node_descriptions`: 7 노드 (realsense + resize + img2tensor + interleaved + trt + unet + nvblox) → 5 노드 (realsense + img2tensor + trt + unet + nvblox)

### 1.2 수정된 파일 2 — `jupiter_nvblox.yaml`

경로: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml`

**rate/queue/tick 조정 이력** (한 세션 내 여러 번 재조정 — 최종값 기준):
- `tick_period_ms`: 10 → 20 → **10** (20 은 과축소, 30Hz depth 온전 처리 어려움)
- `integrate_color_rate_hz`: 5 → 3 → **5** (마스크 갱신 가시성 우선)
- `update_esdf_rate_hz`: 5 → **10** (공식 원복, dynamic_occupancy 갱신 주기 2배)
- `maximum_input_queue_length`: 4 → 8 → **8** (공식 10 에 근접, Nav2 peak drop 방지)
- `decay_tsdf_rate_hz`: 2 → **5** (공식 원복)
- `update_mesh_rate_hz`: 2 → **0.5** (mesh 비시각화)
- `publish_debug_vis_rate_hz`: 2 → **1**
- `print_rates_to_console`: true → **false**
- `print_statistics_on_console_period_ms`: 10000 → **30000**
- `max_back_projection_distance`: 5 → **3**
- `back_projection_subsampling`: 2 → **4**
- `layer_streamer_bandwidth_limit_mbps`: -1 → **8**
- `layer_visualization_exclusion_radius_m`: 5 → **4**
- `esdf_slice_bounds_visualization_side_length`: 8 → **6**
- `workspace_height_bounds_visualization_side_length`: 8 → **6**

**workspace bounds 조정 이력**:
- `static_mapper.workspace_bounds_max_height_m`: 2.0 → **2.5** (카메라 장착 + 사람 팔 들기 여유)
- `static_mapper.workspace_bounds_min_height_m`: -0.2 → 0.05 → **-0.2 원복** (3D voxel/mesh 는 바닥 포함, 시각화/SLAM 일관성)

**ESDF slice 조정 이력** (핵심: Nav2 에 나가는 범위 = slice, 3D voxel 범위 = bounds 를 **분리**):
- `static_mapper.esdf_slice_min_height`: -0.1 → 0.05 → **0.10** (바닥 depth noise 거리²에 비례하여 5m 에서 ±10cm 까지 여유)
- `static_mapper.esdf_slice_max_height`: 0.5 → **1.2** (사람 상체·책상 장애물 Nav2 에 반영)
- `dynamic_mapper.esdf_slice_min_height`: -0.1 → 0.05 → **0.10**
- `dynamic_mapper.esdf_slice_max_height`: 0.5 → **1.8** (사람 머리까지)

**multi_mapper 조정 이력**:
- `connected_mask_component_size_threshold`: 2000 → 3000 → 1500 → **2000** (PeopleNet bbox-filled mask 는 통째로 큰 component, 공식 기본값 안정)

**dynamic_mapper 조정 이력**:
- `occupied_region_half_width_m`: 0.2 → 0.3 → **0.2** (PeopleNet bbox 가 전신 커버, 확장 불필요)

### 1.3 수정된 파일 3 — `jupiter_nvblox.rviz`

경로: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz`

**전면 재설계** (552줄). Nav2 + SLAM + nvblox 통합 시각화.

- Fixed Frame: `odom` → **`map`** (SLAM Toolbox map→odom 발행 이후 map 이 안정적 기준)
- TF panel: enabled, 핵심 프레임만 (map/odom/base_footprint/base_link/camera_link/camera0_link/laser)
- **RobotModel** 신규 추가 (`/robot_description` Topic source)
- **Nav2 & SLAM 그룹** 신규:
  - SLAM Map (`/map`, Transient Local) — enabled
  - Local Costmap (`/local_costmap/costmap` + updates) — enabled
  - Global Costmap (`/global_costmap/costmap` + updates) — disabled (수동 토글)
  - Global Plan (green, `/plan`) — enabled
  - Local Plan (orange, `/local_plan`) — enabled
  - Goal Pose (red arrow, `/goal_pose`) — enabled
- **LiDAR 그룹** 신규: LaserScan (`/scan`, Best Effort) — enabled
- **Nvblox 그룹** 재구성:
  - NvbloxMesh — disabled
  - Nvblox Voxels (`/nvblox_node/color_layer`, Reliable) — enabled
  - Map Slice Bounds — disabled
  - ESDF Slice (`/nvblox_node/static_esdf_pointcloud`) — disabled
  - Nvblox Combined Slice (`/nvblox_node/combined_occupancy_grid`) — disabled (SLAM Map 과 중복 방지)
  - People Voxels (`/nvblox_node/dynamic_occupancy_layer`, Reliable) — enabled
- **Sensors 그룹**:
  - Color Overlay (`/nvblox_node/dynamic_color_frame_overlay`, Reliable) — enabled
  - Depth Overlay (`/nvblox_node/dynamic_depth_frame_overlay`, Reliable) — enabled
  - **Detection Mask** (구 Segmentation Mask) — `/camera0/unet/colored_segmentation_mask`
  - Depth Image / Color Image — disabled
- Base Link Axes — enabled

### 1.4 수정된 파일 4 — `nav2_params_fused.yaml`

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

**local_costmap + global_costmap 양쪽 동일 수정** (1m 문 / 1.8m 통로 통과를 위한 inflation 축소):

- `nvblox_layer.inflation_distance`: **0.5 → 0.0** (핵심) — nvblox_layer 자체 inflation 제거, inflation_layer 단독 책임. 이중 inflation 이 1m 문 전체를 cost 로 덮던 주 원인.
- `inflation_layer.cost_scaling_factor`: **5.0 → 10.0** — NVIDIA Carter 기본값, 감쇠 가파라져 좁은 통로 DWB trajectory cost 총합 감소.
- `inflation_layer.inflation_radius`: 0.30 유지 (inscribed 0.1225 + 0.1775 여유)

---

## 2. 관찰 기준 요약

| 항목 | 기준 | 실측 / 설정값 |
|---|---|---|
| nvblox `global_frame` | map | map |
| nvblox `pose_frame` | base_link | base_link |
| URDF base_link z 오프셋 | 0.08m | 0.08m |
| `static_mapper.workspace_bounds_min_height_m` | 바닥 포함 | -0.2 |
| `static_mapper.esdf_slice_min_height` | 바닥 제외 (≥ +0.05) | 0.10 |
| `static_mapper.esdf_slice_max_height` | 사람 상체 포함 | 1.2 |
| `dynamic_mapper.esdf_slice_max_height` | 사람 머리 포함 | 1.8 |
| PeopleNet TRT engine 경로 | 존재 | rsu_rs_480_640_mask/1/model.plan |
| `TensorRTNode.input_binding_names` | preprocess/input_1:0 | preprocess/input_1:0 |
| `TensorRTNode.output_binding_names` | postprocess/people_mask | postprocess/people_mask |
| `UNetDecoderNode.network_output_type` | sigmoid | sigmoid |
| Nav2 local_costmap `nvblox_layer.inflation_distance` | 0 | 0.0 |
| Nav2 local_costmap `inflation_layer.cost_scaling_factor` | ≥ 10 | 10.0 |
| Nav2 footprint | 0.36×0.245m | 0.36×0.245m |
| 로봇 inscribed radius | 0.1225m | 0.1225m (자동계산) |
| 1m 문 기대 free 구간 | ≥ 로봇폭(0.245) | 1.0 - 2×0.30 = 0.40m (OK) |
| 1.8m 통로 + 사람(0.3m) 기대 free | ≥ 로봇폭(0.245) | 1.8 - 0.3 - 4×0.30 = 0.30m (OK) |

---

## 3. 추가 세션 — Nav2 좁은 공간 통과 개선 + SLAM Depth 통합

> 배경: (a) 직전 세션의 nvblox_layer.inflation_distance=0 + inflation_radius=0.30 + cost_scaling_factor=10.0 적용 후에도 1m 문 / 1.8m 통로 + 사람 통과 실패·backup 발화 지속. (b) SLAM 지도가 RPLidar `/scan` 만 사용해 바닥 2~3cm 창틀 미반영.
> Plan mode 로 진입해 Explore 에이전트 2개 병렬 조사 → AskUserQuestion 3 항목 확정 → Stage 1~5 순차 구현.

### 3.0 Explore 에이전트 조사 명령 (Phase 1)

호스트/Docker 양쪽의 nav2 구성, SLAM 설정, depth-to-laserscan 패키지 현황 확인.
두 에이전트 병렬 실행 (조사 내용은 §3.1~§3.3 에 반영).

### 3.1 nav2_params_fused.yaml DWB/RotationShim/costmap 직접 확인

```bash
grep -nE "robot_radius|footprint|inflation_radius|cost_scaling_factor|lethal_cost|max_obstacle|NvbloxCostmapLayer|inscribed" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml 2>&1
```
목적: Plan agent 보고에서 언급된 `BaseObstacle: 0.02` / `PathAlign: 32.0` / `local_costmap: 3×3 m` 파라미터 원문 재검증.
결과 토막:
```
220: footprint: "[ [0.18, 0.1225], [0.18, -0.1225], [-0.18, -0.1225], [-0.18, 0.1225] ]"
247: max_obstacle_distance: 1.0
251: cost_scaling_factor: 10.0
252: inflation_radius: 0.30
```
→ Plan 조사 내용과 일치. footprint 0.36×0.245m (inscribed 0.1225m) 확정.

```bash
grep -nE "forward_sampling_distance|sim_time|vtheta_samples|vx_samples|BaseObstacle.scale|PathAlign.scale|PathDist.scale|GoalDist.scale|GoalAlign.scale|^      width:|^      height:" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```
목적: DWB critic scale 및 sample 수 전수 확인.
결과 토막 (변경 전):
```
forward_sampling_distance: 0.3
vtheta_samples: 11
sim_time: 1.5
BaseObstacle.scale: 0.02
PathAlign.scale: 32.0
PathDist.scale: 32.0
width: 3
height: 3
```

### 3.2 SLAM Toolbox 설정 확인

```bash
cat /home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml | head -40
```
목적: SLAM 이 어떤 scan 토픽을 쓰고 있는지.
결과 토막:
```
scan_topic: /scan          ← RPLidar 단일 입력
mode: mapping
max_laser_range: 4.0
```

### 3.3 Docker 내 depth-to-laserscan 패키지 확인/설치

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
echo "== depth_image_proc =="
dpkg -l ros-humble-depth-image-proc 2>/dev/null | tail -3
which ros2 && ros2 pkg list 2>/dev/null | grep -E "depth_image_proc|pointcloud_to_laserscan|image_proc"
echo
echo "== ROS_DISTRO =="
echo $ROS_DISTRO
'
```
목적: Docker 컨테이너 내 필요 패키지 사전 확인.
결과:
```
ros-humble-depth-image-proc 3.0.8-1jammy.20251017.222739 arm64   ← 설치됨
ROS_DISTRO = humble
```
→ pointcloud_to_laserscan 은 미설치 확인.

```bash
docker exec isaac_ros_dev-aarch64-container bash -c 'id; echo "---"; which sudo; apt-get -v 2>/dev/null | head -1'
```
목적: apt 설치 권한 확인.
결과: `uid=0(root)` → 바로 설치 가능.

```bash
docker exec isaac_ros_dev-aarch64-container bash -c 'apt-get install -y ros-humble-pointcloud-to-laserscan 2>&1 | tail -5'
```
목적: pointcloud_to_laserscan 런타임 설치.
결과:
```
Setting up ros-humble-pointcloud-to-laserscan (2.0.1-3jammy.20260326.093043) ...
```
→ Docker 이미지 재빌드 없이 즉시 사용 가능.

### 3.4 depth_image_proc composable plugin 이름 확인

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
grep -r "PointCloudXyzNode" /opt/ros/humble/share/depth_image_proc/ 2>/dev/null | head -5
echo
grep -r "class_loader\|PointCloudToLaserScan" /opt/ros/humble/share/pointcloud_to_laserscan/ 2>/dev/null | head -10
find /opt/ros/humble/lib -name "*pointcloud_to_laserscan*" 2>/dev/null
'
```
목적: composable 지원 여부와 plugin class 명 확인.
결과 토막:
```
depth_image_proc::PointCloudXyzNode      ← composable 지원
/opt/ros/humble/lib/libpointcloud_to_laserscan.so
/opt/ros/humble/lib/pointcloud_to_laserscan/pointcloud_to_laserscan_node
```
→ pointcloud_to_laserscan 은 composable plugin 제공 안 함 → regular Node 로 실행 결정.

### 3.5 공식 point_cloud_xyz.launch.py 레퍼런스 확인

```bash
docker exec isaac_ros_dev-aarch64-container cat /opt/ros/humble/share/depth_image_proc/launch/point_cloud_xyz.launch.py 2>&1 | sed -n '40,80p'
```
목적: PointCloudXyzNode 의 표준 remapping 패턴 확인.
결과 (remapping):
```python
remappings=[('image_rect', '/camera/depth/image_rect_raw'),
            ('camera_info', '/camera/depth/camera_info'),
            ('image', '/camera/depth/converted_image')]
```
→ 우리 launch 에서 `image_rect`, `camera_info`, `points` 3개 remap 으로 이식.

### 3.6 공식 BT XML (backup 제거 대상) 확인

```bash
find /opt/ros/humble/share/nav2_bt_navigator -name "*.xml" 2>/dev/null
cat /opt/ros/humble/share/nav2_bt_navigator/behavior_trees/navigate_to_pose_w_replanning_and_recovery.xml
```
목적: 공식 BT 의 RoundRobin 구조 확보.
결과 토막 (수정 대상):
```xml
<RoundRobin name="RecoveryActions">
  <Sequence name="ClearingActions">
    <ClearEntireCostmap name="ClearLocalCostmap-Subtree" .../>
    <ClearEntireCostmap name="ClearGlobalCostmap-Subtree" .../>
  </Sequence>
  <BackUp backup_dist="0.30" backup_speed="0.05"/>   ← 제거 대상
</RoundRobin>
```
→ BackUp 노드 삭제 + `<Spin spin_dist="1.57"/>`, `<Wait wait_duration="2"/>` 대체.

### 3.7 최종 변경 grep 검증

```bash
echo "== voxel_size (Stage 1) =="
grep -nE "^    voxel_size" /home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml
echo
echo "== DWB / RotationShim / local_costmap (Stage 2) =="
grep -nE "sim_time|forward_sampling_distance|vtheta_samples|PathAlign.scale|PathDist.scale|BaseObstacle.scale|^      width:|^      height:" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml | head -12
echo
echo "== Custom BT (Stage 3) =="
ls /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/behavior_trees/
grep -n "default_nav_to_pose_bt_xml\|behavior_trees" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/setup.py
echo
echo "== Depth pipeline (Stage 4) =="
grep -nE "depth_to_pointcloud_node|pointcloud_to_laserscan_node|PointCloudXyzNode|/scan_from_depth" /home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py | head -12
echo
echo "== scan_merger + SLAM/Nav2 (Stage 5) =="
ls /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/scan_merger.py
grep -n "scan_merger\|scan_topic\|/scan_merged\|max_laser_range" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/setup.py /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py /home/jetson/jupiter_ws_ros/config/slam_params.yaml /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml 2>&1 | head -20
```
목적: 5 Stage 모든 변경의 최종 반영 확인.
결과 요지:
- Stage 1: `voxel_size: 0.05`
- Stage 2: `forward_sampling_distance: 0.6`, `vtheta_samples: 21`, `sim_time: 1.0`, `BaseObstacle.scale: 0.05`, `PathAlign/PathDist.scale: 20.0`, `width: 5`, `height: 5`
- Stage 3: `behavior_trees/navigate_to_pose_no_backup.xml` 생성, `setup.py` 에 data_files 등록, `default_nav_to_pose_bt_xml` 지정
- Stage 4: `depth_to_pointcloud_node` composable + `pointcloud_to_laserscan_node` regular Node, `/scan_from_depth` 발행
- Stage 5: `scan_merger.py` 생성, entry point 등록, `delayed_scan_merger` TimerAction, SLAM `scan_topic: /scan_merged`, Nav2 obstacle_layer `topic: /scan_merged`

### 3.8 재빌드 + 재기동 표준 절차

**Docker 측 nvblox 재빌드**:
```bash
docker exec -it isaac_ros_dev-aarch64-container pkill -INT -f component_container_mt
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'cd /workspaces/isaac_ros-dev && source /opt/ros/humble/setup.bash && \
   colcon build --packages-select jupiter_nvblox --symlink-install'
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch jupiter_nvblox jupiter_nvblox.launch.py run_rviz:=true'
```

**Host 측 jupiter_nav_VO + jupiter_nav 재빌드**:
```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO jupiter_nav --symlink-install
source install/setup.bash
pkill -INT -f "nav2_vslam_fused\|nav2_fused_navigation\|slam_toolbox"
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py
# 별도 터미널
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```

### 3.9 검증 명령 (예상)

```bash
# Stage 4 검증
docker exec -it isaac_ros_dev-aarch64-container bash -c 'source /workspaces/isaac_ros-dev/install/setup.bash && ros2 topic hz /scan_from_depth'
docker exec -it isaac_ros_dev-aarch64-container bash -c 'source /workspaces/isaac_ros-dev/install/setup.bash && ros2 topic echo /scan_from_depth --once | head -40'

# Stage 5 검증
ros2 topic hz /scan_merged
ros2 topic info /scan_merged -v

# Recovery 관찰
ros2 topic echo /behavior_server/status
```

---

## 4. 추가 세션 — 분석/수정 파일 목록

### 4.0 오늘 추가 세션에서 열람/수정/생성한 파일

- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml` ← 수정 (voxel_size 0.05)
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py` ← 수정 (depth→pointcloud + pointcloud→laserscan 노드 2개 추가)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` ← 수정 (bt_xml, DWB critics/sim_time/samples, RotationShim, local_costmap width, obstacle_layer topic)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` ← 수정 (scan_merger_node + TimerAction 추가, SLAM 주석 갱신)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml` ← 수정 (scan_topic /scan_merged, max_laser_range 5.0)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/setup.py` ← 수정 (behavior_trees data_files, scan_merger entry point)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/behavior_trees/navigate_to_pose_no_backup.xml` ← 신규
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/scan_merger.py` ← 신규
- `/opt/ros/humble/share/nav2_bt_navigator/behavior_trees/navigate_to_pose_w_replanning_and_recovery.xml` (열람, 공식 BT 레퍼런스)
- `/opt/ros/humble/share/depth_image_proc/launch/point_cloud_xyz.launch.py` (열람, composable plugin 레퍼런스)

### 4.1 수정 파일 1 — `jupiter_nvblox.yaml`

변경 항목:
- `voxel_size: 0.08 → 0.05` (Stage 1, 공식 기본값 원복). voxel 수 ≈ 4.1× 증가 → `jtop` 실측 필수.

### 4.2 수정 파일 2 — `jupiter_nvblox.launch.py`

변경 항목 (Stage 4):
- **신규 composable** `depth_to_pointcloud_node` (`depth_image_proc::PointCloudXyzNode`):
  - remappings: `image_rect` → `/camera0/depth/image_rect_raw`, `camera_info` → `/camera0/depth/camera_info`, `points` → `/camera0/depth/points`
- **신규 regular Node** `pointcloud_to_laserscan_node`:
  - parameters: `target_frame: base_link`, `min_height: 0.02`, `max_height: 0.08`, `angle_min/max: ±1.5708`, `angle_increment: 0.008727` (0.5°), `range_min: 0.2`, `range_max: 5.0`, `transform_tolerance: 0.05`
  - remappings: `cloud_in` → `/camera0/depth/points`, `scan` → `/scan_from_depth`
- container composable_node_descriptions 에 `depth_to_pointcloud_node` 추가 (nvblox_node 앞에)
- LaunchDescription 에 `pointcloud_to_laserscan_node` 추가 (container 뒤)

### 4.3 수정 파일 3 — `nav2_params_fused.yaml` (Stage 2 + 3 + 5)

변경 항목:
- **bt_navigator** (Stage 3):
  - `default_nav_to_pose_bt_xml: "$(find-pkg-share jupiter_nav_VO)/behavior_trees/navigate_to_pose_no_backup.xml"` 추가
- **FollowPath/DWB** (Stage 2):
  - `forward_sampling_distance: 0.3 → 0.6`
  - `vtheta_samples: 11 → 21`
  - `sim_time: 1.5 → 1.0`
  - `BaseObstacle.scale: 0.02 → 0.05`
  - `PathAlign.scale: 32.0 → 20.0`
  - `PathDist.scale: 32.0 → 20.0`
- **local_costmap** (Stage 2 + 5):
  - `width: 3 → 5`
  - `height: 3 → 5`
  - `obstacle_layer.scan.topic: /scan → /scan_merged`

### 4.4 수정 파일 4 — `nav2_vslam_fused.launch.py` (Stage 5)

변경 항목:
- **신규 노드** `scan_merger_node` (package: `jupiter_nav_VO`, executable: `scan_merger`):
  - parameters: `target_frame: base_link`, `merged_range_max: 5.0`, `publish_rate_hz: 10.0`
- **신규 TimerAction** `delayed_scan_merger` (period: 4.0s, EKF 초기화 이후 기동)
- LaunchDescription 에 `delayed_scan_merger` 추가 (delayed_navsat 와 delayed_slam_toolbox 사이)
- SLAM Toolbox 주석 갱신 (`/scan_merged` 사용 명시)

### 4.5 수정 파일 5 — `slam_params.yaml` (Stage 5)

변경 항목:
- `scan_topic: /scan → /scan_merged`
- `max_laser_range: 4.0 → 5.0` (D455 depth 유효 범위 반영)

### 4.6 수정 파일 6 — `jupiter_nav_VO/setup.py`

변경 항목:
- `data_files` 에 `(share/{pkg}/behavior_trees, glob('behavior_trees/*.xml'))` 추가
- `entry_points.console_scripts` 에 `'scan_merger = jupiter_nav_VO.scan_merger:main'` 추가

### 4.7 신규 파일 1 — `behavior_trees/navigate_to_pose_no_backup.xml`

- 공식 `navigate_to_pose_w_replanning_and_recovery.xml` 에서 `<BackUp>` 제거, `<Spin spin_dist="1.57"/>` + `<Wait wait_duration="2"/>` 추가.
- RoundRobin RecoveryActions: ClearingActions → Spin → Wait 순환.

### 4.8 신규 파일 2 — `jupiter_nav_VO/scan_merger.py`

- ROS 2 Python 노드. `/scan` + `/scan_from_depth` 구독, base_link 기준 angle grid 에 투영, bin 별 최소 range 선택 후 `/scan_merged` 발행.
- 주요 함수: `_quat_to_yaw()`, `_laserscan_to_base_points()` (TF 변환 + 점군화), `_publish_merged()` (10 Hz 타이머).
- 파라미터: `target_frame`, `merged_angle_min/max/increment`, `merged_range_min/max`, `publish_rate_hz`, `scan_timeout_sec`.

---

## 5. 추가 세션 관찰 기준 요약

| 항목 | 기준 | 상태 |
|---|---|---|
| `voxel_size` | 0.05 | 0.05 ✔ |
| `DWB.sim_time` | 1.0 | 1.0 ✔ |
| `DWB.vtheta_samples` | 21 | 21 ✔ |
| `RotationShim.forward_sampling_distance` | 0.6 | 0.6 ✔ |
| `PathAlign.scale` / `PathDist.scale` | 20 | 20 ✔ |
| `BaseObstacle.scale` | 0.05 | 0.05 ✔ |
| `local_costmap.width/height` | 5 / 5 | 5 / 5 ✔ |
| `default_nav_to_pose_bt_xml` | custom no-backup | 지정 ✔ |
| `<BackUp>` 노드 존재 | 부재 | 부재 ✔ |
| Docker `pointcloud_to_laserscan` | 설치 | 설치 완료 ✔ |
| `depth_image_proc::PointCloudXyzNode` composable 등록 | 있음 | container 에 추가 ✔ |
| `/scan_from_depth` 발행 | 예상 rate ≥ 5 Hz | 재기동 후 검증 대기 |
| `scan_merger` entry point | 등록 | 등록 ✔ |
| `/scan_merged` 발행 | 예상 rate ≥ 5 Hz | 재기동 후 검증 대기 |
| SLAM `scan_topic` | /scan_merged | /scan_merged ✔ |
| Nav2 `obstacle_layer.scan.topic` | /scan_merged | /scan_merged ✔ |
| 1m 문 기대 유효 free 폭 | ≥ 로봇 폭 0.245m | 계산상 0.40m (OK) — 실측 대기 |
| 1.8m 통로 + 사람 기대 free | ≥ 0.245m | 계산상 0.30m (OK) — 실측 대기 |
| nvblox RAM (voxel 0.05 적용 후) | < 7.5 GB | jtop 실측 대기 (초과 시 0.06 fallback) |
