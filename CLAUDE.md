# jupiter_ws_ros2 작업 규약

> 이 CLAUDE.md 는 `/home/jetson/jupiter_ws_ros2` 및 연관 로봇 작업공간(`/home/jetson/isaac_ws_ros`, `/home/jetson/allan_ws` 등)에서 작업할 때 적용되는 고정 규약을 정의한다.

---

## 1. 문서 양식 규약

### 1.1 트리거 문구
사용자가 다음 표현 중 하나를 말하면, **양식(§1.3, §1.4) 대로 두 파일을 만들고 저장한다**:
- "진단_명령어와 진행과정을 정리하라"
- "진단_명령어를 정리하라"
- "진행과정을 정리하라"
- "오늘 작업을 정리하라" / "작업 로그 남겨라" / "세션을 문서로 남겨라"
- "〈날짜〉 진단 문서를 작성하라" / "〈날짜〉 진행과정을 작성하라"

### 1.2 기본 저장 위치와 파일명
- 디렉토리: `/home/jetson/jupiter_ws_ros2/`
- 파일명: `진단_명령어_YYYY_MM_DD.md` 와 `진행과정_YYYY_MM_DD.md`
  - 날짜는 시스템 `date +%Y_%m_%d` 사용 (예: `2026_04_23`)
  - 사용자가 다른 경로/날짜를 지정하면 그걸 따른다
- 두 파일은 **항상 동시 생성**한다. 한쪽만 요청돼도 상대 파일의 부재를 언급한다.

### 1.3 `진단_명령어_YYYY_MM_DD.md` 양식

**제목 라인**
```markdown
# 명령어 로그 YYYY-MM-DD: <오늘 주제 한 줄>
```

**첫 줄(인용문, 고정 문구)**
```markdown
> 진단 및 수정에 사용한 명령어와 스크립트/설정 파일을 요약 없이 전부 기록한다.
```

**섹션 구성**

1. `## 0. 오늘 사용한 터미널 명령어 원문`
   - 하위 `### 0.N <주제>` 단위로 분류
   - 각 subsection에 **실행한 명령어 전체를 코드블록으로 원문 그대로** 넣는다. 줄임표/요약 금지. 파이프/리다이렉트/백그라운드(&)도 보존.
   - 코드블록 바로 아래에 `목적:` 한 줄, `결과:` 실측값/출력 요지. 출력이 의미있으면 **토막 로그를 코드블록으로 붙여둔다**.
   - 여러 경로/런을 묶어 실행한 경우 같은 subsection 안에 여러 코드블록 나열 허용.

2. `## 1. 분석에 사용한 설정 / 스크립트 파일`
   - `### 1.0 오늘 사용/열람한 스크립트 및 파일 전체 경로`
     - 절대 경로를 불릿 리스트로 **전부** 기록. 수정된 파일은 `← 수정` 표시. 열람만 한 파일은 `(열람)` 표시.
   - `### 1.1 수정된 파일 N — <역할>` 형식으로 수정 파일마다 섹션. 파일 경로 + 수정 항목 불릿 리스트.

3. `## 2. 관찰 기준 요약` (선택)
   - 토픽 rate, 파라미터 값, 파일 권한 등을 `기준 | 실측` 표로 정리

**스타일 규칙**
- Bash 명령어는 원문 그대로. `\n` / 개행을 임의 추가하지 않는다.
- 한 명령이 여러 줄이면 원본 개행 유지.
- `docker exec` 과 `ros2` 내부 `bash -c '...'` 블록도 그대로 옮긴다.
- 요약 금지: "외 N개" / "..." / "기타 명령어 생략" 따위 표현을 쓰지 않는다.
- 결과 캡쳐는 3~30줄 내 핵심 토막만. 긴 로그는 파일 경로 + 앵커(라인번호)로 대체.

### 1.4 `진행과정_YYYY_MM_DD.md` 양식

**제목 라인**
```markdown
# 진행과정 YYYY-MM-DD: <오늘 주제 한 줄>
```

**섹션 구성 (순서 고정)**

1. `## 1. 목표`
   - `### 1.1 사용자 요청 (시간순)` — 인용문(`>`)으로 받은 지시를 번호로 나열, 말투 원문 최대한 유지
   - `### 1.2 이전 세션 요약 / 연관 작업` — 오늘 작업의 전제가 되는 이전 로그 링크 또는 요약

2. `## 2. 대상 시스템 구성` (필요 시)
   - Launch 파일, 노드 목록, 외부 TF 의존 등 "어디를 건드렸는지" 를 빠르게 파악할 수 있도록

3. `## 3. 문제 분석`
   - 이슈 단위 subsection. 각 이슈는 다음 틀을 따른다:
     - **증상**:
     - **수학적/코드상 원인** (가능하면 소스 파일:라인 링크)
     - **근본 원인**:
     - **보조 원인** (있을 때)
   - 원인을 단정하기 전 "초기 의심" / "정정 후" 같이 **분석의 흐름을 유지**하여 기록한다.

4. `## 4. 적용한 변경`
   - `### 4.N <변경 주제>` 단위
   - 변경된 파일 경로 + `변경 전 | 변경 후` 표
   - 의도 / 주의사항 / 트레이드오프 서술

5. `## 5. 빌드 및 검증`
   - `### 5.1 빌드` 코드블록
   - `### 5.N <검증 단계>` 코드블록 + 결과
   - `### 5.<끝> 검증 결과` 표 (기준 | 실측)

6. `## 6. 최종 결론`
   - `### 6.1 오늘 해결된 항목`
   - `### 6.2 오늘 새로 확인된 항목`
   - `### 6.3 남은 제약 / 주의`
   - `### 6.4 다음 분석 우선순위`

7. `## 7. 생성 / 수정 파일 목록`
   - `### 7.1 수정된 파일`
   - `### 7.2 생성된 문서`

**스타일 규칙**
- 원인 분석은 "증상 → 데이터 → 근본 원인 → 조치" 흐름을 반드시 지킨다. 결론만 적지 말고 과정을 남긴다.
- 설정 변경은 **반드시** 변경 전/후 표로 기록.
- 외부 파일 / 소스 참조는 절대 경로 + 라인 번호로 링크.
- 추측과 확인된 사실을 분리해 표기한다.

### 1.5 두 문서 간 연동
- `진행과정` 의 각 변경/검증 항목은 `진단_명령어` 의 해당 섹션 번호를 참조할 수 있다.
- 두 파일 모두 **현재 세션의 실제 작업 흐름** 만 기록한다. 추후 작업은 별도 날짜 파일로 분리.
- 이미 같은 날 파일이 존재하면 **덮어쓰지 말고 이어쓰기** (예: `## 8. 추가 세션` 섹션 추가). 사용자가 "새로 쓰라"고 명시하면 덮어쓴다.

---

## 2. 참조 템플릿
- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_04_23.md` — 최신 진단 양식 예시
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_04_23.md` — 최신 진행과정 양식 예시
- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_04_21.md` — 레거시 참고 (동일 양식)
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_04_21.md` — 레거시 참고 (동일 양식)

---

## 3. 관련 워크스페이스 레이아웃

- `/home/jetson/jupiter_ws_ros2/` — 호스트 ROS2 워크스페이스 (EKF, SLAM Toolbox, Nav2, RTK, 드라이버). 문서 저장 기본 경로.
- `/home/jetson/isaac_ws_ros/` — Docker 기반 Isaac ROS 워크스페이스 (nvblox, people_segmentation, cuVSLAM 등).
- `/home/jetson/allan_ws/` — Allan variance 분석 등 보조 워크스페이스.

두 ROS 워크스페이스가 모두 한 로봇(Jupiter)을 구성하므로, 한쪽을 수정해도 다른쪽 TF/파라미터를 함께 확인한다.

---

## 4. 한국어/영어 혼용 규약
- 사용자 요청은 한국어이므로, 응답과 문서는 **한국어 우선**.
- 코드/로그/파라미터 이름은 원문 영어 그대로.
- 번역보다 의미 보존을 우선한다.
