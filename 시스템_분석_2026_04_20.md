# Jupiter X3 전체 시스템 분석 보고서
**날짜:** 2026-04-20  
**목적:** 시스템 현황 파악, 다음 단계 도출, 후진 시 쏠림 현상 원인 분석 및 해결 방안 제시

---

## 1. 시스템 아키텍처 요약

```
/cmd_vel (Nav2, 10Hz)
    ↓
jupiter_driver_compensated.py
    ├─ ANGULAR_SCALE_FACTOR 적용 → MCU 시리얼 전송
    ├─ MCU → /jupiter/imu (100Hz)
    └─ MCU → /jupiter/get_vel (50Hz, vx=-vx 반전)
                ↓
        base_node (C++) → /odom_raw (dead-reckoning 적분)
                ↓
        odom_covariance_adapter → /odom_adapted (동적 공분산)
                ↓
        EKF (robot_localization, 30Hz)
            ├─ odom0: /odom_adapted (vx, vy, vyaw)
            ├─ odom1: /visual_slam/.../odometry_adapted (x, y, vx, vy, differential)
            ├─ odom2: /odom_rf2o_adapted (x, y, differential)
            ├─ odom3: /odometry/gps (x, y, outdoor only)
            └─ imu0: /imu/data_calibrated (vyaw only)
                ↓
            /odom + odom→base_footprint TF
                ↓
        Nav2 (DWB + RotationShimController)
            → /cmd_vel (루프)
```

---

## 2. 현재 상태 점검

### 2.1 정상 동작 확인 완료 (✅)

| 항목 | 상태 | 비고 |
|------|------|------|
| imu_invert_z | ✅ 정상 | gz = -gz 소스 적용, params.yaml: true |
| vx = -vx | ✅ 정상 | MCU 전진 시 Vx 음수 → ROS 양수 변환 |
| ANGULAR_SCALE_FACTOR = 1.0 | ✅ 적용됨 | 신규 모터용 (구: 0.401) |
| MCU_MIN_ANGULAR = 0.0 (소스) | ✅ 소스 반영 | PID 적분 누적으로 무의미 |
| 회전 방향 일치 | ✅ 정상 | EKF ≈ wheel ≈ RF2O (CSV 3차 확인) |
| 전진/후진 방향 일치 | ✅ 정상 | wheel ≈ RF2O (CSV 4차 확인) |
| imu_gyro_scale = 1.025 | ✅ 정상 | 2.3% gyro 보정 |

### 2.2 미해결 이슈 (❌)

| 항목 | 상태 | 심각도 |
|------|------|--------|
| **설치된 코드 불일치** | ❌ | 중 |
| **후진 시 좌측 쏠림** | ❌ | **높음** |
| **ANGULAR_SCALE_FACTOR 미교정** | ❌ | 높음 |
| **MECANUM 물리 상수 미검증** | ❌ | 중 |
| **PID 파라미터 소스 불일치** | ⚠️ | 낮음 |

---

## 3. 발견된 문제의 상세 분석

### 3.1 설치된 코드 불일치

**증상:** `install/` 디렉토리의 코드가 소스와 다름  
**원인:** MCU_MIN_ANGULAR 변경 후 빌드하지 않음

```
소스:   MCU_MIN_ANGULAR = 0.0   (정상)
설치됨: MCU_MIN_ANGULAR = 0.37  (구 값)
```

**영향:** MCU_MIN_ANGULAR가 PID 적분 누적 때문에 무의미하므로, 실질적 영향 없음.  
**조치:** 빌드하여 일관성 확보 필요.

---

### 3.2 후진 시 좌측 쏠림 — 핵심 문제 분석

**증상:** 후진 시 좌측 바퀴(M1)가 우측(M3)보다 더 많이 회전 → 로봇이 한쪽으로 틀어짐

#### 3.2.1 데이터 흐름 추적 (후진 명령: V_x = -300)

```
┌─ Mecanum_Ctrl(V_x=-300, V_y=0, V_z=0)
│   speed_fb = -V_x = 300 (양수)
│   speed_L1_setup = 300, speed_R1_setup = 300
│
├─ 밸런스 보정 (V_y==0 && V_z==0 → 활성화):
│   diff = m1_delta - m3_delta
│   dir = (V_x > 0) ? 1 : -1  →  dir = -1
│   L1 += dir * correction = L1 - correction
│   R1 -= dir * correction = R1 + correction
│
├─ Motion_Set_Speed → PID_Set_Motor_Target (각 모터 독립)
│
├─ PID 피드백:
│   speed_mm[0] = -1 × (-encoder_M1_delta) × 100 × circle_mm / circle_pulse
│   speed_mm[2] = -1 × (+encoder_M3_delta) × 100 × circle_mm / circle_pulse
│
└─ 속도 보고:
    Vx = (speed_mm[0] + speed_mm[2]) / 2
    Vz = -(speed_mm[0] - speed_mm[2]) / 2 / APB × 1000
```

#### 3.2.2 엔코더 부호 분석 (핵심)

**CAR_MECANUM 엔코더 부호 설정** (`bsp_encoder.c`):
```c
M1: g_Encoder_M1_Now -= Encoder_Read_CNT(M1)  // 빼기
M2: g_Encoder_M2_Now -= Encoder_Read_CNT(M2)  // 빼기
M3: g_Encoder_M3_Now += Encoder_Read_CNT(M3)  // 더하기 ← 좌우 비대칭
M4: g_Encoder_M4_Now += Encoder_Read_CNT(M4)  // 더하기
```

**CAR_MECANUM_MAX 엔코더 부호 (참고 — 올바른 패턴):**
```c
M1: -= (빼기)
M2: += (더하기)
M3: += (더하기)
M4: -= (빼기)
```

- CAR_MECANUM_MAX: M1/M4 `−`, M2/M3 `+` → 대각선 대칭 (정상)
- **CAR_MECANUM: M1/M2 `−`, M3/M4 `+` → 좌-우 일방 비대칭**

이 부호 설정은 원래 구형 모터에 맞춰 튜닝된 것으로, **신규 모터 교체 후 좌우 인코더 방향이 올바른지 별도 검증이 필요하다.**

#### 3.2.3 밸런스 보정 로직 분석

`app_mecanum.c` 라인 96~119:
```c
if (V_y == 0 && V_z == 0) {
    // 방향 전환 시 기준값 리셋
    if ((balance_prev_vx > 0) != (V_x > 0) || balance_prev_vx == 0) {
        balance_m1_ref = Encoder_Get_Count_Now(M1);
        balance_m3_ref = Encoder_Get_Count_Now(M3);
    }
    int32_t diff = m1_delta - m3_delta;
    int16_t correction = (int16_t)(diff * 0.02f);   // 제한: ±50
    int dir = (V_x > 0) ? 1 : -1;
    speed_L1_setup += dir * correction;
    speed_R1_setup -= dir * correction;
}
```

**이 로직의 의도:** M1이 M3보다 많이 회전하면 diff > 0 → correction > 0  
- 전진(dir=+1): L1 += 양수 (덜 음수=느림), R1 -= 양수 (더 음수=빠름) → M1 감속, M3 가속 ✅
- **후진(dir=-1):** L1 += 음수 (더 양수=빠름), R1 -= 음수 (덜 양수=느림) → **M1 가속, M3 감속** ✅

**결론: 밸런스 로직 자체는 전진/후진 모두 올바르게 동작한다.**  
`dir` 플래그가 목표값의 부호 반전을 정확히 처리하고 있음.

#### 3.2.4 진짜 원인: 물리적 비대칭 + PID 한계

쏠림의 실제 원인 후보:

1. **물리적 모터 비대칭:** 교체한 좌/우 모터의 특성(마찰, 시동 토크, 기어 백래시)이 서로 다름
2. **PWM→토크 비선형성:** M1과 M3에 같은 PWM을 주어도 실제 발생 토크가 다름 (ESC 특성)
3. **밸런스 보정 게인 부족:** correction factor = 0.02 × diff. 누적 오차가 빠르게 증가하면 보정이 따라가지 못함
4. **밸런스 보정 제한값:** ±50 제한이 실제 비대칭보다 작을 수 있음
5. **Additive Dead Zone의 차이:** bsp_motor.c에서 M1/M3 모두 dead_zone=700이지만, 실제 ESC 반응점이 다를 수 있음

---

## 4. 다음 단계 (Action Items)

### 4.1 즉시 수행 (빌드 일관성)

```bash
cd ~/jupiter_ws_ros2
colcon build --packages-select jupiter_bringup
```

### 4.2 쏠림 진단 테스트 (우선순위 1)

**목적:** 좌우 모터 비대칭의 정확한 크기를 정량적으로 측정

**테스트 1: 개별 모터 엔코더 모니터링**
```bash
# 로봇을 들어올린 상태에서 전진/후진 명령
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.2}}" --once
# 별도 터미널에서 엔코더 누적값 확인
# (MCU 디버그 로그 또는 /jupiter/get_vel 기반 분석)
```

실측 필요 데이터:
- **M1 vs M3 속도 차이 (mm/s)** — 같은 target에 대한 실제 속도 편차
- **전진 vs 후진 비대칭 크기** — 방향별 차이가 다른지 확인
- **시동 토크 차이** — 정지 → 출발 시 어느 바퀴가 먼저 돌기 시작하는지

**테스트 2: MCU 모터별 PID 목표 vs 실측 비교**
```
MCU 디버그 로그 활성화:
- app_mecanum.c의 #define YB_PRINT_LOG 1 주석 해제
- 재빌드/플래시
- 시리얼 로그 캡처: >S1:xxx, >S3:xxx (목표), >smm:xxx (실측)
```

### 4.3 쏠림 해결 방안 (우선순위 순)

#### 방안 A: MCU 밸런스 보정 강화 (펌웨어 수정)

**현재:**
- correction = diff × 0.02, 제한 ±50
- 전진/후진 시에만 동작 (V_y==0 && V_z==0)

**개선:**
```c
// 1. 보정 게인 증가
int16_t correction = (int16_t)((float)diff * 0.05f);  // 0.02 → 0.05
// 2. 제한값 확대
if (correction > 100) correction = 100;   // ±50 → ±100
if (correction < -100) correction = -100;
```

**장점:** 펌웨어 레벨에서 근본 보정, 소프트웨어 복잡도 최소  
**단점:** 펌웨어 빌드/플래시 필요, 적절한 게인값 실험 필요

#### 방안 B: 모터별 Dead Zone 오프셋 튜닝 (펌웨어 수정)

```c
// bsp_motor.c에서 M1, M3의 dead zone을 개별 조정
static int16_t motor_dead_zone_fwd[MAX_MOTOR] = {700, 760, 720, 700};  // M3: 700→720 예시
static int16_t motor_dead_zone_bwd[MAX_MOTOR] = {680, 760, 700, 700};  // M1: 700→680 예시
```

**장점:** ESC 개별 특성 보상 가능  
**단점:** 실측 기반 수동 튜닝, 모터 교체 시 재조정 필요

#### 방안 C: 드라이버 레벨 좌우 보정 (소프트웨어)

```python
# jupiter_driver_compensated.py에서 후진 시 M1/M3 비율 보정 파라미터 추가
REVERSE_LEFT_SCALE = 0.95  # M1 출력 감소
```

**장점:** 펌웨어 수정 불필요  
**단점:** MCU PID와 충돌 가능성, 근본 해결 아님

#### 방안 D: ROS 레벨 EKF 보상 (소프트웨어)

EKF가 이미 다른 센서(VSLAM, RF2O, IMU)와 융합하므로, 휠 오도메트리의 bias를 EKF가 흡수할 수 있음. 그러나 이는 **경로 추종 품질** 문제를 해결하지 못함 — cmd_vel이 의도대로 실행되지 않는 것이 문제.

**권장 순서:** 방안 A → 불충분하면 방안 B 추가

### 4.4 ANGULAR_SCALE_FACTOR 재교정 (우선순위 2)

현재 1.0이지만, 신규 모터의 실제 회전 응답이 cmd_vel 요청과 일치하는지 확인 필요.

**교정 방법:**
1. `ros2 topic pub /cmd_vel ... angular.z: 1.0` 명령 (360° 회전)
2. IMU gyro + RF2O + VSLAM으로 실제 회전량 측정
3. `실제_회전 / 요청_회전 = 보정계수`
4. ANGULAR_SCALE_FACTOR = 1.0 / 보정계수

### 4.5 물리 상수 검증 (우선순위 3)

교체된 바퀴/모터의 물리적 파라미터가 펌웨어 상수와 일치하는지 확인:

| 상수 | 현재값 | 검증 방법 |
|------|--------|-----------|
| MECANUM_CIRCLE_MM = 150.796mm | 바퀴 지름 48mm → π×48 = 150.796 | 바퀴 실측 지름 확인 |
| ENCODER_CIRCLE_330 = 1320 | 330선 × 4체배 = 1320 | 바퀴 1회전 시 엔코더 펄스 실측 |
| MECANUM_APB = 95.0mm | ROBOT_WIDTH/2 = 190/2 | 좌우 바퀴 중심간 거리 실측 |
| ROBOT_LENGTH = 250.0mm | 전후 바퀴 중심간 거리 | 실측 확인 |

### 4.6 PID 파라미터 정합성 확인 (우선순위 4)

**현재 상태:**
- MCU 펌웨어 기본값: Kp=0.8, Ki=0.06, Kd=0.5 (`app_pid.h`)
- 드라이버 소스 선언: Kp=0.8, Ki=0.02, Kd=0.3 (jupiter_driver_compensated.py)
- params.yaml 오버라이드: Kp=0.8, Ki=0.06, Kd=0.5

**실질 동작 흐름:**
1. MCU 부팅 → 기본값 (Kp=0.8, Ki=0.06, Kd=0.5) 로드
2. 드라이버 노드 시작 → params.yaml 값 (0.8, 0.06, 0.5) 을 MCU로 전송
3. 결과: MCU PID = (0.8, 0.06, 0.5)

⚠️ jupyter_driver_compensated.py 소스의 기본값(0.02, 0.3)은 **사용되지 않음** (params.yaml이 오버라이드). 혼란 방지를 위해 소스 기본값을 params.yaml과 동기화하거나, 주석으로 명시 권장.

---

## 5. 작업 우선순위 정리

| 순위 | 작업 | 목적 | 난이도 |
|------|------|------|--------|
| **1** | jupiter_bringup 빌드 | 설치 코드 일관성 | 즉시 |
| **2** | 엔코더 부호/속도 실측 | 쏠림 원인 정량 확인 | 중 |
| **3** | 밸런스 보정 강화 (게인/제한) | 쏠림 완화 | 중 (펌웨어) |
| **4** | ANGULAR_SCALE_FACTOR 교정 | 회전 정확도 | 중 |
| **5** | 물리 상수 실측 검증 | 오도메트리 정확도 | 낮음 |
| **6** | PID 파라미터 코드 동기화 | 유지보수성 | 낮음 |

---

## 6. 참고: MCU 데이터 흐름 상세

### CAR_MECANUM 모터 배치
```
     전방
   ┌──────┐
M1 │  L1  │ R1  M3
   │      │
M2 │  L2  │ R2  M4
   └──────┘
     후방
```

### 엔코더 → 속도 변환 경로
```
엔코더 카운터 (TIM2/4/5/3)
    ↓ Encoder_Read_CNT(): 0x7fff - CNT
    ↓ Encoder_Update_Count(): CAR_MECANUM부호 적용 (M1/M2: -=, M3/M4: +=)
    ↓ g_Encoder_M*_Now 누적
    ↓ Motion_Get_Encoder(): offset = now - last, 로우패스(α=0.8)
    ↓ g_Encoder_All_Offset_Filtered[i]
    ↓ speed_mm[i] = -1 × filtered × 100 × circle_mm / circle_pulse
    ↓ CAR_MECANUM:
        Vx = (speed_mm[0] + speed_mm[2]) / 2    ← M1 + M3 평균
        Vz = -(speed_mm[0] - speed_mm[2]) / 2 / APB × 1000
    ↓ Jetson으로 전송 (FUNC_REPORT_SPEED)
    ↓ jupiter_driver_compensated.py: vx = -vx
    ↓ /jupiter/get_vel (Twist)
```

### PID 제어 루프 (10ms 주기)
```
목표: speed_*_setup (mm/s) → PID_Set_Motor_Target()
실측: speed_mm[i] (mm/s) ← Motion_Get_Speed()
PID: 증분형, pwm_output += Kp*(err-err_prev) + Ki*err + Kd*(err-2*err_prev+err_prev2)
출력: speed_pwm[i] → Motor_Ignore_Dead_Zone() → additive offset(±700) → Motor_Set_Pwm()
필터: 로우패스 α=0.3 적용
방향 클램프: target과 반대 방향 PWM은 0으로 클램프
```
