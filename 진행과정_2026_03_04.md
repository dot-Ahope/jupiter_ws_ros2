# 진행과정 2026-03-04

## 1. 오늘 작업 요약

### 1.1 NTP 동기화 검증
- PC: Windows `w32tm` → Stratum 4, time.windows.com 동기화 완료
- Jetson: chrony → Stratum 3, <1ms 정확도 (어제 설정)
- stamp_offset: +0.04s ~ -0.70s (이전 ~2.4s 대비 대폭 개선)

### 1.2 실주행 검증 결과
- **Goal 1 (0.27m)**: 성공 ✅
- **Goal 2 (-1.79m)**: 실패 ❌ — 13초 후 planner TF buffer eviction
- **Goal 3, 4**: 동일 실패 패턴

## 2. 문제 분석

### 2.1 핵심 문제: TF Buffer Eviction (Nav2 구조적 한계)
- Nav2 TF buffer 기본값: 10초
- 어제 restamper가 `stamp=now`로 찍어도, 13초+ 후 periodic replan 시점에는 evicted
- BT recovery (clearCostmap → retry → backup) 중 동일 goal stamp 재사용 → 무한 실패 루프
- **NTP 동기화만으로는 해결 불가한 구조적 문제**

### 2.2 RF2O 조기 사망 (elapsed=34.1s)
- 전체 세션 116초 중 28초만 동작
- 로봇 yaw ±180° 전환 시점과 일치
- RF2O 노드 크래시 추정 (C++ rf2o_laser_odometry)
- respawn 미설정으로 복구 불가

### 2.3 VSLAM Tracking Loss (elapsed=42.5~50.4s)
- 7.9초 gap — RF2O 사망 직후, backup 동작 중
- 모션 블러 + 특징점 부족 추정

## 3. 수정사항

### 3.1 goal_pose_restamper.py — stamp=Time(0,0) 전환
```python
# 이전: msg.header.stamp = self.get_clock().now().to_msg()
# 현재:
msg.header.stamp = Time(sec=0, nanosec=0)  # TF2 latest available
```
- `Time(0,0)` = ROS2 TF2에서 "최신 가용 transform 사용"
- TF buffer eviction 문제를 원천적으로 해결
- Recovery delay와 무관하게 항상 TF 조회 성공

### 3.2 RF2O 노드 respawn 추가
```python
rf2o_node = Node(
    ...
    respawn=True,
    respawn_delay=2.0,
    ...
)
```
- RF2O 크래시 시 2초 후 자동 재시작
- EKF sensor_timeout=0.5초로 dropout 기간 자연스럽게 handling

### 3.3 RF2O Adapter 설정 수정
- `enable_vo_state_gating`: False (RF2O에 VSLAM status 없음)
- `max_yaw_delta`: 99.0 (anomaly detection 비활성)
- `max_pos_delta`: 99.0 (RF2O 자체 필터링 충분)
- `respawn=True, respawn_delay=2.0`

## 4. 센서 데이터 분석 (plot_data.csv)

### 4.1 토픽별 요약
| 토픽 | 샘플수 | 시간 범위 | Gap |
|---|---|---|---|
| /odom_rf2o_adapted | 279 | 6.2~34.1s | 34.1s에서 영구 사망 |
| /visual_slam/.../adapted | 8643 | 6.2~110.6s | 42.5~50.4s (7.9s gap) |
| /odom (raw wheel) | 3308 | 6.2~116.3s | 없음 |
| /odom_adapted (wheel) | 5503 | 6.2~116.3s | 없음 |

### 4.2 센서 장애 연쇄 패턴
```
t=34.1  RF2O 사망 (yaw ±180° 전환)
t=39.4  Planner 첫 TF 에러 (goal stamp evicted)
t=42.5  VSLAM tracking loss 시작
t=47~53 Nav2 backup 실행 (wheel+IMU only)
t=50.4  VSLAM tracking 복구
t=110.6 VSLAM 최종 정지
```

## 5. 수정 파일 목록
1. `jupiter_nav_VO/goal_pose_restamper.py` — stamp=Time(0,0)
2. `launch/nav2_vslam_fused.launch.py` — RF2O respawn + adapter 설정

## 6. 빌드 결과
- `colcon build --packages-select jupiter_nav_VO --symlink-install` → 성공 (4.29s)

## 7. 다음 단계
- [x] 실주행 재검증: 중거리(2m+) 목표 → planner replan 성공 확인 ✅
- [ ] RF2O respawn 동작 확인: `ros2 lifecycle` 또는 강제 kill로 검증
- [ ] RF2O 근본 원인 조사: 왜 yaw ±180° 시점에서 크래시하는지
  - rf2o_laser_odometry source code 분석
  - RPLidar scan 데이터 연속성 확인
- [ ] VSLAM tracking loss 빈도 기록
- [ ] transform_tolerance 3.0 → 0.3 축소 가능성 평가 (stamp=0 이후)

---

## 8. 2차 실주행 분석 (plot_data.csv #2)

### 8.1 데이터 요약
| 토픽 | 샘플수 | 시간 범위 | 상태 |
|---|---|---|---|
| /odom (EKF) | 6256 | 6.5~241.4s | ✅ |
| /odom_adapted (wheel) | 10397 | 6.5~241.4s | ✅ |
| /odom_rf2o_adapted | 2075 | 6.5~241.4s | ✅ 전 구간 동작 (respawn 확인) |
| VSLAM adapted | 2638 | 32.0~90.1s | ⚠️ 90초에서 사망 |

### 8.2 RF2O respawn 확인
- 1차 테스트에서 34.1초에 사망하던 RF2O가 **전 구간(241초) 동작**
- respawn=True 설정 효과 확인

### 8.3 VSLAM Frozen → 사망 패턴 발견
- t=64.3~87.0: cuVSLAM이 동일 qz=+0.993252를 23초간 반복 발행 (frozen)
- t=87.0~89.0: 잠시 복구 후 영구 tracking loss
- **frozen data detection** 추가하여 EKF에 stale 데이터 차단

### 8.4 odom_adapted 부호 반전 분석
- t=65+ 이후 wheel odom qz 부호가 다른 센서와 반대
- 원인: base.cpp의 `heading_ += delta_heading`에 [-π,π] 정규화 미적용
- EKF는 odom0에서 twist(vx,vy,vyaw)만 사용 → **기능 영향 없음** (표시 문제)

## 9. VSLAM Frozen Data Detection 구현

### 9.1 vslam_covariance_adapter.py 수정
- `_check_frozen()` 메서드 추가
- 연속 동일 pose 10회 이상 시 anomaly로 처리 → EKF에 stale 데이터 차단
- 새 파라미터: `frozen_threshold=10`, `frozen_pos_tolerance=1e-6`, `frozen_yaw_tolerance=1e-6`

## 10. 3차 실주행: 목표 도착 후 회전 진동 문제

### 10.1 현상
- 가까운 목표(~1m): 정상 도착
- 먼 목표(~2.7m, Goal 5): 도착 후 **75초간 360° 풀스핀 16회 반복**
- `Failed to make progress` 4회 → backup → 최종 127초 소요

### 10.2 근본 원인: DWB RotateToGoal 토글링
```
1. xy_goal_tolerance(0.10m) 진입 → RotateToGoal 활성
2. 제자리 회전 중 바퀴 슬립으로 XY 드리프트 → 0.10m 벗어남
3. RotateToGoal 비활성 → GoalDist/PathDist가 전진 명령
4. 다시 진입 → 다른 각도에서 RotateToGoal → 반복 스핀
```

### 10.3 부차 원인: 모터 데드존
- 차동구동 로봇의 정지 상태 순수 회전(vx=0, vθ≠0)은 양쪽 바퀴가 반대 방향으로 정지마찰을 동시에 극복 필요
- DWB slowing_factor 적용 후 angular.z ≈ 0.042 rad/s → 모터 데드존 진입 → 응답 없음
- 전진 중 회전은 이미 동마찰 상태이므로 정상 동작

### 10.4 수정사항 (nav2_params_fused.yaml)

#### A. SimpleProgressChecker 조정
| 파라미터 | 이전 | 변경 |
|---------|------|------|
| required_movement_radius | 0.5 | **0.10** |
| movement_time_allowance | 10.0 | **15.0** |

#### B. GoalChecker 완화
| 파라미터 | 이전 | 변경 |
|---------|------|------|
| xy_goal_tolerance | 0.10 | **0.15** |
| yaw_goal_tolerance | 0.12 | **0.35** (~20°) |

#### C. RotationShimController 도입 (핵심)
```yaml
FollowPath:
  plugin: "nav2_rotation_shim_controller::RotationShimController"
  primary_controller: "dwb_core::DWBLocalPlanner"
  angular_dist_threshold: 0.785    # 45° 이상이면 제자리 회전
  rotate_to_heading_angular_vel: 0.5  # 고정 0.5 rad/s (데드존 초과 보장)
```
- 정지 상태 제자리 회전을 DWB 대신 RotationShimController가 **고정 속도(0.5 rad/s)** 로 처리
- DWB는 이후 경로 추종만 담당

#### D. DWB Critics 개선
| 변경 | 설명 |
|------|------|
| DWB xy_goal_tolerance | 0.10→**0.25** (RotateToGoal 토글 방지) |
| min_speed_theta | 0.05→**0.30** (모터 데드존 초과 보장) |
| RotateToGoal.slowing_factor | 5.0→**2.0** |
| **TwirlingCritic** 추가 | scale=5.0, 불필요한 스핀 페널티 |
| **OscillationCritic** 강화 | reset_dist=0.15, reset_angle=0.30 |

### 10.5 ProgressChecker 대안 검토
| ProgressChecker | 상태 | 설명 |
|----------------|------|------|
| SimpleProgressChecker | 사용 중 | XY만 측정, Humble 유일 내장 |
| PoseProgressChecker | Humble 미지원 | Iron/Rolling에서 추가 (회전도 측정) |
| 커스텀 플러그인 | 가능 | C++ nav2_core::ProgressChecker 상속 |

## 12. 4차 실주행 분석 (plot_data.csv #3)

### 12.1 데이터 요약
| 토픽 | 샘플수 | 시간 범위 | 평균 Hz | 상태 |
|---|---|---|---|---|
| /odom (EKF) | 603 | 24.1~165.6s | ~4.3 | ✅ |
| /odom_adapted (wheel) | 945 | 5.7~165.0s | ~5.9 | ✅ |
| /odom_rf2o_adapted | 514 | 5.4~165.4s | ~3.2 | ✅ 전 구간 동작 |
| VSLAM adapted | 781 | 23.5~138.2s | ~6.8 | ⚠️ 다수 gap |

### 12.2 네비게이션 결과
- **Goal 1** (1.29, -0.37): 성공 ✅ (약 6초)
- **Goal 2** (1.92, -0.11): 성공 ✅ (약 3초)
- **Goal 3** (0.41, -0.29): 성공 ✅ (약 13초) — U턴 포함
- **Goal 4** (-0.32, -0.23): 성공 ✅ (약 4초)
- RotationShimController 도입 후 **회전 진동 없이 4개 목표 모두 성공**

### 12.3 VSLAM 불안정
- 3개 대형 gap: 33.4~66.7s (33s), 94.8~113.8s (19s), 120.7~133.4s (13s)
- 소규모 frozen: t=135.2~136.7 (5 samples, qz=0.989089)
- t=138.2에서 영구 종료 — 전체 세션 165초 중 **66초만 유효**
- frozen detection (threshold=10)이 동작 중이나, threshold=5로 낮추는 것 검토 필요

### 12.4 odom_adapted heading 부호 반전 재확인
- t=118.7부터 EKF(qz=-0.9999)와 odom_adapted(qz=+0.9987) 완전 반전
- heading_이 ±π 경계를 넘으면서 quaternion qz 부호가 뒤집힘
- diff=1.9986 (최대 2.0 — 정반대 방향)
- **→ base.cpp heading 정규화 적용** (12.5절)

### 12.5 센서 공통 gap 패턴
- 5개 구간에서 모든 센서가 동시에 데이터 부족: ~12-24s, ~26-33s, ~41-47s, ~56-65s, ~71-77s
- Foxglove 녹화 도구의 sampling 문제이거나, Jetson CPU 부하에 의한 publish 지연 추정

## 13. 로그 분석: Costmap 및 TF 문제

### 13.1 global_costmap "Sensor origin out of map bounds" (시작 후 ~30초)
```
Sensor origin at (-0.03, -0.00) is out of map bounds (-0.03, -3.71) to (2.70, 3.36)
```
- **원인**: 로봇 시작 위치(-0.03, 0.0)가 맵 경계의 min_x=-0.03과 정확히 일치
- global_costmap이 static_layer의 /map을 아직 완전히 로드하지 않은 초기 상태
- t≈64 (Goal 1 수신) 이후 costmap resize (83×144→102×146) 되면서 자동 해소
- **영향**: 초기 30초간 global_costmap에서 raytrace 미동작 → 동적 장애물 미감지
- **대응**: 실질적 문제 없음 (네비게이션 시작 전에 해소됨)

### 13.2 BT tick rate exceeded
```
Behavior Tree tick rate 100.00 was exceeded!
```
- Goal 수신 직후(1-2 tick) 반복 발생
- **원인**: Jetson CPU 부하로 BT tick이 10ms 내 완료 못함
- action server ack timeout과 연결: "Timed out while waiting for action server to acknowledge goal request for follow_path"
- **영향**: 첫 1-2초간 costmap clear + retry 후 정상 진행. Goal 성공에는 영향 없음

### 13.3 Extrapolation into the future (간헐적)
```
Extrapolation Error: Requested time 1772609270.502 but latest data at 1772609270.462
```
- Δt ≈ 40ms — EKF publish rate(30Hz=33ms) 대비 1 cycle 지연
- **원인**: controller_server가 path 수신 직후 TF 조회 → EKF 아직 해당 시점 TF 미발행
- **영향**: 해당 cycle만 skip, 다음 cycle에서 정상 처리. 실제 경로 추종 영향 없음

### 13.4 ⚠️ Message Filter dropping laser (Goal 4 이후, ~19초간)
```
Message Filter dropping message: frame 'laser' at time 1772609345.484
for reason 'the timestamp on the message is earlier than all the data in the transform cache'
```
- **시점**: Goal 4 완료(t≈135) 직후, t≈144~163에서 약 19초간 **모든 laser 메시지 버려짐**
- laser timestamp(1772609345~360)이 TF cache의 earliest data(1772609354)보다 과거
- **의미**: TF buffer가 flush/reset 되었고, laser 데이터가 ~3초 뒤늦게 도착
- global_costmap + local_costmap 모두 영향 → **19초간 장애물 감지 불가**

#### 근본 원인 추정
- SLAM Toolbox의 map→odom TF가 점프하면서 TF buffer 재구축
- 또는 Jetson CPU 과부하로 TF publisher가 일시 중단 후 재개
- laser scanner(RPLidar S2L) 자체 지연은 ~3초로 보이나, TF cache rebuild와 겹쳐 19초로 확대

### 13.5 Extrapolation into the past (t≈160)
```
Requested time 1772609347.797 but earliest data at 1772609354.072
```
- planner가 goal stamp(t=347.8)으로 TF 조회 → TF cache가 354.1부터만 보유
- **원인**: 13.4절의 TF buffer flush 후유증. cache가 재구축되면서 과거 데이터 소실
- **영향**: planner 1회 실패 후 다음 cycle에 최신 TF로 정상 동작

## 14. base.cpp heading 정규화 수정

### 14.1 문제
- `heading_ += delta_heading` 누적으로 heading이 ±π를 넘어감
- `tf2::Quaternion::setRPY(0, 0, heading_)` 에서 qz 부호가 [-π,π] 바깥에서 반전
- 예: heading=+3.2 → qz=+0.9997, heading=-3.2 → qz=-0.9997 (같은 물리적 방향)
- t=118.7부터 EKF와 odom_adapted qz 부호가 반대 (diff≈2.0)

### 14.2 수정
```cpp
// base.cpp L87-89
heading_ += delta_heading;
// [-π, π] 정규화
heading_ = std::atan2(std::sin(heading_), std::cos(heading_));
```
- `atan2(sin(h), cos(h))` → 항상 [-π, π] 범위 반환
- 성능 영향: sin/cos/atan2 3회 추가 호출 — velCallback은 ~20Hz → **무시 가능**
- `<cmath>` include 추가

### 14.3 영향 분석
- EKF는 odom0에서 twist(vx, vy, vyaw)만 사용 → heading 정규화는 EKF에 직접 영향 없음
- 하지만 odom_raw/odom_adapted의 pose.orientation이 정확해짐 → 디버깅/시각화 개선
- 향후 odom0에서 pose 데이터를 사용하게 될 경우 안전성 확보

## 15. 수정 파일 목록 (전체)
1. `jupiter_nav_VO/goal_pose_restamper.py` — stamp=Time(0,0)
2. `launch/nav2_vslam_fused.launch.py` — RF2O respawn + adapter 설정
3. `jupiter_nav_VO/vslam_covariance_adapter.py` — frozen data detection
4. `config/nav2_params_fused.yaml` — RotationShimController + DWB 파라미터 전면 개선
5. `jupiter_base/src/base.cpp` — heading [-π,π] 정규화 + `<cmath>` include
