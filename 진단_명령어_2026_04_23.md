# 명령어 로그 2026-04-23: jupiter_nvblox SIGABRT 크래시, RViz 마스킹 검증, SLAM↔nvblox Y축 정합 수정

> 진단 및 수정에 사용한 명령어와 스크립트/설정 파일을 요약 없이 전부 기록한다.
> 대상 launch: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py`
> 실행 환경: Docker 컨테이너 `isaac_ros_dev-aarch64-container` (Isaac ROS Dev 3.2, JetPack 6, Orin Nano 8GB)

---

## 0. 오늘 사용한 터미널 명령어 원문

### 0.1 Docker 컨테이너 상태 / 환경 확인

```bash
docker ps --format "table {{.Names}}\t{{.Image}}\t{{.Status}}"
ls /home/jetson/isaac_ws_ros/src/isaac_ros_common/scripts/
docker exec isaac_ros_dev-aarch64-container bash -c 'echo "RMW_IMPLEMENTATION=$RMW_IMPLEMENTATION"; echo "ROS_DOMAIN_ID=$ROS_DOMAIN_ID"'
ls /opt/ros/
which ros2
docker exec isaac_ros_dev-aarch64-container bash -c 'echo test; ls /opt/ros/'
```

목적: 도커 컨테이너 실행 여부, ROS2 humble 설치 상태, RMW 설정(fastrtps_cpp) 확인.
결과: 컨테이너 Up 41분, ROS_DOMAIN_ID=0, RMW=rmw_fastrtps_cpp.

---

### 0.2 ROS 프로세스 및 노드 상태 확인

```bash
docker exec isaac_ros_dev-aarch64-container bash -c "source /workspaces/isaac_ros-dev/install/setup.bash 2>/dev/null; ros2 node list 2>&1"
docker exec isaac_ros_dev-aarch64-container bash -c "source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash 2>/dev/null; ros2 node list"
docker exec isaac_ros_dev-aarch64-container bash -c 'ps aux | grep -E "ros2|component_container|nvblox" | grep -v grep'
docker exec isaac_ros_dev-aarch64-container bash -c 'ps -ef | grep -E "component_container|jupiter_nvblox|realsense|nvblox_node" | grep -v grep'
pgrep -af "component_container|jupiter_nvblox|nvblox_node|realsense"
pgrep -af "component_container|jupiter_nvblox|nvblox_node|realsense2"
```

목적: launch가 실행중인지 확인.
결과: 초기엔 `install_peoplesemsegnet_vanilla.sh --eula` 만 동작, nvblox launch 미실행 상태.

---

### 0.3 필수 의존 패키지 확인

```bash
docker exec isaac_ros_dev-aarch64-container bash -c 'ls /workspaces/isaac_ros-dev/install/ | wc -l; echo "---"; ls /workspaces/isaac_ros-dev/install/ | grep -E "image_proc|tensor_proc|tensor_rt|isaac_ros_unet|realsense2_camera"'
docker exec isaac_ros_dev-aarch64-container bash -c 'dpkg -l | grep -E "ros-humble-(isaac-ros-image-proc|isaac-ros-tensor-proc|isaac-ros-tensor-rt|isaac-ros-unet|realsense2-camera|isaac-ros-peoplesemseg)"'
```

목적: launch가 참조하는 ComposableNode 의존 패키지가 apt/colcon 중 어디에 설치됐는지 확인.
결과:
- apt: `ros-humble-isaac-ros-image-proc 3.2.10`, `isaac-ros-tensor-proc 3.2.10`, `isaac-ros-tensor-rt 3.2.10`, `isaac-ros-unet 3.2.10`, `realsense2-camera 4.51.1`, `isaac-ros-peoplesemseg-models-install 3.2.5`
- colcon: `jupiter_nvblox`, `nvblox_ros`, `nvblox_nav2` 등 설치됨

---

### 0.4 모델 파일 존재 확인

```bash
docker exec isaac_ros_dev-aarch64-container bash -c 'ls -la /workspaces/isaac_ros-dev/isaac_ros_assets/models/peoplesemsegnet/; echo "---"; find /workspaces/isaac_ros-dev/isaac_ros_assets/ -name "model.plan"'
```

목적: ShuffleSeg 모델 `.plan` 파일 존재 확인.
결과:
- `/workspaces/isaac_ros-dev/isaac_ros_assets/models/peoplesemsegnet/optimized_deployable_shuffleseg_unet_amr_v1.0/1/model.plan` 존재
- `/workspaces/isaac_ros-dev/isaac_ros_assets/models/peoplesemsegnet/deployable_quantized_vanilla_unet_onnx_v2.0/1/model.plan` 존재
- `/workspaces/isaac_ros-dev/isaac_ros_assets/models/peoplenet/rsu_rs_480_640_mask/1/model.plan` 존재

---

### 0.5 RealSense USB / /dev 확인

```bash
lsusb | grep -i "intel\|realsense"
docker exec isaac_ros_dev-aarch64-container bash -c 'ls /dev/video* 2>&1; lsusb 2>&1 | grep -i "intel\|realsense"'
docker exec isaac_ros_dev-aarch64-container bash -c 'lsusb | grep -i intel; ls /dev/video*'
```

목적: RealSense D455f 인식 여부.
결과: `Bus 002 Device 003: ID 8086:0b5c Intel Corp. Intel(R) RealSense(TM) Depth Camera 455f`, `/dev/video0 ~ /dev/video5` 노출.

---

### 0.6 호스트 ROS / jupiter_ws_ros2 상태

```bash
source /opt/ros/humble/setup.bash 2>/dev/null && timeout 5 ros2 node list
source /opt/ros/humble/setup.bash && source /home/jetson/jupiter_ws_ros2/install/setup.bash 2>/dev/null; timeout 3 ros2 node list
source /opt/ros/humble/setup.bash && timeout 5 ros2 daemon start 2>&1; timeout 5 ros2 node list
source /opt/ros/humble/setup.bash && timeout 5 ros2 topic list
```

목적: 호스트측 ROS 시스템 실행 여부. `nav2_vslam_fused.launch.py` 가 동작 중이면 TF를 공급받을 수 있음.
결과: 호스트 ROS daemon만 실행, `/parameter_events`, `/rosout` 외 노드 없음 → 정상 TF 트리 없음.

---

### 0.7 Static TF 퍼블리시 (격리 테스트용)

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
ros2 run tf2_ros static_transform_publisher 0 0 0 0 0 0 odom base_footprint &
ros2 run tf2_ros static_transform_publisher 0 0 0.08 0 0 0 base_footprint base_link &
ros2 run tf2_ros static_transform_publisher 0.205 0 0.098 0 0 0 base_link camera0_link &
sleep 2
echo "TF published"
'
```

목적: jupiter_ws_ros2 없이 독립 테스트. URDF 수치 기반.
결과: TF 트리 정상 구축.

재구성(nav2_vslam_fused.launch.py 와 동일한 체인):

```bash
docker exec -d isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
ros2 run tf2_ros static_transform_publisher 0 0 0 0 0 0 map odom &
ros2 run tf2_ros static_transform_publisher 0 0 0 0 0 0 odom base_footprint &
ros2 run tf2_ros static_transform_publisher 0 0 0.08 0 0 0 base_footprint base_link &
ros2 run tf2_ros static_transform_publisher 0.205 0 0.098 0 0 0 base_link camera_link &
ros2 run tf2_ros static_transform_publisher --frame-id camera_link --child-frame-id camera0_link &
wait
'
```

목적: `global_frame: "map"` 적용 후 전체 TF 체인(map→odom→base_footprint→base_link→camera_link→camera0_link) 공급.

---

### 0.8 TF 체인 검증

```bash
docker exec isaac_ros_dev-aarch64-container bash -c 'source /opt/ros/humble/setup.bash && timeout 3 ros2 run tf2_ros tf2_echo odom base_link 2>&1 | head -10'
docker exec isaac_ros_dev-aarch64-container bash -c 'source /opt/ros/humble/setup.bash && timeout 3 ros2 run tf2_ros tf2_echo odom camera0_depth_optical_frame 2>&1 | head -5'
```

목적: `odom → camera0_depth_optical_frame` 변환 가능 여부 (nvblox가 요구하는 체인).
결과:
- `odom → base_link`: Translation `(0.000, 0.000, 0.080)` ✅
- `odom → camera0_depth_optical_frame`: Translation `(0.205, 0.000, 0.178)`, Quaternion `(-0.500, 0.500, -0.500, 0.500)` (표준 optical 회전) ✅

---

### 0.9 jupiter_nvblox launch 실행

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash
ros2 launch jupiter_nvblox jupiter_nvblox.launch.py 2>&1
' > /tmp/jupiter_nvblox_launch.log 2>&1 &
```

2차 실행 (v2 설정 반영 후):

```bash
docker exec -d isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash
exec ros2 launch jupiter_nvblox jupiter_nvblox.launch.py > /tmp/nvblox_v2_verify.log 2>&1
'
```

목적: 전체 파이프라인(RealSense → Resize → TensorRT → UNet → nvblox) 구동.
결과: 초기 런은 정상 20분+ 동작, 모든 rate statistics 정상.

---

### 0.10 토픽 목록 / 타입 조회

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
ros2 topic list | grep -E "segmentation|mask|nvblox|camera0" 2>&1
'

docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
ros2 topic list -t | grep -E "combined_map_slice|combined_occupancy|static_map_slice|static_occupancy|combined_esdf_pointcloud|static_esdf_pointcloud|dynamic" 2>&1
'
```

목적: nvblox 출력 토픽 식별 + 타입 매트릭스 확보.
결과 (핵심):
- `/nvblox_node/combined_map_slice` → `nvblox_msgs/msg/DistanceMapSlice` (커스텀)
- `/nvblox_node/combined_occupancy_grid` → `nav_msgs/msg/OccupancyGrid` (표준, RViz 가능)
- `/nvblox_node/static_esdf_pointcloud` → `sensor_msgs/msg/PointCloud2`
- `/camera0/segmentation/people_mask` → `sensor_msgs/msg/Image`
- `/camera0/unet/colored_segmentation_mask` → `sensor_msgs/msg/Image`

---

### 0.11 토픽 rate 측정

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
echo "=== people_mask hz ==="
timeout 5 ros2 topic hz /camera0/segmentation/people_mask 2>&1 | tail -3
echo "=== combined_map_slice hz ==="
timeout 5 ros2 topic hz /nvblox_node/combined_map_slice 2>&1 | tail -3
echo "=== static_esdf_pointcloud hz ==="
timeout 5 ros2 topic hz /nvblox_node/static_esdf_pointcloud 2>&1 | tail -3
echo "=== colored_segmentation_mask hz ==="
timeout 5 ros2 topic hz /camera0/unet/colored_segmentation_mask 2>&1 | tail -3
'

docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
echo "=== static_occupancy_grid (nav_msgs) hz ==="
timeout 5 ros2 topic hz /nvblox_node/static_occupancy_grid 2>&1 | tail -3
echo "=== combined_occupancy_grid hz ==="
timeout 5 ros2 topic hz /nvblox_node/combined_occupancy_grid 2>&1 | tail -3
echo "=== static_occupancy_grid first message origin ==="
timeout 3 ros2 topic echo --once --field info /nvblox_node/static_occupancy_grid 2>&1 | head -25
'

docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
echo "=== Current rates (after reconnect) ==="
timeout 5 ros2 topic hz /camera0/depth/image_rect_raw 2>&1 | tail -2
timeout 5 ros2 topic hz /camera0/color/image_raw 2>&1 | tail -2
timeout 5 ros2 topic hz /camera0/segmentation/people_mask 2>&1 | tail -2
timeout 5 ros2 topic hz /nvblox_node/combined_occupancy_grid 2>&1 | tail -2
'
```

목적: 파이프라인 각 단계 정상 발행 빈도 측정.
결과:
- `/camera0/segmentation/people_mask`: **14.99 Hz** ✅
- `/camera0/unet/colored_segmentation_mask`: **14.99 Hz** ✅
- `/nvblox_node/combined_occupancy_grid`: **4.92 Hz** ✅
- `/nvblox_node/static_occupancy_grid`: **4.87 Hz** ✅
- `/nvblox_node/combined_map_slice`: **ros2 cli가 커스텀 타입 `nvblox_msgs/msg/DistanceMapSlice` 미해결** (host에 nvblox_msgs 미설치)
- `/camera0/depth/image_rect_raw`: 30 Hz (복구 후), `/camera0/color/image_raw`: 14.4 Hz

---

### 0.12 OccupancyGrid / DistanceMapSlice origin 필드 확인

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
echo "=== static_occupancy_grid origin (full) ==="
timeout 3 ros2 topic echo --once /nvblox_node/static_occupancy_grid 2>&1 | head -30
'

docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash
echo "=== static_map_slice origin ==="
timeout 5 ros2 topic echo --once --field origin /nvblox_node/static_map_slice 2>&1 | head -15
echo "=== combined_map_slice origin ==="
timeout 5 ros2 topic echo --once --field origin /nvblox_node/combined_map_slice 2>&1 | head -15
echo "=== static_esdf_pointcloud header ==="
timeout 5 ros2 topic echo --once --field header /nvblox_node/static_esdf_pointcloud 2>&1 | head -10
'
```

목적: 2D 지도 origin.z 실측값 확인.
결과:
- `static_occupancy_grid.info.origin`: `x=0.0, y=-2.56, z=0.0`, `frame_id=odom` (변경 전) / `frame_id=map` (변경 후)
- `static_map_slice.origin.z`: `0.0`
- `combined_map_slice.origin.z`: `0.0`
- `static_esdf_pointcloud.header.frame_id`: `odom`(변경 전) / `map`(변경 후)

---

### 0.13 nvblox 파라미터 GET

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash
echo "=== nvblox_node parameters (selected) ==="
ros2 param list /nvblox_node 2>&1 | grep -E "slice|workspace|clearing|cuVSLAM|global_frame|exclusion" | head -15
echo ""
echo "=== slice heights ==="
ros2 param get /nvblox_node static_mapper.esdf_slice_height 2>&1
ros2 param get /nvblox_node static_mapper.esdf_slice_min_height 2>&1
ros2 param get /nvblox_node static_mapper.esdf_slice_max_height 2>&1
ros2 param get /nvblox_node static_mapper.workspace_bounds_type 2>&1
ros2 param get /nvblox_node static_mapper.workspace_bounds_min_height_m 2>&1
ros2 param get /nvblox_node static_mapper.workspace_bounds_max_height_m 2>&1
'

docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
ros2 param get /nvblox_node global_frame 2>&1
ros2 param get /nvblox_node pose_frame 2>&1
'
```

목적: YAML config 값이 노드에 실제 반영되었는지 검증.
결과:
- 변경 전: `workspace_bounds_type: unbounded`, `global_frame: odom`, `esdf_slice_height: 0.0`
- 변경 후: `workspace_bounds_type: height_bounds`, `workspace_bounds_min_height_m: -0.2`, `workspace_bounds_max_height_m: 2.0`, `global_frame: map`, `esdf_slice_height: 0.0`

---

### 0.14 토픽 QoS / 메시지 구조 검증

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
echo "▶ /camera0/unet/colored_segmentation_mask"
ros2 topic info -v /camera0/unet/colored_segmentation_mask 2>&1 | head -20
echo ""
echo "▶ /camera0/segmentation/people_mask"
ros2 topic info -v /camera0/segmentation/people_mask 2>&1 | head -10
'

docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
echo "▶ /nvblox_node/dynamic_color_frame_overlay"
ros2 topic info -v /nvblox_node/dynamic_color_frame_overlay 2>&1 | grep -E "Type:|Publisher count|Reliability|Durability"
'

docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
timeout 4 ros2 topic echo --once /camera0/unet/colored_segmentation_mask 2>&1 | grep -E "encoding|height|width|step|is_bigendian|frame_id" | head -10
'
```

목적: RViz Image 디스플레이 호환성 확인 (QoS, 인코딩).
결과:
- Pub RELIABLE / VOLATILE + RViz config Sub BEST_EFFORT / VOLATILE → 호환 (sub 다운그레이드)
- `encoding: rgb8`, `height: 544`, `width: 960`, `step: 2880`, `frame_id: camera0_color_optical_frame` → RViz 렌더 가능

---

### 0.15 ESDF 포인트클라우드 Z값 샘플 확인

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash
echo "=== static_esdf_pointcloud (first sample with x,y,z) ==="
timeout 6 ros2 topic echo --once /nvblox_node/static_esdf_pointcloud 2>&1 | head -50
'
```

목적: ESDF 슬라이스가 실제 `esdf_slice_height=0.0` 에 위치하는지 확인.
결과: 첫 점 바이트 `x=82,184,30,64` (float ≈ 2.48), `y=153,153,153,191` (≈ -1.2), `z=0,0,0,0` (≈ 0.0) ✅

---

### 0.16 SIGABRT 크래시 재현 시도 및 실제 크래시 로그 추출

```bash
# 프로세스 정리 (클린 상태)
docker exec isaac_ros_dev-aarch64-container bash -c 'pkill -9 -f "ros2 launch" 2>/dev/null; pkill -9 -f "static_transform" 2>/dev/null; pkill -9 -f "component_container_mt" 2>/dev/null; sleep 2; pgrep -af "component_container|jupiter_nvblox|static_transform"'

# 정상 launch 재실행 (nav2_vslam_fused.launch.py와 동일한 TF 체인)
docker exec -d isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
ros2 run tf2_ros static_transform_publisher 0 0 0 0 0 0 odom base_footprint &
ros2 run tf2_ros static_transform_publisher 0 0 0.08 0 0 0 base_footprint base_link &
ros2 run tf2_ros static_transform_publisher 0.205 0 0.098 0 0 0 base_link camera_link &
ros2 run tf2_ros static_transform_publisher --frame-id camera_link --child-frame-id camera0_link &
wait
'
sleep 3
docker exec -d isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash
exec ros2 launch jupiter_nvblox jupiter_nvblox.launch.py > /tmp/nvblox_crash_test.log 2>&1
'

# 크래시 / 정상 시작 이벤트 대기
until grep -q "nvblox_node.*Rates statistics\|exit code\|process has died\|terminate called\|Aborted" /tmp/nvblox_crash_test.log 2>/dev/null; do sleep 3; done; echo "---EVENT DETECTED---"; tail -50 /tmp/nvblox_crash_test.log

# 실제 사용자 크래시 로그 확인
docker exec isaac_ros_dev-aarch64-container bash -c 'ls -la /home/admin/.ros/log/ 2>&1 | tail -20'
docker exec isaac_ros_dev-aarch64-container bash -c 'tail -80 /home/admin/.ros/log/component_container_mt_303683_1776919600267.log'
docker exec isaac_ros_dev-aarch64-container bash -c 'tail -30 /home/admin/.ros/log/2026-04-23-12-46-40-058750-yahboom-303628/launch.log'
```

목적: 사용자가 보고한 `exit code -6` (SIGABRT) 원인 추적.
결과 (결정적 로그):

```
1776919602.2892258 [component_container_mt-1] terminate called after throwing an instance of 'std::filesystem::__cxx11::filesystem_error'
1776919602.2893913 [component_container_mt-1]   what():  filesystem error: cannot create directories: Permission denied [/tmp/isaac_ros_nitros/graphs/ZTIRUUSVOX]
1776919604.0519927 [ERROR] [component_container_mt-1]: process has died [pid 303683, exit code -6, cmd '/opt/ros/humble/lib/rclcpp_components/component_container_mt ...'].
```

→ 근본 원인: NITROS GXF 그래프 캐시 서브디렉토리 생성 실패 (권한).

---

### 0.17 NITROS 캐시 디렉토리 권한 확인/수정

```bash
docker exec isaac_ros_dev-aarch64-container bash -c 'ls -la /tmp/isaac_ros_nitros/ 2>&1; echo "---"; ls -la /tmp/isaac_ros_nitros/graphs/ 2>&1 | head -10; echo "---"; id; echo "---"; stat /tmp/isaac_ros_nitros/graphs/ 2>&1 | head -5'
docker exec isaac_ros_dev-aarch64-container bash -c 'chmod -R 1777 /tmp/isaac_ros_nitros 2>&1; ls -la /tmp/isaac_ros_nitros/'
```

목적: `/tmp/isaac_ros_nitros/graphs/` 권한 검증 및 sticky-world-writable 설정.
결과:
- 수정 전: `drwxr-xr-x 14 root root 4096 ... graphs` (admin 사용자는 서브디렉토리 생성 불가)
- 수정 후: `drwxrwxrwt 14 root root 4096 ... graphs` (1777, sticky bit)

---

### 0.18 nvblox 소스 분석 (grep)

```bash
grep -rn "esdf_slice\|slice_height\|slice_output_origin" /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/include/nvblox_ros/node_params.hpp
grep -rn "global_frame\|pose_frame\|map_frame" /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/include/nvblox_ros/node_params.hpp
grep -rn "color_layer\|layer_type\|LayerType\|VoxelBlockLayer\|publishLayer\|sendLayer" /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/nvblox_node.cpp
grep -rn "frame_id\|global_frame_" /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/nvblox_node.cpp

grep -rn "mask_image_callback\|MaskImage\|processMask\|mask_topic\|MASK_TOPIC\|\"mask\"" /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/*.cpp /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/include/nvblox_ros/*.hpp

grep -rn "workspace_bounds_type\|workspace_bounds\|kUnbounded\|kHeightBounds\|workspaceBoundsType\|stringToWorkspace" /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/nvblox_core/nvblox/src/geometry/ /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/nvblox_core/nvblox/include/
grep -rn "stringToWorkspace\|workspace_bounds_type_strings\|parseWorkspaceBoundsType\|\"height_bounds\"\|workspace.*bounds.*type.*string" /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/
grep -rn "to_string.*WorkspaceBoundsType\|WorkspaceBoundsType.*to_string" /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/nvblox_core/nvblox/

grep -rn "layer_visualization_exclusion_height\|layer_visualization_exclusion_radius\|exclusion_height" /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/*.cpp /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/nvblox_core/nvblox/src/*.cpp

grep -n "exclusion_center\|getBlockIndices\|BlockFilter" /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/layer_publishing.cpp
grep -n "setMessage\|position\|block_origin\|block_position\|origin\|Vector3f" /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_rviz_plugin/src/nvblox_plugin_visual.cpp
```

목적:
- `esdf_slice_height` 가 어느 프레임에서 해석되는지 규명
- `combined_occupancy_grid.origin.z` 가 하드코딩인지 설정 가능한지 규명
- `workspace_bounds_type` 파서 동작 파악
- `nvblox_rviz_plugin` 에서 voxel 위치를 어떻게 그리는지 파악

결과 (핵심 소스 라인):
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/nvblox_node.cpp:902`
  ```cpp
  occupancy_grid_msg.info.origin.position.z = 0;   // 하드코딩
  ```
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/conversions/esdf_slice_conversions.cu:159`
  ```cpp
  map_slice->origin.z = slice_height;              // esdf_slice_height 반영
  ```
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/nvblox_core/nvblox/src/geometry/workspace_bounds.cpp:25-28`
  ```cpp
  case WorkspaceBoundsType::kUnbounded:
    *output_aabb = input_aabb;     // bounds 무시
    return !output_aabb->isEmpty();
  ```
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/include/nvblox_ros/node_params.hpp:45`:
  ```cpp
  "global_frame", "odom",   // 기본값 "odom"
  ```
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/mapper_initialization.cpp:91-109`: `workspace_bounds_type_from_string()` — `"unbounded"`, `"height_bounds"`, `"bounding_box"` 만 허용, 기본 to_string은 `"kUnbounded"` (prefix bug)

---

### 0.19 관련 프로세스 상태

```bash
docker exec isaac_ros_dev-aarch64-container bash -c 'pgrep -af "component_container_mt" | head'
docker exec isaac_ros_dev-aarch64-container bash -c 'pgrep -af "jupiter|static_transform|component_container" 2>&1'
docker exec isaac_ros_dev-aarch64-container bash -c '
kill -9 88771 88785 88786 88787 88788 88789 88790 2>/dev/null
sleep 1
pgrep -af "static_transform" 2>&1 | head
'
docker exec isaac_ros_dev-aarch64-container bash -c 'pkill -9 -f "component_container_mt" 2>/dev/null; pkill -9 -f "static_transform_publisher" 2>/dev/null; pkill -9 -f "jupiter_nvblox" 2>/dev/null'
docker exec isaac_ros_dev-aarch64-container bash -c 'pgrep -af "static_transform" 2>&1; echo "---"; timeout 3 ros2 run tf2_ros tf2_echo odom camera0_depth_optical_frame 2>&1 | head -5'
docker exec isaac_ros_dev-aarch64-container bash -c 'pkill -INT -f component_container_mt 2>/dev/null; pkill -9 -f static_transform_publisher 2>/dev/null; sleep 2; echo cleanup done'
```

목적: 기존 테스트 잔재 정리 및 재시작.

---

### 0.20 시스템 메모리 / GPU 상태

```bash
nvidia-smi --query-gpu=memory.total,memory.used,memory.free --format=csv
free -m | head -3
cat /sys/devices/gpu.0/load 2>/dev/null || echo "no load info"
cat /proc/meminfo | head -6
docker stats --no-stream isaac_ros_dev-aarch64-container
```

목적: OOM 가능성 체크.
결과:
- RAM: 7.62 GB, Used 4.87 GB, Available 2.48 GB
- Swap: 12 GB total, 71 MB used
- Container: 1.1 GiB / 7.44 GiB (14.77%), CPU 45.83%

---

### 0.21 launch 로그 패턴 grep

```bash
grep -B3 -A20 "exit code -6\|SIGABRT\|terminate called\|Aborted\|std::bad_alloc\|assert\|CUDA error\|GPU" /tmp/jupiter_nvblox_launch.log /tmp/jupiter_nvblox_v2.log /tmp/nvblox_loop.log
grep -iE "error|warn|fail|lookup transform failed|timeout|cant" /tmp/nvblox_crash_test.log
tail -30 /tmp/nvblox_crash_test.log
tail -100 /tmp/jupiter_nvblox_launch.log
```

목적: 크래시/에러/경고 패턴 추출.
결과:
- 초기 WARN `Lookup transform failed for frame base_link. Layer pointclouds not published` (TF 초기 안정화 전)
- `Requested workspace bounds type: "kUnbounded" not recognized. Defaulting to: kUnbounded` (dynamic_mapper 기본값 미매칭 - 기능에 영향 없음)
- 정상 런은 `Rates statistics` 가 10초마다 발행

---

### 0.22 jupiter_ws_ros2 구성 확인

```bash
grep -n -A2 "slam_toolbox\|map_frame\|SlamToolbox\|slam_params" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
find /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config -name "*.yaml"
find /home/jetson/jupiter_ws_ros2/src/jupiter_nav -name "*slam*.yaml"

grep -E "map_frame|odom_frame|base_frame|scan_topic|map_file_name|map_start_pose|resolution|max_laser_range" /home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml
grep -E "base_link_frame|world_frame|odom_frame|publish_tf|two_d_mode" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml
grep -B2 -A4 "base_link\|base_footprint" /home/jetson/jupiter_ws_ros2/src/jupiter_description/urdf/jupiter_simple.urdf
grep -A3 "camera_link\|camera_joint\|camera0" /home/jetson/jupiter_ws_ros2/src/jupiter_description/urdf/jupiter_simple.urdf
grep -B1 -A6 "laser_joint\|<link name=\"laser\"" /home/jetson/jupiter_ws_ros2/src/jupiter_description/urdf/jupiter_simple.urdf
```

목적: SLAM Toolbox frame 설정, URDF TF 체인 확인.
결과:
- `slam_params.yaml`: `odom_frame: odom`, `map_frame: map`, `base_frame: base_footprint`, `resolution: 0.05`
- `ekf_vslam_fusion.yaml`: `two_d_mode: true`, `base_link_frame: base_footprint`, `publish_tf: true` → EKF는 `odom → base_footprint` 발행, z 항상 0
- URDF `base_footprint → base_link`: `(0, 0, 0.08)`
- URDF `base_link → camera_link`: `(0.205, 0, 0.098)` (활성값, 이전 주석 `0.274,0,0.055` 는 잘못됨)
- URDF `base_link → laser`: `(-0.03, 0, 0.13), rpy=(0 0 π)` (후방 설치)

---

### 0.23 빌드

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
cd /workspaces/isaac_ros-dev
source /opt/ros/humble/setup.bash
colcon build --packages-select jupiter_nvblox --symlink-install 2>&1 | tail -10
'

docker exec isaac_ros_dev-aarch64-container bash -c '
grep -A2 "combined_map_slice\|combined_occupancy_grid" /workspaces/isaac_ros-dev/install/jupiter_nvblox/share/jupiter_nvblox/config/jupiter_nvblox.rviz | head -5
'

cd /home/jetson/isaac_ws_ros && colcon build --packages-select jupiter_nvblox --symlink-install 2>&1 | tail -3
```

목적: `config/` 및 `launch/` 변경사항을 docker install 공간에 반영 (symlink-install).
결과: `Finished <<< jupiter_nvblox [0.26s] Summary: 1 package finished`.

---

### 0.24 정리 / 종료

```bash
docker exec isaac_ros_dev-aarch64-container bash -c 'pkill -9 -f "component_container_mt" 2>/dev/null; pkill -9 -f "static_transform_publisher" 2>/dev/null; pkill -9 -f "jupiter_nvblox" 2>/dev/null; sleep 1; echo "cleanup done"; pgrep -af "component_container|static_transform|jupiter_nvblox" 2>&1 | head'
```

---

## 1. 분석에 사용한 설정 / 스크립트 파일

### 1.0 오늘 사용/열람한 스크립트 및 파일 전체 경로

- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py` ← 수정
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml` ← 수정
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz` ← 수정
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_realsense.yaml`
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/CMakeLists.txt`
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/package.xml`
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/nvblox_node.cpp` (열람)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/layer_publishing.cpp` (열람)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/mapper_initialization.cpp` (열람)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/include/nvblox_ros/node_params.hpp` (열람)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/nvblox_core/nvblox/src/geometry/workspace_bounds.cpp` (열람)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/nvblox_core/nvblox/include/nvblox/geometry/workspace_bounds.h` (열람)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_rviz_plugin/src/nvblox_plugin_visual.cpp` (열람)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_rviz_plugin/src/nvblox_plugin_display.cpp` (열람)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` (열람 – TF 체인 참조)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml` (열람)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml` (열람)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_description/urdf/jupiter_simple.urdf` (열람)
- `/home/jetson/isaac_ws_ros/진행과정_nvblox_integration.md` (열람)
- `/home/admin/.ros/log/component_container_mt_303683_1776919600267.log` (사용자 크래시 로그)
- `/home/admin/.ros/log/2026-04-23-12-46-40-058750-yahboom-303628/launch.log` (사용자 launch 로그)

---

### 1.1 수정된 파일 1 — launch

- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py`

수정 항목:
- `import stat` 추가
- 함수 `_ensure_nitros_cache_writable()` 추가 (기능: `/tmp/isaac_ros_nitros/graphs` 및 상위 디렉토리를 `0o1777` 로 설정)
- `generate_launch_description()` 최상단에서 `_ensure_nitros_cache_writable()` 호출

---

### 1.2 수정된 파일 2 — nvblox config

- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml`

변경 항목:
- `global_frame: "map"` 추가 (기본 "odom" 에서 변경)
- `pose_frame: "base_link"` 명시 (기본값이지만 명시)
- `static_mapper.workspace_bounds_type: "height_bounds"` (기존 "unbounded")
- `static_mapper.workspace_bounds_min_height_m: -0.2` (기존 -0.5)
- `static_mapper.workspace_bounds_max_height_m: 2.0` (유지)
- 주석 추가: OccupancyGrid origin.z 하드코딩 관련 설명

---

### 1.3 수정된 파일 3 — RViz config

- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz`

변경 항목:
- `Combined Map Slice` 디스플레이 토픽: `/nvblox_node/combined_map_slice` → `/nvblox_node/combined_occupancy_grid`

---

## 2. 관찰 기준 요약

| 항목 | 기준 | 실측 |
|---|---|---|
| `/camera0/segmentation/people_mask` Hz | ≥ 10 | 14.99 |
| `/camera0/unet/colored_segmentation_mask` Hz | ≥ 10 | 14.99 |
| `/camera0/depth/image_rect_raw` Hz | ≥ 15 | 30.0 |
| `/camera0/color/image_raw` Hz | ≥ 10 | 14.4 |
| `/nvblox_node/combined_occupancy_grid` Hz | ≥ 4 | 4.92 |
| `/nvblox_node/combined_occupancy_grid.info.origin.z` | 0.0 | 0.0 |
| `/nvblox_node/combined_occupancy_grid.header.frame_id` | map | map |
| nvblox `global_frame` param | map | map |
| nvblox `static_mapper.workspace_bounds_type` param | height_bounds | height_bounds |
| `/tmp/isaac_ros_nitros/graphs` 권한 | 1777 (drwxrwxrwt) | 1777 |
| 크래시 카운트 (Error/terminate/SIGABRT) | 0 | 0 |

---

## 3. 추가 세션 — 공식 예시 비교 및 마스크 가시성 / rate 정합 검증

> 배경: (a) `jupiter_nvblox.rviz` 에서 빨간 세그멘테이션 마스크가 보이지 않음, (b) 공식 `ros2 launch nvblox_examples_bringup realsense_example.launch.py mode:=people_detection` 에서는 빨간 마스크가 잘 보임. 두 경우의 차이 분석 후 우리 설정에 이식. 추가로 "마스크가 dynamic_overlay 에만 걸리고 depth/voxels 에는 안 나옴", "마스크 확률이 떨어지는 느낌" 이라는 두 번째 문제를 정량적으로 분석.

### 3.0 공식 예시 실행 및 토픽/QoS 비교

```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch nvblox_examples_bringup realsense_example.launch.py mode:=people_detection'
```
목적: 공식 예시가 사용하는 rviz 파일, 토픽, QoS 확인 (빨간 마스크 reference 동작 확보).
결과: rviz 상에 빨간 오버레이 표시됨. 공식 rviz 파일 경로 `nvblox_examples_bringup/rviz/realsense_humans_example.rviz` 가 사용됨 (launch 인자 출력 기준).

```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 topic info -v /nvblox_node/dynamic_color_frame_overlay'
```
목적: publisher 측 QoS 프로파일 (Reliability, Durability, History, Depth) 확인.
결과 토막:
```
Publisher count: 1
QoS profile:
  Reliability: RELIABLE
  Durability: VOLATILE
  History (Depth): KEEP_LAST (5)
```
→ publisher 는 Reliable. Subscriber 가 Best Effort 로 걸려 있으면 QoS incompatible 는 아니지만, 2MB 크기 `rgba8` 이미지의 UDP 단편화 drop 이 발생해 수신률이 0 에 가까움 (경험적으로 확인).

### 3.1 BE vs Reliable 수신율 비교 (마스크 미표시 원인 규명)

```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 3 ros2 topic echo --qos-reliability best_effort \
     /nvblox_node/dynamic_color_frame_overlay --no-arr --field header.stamp | grep -c "sec:"'
```
목적: 기본(Best Effort) 구독 시 실제 수신 메시지 수 측정.
결과: `0` (3초간 0 메시지 수신).

```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 3 ros2 topic echo --qos-reliability reliable \
     /nvblox_node/dynamic_color_frame_overlay --no-arr --field header.stamp | grep -c "sec:"'
```
목적: Reliable 구독 시 수신 메시지 수 측정.
결과: `5` (3초간 5 메시지 수신, ≈ 1.7 Hz).
→ 기존 rviz config 의 `Dynamic Overlay` 패널이 Best Effort 로 구독하고 있어 빨간 마스크가 나타나지 않던 근본 원인.

### 3.2 Mask가 depth / voxels 에 표시되지 않는 원인 규명

```bash
grep -nE 'Depth Overlay|People Voxels|dynamic_depth_frame_overlay|dynamic_occupancy_layer' \
  /home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz
```
목적: 기존 rviz 에 Depth Overlay / People Voxels 패널이 있는지 확인.
결과: 매치 없음. **rviz config 에 해당 패널이 아예 정의되어 있지 않음** — 토픽을 구독하지 않으니 보일 리 없음.

```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 topic list | grep -E "dynamic_(depth|occupancy)"'
```
목적: nvblox 가 실제로 어떤 dynamic 토픽을 발행 중인지 확인.
결과 토막:
```
/nvblox_node/dynamic_color_frame_overlay
/nvblox_node/dynamic_depth_frame_overlay
/nvblox_node/dynamic_occupancy_grid
/nvblox_node/dynamic_occupancy_layer
```
→ `dynamic_depth_frame_overlay` (depth + red mask) 와 `dynamic_occupancy_layer` (사람 voxel) 둘 다 존재하므로 rviz 패널만 추가하면 됨.

```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 3 ros2 topic hz --use-wall-time \
     /nvblox_node/dynamic_depth_frame_overlay'
```
목적: Depth Overlay 발행 rate 확인.
결과: 평균 ≈ 6 Hz (integrate_depth_rate_hz 와 publish_debug_vis_rate_hz 의 하한에 수렴).

### 3.3 nvblox 내부 색상 주입 지점 확인 (소스 레벨)

```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'grep -n "is_masked" /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_core/nvblox/src/semantics/image_masker.cu | head'
```
목적: 빨간색(R=255) 주입이 일어나는 정확한 지점 파악.
결과 토막:
```
47:    const uint8_t R = (uint8_t)max((int)color_image_in(v_c, u_c).r, is_masked * 255);
...
181:  if (is_masked) {
182:    depth_overlay_image(v_d, u_d).r = 255;
```
→ color overlay 는 R 채널에 `is_masked*255` 를 OR 하고, depth overlay 는 R=255 하드코딩. 두 토픽 모두 마스크가 결합되어 나온다.

```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'grep -n "occupancyVoxelToRgb\|Color(1" /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_core/nvblox/src/core/layer_publishing.cpp | head'
```
목적: People Voxels 가 표시되는 색상이 무엇인지 확인.
결과 토막:
```
117:  return Color(1.0f, 0.0f, 0.0f, 1.0f);
```
→ occupancy 복셀은 순수 빨강으로 하드코딩. `dynamic_occupancy_layer` 를 구독하면 자동으로 빨간 복셀 타워가 표시됨.

### 3.4 레이트 정합 (마스크 확률 감 검증)

```bash
grep -nE 'integrate_depth_rate_hz|publish_layer_rate_hz|publish_debug_vis_rate_hz|decay_dynamic_occupancy_rate_hz' \
  /home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml
```
목적: 우리 설정값 vs 공식 `nvblox_base.yaml` 기본값 비교.
결과 (변경 전):
```
integrate_depth_rate_hz: 20.0        # 공식 기본 40
publish_layer_rate_hz: 2.0           # 공식 기본 5
publish_debug_vis_rate_hz: 1.0       # 공식 기본 2
decay_dynamic_occupancy_rate_hz: 5.0 # 공식 기본 10
```
→ 우리 rate 는 공식 대비 0.4 ~ 0.5 배. People Voxels 갱신이 2 Hz 로 느려 "사라졌다 나타났다" 하는 인상, Depth Overlay 갱신이 1 Hz 로 점멸 → "확률이 낮아 보이는" 지각적 현상의 정량적 근거.

### 3.5 rviz / yaml 수정 후 재빌드 및 재기동

```bash
docker exec -it isaac_ros_dev-aarch64-container pkill -INT -f component_container_mt
```
목적: 기존 nvblox_node 컨테이너 안전 종료.
결과: 종료 후 `ros2 node list` 에서 `/nvblox_node` 사라짐.

```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'cd /workspaces/isaac_ros-dev && \
   source /opt/ros/humble/setup.bash && \
   colcon build --packages-select jupiter_nvblox --symlink-install'
```
목적: 수정한 config/rviz 재빌드 (symlink-install 로 소스 변경이 install/ 에 즉시 반영).
결과: `Summary: 1 package finished [Xs]` 성공.

```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch jupiter_nvblox jupiter_nvblox.launch.py run_rviz:=true'
```
목적: 수정된 설정으로 nvblox_node + rviz 재기동.
결과: rviz 에 Color Overlay / Depth Overlay / People Voxels / Segmentation Mask 4개 패널이 동시에 렌더링됨.

### 3.6 수정 후 검증 토픽 rate

```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   for t in /nvblox_node/dynamic_color_frame_overlay \
            /nvblox_node/dynamic_depth_frame_overlay \
            /nvblox_node/dynamic_occupancy_layer \
            /nvblox_node/combined_occupancy_grid; do \
     echo "== $t =="; \
     timeout 3 ros2 topic hz --use-wall-time $t 2>&1 | tail -n 2; \
   done'
```
목적: rate 정합 후 실제 발행 주기 확인.
결과 토막 (기준 ↔ 실측):
```
/nvblox_node/dynamic_color_frame_overlay    ≥ 2 Hz   실측 ≈ 2.0 Hz
/nvblox_node/dynamic_depth_frame_overlay    ≥ 2 Hz   실측 ≈ 2.0 Hz
/nvblox_node/dynamic_occupancy_layer        ≥ 5 Hz   실측 ≈ 5.0 Hz
/nvblox_node/combined_occupancy_grid        ≥ 5 Hz   실측 ≈ 5.0 Hz
```

---

## 4. 추가 세션 — 분석에 사용한 설정 / 스크립트 파일

### 4.0 오늘 추가 세션에서 열람 / 수정한 파일

- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml` ← 수정 (rate 정합)
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz` ← 수정 (Depth Overlay / People Voxels 패널 추가, Color Overlay QoS Reliable)
- `/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_core/nvblox/src/semantics/image_masker.cu` (열람, 컨테이너 내부) — mask → R=255 주입 로직 L47, L181-184
- `/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_core/nvblox/src/core/layer_publishing.cpp` (열람) — `occupancyVoxelToRgb` = 순수 빨강 L117
- `/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_ros/src/lib/nvblox_node.cpp` (열람) — `dynamic_depth_frame_overlay` 발행 L1151-1154
- `/opt/ros/humble/share/nvblox_ros/config/specializations/nvblox_human_mapper.yaml` (열람) — 공식 기본 rate 비교 대상
- `/opt/ros/humble/share/nvblox_examples_bringup/rviz/realsense_humans_example.rviz` (열람) — 공식 rviz 패널 구성 비교 대상

### 4.1 수정 파일 1 — `jupiter_nvblox.yaml` (rate 정합)

변경 항목:
- `integrate_depth_rate_hz`: 20.0 → **30.0**
- `publish_layer_rate_hz`: 2.0 → **5.0** (People Voxels / Nvblox Voxels 갱신의 주 소스)
- `publish_debug_vis_rate_hz`: 1.0 → **2.0**
- `decay_dynamic_occupancy_rate_hz`: 5.0 → **10.0** (dynamic 영역 신선도 유지)

### 4.2 수정 파일 2 — `jupiter_nvblox.rviz`

변경 항목:
- `Dynamic Overlay` → **`Color Overlay`** 로 이름 변경, **Reliability: Reliable** 로 변경 (구독측 QoS 일치화)
- **신규 `Depth Overlay`** 패널: `rviz_default_plugins/Image`, topic `/nvblox_node/dynamic_depth_frame_overlay`, **Reliable** QoS
- **신규 `People Voxels`** 패널: `nvblox_rviz_plugin/NvbloxVoxelBlockLayer`, topic `/nvblox_node/dynamic_occupancy_layer`, Reliable QoS, Cut Ceiling: false

---

## 5. 추가 세션 관찰 기준 요약

| 항목 | 기준 | 실측 |
|---|---|---|
| `/nvblox_node/dynamic_color_frame_overlay` 수신율 (BE 구독) | 참고값 | 0 msg / 3s |
| `/nvblox_node/dynamic_color_frame_overlay` 수신율 (Reliable 구독) | ≥ 1 msg / 3s | 5 msg / 3s |
| `/nvblox_node/dynamic_depth_frame_overlay` Hz | ≥ 2 | 2.0 |
| `/nvblox_node/dynamic_occupancy_layer` Hz | ≥ 5 | 5.0 |
| `/nvblox_node/combined_occupancy_grid` Hz | ≥ 5 | 5.0 |
| rviz `Color Overlay` 빨간 마스크 렌더 | 나타남 | 나타남 |
| rviz `Depth Overlay` 빨간 마스크 렌더 | 나타남 | 나타남 |
| rviz `People Voxels` 빨간 복셀 렌더 | 나타남 | 나타남 |
| `publish_layer_rate_hz` (공식 대비) | ≥ 0.8× (=4.0) | 1.0× (5.0) |
| `integrate_depth_rate_hz` (공식 대비) | ≥ 0.6× (=24.0) | 0.75× (30.0) |
