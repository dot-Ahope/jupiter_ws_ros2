# 진행과정 2026-04-10: Rosmaster_Lib 비교 분석 + 경량 VNC 환경 구축

## 1. 목표

### 1.1 사용자 요청 (시간순)
> 1. Rosmaster_Lib.py 두 파일을 비교하고, 현재 런타임에 적용 중인 파일을 분석하라
> 2. 자이로 부호/가속도 비율 변경은 의도된 것이다 (컨트롤러 보드 자이로 센서 변경, 데이터 타입 이슈)
> 3. 모터 문제는 물리적 문제이다. 코드 분석 불필요
> 4. VNC가 그래픽 자원을 많이 먹으므로, rviz2 시각화 가능하면서 최대한 경량화된 원격 디스플레이 환경을 구축하라
> 5. RDP 전환은 어떤가? rviz2 시각화가 가능한가?
> 6. 진행과정과 명령어를 md파일로 정리하라

### 1.2 이전 세션 요약
- Part 9-10 (04-08~09): Nav2 진단, MCU USB 재연결, RTK udev 수정, NavSatStatus 분석

---

## 2. Rosmaster_Lib.py 비교 분석

### 2.1 파일 위치 및 런타임 경로

시스템에 존재하는 Rosmaster_Lib.py 3개:

| 위치 | 크기 | 날짜 | 런타임 사용 여부 |
|---|---|---|---|
| `/home/jetson/yahboomcar_ros2_ws/software/py_install_V3.3.1/Rosmaster_Lib/Rosmaster_Lib.py` | 원본 | — | ❌ |
| `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py` | 62257B | Apr 9 10:08 | ✅ (src 원본) |
| `/home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py` | 62257B | Apr 9 10:08 | ✅ (런타임 실제 로드) |
| `/home/jetson/install/jupiter_bringup/lib/python3.10/site-packages/jupiter_bringup/Rosmaster_Lib.py` | 60348B | Jan 28 14:18 | ❌ (AMENT_PREFIX_PATH에 미포함) |

**런타임 경로 해석 체인:**
```
AMENT_PREFIX_PATH → jupiter_ws_ros2/install/jupiter_bringup
  → egg-link → jupiter_ws_ros2/build/jupiter_bringup
    → Rosmaster_Lib.py (일반 파일, src와 동일 내용)
```

- build/ 의 파일은 src/ 원본과 완전히 동일 (diff exit=0)
- build/ 파일은 일반 파일(symlink 아님) — colcon build 시 복사됨

### 2.2 수정본 vs 원본 (V3.3.1) 차이점

| # | 항목 | 원본 (yahboom V3.3.1) | 수정본 (jupiter_bringup) | 영향도 |
|---|------|----------------------|------------------------|--------|
| 1 | **자이로 gy, gz 부호** | `* -gyro_ratio` (부호 반전) | `* gyro_ratio` (부호 반전 제거) | ⚠️ 높음 — 의도된 변경 (센서 교체) |
| 2 | **가속도 비율** | `1 / 1000.0` | `1 / 10000.0` (10배 작음) | ⚠️ 높음 — 의도된 변경 (데이터 타입) |
| 3 | 지자계 파싱 추가 | 없음 | `FUNC_REPORT_MAG = 0x09`, magx/y/z | 낮음 |
| 4 | USB 재연결 로직 | 없음 | `__try_reconnect()`, SerialException 처리 | 중간 |
| 5 | `__receive_data()` | bare loop | try/except 감싸서 reconnect | 중간 |
| 6 | `set_car_motion()` | `except: pass` | `SerialException` → reconnect | 중간 |
| 7 | `set_port()` 추가 | 없음 | `FUNC_PORT_ON_OFF = 0x07`, 라이트 제어 | 낮음 |
| 8 | `is_serial_ok()` 추가 | 없음 | 시리얼 상태 getter | 낮음 |
| 9 | 속도 범위 주석 | 기본값 | 한국어 주석 추가 | 없음 |

### 2.3 사용자 확인 결과
- 자이로 부호 변경: **의도됨** (컨트롤러 보드 자이로 센서가 바뀜)
- 가속도 비율 변경: **의도됨** (데이터 전송시 데이터 타입 문제로 ratio 조정)
- 모터 문제: **물리적 문제** (코드와 무관)

---

## 3. 경량 VNC 환경 구축

### 3.1 변경 전 상태

- **VNC 서버**: TigerVNC 1.12.0, 디스플레이 :1, 포트 5901
- **데스크탑 환경**: GNOME Shell (xstartup에서 gnome-session 실행)
- **리소스 소비**:
  - gnome-shell: **387MB RSS, CPU 46.8%**
  - gnome-software: ~100MB RSS
  - 전체 RAM: 4826MB/7620MB

### 3.2 수행 작업

1. **Xfce4 최소 설치** (`--no-install-recommends`)
   - 패키지: xfce4, xfce4-terminal, dbus-x11

2. **xstartup 변경** (원본 → `xstartup.bak.gnome` 백업)
   - GNOME 우선 → Xfce4 우선으로 순서 변경
   - LXDE/polkit 규칙 등 불필요 내용 제거

3. **Colord PolicyKit 규칙 분리**
   - xstartup에 잘못 포함되어 있던 PolicyKit 규칙을 `/etc/polkit-1/localauthority/50-local.d/45-allow-colord.pkla`로 이동

4. **VNC 재시작**: :1 세션 kill → Xfce4로 재시작 (해상도 1920x1080)

5. **Xfce4 최적화**:
   - Compositor 비활성화 (`xfconf-query -c xfwm4 -p /general/use_compositing -s false`)
   - autostart 비활성화: update-notifier, gnome-software, gnome-initial-setup

6. **mesa-utils 설치** (glxinfo 확인용)

### 3.3 변경 후 상태

| 항목 | GNOME VNC (변경 전) | Xfce4 VNC (변경 후) |
|---|---|---|
| DE 프로세스 RSS | gnome-shell **387MB** | xfce4 전체 **~270MB** |
| CPU (DE) | **46.8%** | **~3%** |
| GR3D (GPU) | 사용 중 | **0%** |
| RAM 전체 | ~4826MB | **~4351MB** (~475MB 절약) |
| rviz2 | 정상 (OpenGL 4.5 llvmpipe) | **정상** (OpenGL 4.5 llvmpipe) |

### 3.4 rviz2 테스트 결과
```
[INFO] [rviz2]: Stereo is NOT SUPPORTED
[INFO] [rviz2]: OpenGl version: 4.5 (GLSL 4.5)
```
정상 실행 확인. timeout 후 정상 종료.

### 3.5 OpenGL 렌더러 분석
```
OpenGL vendor string: Mesa
OpenGL renderer string: llvmpipe (LLVM 15.0.7, 128 bits)
OpenGL version string: 4.5 (Compatibility Profile) Mesa 23.2.1-1ubuntu3.1~22.04.3
```

VNC 가상 디스플레이(:1)에서는 Jetson GPU가 직접 사용되지 않으며, Mesa llvmpipe(소프트웨어 렌더러)로 동작함.

---

## 4. RDP vs VNC 분석

### 4.1 비교

| 항목 | VNC (현재, TigerVNC + Xfce4) | xrdp |
|---|---|---|
| OpenGL | llvmpipe (소프트웨어) | llvmpipe (소프트웨어) — **동일** |
| 프로토콜 효율 | 픽셀 전송 (대역폭 높음) | 드로잉 명령 전송 (대역폭 낮음) |
| rviz2 동작 | **동작함** (OpenGL 4.5) | 동작 가능하나 **불안정** |
| 3D 성능 | llvmpipe로 느리지만 안정 | xrdp+Xorg 모드에서 가끔 GLX 충돌 |
| 클라이언트 | VNC viewer 필요 | Windows 기본 원격 데스크탑 사용 가능 |

### 4.2 결론
- xrdp는 2D 작업에서 더 효율적이나, rviz2 같은 OpenGL 3D 앱 안정성은 VNC보다 떨어짐
- xrdp Xvnc 백엔드 사용 시 내부적으로 VNC와 동일 → 이점 감소
- xrdp Xorg 백엔드는 Jetson에서 GLX 호환성 이슈 가능
- **추천: rviz2 시각화 필수 → 현재 Xfce4 + VNC 유지**

---

## 5. 복원 방법

GNOME VNC로 복원:
```bash
cp ~/.vnc/xstartup.bak.gnome ~/.vnc/xstartup
vncserver -kill :1 && vncserver :1 -localhost=0 -geometry 1920x1200 -depth 24
```

---

## 6. 변경된 파일 목록

| 파일 | 변경 내용 |
|---|---|
| `~/.vnc/xstartup` | GNOME → Xfce4 우선, 불필요 내용 제거 |
| `~/.vnc/xstartup.bak.gnome` | 원본 백업 (신규) |
| `/etc/polkit-1/localauthority/50-local.d/45-allow-colord.pkla` | Colord PolicyKit 규칙 (신규) |
| `~/.config/autostart/update-notifier.desktop` | Hidden=true (신규) |
| `~/.config/autostart/org.gnome.Software.desktop` | Hidden=true (신규) |
| `~/.config/autostart/gnome-initial-setup-copy-worker.desktop` | Hidden=true (신규) |

---

## 7. 설치된 패키지

| 패키지 | 용도 |
|---|---|
| xfce4 | 경량 데스크탑 환경 |
| xfce4-terminal | Xfce4 터미널 에뮬레이터 |
| dbus-x11 | DBus X11 연동 |
| mesa-utils | glxinfo (OpenGL 정보 확인) |
