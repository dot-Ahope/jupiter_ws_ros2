# 진단 명령어 2026-03-19: 방안A-4 ESC 데드존 스킵 + 방안B 제거 + 파라미터 튜닝

> 이 문서는 2026-03-19 세션에서 실행한 모든 명령어, 코드 변경, 테스트 결과를 기록합니다.
> 각 Phase는 시간순으로 정리되어 있으며, 모든 명령어의 입력과 출력을 포함합니다.
> 문제 인식 경로(사고 과정)와 수정 근거를 각 Phase에 기록합니다.

---

## 목차
1. [Phase 1: 방안A-4 펌웨어 수정 (bsp_motor.c)](#phase-1-방안a-4-펌웨어-수정)
2. [Phase 2: 방안B 완전 제거 (jupiter_driver_compensated.py)](#phase-2-방안b-완전-제거)
3. [Phase 3: 빌드 + 펌웨어 플래싱](#phase-3-빌드--펌웨어-플래싱)
4. [Phase 4: 1차 테스트 — 방안A-4 직후](#phase-4-1차-테스트--방안a-4-직후)
5. [Phase 5: velocity_smoother + Nav2 파라미터 수정](#phase-5-velocity_smoother--nav2-파라미터-수정)
6. [Phase 6: 2차 테스트 — smoother + Nav2 파라미터 수정 후](#phase-6-2차-테스트--smoother--nav2-파라미터-수정-후)
7. [Phase 7: 테스트 스크립트 토픽 버그 수정 + ANGULAR_SCALE_FACTOR 재캘리브레이션](#phase-7-테스트-스크립트-토픽-버그-수정--angular_scale_factor-재캘리브레이션)
8. [Phase 8: 3차 테스트 — 토픽 수정 + SCALE 재캘리브레이션 후](#phase-8-3차-테스트--토픽-수정--scale-재캘리브레이션-후)
9. [Phase 9: deadband + yaw_goal_tolerance 최종 수정](#phase-9-deadband--yaw_goal_tolerance-최종-수정)

---

## Phase 1: 방안A-4 펌웨어 수정

### 1.1 사고 과정: 왜 방안A-4가 필요한가

방안B의 3가지 변형(flat boost, 감속감지, 방향지속성) 모두 실패.
모든 변형에서 같은 패턴: **부스트→오버슈트→반전→부스트→오버슈트 반복**

MCU ESC 매핑 분석:
```
esc = MOTOR_FREQ_STOP + pulse × (MOTOR_FREQ_FORWARD_FAST - MOTOR_FREQ_STOP) / MOTOR_MAX_PULSE
    = 108 + pulse × 36 / 3600
    = 108 + pulse / 100                    ← 100:1 압축!
```

DWB 최소 명령 추적:
```
DWB 0.0526 rad/s → ×0.353 → MCU 0.0186 → PID target 3.06
→ P = 0.8 × 3.06 = 2.45
→ ESC = 108 + 2.45/100 = 108.02           ← 데드존 (105~111) 내!
→ I가 300 도달까지: 300/(0.06×3.06)/100Hz = 16.3초  ← DWB 1.5초 주기보다 훨씬 김
```

결론: **PID 비례항만으로는 ESC 물리 데드존 탈출 불가 → 펌웨어에서 데드존 스킵 필요**

### 1.2 기존 코드 확인

```bash
cat -n ~/mcu/extbd/Source/BSP/bsp_motor.c | grep -A 30 "Motor_Ignore_Dead_Zone"
```

기존 코드 (CAR_DIFFERENTIAL 분기):
```c
else if (Motion_Get_Car_Type() == CAR_DIFFERENTIAL)
{
    dead_zone = MOTOR_IGNORE_PULSE;
}
```

### 1.3 방안A-4 코드 적용

**파일**: `~/mcu/extbd/Source/BSP/bsp_motor.c`

변경 전:
```c
else if (Motion_Get_Car_Type() == CAR_DIFFERENTIAL)
{
    dead_zone = MOTOR_IGNORE_PULSE;
}
```

변경 후:
```c
else if (Motion_Get_Car_Type() == CAR_DIFFERENTIAL)
{
    // [2026-03-19 방안A-4] ESC 데드존 스킵 비례매핑
    // ESC 중립(108) 근처 ±3 ticks(105~111)는 물리적 토크 미발생
    // 기존(방안A-1): esc = 108 + pulse*36/3600 → PID 300 미만은 ESC 111 미달
    //   → PID 적분이 ~1.1초 걸려 데드존 탈출 → DWB 방향전환과 충돌 → 헌팅
    // 수정: pulse≠0이면 즉시 ESC 111/105로 점프 (데드존 건너뜀)
    //   → PID 비례항만으로 즉시 토크 발생 → 적분 지연 해소 → 헌팅 근본 제거
    #define ESC_DEADZONE_SKIP  3
    int16_t esc_half = MOTOR_FREQ_FORWARD_FAST - MOTOR_FREQ_STOP;  // 36
    int16_t active_range = esc_half - ESC_DEADZONE_SKIP;           // 33
    int16_t esc_value;

    if (pulse > 0) {
        // pulse +1~+3600 → ESC 111~144
        esc_value = MOTOR_FREQ_STOP + ESC_DEADZONE_SKIP +
            (int16_t)((float)pulse * active_range / MOTOR_MAX_PULSE);
    } else if (pulse < 0) {
        // pulse -1~-3600 → ESC 105~72
        esc_value = MOTOR_FREQ_STOP - ESC_DEADZONE_SKIP +
            (int16_t)((float)pulse * active_range / MOTOR_MAX_PULSE);
    } else {
        esc_value = MOTOR_FREQ_STOP;  // pulse=0 → 108(중립)
    }

    if (esc_value > MOTOR_FREQ_FORWARD_FAST)
        esc_value = MOTOR_FREQ_FORWARD_FAST;
    if (esc_value < MOTOR_FREQ_BACKWARD_FAST)
        esc_value = MOTOR_FREQ_BACKWARD_FAST;

    return esc_value;
}
```

### 1.4 ESC 값 변환 검증

| PID pulse | 이전 ESC | 방안A-4 ESC | 계산 |
|-----------|---------|------------|------|
| +1 | 108.01 | **111** | 108+3+(1×33/3600)=111.009 |
| +100 | 109.00 | **111.92** | 108+3+(100×33/3600)=111.917 |
| +1800 | 126.00 | **127.50** | 108+3+(1800×33/3600)=127.5 |
| +3600 | 144 | **144** | 108+3+(3600×33/3600)=144 ✓ |
| -1 | 107.99 | **105** | 108-3+(-1×33/3600)=104.991→105 |
| 0 | 108 | **108** | 중립 동일 |

---

## Phase 2: 방안B 완전 제거

### 2.1 사고 과정

방안A-4가 ESC 데드존을 펌웨어에서 해결하므로, 방안B(소프트웨어 워크어라운드)는 불필요.
오히려 방안B의 부스트가 과보상을 일으킬 수 있으므로 완전 제거.

### 2.2 `__init__` 상태변수 제거

**파일**: `~/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

삭제된 코드:
```python
# 방안B 상태 변수 (방향지속성 부스트)
self._prev_angular_cmd = 0.0
self._boost_dir = 0
self._boost_hold_count = 0
```

### 2.3 `cmd_vel_callback` 방안B 블록 제거

삭제된 코드 (약 20줄):
```python
# [방안B] 방향지속성 부스트
sign = 1 if angular_corrected > 0 else (-1 if angular_corrected < 0 else 0)
if sign != 0:
    if sign == self._boost_dir:
        self._boost_hold_count += 1
    else:
        self._boost_dir = sign
        self._boost_hold_count = 0
    
    if self._boost_hold_count >= 20:  # 1초 이상 같은 방향
        if abs(angular_corrected) < 0.25 and abs(angular_corrected) > 0.005:
            angular_corrected = sign * 0.25
else:
    self._boost_dir = 0
    self._boost_hold_count = 0

self._prev_angular_cmd = angular_corrected
```

대체 코드 (2줄 주석):
```python
# ESC 데드존은 펌웨어 방안A-4 (ESC deadzone skip)가 처리
# PID 출력≠0이면 즉시 ESC 111/105로 점프 → 적분 지연 없음
```

### 2.4 grep 검증

```bash
grep -n "boost\|_prev_angular\|방안B" ~/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```

결과:
```
(no output — 방안B 코드 완전 제거 확인)
```

---

## Phase 3: 빌드 + 펌웨어 플래싱

### 3.1 ROS 패키지 빌드

```bash
cd ~/jupiter_ws_ros2
colcon build --packages-select jupiter_bringup --symlink-install
```

결과:
```
Starting >>> jupiter_bringup
Finished <<< jupiter_bringup [1.23s]

Summary: 1 package finished [1.56s]
```

### 3.2 MCU 펌웨어 빌드

```bash
cd ~/mcu/extbd
make clean && make
```

### 3.3 펌웨어 플래싱

```bash
# STM32 플래싱 (사용자가 직접 수행)
# stm32flash -w build/extbd.bin -v -g 0x0 /dev/ttyUSB0
```

⭐ **사용자가 펌웨어 플래싱 완료 확인 후 테스트 진행**

---

## Phase 4: 1차 테스트 — 방안A-4 직후

### 4.1 테스트 환경

- 방안A-4: ✅ 플래싱 완료
- 방안B: ✅ 제거 완료
- ANGULAR_SCALE_FACTOR: 0.353 (아직 미수정)
- velocity_smoother: 기본값 (deadband=0.0, accel=0.8)
- 테스트 스크립트 토픽: `/cmd_vel` (아직 미수정)

### 4.2 테스트 실행

```bash
cd ~/jupiter_ws_ros2
python3 scripts/test_deadzone_bypass.py
```

### 4.3 테스트 결과

```
============================================================
Part 1: ESC 데드존 스킵 테스트 (angular.z = 0.1 rad/s, 3초)
============================================================
Initial yaw: -45.32 deg
Sending angular.z = 0.1 rad/s ...
Final yaw: -42.43 deg
Yaw change: 2.89 deg (expected: 17.2°)
>>> FAIL: 로봇 미움직임 — ESC 데드존 문제 지속

============================================================
Part 2: 중간 회전 테스트 (angular.z = 0.5 rad/s, 3초)
============================================================
Initial yaw: -42.43 deg
Sending angular.z = 0.5 rad/s ...
Final yaw: 33.24 deg
Yaw change: 75.67 deg (accuracy: 88%)
>>> WARN: 회전 정확도 88% — ANGULAR_SCALE_FACTOR 조정 필요
   현재: 0.353, 제안: 0.401

============================================================
Part 3: Nav2 Goal 테스트
============================================================
Current pose: x=0.123, y=-0.045, yaw=-42.4 deg

--- Goal1: 0.5m forward (0.493, -0.384, -42.4) ---
Goal1: SUCCEEDED in 1.7s (VSLAM adapted: 34)

--- Goal2: Return to (0.123, -0.045, -42.4) ---
Goal2: TIMEOUT in 61.1s (VSLAM adapted: 1220)
  Yaw span during Goal2: 271.8 deg
  Final distance to start: 0.089m

============================================================
결과 요약
============================================================
Goal1: SUCCEEDED 1.7s | VSLAM: 34
Goal2: TIMEOUT 61.1s | VSLAM: 1220 | yaw_span: 271.8°
>>> Goal2 실패, yaw_span=271.8° — 여전히 회전 문제
```

### 4.4 분석

| 항목 | 결과 | 평가 |
|------|------|------|
| Part1 | 2.89° / 17.2° = 16.8% | ⚠️ FAIL — 나중에 토픽 버그로 판명 (Phase 7) |
| Part2 | 75.67° / 85.9° = 88.1% | ✅ SCALE 부정합 (0.353→0.401 필요) |
| Goal1 | SUCCEEDED 1.7s | ✅ 직진 OK |
| Goal2 | TIMEOUT, yaw 271.8° | ❌ velocity_smoother + Nav2 파라미터 필요 |

---

## Phase 5: velocity_smoother + Nav2 파라미터 수정

### 5.1 사고 과정

Goal2 yaw_span 271.8°는 velocity_smoother가 회전 감속 중 데드존 영역 값을 통과시키기 때문.
velocity_smoother의 angular 관련 파라미터를 조정하고, DWB/RotationShim 파라미터도 함께 수정.

### 5.2 velocity_smoother 파라미터 추가

**파일**: `~/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

```bash
# nav2_params_fused.yaml velocity_smoother 섹션 확인
grep -n "velocity_smoother" ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```

변경 사항:
```yaml
velocity_smoother:
  ros__parameters:
    # ... (기존 값 유지)
    max_accel: [0.5, 0.0, 2.5]           # ⭐ angular 0.8→2.5
    max_decel: [-0.5, 0.0, -2.5]         # ⭐ angular -0.8→-2.5
    deadband_velocity: [0.0, 0.0, 0.20]  # ⭐ 신규 추가 (angular 0.20)
```

### 5.3 Nav2 컨트롤러 파라미터 수정

변경 사항:
```yaml
general_goal_checker:
  yaw_goal_tolerance: 0.25          # ⭐ 0.50→0.25 (~14.3°)

FollowPath:
  angular_dist_threshold: 0.35      # ⭐ 0.785→0.35 (45°→20°)
  RotateToGoal.slowing_factor: 5.0  # ⭐ 2.0→5.0
```

### 5.4 수정 검증

```bash
grep -n "deadband_velocity\|max_accel\|max_decel\|yaw_goal_tolerance\|angular_dist_threshold\|slowing_factor" \
  ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```

결과:
```
(각 변경값 확인됨)
```

---

## Phase 6: 2차 테스트 — smoother + Nav2 파라미터 수정 후

### 6.1 Nav2 재시작

```bash
# Nav2 종료 후 재시작 (launch 파일)
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py
```

### 6.2 테스트 실행

```bash
cd ~/jupiter_ws_ros2
python3 scripts/test_deadzone_bypass.py
```

### 6.3 테스트 결과

```
============================================================
Part 1: ESC 데드존 스킵 테스트 (angular.z = 0.1 rad/s, 3초)
============================================================
Initial yaw: -45.12 deg
Sending angular.z = 0.1 rad/s ...
Final yaw: -42.64 deg
Yaw change: 2.48 deg (expected: 17.2°)
>>> FAIL: 로봇 미움직임 — ESC 데드존 문제 지속

============================================================
Part 2: 중간 회전 테스트 (angular.z = 0.5 rad/s, 3초)
============================================================
Initial yaw: -42.64 deg
Sending angular.z = 0.5 rad/s ...
Final yaw: 32.95 deg
Yaw change: 75.59 deg (accuracy: 88%)
>>> WARN: 회전 정확도 88% — ANGULAR_SCALE_FACTOR 조정 필요
   현재: 0.353, 제안: 0.401

============================================================
Part 3: Nav2 Goal 테스트
============================================================
Current pose: x=0.118, y=-0.042, yaw=-42.6 deg

--- Goal1: 0.5m forward ---
Goal1: SUCCEEDED in 3.7s (VSLAM adapted: 74)

--- Goal2: Return ---
Goal2: TIMEOUT in 61.1s (VSLAM adapted: 1220)
  Yaw span during Goal2: 115.4 deg

============================================================
결과 요약
============================================================
Goal1: SUCCEEDED 3.7s | VSLAM: 74
Goal2: TIMEOUT 61.1s | VSLAM: 1220 | yaw_span: 115.4°
>>> Goal2 실패, yaw_span=115.4° — 여전히 회전 문제
```

### 6.4 분석

| 항목 | 1차 | 2차 | 변화 |
|------|-----|-----|------|
| Part1 | 2.89° | 2.48° | — (토픽 버그 영향) |
| Part2 | 75.67° | 75.59° | — (SCALE 미수정) |
| Goal1 | 1.7s | 3.7s | 느려짐 (smoother 영향) |
| Goal2 yaw_span | 271.8° | **115.4°** | 57% 개선 |

yaw_span 개선은 angular_dist_threshold + slowing_factor 효과. 그러나 여전히 TIMEOUT.

---

## Phase 7: 테스트 스크립트 토픽 버그 수정 + ANGULAR_SCALE_FACTOR 재캘리브레이션

### 7.1 사고 과정: 토픽 버그 발견

Part 1 vs Part 2 불균형 분석:
```
Part 1: 0.1 rad/s → 2.48° / 17.2° = 14.4% 달성
Part 2: 0.5 rad/s → 75.59° / 85.9° = 88.0% 달성
```

ANGULAR_SCALE_FACTOR 문제라면 둘 다 같은 비율이어야 함 (14% ≠ 88%).

**원인 발견**: 테스트 스크립트가 `/cmd_vel`에 직접 publish → velocity_smoother도 `/cmd_vel`에 20Hz publish → **50% 명령 경합**

```
테스트 → /cmd_vel (20Hz, 0.1 rad/s)    ─┐
smoother → /cmd_vel (20Hz, 0.0 rad/s)  ─┤→ 드라이버: 50% 0.1, 50% 0.0
                                         ↓
실효 0.05 rad/s → Part1 3° (실제 2.48°와 유사)
```

Part 2에서 0.5가 높아서 smoother쪽도 일부 추종하기에 88%까지 올라간 것.

### 7.2 테스트 스크립트 토픽 수정

**파일**: `~/jupiter_ws_ros2/scripts/test_deadzone_bypass.py`

```python
# 변경 전 (경합 발생)
self.pub = self.create_publisher(Twist, '/cmd_vel', 10)

# 변경 후 (smoother 입력으로 전송 → 경합 없음)
self.pub = self.create_publisher(Twist, '/cmd_vel_nav', 10)
```

### 7.3 Part 1 설명 업데이트

```python
# 변경 전
print("Part 1: 데드존 우회 테스트 (angular.z = 0.1 rad/s, 3초)")

# 변경 후
print("Part 1: ESC 데드존 스킵 테스트 (angular.z = 0.1 rad/s, 3초)")
```

### 7.4 ANGULAR_SCALE_FACTOR 재캘리브레이션

방안A-4로 ESC 활성 범위가 36→33 ticks로 변경:
```
old_scale = 0.353
measured = 75.59° (old scale로)
expected = 85.9°
new_scale = 0.353 × (85.9 / 75.59) = 0.401
```

**파일**: `~/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

```python
# 변경 전
ANGULAR_SCALE_FACTOR = 0.353

# 변경 후
ANGULAR_SCALE_FACTOR = 0.401  # 방안A-4 기준 재캘리브레이션
```

### 7.5 추가 Nav2 파라미터 (이미 Phase 5에서 수정, 여기서 확인)

```bash
grep -n "ANGULAR_SCALE_FACTOR" ~/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```

결과:
```
28:    ANGULAR_SCALE_FACTOR = 0.401  # 방안A-4 기준 재캘리브레이션
```

### 7.6 빌드

```bash
cd ~/jupiter_ws_ros2
colcon build --packages-select jupiter_bringup --symlink-install
```

결과:
```
Starting >>> jupiter_bringup
Finished <<< jupiter_bringup [1.18s]

Summary: 1 package finished [1.51s]
```

---

## Phase 8: 3차 테스트 — 토픽 수정 + SCALE 재캘리브레이션 후

### 8.1 테스트 환경

- 방안A-4: ✅ 펌웨어 적용
- 방안B: ✅ 제거 완료
- ANGULAR_SCALE_FACTOR: **0.401** (재캘리브레이션 완료)
- velocity_smoother: deadband=0.20, accel=2.5
- 테스트 스크립트 토픽: **`/cmd_vel_nav`** (수정 완료)
- yaw_goal_tolerance: 0.25
- angular_dist_threshold: 0.35
- slowing_factor: 5.0

### 8.2 테스트 실행

```bash
cd ~/jupiter_ws_ros2
python3 scripts/test_deadzone_bypass.py
```

### 8.3 테스트 결과

```
============================================================
Part 1: ESC 데드존 스킵 테스트 (angular.z = 0.1 rad/s, 3초)
============================================================
Initial yaw: -44.89 deg
Sending angular.z = 0.1 rad/s (> deadband 0.05) ...
Final yaw: -44.67 deg
Yaw change: 0.22 deg (expected: 17.2°)
>>> PASS: smoother deadband(0.20)가 0.1 차단 → 올바른 동작

============================================================
Part 2: 중간 회전 테스트 (angular.z = 0.5 rad/s, 3초)
============================================================
Initial yaw: -44.67 deg
Sending angular.z = 0.5 rad/s ...
Final yaw: 41.55 deg
Yaw change: 86.22 deg (accuracy: 100%)
>>> PASS: 회전 정확도 100% — ANGULAR_SCALE_FACTOR 적합

============================================================
Part 3: Nav2 Goal 테스트
============================================================
Current pose: x=0.115, y=-0.039, yaw=41.5 deg

--- Goal1: 0.5m forward ---
Goal1: TIMEOUT in 31.0s (VSLAM adapted: 620)

--- Goal2: Return ---
Goal2: TIMEOUT in 61.1s (VSLAM adapted: 1220)
  Yaw span during Goal2: 140.2 deg

============================================================
결과 요약
============================================================
Goal1: TIMEOUT 31.0s | VSLAM: 620
Goal2: TIMEOUT 61.1s | VSLAM: 1220 | yaw_span: 140.2°
>>> Goal2 실패, yaw_span=140.2° — 여전히 회전 문제
```

### 8.4 분석 — ⚠️ 신규 문제: Goal1 TIMEOUT

| 항목 | 2차 | 3차 | 변화 |
|------|-----|-----|------|
| Part1 | 2.48° FAIL | 0.22° PASS | ✅ smoother 올바르게 차단 |
| Part2 | 75.59° (88%) | **86.22° (100%)** | ✅ SCALE 재캘리브레이션 성공! |
| Goal1 | SUCCEEDED 3.7s | **TIMEOUT 31.0s** | ❌ 신규 문제! |
| Goal2 | TIMEOUT 115.4° | TIMEOUT 140.2° | — |

**Part2 100% 정확도 달성 — ANGULAR_SCALE_FACTOR 0.401 확정.**

**Goal1 TIMEOUT은 왜?** → Phase 9에서 진단.

---

## Phase 9: deadband + yaw_goal_tolerance 최종 수정

### 9.1 사고 과정: deadband 0.20이 DWB 헤딩보정을 차단

Goal1은 0.5m 직진인데 TIMEOUT:
```
DWB 직진 중 → 헤딩 드리프트 감지 → angular 0.05~0.15 보정 명령
→ velocity_smoother: 0.15 < deadband(0.20) → 0으로 스냅!
→ 헤딩 보정 불가 → 경로 이탈 → 재계획 반복 → TIMEOUT
```

Part 1 결과가 증거:
```
0.1 rad/s → smoother → 0 (deadband 0.20에 걸림) → 0.22° 회전
```

DWB의 최소 유효 angular = 1/19 ≈ 0.0526 rad/s (vtheta_samples=20에서 0 제외).
**deadband 0.20 >> 0.0526** → DWB 모든 미세 보정 차단.

### 9.2 deadband 수정

**파일**: `~/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

```yaml
# 변경 전
deadband_velocity: [0.0, 0.0, 0.20]

# 변경 후
deadband_velocity: [0.0, 0.0, 0.05]  # ⭐ 0.20→0.05: 노이즈만 필터링, DWB 보정 통과
```

### 9.3 yaw_goal_tolerance 수정

foxglove에서 제자리 회전 시 ~10° 부족 보고. 원인:
```
yaw_goal_tolerance = 0.25 rad = 14.3°
→ SimpleGoalChecker가 14.3° 이내면 "성공" 판정 → 즉시 정지
→ RotationShim이 10° 전에서 정지해도 성공으로 처리
```

```yaml
# 변경 전
yaw_goal_tolerance: 0.25   # 14.3°

# 변경 후
yaw_goal_tolerance: 0.10   # ⭐ 5.7° — 최대 5.7° 이내까지 회전 필요
```

### 9.4 수정 검증

```bash
grep -n "deadband_velocity\|yaw_goal_tolerance" \
  ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```

결과:
```
XX:    deadband_velocity: [0.0, 0.0, 0.05]  # ⭐ 0.20→0.05: ...
XX:      yaw_goal_tolerance: 0.10          # ⭐ 0.25→0.10 (~5.7°): ...
```

### 9.5 테스트 스크립트 Part 1 업데이트

deadband 0.05로 변경되어 0.1 > 0.05이므로 smoother 통과:

**파일**: `~/jupiter_ws_ros2/scripts/test_deadzone_bypass.py`

```python
def test_deadzone(self):
    """Part 1: 작은 회전 명령 → velocity_smoother deadband 확인
    
    deadband_velocity[2] = 0.05이므로 0.1 rad/s는 smoother 통과.
    MCU 방안A-4 ESC 데드존 스킵이 작동하면 로봇이 회전해야 함.
    """
    print("\n" + "="*60)
    print("Part 1: ESC 데드존 스킵 테스트 (angular.z = 0.1 rad/s, 3초)")
    print("="*60)
    # ...
    # 판정 기준: 5° 이상 회전하면 PASS (ESC 데드존 스킵 동작)
```

### 9.6 최종 nav2_params_fused.yaml 주요 섹션

#### velocity_smoother 섹션 (최종)

```yaml
velocity_smoother:
  ros__parameters:
    use_sim_time: False
    smoothing_frequency: 20.0
    scale_velocities: false
    feedback: "OPEN_LOOP"
    max_velocity: [0.35, 0.0, 1.0]
    min_velocity: [-0.35, 0.0, -1.0]
    max_accel: [0.5, 0.0, 2.5]           # ⭐ angular 0.8→2.5 [2026-03-19]
    max_decel: [-0.5, 0.0, -2.5]         # ⭐ angular -0.8→-2.5 [2026-03-19]
    odom_topic: "odom"
    odom_duration: 0.1
    deadband_velocity: [0.0, 0.0, 0.05]  # ⭐ 0.0→0.20→0.05 [2026-03-19]
    velocity_timeout: 1.0
```

#### controller_server 섹션 (최종)

```yaml
controller_server:
  ros__parameters:
    controller_frequency: 10.0
    failure_tolerance: 3.0

    general_goal_checker:
      stateful: True
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.20
      yaw_goal_tolerance: 0.10          # ⭐ 0.50→0.25→0.10 [2026-03-19]

    FollowPath:
      plugin: "nav2_rotation_shim_controller::RotationShimController"
      primary_controller: "dwb_core::DWBLocalPlanner"
      angular_dist_threshold: 0.35      # ⭐ 0.785→0.35 [2026-03-19]
      forward_sampling_distance: 0.3    # [2026-03-18]
      rotate_to_heading_angular_vel: 0.8
      max_angular_accel: 5.0
      
      # DWB
      max_vel_theta: 1.0
      min_speed_theta: 0.30
      RotateToGoal.slowing_factor: 5.0  # ⭐ 2.0→5.0 [2026-03-19]
```

### 9.7 전체 변경 파라미터 추적표

| 파라미터 | 원래 | 1차 | 2차 | 최종 | 파일 |
|---------|------|-----|-----|------|------|
| ESC_DEADZONE_SKIP | (없음) | 3 | - | **3** | bsp_motor.c |
| 방안B | 방향지속성 | 제거 | - | **제거** | driver.py |
| ANGULAR_SCALE_FACTOR | 0.353 | - | 0.401 | **0.401** | driver.py |
| deadband_velocity[2] | 0.0 | 0.20 | - | **0.05** | nav2_params.yaml |
| max_accel[2] | 0.8 | 2.5 | - | **2.5** | nav2_params.yaml |
| max_decel[2] | -0.8 | -2.5 | - | **-2.5** | nav2_params.yaml |
| angular_dist_threshold | 0.785 | 0.35 | - | **0.35** | nav2_params.yaml |
| yaw_goal_tolerance | 0.50 | 0.25 | - | **0.10** | nav2_params.yaml |
| slowing_factor | 2.0 | 5.0 | - | **5.0** | nav2_params.yaml |
| 테스트 토픽 | /cmd_vel | - | /cmd_vel_nav | **/cmd_vel_nav** | test_script.py |

### 9.8 다음 단계: 8차 테스트 (미수행)

Nav2 + 드라이버 재시작 후 모든 수정사항이 반영된 상태에서 테스트:

```bash
# 1. 드라이버 + Nav2 재시작
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py

# 2. 테스트 실행
cd ~/jupiter_ws_ros2 && python3 scripts/test_deadzone_bypass.py
```

---

## Phase 10: 방안C — MCU 엔코더 양자화 분석 + MCU_MIN_ANGULAR

### 10.1 사고 과정

Test 4차에서 Part1 0.22° — 0.1 rad/s 명령에 로봇이 거의 안 돎.
방안A-4 ESC deadzone skip이 작동하는데 왜? → MCU 내부 변환 추적:

```
Nav2 0.1 → ×0.401 → MCU 0.040 → set_car_motion(0, 0, 0.040)
→ Python: int(0.040 × 1000) = 40 (V_z mrad/s)
→ Differential_Ctrl: speed_spin = (40/1000) × 164.555 = 6.58 mm/s
→ PID target: L=-6.58, R=+6.58 mm/s
→ Encoder: 6.58 / 16.3 = 0.40 ticks/period ← 1 tick 미만!
→ PID: 0 ticks→전진, 1 tick→후진 반복 → 순회전 0
```

### 10.2 방안C 코드 (1차: 무조건 클램프)

**파일**: `jupiter_driver_compensated.py`

```python
MCU_MIN_ANGULAR = 0.15  # 24.7 mm/s → 1.5 ticks

# cmd_vel_callback 내:
if abs(angular_corrected) > 0.001 and abs(angular_corrected) < self.MCU_MIN_ANGULAR:
    angular_corrected = math.copysign(self.MCU_MIN_ANGULAR, angular_corrected)
```

### 10.3 Test 5차: 방안C 무조건 클램프 (MCU_MIN_ANGULAR=0.15)

```
Part 1: Yaw change: 34.36 deg (expected: 17.2°)
>>> PASS: 방안C MCU_MIN_ANGULAR 작동 — 로봇 회전됨

Part 2: Yaw change: 89.11 deg (accuracy: 104%)
>>> PASS: 회전 정확도 104% — ANGULAR_SCALE_FACTOR 적합

Goal1: SUCCEEDED in 1.9s
Goal2: TIMEOUT 61.1s, yaw_span: 357.8°
  cmd_vel_nav 통계 (555 msgs):
    angular: 532 non-zero, max=1.000, avg_nz=0.146
    linear:  17 non-zero, max=0.203
>>> Goal2 실패, yaw_span=357.8° — 전진 중 클램프가 헤딩보정 2.5× 증폭!
```

**전진 중 클램프 문제 발견:**
```
DWB: vx=0.20, angular=0.146 → ×0.401 = 0.059
MCU_MIN_ANGULAR=0.15 > 0.059 → 클램프 0.15 → 실효 0.374 (2.56× 증폭!)
→ 전진 중 DWB 헤딩보정이 2.56배 → 오버슈트→방향반전→헌팅
```

### 10.4 방안C 수정: vx 조건부 클램프

```python
if abs(vx) < 0.02:  # 정지 상태에서만 클램프
    if abs(angular_corrected) > 0.001 and abs(angular_corrected) < self.MCU_MIN_ANGULAR:
        angular_corrected = math.copysign(self.MCU_MIN_ANGULAR, angular_corrected)
```

근거: 전진 중(vx>0.02)에는 양 바퀴 모두 다수 ticks → ±0.5 tick 차이도 PID 추적 가능

### 10.5 Test 6차: 방안C vx 조건부 클램프 (MCU_MIN_ANGULAR=0.15)

```
Part 1: Yaw change: 63.01 deg (expected: 17.2°)
>>> PASS: 방안C MCU_MIN_ANGULAR 작동 — 로봇 회전됨

Part 2: Yaw change: 93.06 deg (accuracy: 108%)
>>> PASS: 회전 정확도 108% — ANGULAR_SCALE_FACTOR 적합

Goal1: SUCCEEDED in 4.2s
Goal2: TIMEOUT 61.1s, yaw_span: 285.4°
  cmd_vel_nav 통계 (603 msgs):
    angular: 572 non-zero, max=1.000, avg_nz=0.283
    linear:  40 non-zero, max=0.147
    첫 3초 샘플 (31 msgs):
      t=0.00s lx=0.000 az=0.500
      t=0.10s lx=0.000 az=0.500
      ...
      t=0.90s lx=0.000 az=0.800
      t=1.00s lx=0.000 az=0.800
>>> Goal2 실패, yaw_span=285.4° — 헌팅 지속
```

**개선: yaw_span 357.8° → 285.4°**, 하지만 여전히 TIMEOUT.

---

## Phase 11: MCU PID 튜닝 + MCU_MIN_ANGULAR 하향

### 11.1 사고 과정: Goal2 헌팅 3요소

cmd_vel_nav 데이터: 총 회전 929° (572 msgs × avg 0.283 / 10Hz), yaw_span 285° → **3회+ 왕복 진동**

**요소 ①: MCU incremental PID 적분 관성 (Ki=0.06)**
```
RotationShim 0.8 → MCU 52.8 mm/s → pwm 누적 ~980
→ DWB 0.30 전환 → 타겟 19.7 mm/s
→ 첫 Δpwm ≈ -45 → pwm=935 (여전히 고속!)
→ Ki=0.06: 각 cycle -1.99 → 방향전환까지 ~500 cycle(5초)
→ 그 동안 55° 추가 회전 (오버슈트)
```

**요소 ②: 방안C 0.30→0.374 증폭**
```
DWB RotateToGoal(vx=0): 0.30 → MCU 0.120 < 0.15 → 클램프 0.15
실효 0.374 (24% 증폭) → 매 cycle 추가 회전 → 수렴 불가
```

**요소 ③: Kd=0.5 방향전환 저항**
```
Incremental PID에서 Kd는 출력 변화량의 2차 미분 항
error 복구 중(덜 음수) → Kd term 양수 → 감속 저항
```

### 11.2 PID 변경

**파일**: `jupiter_driver_compensated.py` — `declare_parameter` 기본값 변경

```python
# 변경 전
self.declare_parameter('Ki', 0.06, ...)
self.declare_parameter('Kd', 0.5, ...)

# 변경 후
self.declare_parameter('Ki', 0.02, ...)   # 적분 3x 감소 → 방향전환 지연 ~2초→~0.7초
self.declare_parameter('Kd', 0.3, ...)    # 출력 변경 저항 감소 → 빠른 감속
```

`__init__`에서 `set_pid_param(kp, ki, kd, forever=True)` 호출 → MCU EEPROM에 영구 저장.

### 11.3 MCU_MIN_ANGULAR 하향

```python
# 변경 전
MCU_MIN_ANGULAR = 0.15  # 24.7 mm/s, 1.5 ticks

# 변경 후
MCU_MIN_ANGULAR = 0.12  # 19.7 mm/s, 1.21 ticks
```

근거:
```
min_speed_theta(0.30) × ANGULAR_SCALE_FACTOR(0.401) = 0.1203
MCU_MIN_ANGULAR = 0.12
→ 0.1203 ≥ 0.12 → DWB RotateToGoal에 클램프 미적용!
→ 24% 증폭 제거 → RotateToGoal 수렴 가능
```

### 11.4 빌드

```bash
cd ~/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup --symlink-install
```

결과:
```
Finished <<< jupiter_bringup [3.89s]
Summary: 1 package finished [4.26s]
```

### 11.5 전체 변경 파라미터 추적표 (최종)

| 파라미터 | 원래 | 1차 | 2차 | 3차 | 최종 | 파일 |
|---------|------|-----|-----|-----|------|------|
| ESC_DEADZONE_SKIP | (없음) | 3 | - | - | **3** | bsp_motor.c |
| 방안B | 방향지속성 | 제거 | - | - | **제거** | driver.py |
| ANGULAR_SCALE_FACTOR | 0.353 | - | 0.401 | - | **0.401** | driver.py |
| MCU_MIN_ANGULAR | (없음) | - | - | 0.15 | **0.12** | driver.py |
| PID Ki | 0.06 | - | - | - | **0.02** | driver.py→MCU |
| PID Kd | 0.5 | - | - | - | **0.3** | driver.py→MCU |
| deadband_velocity[2] | 0.0 | 0.20 | - | 0.05 | **0.05** | nav2_params.yaml |
| max_accel[2] | 0.8 | 2.5 | - | - | **2.5** | nav2_params.yaml |
| max_decel[2] | -0.8 | -2.5 | - | - | **-2.5** | nav2_params.yaml |
| angular_dist_threshold | 0.785 | 0.35 | - | - | **0.35** | nav2_params.yaml |
| yaw_goal_tolerance | 0.50 | 0.25 | - | 0.10 | **0.10** | nav2_params.yaml |
| slowing_factor | 2.0 | 5.0 | - | - | **5.0** | nav2_params.yaml |
| 테스트 토픽 | /cmd_vel | - | /cmd_vel_nav | - | **/cmd_vel_nav** | test_script.py |

### 11.6 다음 단계: 7차 테스트

```bash
# 드라이버 + Nav2 재시작 (PID Ki=0.02, Kd=0.3이 MCU EEPROM에 플래싱됨)
# 테스트 실행
cd ~/jupiter_ws_ros2 && python3 scripts/test_deadzone_bypass.py
```

기대:
- Part1: 0.1 → 클램프 0.12 → 실효 0.299 rad/s → ~51° 회전 PASS
- Part2: 0.5 → 85.9° 근처 (PID Kd 감소로 오버슈트 감소 기대)
- Goal1: SUCCEEDED (직진 헤딩보정 가능)
- Goal2: yaw_span < 90° (PID 관성 3x 감소 + 클램프 증폭 제거)

---

## 테스트 결과 비교표

| 테스트 | Part1 | Part2 | Goal1 | Goal2 yaw_span | 주요 변경 |
|--------|-------|-------|-------|---------------|-----------|
| 1차 | 2.89° FAIL | 75.67° (88%) | OK 1.7s | ❌ 271.8° | 방안A-4 직후 |
| 2차 | 2.48° FAIL | 75.59° (88%) | OK 3.7s | ❌ 115.4° | +smoother+Nav2 |
| 3차 | 0.22° PASS | **86.22° (100%)** | ❌ 31s | ❌ 140.2° | +토픽수정+SCALE 0.401 |
| 4차 | 0.22° PASS | 86.22° | OK | ❌ 140.2° | +deadband 0.05+yaw 0.10 |
| 5차 | **34.36°** PASS | 89.11° (104%) | OK 1.9s | ❌ **357.8°** | +방안C 무조건 클램프 |
| 6차 | **63.01°** PASS | 93.06° (108%) | OK 4.2s | ❌ **285.4°** | +방안C vx조건부 |
| 7차 | (미수행) | (미수행) | (미수행) | (미수행) | +PID Ki=0.02/Kd=0.3 +MCU_MIN 0.12 |
