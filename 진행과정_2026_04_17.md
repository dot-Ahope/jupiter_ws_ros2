# 진행과정 2026-04-17: 모터/인코더 교체 후 오도메트리 캘리브레이션 수정

## 1. 목표

### 1.1 사용자 요청 (시간순)
> 1. angular cmd_vel=2.0 을 보내면 MCU에 800이 전달된다. 왜 2000이 아닌가?
> 2. 모터를 교체했다. 기존 캘리브레이션 값을 주석 처리하라
> 3. MCU 펌웨어(~/mcu/extbd)를 분석하고 jupiter_ws_ros2와의 호환성을 검토하라. 또한 CSV 데이터의 position.x 역전 원인을 분석하라
> 4. (1차 CSV) RF2O가 ground truth이다. odom_adapted가 RF2O와 일치하면 정상이다. 분석을 다시 하라
> 5. (2차 CSV) 회전 시 EKF/wheel/RF2O 오도메트리가 전부 다르게 계산된다. 원인을 찾고 분석하라
> 6. (2차 분석 결과) 문제 2번(CAR_DIFFERENTIAL /4 나눗셈)은 CAR_MECANUM을 사용하므로 이미 해결되어 있다. 모터와 휠 인코더만 교체했다는 사실만 알고 문제를 해결하라
> 7. (3차 CSV) 회전 문제는 해결됐다. 전진 시 오도메트리가 후진한다고 판단한다. 원인을 찾고 해결하라
> 8. 오늘 진행과정과 명령어를 md파일로 정리하라

### 1.2 이전 세션 요약
- 2026-04-10: Rosmaster_Lib 비교 분석, VNC 경량화 (Xfce4)
- 2026-04-15: Docker nvblox + cuVSLAM + people_detection GPU 경합 진단
- 2026-04-16: `jupiter_nvblox` 패키지 생성 (nvblox + people_segmentation, cuVSLAM 제외)

---

## 2. angular cmd_vel 스케일링 분석

### 2.1 질문
`angular = 2.0 rad/s`를 보내면 MCU에 `800`이 전달됨 → `2000`이 아닌 이유?

### 2.2 원인
`jupiter_driver_compensated.py`에서 `ANGULAR_SCALE_FACTOR = 0.401` 적용:

```
angular_corrected = 2.0 × 0.401 = 0.802 rad/s
MCU 프로토콜: vz × 1000 = 802 → MCU에 800 전달
```

이전 캘리브레이션 이력:
- 2026-02-11: angular 1.0 rad/s × 10s → 예상 1.59회전, 실측 4.5회전 → 보정값 0.353
- 2026-03-19: 방안A-4 ESC deadzone skip 후 재측정 → 0.401로 업데이트

### 2.3 결론
의도된 동작. 모터 교체와 무관.

---

## 3. 모터 교체에 따른 캘리브레이션 무효화

### 3.1 변경 내용

모터와 휠 인코더가 새것으로 교체됨 → 기존 캘리브레이션 값 무효.

**파일**: `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

| 항목 | 변경 전 | 변경 후 | 비고 |
|---|---|---|---|
| `ANGULAR_SCALE_FACTOR` | `0.401` | `1.0` | 이전 값 주석 처리, 1:1 passthrough |
| `MCU_MIN_ANGULAR` | `0.12` | `0.0` | 이전 값 주석 처리, 클램프 비활성화 |

---

## 4. MCU 펌웨어 분석

### 4.1 펌웨어 소스
`~/mcu/extbd` — git branch `transbot-encoder` (github: `p-trck/extbd`)

### 4.2 핵심 코드 경로 (CAR_MECANUM)

펌웨어 `Motion_Handle()` → `Motion_Get_Speed()` (CAR_MECANUM):

```c
// app_motion.c - Motion_Get_Speed()
// 바퀴 속도 계산
for (i = 0; i < 4; i++) {
    speed_mm[i] = -1 * (g_Encoder_All_Offset_Filtered[i]) * 100 * circle_mm / circle_pulse;
}
// CAR_MECANUM:
car->Vx = (speed_mm[0] + speed_mm[2]) / 2;
car->Vy = -(speed_mm[0] - speed_mm[2]) / 2;
car->Vz = -(speed_mm[0] - speed_mm[2]) / 2.0f / robot_APB * 1000;
```

엔코더 부호 (`bsp_encoder.c` - CAR_MECANUM):
```c
g_Encoder_M1_Now -= Encoder_Read_CNT(MOTOR_ID_M1);  // M1: 부호 반전
g_Encoder_M2_Now -= Encoder_Read_CNT(MOTOR_ID_M2);  // M2: 부호 반전
g_Encoder_M3_Now += Encoder_Read_CNT(MOTOR_ID_M3);  // M3: 양수
g_Encoder_M4_Now += Encoder_Read_CNT(MOTOR_ID_M4);  // M4: 양수
```

**Vx 부호 추적** (전진 시):
1. 엔코더 offset → 양수
2. `speed_mm = -1 * offset * ...` → **음수**
3. `Vx = (speed_mm[0] + speed_mm[2]) / 2` → **음수**
4. MCU → ROS 드라이버: Vx 음수
5. 드라이버에서 `vx = -vx` → **양수** (ROS 좌표 전진=+x와 일치)

### 4.3 MECANUM 상수

```c
// app_mecanum.h
#define MECANUM_APB        (95.0f)     // 좌우 바퀴 간격의 절반 (mm)
#define MECANUM_CIRCLE_MM  (150.796f)  // 바퀴 1회전 이동 거리 (mm)

// app_motion.c
ENCODER_CIRCLE_330 = 1320  // 1회전 엔코더 펄스 수
```

### 4.4 기타 발견사항

| 항목 | 내용 | 영향도 |
|---|---|---|
| 가속도 스케일 불일치 | ICM20948: accel×1000 전송, Rosmaster_Lib: ÷10000 파싱 → 1/10 | 낮음 (EKF 미사용) |
| `Motion_Get_Pwm()` /4 나눗셈 | CAR_DIFFERENTIAL 전용 함수에서 4바퀴 공식 사용하나 2바퀴만 활성 | 해당없음 (CAR_MECANUM 사용) |

---

## 5. 1차 CSV 분석 — position.x 역전 진단

### 5.1 데이터
첫 번째 CSV: 직선 전진 + 회전 테스트

### 5.2 결과
- `/odom` (EKF): position.x **+0.18**
- `/odom_adapted` (wheel): position.x **-0.15**
- `/odom_rf2o_adapted` (RF2O/LiDAR): position.x **-0.24**

사용자 확인: **RF2O가 ground truth**, odom_adapted가 RF2O와 같은 부호 → wheel이 정상.
EKF가 반대 부호인 것이 문제.

### 5.3 원인 (잘못된 진단 → 수정)
- 최초 `vx = -vx` 제거 시도 → **오진** (1차에서는 이전 빌드된 코드가 실행 중이었음)
- 이후 `imu_invert_z: true → false` 변경 → **오진** (IMU 물리적 장착 방향은 모터 교체와 무관)

---

## 6. 2차 CSV 분석 — 회전 불일치 진단

### 6.1 데이터
두 번째 CSV: 약 180° 회전 포함

### 6.2 회전 구간 (t=67~82s) 결과

| 센서 | Δyaw | 비고 |
|---|---|---|
| EKF (`/odom`) | **-24°** | 극심한 과소평가 |
| Wheel (`/odom_adapted`) | **-151°** | |
| RF2O (`/odom_rf2o_adapted`) | **-181°** | RF2O ≈ 실제 ≈ 180° |

### 6.3 imu_invert_z 복원

2차 CSV에서 `imu_invert_z: false`로 변경된 상태였음.
IMU gyro_z가 반전 없이 발행 → wheel vyaw와 **반대 부호** → EKF가 상쇄하여 -24°만 인식.

**수정**: `imu_invert_z: true`로 복원 (IMU는 보드에 고정, 모터 교체와 무관)

---

## 7. 3차 CSV 분석 — 빌드 누락 발견 및 해결

### 7.1 핵심 발견: 설치된 코드와 소스 코드 불일치

| 항목 | 설치된 코드 (452줄) | 소스 코드 (526줄) |
|---|---|---|
| `imu_invert_z` 적용 | 선언만, **미적용** | `if ... gz = -gz` 적용 |
| `vx = -vx` | 없음 | 있음 (이전 상태) |
| `ANGULAR_SCALE_FACTOR` | SLOPE/BIAS 방식 (이전 코드) | 1.0 |
| 파일 크기 | 452줄 | 526줄 |

**근본 원인**: 소스 코드에 `imu_invert_z` 적용 로직과 `vx = -vx` 등이 있었으나, **colcon build가 수행되지 않아** 설치된(실행 중인) 코드에는 해당 로직이 없었음.

### 7.2 3차 CSV 회전 데이터

빌드 후 테스트. 회전 구간 (t=362~380s):

| 센서 | 최종 yaw | 비고 |
|---|---|---|
| EKF | +44° | 여전히 과소평가 |
| Wheel | ~+170° | |
| RF2O | ~+147° | |

→ `imu_invert_z` 로직이 설치된 코드에 없어서 여전히 IMU와 wheel이 상쇄.

### 7.3 수정 및 빌드

1. **colcon build 수행** → 소스 코드의 `imu_invert_z` 로직이 설치됨
2. 설치된 코드 확인: line 423에 `if self.get_parameter('imu_invert_z').value: gz = -gz` 존재 ✅
3. **회전 문제 해결 확인** (다음 CSV에서 확인)

---

## 8. 4차 CSV 분석 — 전진/후진 역전 발생

### 8.1 증상
회전 문제 해결 후, 전진 시 odom이 후진으로 계산됨.

### 8.2 데이터 (전진 구간 t=10~40s)

| 센서 | position.x | 비고 |
|---|---|---|
| EKF | **-1.11** | 음수 (후진) |
| Wheel | **-1.19** | 음수 (후진) |
| RF2O | **+1.29** | 양수 (**전진** — ground truth) |

### 8.3 원인

이전 단계에서 `vx = -vx`를 `publish_velocity()`에서 제거했음:
- CAR_MECANUM의 `Motion_Get_Speed()`: `speed_mm = -1 * offset` → 전진 시 Vx **음수**
- `vx = -vx` 없으면 ROS에 음수 그대로 전달 → 후진으로 인식

### 8.4 수정

`publish_velocity()`에 `vx = -vx` **복원**:

```python
# [2026-04-17] CAR_MECANUM Motion_Get_Speed(): speed_mm = -1 * offset
# → MCU Vx가 전진 시 음수. ROS 좌표(전진=+x)와 반대이므로 부호 반전 필요.
vx = -vx
```

**colcon build 수행** → 설치된 코드에서 line 476에 `vx = -vx` 확인 ✅

---

## 9. 최종 코드 상태

### 9.1 jupiter_driver_compensated.py

| 항목 | 값 | 비고 |
|---|---|---|
| `ANGULAR_SCALE_FACTOR` | `1.0` | 이전 0.401 주석 처리 |
| `MCU_MIN_ANGULAR` | `0.0` | 이전 0.12 주석 처리 |
| `vx = -vx` (publish_velocity) | **적용** | MCU Vx 부호 보정 |
| `imu_invert_z` 로직 (publish_imu) | **적용** | gz = -gz when true |
| `imu_gyro_scale` 파라미터 | **적용** | 기본값 1.0 |

### 9.2 jupiter_driver_params.yaml

| 항목 | 값 |
|---|---|
| `imu_invert_z` | `true` |
| `imu_invert_x` | `false` |
| `imu_invert_y` | `false` |

### 9.3 MCU 펌웨어 (변경 없음)
- 차량 타입: `CAR_MECANUM`
- Motion 핸들러: `Motion_Get_Speed()` (엔코더 PID)
- 엔코더: M1 `-=`, M2 `-=`, M3 `+=`, M4 `+=`
- Mecanum_Ctrl: `speed_fb = -V_x`, `speed_spin = (-V_z/1000) * APB`

---

## 10. 변경된 파일 목록

| 파일 | 변경 내용 |
|---|---|
| `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` | ANGULAR_SCALE_FACTOR=1.0, MCU_MIN_ANGULAR=0.0, vx=-vx 복원, imu_invert_z 로직 (이전 빌드에서 누락된 것 빌드) |
| `src/jupiter_bringup/param/jupiter_driver_params.yaml` | imu_invert_z: true 유지 (변경→복원→유지) |

---

## 11. 남은 작업

- [ ] 회전 재캘리브레이션: `ANGULAR_SCALE_FACTOR` 값을 새 모터에 맞게 재측정 필요
- [ ] MECANUM 물리 상수 검증: `MECANUM_APB`(95.0mm), `MECANUM_CIRCLE_MM`(150.796mm), `ENCODER_CIRCLE_330`(1320) 값이 새 바퀴와 일치하는지 실측 필요
- [ ] 가속도 스케일 불일치: Rosmaster_Lib `accel_ratio = 1/10000.0` vs MCU `accel×1000` (현재 EKF 미사용으로 영향 없음)
