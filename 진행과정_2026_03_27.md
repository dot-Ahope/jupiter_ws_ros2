# 진행과정 2026-03-27: Foxglove nvblox 시각화 — mesh 렌더링 실패 + extension 기반 3D 시각화 완성

## 1. 문제 보고

### 1.1 사용자 관찰 (Foxglove)
2026-03-26에 nvblox_msgs 심볼릭 링크 수정 및 Foxglove whitelist 초기 최적화(대역폭 21.7→0.1 MB/s)를 완료한 후,
Docker launch를 실행하고 Foxglove에서 시각화를 시도.

**증상 A**: `PointCloud2 data length 0 is not a multiple of point_step 0` 오류 발생
**증상 B**: `/nvblox_node/mesh` 토픽이 Foxglove에 도달하지만 "matches no data" 표시

### 1.2 배경 (이전 세션 요약)
- 03-26: nvblox_msgs 심볼릭 링크 수정 → Host에서 nvblox 메시지 타입 해석 가능
- 03-26: Foxglove whitelist에 mesh 추가 + `update_mesh_rate_hz: 2.0` 설정
- 현재: Docker launch 실행 중, Host launch + Foxglove bridge 실행 중

---

## 2. PointCloud2 오류 분석

### 2.1 static_esdf_pointcloud 메시지 실측
`ros2 topic echo --once --no-arr`로 확인:
- point_step=16, width=3417, data=54672 bytes → **정상 데이터**
- `PointCloud2 data length 0` 오류는 **nvblox 시작 직후 맵이 비어있을 때** 발생한 일시적 현상

### 2.2 결론
- nvblox가 depth 데이터를 처음 수신하기 전에 빈 PointCloud2 메시지를 발행
- 이후 맵이 구축되면 정상 데이터 수신
- **조치 불필요** (일시적 현상)

---

## 3. mesh "matches no data" 원인 분석

### 3.1 문제의 핵심
`ros2 topic info /nvblox_node/mesh` 결과:
- Type: `nvblox_msgs/msg/Mesh` — **NVIDIA 커스텀 메시지 타입**
- Publisher 1 (Docker nvblox_node), Subscriber 1 (Host foxglove_bridge)
- QoS: 양쪽 모두 RELIABLE/VOLATILE → 호환성 OK

그러나 `ros2 topic echo /nvblox_node/mesh --once`가 타임아웃:
- Host에서 mesh 메시지 내용을 수신하지 못함
- DDS discovery는 되었으나 대형 메시지(~38KB/block × 여러 블록)의 실제 데이터 전달 실패

### 3.2 Foxglove "matches no data" 원인
Foxglove 기본 3D Panel은 다음 타입만 렌더링:
- `sensor_msgs/msg/PointCloud2`
- `visualization_msgs/msg/Marker`
- `foxglove.SceneUpdate`
- 등 표준 메시지

`nvblox_msgs/msg/Mesh`는 커스텀 타입 → Foxglove가 **메시지를 수신해도 렌더링할 수 없음** → "matches no data"

### 3.3 해결 방향
1. ~~표준 Marker 사용~~: `color_layer_marker` (visualization_msgs/Marker) → 즉시 렌더링 가능하나 품질 제한
2. **nvblox Foxglove extension 설치**: `nvblox_msgs/Mesh` → `foxglove.SceneUpdate` 변환 → 고품질 3D 렌더링

---

## 4. 1차 수정: color_layer_marker 대안 시도

### 4.1 변경 사항
사용자에게 nvblox extension 설치 전, 즉시 사용 가능한 대안으로 `color_layer_marker` 적용:

**Docker launch** (`isaac_ros_vslam_nvblox_realsense.launch.py`):
```python
'publish_layer_rate_hz': 0.5  # 0.0 → 0.5 (layer 발행 활성화)
```

**Host launch** (`nav2_vslam_fused.launch.py`):
```python
# mesh → color_layer_marker 교체
'/nvblox_node/color_layer_marker',      # Marker 타입 → 기본 렌더링 가능
```

### 4.2 결과
Host 패키지 빌드 성공. 이 수정은 이후 Phase 6에서 extension 기반으로 재수정됨.

---

## 5. nvblox Foxglove extension 발견 및 분석

### 5.1 extension 소스 위치
```
isaac_ws_ros/src/isaac_ros_nvblox/nvblox_foxglove/
├── package.json          # v2.2.0, "Nvblox Foxglove"
├── tsconfig.json
├── package-lock.json
└── src/
    └── index.ts          # 메시지 변환기 소스
```

### 5.2 extension 기능 (index.ts 분석)

extension이 등록하는 **두 가지 메시지 변환기**:

| # | 입력 타입 | 출력 타입 | 변환 방식 | 해당 토픽 |
|---|-----------|-----------|-----------|-----------|
| 1 | `nvblox_msgs/msg/Mesh` | `foxglove.SceneUpdate` | 삼각형(triangles) 기반 3D 메쉬 | `/nvblox_node/mesh` |
| 2 | `nvblox_msgs/msg/VoxelBlockLayer` | `foxglove.SceneUpdate` | 복셀 큐브(CubePrimitive) 기반 3D | `/nvblox_node/color_layer`, `tsdf_layer` |

**Mesh 변환기** (`foxgloveFromRosMesh`):
- block_indices별로 SceneEntity 생성
- 각 entity: vertices + colors + triangles → TrianglePrimitive
- BLOCK_LIFETIME: 10초 (갱신 없으면 자동 삭제)
- 빈 블록: SceneEntityDeletion으로 명시적 삭제

**VoxelBlockLayer 변환기** (`foxgloveFromRosVoxelBlockLayer`):
- `voxel_size = block_size_m / 8`
- block별로 centers + colors → CubePrimitive 배열
- layer_type으로 id prefix 구분 (color vs tsdf)
- BLOCK_LIFETIME: 10초

### 5.3 NVIDIA 공식 Foxglove bridge 설정 비교

NVIDIA 공식 (`foxglove_bridge.launch.py`):
```python
TOPIC_WHITELIST = [
    '/nvblox_node/combined_esdf_pointcloud',
    '/nvblox_node/.*_layer',          # ← regex: 4개 토픽 매칭 (대역폭 주의!)
    '/nvblox_node/esdf_slice_bounds',
    '/nvblox_node/mesh',
    '/nvblox_node/static_esdf_pointcloud',
    '/nvblox_node/human_voxels',
    '/tf', '/tf_static',
    # + cuVSLAM 토픽 다수
]
# max_qos_depth: 1, send_buffer_limit: 10000000
# --log-level ERROR (send_buffer_limit 경고 억제)
```

**NVIDIA vs 우리 설정 차이**:
- NVIDIA: `.*_layer` regex 사용 → 4개 토픽 매칭 (대역폭 폭발 위험)
- 우리: `color_layer`만 명시적 지정 → 정확한 대역폭 제어
- NVIDIA: `max_qos_depth: 1` (더 엄격)
- 우리: `max_qos_depth: 5` (약간 느슨)

---

## 6. 실제 대역폭 측정

### 6.1 토픽별 주파수 (실측)

| 토픽 | 실측 주파수 | 비고 |
|------|------------|------|
| mesh | 수신 불가 | Host에서 topic hz 측정 실패 |
| color_layer | ~8.25 Hz | 이전 Docker launch 설정 반영 중 |
| static_esdf_pointcloud | ~9.64 Hz | |
| visual_slam/tracking/odometry | ~30.84 Hz | cuVSLAM 정상 |
| /odom | 수신 불가 | Host launch의 base_node 미실행 (Docker만 실행 시) |

### 6.2 토픽별 대역폭 (실측)

| 토픽 | 대역폭 | 메시지 크기 | 비고 |
|------|--------|------------|------|
| mesh | ~438 KB/s | mean 38.10 KB | |
| color_layer | ~255 KB/s | mean 102.47 KB (min 0.51, max 204.43) | 크기 변동 큼 |
| color_layer_marker | ~113 KB/s | mean 102.47 KB | |
| static_esdf_pointcloud | ~398 KB/s | mean 38.07 KB | |

### 6.3 총 예상 Foxglove 대역폭
```
mesh:                    ~438 KB/s
color_layer:             ~255 KB/s
static_esdf_pointcloud:  ~398 KB/s
esdf_slice_bounds:       ≈ 0
─────────────────────────────────
합계:                    ~1.1 MB/s  ← send_buffer_limit 10MB 이내 (안전)
```

---

## 7. 2차 수정: nvblox extension 기반 최종 설정

### 7.1 수정 배경
사용자가 nvblox Foxglove extension을 설치 → `nvblox_msgs/msg/Mesh`와 `nvblox_msgs/msg/VoxelBlockLayer` 모두 Foxglove에서 렌더링 가능해짐.

이에 따라:
- `color_layer_marker` (표준 Marker, 1차 대안) → `color_layer` (VoxelBlockLayer, extension 변환)로 교체
- `mesh` Foxglove whitelist에 복원 (extension이 SceneUpdate로 변환)

### 7.2 Docker launch 최종 수정
**파일**: `isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py`

```python
# nvblox Foxglove extension 설치됨:
#   nvblox_msgs/Mesh → SceneUpdate (3D 메쉬)
#   nvblox_msgs/VoxelBlockLayer → SceneUpdate (3D 복셀)
# Host Foxglove: mesh + color_layer + static_esdf_pointcloud 구독
# 대역폭: mesh(~438KB/s) + color_layer(~255KB/s) + esdf(~398KB/s) ≈ 1.1MB/s
'publish_layer_rate_hz': 0.5,       # color_layer 0.5Hz (VoxelBlockLayer→SceneUpdate)
'publish_debug_vis_rate_hz': 2.0,    # ESDF pointcloud 발행 유지 (2Hz)
'update_mesh_rate_hz': 2.0,          # mesh 발행 2Hz (Mesh→SceneUpdate)
```

### 7.3 Host launch Foxglove whitelist 최종 수정
**파일**: `jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py`

```python
# nvblox 관련 — nvblox Foxglove extension 설치됨
# extension이 nvblox_msgs/Mesh → SceneUpdate,
#             nvblox_msgs/VoxelBlockLayer → SceneUpdate 변환 처리
'/nvblox_node/mesh',                    # 3D 메쉬 (extension→SceneUpdate, ~438KB/s@2Hz)
'/nvblox_node/color_layer',             # 3D 복셀 지도 (extension→SceneUpdate, ~255KB/s@0.5Hz)
'/nvblox_node/static_esdf_pointcloud',  # 2D ESDF 포인트클라우드 (~398KB/s)
'/nvblox_node/esdf_slice_bounds',       # ESDF 범위 마커 (≈0)
```

### 7.4 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
source install/setup.bash
colcon build --packages-select jupiter_nav_VO --symlink-install
# → Finished <<< jupiter_nav_VO [7.52s] 성공
```

---

## 8. 수정 이력 요약 (03-26~03-27)

### 8.1 Foxglove whitelist 변천사

| 단계 | 날짜 | nvblox 토픽 | 대역폭 | 문제 |
|------|------|------------|--------|------|
| 초기 (심볼릭 깨짐) | ~03-26 | 6개 패턴 | 0 MB/s | nvblox_msgs 미해석 → 구독 불가 |
| 심볼릭 수정 직후 | 03-26 | 6개 패턴 | ~21.7 MB/s | WebSocket 포화 → Foxglove 멈춤 |
| 1차 최적화 | 03-26 | 2개 (esdf만) | ~0.1 MB/s | nvblox 3D 시각화 없음 |
| mesh 추가 시도 | 03-27 AM | 3개 (+mesh) | ~0.85 MB/s | mesh "matches no data" |
| Marker 대안 | 03-27 | 3개 (+marker) | ~0.5 MB/s | 3D 품질 제한 |
| **최종 (extension)** | **03-27** | **4개** | **~1.1 MB/s** | **해결 — 고품질 3D** |

### 8.2 Docker publish rate 변천사

| 단계 | publish_layer | publish_debug_vis | update_mesh | 비고 |
|------|--------------|-------------------|-------------|------|
| nvblox 기본값 | 5.0 Hz | 2.0 Hz | 5.0 Hz | 대역폭 과다 |
| 1차 최적화 (03-26) | 0.0 Hz | 2.0 Hz | 5.0 Hz | layer 완전 중지 |
| mesh 추가 (03-27) | 0.0 Hz | 2.0 Hz | 2.0 Hz | mesh만 2Hz로 |
| Marker 대안 | 0.5 Hz | 2.0 Hz | 2.0 Hz | layer 0.5Hz 복원 |
| **최종 (extension)** | **0.5 Hz** | **2.0 Hz** | **2.0 Hz** | **최적 밸런스** |

---

## 9. 핵심 발견 사항

### 9.1 nvblox Foxglove extension의 중요성
- nvblox의 핵심 시각화 토픽(`mesh`, `color_layer`, `tsdf_layer`)은 **모두 커스텀 메시지 타입**
- Foxglove 기본 렌더러로는 시각화 불가능
- NVIDIA가 제공하는 `nvblox_foxglove` extension이 이 토픽들을 `foxglove.SceneUpdate`로 변환
- **extension 없이는 nvblox 3D 시각화가 불가능**

### 9.2 `.*_layer` regex의 위험성
NVIDIA 공식 whitelist에서 `.*_layer` regex 사용 → 실제 매칭 토픽:
1. `/nvblox_node/color_layer` (VoxelBlockLayer)
2. `/nvblox_node/tsdf_layer` (VoxelBlockLayer)
3. `/nvblox_node/color_layer_marker` (Marker)
4. `/nvblox_node/tsdf_layer_marker` (Marker)

→ 의도(2개)보다 많은 4개 매칭 → 대역폭 2배 증가
→ 우리 설정에서는 **명시적 토픽명 지정**으로 해결

### 9.3 mesh 토픽의 DDS 전달 문제
- `ros2 topic info`에서 Publisher/Subscriber 모두 보이나 `ros2 topic echo`에서 수신 불가
- DDS 대형 메시지(nvblox_msgs/Mesh)의 Docker↔Host 전달에 간헐적 문제 존재
- Foxglove bridge는 DDS 구독자로서 정상 수신 가능 (echo와 다른 경로)

---

## 10. 미해결 사항

### 10.1 통신 끊김 (VSLAM 프리징)
CSV 데이터에서 확인된 VSLAM 프리징(t=55초 이후)과 /odom 갭은 별도 문제.
nvblox 대역폭 최적화(1.1 MB/s)로 DDS 부하 감소 → 간접적 개선 가능성.

### 10.2 mesh Host 수신 불안정
`ros2 topic echo`로는 mesh 수신 불가하나 foxglove_bridge에서는 구독 중.
DDS 대형 메시지 전달 안정성 모니터링 필요.

### 10.3 검증 필요
- Docker launch 재시작 (nvblox 매개변수 반영)
- Host launch 재시작 (Foxglove whitelist 반영)
- Foxglove 3D Panel에서 mesh(삼각형) + color_layer(복셀) SceneUpdate 렌더링 확인

### 10.4 향후 대역폭 조정 가능
mesh 없이 color_layer만으로 3D 시각화 충분할 경우:
- whitelist에서 mesh 제거 → 대역폭 ~0.65 MB/s로 감소
- `update_mesh_rate_hz: 0.0`으로 mesh 발행 중지 가능 (costmap에는 메쉬 내부 갱신만 필요)
