# =============================================================================
# Nav2 Navigation Parameters for 4-Sensor EKF Fusion + SLAM Toolbox System
# =============================================================================
#
# 시스템 구성:
#   - 센서 융합: nav2_vslam_fused.launch.py (EKF: wheel + VSLAM + RF2O + IMU)
#   - 위치 추정: SLAM Toolbox (map→odom TF) + EKF (odom→base_footprint TF)
#   - 장애물 감지: RPLidar S2L (/scan)
#   - 오도메트리: /odom (EKF 융합 출력, 30Hz)
#
# TF 체인:
#   map → odom (SLAM Toolbox)
#       → base_footprint (EKF)
#           → base_link (URDF, +8cm Z)
#               → laser (URDF, -3cm X, +13cm Z, 180° yaw)
#
# 주의: AMCL 사용하지 않음 — SLAM Toolbox가 map→odom TF 제공
#       map_server 사용하지 않음 — SLAM Toolbox가 실시간 맵 제공
#
# 사용법:
#   1. nav2_vslam_fused.launch.py 먼저 실행 (센서 융합 + SLAM Toolbox)
#   2. 이 파일을 사용하는 nav2 launch를 별도로 실행
#
# =============================================================================

# ==============================================================
# Behavior Tree Navigator
# ==============================================================
bt_navigator:
  ros__parameters:
    use_sim_time: False
    global_frame: map
    robot_base_frame: base_footprint
    odom_topic: /odom
    bt_loop_duration: 10
    default_server_timeout: 20
    enable_groot_monitoring: False
    # 2026-04-24 (Stage 3): custom BT XML — backup 제거, Spin + Wait + Clearing round-robin.
    # 좁은 통로에서 뒤로 밀리는 현상 억제. 복구는 시야 재확보(Spin) + 장애물 clear 로만 수행.
    default_nav_to_pose_bt_xml: "$(find-pkg-share jupiter_nav_VO)/behavior_trees/navigate_to_pose_no_backup.xml"
    plugin_lib_names:
    - nav2_compute_path_to_pose_action_bt_node
    - nav2_compute_path_through_poses_action_bt_node
    - nav2_follow_path_action_bt_node
    - nav2_back_up_action_bt_node
    - nav2_spin_action_bt_node
    - nav2_wait_action_bt_node
    - nav2_clear_costmap_service_bt_node
    - nav2_is_stuck_condition_bt_node
    - nav2_goal_reached_condition_bt_node
    - nav2_goal_updated_condition_bt_node
    - nav2_initial_pose_received_condition_bt_node
    - nav2_reinitialize_global_localization_service_bt_node
    - nav2_rate_controller_bt_node
    - nav2_distance_controller_bt_node
    - nav2_speed_controller_bt_node
    - nav2_truncate_path_action_bt_node
    - nav2_goal_updater_node_bt_node
    - nav2_recovery_node_bt_node
    - nav2_pipeline_sequence_bt_node
    - nav2_round_robin_node_bt_node
    - nav2_transform_available_condition_bt_node
    - nav2_time_expired_condition_bt_node
    - nav2_distance_traveled_condition_bt_node
    - nav2_single_trigger_bt_node
    - nav2_is_battery_low_condition_bt_node
    - nav2_navigate_through_poses_action_bt_node
    - nav2_navigate_to_pose_action_bt_node
    - nav2_remove_passed_goals_action_bt_node
    - nav2_planner_selector_bt_node
    - nav2_controller_selector_bt_node
    - nav2_goal_checker_selector_bt_node

# ==============================================================
# Controller Server (DWB Local Planner)
# ==============================================================
# Jupiter 차동구동 로봇 파라미터:
#   바퀴 지름: 68.5mm, 축간 거리: 329.11mm
#   최대 선속도: ~0.45 m/s, 최대 각속도: ~2.0 rad/s
# ==============================================================
controller_server:
  ros__parameters:
    use_sim_time: False
    controller_frequency: 10.0          # Jetson 부하 고려 (20→10Hz)
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.5
    min_theta_velocity_threshold: 0.10  # ⭐ [2026-04-21] 0.05→0.10: 정지 회전 시 실기구가 못 도는 0.0526 영역 차단
    failure_tolerance: 5.0              # 2026-04-28: 3.0→5.0 (사람 통로 횡단 대기 시간 확보, 50 cycles @10Hz)
    progress_checker_plugin: "progress_checker"
    goal_checker_plugins: ["general_goal_checker"]
    controller_plugins: ["FollowPath"]

    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.001   # ⭐ [2026-04-21] 5mm→1mm: goal 근접 정렬 중 정지 회전/미세 드리프트를 progress 실패로 오판하지 않도록 완화
      movement_time_allowance: 120.0    # ⭐ [2026-04-21] 45s→120s: near-goal 장시간 yaw 정렬 중 불필요한 backup 진입 억제

    general_goal_checker:
      stateful: True
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.20          # ⭐ 0.15→0.20 (DWB xy_tol=0.25 보다 좁아야 함, 여유 확보)
      yaw_goal_tolerance: 0.32          # ⭐ [2026-04-21] 0.30→0.32: near-goal 정렬 마무리에서 success 판정 여유를 소폭 확대해 불필요한 progress timeout 억제

    # ==============================================================
    # RotationShimController → DWB 래핑
    # ==============================================================
    # 정지 상태에서 순수 회전(vx=0, vθ≠0) 시 양쪽 바퀴가 반대 방향으로
    # 정지마찰을 동시에 극복해야 하므로, DWB의 낮은 angular.z 출력으로는
    # 모터가 응답하지 못함 (데드존). RotationShimController가 정지 회전을
    # 고정 속도(0.8 rad/s)로 처리한 후, DWB에 전달.
    # ==============================================================
    FollowPath:
      plugin: "nav2_rotation_shim_controller::RotationShimController"
      primary_controller: "dwb_core::DWBLocalPlanner"
      angular_dist_threshold: 0.35        # ⭐ 45°→20°: RotationShim이 160° 회전 담당, DWB는 20°만 보otion [2026-03-19]
      # 2026-04-28 (수정 #3): 0.6 → 0.3 복원
      # 이유: 04-24 에 0.3→0.6 으로 키웠으나, 짧은 경로(<1.2m)에서 forward sample 지점이
      #   path 끝 너머로 가서 RotationShim 활성 판단이 부정확 → 정지 회전이 DWB 로 떨어져
      #   데드존 진입. 03-18 검증값 0.3 으로 복원.
      forward_sampling_distance: 0.3
      rotate_to_heading_angular_vel: 0.5 # ⭐ 0.8→0.5 [2026-03-20] 방안A-4 ESC deadzone skip 후 0.5도 즉시 토크 발생. 0.8은 PID 적분 모멘텀 과다→핸드오프 오버슈트
      max_angular_accel: 3.0             # ⭐ 5.0→3.0 [2026-03-20] 0.5 목표 시 5.0 불필요. 3.0이면 0→0.5 도달 0.17s. RotateToGoal 급가속 억제
      simulate_ahead_time: 1.5           # 2026-04-28: 1.0→1.5 (회전 후 충돌 검사 시간 확대, path 추종 안정)

      # DWB 파라미터 (primary_controller)
      debug_trajectory_details: True
      min_vel_x: 0.0
      min_vel_y: 0.0
      max_vel_x: 0.20              # ⭐ 0.35→0.20 [2026-04-20] 후진 쏠림 방지. 저속에서 밸런스 보정 충분
      max_vel_y: 0.0               # 차동구동 — Y 속도 없음
      max_vel_theta: 1.0           # 각속도 최대값 (방안A 펌웨어 적용으로 1.0 복원)
      min_speed_xy: 0.0
      max_speed_xy: 0.20
      min_speed_theta: 0.20        # ⭐ [2026-04-21] 0.15→0.20: slowing_factor 출력이 driver ROT_MIN(0.08) 이상이도록 안전 하한 확보
      acc_lim_x: 0.5
      acc_lim_y: 0.0
      acc_lim_theta: 0.8           # 각가속도 (방안A 펌웨어 적용으로 0.8 복원)
      decel_lim_x: -0.5
      decel_lim_y: 0.0
      decel_lim_theta: -0.8        # 각감속도 (방안A 펌웨어 적용으로 -0.8 복원)
      vx_samples: 20
      vy_samples: 1                # 차동구동이므로 Y 샘플 최소화
      # 2026-04-28 (수정 #3): 21 → 11 복원. 04-24 에 21 로 키웠으나 04-21 분석:
      #   "1/19=0.0526 병적 최소 샘플 제거, 최소 스텝≈0.10" — 11 이 데드존 안전.
      #   21 은 sample 평가 시간 2배 → 10Hz controller cycle 압박 → 정지 회전 timing 불안.
      vtheta_samples: 11
      # 2026-04-24 (Stage 2): 1.5 → 1.0. 예측 거리 0.3→0.2m. 좁은 통로에서 trajectory 가
      # 부풀어진 lethal 영역에 걸릴 확률 감소 → DWB 가 유효 trajectory 를 더 쉽게 찾음.
      sim_time: 1.0
      linear_granularity: 0.05
      angular_granularity: 0.025
      transform_tolerance: 0.5           # stamp=Time(0,0) + NTP chrony 적용 후 축소 (3.0→0.5)
      xy_goal_tolerance: 0.25          # RotateToGoal 활성 영역 확대 — 회전 중 드리프트로 토글 방지
      trans_stopped_velocity: 0.05        # 정지 판정
      short_circuit_trajectory_evaluation: True
      stateful: True
      critics: ["RotateToGoal", "Oscillation", "BaseObstacle", "GoalAlign", "PathAlign", "PathDist", "GoalDist", "Twirling"]
      # 2026-04-24 (Stage 2): BaseObstacle 0.02 → 0.05 (장애물 회피 비용 소폭 상향,
      # 여전히 PathDist 대비 작아 경로 추종은 유지하지만 free-but-close trajectory 가 경쟁에서 덜 밀림).
      BaseObstacle.scale: 0.05
      # 2026-04-24 (Stage 2): PathAlign/PathDist 32 → 20 (좁은 통로 회피 위해 plan 구속력 완화)
      # 2026-04-28 (수정 #2): PathDist 20→28 (한쪽 바퀴 보정 강화)
      # 2026-04-28 (수정 #3): PathDist 28→24 절충 — 28 이 너무 강해 회전 trajectory 까지
      #   reject → 정지 회전 어려움 발생. 24 로 회전 자유도 회복하면서 위치 추종 일정 확보.
      PathAlign.scale: 20.0
      # 2026-04-28 (수정 #4): forward_point_distance 0.1 → 0.2
      #   path 의 더 앞 지점(0.2m 앞) 의 heading 으로 정렬 → preemptive turn-in,
      #   MCU yaw 보정 활성화와 함께 path 추종 정확도 향상.
      PathAlign.forward_point_distance: 0.2
      GoalAlign.scale: 24.0
      GoalAlign.forward_point_distance: 0.2
      PathDist.scale: 24.0
      GoalDist.scale: 24.0
      RotateToGoal.scale: 32.0
      RotateToGoal.slowing_factor: 2.0   # ⭐ [2026-04-21] 2.5→2.0: final yaw 정렬 출력 하한 확보(0.32/2.0=0.16)로 정지 회전 구간 정체 감소
      RotateToGoal.lookahead_time: -1.0
      Twirling.scale: 5.0                # 경로 추종 중 불필요한 스핀 방지 (방안A 적용으로 5.0 복원)
      Oscillation.oscillation_reset_dist: 0.15
      Oscillation.oscillation_reset_angle: 0.30
      Oscillation.scale: 1.0              # 진동 페널티 (방안A 적용으로 1.0 복원)

# ==============================================================
# Planner Server (SmacPlanner2D Global Planner)
# ==============================================================
# 2026-04-28 (수정 #2): SmacPlannerHybrid → SmacPlanner2D 다운그레이드
# 이유: Hybrid + DUBIN 은 minimum_turning_radius=0.20m 곡선 plan 을 생성하는데,
#   2바퀴 차동구동 + 한쪽 바퀴 기구 이상 환경에서:
#     · 곡선 plan 추종 시 좌우 속도차 명령 → 이상 바퀴 시 이탈 누적
#     · RotationShim 의 정지 회전 처리가 무력화됨 (plan 자체에 회전 곡선 포함)
#     · 결과: 방향 못 잡는 증상
#   SmacPlanner2D 는 motion model 없는 holonomic A* (NavFn 진화형):
#     · 자유 회전 plan → RotationShim 이 정지 회전 처리 (NavFn 시절 동작 회복)
#     · footprint collision check 정확 (좁은 통로 통과 능력 NavFn 보다 우월)
#     · minimum_turning_radius / motion_model 등 차동구동 부적합 파라미터 불필요
# 백업: backup_2026_04_28/nav2_params_fused.yaml.bak (Hybrid + DUBIN 시절)
# ==============================================================
planner_server:
  ros__parameters:
    expected_planner_frequency: 3.0
    use_sim_time: False
    transform_tolerance: 0.5
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "nav2_smac_planner/SmacPlanner2D"
      downsample_costmap: false           # costmap 해상도 그대로 (5cm)
      downsampling_factor: 1
      tolerance: 0.25                     # 목표 근접
      allow_unknown: true                 # SLAM 미탐색 영역 허용
      max_iterations: 1000000             # 충분한 탐색 깊이
      max_on_approach_iterations: 1000
      max_planning_time: 5.0              # 최대 5초
      cost_travel_multiplier: 2.0         # cost 셀 회피 가중치 (NavFn cost_factor 와 유사)
      use_final_approach_orientation: false  # final yaw 는 RotateToGoal critic 이 처리

# ==============================================================
# Behavior Server (Recovery Behaviors)
# ==============================================================
behavior_server:
  ros__parameters:
    use_sim_time: False
    costmap_topic: local_costmap/costmap_raw
    footprint_topic: local_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins: ["spin", "backup", "wait"]
    spin:
      plugin: "nav2_behaviors/Spin"
    backup:
      plugin: "nav2_behaviors/BackUp"
    wait:
      plugin: "nav2_behaviors/Wait"
    global_frame: odom
    robot_base_frame: base_footprint
    transform_tolerance: 0.5            # stamp=Time(0,0) + NTP chrony 적용 후 축소 (3.0→0.5)
    simulate_ahead_time: 2.0
    max_rotational_vel: 0.8
    min_rotational_vel: 0.15
    rotational_acc_lim: 0.8

# ==============================================================
# Local Costmap
# ==============================================================
# LiDAR /scan 기반 장애물 감지 (2D)
# SLAM Toolbox가 /scan을 사용하므로, costmap도 동일 센서 활용
# ==============================================================
local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 2.0
      global_frame: odom
      robot_base_frame: base_footprint
      use_sim_time: False
      rolling_window: true
      # 2026-04-24 (Stage 2): 3 → 5. 좁은 통로에서 DWB 가 회피 trajectory 탐색할 공간 여유.
      # 셀 수 60×60 → 100×100 (2.78배). CPU 감내 범위.
      width: 5
      height: 5
      resolution: 0.05
      transform_tolerance: 0.5          # stamp=Time(0,0) + NTP chrony 적용 후 축소 (3.0→0.5)
      # Jupiter footprint (360mm × 245mm)
      footprint: "[ [0.18, 0.1225], [0.18, -0.1225], [-0.18, -0.1225], [-0.18, 0.1225] ]"
      plugins: ["obstacle_layer", "nvblox_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: scan
        scan:
          # 2026-04-24 (Stage 5): /scan → /scan_merged (RPLidar + D455 depth 병합)
          # 저층 2~3cm 창틀도 obstacle_layer 에 반영되어 local costmap 회피 가능.
          topic: /scan_merged
          max_obstacle_height: 2.0
          min_obstacle_height: 0.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 8.0
          raytrace_min_range: 0.1
          obstacle_max_range: 5.0
          obstacle_min_range: 0.1
          inf_is_valid: False
      # nvblox 3D → 2D ESDF costmap layer (Docker nvblox_node에서 발행)
      # nvblox_node 미실행 시 토픽 미수신 → 영향 없음 (graceful degradation)
      # ⭐ [2026-04-20] static_map_slice → combined_map_slice: 사람 감지+정적 장애물 합산 (people_segmentation 모드)
      nvblox_layer:
        plugin: "nvblox::nav2::NvbloxCostmapLayer"
        enabled: True
        nav2_costmap_global_frame: "odom"   # local_costmap global_frame과 일치
        nvblox_map_slice_topic: "/nvblox_node/combined_map_slice"
        convert_to_binary_costmap: True     # NVIDIA 권장: inflation은 별도 layer 사용
        max_obstacle_distance: 1.0
        # 2026-04-24: 0.5 → 0.0. convert_to_binary_costmap:True 와 일관된 구성.
        # nvblox_layer 는 obstacle binary 만 표시, inflation 은 inflation_layer 단독 책임.
        # 기존 0.5m + inflation_layer 0.30m 이중 inflation 이 1m 문/통로 통과를 막던 주 원인.
        inflation_distance: 0.0
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        # 2026-04-28: 10.0 → 5.0 (감쇠 더 완만, SmacPlanner 의 cost_penalty 와 균형)
        cost_scaling_factor: 5.0
        # 2026-04-28: 0.30 → 0.20 (좁은 통로 통과 우선, 1m 문 free 폭 0.40→0.60m)
        # SmacPlanner 가 footprint 충돌 검사를 정확히 하므로 inflation 작아도 안전.
        inflation_radius: 0.20

# ==============================================================
# Global Costmap
# ==============================================================
# SLAM Toolbox가 제공하는 /map을 static layer로 사용
# + LiDAR /scan으로 동적 장애물 감지
# ==============================================================
global_costmap:
  global_costmap:
    ros__parameters:
      update_frequency: 1.0
      publish_frequency: 0.5
      global_frame: map
      robot_base_frame: base_footprint
      use_sim_time: False
      transform_tolerance: 0.5          # stamp=Time(0,0) + NTP chrony 적용 후 축소 (3.0→0.5)
      footprint: "[ [0.18, 0.1225], [0.18, -0.1225], [-0.18, -0.1225], [-0.18, 0.1225] ]"
      resolution: 0.05
      track_unknown_space: true
      # SLAM Toolbox의 맵을 사용 — static_layer가 /map 토픽을 구독
      # ⭐ [2026-04-07] obstacle_layer 제거:
      #   - SLAM Toolbox가 /map을 통해 이미 장애물 정보 제공 (static_layer)
      #   - 실시간 장애물 회피는 local_costmap의 obstacle_layer가 담당
      #   - global costmap의 obstacle_layer는 중복이며, 로봇이 맵 경계에 있을 때
      #     "Sensor origin out of map bounds" 경고와 함께 raytrace 완전 실패
      #     (LiDAR가 base_link에서 -3cm X 오프셋 → 맵 좌측 경계와 겹침)
      plugins: ["static_layer", "nvblox_layer", "inflation_layer"]
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
        # /map 토픽 (SLAM Toolbox 발행)을 자동 구독
      # nvblox 3D → 2D ESDF costmap layer (Docker nvblox_node에서 발행)
      # ⭐ [2026-04-20] static_map_slice → combined_map_slice: 사람 감지+정적 장애물 합산
      nvblox_layer:
        plugin: "nvblox::nav2::NvbloxCostmapLayer"
        enabled: True
        nav2_costmap_global_frame: "map"    # global_costmap global_frame과 일치
        nvblox_map_slice_topic: "/nvblox_node/combined_map_slice"
        convert_to_binary_costmap: True
        max_obstacle_distance: 1.0
        inflation_distance: 0.0         # 2026-04-24: local 과 동일 (이중 inflation 제거)
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        # 2026-04-28: local 과 동일하게 완화 (10→5, 0.30→0.20)
        cost_scaling_factor: 5.0
        inflation_radius: 0.20

# ==============================================================
# Velocity Smoother
# ==============================================================
# navigation_launch.py가 기본 생성하는 velocity_smoother 노드의 파라미터
# controller_server → /cmd_vel_nav → velocity_smoother → /cmd_vel → driver
# 이 설정 없으면 기본값(max_accel:[2.5,0,3.2]) 사용 — 로봇 특성과 불일치 [2026-03-18]
velocity_smoother:
  ros__parameters:
    use_sim_time: False
    smoothing_frequency: 20.0
    scale_velocities: false
    feedback: "OPEN_LOOP"
    max_velocity: [0.20, 0.0, 1.0]       # ⭐ 0.35→0.20 [2026-04-20] DWB max_vel_x와 동기화
    min_velocity: [-0.20, 0.0, -1.0]
    max_accel: [0.5, 0.0, 2.5]           # ⭐ angular 0.8→2.5: ESC 데드존 구간 빠르게 통과 [2026-03-19]
    max_decel: [-0.5, 0.0, -2.5]         # ⭐ angular -0.8→-2.5: 회전 정지 시 데드존 통과 최소화 [2026-03-19]
    odom_topic: "odom"
    odom_duration: 0.1
    deadband_velocity: [0.0, 0.0, 0.05]  # ⭐ 0.20→0.05: 0.20은 DWB 헤딩보정(0.05~0.15) 차단→Goal1 TIMEOUT. 노이즈만 필터링 [2026-03-19]
    velocity_timeout: 1.0

# ==============================================================
# Waypoint Follower
# ==============================================================
waypoint_follower:
  ros__parameters:
    use_sim_time: False
    loop_rate: 20
    stop_on_failure: false
    waypoint_task_executor_plugin: "wait_at_waypoint"
    wait_at_waypoint:
      plugin: "nav2_waypoint_follower::WaitAtWaypoint"
      enabled: True
      waypoint_pause_duration: 200
