# 명령어 로그 2026-04-20: 시스템 분석 + nvblox 통합 + 속도 제한 + TF 수정 + 시각화 구성

> 진단 및 수정에 사용한 명령어와 결과를 요약 없이 전부 기록한다.

---

## 1. 시스템 종합 분석 (MCU 펌웨어 + ROS2 드라이버)

### 1.1 MCU 펌웨어 파일 분석

**목적**: jupiter_ws_ros2 + MCU 펌웨어 전체 코드 분석

```bash
# MCU 펌웨어 소스 파일 전체 읽기
cat /home/jetson/mcu/Application/app_mecanum.c
cat /home/jetson/mcu/Bsp/bsp_encoder.c
cat /home/jetson/mcu/Application/app_motion.c
cat /home/jetson/mcu/Application/app_pid.c
cat /home/jetson/mcu/Bsp/bsp_motor.c
```

---

### 1.2 ROS2 드라이버 파일 분석

```bash
# 전체 드라이버 소스
cat /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
cat /home/jetson/jupiter_ws_ros2/src/jupiter_base/jupiter_base/base_node.py

# EKF 설정
cat /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml

# Nav2 설정
cat /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml

# 런치 파일
cat /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
cat /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_fused_navigation.launch.py
```

---

### 1.3 분석 결과 저장

```bash
# 시스템_분석_2026_04_20.md 생성 (에이전트가 직접 작성)
```

---

## 2. Nav2 속도 제한 (0.35 → 0.20 m/s)

### 2.1 nav2_params_fused.yaml 수정

**목적**: 후진 쏠림 방지를 위해 max_vel_x를 0.20으로 감소

**파일**: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

**변경 1 — DWB max_vel_x**:
```yaml
# 변경 전
      max_vel_x: 0.35
# 변경 후
      max_vel_x: 0.20              # ⭐ 0.35→0.20 [2026-04-20] 후진 쏠림 방지. 저속에서 밸런스 보정 충분
```

**변경 2 — DWB max_speed_xy**:
```yaml
# 변경 전
      max_speed_xy: 0.35
# 변경 후
      max_speed_xy: 0.20
```

**변경 3 — velocity_smoother max_velocity**:
```yaml
# 변경 전
    max_velocity: [0.35, 0.0, 1.0]
# 변경 후
    max_velocity: [0.20, 0.0, 1.0]       # ⭐ 0.35→0.20 [2026-04-20] DWB max_vel_x와 동기화
```

**변경 4 — velocity_smoother min_velocity**:
```yaml
# 변경 전
    min_velocity: [-0.35, 0.0, -1.0]
# 변경 후
    min_velocity: [-0.20, 0.0, -1.0]
```

---

## 3. nvblox costmap 토픽 변경 (static → combined)

### 3.1 nav2_params_fused.yaml nvblox_layer 수정

**목적**: nvblox people_segmentation의 사람 감지 결과를 Nav2 costmap에 반영

**파일**: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

**변경 1 — local_costmap nvblox_layer**:
```yaml
# 변경 전
      # ⭐ [2026-04-20] static_map_slice → combined_map_slice: 사람 감지+정적 장애물 합산 (people_segmentation 모드)
      nvblox_layer:
        plugin: "nvblox::NvbloxCostmapLayer"
        enabled: true
        nav2_costmap_global_frame: odom
        nvblox_map_slice_topic: "/nvblox_node/static_map_slice"
# 변경 후
      # ⭐ [2026-04-20] static_map_slice → combined_map_slice: 사람 감지+정적 장애물 합산 (people_segmentation 모드)
      nvblox_layer:
        plugin: "nvblox::NvbloxCostmapLayer"
        enabled: true
        nav2_costmap_global_frame: odom
        nvblox_map_slice_topic: "/nvblox_node/combined_map_slice"
```

**변경 2 — global_costmap nvblox_layer**:
```yaml
# 변경 전
      # ⭐ [2026-04-20] static_map_slice → combined_map_slice: 사람 감지+정적 장애물 합산
      nvblox_layer:
        plugin: "nvblox::NvbloxCostmapLayer"
        enabled: true
        nav2_costmap_global_frame: map
        nvblox_map_slice_topic: "/nvblox_node/static_map_slice"
# 변경 후
      # ⭐ [2026-04-20] static_map_slice → combined_map_slice: 사람 감지+정적 장애물 합산
      nvblox_layer:
        plugin: "nvblox::NvbloxCostmapLayer"
        enabled: true
        nav2_costmap_global_frame: map
        nvblox_map_slice_topic: "/nvblox_node/combined_map_slice"
```

---

## 4. EKF ↔ Nav2 설정 비교 검증

### 4.1 ekf_vslam_fusion.yaml 전체 읽기

**목적**: EKF와 Nav2 설정 간 프레임/파라미터 불일치 확인

```bash
cat /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml
```

**결과**: 불일치 없음 확인
- odom_frame: `odom` (EKF = Nav2)
- base_link_frame: `base_footprint` (EKF) / robot_base_frame: `base_footprint` (Nav2)
- world_frame: `odom` (EKF)
- map_frame: `map` (Nav2, SLAM Toolbox 발행)
- EKF 5센서: odom0(wheel), odom1(VSLAM), odom2(RF2O), odom3(GPS), imu0(IMU)

---

## 5. 실행 순서 확인

### 5.1 3단계 런치 구성

**목적**: nvblox 통합 후 전체 실행 순서 확인

```bash
# 1단계: 센서 융합 + SLAM (Host)
ros2 launch /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py

# 2단계: Nav2 네비게이션 (Host)
ros2 launch /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_fused_navigation.launch.py

# 3단계: nvblox + people segmentation (Docker, 선택)
# Docker 내에서:
source /workspaces/isaac_ros-dev/install/setup.bash
ros2 launch jupiter_nvblox jupiter_nvblox.launch.py
```

---

## 6. TF 트리 디버깅 (camera_link ↔ camera0_link)

### 6.1 nvblox 토픽 확인

**목적**: nvblox 노드가 정상 발행하는지 확인

```bash
ros2 topic list | grep nvblox
```

**결과**:
```
/nvblox_node/color_layer
/nvblox_node/combined_map_slice
/nvblox_node/dynamic_color_frame_overlay
/nvblox_node/dynamic_occupancy_grid
/nvblox_node/esdf_slice_bounds
/nvblox_node/freespace
/nvblox_node/map_slice
/nvblox_node/mesh
/nvblox_node/mesh_marker
/nvblox_node/occupancy
/nvblox_node/static_esdf_pointcloud
/nvblox_node/static_map_slice
/nvblox_node/static_occupancy
```

---

### 6.2 TF 프레임 목록 확인

**목적**: camera0_link가 TF 트리에 존재하는지 확인

```bash
ros2 run tf2_ros tf2_monitor 2>&1 | head -50
```

---

### 6.3 TF 트리 구조 확인

**목적**: camera0_link와 camera_link 간 연결 상태 확인

```bash
ros2 run tf2_tools view_frames
```

**결과**: `camera0_link`가 메인 TF 트리에 연결되지 않음
- Host 트리: `map → odom → base_footprint → base_link → camera_link → ...`
- Docker 트리 (분리됨): `camera0_link → camera0_depth_frame → camera0_depth_optical_frame`
- nvblox가 `camera0_depth_optical_frame` → `odom` 변환을 찾지 못함

---

### 6.4 수동 TF 브릿지 테스트

**목적**: camera_link → camera0_link identity TF 추가로 문제 해결 확인

```bash
ros2 run tf2_ros static_transform_publisher 0 0 0 0 0 0 camera_link camera0_link
```

**결과**: TF 체인 연결 성공
```
map → odom → base_footprint → base_link → camera_link → camera0_link → camera0_depth_frame → camera0_depth_optical_frame
```

---

### 6.5 TF 브릿지 후 토픽 발행 확인

**목적**: TF 연결 후 nvblox가 정상 동작하는지 확인

```bash
ros2 topic hz /nvblox_node/combined_map_slice
```

**결과**:
```
average rate: 4.979
```

```bash
ros2 topic hz /nvblox_node/dynamic_color_frame_overlay
```

**결과**:
```
average rate: 4.886
```

---

### 6.6 영구 TF 브릿지 적용

**목적**: nav2_vslam_fused.launch.py에 camera_link → camera0_link TF를 영구 추가

**파일**: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py`

**추가 코드** (tf_base_to_camera 직후):
```python
    # ============================================================
    # 2-1. Static TF: camera_link → camera0_link
    # ============================================================
    # Docker RealSense 드라이버가 camera0 namespace를 사용하므로
    # camera0_link 프레임이 메인 TF 트리에 연결되어야 nvblox가 동작함
    # [2026-04-20] identity transform (같은 물리 카메라)
    tf_camera_to_camera0 = Node(
        package='tf2_ros',
        executable='static_transform_publisher',
        name='camera_to_camera0_tf',
        arguments=['--frame-id', 'camera_link', '--child-frame-id', 'camera0_link']
    )
```

**LaunchDescription return 리스트에 추가**:
```python
    tf_camera_to_camera0,
```

---

## 7. nvblox 시각화 오버레이 확인

### 7.1 segmentation 토픽 확인

**목적**: people segmentation 관련 토픽이 발행되는지 확인

```bash
ros2 topic list | grep -E "segmentation|unet|overlay"
```

**결과**:
```
/camera0/segmentation/camera_info_resized
/camera0/segmentation/image_resized
/camera0/segmentation/people_mask
/camera0/segmentation/tensor_input
/camera0/segmentation/tensor_output
/camera0/unet/colored_segmentation_mask
/nvblox_node/dynamic_color_frame_overlay
```

---

### 7.2 오버레이 발행 속도 확인

```bash
ros2 topic hz /nvblox_node/dynamic_color_frame_overlay
```

**결과**:
```
average rate: 4.886
```

```bash
ros2 topic hz /camera0/unet/colored_segmentation_mask
```

**결과**:
```
average rate: 16.305
```

---

### 7.3 rqt_image_view 시도

**목적**: 오버레이 이미지 직접 확인

```bash
xhost +
ros2 run rqt_image_view rqt_image_view /nvblox_node/dynamic_color_frame_overlay
```

**결과**: PyQt5 미설치로 실패
```
ModuleNotFoundError: No module named 'PyQt5'
```

---

### 7.4 Foxglove Bridge 확인

**목적**: 대안 시각화 경로 확인

```bash
ros2 node list | grep foxglove
```

**결과**:
```
/foxglove_bridge
```
→ Host에서 이미 foxglove_bridge 실행 중 (ws://JETSON_IP:8765)

---

### 7.5 realsense_example.launch.py vs jupiter_nvblox.launch.py 비교

**목적**: isaac_ros 예제에서 오버레이가 보이는 이유 확인

```bash
cat /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/launch/perception/realsense_example.launch.py
cat /home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py
```

**결과**: realsense_example.launch.py도 별도 RViz 노드 없음. 두 런치 파일 모두 오버레이 토픽을 발행하지만 시각화 노드가 미포함.

---

## 8. jupiter_nvblox RViz config 생성

### 8.1 참조 RViz 파일 확인

**목적**: isaac_sim_example.rviz를 참조하여 jupiter_nvblox용 RViz config 작성

```bash
cat /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/config/visualization/isaac_sim_example.rviz
```

**결과** (주요 구조):
- Panels: Displays, Selection, Tool Properties, Views
- Displays: TF (disabled), Grid (disabled), Nvblox Group (NvbloxMesh, NvbloxVoxelBlockLayer, Map Slice Bounds, ESDF Slice), Sensors Group (Depth Image, Color Image, 3D LiDAR, Back Projected Depth), Navigation Group (Path, Local Costmap, Global Costmap), Base Link Axes
- Fixed Frame: `odom`
- Target Frame: `camera_link`
- 토픽: `/nvblox_node/mesh`, `/nvblox_node/color_layer`, `/nvblox_node/esdf_slice_bounds`, `/nvblox_node/static_esdf_pointcloud`, `/front_stereo_camera/...` (Isaac Sim 전용)

---

### 8.2 jupiter_nvblox.rviz 생성

**목적**: 실제 환경에 맞는 RViz config 파일 생성

**파일**: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz`

**Docker 경로**: `/workspaces/isaac_ros-dev/src/jupiter_nvblox/config/jupiter_nvblox.rviz`

**구성 (isaac_sim_example.rviz 기반, 토픽 수정)**:

| 그룹 | 디스플레이 | 토픽 | 기본 상태 |
|------|-----------|------|----------|
| Nvblox | NvbloxMesh | `/nvblox_node/mesh` | disabled |
| Nvblox | NvbloxVoxelBlockLayer | `/nvblox_node/color_layer` | enabled |
| Nvblox | Map Slice Bounds | `/nvblox_node/esdf_slice_bounds` | enabled |
| Nvblox | ESDF Slice | `/nvblox_node/static_esdf_pointcloud` | enabled |
| Nvblox | Combined Map Slice | `/nvblox_node/combined_map_slice` | enabled |
| Nvblox | Dynamic Occupancy Grid | `/nvblox_node/dynamic_occupancy_grid` | disabled |
| Sensors | Dynamic Overlay | `/nvblox_node/dynamic_color_frame_overlay` | enabled |
| Sensors | Segmentation Mask | `/camera0/unet/colored_segmentation_mask` | enabled |
| Sensors | Depth Image | `/camera0/depth/image_rect_raw` | disabled |
| Sensors | Color Image | `/camera0/color/image_raw` | disabled |
| Navigation | Local Costmap | `/local_costmap/costmap` | disabled |
| Navigation | Global Costmap | `/global_costmap/costmap` | disabled |

- Fixed Frame: `odom`
- Target Frame: `base_link`

---

## 9. jupiter_nvblox.launch.py에 rviz/foxglove 파라미터 추가

### 9.1 launch 파일 수정

**목적**: Docker 내에서 RViz/Foxglove를 선택적으로 실행

**파일**: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py`

**Docker 경로**: `/workspaces/isaac_ros-dev/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py`

**변경 1 — import 추가**:
```python
# 변경 전
from launch import LaunchDescription
from launch.actions import DeclareLaunchArgument, GroupAction
from launch.substitutions import LaunchConfiguration
from launch_ros.actions import ComposableNodeContainer
from launch_ros.descriptions import ComposableNode

# 변경 후
from launch import LaunchDescription
from launch.actions import DeclareLaunchArgument, GroupAction
from launch.conditions import IfCondition
from launch.substitutions import LaunchConfiguration
from launch_ros.actions import ComposableNodeContainer, Node
from launch_ros.descriptions import ComposableNode
```

**변경 2 — Launch Arguments 추가**:
```python
    # ─── Launch Arguments ───
    container_name_arg = DeclareLaunchArgument(
        'container_name', default_value='jupiter_nvblox_container')
    log_level_arg = DeclareLaunchArgument(
        'log_level', default_value='info')
    run_rviz_arg = DeclareLaunchArgument(
        'run_rviz', default_value='false',
        description='Launch rviz2 with nvblox visualization')
    run_foxglove_arg = DeclareLaunchArgument(
        'run_foxglove', default_value='false',
        description='Launch foxglove_bridge for web visualization')

    container_name = LaunchConfiguration('container_name')
    log_level = LaunchConfiguration('log_level')
    run_rviz = LaunchConfiguration('run_rviz')
    run_foxglove = LaunchConfiguration('run_foxglove')
```

**변경 3 — RViz2 노드 추가 (조건부)**:
```python
    # ═══════════════════════════════════════
    # RViz2 (조건부)
    # ═══════════════════════════════════════
    rviz_config = os.path.join(config_dir, 'jupiter_nvblox.rviz')
    rviz_node = Node(
        package='rviz2',
        executable='rviz2',
        name='rviz2',
        arguments=['-d', rviz_config],
        condition=IfCondition(run_rviz),
    )
```

**변경 4 — Foxglove Bridge 노드 추가 (조건부)**:
```python
    # ═══════════════════════════════════════
    # Foxglove Bridge (조건부)
    # ═══════════════════════════════════════
    foxglove_node = Node(
        package='foxglove_bridge',
        executable='foxglove_bridge',
        name='foxglove_bridge',
        parameters=[{
            'port': 8765,
            'send_buffer_limit': 10000000,
        }],
        condition=IfCondition(run_foxglove),
    )
```

**변경 5 — LaunchDescription 반환 리스트 수정**:
```python
    return LaunchDescription([
        container_name_arg,
        log_level_arg,
        run_rviz_arg,
        run_foxglove_arg,
        container,
        rviz_node,
        foxglove_node,
    ])
```

---

### 9.2 사용법

```bash
# Docker 내에서:
source /workspaces/isaac_ros-dev/install/setup.bash

# nvblox만 (기본)
ros2 launch jupiter_nvblox jupiter_nvblox.launch.py

# nvblox + rviz
ros2 launch jupiter_nvblox jupiter_nvblox.launch.py run_rviz:=true

# nvblox + foxglove
ros2 launch jupiter_nvblox jupiter_nvblox.launch.py run_foxglove:=true

# nvblox + rviz + foxglove
ros2 launch jupiter_nvblox jupiter_nvblox.launch.py run_rviz:=true run_foxglove:=true
```
