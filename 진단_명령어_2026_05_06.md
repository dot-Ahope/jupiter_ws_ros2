# 명령어 로그 2026-05-06: MPPI 전환 + 펌웨어 wheel_speeds 추가 + 캘리브레이션 노드 + M3 mechanical 진단

> 진단 및 수정에 사용한 명령어와 스크립트/설정 파일을 요약 없이 전부 기록한다.

## 0. 오늘 사용한 터미널 명령어 원문

### 0.1 MPPI 패키지 설치 확인

```bash
dpkg -l 2>/dev/null | grep -E "nav2-mppi|nav2-controller" | awk '{print $2, $3}' ; ls /opt/ros/humble/lib/libmppi* 2>/dev/null
```
목적: nav2 MPPI controller 가 설치돼있는지 확인.
결과:
```
ros-humble-nav2-controller 1.1.18-1jammy.20250210.235638
ros-humble-nav2-mppi-controller 1.1.18-1jammy.20250210.235024
/opt/ros/humble/lib/libmppi_controller.so
/opt/ros/humble/lib/libmppi_critics.so
```
설치됨 — DWB → MPPI 전환 가능.

### 0.2 MPPI 첫 실행 + 에러 진단

```bash
ros2 launch /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_fused_navigation.launch.py
```
목적: nav2 단독 launch 로 MPPI 동작 검증.
결과 (controller_server log):
```
[ERROR] Original error: Controller period more then model dt, set it equal to model dt
[ERROR] Failed to change state for node: controller_server
[ERROR] Failed to bring up all requested nodes. Aborting bringup.
```
원인: `controller_frequency=10` (period 100ms) > `model_dt=0.05` (50ms). MPPI 제약 위반.

수정 후 재실행:
```
[INFO] Controller frequency set to 20.0000Hz
[INFO] Controller period is equal to model dt. Control sequence shifting is ON
[INFO] Critic loaded : mppi::critics::ConstraintCritic
[INFO] Critic loaded : mppi::critics::ObstaclesCritic
[INFO] Critic loaded : mppi::critics::GoalCritic
[INFO] Critic loaded : mppi::critics::GoalAngleCritic
[INFO] Critic loaded : mppi::critics::PathAlignCritic
[INFO] Critic loaded : mppi::critics::PathFollowCritic
[INFO] Critic loaded : mppi::critics::PathAngleCritic
[INFO] Critic loaded : mppi::critics::PreferForwardCritic
[INFO] Optimizer reset
[INFO] Configured MPPI Controller
```

### 0.3 시스템 부하 + cmd_vel rate 관찰

```bash
top -bn1 -d 0 -o %CPU 2>/dev/null | head -15
```
목적: nav2 동작 중 CPU 사용량 1차 측정.
결과 1차 (rviz2 켜져있음):
```
load average: 16.48, 14.70, 11.64
%Cpu(s): 75.4 us, 10.7 sy, 11.5 id
PID 17715 rviz2     CPU 217.6%   (2.2 코어)
PID controller_server  CPU 23.5%
```
rviz2 가 시각화 부하로 시스템 전체 saturation.

```bash
ros2 topic hz /cmd_vel
```
결과 (최초):
```
average rate: 19.997   min: 0.047s max: 0.052s std dev: 0.00133s window: 22
average rate: 19.944   min: 0.047s max: 0.057s std dev: 0.00168s window: 42
average rate: 6.842    min: 0.044s max: 10.050s std dev: 0.97589s window: 104
```
goal 활성 시 20Hz, idle 시 갭 → 평균 떨어짐. MPPI 자체는 OK.

```bash
top -p $(pgrep -f controller_server)
```
결과 (1m 주행 시):
```
PID 25554 controller_server  CPU 28.3%
```

### 0.4 controller_server / bt_navigator log 분석

```bash
ls -t /home/jetson/.ros/log/controller_server_*.log 2>/dev/null | head -1 | xargs tail -50
```
목적: control loop missed 빈도, recovery 패턴 확인.
결과:
```
[WARN] Control loop missed its desired rate of 20.0000Hz   (간헐 발생)
[INFO] Passing new path to controller.
[ERROR] Failed to make progress
[WARN] [follow_path] [ActionServer] Aborting handle.
[INFO] Received request to clear entirely the local_costmap
[INFO] Received a goal, begin computing control effort.
```

```bash
ls -t /home/jetson/.ros/log/bt_navigator_*.log | head -1 | xargs grep -vE "tick rate" | tail -20
```
결과:
```
[INFO] Begin navigating from current location (0.49, 0.07) to (-1.06, -0.09)
[INFO] Goal succeeded
[INFO] Begin navigating from current location (-1.06, -0.00) to (0.33, -0.00)
[INFO] Goal succeeded
... (총 7회 succeeded)
[INFO] Received goal preemption request
[INFO] Begin navigating from current location (-0.70, -0.44) to (-0.12, -0.13)   ← 실패
```

### 0.5 좌회전 못함 + lethal zone 진단 — 토픽 직접 echo

```bash
source /opt/ros/humble/setup.bash 2>/dev/null && source /home/jetson/jupiter_ws_ros2/install/setup.bash 2>/dev/null && timeout 5 ros2 topic echo /cmd_vel 2>&1 | head -10
```
결과:
```
linear:  x: 0.0
angular: z: 0.15      ← nav2 가 0.15 rad/s 회전 명령 중
```

```bash
timeout 5 ros2 topic echo /cmd_vel_nav_filtered 2>&1 | head -15
```
결과: timeout (무발행) — collision_monitor 가 차단.

```bash
timeout 5 ros2 topic info /cmd_vel_nav_filtered -v 2>&1 | grep -E "Node|count"
```
결과:
```
Publisher count: 1   Node: collision_monitor
Subscription count: 1   Node: twist_mux
```
topology 정상. publish 만 안 됨 → collision_monitor STOP 동작.

```bash
ls -t /home/jetson/.ros/log/collision_monitor_*.log | head -1 | xargs tail -30
```
결과 (반복):
```
[INFO] Robot to stop due to PolygonStop polygon
[INFO] Robot to slowdown for 40.000000 percents due to PolygonSlow polygon
```
PolygonStop 지속 trip → nav2 모든 stream 차단.

### 0.6 펌웨어 코드 탐색

```bash
ls /home/jetson/mcu/extbd/Source/APP/
find /home/jetson -name "Rosmaster*.py" -not -path "*/__pycache__/*" 2>/dev/null
```
목적: 펌웨어 + Rosmaster_Lib 위치 파악.

```bash
grep -rnE "AUTO_REPORT|Send_Auto|FUNC_REPORT" /home/jetson/mcu/extbd/Source/ 2>/dev/null | head -10
```
결과:
```
protocol.h:20: FUNC_REPORT_SPEED (0x0A)
protocol.h:23: FUNC_REPORT_ENCODER (0x0D)
app_motion.c:628: data_buffer[3] = FUNC_REPORT_SPEED;
bsp_encoder.c:294: data_buffer[3] = FUNC_REPORT_ENCODER;
```

```bash
grep -oE "FUNC_[A-Z_]+ +\(0x[0-9A-F]+\)" /home/jetson/mcu/extbd/Source/APP/protocol.h | awk '{print $NF}' | tr -d '()' | sort -u
```
목적: 사용 중인 FUNC 코드 전체 list. 비어있는 0x08 발견 → FUNC_REPORT_WHEEL_SPEED 로 사용.

### 0.7 빌드 명령

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup --symlink-install
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup jupiter_nav_VO --symlink-install
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_safety --symlink-install
```
결과: 모두 성공.

### 0.8 bag 분석 — 좌회전 회전 부족 진단

```bash
ros2 bag info /home/jetson/bags/latest
```
결과:
```
Duration: 216.182051266s
Bag size: 62.5 MiB
Topic information: 18 topics including /jupiter/wheel_speeds (Count: 5300, ~25Hz)
```

```bash
python3 /tmp/analyze_path_curve.py
```
스크립트 핵심: cmd_vel 의 좌회전 명령 시 actual 거동 + wheel_speeds 정량 분석.
결과 1차 (RUNNING_MIN=0.12):
```
좌회전 명령 (vx<0.05 + ω>+0.10) n=1611
  평균 cmd ω=+0.204, actual ω=+0.066
  실제 좌회전 26.1% | 정지 68.8% | 우회전 5.1%
```

```bash
python3 /tmp/analyze_path_followv2.py
```
결과 2차 (RUNNING_MIN=0.30 적용 후):
```
좌회전 명령 n=405
  평균 cmd ω=+0.540, actual ω=+0.328
  실제 좌회전 89.6% | 정지 8.6% | 우회전 1.7%

Path 추종 정확도 (lateral 거리):
  평균: 19.7 cm, median: 20.7 cm, p95: 32.8 cm, max: 50.5 cm

cmd_vel ω 부호 반전: 0.16 Hz (정상)
```

### 0.9 캘리브레이션 노드 실행

```bash
pkill -f nav2_vslam_fused.launch.py ; pkill -f jupiter_driver_compensated ; sleep 2
ros2 run jupiter_bringup wheel_deadzone_calibrator
```
목적: 모터별 effective deadzone + PID 응답 측정.
결과 핵심 토막:
```
=== Phase 1 v2 SUMMARY ===
[Linear effective deadzone — actual ≥ 50% of target]
  M1 forward DZ: vx ≥ 0.005
  M3 forward DZ: vx ≥ 0.030
  M1 reverse DZ: vx ≥ 0.015
  M3 reverse DZ: vx ≥ 0.100      ← 6.67× 비대칭

[Rotation effective deadzone]
  M1 in CCW (left turn): ω ≥ 0.05
  M3 in CCW (left turn): ω ≥ 0.40   ← 8× 비대칭
  M1 in CW (right turn): ω ≥ 0.05
  M3 in CW (right turn): ω ≥ 0.30   ← 6× 비대칭

[좌-우 비대칭 진단]
  M3/M1 forward DZ 비율: 2.00× ⚠️
  M3/M1 reverse DZ 비율: 6.67× ⚠️ 큰 비대칭

[Phase 2 — PID Step Response]
  ✓ wz_0.4: ratio 95%, rise 103 ms
  ✓ wz_0.2: ratio 105%, rise 1228 ms      ← 1.2초 rise (M3 정지마찰 극복)
  ⚠ wz_0.1: ratio 48%, rise 203 ms
```
저장: `/tmp/deadzone_calib.yaml`

## 1. 분석에 사용한 설정 / 스크립트 파일

### 1.0 오늘 사용/열람한 스크립트 및 파일 전체 경로

**펌웨어 (MCU C 코드)**:
- `/home/jetson/mcu/extbd/Source/APP/protocol.h` ← 수정
- `/home/jetson/mcu/extbd/Source/APP/protocol.c` (열람)
- `/home/jetson/mcu/extbd/Source/APP/app_motion.c` ← 수정
- `/home/jetson/mcu/extbd/Source/APP/app_motion.h` ← 수정
- `/home/jetson/mcu/extbd/Source/APP/app.c` ← 수정
- `/home/jetson/mcu/extbd/Source/APP/app_pid.h` (열람) — `motor_data_t` struct 확인
- `/home/jetson/mcu/extbd/Source/APP/app_differential.c` (열람) — M1=L, M3=R 매핑 확인
- `/home/jetson/mcu/extbd/Source/BSP/bsp_encoder.c` (열람) — 기존 encoder 송출 함수 참조

**Python (Rosmaster 라이브러리 + ROS driver)**:
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py` ← 수정
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` ← 수정
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/wheel_deadzone_calibrator.py` ← 신규
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/setup.py` ← 수정 (entry point)

**ROS2 설정 / launch**:
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` ← 수정 (DWB→MPPI)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` ← 수정 (자동 bag)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/stuck_detector.py` ← 수정 (angular stall)

**임시 분석 스크립트**:
- `/tmp/analyze_path_curve.py` (생성, RUNNING_MIN=0.12 시점 분석)
- `/tmp/analyze_path_followv2.py` (생성, RUNNING_MIN=0.30 적용 후 + path RMS 측정)

**Bag**:
- `/home/jetson/bags/latest/` (자동 기록, 매 launch 마다 덮어씀)

### 1.1 수정된 파일 1 — protocol.h (펌웨어 protocol 정의)

경로: `/home/jetson/mcu/extbd/Source/APP/protocol.h`

수정 항목:
- line 23 부근에 `FUNC_REPORT_WHEEL_SPEED (0x08)` 신규 정의 추가
- 코멘트로 의도 명시

배경: 기존 `FUNC_REPORT_SPEED (0x0A)` 는 차체 속도 (vx/vy/vz) 만 보냄. 개별 휠 속도 진단 위해 새 코드 필요.

### 1.2 수정된 파일 2 — app_motion.c (펌웨어 모션 송출 함수)

경로: `/home/jetson/mcu/extbd/Source/APP/app_motion.c`

수정 항목:
- 기존 `Motion_Send_Data()` 함수 직후에 `Motion_Send_Wheel_Speed()` 함수 추가
- 9 byte UART packet 구성 → USART1 송출

### 1.3 수정된 파일 3 — app_motion.h (펌웨어 헤더)

경로: `/home/jetson/mcu/extbd/Source/APP/app_motion.h`

수정 항목:
- `Motion_Send_Wheel_Speed(void)` 선언 추가

### 1.4 수정된 파일 4 — app.c (FreeRTOS auto-report 태스크)

경로: `/home/jetson/mcu/extbd/Source/APP/app.c`

수정 항목:
- `vTask_Auto_Report` 함수의 if-else if chain 에 `report_count == 6` 슬롯 추가
- 추가 슬롯에서 `Motion_Send_Wheel_Speed()` 호출

### 1.5 수정된 파일 5 — Rosmaster_Lib.py (Python 시리얼 라이브러리)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py`

수정 항목:
- `__init__` 의 FUNC 코드 정의에 `FUNC_REPORT_WHEEL_SPEED = 0x08` 추가
- `__init__` 의 state 변수에 `__wheel_left_mps`, `__wheel_right_mps` 추가
- `__parse_data` 메서드에 0x08 packet 파싱 case 추가
- 신규 getter `get_wheel_speeds()` 추가

### 1.6 수정된 파일 6 — jupiter_driver_compensated.py (ROS2 드라이버 노드)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

수정 항목 (수정 1 — wheel_speeds 토픽):
- line 10: `Float32MultiArray` import 추가
- 토픽 publisher `wheel_speeds_pub` 추가 (`/jupiter/wheel_speeds`)
- 25Hz timer + `publish_wheel_speeds()` 메서드 추가

수정 항목 (수정 2 — 정지 회전 RUNNING floor 상향):
- `ROT_RUNNING_MIN_ANGULAR: 0.12 → 0.30`
- bag 분석 결과 (좌회전 정지 68.8%) 기반 — wheel-level diff 가 엔코더 분해능 (16.3 mm/s) 미달이라 0.30 으로 boost.

### 1.7 수정된 파일 7 — nav2_params_fused.yaml (Nav2 파라미터)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

수정 항목:
- `controller_frequency: 10.0 → 20.0` (MPPI 의 period ≤ model_dt 제약)
- `controller_server.FollowPath`: DWB → MPPI 전환
  - plugin: `nav2_rotation_shim_controller::RotationShimController` (유지)
  - primary_controller: `dwb_core::DWBLocalPlanner` → `nav2_mppi_controller::MPPIController`
  - DWB 파라미터 (vx_samples, sim_time, BaseObstacle.scale 등) 모두 제거
  - MPPI 파라미터 추가:
    - `time_steps: 56 → 48` (horizon 2.4s)
    - `model_dt: 0.05`
    - `batch_size: 1000 → 700`
    - `vx_std: 0.2`, `wz_std: 0.4`
    - `motion_model: DiffDrive`
    - `temperature: 0.3`, `gamma: 0.015`
    - `iteration_count: 1`
  - Critic 8개: ConstraintCritic, ObstaclesCritic, GoalCritic, GoalAngleCritic, PathAlignCritic, PathFollowCritic, PathAngleCritic, PreferForwardCritic
  - GoalAngleCritic.cost_weight: 3.0 → 8.0 (회전 부족 보정)
  - PathAngleCritic.cost_weight: 2.0 → 5.0
- RotationShim 의 `angular_dist_threshold: 0.35 → 0.20` (더 엄격한 정렬 후 handover)

### 1.8 수정된 파일 8 — nav2_vslam_fused.launch.py (통합 launch)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py`

수정 항목:
- line 57: `from launch.actions import ExecuteProcess` 추가
- safety_layer 직후에 `bag_recorder` (TimerAction + ExecuteProcess) 추가
  - 매 launch 시 `/home/jetson/bags/latest/` 삭제 후 신규 record (덮어쓰기)
  - 기록 토픽 22개: cmd_vel pipeline + 센서 + costmap + tf
  - 카메라/depth/pointcloud 토픽 제외 (용량)

### 1.9 수정된 파일 9 — stuck_detector.py (jupiter_safety)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/stuck_detector.py`

수정 항목:
- 신규 상수 `ANGULAR_STALL_CMD_MIN = 0.10`, `ANGULAR_STALL_ACTUAL_MAX = 0.05`
- `_tick()` 의 stall 검사를 linear OR angular 로 확장
  - linear_stall: |cmd_x| > 0.05 AND |actual_x| < 0.02
  - angular_stall: |cmd_x| ≤ 0.05 AND |cmd_z| > 0.10 AND |actual_z| < 0.05
- 회전 명령 시점에 collision_monitor 차단으로 인한 deadlock 도 검출 → forced_recovery 가 angular stall 시에도 backup escalate.

### 1.10 신규 파일 — wheel_deadzone_calibrator.py

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/wheel_deadzone_calibrator.py`

기능:
- Phase 1 v2: closed-loop deadzone (set_car_motion sweep)
  - Linear: vx ∈ [0.005, 0.010, ..., 0.100] forward + reverse
  - Rotation: ω ∈ [0.05, 0.10, ..., 0.50] CCW + CW
  - per-wheel ratio (actual/target) 계산 → M1/M3 비대칭 정량화
  - effective deadzone = ratio ≥ 50% 도달하는 최소 target
- Phase 2: PID step response
  - target vx 0.20/0.10/0.05, target ω 0.40/0.20/0.10
  - rise time, steady-state ratio 측정
- 결과 yaml 저장: `/tmp/deadzone_calib.yaml`

배경: 이전 v1 (open-loop set_motor) 은 펌웨어 한계로 wheel_speeds 측정 불가 (`motor_data.speed_mm_s[]` 가 PID 루프 안에서만 갱신).

### 1.11 수정된 파일 11 — setup.py (jupiter_bringup)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/setup.py`

수정 항목:
- entry_points 에 `wheel_deadzone_calibrator = jupiter_bringup.wheel_deadzone_calibrator:main` 추가

## 2. 관찰 기준 요약

| 기준 | 측정값 |
|---|---|
| `/jupiter/wheel_speeds` rate | 25Hz (펌웨어 25Hz cycle 일치) |
| MPPI controller_frequency | 20Hz (= model_dt 0.05s 의 역수) |
| MPPI batch_size | 700 (Orin Nano 안정 동작) |
| MPPI CPU 1m 주행 시 | 28.3% (peak) |
| Control loop missed 빈도 | 간헐 발생 (rviz2 부하 시) |
| 좌회전 명령 정지 비율 (RUNNING_MIN=0.12) | 68.8% |
| 좌회전 명령 정지 비율 (RUNNING_MIN=0.30) | 8.6% |
| Path 추종 lateral RMS (현재) | 평균 19.7 cm, p95 32.8 cm |
| 엔코더 분해능 | 1 tick = 16.3 mm/s |
| M1 forward effective DZ | 0.005 m/s |
| M3 forward effective DZ | 0.030 m/s (6×) |
| M1 reverse effective DZ | 0.015 m/s |
| **M3 reverse effective DZ** | **0.100 m/s (6.67× 비대칭, mechanical 이슈)** |
| M3 PID rise time @ ω=0.20 | 1228 ms |
