# 진행과정 2026-03-09

## 1. 오늘 작업 목표
1. Foxglove에서 `/visual_slam/tracking/odometry_adapted.pose.pose.position.x` "no data" 문제 진단 및 해결
2. 미해결 항목 진행: base_node heading wrap 적용 확인, RF2O 패치 등

---

## 2. 수행 요약 (Phase별)

### 2.1 Phase 1: Foxglove "no data" 원인 진단

#### 증상
- Foxglove에서 `/visual_slam/tracking/odometry_adapted` 토픽의 `pose.pose.position.x` Series 경로가 "matches no data"로 표시
- adapted 토픽에 메시지가 0Hz 상태

#### 진단 과정
1. **토픽 존재 확인**: `ros2 topic list` → `/visual_slam/tracking/odometry_adapted` 토픽 존재, publisher 1개(vslam_covariance_adapter), subscriber 1개(ekf_filter_node)
2. **데이터 흐름 확인**: `ros2 topic hz` → adapted = 0Hz, raw VSLAM odometry = 0Hz
3. **Docker 컨테이너 확인**: `docker ps` → `isaac_ros_dev-aarch64-container` Up 12분
4. **Docker 내부 노드 확인**: `ros2 node list` → visual_slam_node, camera/camera 모두 ALIVE
5. **Docker 내부 데이터 확인**: `ros2 topic hz /visual_slam/tracking/odometry` (Docker 내) → **NO DATA** (0Hz)
6. **카메라 스트림 확인**: `/camera/infra1/image_rect_raw` → NO DATA
7. **카메라 하드웨어 확인**: `lsusb` → RealSense D455F 연결 OK
8. **Docker 로그 확인**: cuVSLAM 초기화 성공, 시작 시 jitter 경고 1건 → 이후 스트리밍 hang

#### 1차 원인
- **카메라 스트리밍 hang**: 재부팅 후 카메라 노드 alive but 데이터 없음 (이전 세션과 동일 패턴)
- `component_container` CPU 94.6% (데이터 없이 spin)

---

### 2.2 Phase 2: VSLAM 프로세스 정리 및 재시작

#### 조치
1. Docker 내 VSLAM/카메라 프로세스 SIGINT → SIGKILL 정리
2. USB 리셋 (`reset_realsense.py`)
3. `docker exec -d` 로 VSLAM 재시작

#### 결과
- Docker 내부: 60Hz 정상 발행 ✅
- **호스트에서 수신 불가** ❌ — 토픽 discoverable하지만 데이터 도달 안 됨

---

### 2.3 Phase 3: DDS SHM UID 불일치 문제 발견 및 해결

#### 진단
1. **호스트 DDS 상태**: `ros2 node list` → 빈 출력 (SHM 정리 후 DDS 완전 끊김)
2. **SHM 파일 확인**: `/dev/shm/fastrtps_*` → root 소유(Docker) + jetson 소유(호스트) 혼재
3. **topic info 확인**: publisher의 node_name = `_NODE_NAME_UNKNOWN_` → DDS 데이터 전송 계층 파괴
4. **UID 매핑 확인**:
   - Docker 내 `admin` = UID 1000
   - 호스트 `jetson` = UID 1000
   - `docker exec -d` 기본 실행 = **root** (UID 0)

#### 근본 원인
```
docker exec -d (root, UID=0) → VSLAM 프로세스 생성
  → FastRTPS SHM 파일: /dev/shm/fastrtps_* (소유자: root, 권한: -rw-r--r--)
  → 호스트 프로세스 (jetson, UID=1000) SHM 읽기 전용 → 쓰기 불가
  → DDS SHM 전송 실패 → 토픽 discovery는 되지만 데이터 교환 불가
  → `_NODE_NAME_UNKNOWN_` (메타데이터 전달 실패)
```

#### 핵심 발견
- FastRTPS SHM은 UID 기반 파일 권한으로 동작
- Docker `--ipc host` 사용 시 `/dev/shm` 공유
- Docker 내 프로세스가 root로 실행되면 SHM 파일이 root 소유
- 호스트 프로세스(UID 1000)가 이 SHM 파일에 쓰기 불가 → 데이터 교환 실패

#### 수정
```bash
# 수정 전 (root로 실행 — DDS 통신 실패)
docker exec -d isaac_ros_dev-aarch64-container bash -c '... ros2 launch ...'

# 수정 후 (admin으로 실행 — UID 1000 매칭)
docker exec -d --user admin isaac_ros_dev-aarch64-container bash -c '... ros2 launch ...'
```

#### 검증
- `--user admin` 적용 후: 호스트에서 VSLAM 60Hz 수신 ✅
- SHM 파일 전부 jetson:jetson 소유 (UID 1000 매칭)
- `ros2 node list`: Docker 노드 포함 전체 24개 정상 발견

---

### 2.4 Phase 4: "failed to set power state" 카메라 에러 복구

#### 증상
- VSLAM 재시작 시 RealSense 카메라가 `failed to set power state` 에러로 열리지 않음
- 반복 retry (6초 간격)에도 지속 실패

#### 원인
- `kill -9`로 강제 종료된 이전 프로세스가 USB 디바이스 핸들을 해제하지 못함
- `reset_realsense.py` (ioctl USB reset)가 불충분한 경우 발생
- USB 컨트롤러 레벨에서 디바이스 상태가 불안정

#### 해결: USB 버스 deauthorize/reauthorize
```bash
# 기존 방법 (ioctl reset — 때때로 불충분)
sudo python3 /home/jetson/reset_realsense.py

# 새 방법 (USB 호스트 컨트롤러 레벨 reset — 더 강력)
echo "0" | sudo tee /sys/bus/usb/devices/usb2/authorized   # USB 3.0 버스 전체 비활성화
sleep 3
echo "1" | sudo tee /sys/bus/usb/devices/usb2/authorized   # 재활성화 → 전체 재열거
```

또는 특정 포트만:
```bash
# RealSense 디바이스 찾기
for dev in /sys/bus/usb/devices/*/idProduct; do
  if [[ "$(cat $dev 2>/dev/null)" == "0b5c" ]]; then
    USB_DEV=$(basename $(dirname $dev))
    echo "$USB_DEV" | sudo tee /sys/bus/usb/drivers/usb/unbind
    sleep 2
    echo "$USB_DEV" | sudo tee /sys/bus/usb/drivers/usb/bind
    break
  fi
done
```

#### 검증
- `dmesg` 커널 로그에서 `Found UVC 1.50 device Intel(R) RealSense(TM) Depth Camera 455f` 정상 재열거 확인
- `lsusb` 에서 디바이스 번호 갱신 확인 (예: device 8 → device 12)

---

### 2.5 Phase 5: Frozen Detection 과도한 게이팅 문제 발견 및 수정

#### 증상
- VSLAM raw 60Hz 정상 수신, DDS 통신 정상
- 그러나 `/visual_slam/tracking/odometry_adapted` = **0Hz** (데이터 없음)
- adapter 통계 로그: `pub=300 (1%) | gated=28384 (99%) [frozen=28384]`

#### 근본 원인
- 로봇 정지 상태에서 cuVSLAM이 60Hz로 거의 동일한 pose 발행
- 연속 pose 간 차이: `pos_delta ≈ 0`, `yaw_delta ≈ 5.8e-08` → 둘 다 `< 1e-6` (frozen 임계값)
- 10프레임(0.17초) 후 frozen=True → **모든 메시지 완전 차단**
- 3/6 세션에서 frozen/anomaly를 분리하여 tracking 상태 복구는 수정했지만,
  **frozen 상태에서의 메시지 발행 자체가 0Hz**인 문제는 미해결이었음

```
기존 로직 (3/6 수정):
  frozen → return (완전 차단, 0Hz)
  tracking_ok = anomaly만으로 판단 (frozen 분리)
  
문제:
  정지 시 60Hz 전부 frozen → adapted 0Hz
  → Foxglove "no data"
  → EKF에 VSLAM 위치 정보 전달 중단
```

#### 수정: frozen 완전차단 → 저주파 다운샘플링 (1Hz)

**파일**: `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py`

**수정 전** (3/6 기존):
```python
# 4) 게이팅 판정: frozen이면 무조건 gate (중복 데이터 EKF 투입 방지)
if is_frozen:
    self.gated_count += 1
    self.frozen_gated_count += 1
    return
```

**수정 후**:
```python
# 4) 게이팅 판정: frozen이면 downsample (중복 데이터 EKF 과투입 방지)
#    완전 차단 시 Foxglove 데이터 없음 + EKF에 VSLAM 위치 전달 중단
#    → frozen_publish_rate(기본 1Hz)로 저주파 발행
if is_frozen:
    now_sec = time.monotonic()
    if self.frozen_publish_rate > 0:
        interval = 1.0 / self.frozen_publish_rate
        if (now_sec - self.last_frozen_publish_time) >= interval:
            self.last_frozen_publish_time = now_sec
            self.frozen_published_count += 1
            pass  # fall through to covariance correction and publish
        else:
            self.gated_count += 1
            self.frozen_gated_count += 1
            return
    else:
        # frozen_publish_rate=0: 완전 차단 (이전 동작)
        self.gated_count += 1
        self.frozen_gated_count += 1
        return
```

**추가 변경사항**:
1. 새 파라미터: `frozen_publish_rate` (기본값: 1.0 Hz)
2. 새 상태 변수: `frozen_published_count`, `last_frozen_publish_time`
3. 통계 로그에 `frozen_pub=` 항목 추가

**설계 근거**:

| 방식 | 장점 | 단점 |
|------|------|------|
| 완전 차단 (0Hz) | EKF 중복 투입 없음 | Foxglove "no data", EKF VSLAM 정보 단절 |
| 전량 통과 (60Hz) | 항상 데이터 있음 | EKF에 동일 데이터 60Hz 투입 → 불필요한 연산 |
| **다운샘플 (1Hz)** | **Foxglove 지속 표시, EKF 저부하** | 1초 지연 가능 (정지 중이므로 무관) |

#### 검증
```
Stats: 1804 msgs | pub=110 (6%) | gated=1694 (94%) [frozen=1664, frozen_pub=30]
Stats: 3599 msgs | pub=160 (4%) | gated=3439 (96%) [frozen=3379, frozen_pub=60]
```
- `frozen_pub=30` / 30초 = **정확히 1Hz** ✅
- `tracking_ok=True` ✅ (frozen이 tracking 상태에 영향 주지 않음)
- `ros2 topic hz /visual_slam/tracking/odometry_adapted` → **~1Hz** ✅

---

### 2.6 Phase 6: 호스트 스택 재시작 및 전체 검증

#### 시스템 재시작 순서
1. 호스트 `nav2_vslam_fused.launch.py` SIGINT 종료
2. SHM 파일 정리: `sudo rm -f /dev/shm/fastrtps_*`
3. ros2 daemon 정리: `ros2 daemon stop`
4. Docker VSLAM 재시작: `docker exec -d --user admin ...`
5. VSLAM 60Hz 확인 후 호스트 스택 시작

#### 최종 검증 결과

| 항목 | 결과 |
|------|------|
| Docker VSLAM (inside) | 60Hz ✅ |
| Raw VSLAM (host) | 60Hz ✅ |
| Adapted VSLAM (host) | ~1Hz (frozen downsample) ✅ |
| 전체 노드 발견 | 24개 (Docker 포함) ✅ |
| DDS 통신 | SHM 정상 (UID 1000 매칭) ✅ |
| Foxglove `pose.pose.position.x` | 데이터 표시 ✅ |
| base_node heading wrap | 재시작 포함 (빌드 3/4, 최초 적용) ✅ |

---

## 3. 전체 수정 내역 (수정 전/후 비교)

### 3.1 vslam_covariance_adapter.py 변경 요약

**파일**: `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py`

| # | 수정 항목 | 수정 전 | 수정 후 | 근거 |
|---|----------|---------|---------|------|
| 1 | frozen 게이팅 방식 | 완전 차단 (return) | 1Hz 다운샘플 | 정지 시 Foxglove "no data" + EKF 단절 |
| 2 | `frozen_publish_rate` 파라미터 | 없음 | 1.0 Hz (기본값) | 다운샘플 주파수 제어 |
| 3 | frozen 통계 | `frozen_gated_count`만 | `+ frozen_published_count` | 다운샘플 발행 횟수 추적 |

### 3.2 Docker VSLAM 시작 방법 변경

| 항목 | 수정 전 | 수정 후 | 근거 |
|------|---------|---------|------|
| `docker exec` 사용자 | 기본 (root, UID 0) | `--user admin` (UID 1000) | DDS SHM 파일 UID 불일치 → 데이터 전송 실패 |

### 3.3 카메라 USB 복구 방법 추가

| 방법 | 명령 | 강도 | 용도 |
|------|------|------|------|
| ioctl reset | `python3 reset_realsense.py` | 약 | 일반적 hang 복구 |
| sysfs unbind/bind | `echo "2-1.4" > unbind/bind` | 중 | ioctl 불충분 시 |
| **USB 버스 deauthorize** | `echo "0/1" > usb2/authorized` | **강** | **"failed to set power state" 시** |

---

## 4. 근본 원인 분석 다이어그램

### 4.1 DDS SHM UID 불일치 → 토픽 발견 but 데이터 없음

```
docker exec -d (root, UID=0)
  ↓
VSLAM 프로세스 생성 (PID=root)
  ↓
FastRTPS SHM 세그먼트 생성: /dev/shm/fastrtps_xxxxx
  소유자: root:root, 권한: -rw-r--r--
  ↓
호스트 프로세스 (jetson, UID=1000) → SHM open for read/write
  ↓
write() 실패 (permission denied)
  ↓
FastRTPS: SHM 전송 불가 → UDP fallback 시도
  → UDP discovery는 성공 (토픽 목록 보임)
  → UDP 데이터 전송도 SHM 관련 lock 파일에 의존
  → 결과: 토픽 discoverable, 데이터 교환 불가
  → ros2 topic info: _NODE_NAME_UNKNOWN_
  → ros2 topic hz: "does not appear to be published yet"
```

### 4.2 Frozen Detection 과도 게이팅 → Foxglove "no data"

```
cuVSLAM 60Hz (정지 상태)
  ↓
pose[n] - pose[n-1] = delta ≈ 0
  pos_delta < 1e-6m, yaw_delta < 1e-6rad
  ↓
frozen_count++ → 10프레임 후 frozen=True (0.17초)
  ↓
[3/6 기존] is_frozen → return (완전 차단)
  → 0Hz 발행 → Foxglove "no data"
  ↓
[3/9 수정] is_frozen → 1Hz 다운샘플 발행
  → ~1Hz 발행 → Foxglove 정상 표시
  → EKF에 VSLAM 위치 저주파 전달 유지
```

---

## 5. DDS 통신 교훈 정리

### 5.1 Docker ↔ Host DDS 통신 체크리스트

| # | 확인 사항 | 명령 | 예상 결과 |
|---|----------|------|-----------|
| 1 | Docker network mode | `docker inspect ... --format '{{.HostConfig.NetworkMode}}'` | `host` |
| 2 | Docker IPC mode | `docker inspect ... --format '{{.HostConfig.IpcMode}}'` | `host` |
| 3 | RMW 일치 | `echo $RMW_IMPLEMENTATION` (양쪽) | `rmw_fastrtps_cpp` |
| 4 | DOMAIN_ID 일치 | `echo $ROS_DOMAIN_ID` (양쪽) | 동일 값 (0) |
| 5 | **실행 UID 일치** | `docker exec ... id` | **호스트 UID와 동일 (1000)** |
| 6 | SHM 파일 소유자 | `ls -la /dev/shm/fastrtps_*` | 호스트 사용자 소유 |

### 5.2 SHM 문제 복구 절차

```bash
# 1. 모든 ROS 프로세스 정상 종료 (kill -2, NOT kill -9)
kill -2 <PID>

# 2. stale SHM 정리
sudo rm -f /dev/shm/fastrtps_*

# 3. ros2 daemon 재시작
ros2 daemon stop && ros2 daemon start

# 4. Docker VSLAM 재시작 (--user admin 필수!)
docker exec -d --user admin isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py'

# 5. 호스트 스택 재시작
ros2 launch ... nav2_vslam_fused.launch.py
```

---

## 6. 미해결 항목 (우선순위)

| # | 항목 | 상태 | 영향도 |
|---|------|------|--------|
| 1 | transform_tolerance 3.0→0.5 | ✅ 완료 (3/6) | MED |
| 2 | rotate_to_heading_angular_vel 0.5→0.8 | ✅ 완료 (3/6) | HIGH |
| 3 | max_angular_accel 1.6→5.0 | ✅ 완료 (3/6) | HIGH |
| 4 | VSLAM FPS 90→60, jitter 22→34ms | ✅ 완료 (3/6) | HIGH |
| 5 | Frozen/anomaly tracking 상태 분리 | ✅ 완료 (3/6) | HIGH |
| 6 | **Frozen downsample 1Hz** | ✅ **완료 (3/9)** | **HIGH** |
| 7 | **Docker exec `--user admin`** | ✅ **완료 (3/9)** | **CRITICAL** |
| 8 | **base_node heading wrap 적용** | ✅ **완료 (3/9, 재시작 포함)** | MED |
| 9 | RF2O 패치 (acos NaN 등) | 📋 미착수 | MED |
| 10 | 7차 테스트 (전체 수정 통합 검증) | 📋 다음 작업 | **CRITICAL** |

---

## 7. 오늘 수정된 파일 목록

| 파일 | 변경 유형 | 내용 |
|------|----------|------|
| `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py` | 코드 수정 | frozen 완전차단 → 1Hz 다운샘플, 파라미터/통계 추가 |
| (운영 절차) Docker exec 시작 방법 | 프로세스 변경 | `docker exec -d` → `docker exec -d --user admin` |
| (운영 절차) USB 복구 방법 | 신규 추가 | sysfs unbind/bind, USB 버스 deauthorize 방법 문서화 |
