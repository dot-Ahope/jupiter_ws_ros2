# 진행과정 — 2026-03-03 Nav2 Goal Stamp 문제 해결 & 센서 안정성 개선

**관련 TIL**: `TIL_2026_03_03_Nav2_Goal_Stamp_Fix.md`  
**이전 작업**: `진행과정_2026_02_27.md`, `TIL_2026_02_27_EKF_Yaw_Overshoot_Diagnosis.md`

---

## 0. 시작 상태

이전 세션(02-27)에서 완료된 상태:
- ✅ VSLAM yaw/vyaw를 EKF에서 제거 (EKF yaw overshoot 해결)
- ✅ EKF process_noise vyaw: 0.0001→0.01
- ✅ Nav2 transform_tolerance: 3.0 (5곳) — backup 반복 해소
- ✅ imu_gyro_scale 파라미터 추가 (기본 1.0)
- ✅ Foxglove IMU 토픽 whitelist 추가
- ⏳ Nav2 장거리 네비게이션 시 backup 재발생 가능성
- ⏳ PC-Jetson NTP 시간 동기화 미구현
- ⏳ VSLAM 트래킹 손실 / RF2O 종료 미조사

---

## 1. Nav2 실주행 로그 분석

### 1.1. transform_tolerance 3.0 효과 확인

사용자 보고: **매 상황마다 backup을 수행하지 않는다** ✅

로그 분석 결과:
- **Goal 1** (0.01,-0.00)→(0.81,0.07): 첫 planner 실패 → recovery → 성공 ✅
- **Goal 2** (0.81,0.00)→(2.35,-1.50): 첫 planner 실패 → recovery → 성공 ✅
- **Goal 3** (2.38,-1.41)→(0.02,-1.85): 첫 planner 실패 → recovery → 성공 ✅
- **Goal 4** (0.07,-1.87)→(2.51,-1.32): controller "Failed to make progress" → stale timestamp cascade → **실패** ❌

### 1.2. PC-Jetson 시계 차이 분석

```python
# Goal stamp과 BT 수신 시간의 차이 계산
goals = [
    ("Goal 1", 1772522916.67, 1772522921.0, 4.33),
    ("Goal 2", 1772522934.46, 1772522939.0, 4.54),
    ("Goal 3", 1772522952.61, 1772522957.0, 4.39),
    ("Goal 4", 1772522989.05, 1772522993.0, 3.95),
]
# 평균 ~4.3s (이전 2.4s에서 증가)
```

### 1.3. Goal 4 실패 cascade 분석

```
t=989.05  BT 수신 (goal stamp=993.0, Jetson보다 4s 미래)
t=992.06  planner: "future extrapolation" (993.0 - 991.98 = 1.02s)
          → recovery → clearCostmap → 재시도 성공
t=993.09  controller 시작
t=1003.09 controller: "Failed to make progress" (10s에 0.5m 미이동)
t=1003.11 controller abort
t=1006.38 planner: goal stamp 993.0은 이제 13s 과거
          → "past extrapolation" (earliest TF = 996.38)
          → 이후 모든 재계획 실패
t=1013.60 backup #1
t=1034.60 backup #2
t=1055.60 backup #3
t=1068.61 최종 Goal failed
```

**핵심**: transform_tolerance(3.0s)는 초기 ~1s 미래 오차 처리 가능. 그러나 controller 실패(10s) 후 stale timestamp(14s+)는 TF 버퍼(10s) 범위를 초과하여 처리 불가.

---

## 2. Goal Pose Re-stamper 구현

### 2.1. 노드 생성

```bash
# goal_pose_restamper.py 생성 (~80줄)
cat ~/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/goal_pose_restamper.py
```

동작:
1. `/goal_pose_raw` (Foxglove PC 발행) 구독
2. `header.stamp` → Jetson 현재 시간으로 교체
3. `/goal_pose` (Nav2 bt_navigator 구독)로 재발행
4. 시계 차이 로그 출력

### 2.2. setup.py에 entry_point 등록

```python
# setup.py entry_points에 추가
'goal_pose_restamper = jupiter_nav_VO.goal_pose_restamper:main',
```

### 2.3. Nav2 Navigation Launch 수정

```bash
# nav2_fused_navigation.launch.py 수정:

# 1. goal_pose_restamper 노드 추가
goal_pose_restamper = Node(
    package='jupiter_nav_VO',
    executable='goal_pose_restamper',
    name='goal_pose_restamper',
    output='screen',
    remappings=[
        ('goal_pose_in', '/goal_pose_raw'),
        ('goal_pose_out', '/goal_pose'),
    ],
)

# 2. LaunchDescription에 추가
```

### 2.4. Foxglove Bridge Whitelist 수정

```bash
# nav2_vslam_fused.launch.py의 foxglove_bridge 파라미터:

# 변경 전
'client_topic_whitelist': ['/cmd_vel', '/goal_pose', '/initialpose'],

# 변경 후
'client_topic_whitelist': ['/cmd_vel', '/goal_pose_raw', '/initialpose'],

# ⚠️ Foxglove 3D Panel의 Publish topic도 /goal_pose_raw로 변경 필요!
```

### 2.5. 빌드 및 확인

```bash
cd ~/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO --symlink-install
# Finished <<< jupiter_nav_VO [4.33s] ✅
```

---

## 3. NTP 시간 동기화 (chrony)

### 3.1. 설치

```bash
sudo apt install -y chrony
sudo systemctl enable chrony && sudo systemctl start chrony
```

### 3.2. 동기화 확인

```bash
chronyc tracking
```

```
Reference ID    : 0327B041 (ec2-3-39-176-65.ap-northeast-2.compute.amazonaws.com)
Stratum         : 3
System time     : 0.001075122 seconds fast of NTP time
Last offset     : +0.001114258 seconds
Leap status     : Normal
```

→ Jetson 시간이 NTP 서버와 **1ms 이내** 동기화 ✅

### 3.3. NTP 소스 확인

```bash
chronyc sources
```

```
^* ec2-3-39-176-65.ap-north>     2   6   177    58   +393us[ +503us] +/- 5605us
^+ 121.134.215.104               2   6   177    58   +819us[ +819us] +/- 3381us
# + 6개 추가 소스
```

### 3.4. PC 측 동기화 (사용자 수행 필요)

```bash
# Linux PC:
sudo apt install chrony && sudo systemctl start chrony

# Windows PC:
w32tm /resync
# 또는 설정 → 시간 및 언어 → 날짜 및 시간 → 지금 동기화
```

---

## 4. VSLAM 트래킹 손실 조사

### 4.1. USB 토폴로지 확인

```bash
lsusb -t
```

```
Bus 02 (USB 3.0, 10Gbps):  ← RealSense D455F 전용
  └── Hub → Port4: RealSense (5Gbps)

Bus 01 (USB 2.0, 480Mbps):  ← 저속 장치들
  └── Hub → CP210x (RPLidar) + ch34x (MCU) + Bluetooth
```

→ RealSense와 RPLidar가 **별도 USB 호스트 컨트롤러**에 있어 대역폭 경합 없음 ✅

### 4.2. USB 안정성 확인

```bash
sudo dmesg | grep -iE "disconnect|reset|error" | grep -iE "usb|realsense|cp210|uvc"
```

→ 관련 이벤트 없음 (Bluetooth reset만 있음) ✅

### 4.3. 원인 추정

| 가능성 | 검증 | 결과 |
|--------|------|------|
| USB 대역폭 경합 | lsusb -t | ❌ 별도 버스 |
| USB 연결 끊김 | dmesg | ❌ 이벤트 없음 |
| GPIO 부하 | cuVSLAM 시각화 | ⚠️ 모두 True 상태 |
| Motion blur | 빠른 회전 시 | ✅ 가장 유력 |

### 4.4. 개선: VSLAM 시각화 비활성화

```bash
# isaac_ros_visual_slam_realsense.launch.py 수정

# 변경 전 (GPU 부하 높음)
'enable_slam_visualization': True,
'enable_landmarks_view': True,
'enable_observations_view': True,

# 변경 후 (GPU 부하 감소)
'enable_slam_visualization': False,   # GPU 절약
'enable_landmarks_view': False,       # 디버깅 시에만 True
'enable_observations_view': False,    # 디버깅 시에만 True
```

**예상 효과**: GPU VRAM 및 처리 부하 10-15% 감소 → 빠른 회전 시 tracking 유지 가능성 향상

---

## 5. RF2O 종료 조사

### 5.1. 확인 사항

- USB 안정: ✅ (별도 버스, dmesg 정상)
- Foxglove bridge 로그: RF2O 노드가 parameter timeout으로 ignore list에 추가됨 (bridge 문제, RF2O 자체 문제 아님)

### 5.2. 추정 원인

- LiDAR scan 품질 이슈 (특정 환경에서 반사/간섭)
- RF2O 알고리즘 한계 (scan matching 실패 시 복구 메커니즘 없음)
- EKF `sensor_timeout: 0.5` → 센서 중단 시 자동으로 나머지 센서만 사용 (안전)

### 5.3. 추가 조사 필요

- 다음 실주행 시 `/rosout` 로그에서 RF2O 관련 에러 확인
- RPLidar S2L의 `scan_mode: 'Standard'` → `'Boost'` 모드로 변경 시 안정성 개선 가능

---

## 6. 수정 파일 요약

| # | 파일 | 변경 | 이유 |
|---|------|------|------|
| 1 | `jupiter_nav_VO/goal_pose_restamper.py` | 신규 생성 | PC→Jetson goal stamp 변환 |
| 2 | `launch/nav2_fused_navigation.launch.py` | restamper 노드 + 문서화 | launch 통합 |
| 3 | `setup.py` | entry_point 추가 | restamper 실행 등록 |
| 4 | `launch/nav2_vslam_fused.launch.py` | whitelist `/goal_pose_raw` | Foxglove 경로 변경 |
| 5 | `isaac_ros_visual_slam_realsense.launch.py` | 시각화 False | GPU 부하 감소 |
| 6 | Jetson system | chrony 설치 | NTP 동기화 |

---

## 7. 토픽 흐름 (최종)

```
[Foxglove PC]
  │
  │ /goal_pose_raw (PC 시계 기준 stamp)
  ▼
[Foxglove Bridge] ── client_topic_whitelist: /goal_pose_raw
  │
  │ /goal_pose_raw
  ▼
[goal_pose_restamper] ── stamp → Jetson 현재 시간으로 교체
  │
  │ /goal_pose (Jetson 시계 기준 stamp)
  ▼
[Nav2 bt_navigator] → planner → controller → /cmd_vel → 로봇
```

---

## 8. 향후 과제

### 즉시 필요
- ⏳ Foxglove 3D Panel: Publish topic을 `/goal_pose_raw`로 변경
- ⏳ PC 측 NTP 동기화 확인 (chrony 또는 w32tm)
- ⏳ Re-stamper + NTP 적용 후 장거리 네비게이션 재검증

### 단기 과제
- ⏳ VSLAM 시각화 비활성화 후 tracking 손실 빈도 재측정
- ⏳ process_noise vyaw=0.01 실주행 검증
- ⏳ RF2O 종료 원인 → 실주행 시 `/rosout` 로그 수집

### 장기 과제
- 💡 transform_tolerance를 기본값(0.3)으로 복원 (NTP 확인 후)
- 💡 RPLidar scan_mode: 'Standard' → 'Boost' 시도
- 💡 RF2O respawn 메커니즘 (launch에서 `respawn=True`)
