# 진행과정 2026-04-21: 정지 회전 헌팅 억제, near-goal yaw 데드락 수정, 도착 후 backup 복구 원인 분석

## 1. 목표

### 1.1 사용자 요청 (시간순)
> 1. 정지 상태에서 `cmd_vel` 이 좌우로 흔들리며 회전이 불안정하다. 안정화를 수행하라.
> 2. 안정화 후 `wz=0.30` 수준의 회전 명령에서도 로봇이 움직이지 못한다. 원인을 수정하라.
> 3. goal 근처까지 도착한 뒤에도 success를 내지 않고 `cmd_vel` 을 계속 낸다. 원인을 수정하라.
> 4. 반대 방향(약 180°) goal에서 `wz=-0.052631...` 이 고정된 채 yaw 수렴을 못한다. 원인을 수정하라.
> 5. 더 나아지긴 했으나, 도착 후 회전시키려 하면 `running backup` 을 수행한다. 원인을 분석하라.
> 6. 오늘 진행과정 전체와 사용 명령어/스크립트를 별도 md 파일로 정리하라.

### 1.2 이전 세션 요약
- 2026-04-17: 모터/인코더 교체 후 기존 캘리브레이션 무효화 반영
  - `ANGULAR_SCALE_FACTOR = 1.0`
  - `MCU_MIN_ANGULAR = 0.0`
  - `vx = -vx` 복원
  - `imu_invert_z` 적용
- 2026-04-20: Nav2 선속도 제한 `0.35 -> 0.20 m/s`, nvblox `combined_map_slice` 적용

---

## 2. 문제 분석

### 2.1 정지 상태 회전 헌팅

**증상**:
- 정지 근처에서 `cmd_vel.angular.z` 가 `+/-0.05 ~ +/-0.37` 범위로 좌우 교번
- 실제 로봇은 회전 시작/정지 문턱을 넘나들며 헌팅

**원인**:
- 정지마찰 때문에 회전 시작 최소값은 약 `0.37 rad/s`
- 그러나 Nav2 / DWB / velocity_smoother 는 그보다 작은 회전 명령도 계속 출력
- 드라이버가 작은 회전 명령을 그대로 MCU에 전달하면, 실제 바퀴는 반응하지 못하고 다음 제어 주기에서 반대 부호의 미세 명령이 다시 발생
- 결과적으로 deadzone-induced limit cycle 발생

### 2.2 첫 안정화 패치 후 회전 lockout (`wz=0.30` 무반응)

**증상**:
- 헌팅 억제용 start/stop 히스테리시스 적용 후, `wz=0.30` 을 계속 보내도 회전 시작이 되지 않음

**원인**:
- 시작 조건이 사실상 `0.37` 이상에 묶여 있었음
- 실제 사용자 요구는 `0.30` 수준의 유효 회전도 허용해야 함

**해결 방향**:
- 회전 "시작 조건" 과 "기동 키크 값" 을 분리
- 작은 요청이라도 일정 기준 이상이면 회전을 시작하고, 시작 순간만 `0.37` 로 승격해야 함

### 2.3 near-goal 수렴 중 progress abort

**증상**:
- goal 근처에서 `wz=0.157` 수준 회전을 계속 내보내는 동안 `Failed to make progress` 로 종료

**원인**:
- 회전 활성 상태가 너무 이른 시점에 해제되면서 near-goal 저속 회전이 끊김
- 동시에 progress checker 가 제자리 yaw 정렬을 충분히 기다려주지 못함

### 2.4 반대 방향 goal 에서 `wz=-0.0526315789` 고정

**증상**:
- goal 방향이 현재 heading 과 거의 반대일 때, 최종 수렴 단계에서 `wz=-0.052631578947...` 가 반복
- yaw 는 거의 바뀌지 않고 success 도 나지 않음

**수학적 원인**:
- `0.052631... = 1.0 / 19`
- 현재 설정 `max_vel_theta=1.0`, `vtheta_samples=20` 에서 DWB 의 최소 non-zero theta 샘플
- 또한 `RotateToGoal.slowing_factor` 로 감속된 near-goal 출력이 이 값 근처로 수렴 가능

**초기 판단**:
- 드라이버 시작 문턱 `ROT_MIN_REQUEST_ANGULAR=0.08` 이 `0.0526` 보다 높으면 회전이 시작되지 않음

**더 근본적인 원인**:
- active 상태에서 `max(raw_abs, MCU_ROT_STOP_ANGULAR)` 로 최소 `0.22 rad/s` 를 강제하면,
  `0.0526` 요청이 항상 `0.22` 로 증폭되어 오버슈트 발생
- 오버슈트 후 다시 반대쪽 near-goal 미세 제어 구간으로 돌아오며 동일 루프 반복
- 즉 기존 active-state minimum clamp 설계 자체가 Nav2의 비례 감속 제어와 충돌

### 2.5 도착 후 `running backup` 발생

**증상**:
- 회전 안정화는 이전보다 개선되었으나, 도착 후 회전 정렬 과정 중 BT 가 `running backup` 으로 넘어감
- `/cmd_vel` 에서 `linear.x = -0.05`, `angular.z = 0.0` 이 반복 출력됨

**초기 로그 분석 결과**:
1. `controller_server: Passing new path to controller.` 가 약 1Hz 로 반복됨
2. 이후 다음 경고가 연속 발생
   - `Behavior Tree tick rate 100.00 was exceeded!`
   - `Timed out while waiting for action server to acknowledge goal request for compute_path_to_pose`
   - `Control loop missed its desired rate of 10.0000Hz`
   - `Planner loop missed its desired rate of 5.0000 Hz. Current loop rate is 2.2810 Hz`
   - `Node timed out while executing service call to global_costmap/clear_entirely_global_costmap`
3. 그 직후
   - `Failed to get result for follow_path in node halt!`
   - `Goal failed`
   - `behavior_server: Running backup`

**중간 결론**:
- `backup` 은 원인이 아니라 결과다.
- planner / controller / bt_navigator / costmap clear 서비스가 time out 되며 `follow_path` 가 실패했고,
  기본 recovery tree 가 `BackUp` 행동을 실행했다.
- 드라이버는 `|vx| < 0.02` 인 정지 회전에서만 `angular.z` 를 조정하므로,
  `linear.x = -0.05`, `angular.z = 0.0` 인 backup 명령은 드라이버가 만든 값이 아니다.
- 즉 현재 이슈의 직접 원인은 회전 상태기계가 아니라 Nav2 recovery 진입이다.

**직접 원인**:
- replanning / costmap clear / BT tick 중 action/service 응답 지연으로 `follow_path` 실패

**기여 요인**:
- `bt_loop_duration: 10` -> BT 100Hz
- `controller_frequency: 10.0`, `expected_planner_frequency: 5.0`
- 맵 resize 및 costmap clear 시점에 planner loop 가 `2.281 Hz` 까지 하락
- 결과적으로 BT tick 이 action/service 응답을 제때 받지 못함

### 2.7 near-goal `Failed to make progress -> backup -> 이후 즉시 goal success`

**최신 로그**:
1. `Passing new path to controller.` 가 반복됨
2. `controller_server: Failed to make progress`
3. `behavior_server: Running backup`
4. `backup completed successfully`
5. 직후 `controller_server: Reached the goal!`, `bt_navigator: Goal succeeded`

**의미**:
- 이 경우에는 이전 timeout 경로보다 더 직접적으로, `SimpleProgressChecker` 가 near-goal 정렬 구간을 "진행 없음"으로 오판했다.
- recovery 의 `backup` 이 실제로 장애물을 피해서 문제를 해결한 것이 아니라, 잠시 시간을 소비한 뒤 다시 제어가 들어가며 goal 판정이 난 것이다.
- 즉 이 케이스의 직접 원인은 **progress checker false abort** 다.

**최종 판단**:
- 오늘 관측된 `running backup` 은 두 형태가 있었다.
   1. planner / BT timeout 기반 recovery 진입
   2. near-goal progress checker false abort 기반 recovery 진입
- 최신 재현 케이스는 2번이다.

### 2.6 정지 상태 `wz=-0.052631578947...` 은 실제 원인인가?

**판단**: 정지 회전 미동작의 실제 원인이다. 다만 `backup` 의 직접 원인은 아니다.

세부 판단:
- `/cmd_vel` 에 보이는 `-0.0526` 은 DWB / RotateToGoal 쪽에서 나온 실제 회전 요청값이다.
- 이전 수정으로 startup 순간에는 드라이버가 `0.37` 키크를 주므로, `-0.0526` 이 모든 문제의 단일 원인은 아니다.
- 그러나 startup 이후 RUNNING 단계가 완전 패스스루이면, 실제 MCU 입력이 다시 `-0.0526` 으로 떨어진다.
- 사용자가 관찰한 것처럼 정지 상태의 실기구는 그 값으로는 실제 회전을 유지하지 못한다.

결론:
- **정지 회전 미동작 문제**에서는 `-0.0526` 이 직접 원인이다.
- **도착 후 backup 문제**에서는 timeout/recovery 가 직접 원인이다.

---

## 3. 적용한 변경

### 3.1 정지 회전 헌팅 억제 1차 패치

**파일**: `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

초기 적용값:

| 항목 | 값 |
|---|---|
| `MCU_ROT_START_ANGULAR` | `0.37` |
| `MCU_ROT_STOP_ANGULAR` | `0.22` |
| `ROT_MIN_REQUEST_ANGULAR` | `0.08` |
| `ROT_RELEASE_ANGULAR` | `0.03` |
| `ROT_SIGN_HOLD_SEC` | `0.25` |

적용 내용:
- start/stop 히스테리시스
- 부호 전환 hold
- 정지 회전 상태 변수 추가

목표:
- deadzone 근처 좌우 교번 제거

### 3.2 회전 lockout 수정

**문제**:
- `wz=0.30` 이 반복되어도 회전 시작이 막힘

**조치**:
- `ROT_MIN_REQUEST_ANGULAR = 0.08` 을 start gate 로 사용해, `0.30` 명령도 회전 시작 허용

### 3.3 near-goal progress stall 완화

**파일**: `src/jupiter_nav_VO/config/nav2_params_fused.yaml`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `required_movement_radius` | `0.03` | `0.005` |
| `movement_time_allowance` | `20.0` | `45.0` |
| `yaw_goal_tolerance` | `0.20` | `0.30` |

의도:
- 최종 yaw 정렬 시 제자리 회전 허용 폭 확대
- progress abort 방지

### 3.8 near-goal progress false abort 추가 완화

**배경**:
- 최신 로그에서 `Failed to make progress -> backup -> backup 후 즉시 goal success` 가 발생
- 이는 progress checker 가 실제로는 거의 도착한 상태를 실패로 처리하고 있음을 의미

**파일**: `src/jupiter_nav_VO/config/nav2_params_fused.yaml`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `required_movement_radius` | `0.005` | `0.001` |
| `movement_time_allowance` | `45.0` | `120.0` |
| `yaw_goal_tolerance` | `0.30` | `0.32` |
| `RotateToGoal.slowing_factor` | `2.5` | `2.0` |

의도:
- near-goal 정지 회전/미세 드리프트를 progress 실패로 오판하지 않도록 완화
- 최종 yaw 정렬 출력 하한을 높여 정체 시간을 줄임
- `backup` 으로 가기 전에 goal success 로 자연 종료될 확률을 높임

### 3.4 `0.0526` yaw 데드락 해결용 Nav2 파라미터 조정

**파일**: `src/jupiter_nav_VO/config/nav2_params_fused.yaml`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `min_speed_theta` | `0.15` | `0.20` |
| `RotateToGoal.slowing_factor` | `5.0` | `2.5` |

의도:
- near-goal 감속값이 너무 작아져 드라이버 시작 문턱 아래로 떨어지는 상황 완화

### 3.5 정지 회전 상태기계 전면 재설계 (최종)

**파일**: `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

최종 설계:

| 항목 | 기존 | 최종 |
|---|---|---|
| 시작 방식 | start/stop + active 최소값 강제 | `STARTUP 키크 -> RUNNING 패스스루` |
| `ROT_MIN_REQUEST_ANGULAR` | `0.08` | `0.04` |
| `MCU_ROT_STOP_ANGULAR` | `0.22` | 삭제 |
| 신규 | 없음 | `ROT_STARTUP_KICK_SEC = 0.12` |

핵심 로직:
1. **STARTUP 단계**
   - 회전 시작 또는 방향 전환 직후 `0.12s` 동안 `0.37 rad/s` 출력
   - 목적: 정지마찰 극복
2. **RUNNING 단계**
   - 이후 Nav2 요청값 `angular.z` 를 그대로 통과
   - 목적: near-goal 미세 회전 허용
3. **release 조건**
   - `raw_abs < 0.03` 이거나 요청 부호가 0일 때 회전 종료
4. **부호 전환 hold**
   - `0.25s` 동안 이전 부호 유지
   - 목적: 좌우 교번 억제

**핵심 효과**:
- 기존 `max(raw_abs, 0.22)` 제거
- `0.0526` 같은 미세 제어값을 near-goal 에서 그대로 MCU로 전달 가능

### 3.6 정지 회전 RUNNING 하한 추가

**문제**:
- 완전 패스스루 설계는 오버슈트는 줄였지만, `0.0526` 같은 비현실적 저속 회전 명령을 다시 MCU에 흘려보낼 수 있었다.

**파일**: `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| RUNNING 단계 | `req_sign * raw_abs` | `req_sign * max(raw_abs, 0.12)` |
| 신규 상수 | 없음 | `ROT_RUNNING_MIN_ANGULAR = 0.12` |

의도:
- 회전 시작 후에도 정지 상태에서 실제로 못 도는 `0.0526` 영역은 피한다.
- 기존 실패 원인이었던 `0.22` 강제보다 훨씬 낮은 `0.12` 만 보장해 오버슈트 위험은 줄인다.

### 3.7 DWB 최소 theta 샘플 제거

**파일**: `src/jupiter_nav_VO/config/nav2_params_fused.yaml`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `min_theta_velocity_threshold` | `0.05` | `0.10` |
| `vtheta_samples` | `20` | `11` |

의도:
- `1/19 = 0.0526` 형태의 병적인 최소 비영점 회전 샘플을 줄인다.
- Nav2 가 애초에 실기구 비가동 영역의 회전값을 덜 내도록 한다.

---

## 4. 빌드 및 검증

### 4.1 드라이버 빌드

**명령**:
```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup 2>&1 | tail -5
```

**결과**:
- `jupiter_bringup` 빌드 성공
- Python / YAML 오류 없음

### 4.2 런치 재실행

```bash
ros2 launch /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
ros2 launch /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_fused_navigation.launch.py
```

### 4.3 관찰 결과

개선된 점:
- `wz=-0.0526` 고정 루프는 이전보다 완화
- 정지 회전 미세 제어가 이전 active-state minimum clamp 구조보다 자연스러워짐

새로 분리된 문제:
- goal 직후 회전 정렬 구간에서 Nav2 recovery tree 가 `backup` 실행
- 이 문제는 드라이버 회전 상태기계가 아니라 Nav2 stack timeout / recovery 문제로 분류됨

추가 관찰:
- 최신 케이스에서는 `backup completed successfully` 직후 바로 `Reached the goal!` / `Goal succeeded` 가 발생
- 이는 recovery 자체가 해결책이라기보다, progress checker abort 조건이 과민하다는 근거로 해석됨

---

## 5. 최종 결론

### 5.1 오늘 해결된 항목
- 정지 회전 헌팅 완화
- `wz=0.30` 회전 시작 lockout 제거
- near-goal progress abort 완화
- `0.0526` 미세 회전 deadlock 의 구조적 원인 제거
- 정지 회전 RUNNING 단계에서 `0.0526` 같은 실기구 비가동 명령 차단

### 5.2 오늘 새로 확인된 항목
- 도착 후 `backup` 은 회전 명령이 잘못 변형된 것이 아니라, Nav2 recovery 진입 결과
- 초기 케이스의 직접 원인은 planner / controller / bt_navigator / costmap clear 서비스의 응답 지연과 timeout
- 최신 케이스의 직접 원인은 near-goal `SimpleProgressChecker` false abort

### 5.3 다음 분석 우선순위
1. BT replanning / recovery tree 를 사용자 환경에 맞게 완화할 필요가 있는지 검토
2. planner / costmap clear 지연의 CPU 부하 원인 확인
3. 필요 시 custom BT XML 로 near-goal 재계획 빈도 또는 recovery 정책 조정
