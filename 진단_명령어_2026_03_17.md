# 진단 명령어 2026-03-17: 방안B-2 Anti-Hunting Filter 구현 및 배포

> 이 문서는 2026-03-17 세션에서 사용한 모든 명령어, 수정 내역, 빌드/배포 과정을 기록합니다.

---

## 목차
1. [방안B-2 구현: 드라이버 수정](#1-방안b-2-구현-드라이버-수정)
2. [Nav2 파라미터 튜닝](#2-nav2-파라미터-튜닝)
3. [빌드 및 배포](#3-빌드-및-배포)
4. [배포 검증 명령어](#4-배포-검증-명령어)
5. [테스트 실행](#5-테스트-실행)
6. [시스템 상태 확인](#6-시스템-상태-확인)
7. [펌웨어 플래시 후 Revert 명령어](#7-펌웨어-플래시-후-revert-명령어)

---

## 1. 방안B-2 구현: 드라이버 수정

### 1.1 수정 대상 파일
```
jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```

### 1.2 변경 1: `__init__`에 상태 변수 추가
`self.get_logger().info('Jupiter Driver Node has been initialized')` 직전에 삽입:
```python
# [2026-03-17 방안B-2] Anti-hunting angular velocity filter state
# MCU 데드존(0/72/144) + PID 적분 윈드업으로 인한
# 헌팅→오버슈트→발산 방지용 상태 변수
# ※ 방안A(펌웨어) 적용 후 아래 초기화 및 _anti_hunting_filter() 삭제할 것
self._ah_last_angular = 0.0
self._ah_last_time = time.time()
self._ah_reversal_count = 0
self._ah_last_reversal_time = 0.0
self._ah_brake_until = 0.0  # 제동 해제 시점
```

### 1.3 변경 2: `_anti_hunting_filter()` 메서드 추가
`__init__` 종료 후, `parameter_callback` 앞에 삽입:
```python
def _anti_hunting_filter(self, angular_corrected):
    """[방안B-2] MCU 데드존으로 인한 헌팅→윈드업→오버슈트 방지 필터.
    ※ 방안A(펌웨어) 적용 후 이 메서드 전체 삭제할 것.
    """
    now = time.time()
    dt = max(now - self._ah_last_time, 0.001)
    self._ah_last_time = now
    
    # 제동 구간 중이면 zero 출력 (MCU Differential_Ctrl이 Motion_Stop 호출)
    if now < self._ah_brake_until:
        self._ah_last_angular = 0.0
        return 0.0
    
    # 방향 전환 감지 (angular sign flip)
    if (angular_corrected * self._ah_last_angular < -0.001 and
            abs(self._ah_last_angular) > 0.03):
        self._ah_reversal_count += 1
        self._ah_last_reversal_time = now
    else:
        # 1초 이상 전환 없으면 카운터 감소
        if now - self._ah_last_reversal_time > 1.0:
            self._ah_reversal_count = max(0, self._ah_reversal_count - 1)
    
    # 연속 방향 전환 감지 (0.8초 내 2회 이상) → 헌팅 상태
    # 제동 삽입: MCU에 (0,0,0) 전송 → Motion_Stop → PID 리셋
    if (self._ah_reversal_count >= 2 and
            now - self._ah_last_reversal_time < 0.8):
        self._ah_brake_until = now + 0.15  # 150ms 제동
        self._ah_reversal_count = 0
        self._ah_last_angular = 0.0
        return 0.0
    
    # Angular rate limiting: 급격한 각속도 변화 방지
    MAX_ANG_RATE = 2.0  # rad/s² (scaled 후 기준)
    max_change = MAX_ANG_RATE * dt
    diff = angular_corrected - self._ah_last_angular
    if abs(diff) > max_change:
        angular_corrected = self._ah_last_angular + math.copysign(max_change, diff)
    
    self._ah_last_angular = angular_corrected
    return angular_corrected
```

### 1.4 변경 3: `cmd_vel_callback`에 필터 호출 삽입
`── 방안B 끝 ──` 주석 뒤, deadband 로직 앞에 삽입:
```python
# [2026-03-17 방안B-2] Anti-hunting filter: 방향전환 감쇠 + rate limiting
# ※ 방안A(펌웨어) 적용 후 아래 1줄 삭제할 것
angular_corrected = self._anti_hunting_filter(angular_corrected)
```

---

## 2. Nav2 파라미터 튜닝

### 2.1 수정 대상 파일
```
jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```

### 2.2 DWB 파라미터 변경

**max_vel_theta** (최대 각속도):
```yaml
# 변경 전:
max_vel_theta: 1.0           # 각속도 최대값
# 변경 후:
max_vel_theta: 0.7           # ⭐ 1.0→0.7: 데드존 오버슈트 방지 — 최대 회전속도 제한 [2026-03-17]
```

**acc_lim_theta / decel_lim_theta** (각가감속도):
```yaml
# 변경 전:
acc_lim_theta: 0.8
decel_lim_theta: -0.8
# 변경 후:
acc_lim_theta: 0.5           # ⭐ 0.8→0.5: 각가속도 제한 강화 — MCU PID 적응 시간 확보 [2026-03-17]
decel_lim_theta: -0.5        # ⭐ -0.8→-0.5: 각감속도 제한 강화 [2026-03-17]
```

**Twirling.scale** (스핀 방지 critic):
```yaml
# 변경 전:
Twirling.scale: 5.0                # 경로 추종 중 불필요한 스핀 방지
# 변경 후:
Twirling.scale: 15.0               # ⭐ 5.0→15.0: 데드존 오버슈트 스핀 강력 억제 [2026-03-17]
```

**Oscillation.scale** (진동 방지 critic):
```yaml
# 변경 전:
Oscillation.scale: 1.0
# 변경 후:
Oscillation.scale: 5.0              # ⭐ 1.0→5.0: 좌우 진동(헌팅) 탐지 시 강한 페널티 [2026-03-17]
```

---

## 3. 빌드 및 배포

### 3.1 빌드 명령어
```bash
cd /home/jetson/jupiter_ws_ros2
source /opt/ros/humble/setup.bash
colcon build --packages-select jupiter_bringup jupiter_nav_VO --symlink-install
```

빌드 결과:
```
Starting >>> jupiter_bringup
Finished <<< jupiter_bringup [4.08s]
Starting >>> jupiter_nav_VO
Finished <<< jupiter_nav_VO [3.87s]
Summary: 2 packages finished [8.37s]
```

---

## 4. 배포 검증 명령어

### 4.1 드라이버 방안B-2 코드 반영 확인
```bash
grep -n "_anti_hunting_filter\|_ah_last_angular\|방안B-2" \
  /home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```
예상 출력:
```
181:        # [2026-03-17 방안B-2] Anti-hunting angular velocity filter state
185:        self._ah_last_angular = 0.0
193:    def _anti_hunting_filter(self, angular_corrected):
194:        """[방안B-2] MCU 데드존으로 인한 헌팅→윈드업→오버슈트 방지 필터.
```

### 4.2 Nav2 파라미터 반영 확인
```bash
grep -n "Twirling.scale\|Oscillation.scale\|acc_lim_theta\|max_vel_theta" \
  /home/jetson/jupiter_ws_ros2/install/jupiter_nav_VO/share/jupiter_nav_VO/config/nav2_params_fused.yaml
```
예상 출력:
```
124:      max_vel_theta: 0.7
130:      acc_lim_theta: 0.5
156:      Twirling.scale: 15.0
159:      Oscillation.scale: 5.0
```

### 4.3 드라이버 프로세스 확인 (재시작 필요 여부)
```bash
# 현재 실행 중인 드라이버 PID와 시작시간 확인
ps -eo pid,start,args | grep jupiter_driver_comp | grep -v grep

# symlink-install이므로 build 경로의 파일 수정 시간 확인
ls -la /home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py

# 프로세스 시작 시각이 빌드 시각 이전이면 재시작 필요
```

---

## 5. 테스트 실행

### 5.1 시스템 재시작 (드라이버 변경 반영)
```bash
# 모든 ROS 노드 종료
pkill -f ros2
sleep 2

# 전체 시스템 재시작 (사용하는 launch 파일에 따라)
source /home/jetson/jupiter_ws_ros2/install/setup.bash
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py
```

### 5.2 방안B-2 테스트 실행
```bash
# 전체 테스트: Part1(데드존) + Part2(회전) + Part3(Nav2 Goal)
cd /home/jetson/jupiter_ws_ros2
python3 scripts/test_deadzone_bypass.py

# Nav2 Goal만 테스트
python3 scripts/test_deadzone_bypass.py --skip-rotation

# 회전만 테스트 (Nav2 제외)
python3 scripts/test_deadzone_bypass.py --skip-nav
```

### 5.3 수동 회전 테스트
```bash
# 작은 회전 명령 (데드존 영역)
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{angular: {z: 0.1}}" -r 10

# 정지
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{}" --once
```

### 5.4 Anti-hunting 효과 실시간 모니터링
```bash
# cmd_vel 명령 모니터링 (Nav2 → 드라이버)
ros2 topic echo /cmd_vel --field angular.z

# MCU 응답 속도 모니터링 (드라이버 → ROS)
ros2 topic echo /vel_raw --field angular.z
```

---

## 6. 시스템 상태 확인

### 6.1 현재 적용된 워크어라운드 확인
```bash
# 방안B: MIN_ANGULAR_DEADZONE 값 확인
grep "MIN_ANGULAR_DEADZONE" /home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py

# 방안B-2: Anti-hunting filter 존재 확인  
grep "_anti_hunting_filter" /home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py

# Nav2 파라미터 현재값 확인
grep -E "max_vel_theta|acc_lim_theta|decel_lim_theta|Twirling.scale|Oscillation.scale" \
  /home/jetson/jupiter_ws_ros2/install/jupiter_nav_VO/share/jupiter_nav_VO/config/nav2_params_fused.yaml
```

### 6.2 노드 목록 확인
```bash
ros2 node list
# 기대하는 노드:
# /jupiter_driver
# /ekf_filter_node
# /controller_server
# /planner_server
# /bt_navigator
# /rf2o_covariance_adapter
# /vslam_covariance_adapter
# /slam_toolbox
```

---

## 7. 펌웨어 플래시 후 Revert 명령어

> 방안A가 MCU에 플래시된 후, 아래 명령어들을 순서대로 실행하여 ROS 워크어라운드를 제거합니다.

### 7.1 드라이버 Revert

편집할 파일:
```
jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```

삭제 대상을 검색하여 확인:
```bash
# 삭제할 코드 위치 확인
grep -n "방안B\|_ah_\|_anti_hunting" \
  /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```

삭제할 영역:
1. `── 방안B 시작 ──` ~ `── 방안B 끝 ──` 사이 코드 (및 위 주석 3줄)
2. `self._ah_last_angular` ~ `self._ah_brake_until` 초기화 5줄 (및 위 주석 4줄)
3. `def _anti_hunting_filter(self, angular_corrected):` 메서드 전체
4. `angular_corrected = self._anti_hunting_filter(angular_corrected)` 및 위 주석 2줄

### 7.2 Nav2 파라미터 Revert

편집할 파일:
```
jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```

```bash
# sed로 원래 값 복원
cd /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config

# max_vel_theta: 0.7 → 1.0
sed -i 's/max_vel_theta: 0.7/max_vel_theta: 1.0/' nav2_params_fused.yaml

# acc_lim_theta: 0.5 → 0.8
sed -i 's/acc_lim_theta: 0.5/acc_lim_theta: 0.8/' nav2_params_fused.yaml

# decel_lim_theta: -0.5 → -0.8
sed -i 's/decel_lim_theta: -0.5/decel_lim_theta: -0.8/' nav2_params_fused.yaml

# Twirling.scale: 15.0 → 5.0
sed -i 's/Twirling.scale: 15.0/Twirling.scale: 5.0/' nav2_params_fused.yaml

# Oscillation.scale: 5.0 → 1.0
sed -i 's/Oscillation.scale: 5.0/Oscillation.scale: 1.0/' nav2_params_fused.yaml

# 변경 결과 확인
grep -E "max_vel_theta|acc_lim_theta|decel_lim_theta|Twirling.scale|Oscillation.scale" nav2_params_fused.yaml
```

### 7.3 재빌드 및 테스트
```bash
cd /home/jetson/jupiter_ws_ros2
source /opt/ros/humble/setup.bash
colcon build --packages-select jupiter_bringup jupiter_nav_VO --symlink-install

# 시스템 재시작 후 Nav2 Goal2 테스트
python3 scripts/test_deadzone_bypass.py
```
