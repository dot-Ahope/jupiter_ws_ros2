# 명령어 로그 2026-05-08: SLAM strict + Path inflation 조정 + forced_recovery 비활성화 + firmware 보정 환원

> 진단 및 수정에 사용한 명령어와 스크립트/설정 파일을 요약 없이 전부 기록한다.

## 0. 오늘 사용한 터미널 명령어 원문

### 0.1 새 bag 정보 확인 (3개 bag)

```bash
ls -la /home/jetson/bags/latest/ 2>&1 | head -10 && ros2 bag info /home/jetson/bags/latest 2>&1 | head -50
```

목적: 사용자가 매번 만든 새 bag 의 시간, 메시지 수, topic 종류 확인.
결과: 첫 bag 166.6s, 두 번째 bag 219.3s, 세 번째 bag 137.1s. `/cmd_vel`, `/cmd_vel_force`, `/stuck_status`, `/scan_merged`, `/jupiter/wheel_speeds` 등 분석에 필요한 토픽 모두 기록됨.

```
Files:             latest_0.db3
Bag size:          47.4 MiB
Storage id:        sqlite3
Duration:          166.594823020s
Topic information: Topic: /cmd_vel | Type: geometry_msgs/msg/Twist | Count: 2172
                   ...
                   Topic: /jupiter/wheel_speeds | Type: std_msgs/msg/Float32MultiArray | Count: 4085
                   Topic: /cmd_vel_force | Type: geometry_msgs/msg/Twist | Count: 26
```

### 0.2 yaw jump 정밀 분석 (단계 1 효과 검증 사전 조사)

```bash
cd /tmp && source /opt/ros/humble/setup.bash && python3 analyze_yaw_jump.py 2>&1 | head -120
```

목적: bag 의 yaw jump 시점 / 폭 / robot 거동 확인.
결과: 20회 jump, 6건 stationary, 최대 +19.58°. 회전/직진 무관하게 발생.

### 0.3 path corner clearance 분석

```bash
cd /tmp && source /opt/ros/humble/setup.bash && python3 analyze_path_corners.py 2>&1 | head -80
```

목적: path waypoint 의 obstacle 까지 최단 거리, 곡률 기준 직선/코너 분리.
결과: 첫 bag 코너 path 5.6% 가 <20cm (inflation), 코너 vx 0.090 m/s (직선 0.110 의 18% 감속).

### 0.4 path lateral 분석 (signed)

```bash
cd /tmp && source /opt/ros/humble/setup.bash && python3 analyze_lateral.py 2>&1 | tail -50
```

목적: robot trajectory 가 path 에서 좌/우 어느 쪽으로 얼마나 치우치는지.
결과 (단계 1 적용 후 첫 bag): RMS 3.1cm, p95 9.1cm, 평균 signed -0.2cm — 매우 양호.

### 0.5 wheel asymmetry 분석 (사용자 "쏠림" 의혹 확인)

```bash
cd /tmp && source /opt/ros/humble/setup.bash && python3 analyze_wheel_asym.py 2>&1 | head -120
cd /tmp && source /opt/ros/humble/setup.bash && python3 analyze_wheel_asym2.py 2>&1 | head -80
```

목적: cmd_vel 직진 시 좌/우 wheel speed 차이 측정. 첫 시도는 4-wheel 가정으로 실패 (data 가 2-element 였음), 두 번째 스크립트로 재분석.
결과: 평균 좌/우 차이 0.7%, 시간대별 ±9% 변동, 좌회전 응답 우회전 대비 30% 큼, IMU 직진 시 -0.33°/s drift.

```
--- 직진 명령 (vx>+0.05, |ω|<0.05) 표본 분석 (n=189) ---
  cmd_vx 평균: +0.1426 m/s
  data[0]:    -0.1212
  data[1]:    -0.1204
  data[0] - data[1]: -0.0008 (-0.70%)
```

### 0.6 robot trajectory vs path clearance (코너 cut 검증)

```bash
cd /tmp && source /opt/ros/humble/setup.bash && python3 analyze_robot_vs_path_clearance.py 2>&1 | tail -50
```

목적: path 가 wide 한데 robot 이 따라가지 못하고 안쪽 cut 하는 현상 정량화.
결과 (요청 1 v1 적용 후 두 번째 bag): path↔obstacle 51.3cm vs **robot↔obstacle 41.8cm** (9.5cm 차). 14.2% 가 <25cm. → MPPI obstacle 회피 약화 입증.

### 0.7 safety vs nav2 충돌 분석

```bash
cat <<'EOF' > /tmp/analyze_safety_conflict.py
... (스크립트 본문 — §1.0 참조)
EOF
python3 /tmp/analyze_safety_conflict.py 2>&1 | tail -60
```

목적: `/stuck_status` True 전환 시점, `/cmd_vel_force` backup burst, nav2 BackUp burst 의 시간 겹침 분석.
결과 (두 번째 bag): stuck event 4건 (3건이 angular stall, 1건이 nav2 backup 중 stall), force backup 4 burst, nav2 backup 8 burst, force↔nav 동시 backup 1건 (t=204.5s, 6.2s nav burst 중 force 끼어듦).

### 0.8 EKF / IMU mount 검증

```bash
find /home/jetson/jupiter_ws_ros2/src -type f \( -name "*.yaml" -o -name "*.py" -o -name "*.launch" -o -name "*.urdf*" -o -name "*.xacro" \) 2>/dev/null | xargs grep -l "ekf\|robot_localization\|imu.*mount\|imu_link\|base_link.*imu\|imu.*base_link" 2>/dev/null | head -20
```

```bash
grep -nE "imu_link|base_link|imu.*static|StaticTransform" /home/jetson/jupiter_ws_ros2/src/jupiter_description/urdf/jupiter_simple.urdf | head -20 && echo "---" && grep -nE "imu_link|imu.*static|StaticTransform" /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/launch/bringup.launch.py | head -20
```

목적: IMU mount 의 yaw alignment 보정 위치 확인.
결과: URDF (`jupiter_simple.urdf:178-182`) `rpy="0 0 0"`, bringup (`bringup.launch.py:79-80`) static_transform 도 rpy=0,0,0. **z 위치만 다름 (0.05 vs 0.02) — TF 이중 정의 발견**.

### 0.9 driver / firmware 의 mechanical fix 전 보정 코드 전수 조사

```bash
grep -nE "imu|angular_velocity|orientation|gyro|yaw_offset|sign|flip|invert|reverse|negat" /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py | head -40
```

```bash
grep -nE "before|기존|이전|outdate|deprecat|TODO|FIXME|2026-03|2026-04-1[0-7]" /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py | head -30 && echo "===" && grep -nE "ANGULAR_SCALE|MCU_MIN_ANG|모터.*교체|기구|wheel.*replac|교체" /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```

```bash
grep -rnE "M3|기구|모터.*교체|wheel.*replac|교체|caster|edge.*spike|encoder.*compen" /home/jetson/mcu/extbd/Source/ 2>/dev/null | grep -v "Binary" | head -25
```

목적: mechanical fix 이전 (2026-04-17 모터 교체 전) 만들어진 보정 코드 시간순 정리.
결과: ANGULAR_SCALE_FACTOR (driver), MCU_MIN_ANGULAR (driver), Ki=0.02/Kd=0.3 (driver PID), M1/M3 deadzone 720/690 (firmware), balance correction (firmware), IMU mount TF 이중정의 — 8개 항목 식별.

### 0.10 jupiter_safety 모듈 분석

```bash
ls /home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/ && echo "===" && grep -nE "backup|recovery|backup|cmd_vel_force|stuck" /home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/*.py 2>/dev/null | head -30
```

목적: forced_recovery, stuck_detector 의 동작 메커니즘 / threshold / publish 토픽 확인.
결과: forced_recovery `BACKUP_VX=-0.10`, `BACKUP_DURATION=1.0`, `RECOVERY_COOLDOWN=5.0`, rear safety check `REAR_SAFE_DIST=0.20`. stuck_detector `STALL_DURATION=1.5`, `ANGULAR_STALL_CMD_MIN=0.10`, `STALL_CMD_MIN=0.05`.

### 0.11 inflation / critic 설정 확인

```bash
grep -n "inflation_radius\|cost_scaling_factor\|footprint_padding\|FollowPath\|cost_critic\|ObstaclesCritic\|PathFollowCritic" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml | head -40
```

목적: 변경 대상 파라미터 위치 식별.
결과: local_costmap inflation 0.20/3.0, global 0.25/3.0, ObstaclesCritic critical_weight 20.0, collision_margin 0.10, PathAlignCritic 14.0.

### 0.12 forced_recovery launch 위치 확인

```bash
grep -rnE "forced_recovery|stuck_detector|jupiter_safety" /home/jetson/jupiter_ws_ros2/src/jupiter_safety/launch/ /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/ /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/launch/ 2>/dev/null | head -30
```

목적: 비활성화 위치 찾기.
결과: `safety_layer.launch.py:75-80` 에서 노드 정의, line 106 LaunchDescription 에 추가됨.

### 0.13 firmware 빌드 환경 확인

```bash
ls /home/jetson/mcu/ | head -20 && echo "===" && find /home/jetson/mcu -maxdepth 3 -name "Makefile" -o -name "*.mk" -o -name "build.sh" -o -name "*.uvprojx" -o -name "*.ioc" 2>/dev/null | head -10
ls /home/jetson/mcu/extbd/ && echo "===" && find /home/jetson/mcu/extbd -maxdepth 4 -name "*.bin" -o -name "*.hex" -o -name "*.axf" 2>/dev/null | head -10
which arm-none-eabi-gcc && echo "===" && ls /home/jetson/mcu/extbd/*.bin /home/jetson/mcu/extbd/Output 2>/dev/null && echo "===" && find /home/jetson/mcu -maxdepth 5 -name "*.bin" -o -name "*.hex" 2>/dev/null | head -5
```

목적: Jetson 에서 firmware 직접 빌드 가능한지 확인.
결과: Keil µVision (`rosmaster.uvprojx`) 만 존재. `arm-none-eabi-gcc` 없음. **Windows + Keil 필요**.

### 0.14 패키지 빌드 (변경 적용)

#### 0.14.1 jupiter_nav (단계 1: SLAM matching strict)

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_nav --symlink-install 2>&1 | tail -30
```

목적: slam_params.yaml 변경 (5 항목) 후 빌드.
결과: 4.63s, 성공.

#### 0.14.2 jupiter_nav_VO (요청 1 v1)

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_nav_VO --symlink-install 2>&1 | tail -10
```

목적: nav2_params_fused.yaml 변경 (inflation 0.32, ObstaclesCritic critical 15) 후 빌드.
결과: 4.70s, 성공.

#### 0.14.3 jupiter_nav_VO (요청 1 v2)

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_nav_VO --symlink-install 2>&1 | tail -5
```

목적: critic 부분 환원 + PathAlign 강화.
결과: 4.61s, 성공.

#### 0.14.4 jupiter_safety (forced_recovery 비활성화)

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_safety --symlink-install 2>&1 | tail -5
```

목적: safety_layer.launch.py 변경 후 빌드.
결과: 4.53s, 성공.

## 1. 분석에 사용한 설정 / 스크립트 파일

### 1.0 오늘 사용 / 열람한 스크립트 및 파일 전체 경로

#### 분석 스크립트 (작성 / 실행)
- `/tmp/analyze_yaw_jump.py` (열람 — 이전 세션 작성)
- `/tmp/analyze_path_clearance.py` (열람)
- `/tmp/analyze_lateral.py` (열람)
- `/tmp/analyze_path_corners.py` ← 신규 작성
- `/tmp/analyze_wheel_asym.py` ← 신규 작성 (4-wheel 가정 — 실패)
- `/tmp/analyze_wheel_asym2.py` ← 신규 작성 (2-wheel 으로 재작성)
- `/tmp/analyze_robot_vs_path_clearance.py` ← 신규 작성
- `/tmp/analyze_safety_conflict.py` ← 신규 작성

#### 수정된 파일
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml` ← 수정 (단계 1)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` ← 수정 (요청 1 v1, v2)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/launch/safety_layer.launch.py` ← 수정 (forced_recovery 비활성화)
- `/home/jetson/mcu/extbd/Source/APP/app_mecanum.c` ← 수정 (balance correction 비활성화)
- `/home/jetson/mcu/extbd/Source/APP/app.c` ← 수정 (M1/M3 deadzone 700/700 환원)

#### 열람한 파일
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` (열람: line 180-260, 260-440)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/forced_recovery.py` (열람)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/stuck_detector.py` (열람)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/launch/safety_layer.launch.py` (열람)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` (열람: line 1-200, 450-650)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_description/urdf/jupiter_simple.urdf` (열람: line 160-189)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml` (열람: line 170-249)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/odom_covariance_adapter.py` (열람: line 1-100)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/config/collision_monitor.yaml` (열람)
- `/home/jetson/mcu/extbd/Source/APP/app_mecanum.c` (열람 전체)
- `/home/jetson/mcu/extbd/Source/BSP/bsp_motor.c` (열람 전체)
- `/home/jetson/mcu/extbd/Source/APP/app.c` (열람: line 140-189)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/param/jupiter_driver_params.yaml` (grep)

### 1.1 수정 #1 — `slam_params.yaml` (단계 1: SLAM matching strict)

`/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml`

| 라인 | 파라미터 | 변경 전 | 변경 후 |
|---|---|---|---|
| 41 | `minimum_travel_heading` | 0.05 (≈2.9°) | **0.20 (≈11.5°)** |
| 49 | `link_match_minimum_response_fine` | 0.50 | **0.65** |
| 56 | `loop_search_maximum_distance` | 4.0 | **2.5** |
| 60 | `loop_match_minimum_response_coarse` | 0.55 | **0.70** |
| 64 | `correlation_search_space_dimension` | 0.6 | **0.3** |

`loop_match_minimum_response_fine` 은 이전 세션에 이미 0.65 로 변경됨 — skip.

### 1.2 수정 #2 — `nav2_params_fused.yaml` (요청 1: path obstacle 거리 + 코너 감속)

`/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

#### v1 (첫 적용 — robot 안쪽 cut 부작용 발견)

| 라인 | 파라미터 | 변경 전 | v1 |
|---|---|---|---|
| 215 | `ObstaclesCritic.critical_weight` | 20.0 | 15.0 |
| 218 | `ObstaclesCritic.collision_margin_distance` | 0.10 | 0.08 |
| 437 | `global_costmap.inflation_layer.inflation_radius` | 0.25 | **0.32** |
| 436 | `global_costmap.inflation_layer.cost_scaling_factor` | 3.0 | **2.5** |

#### v2 (부작용 보완 — 최종)

| 라인 | 파라미터 | v1 | **v2 (최종)** |
|---|---|---|---|
| 215 | `ObstaclesCritic.critical_weight` | 15.0 | **18.0** (부분 환원) |
| 218 | `ObstaclesCritic.collision_margin_distance` | 0.08 | **0.10** (환원) |
| 226 | `PathAlignCritic.cost_weight` | 14.0 | **20.0** (강화) |
| 437 | `global_costmap.inflation_radius` | 0.32 | 0.32 (유지) |
| 436 | `cost_scaling_factor` | 2.5 | 2.5 (유지) |

### 1.3 수정 #3 — `safety_layer.launch.py` (forced_recovery 비활성화)

`/home/jetson/jupiter_ws_ros2/src/jupiter_safety/launch/safety_layer.launch.py`

- line 73-80 (`forced_recovery_node` 정의): 코멘트 처리 + 비활성화 사유 명시
- line 106: LaunchDescription 에서 `forced_recovery_node` 라인 코멘트

### 1.4 수정 #4 — `app_mecanum.c` (balance correction 비활성화)

`/home/jetson/mcu/extbd/Source/APP/app_mecanum.c`

- line 120-167: balance correction 블록 (직진 시 M1/M3 encoder delta 보정) 전체 주석 처리
- 변수 (`balance_m1_ref`, `balance_m3_ref`, `balance_prev_vx`) 는 보존 + `(void)` 캐스트로 unused 경고 억제 — 재활성화 시 코드 부활 가능하도록

### 1.5 수정 #5 — `app.c` (M1/M3 deadzone 환원)

`/home/jetson/mcu/extbd/Source/APP/app.c`

| Motor | 변경 전 (fwd, bwd) | **변경 후** |
|---|---|---|
| M1 | 720, 690 | **700, 700** |
| M2 | 780, 750 | 780, 750 (그대로) |
| M3 | 720, 690 | **700, 700** |

## 2. 관찰 기준 요약

### 2.1 Bag 분석 결과 (3개 bag 비교)

| 지표 | bag #1 (단계 1 후) | bag #2 (요청 1 v1) | bag #3 (v2 + safety 비활성) |
|---|---|---|---|
| Duration | 166.6s | 219.3s | 137.1s |
| Path lateral RMS | 3.1cm | 3.6cm | 3.8cm |
| Path lateral p95 | 9.1cm | 12.3cm | 12.9cm |
| Path lateral max | 18.3cm | 18.7cm | 23.0cm |
| 코너 path↔obstacle <20cm | 5.6% | 0.8% | 0.4% |
| 코너 vx 평균 | 0.090 m/s | 0.098 m/s | 0.082 m/s |
| 직선 vx 평균 | 0.110 m/s | 0.110 m/s | 0.139 m/s |
| Robot↔obstacle (코너 평균) | — | 41.8cm | **61.3cm** |
| Robot↔obstacle <25cm | — | 14.2% | **5.9%** |
| /stuck_status True 전환 | — | 4 | **0** |
| /cmd_vel_force burst | — | 4 | **0** |
| nav2 backup burst | — | 8 (max 6.2s) | 7 (max 2.55s) |

### 2.2 wheel asymmetry (bag #2 측정, 모터 보정 환원 전 baseline)

| 지표 | 측정값 | 평가 |
|---|---|---|
| 직진 시 좌/우 평균 차이 | -0.0008 (-0.7%) | 작음 |
| 시간대별 최대 차이 (90-120s) | +0.0386 (9.0%) | 변동 큼 |
| 좌회전 응답 좌/우 비율 | 30% 차 (좌측 큼) | 비대칭 |
| 우회전 응답 좌/우 비율 | 3% 차 | 거의 대칭 |
| IMU 직진 시 yaw rate 평균 | -0.33°/s (CW) | drift 존재 |
| /jupiter/get_vel.linear.x / cmd_vel.linear.x | 0.871 | 13% 응답 부족 |
