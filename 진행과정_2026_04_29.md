# 진행과정 2026-04-29: 상태 의존적 cmd_vel 응답 — balance correction stale refs 근본 원인 규명 및 수정

## 1. 목표

### 1.1 사용자 요청 (시간순)

> 1. (세션 시작) "플래싱 했는데, 로버가 안움직인다. 확인해보라."
> 2. (시리얼 충돌 진단 후, 재부팅 → 모터 동작 확인) "재부팅 시키니 모터가 움직인다. 플래싱을 한 이후로도, 로버의 상태에 따라 움직임이 달라지는 문제가 여전히 발생한다."
> 3. (3가지 시나리오 정리 후) "3개의 시나리오 모두 발생하고 있다. 참고로 nav2_vslam_fused.launch.py 를 실행하여, 프로세스를 실행시킨다."
> 4. (path_curvature_diag.py ratio 버그 수정) "무해하다면, 그대로 두고, 순서대로 진행하도록 하자"
> 5. (재측정 결과 ratio=0.981 정상) — 시나리오 2 측정 아티팩트 가능성
> 6. (scenario3_diag.py 결과 rf2o_back=0.0000) — scenario 3 도 의심
> 7. "모든 버전에서 똑같은 문제가 발생한다." — 4개 백업 driver 검증
> 8. (백업 driver 전환 도구 요청) "jupiter_ws_ros2의 백업 파일로 실행시켜봐야겠다. 실행시킬 방법을 준비하라."
> 9. **"처음부터 다시 생각하자. 로버가 움직이는 상태에 따라, 같은 cmd_vel 명령에도, 모터가 다르게 움직이는 상황임을 인지하자. 이 때, 전체 코드베이스를 분석하고, 해당하는 문제의 원인을 파악하도록 하자."** ← Plan mode 전환
> 10. (Plan 작성 후) "시나리오가 잘못됬다. 2번 시나리오 : 직진 중 좌회전 후 직진 시, 좌회전 이전 원래 직진 방향으로 돌아올 때까지 로버가 우회전 함. 3번 시나리오 : 멈춰있는 상황에서 회전을 시키고, 그 상태에서 직진을 시키면서, 회전을 멈출 경우, 일직선으로 가는 것이 아니라, 회전 반대방향으로 회전하면서 직진함."
> 11. (정정된 plan 승인) → T1 + T2 펌웨어 패치 적용
> 12. (사용자 펌웨어 플래시 후) "증상이 남아있기는 하나 많이 개선되었다. 무엇이 문제였는가?"
> 13. "전진 시 오른쪽으로 회전을 줄 시, 왼쪽으로 복귀하는 현상이 있는데, 미세하다."

### 1.2 이전 세션 요약 / 연관 작업

- [진행과정_2026_04_28](/home/jetson/jupiter_ws_ros2/진행과정_2026_04_28.md) — `Motion_Set_Yaw_Adjust` edge-trigger 패치 4개 차종 \_Ctrl 함수 적용. 사용자가 그 펌웨어를 플래시한 상태로 본 세션 시작.
- 4개 차종 펌웨어 백업: `Source/APP/*.c.bak_2026_04_28`
- driver 백업 3개: `src/jupiter_bringup/backup_2026_04_28/*.py.bak{,2,3}`

---

## 2. 대상 시스템 구성

| 구성요소 | 경로 | 역할 |
|---|---|---|
| Launch | `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` | Nav2 + VSLAM 융합 실행 (jupiter_driver 포함) |
| Driver | `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` | cmd_vel → MCU serial. 670 lines |
| Driver lib | `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py` | `set_car_motion(v_x,v_y,v_z,adjust)` 패킷 빌더, FUNC_MOTION=0x12 |
| MCU 펌웨어 | `/home/jetson/mcu/extbd/Source/APP/` | STM32F1, FreeRTOS, app_mecanum.c (실사용 차종) |
| 입력 | `joy joy_node` + `jupiter_ctrl jupiter_joy` | 강도 2.0 으로 cmd_vel publish |
| 차종 설정 | driver `car_type=1` = `CAR_MECANUM` (실제 2-wheel diff drive 이지만 코드상 mecanum 경로) |

---

## 3. 문제 분석

### 3.1 시리얼 포트 충돌 — 모터 무동작 (세션 초반)

**증상**: 펌웨어 플래시 후 `cmd_vel` publish 해도 모터 무반응.

**원인**: `lsof /dev/ttyUSB0` 결과로 두 프로세스가 동시에 점유 발견:
- PID 2958 — `python3 /home/jetson/Rosmaster/rosmaster/rosmaster_main.py` (4시간 36분 째 실행 중)
- PID 225895 — `jupiter_driver_compensated` (현재 ROS2 드라이버)

레거시 야붐 드라이버가 자동시작 (`/home/jetson/.config/autostart/bash.desktop` → `start_app.sh` → `rosmaster_main.py`) 으로 부팅 시 떠 있어 시리얼 패킷 충돌 발생. 두 프로세스가 같은 포트에 write/read → 체크섬 깨짐 → MCU 가 패킷 폐기.

**근본 원인**: GUI 세션 자동시작 항목이 활성화되어 있음.

**해결**: 사용자가 시스템 재부팅 → 충돌 해소되어 모터 동작 확인. 자동시작 비활성화는 후속 옵션 제시 (사용자 미수행).

### 3.2 시나리오 2, 3 의 진짜 원인 — Mecanum_Ctrl balance correction 의 stale 인코더 참조

**정정된 시나리오** (사용자 보고 #10):
- **시나리오 2**: 직진 → 좌회전 → 직진 시, **좌회전 직전 헤딩으로 돌아올 때까지 로버가 우회전 (자동 복귀)**
- **시나리오 3**: 정지 → 회전 → (회전 중) 직진 추가 → 회전 OFF → **회전 반대 방향으로 yaw 하면서 직진**

**초기 의심 (틀림)**: MCU yaw PID (`Motion_Set_Yaw_Adjust` + `Mecanum_Yaw_Calc`) 가 SetPoint 를 잘못된 시점에 캡처해서 자동 복귀를 만들고 있다고 가정.

**정정 후 — 정확한 메커니즘**: [app_mecanum.c:25,154,214](/home/jetson/mcu/extbd/Source/APP/app_mecanum.c#L154) 에서 `g_speed_setup` 은 `Mecanum_State` (FUNC_AUTO_RUN 경로) 에서만 갱신되고 `Mecanum_Ctrl` (FUNC_MOTION = ROS cmd_vel 경로) 에서는 절대 갱신 안 됨. 따라서 `g_offset_yaw = yaw_offset × g_speed_setup = yaw_offset × 0 = 0` → MCU yaw PID 의 모터 영향이 **수학적으로 0**. 4개 차종 edge-trigger 패치 (2026-04-28) 도 functionally inert.

**진짜 메커니즘** ([app_mecanum.c:117-142](/home/jetson/mcu/extbd/Source/APP/app_mecanum.c#L117) — 2026-04-29 패치 전 상태):

```c
if (V_y == 0 && V_z == 0) {
    if ((balance_prev_vx > 0) != (V_x > 0) || balance_prev_vx == 0) {
        balance_m1_ref = Encoder_Get_Count_Now(MOTOR_ID_M1);
        balance_m3_ref = Encoder_Get_Count_Now(MOTOR_ID_M3);
    }
    int32_t m1_delta = Encoder_Get_Count_Now(MOTOR_ID_M1) - balance_m1_ref;
    int32_t m3_delta = Encoder_Get_Count_Now(MOTOR_ID_M3) - balance_m3_ref;
    int32_t diff = m1_delta - m3_delta;
    int16_t correction = (int16_t)((float)diff * 0.02f);
    if (correction > 50) correction = 50;
    if (correction < -50) correction = -50;
    int dir = (V_x > 0) ? 1 : -1;
    speed_L1_setup += dir * correction;
    speed_R1_setup -= dir * correction;
    ...
}
balance_prev_vx = V_x;   // line 142, if 블록 밖 (무조건 갱신)
```

reset 조건이 부호 flip + `prev==0` 만이라, **회전/측방 phase 동안 인코더가 발산해도 refs 는 회전 직전 값 유지**. 회전 직후 직진 진입 시:
- 두 reset 조건 모두 false (prev=200, V_x=200, 같은 부호; prev≠0)
- → refs **회전 이전 값 그대로**
- → m1/m3_delta = 회전 중 누적된 인코더 발산 분 + 직진 분
- → diff 가 큼 → correction clamp ±50 PWM
- → L 또는 R 모터에 50 PWM 비대칭 차이가 **여러 tick 동안 지속**
- → 로버가 회전 반대 방향으로 강제 회전
- → m1_delta 가 m3_delta 와 같아질 때까지 = 회전 양만큼 정확히 되돌아옴 = "원래 헤딩 자동 복귀"

**측정 시 우연히 못 잡은 이유** (`scenario3_diag.py` 첫 결과 rf2o_back=0):
- 테스트 시퀀스에 회전→정지(0.5s)→직진 사이에 idle stop 이 있었음
- idle stop 동안 driver 가 V=0 보내면 → MCU `Motion_Stop` → 별개로 직전 회전 phase 마지막 호출에서 `balance_prev_vx = V_x = 0` 갱신됨 (line 142)
- → 이후 직진 진입 시 `prev_vx==0` 조건 트리거 → refs 자연 reset
- 이 우회 메커니즘이 작동했었음. 사용자 실제 시나리오는 idle stop 없는 연속 시퀀스라 우회 안 됨.

### 3.3 시나리오 1 — Deadzone 비대칭

**증상**: 정지 → 직진 (vx=0.2) 시 약한 좌측 드리프트.

**원인** ([app.c:158](/home/jetson/mcu/extbd/Source/APP/app.c#L158)):
```c
Motor_Set_Dead_Zone(MOTOR_ID_M1, 720, 690);   // L motor: fwd=720, bwd=690
// MOTOR_ID_M3 → bsp_motor.c:19-20 default {700, 760, 700, 700} 의 [2] = 700/700
```

직진 시 `speed_fb = -V_x` (음수, bwd 영역) → [Motor_Ignore_Dead_Zone (else 분기)](/home/jetson/mcu/extbd/Source/BSP/bsp_motor.c#L75):
- M1: `pulse - 690`
- M3: `pulse - 700`
- `|M3| > |M1|` by 10 PWM units → R wheel 약 1.1% 더 빠름 → 좌측 누적 편향

balance correction 이 lag 으로 일부 보정하지만 완전 제거는 못 함 → 작은 잔류 좌측 드리프트.

### 3.4 측정 도구 버그 — path_curvature_diag.py 부호 평균 상쇄

**증상**: 이전 세션에서 측정한 cmd→rf2o ratio = 1.589× 가 "over-rotation 1.589배" 로 보고됨.

**원인**: `_stats()` 의 `avg_nz` 가 `|z|>0.05` sample 의 **부호 있는 평균**:
```python
nonzero = [v for v in vals if abs(v) > 0.05]
avg_nz = (sum(nonzero) / len(nonzero)) if nonzero else 0.0   # ← 부호 평균
```

좌(+) / 우(-) 회전 sample 이 섞이면 서로 상쇄되어 cmd 와 rf2o 의 평균 magnitude 가 우연히 안 맞아도 ratio 가 왜곡.

**근본 원인**: 절댓값 평균 (`mag_nz`) 을 계산하지 않은 통계 버그.

**수정 후 측정**: ratio = **0.981** (정상 범위 0.85~1.15) → over-rotation 1.589× 는 측정 아티팩트, 회전량 자체는 명령대로 ±5% 이내 처리됨이 확정.

---

## 4. 적용한 변경

### 4.1 path_curvature_diag.py — `mag_nz` 추가

`/home/jetson/path_curvature_diag.py`

| 변경 전 | 변경 후 |
|---|---|
| `avg_nz = sum(nonzero) / len(nonzero)` | `mag_nz = sum(abs(v) for v in nonzero) / len(nonzero)` 추가 |
| ratio 계산이 `avg_nz` (부호 평균) 사용 | ratio 계산이 `mag_nz` (절댓값 평균) 사용 |
| 컬럼 헤더 `avg|>0.05|` | `mag|>0.05|` |
| final summary `cmd → rf2o (전체 평균비)` | `cmd → rf2o (전체 |z| 평균비)` + `|cmd|`, `|rf2o|` 별도 표기 |

### 4.2 scenario3_diag.py 신규 — 회전 종료 후 자동 복귀 검증

`/home/jetson/scenario3_diag.py` (199 lines, 신규)

상태기계 IDLE → ROTATING (cmd_z>0.30) → SETTLE (cmd_z≤0.05 후 3초) 로 회전 종료 시점 자동 감지. SETTLE 동안 50ms 마다 `cmd_x/cmd_z/rf2o_z/imu_z/mcu_z` 출력. **복귀 peak** = 회전과 반대 방향으로 측정된 angular_z 의 peak 자동 산출.

### 4.3 scenario1_diag.py 신규 — 직진 드리프트 정량화

`/home/jetson/scenario1_diag.py` (177 lines, 신규)

상태기계 IDLE → FORWARD (vx>0.05, |angular_cmd|≤0.10). FORWARD segment 별로:
- 지속 시간, mcu vx 적분으로 거리 산출
- ∫rf2o yaw [°], ∫imu yaw [°] 적분
- 평균 yaw rate [°/s], drift per meter [°/m]
- direction 분류 (LEFT(+) / RIGHT(-) / NONE)

### 4.4 swap_driver.sh 신규 — driver 백업 버전 전환 도구

`/home/jetson/swap_driver.sh` (76 lines, 신규)

`bak/bak2/bak3/current/status` 인자로 src/ 와 build/ 동시 갱신, colcon build 없이 다음 launch 부터 적용.

### 4.5 app_mecanum.c — Balance refs invalidate (T1, 핵심 fix)

`/home/jetson/mcu/extbd/Source/APP/app_mecanum.c`

**변경 1** — V=0 stop 분기 ([line 60-68](/home/jetson/mcu/extbd/Source/APP/app_mecanum.c#L60)):

| 변경 전 | 변경 후 |
|---|---|
| `Motion_Stop`, `g_mecanum_prev_adjust=0`, `return` | 위 + `balance_prev_vx = 0;` 한 줄 추가 |

**변경 2** — balance correction 블록 재구성 ([line 120-157](/home/jetson/mcu/extbd/Source/APP/app_mecanum.c#L120)):

| 변경 전 | 변경 후 |
|---|---|
| `if (V_y==0 && V_z==0) { ... }` 단일 분기 | `if (V_y==0 && V_z==0) { ... balance_prev_vx = V_x; } else { balance_prev_vx = 0; }` |
| 무조건 `balance_prev_vx = V_x;` (구 line 142) 외부 | 제거 (각 분기 안에서 deterministic 갱신) |
| 회전/측방 phase 후 직진 진입 시 stale refs 사용 | 회전/측방 진입 시 invalidate → 다음 직진 진입 시 prev_vx==0 트리거 → fresh refs 캡처 |

의도/주의: 모터 매핑 (`speed_L1/L2/R1/R2_setup` 산출) 부분은 손대지 않음. encoder-balance 자체 로직은 유지하고 invalidate 조건만 확장.

### 4.6 app.c — M3 deadzone 명시 (T2)

`/home/jetson/mcu/extbd/Source/APP/app.c`

| 변경 전 | 변경 후 |
|---|---|
| `M1: 720/690`, `M2: 780/750` 만 설정. M3 = default 700/700 | 위 + `Motor_Set_Dead_Zone(MOTOR_ID_M3, 720, 690);` 한 줄 추가 |
| 직진 시 M3 가 M1 보다 10 PWM 더 강함 (좌측 편향) | 양 모터 동일 패턴 → 좌우 deadzone 대칭 |

가정: 같은 모터 모델/배치라면 fwd-vs-bwd 비대칭 (+30) 도 동일.

---

## 5. 빌드 및 검증

### 5.1 펌웨어 빌드 + 플래시 (사용자 측 PC)

사용자가 직접 STM32 IDE 로 빌드 후 플래시 (Jetson 측에서는 미실행).

### 5.2 도구 syntax 검증

`python3 -c "import ast; ast.parse(...)"`, `bash -n` 으로 모든 신규/수정 파일 syntax OK 확인.

펌웨어 변경 brace/paren 균형 검증:
- `app_mecanum.c`: `{ 16 / } 16`, `( 106 / ) 106`
- `app.c`: `{ 101 / } 101`, `( 351 / ) 351`

### 5.3 측정 — path_curvature_diag.py 재측정 (mag_nz 패치 적용)

joy 로 회전 명령 발행 후 final summary:
```
cmd_angular     n= 769 avg= -0.3492 std=0.1870 peak= -1.0000
rf2o_angular    n= 252 avg= -0.3092 std=0.3180 peak= -0.9523
★ cmd → rf2o (전체 |z| 평균비) = 0.981  (|cmd|=0.3523, |rf2o|=0.3455)
→ ratio 정상 범위 (0.85~1.15). 회전 자체는 정상.
```

이전 1.589× 는 측정 아티팩트로 확정.

### 5.4 측정 — scenario3_diag.py (T1 적용 전, idle stop 포함 시퀀스)

```
event #1: rf2o_back +0.0000, rf2o_n_resid 48
events 2-4: rf2o_back +0.0000, rf2o_n_resid 9
평균 복귀 peak: rf2o=+0.0000
→ 자동 복귀 미관측
```

idle stop 우회 메커니즘으로 인해 측정 대상 우회됨 (3.2 절 참조). 진짜 시나리오 재현 위해선 stop 없이 발행해야 함이 확인됨.

### 5.5 측정 — T1+T2 적용 후 사용자 정성 검증

사용자 피드백 #12: "증상이 남아있기는 하나 많이 개선되었다."
사용자 피드백 #13: "전진 시 오른쪽으로 회전을 줄 시, 왼쪽으로 복귀하는 현상이 있는데, 미세하다."

→ T1 (큰 자동 복귀 = balance ±50 PWM × 다수 tick) 제거 확인.
→ 잔류 미세 복귀는 motor PID 적분 + motor_last_pulse 필터 (α=0.3) + balance reset 타이밍 vs 휠 감속 시간 mismatch 의 transient (3.2 후속 절).

### 검증 결과

| 항목 | 기준 | 실측 |
|---|---|---|
| path_curvature ratio (회전 자체 정상성) | 0.85~1.15 | 0.981 |
| 시나리오 2 자동 복귀 큰 거동 | 사라짐 | 사라짐 (사용자 정성 확인) |
| 시나리오 3 자동 복귀 큰 거동 | 사라짐 | 사라짐 (사용자 정성 확인) |
| 시나리오 1 직진 드리프트 | 시각적 직선 | 개선 확인, 정량 측정 미실시 |
| 잔류 미세 좌측 복귀 (우회전 후) | 0 또는 transient | "미세" 잔존 |

---

## 6. 최종 결론

### 6.1 오늘 해결된 항목

1. **시리얼 포트 충돌** 진단 → 원인 (legacy rosmaster_main.py 자동시작) 식별. 재부팅으로 즉시 해소.
2. **시나리오 2, 3 의 큰 자동 복귀 거동** — `app_mecanum.c` balance correction 의 stale refs 버그 수정으로 해결.
3. **시나리오 1 의 deadzone 비대칭** — `app.c` 에 M3 deadzone 명시 추가로 좌우 균형.
4. **path_curvature_diag.py 의 부호 상쇄 측정 버그** — `mag_nz` 절댓값 평균 추가로 정확한 ratio 산출.
5. **이전 가설 (MCU yaw PID 자동 복귀) 의 오류 확정** — `g_speed_setup=0` 으로 yaw PID 의 모터 영향이 0, 4개 차종 edge-trigger 패치 (2026-04-28) 는 functionally inert.

### 6.2 오늘 새로 확인된 항목

1. driver `car_type=1` = `CAR_MECANUM` 이며, 실제 2-wheel diff drive 인데 MCU `Mecanum_Ctrl` 경로 사용. M2/M4 미사용.
2. `set_car_motion` (FUNC_MOTION) 경로는 `Mecanum_Ctrl` → `Motion_Set_Speed` 만 거치며, `g_speed_setup` 갱신 안 함. 따라서 MCU yaw PID 가 모터 PWM 에 미치는 영향 0.
3. `Mecanum_Ctrl` 의 encoder balance correction 은 유일하게 작동하는 헤딩 안정화 메커니즘이었음. 하지만 reset 조건이 회전 phase 통과를 다루지 못해 stale refs → 자동 복귀 현상의 진짜 원인.
4. `scenario3_diag.py` 의 SETTLE 측정에서 idle stop 우회 메커니즘 발견 (driver 가 V=0 보낼 때 직전 회전 phase 의 V_x=0 갱신이 prev_vx 를 0 으로 만들어 우연히 reset 트리거).
5. 4개 driver 백업 (bak/bak2/bak3/current) 모두 동일 증상 → driver 측 변경 (linear startup, threshold 분기 등) 은 본 문제와 무관.

### 6.3 남은 제약 / 주의

1. **잔류 미세 좌측 복귀 (우회전 후)**: motor_last_pulse 필터 (α=0.3) + per-motor PID 적분 + balance reset 타이밍 vs 휠 감속 시간 mismatch 의 transient. ~50-100ms 지속 후 자연 수렴. path 추종 영향은 Nav2 close-loop 으로 흡수 가능 추정.
2. **edge-trigger 패치 (4개 차종 \_Ctrl 함수, 2026-04-28)** 는 functionally inert 이지만 revert 비용 (재플래시) > 이득으로 그대로 유지.
3. **legacy rosmaster_main.py 자동시작** 은 재부팅 시 다시 살아남. 비활성화 명령 (`mv ~/.config/autostart/bash.desktop{,.disabled}`) 은 사용자 미수행 — 다음 부팅 시 시리얼 충돌 재발 가능.
4. **T2 의 M3 deadzone 720/690 가정** — 같은 모터 모델/배치라는 가정. 만약 M3 가 실제로 다른 마찰 특성이면 우측 편향 가능 (현재까지 보고는 좌측만, 가정 적합 추정).
5. **MCU yaw PID 의 영구 비활성** — `g_speed_setup` 활성화 시도는 제어 아키텍처 재설계 영역, 본 문제와 무관하므로 시도 금지.

### 6.4 다음 분석 우선순위

1. (잔류 미세 좌측 복귀가 path 추종에 영향 주는지) Nav2 곡선 path goal 발행 → rviz Local Plan vs 실제 trajectory 비교.
2. 영향 있으면 T3 검토:
   - `MOTOR_FILTER_ALPHA` 0.3 → 0.5 ([bsp_motor.c:15](/home/jetson/mcu/extbd/Source/BSP/bsp_motor.c#L15)) — transient 메모리 단축
   - 또는 balance correction 의 reset 직후 N tick 동안 correction=0 강제 (휠 감속 시간 보상)
3. 시나리오 1 정량 측정 (`scenario1_diag.py`) 으로 °/m 잔존 드리프트 확인 — 0.5° 이하면 무시.
4. legacy rosmaster_main.py 자동시작 비활성화 결정 (재부팅 검증 후 적용).

---

## 7. 생성 / 수정 파일 목록

### 7.1 수정된 파일

- [/home/jetson/mcu/extbd/Source/APP/app_mecanum.c](/home/jetson/mcu/extbd/Source/APP/app_mecanum.c) — balance refs invalidate (T1, 시나리오 2/3 fix)
- [/home/jetson/mcu/extbd/Source/APP/app.c](/home/jetson/mcu/extbd/Source/APP/app.c) — M3 deadzone 추가 (T2, 시나리오 1 fix)
- [/home/jetson/path_curvature_diag.py](/home/jetson/path_curvature_diag.py) — `mag_nz` 절댓값 평균 추가 (측정 버그 fix)

### 7.2 생성된 파일

- [/home/jetson/scenario3_diag.py](/home/jetson/scenario3_diag.py) — 회전 종료 후 자동 복귀 검증 도구
- [/home/jetson/scenario1_diag.py](/home/jetson/scenario1_diag.py) — 직진 드리프트 정량화 도구
- [/home/jetson/swap_driver.sh](/home/jetson/swap_driver.sh) — driver 백업 버전 전환 도구

### 7.3 생성된 문서

- [/home/jetson/jupiter_ws_ros2/진단_명령어_2026_04_29.md](/home/jetson/jupiter_ws_ros2/진단_명령어_2026_04_29.md)
- [/home/jetson/jupiter_ws_ros2/진행과정_2026_04_29.md](/home/jetson/jupiter_ws_ros2/진행과정_2026_04_29.md) — 본 문서

### 7.4 변경 없음 (확인 후 보존)

- `jupiter_driver_compensated.py` — threshold 버전 유지 (functionally inert 패치이지만 revert 비용 > 이득)
- 4개 차종 \_Ctrl 함수 edge-trigger 패치 (2026-04-28) — 그대로 유지
- `Rosmaster_Lib.py` — 그대로 (`adjust=False` default 안전)
- Nav2 params — 그대로 (`actuator` 편향 fix 후에만 튜닝 검토)
