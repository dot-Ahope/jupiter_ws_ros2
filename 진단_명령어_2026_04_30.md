# 명령어 로그 2026-04-30: Tier 2.5 collision_monitor + lethal-zone deadlock 해결 + 데드존/path 추종 보강

> 진단 및 수정에 사용한 명령어와 스크립트/설정 파일을 요약 없이 전부 기록한다.

## 0. 오늘 사용한 터미널 명령어 원문

### 0.1 시스템 상태 확인 — 노드 / 토픽 / lifecycle

```bash
source /opt/ros/humble/setup.bash 2>/dev/null && source /home/jetson/jupiter_ws_ros2/install/setup.bash 2>/dev/null && ros2 node list 2>&1 | sort
```
목적: 어떤 노드가 떠있는지 확인.
결과: 첫 진단 시 nvblox + joy_node + jupiter_joy + rviz 만 떠있음 (driver/nav2/safety_layer 부재). 사용자 driver 기동 후 재확인 시 controller_server, planner_server, bt_navigator, behavior_server, velocity_smoother, collision_monitor, twist_mux, stuck_detector, forced_recovery, alert_publisher, jupiter_driver, slam_toolbox, ekf_filter_node 등 정상 표시.

```bash
ros2 topic list 2>&1
```
목적: 활성 토픽 전체 보기.

```bash
for t in /cmd_vel /cmd_vel_nav_filtered /cmd_vel_safe /cmd_vel_force /cmd_vel_joy; do echo "=== $t ==="; ros2 topic info $t 2>&1 | head -3; done
```
목적: cmd_vel 파이프라인 5 단계의 publisher/subscriber 카운트.
결과:
```
=== /cmd_vel ===            Publisher: 4, Subscription: 2
=== /cmd_vel_nav_filtered ===  Publisher: 1, Subscription: 1
=== /cmd_vel_safe ===       Publisher: 1, Subscription: 2
=== /cmd_vel_force ===      Publisher: 1, Subscription: 1
=== /cmd_vel_joy ===        Publisher: 0, Subscription: 1
```

```bash
ros2 topic info /cmd_vel -v 2>&1 | grep -E "Node name|Endpoint type" | head -30
```
목적: /cmd_vel publisher 4 개 정체 확인.
결과: behavior_server 3 회 (BackUp/Spin/DriveOnHeading recovery 별도 publisher) + velocity_smoother 1 회.

```bash
ros2 topic info /scan_merged -v 2>&1 | grep -E "Node name|Endpoint type|Reliability|Durability"
```
목적: forced_recovery 가 /scan_merged 데이터 못 받는 원인 추적.
결과:
```
scan_merger        PUBLISHER     BEST_EFFORT / VOLATILE
local_costmap      SUBSCRIPTION  BEST_EFFORT / VOLATILE
forced_recovery    SUBSCRIPTION  RELIABLE     / VOLATILE   ← QoS mismatch
collision_monitor  SUBSCRIPTION  BEST_EFFORT / VOLATILE
slam_toolbox       SUBSCRIPTION  BEST_EFFORT / VOLATILE
```

```bash
ros2 lifecycle nodes 2>&1
for n in /collision_monitor /controller_server /planner_server /bt_navigator /velocity_smoother /behavior_server; do
  echo -n "$n: "; timeout 8 ros2 lifecycle get $n 2>&1 | head -1;
done
```
목적: nav2 lifecycle 노드 active 여부.
결과: 전부 `active [3]` 확인.

### 0.2 cmd_vel 파이프라인 단계별 캡처

```bash
ros2 topic echo /cmd_vel 2>&1 | head -30
```
목적: nav2 raw intent 확인.
결과 (lethal 도달 시점):
```
linear:  x: 0.1578947368421053
angular: z: 0.32000000000000006
```

```bash
ros2 topic echo /cmd_vel_nav_filtered 2>&1 | head -30
```
결과: 같은 시각 `0, 0, 0` — collision_monitor 가 PolygonStop 으로 nav2 입력 차단.

```bash
ros2 topic echo /cmd_vel_safe 2>&1 | head -30
```
결과: timeout / 0 — twist_mux 의 nav 입력 0 또는 timeout 으로 무발행.

```bash
ros2 topic echo /stuck_status 2>&1 | head -10
```
목적: stuck_detector 가 lethal-stuck 검출하는지.
결과: 첫 분석 시 `data: false` (변경 전), stuck_detector 토픽 변경 후 `data: true`.

```bash
ros2 topic echo /cmd_vel_force 2>&1 | head -25
```
목적: forced_recovery 가 backup 명령 publish 하는지.
결과: QoS 수정 전 무발행, 수정 후 `linear.x=-0.10` 발행 확인.

### 0.3 launch 로그 분석

```bash
ls -t /home/jetson/.ros/log/ | grep "^2026" | head -3
ls /home/jetson/.ros/log/2026-04-30-15-38-52-827138-yahboom-1356839/
grep -E "ERROR|WARN|controller_server|bt_navigator|behavior_server|GoalAborted|GoalSucceeded|goal accepted|Path could not|Failed|aborted" \
  /home/jetson/.ros/log/2026-04-30-15-38-52-827138-yahboom-1356839/launch.log 2>/dev/null | tail -50
```
목적: 최근 launch 의 에러/recovery 시도.

```bash
head -30 /home/jetson/.ros/log/2026-04-30-15-24-55-445416-yahboom-1294180/launch.log
```
결과:
```
[INFO] [launch.user]: === Nav2 Navigation (Fused Odometry Mode) ===
[INFO] [launch.user]:   Ensure nav2_vslam_fused.launch.py is running first!
```
즉 첫 launch 는 `nav2_fused_navigation.launch.py` (nav2 단독) 로 driver/safety_layer prerequisite 누락.

```bash
grep -rln "Nav2 Navigation (Fused Odometry Mode)" /home/jetson/jupiter_ws_ros2/src/
```
결과: `nav2_fused_navigation.launch.py` 가 nav2-only launch 로 분리되어 있음. `nav2_vslam_fused.launch.py` 가 driver+EKF+SLAM+safety_layer+nav2 통합 launch.

```bash
ls -t /home/jetson/.ros/log/controller_server_*.log 2>/dev/null | head -1
tail -100 /home/jetson/.ros/log/controller_server_1357236_1777534734475.log
```
결과 (반복 패턴):
```
[INFO] Passing new path to controller. (×N)
[ERROR] Failed to make progress
[WARN] [follow_path] [ActionServer] Aborting handle.
[INFO] Received request to clear entirely the local_costmap
[INFO] Received a goal, begin computing control effort.
```

```bash
tail -30 /home/jetson/.ros/log/bt_navigator_*.log 2>&1 | grep -v "tick rate" | tail -20
```
결과 (BT 의 최종 실패):
```
[INFO] Begin navigating from current location (-0.32, -0.38) to (0.35, 0.45)
[WARN] Timed out while waiting for action server to acknowledge goal request for compute_path_to_pose
[WARN] Node timed out while executing service call to local_costmap/clear_entirely_local_costmap.
```

```bash
tail -20 /home/jetson/.ros/log/behavior_server_*.log
```
결과 (recovery 모두 실패):
```
[INFO] Running spin → Turning 0.79 for spin behavior.
[WARN] Exceeded time allowance before reaching the Spin goal - Exiting Spin
[INFO] Running backup
[WARN] Exceeded time allowance before reaching the DriveOnHeading goal - Exiting DriveOnHeading
[INFO] Running wait → completed successfully
```

```bash
for f in $(ls -t /home/jetson/.ros/log/python3_*.log 2>/dev/null | head -50); do
  if grep -ql "ForcedRecovery up" "$f" 2>/dev/null; then echo "$f"; ls -la "$f"; break; fi;
done
tail -40 /home/jetson/.ros/log/python3_1584831_1777536782937.log
```
결과 (forced_recovery 의 QoS mismatch 증거):
```
[WARN] rear safety: no /scan_merged data → conservative SKIP backup
[ERROR] STUCK detected but rear NOT safe — skipping backup. Operator intervention required.
(2 초 간격으로 무한 반복)
```

### 0.4 코드 / 파라미터 수정 후 빌드 + 노드 재시작

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_safety --symlink-install 2>&1 | tail -8
```
목적: stuck_detector + forced_recovery 코드 변경 빌드.
결과: `Finished <<< jupiter_safety [9.18~9.41s]`.

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup jupiter_nav_VO --symlink-install 2>&1 | tail -10
```
목적: driver 데드존 보상 + DWB critic 튜닝 빌드.
결과: `2 packages finished [10.1s]`.

```bash
ps -ef | grep -E "/jupiter_safety/forced_recovery|jupiter_safety forced_recovery" | grep -v grep
kill 1584831 2>&1 ; sleep 1 ; ps -p 1584831 2>&1
```
목적: 옛 forced_recovery (잘못된 QoS) 종료.

```bash
nohup ros2 run jupiter_safety forced_recovery > /tmp/forced_recovery.log 2>&1 &
sleep 3 ; ros2 node list | grep forced ; tail -20 /tmp/forced_recovery.log
```
목적: 새 binding 으로 forced_recovery 재기동.
결과:
```
/forced_recovery
[INFO] ForcedRecovery up — backup -0.1 m/s × 1.0s, rear safety 0.2m × ±30°, cooldown 5.0s
```

### 0.5 시간 기반 비교

```bash
date +%s
tail -3 /home/jetson/.ros/log/controller_server_1584985_1777536777600.log
```
목적: nav2 가 idle 인지 (마지막 로그 vs 현재 시각 비교).
결과 (16:14 시점 측정): now=1777535980 vs 마지막 controller 로그 1777535955 → 25 초 전. 활성. 후속 측정 (16:29): now=1777537777 vs 마지막 controller 로그 1777536939 → 14 분 전. nav2 idle.

## 1. 분석에 사용한 설정 / 스크립트 파일

### 1.0 오늘 사용/열람한 스크립트 및 파일 전체 경로

수정:
- /home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/stuck_detector.py ← 수정 (subscription topic)
- /home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/forced_recovery.py ← 수정 (QoS BEST_EFFORT)
- /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py ← 수정 (이동 회전 데드존 보상)
- /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml ← 수정 (PathAlign/PathDist 강화)

이전 세션에 이미 적용 (오늘 작업 전제):
- /home/jetson/jupiter_ws_ros2/src/jupiter_safety/launch/safety_layer.launch.py — safety stack 통합 launch
- /home/jetson/jupiter_ws_ros2/src/jupiter_safety/launch/collision_monitor.launch.py — collision_monitor 단독 launch
- /home/jetson/jupiter_ws_ros2/src/jupiter_safety/config/twist_mux.yaml — joy(100) > force(80) > nav(50) 우선순위
- /home/jetson/jupiter_ws_ros2/src/jupiter_safety/config/collision_monitor.yaml — PolygonStop/Slow + /scan_merged source
- /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py — driver remap `/cmd_vel`→`/cmd_vel_safe`

열람:
- /home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/jupiter_ctrl/jupiter_joy.py (열람) — 레거시 joy 의 /cmd_vel 직접 publish 확인
- /home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/jupiter_ctrl/jupiter_joy_new.py (열람) — 신 joy 의 /cmd_vel_joy publish 확인
- /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_fused_navigation.launch.py (열람) — nav2-only launch 의 prerequisite 메시지 확인

로그 (열람):
- /home/jetson/.ros/log/2026-04-30-15-38-52-827138-yahboom-1356839/launch.log
- /home/jetson/.ros/log/controller_server_1357236_1777534734475.log
- /home/jetson/.ros/log/controller_server_1584985_1777536777600.log
- /home/jetson/.ros/log/bt_navigator_1585019_1777536778413.log
- /home/jetson/.ros/log/behavior_server_1585005_1777536778141.log
- /home/jetson/.ros/log/python3_1584831_1777536782937.log (forced_recovery 의 QoS mismatch 증거)

### 1.1 수정된 파일 1 — stuck_detector.py (subscription 토픽 변경)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/stuck_detector.py`

수정 항목:
- docstring 의 2A 설명 갱신 — "/cmd_vel_safe" → "/cmd_vel" + collision_monitor 차단 검출 의도 명시
- `create_subscription(Twist, '/cmd_vel_safe', ...)` → `create_subscription(Twist, '/cmd_vel', ...)`
- 변경 이유 인라인 코멘트 추가

### 1.2 수정된 파일 2 — forced_recovery.py (QoS BEST_EFFORT)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/forced_recovery.py`

수정 항목:
- import `from rclpy.qos import QoSProfile, ReliabilityPolicy, DurabilityPolicy, HistoryPolicy` 추가
- /scan_merged 구독을 sensor QoS (BEST_EFFORT/VOLATILE/depth=10) 로 변경
- 변경 이유 인라인 코멘트 (publisher 가 BEST_EFFORT 라 RELIABLE 구독은 메시지 0 수신)

### 1.3 수정된 파일 3 — jupiter_driver_compensated.py (이동 회전 데드존 보상 + 정지 회전 adaptive kick)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

수정 항목 (1차):
- 클래스 상수 추가:
  - `MOVING_ROT_MIN_ANGULAR = 0.20` — 이동 중 최소 사용 가능 angular
  - `MOVING_ROT_VX_THRESHOLD = 0.02` — 이 이상의 vx 일 때 적용
  - `MOVING_ROT_RELEASE = 0.05` — 이 미만은 0 처리
- `cmd_vel_callback` 의 deadband 처리 직전에 이동 + 회전 floor 분기 추가:
  - `|vx| ≥ 0.02` 인 경우 angular 절댓값 검사
  - `|ω| < 0.05` → 0
  - `0.05 ≤ |ω| < 0.20` → ±0.20 으로 boost
  - `|ω| ≥ 0.20` → 패스스루

수정 항목 (2차 — 정지 회전 PID buildup 지연 해결):
- 클래스 상수 변경 / 추가:
  - `MCU_ROT_START_ANGULAR: 0.37 → 0.55` (kick 강도 상향)
  - `ROT_STARTUP_KICK_SEC: 0.12 → 0.40` (안전 상한 으로 의미 변경)
  - `ROT_KICK_CONFIRM_THRESHOLD = 0.10` (신규, encoder actual_z 임계)
- `__init__` 에 신규 상태 변수 `self._rot_motion_confirmed = False` 추가
- `cmd_vel_callback` 정지 회전 분기 (|vx|<0.02) 내부에 adaptive kick 로직 추가:
  - 매 callback `actual_z = self.bot._Rosmaster__vz` 직접 read
  - `_rot_motion_confirmed` 가 False 인 동안 sign-aware encoder 검사
  - encoder 가 ±0.10 도달 → 즉시 RUNNING 진입 (kick 조기 종료)
  - 0.40s 안전 상한 도달 → 강제 RUNNING 전환 (motor stall 시 stuck_detector 가 escalate)

### 1.4 수정된 파일 4 — nav2_params_fused.yaml (DWB path 추종 강화)

### 1.5 수정된 파일 4 — nav2_params_fused.yaml (DWB path 추종 강화)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

수정 항목 (line 177~189 영역):
- `PathAlign.scale: 20.0 → 28.0`
- `PathAlign.forward_point_distance: 0.2 → 0.3`
- `PathDist.scale: 24.0 → 32.0`
- 변경 이유 코멘트 갱신 — driver MOVING_ROT_MIN_ANGULAR=0.20 floor 도입으로 controller sample 의 실효성 확보됐기에 path 구속력 강화 가능

### 1.6 추가 빌드 명령

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup --symlink-install
```
목적: 정지 회전 adaptive kick 적용 후 driver 재빌드.
결과: `Finished <<< jupiter_bringup [13.4s]`.

## 2. 관찰 기준 요약

| 기준 | 실측 |
|---|---|
| /scan_merged QoS (publisher / forced_recovery 구독) | BEST_EFFORT / **RELIABLE → 수정 후 BEST_EFFORT** |
| /stuck_status 평상시 | false |
| /stuck_status lethal-stuck 시 (수정 후) | true (1.5s 후) |
| /cmd_vel lethal 시 nav2 출력 | linear.x=0.158, angular.z=0.32 |
| /cmd_vel_nav_filtered 같은 시각 | 0, 0, 0 (collision_monitor 차단) |
| /cmd_vel_safe 같은 시각 | 무발행 (nav 입력 timeout) |
| 엔코더 분해능 | 1320 pulse/rev × 10ms → 1 tick = 16.3 mm/s |
| DWB 출력 (lethal 근접 시) linear.x | ~0.034 → 0.084 m/s |
| DWB 출력 angular.z | ~0 → -0.16 rad/s |
| left/right wheel 명령 (vx=0.084, ω=-0.16) | 0.060 / 0.108 m/s (3.7 / 6.6 ticks) |
| MCU M3 deadzone (이전 세션 적용) | (720, 690) — 비대칭 보정 |
| ANGULAR_SCALE_FACTOR (모터 교체 후) | 1.0 |
| MCU_MIN_ANGULAR (이동 중 미적용) | 0.0 |
| **신 MOVING_ROT_MIN_ANGULAR** | **0.20** |
