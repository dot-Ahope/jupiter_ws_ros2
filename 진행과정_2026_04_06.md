# 진행과정 2026-04-06: VSLAM Covariance Adapter emitter_on_off 대응 개선

## 1. 목표

### 1.1 사용자 요청
> "vslam_covariance_adapter.py도 살펴봐라"
> "문제를 해결하기 위한 방법을 설계하고, 제시하라"
> "수정을 진행하라"

emitter_on_off 모드에서 cuVSLAM 64% frozen 프레임이 발생하는 환경에서,
vslam_covariance_adapter의 anomaly/frozen 처리 로직이 정상 데이터까지 차단하는 문제를 분석하고 수정.

### 1.2 기존 상태 (이전 세션 요약)
- 03-30 Part 1: nvblox↔Nav2 costmap 연동 완료 (TF extrapolation 수정)
- 03-30 Part 2: cuVSLAM 오도메트리 진단 — emitter_on_off가 근본 원인 확인
- 03-30 Part 3: 3가지 VSLAM 설정 테스트 (A: 54Hz/0%, B: 실패, C: 25Hz/64% frozen)
- Docker: cuVSLAM + nvblox 실행 (`--network host`)
- Host: EKF + SLAM Toolbox + Nav2 + Adapter 실행

---

## 2. 문제 분석: Adapter 코드 4가지 결함

### 2.1 분석 대상
`jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py`

emitter_on_off 환경(25Hz, 64% frozen)에서 adapter가 정상 작동하지 않는 원인을 코드 레벨에서 분석.

### 2.2 발견된 문제

#### 문제 1: Frozen 프레임이 anomaly 상태머신을 오염
**위치**: `odom_callback()` → `_check_frozen()` → `_check_anomaly()` → `_update_tracking_state()`

Frozen 프레임(delta=0)이 `_check_frozen()` 에서 `True`를 반환하더라도, 이후 `_check_anomaly()` 와 `_update_tracking_state()` 가 계속 실행됨.
- frozen 데이터(delta=0)가 anomaly detector에 "정상"으로 인식됨
- `consecutive_good` 카운터가 frozen 프레임마다 증가
- `prev_odom`이 stale(frozen) pose로 업데이트되어 기준점 오염

#### 문제 2: Unfreeze 직후 거짓 anomaly 발생
**위치**: `_check_frozen()` + `_check_anomaly()`

`_check_frozen()` 에서 frozen 기간 동안 `prev_odom`을 업데이트:
- 60프레임(2.4초) frozen 후 로봇이 0.3m 이동
- unfreeze 시 `prev_odom`은 frozen pose → 실제 위치와 0.3m 차이
- `max_pos_delta=0.5m` 에 가까운 delta → 정상이지만 anomaly로 판정 위험
- 더 오래 frozen되면 확실히 anomaly → `good_count_threshold=30` 대기 → 복구 불가능 루프

#### 문제 3: Frozen 상태에서 10Hz stale 데이터 EKF 투입
**위치**: `odom_callback()` frozen 분기

`frozen_publish_rate=10.0`으로 설정되어 frozen 상태에서도 10Hz로 발행:
- EKF differential 모드: Δ=0 데이터를 "정지"로 해석
- 실제로 로봇이 이동 중이면 wheel odometry/RF2O와 충돌
- 공분산 보정 없이 원본 covariance(대부분 0)로 발행 → EKF가 높은 가중치 부여

#### 문제 4: `good_count_threshold=30` 도달 불가능
**위치**: `_update_tracking_state()` + `nav2_vslam_fused.launch.py` 파라미터

emitter_on_off 환경에서 64% frozen:
- 25Hz 중 ~9Hz만 실제 추적 성공
- 연속 30프레임 비frozen은 통계적으로 극히 드물 ($0.36^{30} ≈ 10^{-13}$)
- anomaly에서 복구가 사실상 불가능 → VSLAM 데이터 영구 차단

---

## 3. 해결 방안 설계

### 3.1 3단계 접근

| 단계 | 대상 | 설명 |
|------|------|------|
| **Tier 1** | Adapter 코드 수정 | Frozen/Anomaly 상태머신 분리, adaptive delta, frozen 공분산 증폭 |
| **Tier 2** | Launch 파라미터 조정 | frozen_threshold, frozen_publish_rate, good_count_threshold 등 |
| **Tier 3** | 아키텍처 개선 (미래) | Splitter bypass, jitter 500ms, cuVSLAM emitter 처리 연구 |

### 3.2 EKF 설정 확인
수정 방향 결정을 위해 EKF 설정 확인:
- 파일: `jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml`
- `odom1_differential: true` — VSLAM은 차분 모드 (절대 위치 무관, Δ만 사용)
- fuse 대상: x, y, vx, vy만 (yaw/vyaw 미사용)
- `odom1_pose_rejection_threshold: 5.0`
- `odom1_twist_rejection_threshold: 3.0`

→ Frozen 시 Δ=0이 EKF에 "정지" 신호로 들어가는 것이 핵심 문제

---

## 4. Tier 1: Adapter 코드 수정

### 4.1 수정 1 — 새 파라미터 추가

`vslam_covariance_adapter.py` 파라미터 선언부에 3개 파라미터 추가:

```python
self.declare_parameter('frozen_cov_multiplier', 50.0)   # frozen 발행 시 공분산 증폭 배율
self.declare_parameter('max_robot_speed', 0.5)           # m/s - 로봇 최대 속도
self.declare_parameter('estimated_frame_rate', 25.0)     # Hz - VSLAM 수신 주파수 추정
```

파라미터 로드부에 대응 추가:
```python
self.frozen_cov_multiplier = self.get_parameter('frozen_cov_multiplier').value
self.max_robot_speed = self.get_parameter('max_robot_speed').value
self.estimated_frame_rate = self.get_parameter('estimated_frame_rate').value
```

### 4.2 수정 2 — `frozen_duration` 상태 변수 추가

```python
self.frozen_duration = 0  # 마지막 frozen 기간 (프레임 수, unfreeze 시 adaptive delta용)
```

### 4.3 수정 3 — 시작 로그에 새 파라미터 표시

```python
f'  frozen detection: ... downsample={self.frozen_publish_rate}Hz, '
f'frozen_cov_mult={self.frozen_cov_multiplier}x\n'
...
f'  adaptive delta: max_robot_speed={self.max_robot_speed}m/s, '
f'est_frame_rate={self.estimated_frame_rate}Hz\n'
```

### 4.4 수정 4 — `_check_frozen()` 재작성

**핵심 변경**: Frozen 시 `prev_odom` 업데이트 차단 + `frozen_duration` 기록

```python
def _check_frozen(self, msg: Odometry) -> bool:
    # frozen 판정 시 prev_odom을 업데이트하지 않음
    # → unfreeze 후 anomaly 판정에서 "마지막 실제 움직임" 기준으로 delta 계산
    
    # 위치 변화량 (prev_odom 기준 — frozen 중에도 prev_odom은 마지막 실제 움직임 시점)
    ...
    if pos_delta < tolerance and yaw_delta < tolerance:
        self.frozen_count += 1
        # ★ prev_odom 업데이트하지 않음
        return self.frozen_count >= self.frozen_threshold
    else:
        if self.frozen_count >= self.frozen_threshold:
            # ★ frozen 기간 기록 → adaptive delta에 사용
            self.frozen_duration = self.frozen_count
        self.frozen_count = 0
        return False
```

**이전 코드**: frozen 판정에도 불구하고 `prev_odom`이 frozen pose로 업데이트됨
**수정 후**: frozen 기간 동안 `prev_odom` 유지 → unfreeze 시 정확한 delta 계산 가능

### 4.5 수정 5 — `_check_anomaly()` adaptive delta threshold

**핵심 변경**: Unfreeze 직후 `max_pos_delta`를 frozen 기간에 비례하여 완화

```python
effective_max_pos = self.max_pos_delta
effective_max_yaw = self.max_yaw_delta
if self.frozen_duration > 0:
    frozen_time_sec = self.frozen_duration / max(self.estimated_frame_rate, 1.0)
    effective_max_pos = self.max_pos_delta + frozen_time_sec * self.max_robot_speed
    effective_max_yaw = self.max_yaw_delta + frozen_time_sec * 1.0  # ~1 rad/s 가정
    self.frozen_duration = 0  # 한 번만 적용
```

**예시**: 60프레임(2.4초) frozen, `max_robot_speed=0.5`:
- `effective_max_pos = 0.5 + 2.4 × 0.5 = 1.7m` (일시적으로 허용 범위 확대)
- 다음 프레임부터는 원래 0.5m 복귀

### 4.6 수정 6 — `odom_callback()` frozen-anomaly 분리

**핵심 변경**: Frozen일 때 `_check_anomaly()` + `_update_tracking_state()` 완전 SKIP

```python
if not is_frozen:
    is_anomaly = self._check_anomaly(msg)
    self._update_tracking_state(is_anomaly)
else:
    is_anomaly = False
    # frozen 중에도 auto-recovery 용 raw pose는 추적
    self.last_raw_pose = msg.pose.pose
```

**이전 코드**: frozen이든 아니든 `_check_anomaly()` + `_update_tracking_state()` 항상 실행
**수정 후**: frozen = "추적 불명" → 상태머신에서 완전 분리

### 4.7 수정 7 — Frozen 발행에 고공분산 적용

**핵심 변경**: Frozen downsample 발행 시 `_publish_with_frozen_covariance()` 호출

```python
# odom_callback() 내 frozen 분기:
if (now_sec - self.last_frozen_publish_time) >= interval:
    self.last_frozen_publish_time = now_sec
    self.frozen_published_count += 1
    self._publish_with_frozen_covariance(msg)  # ★ 기존: self.odom_pub.publish(msg)
    return
```

새 메서드:
```python
def _publish_with_frozen_covariance(self, msg: Odometry):
    """공분산을 frozen_cov_multiplier 배 증가시켜 EKF가 무시하도록"""
    pose_cov = list(msg.pose.covariance)
    for i, diag_idx in enumerate(self.pose_diag_indices):
        base_cov = max(abs(pose_cov[diag_idx]), self.min_pose_cov_diag[i])
        pose_cov[diag_idx] = base_cov * self.frozen_cov_multiplier
    msg.pose.covariance = pose_cov
    # twist도 동일 처리
    ...
    self.odom_pub.publish(msg)
```

**효과**: `min_position_cov=0.05` × `frozen_cov_multiplier=50` = **2.5 m²** 공분산
→ EKF `odom1_pose_rejection_threshold=5.0`과 함께 frozen 데이터 사실상 무시

---

## 5. Tier 2: Launch 파라미터 조정

### 5.1 수정 대상
`jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py`

### 5.2 파라미터 변경 비교

| 파라미터 | 수정 전 | 수정 후 | 근거 |
|----------|---------|---------|------|
| `frozen_threshold` | 60 | **10** | 25Hz × 0.4초 = 10프레임 (기존 60은 2.4초 반응 지연) |
| `frozen_publish_rate` | 10.0 Hz | **1.0 Hz** | Stale Δ=0 투입 최소화 (10Hz→1Hz) |
| `good_count_threshold` | 30 | **10** | 25Hz에서 36% 비frozen: 10연속 달성 가능 ($0.36^{10} ≈ 3×10^{-5}$, ~33000프레임/1 회) |
| `recovery_delay` | 1.0초 | **0.5초** | good_count 감소에 맞춰 복구 지연 단축 |
| `frozen_cov_multiplier` | (신규) | **50.0** | Frozen 발행 공분산 50배 증폭 |
| `max_robot_speed` | (신규) | **0.5 m/s** | Adaptive delta 계산용 최대 속도 |
| `estimated_frame_rate` | (신규) | **25.0 Hz** | emitter_on_off 실효 수신률 |

---

## 6. 빌드 및 검증

### 6.1 패키지 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO --symlink-install
```
**결과**: `Finished <<< jupiter_nav_VO [4.36s]` — 성공

### 6.2 문법 검증
```bash
python3 -c "import py_compile; py_compile.compile(
  'src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py',
  doraise=True)"
```
**결과**: `Syntax OK` — 오류 없음

### 6.3 IDE 오류 확인
**결과**: 오류 0건, 경고 0건

---

## 7. 수정 효과 분석 (이론)

### 7.1 Frozen → Unfreeze 시나리오

**수정 전 (문제 발생 시나리오)**:
1. cuVSLAM 60프레임(2.4초) frozen
2. Frozen 데이터가 `_check_anomaly()`에 진입 → delta=0 → "정상" 판정
3. `consecutive_good` 60++ → anomaly 해제됨 (잘못된 복구)
4. `prev_odom`이 frozen pose로 업데이트
5. Unfreeze → 실제 위치와 prev_odom 차이 > 0.5m → anomaly 발생
6. `good_count_threshold=30` 달성 불가 → VSLAM 영구 차단

**수정 후 (정상 시나리오)**:
1. cuVSLAM 10프레임(0.4초) 후 frozen 판정 (빠른 감지)
2. Frozen 프레임은 `_check_anomaly()` SKIP → 상태머신 오염 없음
3. `prev_odom` 유지 (마지막 실제 움직임 시점)
4. Frozen 발행은 1Hz + 공분산 50배 → EKF 사실상 무시
5. Unfreeze → `frozen_duration`에 기반한 adaptive delta (0.5 + 0.4×0.5 = 0.7m)
6. 정상 추적 재개 → `good_count_threshold=10` 달성 → 복구 완료

### 7.2 EKF 영향 비교

| 항목 | 수정 전 | 수정 후 |
|------|---------|---------|
| Frozen 시 EKF 투입 | 10Hz, 원본 covariance | 1Hz, 공분산 50배 증폭 |
| Frozen Δ=0의 EKF 가중치 | 높음 (cov ≈ 0.05) | 극히 낮음 (cov ≈ 2.5) |
| Unfreeze 후 복구 | 불가능 (good_count=30) | 가능 (good_count=10 + adaptive delta) |
| Anomaly 상태머신 오염 | 있음 (frozen→good 오판) | 없음 (frozen SKIP) |

---

## 8. 수정된 파일 목록

| 파일 | 변경 내용 |
|------|----------|
| `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py` | 9개 코드 블록 수정 (파라미터 3개 추가, _check_frozen 재작성, adaptive delta, odom_callback frozen-anomaly 분리, _publish_with_frozen_covariance 신규) |
| `src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` | Adapter 파라미터 7개 변경/추가 |

---

## 9. 향후 작업

### 9.1 실테스트 필요
- 전체 시스템 기동 (Docker VSLAM+nvblox + Host EKF+adapter+Nav2)
- Adapter 30초 주기 통계 로그로 frozen/anomaly/published 비율 확인
- 이동 중 VSLAM → EKF 기여도 비교 (수정 전/후)

### 9.2 Tier 3 아키텍처 개선 (미래)
- Splitter bypass 테스트: cuVSLAM이 raw IR을 직접 수신하도록 하여 Splitter 지연 제거
- `image_jitter_threshold_ms=500`으로 설정하여 cuVSLAM 경고 억제
- emitter 교대 비율 변경 (OFF:ON = 2:1) 가능성 검토

### 9.3 근본 원인 참고
emitter_on_off 자체가 cuVSLAM 추적 실패의 근본 원인 (03-30 Test A에서 확인).
이번 수정은 adapter 레벨에서의 **증상 완화**이며, 근본 해결은 카메라 아키텍처 변경 필요.

---

## 10. 실테스트 후 추가 진단: VSLAM orient.z + nvblox 메시 + EKF 센서 융합

### 10.1 사용자 요청
> 1. VSLAM orient.z가 제대로 된 방향을 잡지 못하고 띄엄띄엄 나오는 원인을 찾아달라
> 2. nvblox 메시가 바닥을 뚫고 생성되는 원인을 분석해달라
> 3. EKF orient.z가 IMU만 사용하는지, 다중 센서 융합인지 확인해달라

### 10.2 CSV 데이터 분석 (plot_data.csv)
`plot_data.csv` 기록 분석 (172초간, 4개 토픽 orient.z):

| 토픽 | 메시지 수 | orient.z 범위 | 비고 |
|------|----------|--------------|------|
| `/odom_adapted` (EKF) | 7,768 | -0.115 ~ +0.069 | EKF 최종 출력 |
| `/odom` (Wheel) | 4,743 | -0.152 ~ +0.081 | 엔코더 기반 |
| `/odom_rf2o_adapted` (RF2O) | 1,608 | -0.151 ~ +0.079 | LiDAR 스캔 매칭 |
| `/visual_slam/.../odometry_adapted` (VSLAM) | 1,914 | -0.198 ~ +0.0004 | cuVSLAM |

#### VSLAM orient.z 문제 상세

| 구간 | VSLAM orient.z | 다른 센서 | 상태 |
|------|---|---|---|
| t=5~11 | **0** (1Hz, 6개) | ~0 | Frozen → downsample 발행 |
| t=11~45 | **~0.0001** (거의 0) | ~0.0004 | cuVSLAM 회전 추적 실패 |
| t=45~62 | **데이터 없음** (17초 공백) | ~0 | Adapter가 완전 차단 |
| t=62~117 | **~0.0001** (변화 없음) | -0.05~-0.12 | 회전 추적 실패 지속 |
| t=117.05→117.79 | **0.0001 → -0.143** (점프) | -0.115 | cuVSLAM 갑자기 재추적, 16° 점프 |
| t=117.94 | **-0.197** (추가 점프) | -0.115 | cuVSLAM 과보정 (22.6° vs 실제 ~13°) |
| t=145~178 | **-0.194** (220개 동결) | -0.15 | Frozen 상태 지속 |

**핵심 발견:**
- VSLAM은 112초간(t=5~117) 회전을 전혀 추적하지 못함 (orient.z ≈ 0)
- t=117에서 0.7초 만에 -0.143으로 점프 → 불연속 갱신
- 최종 -0.197은 다른 센서(-0.115~-0.15)와 **44% 이상** 차이
- emitter_on_off에 의한 cuVSLAM 시각 추적 파괴가 근본 원인

#### VSLAM 주파수 분석 (10초 윈도우)

| 구간 | 메시지 수 | Hz | 평가 |
|------|----------|-----|------|
| 0~10s | 5 | 0.5 | 시작 안정화 |
| 10~20s | 240 | 24 | 양호 |
| 20~30s | 266 | 26.6 | 양호 |
| 30~40s | 167 | 16.7 | 저하 |
| **40~50s** | **61** | **6.1** | ⚠️ 급감 |
| **50~60s** | **0** | **0** | ❌ 완전 차단 |
| **60~70s** | **115+39** | — | 부분 복구 |
| 120~130s | 282 | 28.2 | 양호 |
| 140~170s | 60~83 | 6~8 | 저하+동결 |

**17초 공백(t=45~62)**: Adapter가 anomaly/frozen 상태로 완전 차단한 구간

### 10.3 VSLAM orient.z 실패 원인 결론

**emitter_on_off 모드가 cuVSLAM의 yaw 추적을 파괴**:
1. IR 프레임이 교대 전달 → 프레임 간격 불규칙 (최대 354ms)
2. cuVSLAM이 특징점 매칭 실패 → 동일 pose 반복 발행 (frozen)
3. 간헐적 추적 성공 시 누적 회전을 한꺼번에 반영 → 불연속 점프
4. EKF는 VSLAM yaw를 사용하지 않으므로 (odom1_config에서 yaw=false) 직접적 영향 없음
5. 단, VSLAM x,y 위치도 같은 원인으로 부정확 → EKF 위치 추정에 영향

---

## 11. nvblox 메시 지면 관통 원인 분석

### 11.1 문제 현상
Foxglove에서 `/nvblox_node/mesh` 시각화 시, 메시가 odom 프레임의 z=0(지면)을 기준으로 위아래로 생성됨.
로봇은 z=0을 시작 위치로 인식하며, 실내 공간은 평면.

### 11.2 근본 원인: base_link→camera_link TF 3중 충돌

**발견: Host launch 파일에 구 값이 남아있었음**

3개의 퍼블리셔가 동일 TF (`base_link → camera_link`)를 발행:

| # | 퍼블리셔 | 위치 | 값 (x, y, z) | 상태 |
|---|----------|------|-------------|------|
| 1 | Host `robot_state_publisher` (URDF) | `nav2_vslam_fused.launch.py` | (0.205, 0, **0.098**) | ✅ 정확 |
| 2 | Host `tf_base_to_camera` (static TF) | `nav2_vslam_fused.launch.py` | (0.274, 0, **0.055**) | ❌ **구 값** |
| 3 | Docker `base_to_camera_tf` (static TF) | `isaac_ros_vslam_nvblox_realsense.launch.py` | (0.205, 0, **0.098**) | ✅ 수정됨 |

**#2가 핵심 버그**: URDF를 수정(0.098)했지만, Host launch의 static TF publisher가 여전히 구 값(0.055)을 사용.
Static TF publisher가 robot_state_publisher의 TF를 간헐적으로 덮어쓰기 → **TF 레이스 컨디션**.

### 11.3 메시 지면 관통 메커니즘

TF chain: `odom(z=0) → base_footprint(z=0) → base_link(z=+0.08) → camera_link`

| 시나리오 | camera_link z (odom) | 바닥까지 거리 | 메시 바닥 z | 결과 |
|----------|---------------------|-------------|-----------|------|
| 정확 (URDF) | 0.08 + **0.098** = **0.178m** | 0.178m | z≈0 | ✅ 정상 |
| 구 값 (static TF) | 0.08 + **0.055** = **0.135m** | 0.178m | z≈**-0.043m** | ❌ 바닥 관통 |

깊이 카메라는 실제로 바닥까지 0.178m를 측정. 그러나 TF에서 camera_link가 z=0.135에 있다고 인식하면:
- 바닥 = 0.135 - 0.178 = **-0.043m** (지면 아래 4.3cm)
- voxel_size=0.05m이므로 약 1 voxel 아래에 바닥 메시 생성

추가로 **`workspace_bounds_type: "unbounded"`** (nvblox_base.yaml)이므로 아래쪽 클리핑 없음 → 지면 아래 메시 생성 허용.

### 11.4 수정 내용

**Host launch 수정** (`nav2_vslam_fused.launch.py`):
```python
# 수정 전:
arguments=['0.274', '0', '0.055', '0', '0', '0', 'base_link', 'camera_link']

# 수정 후:
arguments=['0.205', '0', '0.098', '0', '0', '0', 'base_link', 'camera_link']
```

→ URDF, Host static TF, Docker static TF 3곳 모두 동일 값 (0.205, 0, 0.098)으로 통일

### 11.5 보조 대책: nvblox workspace_bounds (향후 적용 가능)

TF가 정확하더라도 깊이 노이즈와 voxel 양자화로 인해 z=0 아래에 미세한 메시가 생성될 수 있음.
nvblox launch 오버라이드에 height bounds 추가로 완전 차단 가능:

```python
# isaac_ros_vslam_nvblox_realsense.launch.py nvblox_node parameters에 추가:
'static_mapper.workspace_bounds_type': 'height_bounds',
'static_mapper.workspace_bounds_min_height_m': -0.05,  # odom z=-5cm 이하 차단
'static_mapper.workspace_bounds_max_height_m': 1.5,    # odom z=1.5m 이상 차단
```

---

## 12. EKF orient.z 센서 융합 분석

### 12.1 분석 결과

`ekf_vslam_fusion.yaml` 설정 분석:

| 센서 | yaw (절대 방향) | vyaw (각속도) | orient.z 기여 |
|------|----------|-----------|---|
| odom0 (Wheel) | `false` | **`true`** | ✅ vyaw 기여 |
| odom1 (VSLAM) | `false` | `false` | ❌ 미기여 |
| odom2 (RF2O) | `false` | `false` | ❌ 미기여 |
| imu0 (IMU) | `false` | **`true`** | ✅ vyaw 기여 |

**결론: EKF orient.z = IMU vyaw + Wheel vyaw 2개 센서 융합**

- **IMU만 사용하는 것이 아님**: IMU `angular_velocity.z`와 Wheel 엔코더 `angular_velocity.z`를 공분산 가중 융합
- **yaw 절대값**을 직접 제공하는 센서 없음 → vyaw(각속도)를 EKF가 적분하여 yaw 추정
- VSLAM과 RF2O는 yaw/vyaw 모두 비활성 → orient.z에 전혀 기여하지 않음
- Process noise `vyaw(11,11)=0.01` / IMU `gyro_cov=0.001` / Wheel `angular_cov=동적` → 가중치 비율 결정

---

## 13. RF2O yaw/vyaw 비활성이 Nav2 회전에 미치는 영향 분석

### 13.1 현재 RF2O EKF 설정

```yaml
odom2_config: [true,  true,  false,    # x, y만 활성
               false, false, false,    # roll, pitch, yaw ← 비활성
               false, false, false,    # vx, vy, vz
               false, false, false,    # vroll, vpitch, vyaw ← 비활성
               false, false, false]    # ax, ay, az
odom2_differential: true
```

### 13.2 Nav2 회전 흐름

```
Nav2 Controller (RotationShimController/DWB)
   ↓ /cmd_vel (angular.z)
MCU 모터 제어
   ↓ 실제 로봇 회전
EKF가 yaw 추정 (IMU vyaw + Wheel vyaw)
   ↓ odom → base_footprint TF
Nav2 Controller가 TF로 현재 yaw 확인
   ↓ 목표 yaw와 현재 yaw 차이 계산
/cmd_vel 보정
```

### 13.3 RF2O가 회전에 미치는 영향 분석

#### A. Yaw 추정에 대한 영향: **없음**
- RF2O yaw/vyaw 모두 비활성 → EKF yaw 계산에 전혀 관여하지 않음
- Nav2가 사용하는 yaw는 순수하게 IMU + Wheel 기반

#### B. 위치 추정에 대한 영향: **제자리 회전 시 극히 미미**
- RF2O는 x, y 위치만 differential 모드로 제공
- 제자리 회전 시 Δx, Δy ≈ 0이어야 함
- 그러나 RF2O 스캔매칭이 회전 중 부정확 → 허위 x,y 변위 보고 가능
- **`rf2o_covariance_adapter`가 대응**:
  - `cmd_vel.angular.z > 0.15 rad/s` 감지 시 회전 상태로 판정
  - position covariance: 0.5 → **10.0 m²** (20배 증폭)
  - EKF이 RF2O 위치 데이터를 사실상 무시
  - 회전 종료 후 1초 exponential decay로 복귀

#### C. 회전 후 직진에 대한 영향: **간접적으로 긍정적**
- RF2O yaw를 비활성한 이유 (2026-02-26 진단):
  - 제자리 회전 시 RF2O가 잘못된 yaw 보고 → EKF ~50° 발산
  - yaw 비활성화로 이 문제 완전 해결
- 따라서 RF2O yaw 비활성은 Nav2 회전 성능을 **개선**하는 요소

### 13.4 결론

| 질문 | 답변 |
|------|------|
| RF2O yaw 비활성이 Nav2 회전에 영향? | **아니오** — yaw 추정에 전혀 관여하지 않음 |
| RF2O vyaw 비활성이 Nav2 회전에 영향? | **아니오** — 동일 이유 |
| RF2O x,y가 회전 중 문제를 일으키는가? | **아니오** — 동적 공분산 20배 증폭으로 억제 |
| RF2O 비활성화로 인한 부정적 효과? | **없음** — 오히려 비활성화가 회전 안정성 개선 |

Nav2 회전은 **IMU vyaw + Wheel vyaw**만으로 제어되며, RF2O는 회전 과정에 전혀 개입하지 않음.
RF2O는 직진 시 x,y 위치 보정에 집중하고, 회전 시에는 자동으로 영향력이 억제됨.

---

---

# Part 5: 2차 테스트 데이터 분석 (새로운 plot_data.csv)

## 15. 2차 테스트 데이터 개요

### 15.1 기록 조건
- 기록 시간: 264초 (4분 24초)
- 형식: `elapsed time, timestamp, topic, value` (이전 CSV와 다른 형식)
- 토픽: orient.z 4개

### 15.2 데이터 통계

| 토픽 | 메시지 수 | Hz (avg) | orient.z 범위 | 고유값 수 |
|------|----------|----------|--------------|----------|
| `/odom` (Wheel) | 7,207 | 27.1 | -0.064 ~ +0.156 | 7,154 |
| `/odom_adapted` (EKF) | 11,816 | 44.4 | -0.064 ~ +0.119 | **153** |
| `/odom_rf2o_adapted` (RF2O) | 2,430 | 9.1 | -0.067 ~ +0.158 | 1,147 |
| `/visual_slam/.../odometry_adapted` (VSLAM) | 1,151 | 4.5 | -0.000 ~ +0.183 | 934 |

### 15.3 로봇 동작 시퀀스

| 구간 | 시간 | 동작 | Wheel orient.z |
|------|------|------|---------------|
| 정지 1 | t=0~102s (102초) | 정지 상태 | ≈ -0.002 |
| 회전 1 | t=102.3~103.9s (1.6초) | 양의 방향 회전 | -0.002 → +0.155 |
| 정지 2 | t=104~170s (66초) | 정지 상태 | ≈ +0.154 |
| 회전 2 | t=170~177s (~7초) | 음의 방향 회전 | +0.154 → -0.058 |
| 정지 3 | t=177~265s (88초) | 정지 상태 | ≈ -0.060 |

---

## 16. VSLAM orient.z 정확도 분석 (질문 1)

### 16.1 VSLAM 데이터 상태

VSLAM orient.z는 3개의 상태만 존재:

| 구간 | VSLAM orient.z | 실제 로봇 | 상태 | 기간 |
|------|---------------|---------|------|------|
| t=0~41s | **0.000000** | 정지(≈0) | Frozen (1Hz 발행) | 41초 |
| t=42~61s | -0.00003~+0.0005 | 정지(≈0) | 잠시 활성 (22~26Hz) | 19초 |
| t=61~117s | **+0.000552** | 정지→회전→정지 | Frozen (1Hz 발행) | 56초 |
| t=117.2s | **+0.000552 → +0.18253** | 정지(+0.155) | **점프** (20.9° 순간) | 0.3초 |
| t=117~265s | **+0.18258** | +0.155→-0.058 | Frozen (1Hz 발행) | 148초 |

**핵심 문제:**
1. **회전 감지 실패**: 실제 회전(t=102~104)을 전혀 감지하지 못하고, 15초 후(t=117)에 점프로 보고
2. **과보정**: VSLAM +0.183 vs Wheel +0.155 → **18% 과보정** (3.2° 오차)
3. **2차 회전 완전 누락**: t=170~177 회전을 전혀 감지하지 않고 +0.183에서 영원히 동결
4. **평균 Hz 4.5Hz**: 대부분 1Hz frozen publish. 실질적 활성 시간은 19초 뿐

### 16.2 VSLAM Hz 구간별 분석

| 구간 | Hz | 상태 |
|------|-----|------|
| 0~40s | **1.0** | Frozen (adapter 1Hz downsample) |
| 40~60s | 22~26 | 양호 (잠시 활성) |
| 60~80s | 0.6~3.4 | 저하 + 13초 공백 |
| 80~120s | 1.0~1.9 | Frozen |
| 120~140s | 1.2~5.7 | 부분 활성 |
| 140~150s | **0.0** | 완전 공백 (11초) |
| 150~265s | 2.0~9.5 | Frozen (1Hz) + 간헐적 burst |

### 16.3 원인 및 개선 방안

#### A. cuVSLAM 파라미터 개선 (Docker launch)

**발견: `enable_ground_constraint_in_odometry`가 미설정 (기본값 False)**

cuVSLAM 소스 코드에서 확인:
```cpp
// visual_slam_node.cpp L57-60
enable_ground_constraint_in_odometry_(
    declare_parameter<bool>("enable_ground_constraint_in_odometry", false)),
enable_ground_constraint_in_slam_(
    declare_parameter<bool>("enable_ground_constraint_in_slam", false)),
```

NVIDIA Hawk 예제(`isaac_ros_visual_slam_hawk.launch.py`)에서는 활성:
```python
'enable_ground_constraint_in_odometry': True,
'enable_ground_constraint_in_slam': False,
```

**권장 수정** — Docker launch `visual_slam_node` parameters에 추가:
```python
'enable_ground_constraint_in_odometry': True,   # 바닥 로봇 → 2D 구속
'enable_ground_constraint_in_slam': False,       # SLAM은 3D 유지 (map→odom TF용)
```

**예상 효과:**
- cuVSLAM pose 추정이 z/roll/pitch를 ground plane으로 구속
- 6DoF → 실질 3DoF(x,y,yaw) → 추정 안정성 대폭 향상
- yaw 추적 실패 빈도 감소 (자유도 절반 → 수렴 용이)
- nvblox 메시 z축 오류도 간접적으로 완화

#### B. Adapter adaptive delta 상한 추가

현재 문제: 56초 frozen 후 unfreeze 시 adaptive delta 한계:
```
effective_max_yaw = 0.15 + (frozen_frames/25.0) × 1.0
                  = 0.15 + (56/25) × 1.0
                  = 0.15 + 2.24 = 2.39 rad (137°!)
```

137° 한계는 지나치게 관대 → cuVSLAM의 비정상 점프를 무조건 통과시킴.

**권장 수정** — `vslam_covariance_adapter.py`에 cap 추가:
```python
# adaptive delta에 상한 설정
MAX_ADAPTIVE_YAW = 0.5   # 최대 28.6° (어떤 frozen 기간이든)
MAX_ADAPTIVE_POS = 2.0   # 최대 2.0m

effective_max_yaw = min(effective_max_yaw, MAX_ADAPTIVE_YAW)
effective_max_pos = min(effective_max_pos, MAX_ADAPTIVE_POS)
```

**효과**: 56초 frozen 후에도 최대 28.6° 점프만 허용 → 20.9° 점프는 통과하되, 극단적 오류는 차단

#### C. 기타 cuVSLAM 조정

| 파라미터 | 현재 | 권장 | 이유 |
|---------|------|------|------|
| `image_jitter_threshold_ms` | 68.0 | **100.0** | emitter_on_off 타이밍 불규칙 마진 확대 |
| `enable_ground_constraint_in_odometry` | **미설정(False)** | **True** | 바닥 로봇 2D 구속 |

---

## 17. nvblox 메시 지면 관통 분석 (질문 2)

### 17.1 이전(Part 4) 수정 효과 확인

| 수정 사항 | 상태 | 효과 |
|----------|------|------|
| Host static TF 수정 (0.274→0.205, 0.055→0.098) | ✅ 적용됨 | TF 3중 충돌 해소 |
| Docker static TF 수정 | ✅ 사용자 적용 | 동일 |
| URDF camera_joint 수정 | ✅ 사용자 적용 | 동일 |

→ 3곳 모두 (0.205, 0, 0.098)으로 통일됨

### 17.2 잔여 원인 분석

TF 통일 후에도 메시가 지면 아래에 약간 생성될 수 있는 원인:

#### A. 깊이 센서 노이즈 (1차 원인)
```
카메라 높이 = 0.08(base) + 0.098(camera) = 0.178m (지면에서)
D455 깊이 노이즈 = ±5mm (at 18cm)
바닥 측정값 = 0.178 ± 0.005m
→ 바닥 z 좌표 = 0.178 - 0.183 = -0.005m (바닥 아래 5mm)
→ voxel_size=0.05m이므로 z=-0.025m 복셀에 할당 가능
```

#### B. `workspace_bounds_type: "unbounded"` (nvblox_base.yaml)
```yaml
# 현재 설정: 높이 제한 없음
workspace_bounds_type: "unbounded"   # ← z 클리핑 없음
workspace_bounds_min_height_m: -0.5  # unbounded이므로 미사용
workspace_bounds_max_height_m: 2.0   # unbounded이므로 미사용
```
→ 깊이 노이즈로 생성된 z<0 복셀이 삭제되지 않고 유지

#### C. `enable_ground_constraint_in_odometry: False` (cuVSLAM)
- cuVSLAM이 6DoF 모드 → z/roll/pitch가 미세하게 드리프트 가능
- cuVSLAM odometry가 odom→base_footprint과 다른 z offset 보고 → nvblox TF 체인 불일치

### 17.3 권장 수정

**1) cuVSLAM ground constraint 활성 (가장 효과적)**
```python
# Docker launch visual_slam_node parameters:
'enable_ground_constraint_in_odometry': True,
```

**2) nvblox workspace height bounds 활성 (보조)**
```python
# Docker launch nvblox_node parameters 추가:
'static_mapper.workspace_bounds_type': 'height_bounds',
'static_mapper.workspace_bounds_min_height_m': -0.02,   # odom z=-2cm 이하 클리핑
'static_mapper.workspace_bounds_max_height_m': 1.5,     # odom z=1.5m 이상 클리핑
```

**3) Docker `base_to_camera_tf` 조건부 실행 (정리)**

현재 Docker launch가 `base_to_camera_tf`를 **무조건** 발행 → Host의 robot_state_publisher와 중복:
```python
# isaac_ros_vslam_nvblox_realsense.launch.py L256:
return launch.LaunchDescription([
    standalone_arg,
    realsense_camera_node,
    imu_tf_node,
    base_to_camera_tf,         # ← standalone이 아닌 경우에도 항상 포함
    composable_container,
])
```

비록 값이 동일해도, 3개 TF publisher는 여전히 경쟁 → TF2 경고 발생.
향후 `standalone` 조건부로 변경 권장.

---

## 18. RF2O yaw/vyaw EKF 융합 가능성 분석 (질문 3)

### 18.1 RF2O vs Wheel vs EKF 비교

#### 정적 구간 (t=200~265s, 65초)

| 센서 | orient.z 범위 | 드리프트/60s | 평가 |
|------|-------------|------------|------|
| Wheel | 0.006 | -0.003 | 기준 |
| RF2O | **0.007** | **-0.006** | Wheel 대비 1.2x 노이즈, **1.9x 드리프트** |
| EKF (IMU+Wheel) | **0.000** | **0.000** | ✅ 완벽 안정 |

#### 회전 후 절대값 비교

| 시점 | Wheel | RF2O | EKF | VSLAM |
|------|-------|------|-----|-------|
| 회전1 후 (t=104~106) | **+0.155** | **+0.149** | **+0.119** | +0.001 (미감지) |
| 회전2 후 (t=185~200) | **-0.058** | **-0.058** | **-0.064** | +0.183 (미감지) |

#### 회전량 분석

| | 회전1 크기 | 회전2 크기 |
|---|---|---|
| Wheel | +0.157 (9.0°) | -0.213 (-12.2°) |
| RF2O | +0.149 (8.5°) | -0.207 (-11.9°) |
| EKF | +0.119 (6.8°) | -0.183 (-10.5°) |
| EKF/Wheel | **76%** | **86%** |

**EKF가 회전량을 체계적으로 과소추정** (Wheel 대비 76~86%).

### 18.2 RF2O vs Wheel 시간별 차이

| 구간 | RF2O - Wheel | 평가 |
|------|-------------|------|
| 정지1 (t=0~100s) | +0.001 ~ +0.005 | RF2O +편향 (약간 높음) |
| 회전 후 (t=100~170s) | -0.001 ~ +0.003 | 양호한 일치 |
| 정지3 (t=200~265s) | -0.001 → **-0.004** | RF2O 누적 드리프트 ⚠ |

→ RF2O yaw가 **시간 경과에 따라 음의 방향으로 드리프트** (Wheel 대비 -0.004/90s)

### 18.3 EKF yaw 과소추정 원인

```yaml
# EKF yaw 결정 소스:
# IMU vyaw:  gyro_cov = 0.001 (rad/s)²  ← EKF가 가장 신뢰
# Wheel vyaw: 동적 공분산 (통상 ~0.01)    ← EKF가 덜 신뢰
# process_noise vyaw(11,11) = 0.01
```

**IMU cov (0.001) << Wheel cov (~0.01)** → EKF가 IMU를 10배 더 신뢰.
IMU 각속도가 실제보다 작게 보고 → EKF yaw가 Wheel/RF2O 대비 과소추정.

현재 IMU gyro 스케일 보정: `1.025` (jupiter_driver_compensated.py)
실제 필요 보정: ~`1.20~1.25` (EKF가 Wheel의 76~86%만 반영)

### 18.4 RF2O vyaw 융합 평가

| 기준 | 평가 | 근거 |
|------|------|------|
| 정적 정확도 | ❌ **열위** | RF2O 드리프트 1.9x > Wheel |
| 회전 시 정확도 | ✅ Wheel과 유사 | ±0.003 이내 일치 |
| 주파수 | ❌ **열위** | 9.1Hz vs Wheel 27.1Hz |
| 과거 이력 | ❌ **위험** | 2026-02-26: 제자리 회전 시 **50° EKF 발산** 기록 |
| EKF yaw 개선 기여 | △ 미미 | +0.149 vs Wheel +0.155 → 동일 방향이지만 Wheel이 더 정확 |
| 정적 EKF 안정성 영향 | ❌ 저하 | 현재 EKF range=0.000 → RF2O 추가 시 노이즈 유입 |

### 18.5 결론: RF2O vyaw 융합 **비추천**

**이유:**
1. RF2O yaw는 Wheel보다 모든 지표에서 열위 (노이즈, 드리프트, Hz)
2. EKF yaw 과소추정 문제의 원인은 RF2O가 아니라 **IMU 신뢰도 과다** (gyro_cov=0.001)
3. RF2O vyaw 추가는 제자리 회전 시 50° 발산 위험 재현 가능성
4. 정적 상태에서 현재 EKF yaw는 완벽 (range=0.000) → RF2O 추가는 오히려 악화

**대신 권장하는 대안:**

| 대안 | 효과 | 위험도 |
|------|------|--------|
| IMU gyro_cov 증가 (0.001→0.005) | Wheel vyaw 비중 ↑ → 회전량 과소추정 완화 | 낮음 |
| IMU gyro 스케일 재측정 (현재 1.025) | 근본 원인 해결 | 낮음 (측정 필요) |
| process_noise vyaw 증가 (0.01→0.03) | IMU/Wheel 모두 비중 ↑ | 중간 (정적 안정성 약간 저하) |

---

## 19. 수정된 파일 목록 (Part 1~5)

| 파일 | 변경 내용 |
|------|----------|
| `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py` | 9개 코드 블록 수정 (파라미터 3개 추가, _check_frozen 재작성, adaptive delta, odom_callback frozen-anomaly 분리, _publish_with_frozen_covariance 신규) |
| `src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` | Adapter 파라미터 7개 변경/추가 + **static TF 수정 (0.274,0,0.055 → 0.205,0,0.098)** |
| `isaac_ws_ros/.../isaac_ros_vslam_nvblox_realsense.launch.py` | Docker static TF 수정 (0.274,0,0.055 → 0.205,0,0.098) — 사용자가 직접 수정 |

---

# Part 6: 패키지 상호작용 분석 + 권장 수정 적용

## 20. enable_ground_constraint_in_odometry 제외 결정

**야외 주행이 최종 목표**이므로 `enable_ground_constraint_in_odometry: True`는 적용하지 않는다.

- 이 파라미터는 cuVSLAM의 z축(높이)과 roll/pitch를 억제하여 2D 평면 로봇에 맞게 제약
- 실내 평면에서는 효과적이나, 야외 경사/요철에서는 오히려 **cuVSLAM 정확도 저하** 유발
- EKF에서 `two_d_mode: true`로 이미 z/roll/pitch를 억제하고 있어, VSLAM 원본 데이터를 왜곡할 필요 없음

## 21. Isaac VSLAM / nvblox ↔ robot_localization 상호작용 분석

### 21.1 핵심 결론

**cuVSLAM과 nvblox 모두 `robot_localization` 패키지에 대한 직접적 의존성이 없다.**

| 확인 항목 | cuVSLAM | nvblox |
|-----------|---------|--------|
| `package.xml`에 `robot_localization` 의존 | **없음** | **없음** |
| EKF 출력 토픽 구독 | **없음** | **없음** |
| 소스 코드에서 `robot_localization` 참조 | **없음** | **없음** |
| `odom_adapted` / `odom_rf2o` 참조 | **없음** | **없음** |

### 21.2 cuVSLAM의 TF 퍼블리시/구독

**퍼블리시 (2개, 파라미터로 제어):**

| 파라미터 | 기본값 | TF | 현재 설정 |
|----------|--------|-----|----------|
| `publish_map_to_odom_tf` | `true` | `map` → `odom` | **False** (SLAM Toolbox가 담당) |
| `publish_odom_to_base_tf` | `true` | `odom` → `base_link` | **standalone일 때만 True** |

**구독:** `base_link → camera_optical_frame` TF (초기화 시 카메라 외부 파라미터 획득용)

**오도메트리 출력:** `/visual_slam/tracking/odometry` (`nav_msgs/Odometry`, `odom→base_link`)
- 이 토픽이 **vslam_covariance_adapter → EKF**로 전달되는 센서 입력

`publish_odom_to_base_tf`가 **충돌 지점**: True이면 robot_localization EKF와 동일한 `odom→base_link` TF를 중복 퍼블리시. 현재 standalone=false이므로 충돌 없음.

### 21.3 nvblox의 TF 의존

nvblox는 **TF tree에서만 로봇 자세를 획득** (`use_tf_transforms=true` 기본값):

```
odom → base_link → camera_link → camera_depth_optical_frame
  ↑         ↑              ↑                ↑
  │    EKF 퍼블리시    URDF(rsp)      RealSense 드라이버
  │
  global_frame
```

- nvblox는 Odometry 토픽을 **구독하지 않음** — TF tree만 사용
- `global_frame` = `odom` → EKF가 퍼블리시하는 `odom→base_link` TF에 **간접 의존**
- EKF가 `odom→base_link`를 제공하지 않으면 nvblox는 `"Can't transform: odom to camera_depth_optical_frame"` 에러 발생

### 21.4 nvblox Nav2 Costmap Layer

`NvbloxCostmapLayer`는 `/nvblox_node/static_map_slice`를 구독하며:
- Local costmap (`odom` 프레임): TF 변환 불필요, 직접 사용
- Global costmap (`map` 프레임): `lookupTransform("map", "odom")` → SLAM Toolbox의 `map→odom` TF 필요

### 21.5 데이터 흐름 요약

```
cuVSLAM ──── /visual_slam/tracking/odometry ──→ vslam_covariance_adapter ──→ EKF (robot_localization)
  │                                                                              │
  │   cuVSLAM은 EKF에 관여 X                                                  퍼블리시: odom→base_link TF
  │   (출력만 제공하는 센서 역할)                                                 │
  │                                                                              ▼
  └── reads TF: base_link→camera_optical                              TF tree (odom→base_link→camera_link→…)
      (초기화 시 1회)                                                            │
                                                                                 ▼
                                                                          nvblox_node
                                                                    (lookupTransform: odom → camera_depth_optical_frame)
                                                                                 │
                                                                                 ▼
                                                                    /nvblox_node/static_map_slice
                                                                                 │
                                                                                 ▼
                                                                    NvbloxCostmapLayer (Nav2 plugin)
```

### 21.6 운영 시 주의사항

1. **cuVSLAM `publish_odom_to_base_tf`**: 반드시 `False` 유지 (EKF와 TF 충돌 방지)
2. **cuVSLAM `publish_map_to_odom_tf`**: 반드시 `False` 유지 (SLAM Toolbox와 충돌 방지)
3. **nvblox 작동 전제**: EKF가 `odom→base_link` TF를 안정적으로 퍼블리시해야 함
4. **costmap global_frame 일치**: nvblox `global_frame` = Nav2 local costmap `global_frame` = `odom`

## 22. 권장 수정 사항 적용 (4건)

`enable_ground_constraint_in_odometry`를 제외한 4건의 권장 수정 사항을 적용하고 빌드 완료.

### 22.1 image_jitter_threshold_ms: 68 → 100

**파일:** `isaac_ws_ros/src/isaac_ros_visual_slam/.../isaac_ros_vslam_nvblox_realsense.launch.py`

- **이유:** emitter_on_off 모드에서 30Hz(33ms) 간격은 이론적이며, IR 프레임 전달 지연/불규칙이 발생
- 68ms(×2 마진)에서 100ms(×3 마진)로 완화하여 불필요한 frame drop 방지
- cuVSLAM이 프레임을 drop하면 VSLAM tracking 품질 저하 → frozen 빈도 증가

```python
# Before:
'image_jitter_threshold_ms': 68.00,  # ⭐ 34→68ms: 30Hz(33ms) × 2배 마진
# After:
'image_jitter_threshold_ms': 100.0,  # ⭐ 30Hz(33ms) × 3배 마진 — emitter_on_off 간헐 불규칙 대응
```

### 22.2 nvblox workspace_bounds: unbounded → height_bounds

**파일:** 동일 launch (nvblox_node 파라미터)

- **이유:** nvblox 메쉬에 바닥 아래(-z 방향) 노이즈 복셀이 생성되는 문제
- TF 수정(Part 4)으로 대부분 개선되었으나, depth 노이즈에 의한 잔여 아티팩트 제거
- `min_height_m: -0.02` (바닥~2cm 아래까지 허용), `max_height_m: 1.5` (로봇 시야 상한)

```python
# 추가:
'static_mapper.workspace_bounds_type': 'height_bounds',
'static_mapper.workspace_bounds_min_height_m': -0.02,
'static_mapper.workspace_bounds_max_height_m': 1.5,
```

### 22.3 Adaptive delta 상한 (MAX_ADAPTIVE_YAW=0.5, MAX_ADAPTIVE_POS=2.0)

**파일:** `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py` (`_check_anomaly` 메서드)

- **이유:** frozen 기간이 길면(예: 10초) adaptive threshold가 `0.087 + 10 × 1.0 = 10.087 rad(~578°)`까지 허용
- 이론적으로 불가능한 점프까지 허용하게 되어, cuVSLAM 텔레포트를 정상으로 오판할 위험
- 상한: yaw 0.5rad(~28.6°), position 2.0m — 로봇 물리적 한계 이내

```python
# 추가 (effective_max_yaw/pos 계산 후):
MAX_ADAPTIVE_YAW = 0.5   # ~28.6°
MAX_ADAPTIVE_POS = 2.0   # 2m
effective_max_yaw = min(effective_max_yaw, MAX_ADAPTIVE_YAW)
effective_max_pos = min(effective_max_pos, MAX_ADAPTIVE_POS)
```

### 22.4 IMU gyro_cov: 0.001 → 0.005

**파일:** `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

- **이유:** EKF에서 IMU vyaw가 과신되어 Wheel vyaw를 억제, 회전량 과소추정(76~86%)
- gyro_cov를 5배 증가시켜 Wheel angular velocity의 EKF 가중치를 높임
- 근본 해결은 gyro 스케일 재측정(현재 1.025, 실측 1.20~1.25 추정)이나, 우선 공분산 조정으로 1차 개선

```python
# Before:
gyro_cov = 0.001  # 각속도 측정 불확실성 (rad/s)^2
# After:
gyro_cov = 0.005  # 각속도 측정 불확실성 (rad/s)^2 — 0.001→0.005: Wheel vyaw 비중 ↑
```

## 23. 빌드 결과

```
colcon build --packages-select jupiter_nav_VO jupiter_bringup --symlink-install

Summary: 2 packages finished [8.92s]
```

- `jupiter_nav_VO`: adapter delta cap 적용 → 빌드 성공
- `jupiter_bringup`: IMU gyro_cov 변경 → 빌드 성공
- Docker launch (cuVSLAM jitter + nvblox height_bounds): Docker 내부 파일이므로 별도 빌드 불필요 (launch.py 직접 실행)

## 24. 수정된 파일 목록 (Part 6 포함 전체)

| 파일 | 변경 내용 | 파트 |
|------|----------|------|
| `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py` | 9개 코드 블록 수정 + **adaptive delta cap 추가** | Part 1 + 6 |
| `src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` | Adapter 파라미터 7개 변경/추가 + static TF 수정 | Part 1 + 4 |
| `isaac_ws_ros/.../isaac_ros_vslam_nvblox_realsense.launch.py` | Docker static TF + **jitter 100ms** + **nvblox height_bounds** | Part 4 + 6 |
| `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` | **IMU gyro_cov 0.001→0.005** | Part 6 |

### 미적용 항목

| 파일 | 항목 | 상태 | 사유 |
|------|------|------|------|
| `isaac_ros_vslam_nvblox_realsense.launch.py` | `enable_ground_constraint_in_odometry: True` | **제외** | 야외 주행이 최종 목표 — 2D 구속은 경사/요철에서 부정확 |
| `jupiter_driver_compensated.py` | IMU gyro 스케일 재측정 (1.025→~1.2) | **차기** | 실측 필요 (자동 회전 테스트 → 실제 회전량 비교) |

---

# Part 7: RTK GPS 융합 + 휠 슬립 분석

## 25. RTK GPS → EKF 융합 아키텍처

### 25.1 목표

ZED-F9P RTK GPS(`/fix`)를 EKF 센서 융합에 추가하여, 실외에서 절대 위치를 RTK 정밀도로 보정한다.
실내/실외를 `status`와 `position_covariance`로 자동 판별하여, 실내에서는 GPS를 자동 비활성화한다.

### 25.2 데이터 흐름

```
ZED-F9P RTK ─── /fix (NavSatFix, WGS84) ─→ gnss_quality_filter
                                                    │
                                      NO_FIX/POOR → 게이트아웃 (미발행)
                                      GPS/RTK    → 공분산 재설정 후 통과
                                                    │
                                                    ▼
                                            /fix_filtered (NavSatFix)
                                                    │
                                                    ▼
                                         navsat_transform_node
                                        (WGS84→odom 프레임 변환)
                                                    │
                                                    ▼
                                            /odometry/gps (Odometry)
                                                    │
                                                    ▼
                                      EKF (odom3: x, y 절대 위치)
```

### 25.3 핵심 설계 결정

**1) gnss_quality_filter 노드 (신규 생성)**
- `/fix` → status/covariance 기반 품질 판정 → `/fix_filtered` 또는 게이트아웃
- ZED-F9P의 GGA quality indicator 매핑:
  - qual=0 → `STATUS_NO_FIX` → **게이트아웃** (실내)
  - qual=1 → `STATUS_FIX` → GPS (cov override: 10.0 m²)
  - qual=4 → `STATUS_GBAS_FIX`, raw_cov≤0.05 → **RTK Fixed** (cov: 0.02 m²)
  - qual=5 → `STATUS_GBAS_FIX`, raw_cov~0.1 → **RTK Float** (cov: 0.2 m²)
  - raw_cov > 15.0 → **POOR** → 게이트아웃

**2) navsat_transform_node (robot_localization 내장)**
- WGS84 위경도 → odom 프레임 XY 좌표 변환
- `use_odometry_yaw: true` — MCU IMU에 자력계가 없으므로 EKF yaw 사용
- `zero_altitude: true` — 2D 로봇
- 첫 GPS fix를 원점으로 자동 설정

**3) EKF odom3 설정**
- `/odometry/gps` → x, y 절대 위치만 사용 (heading 정보 없음)
- `differential: false` — navsat_transform이 odom 프레임으로 변환 완료
- `pose_rejection_threshold: 10.0` — GPS 아웃라이어 거부

**4) 실내/실외 전환**
- 실내 → 실외: GPS fix 획득 시 gnss_quality_filter가 `/fix_filtered` 발행 시작
  → navsat_transform이 원점 설정 후 `/odometry/gps` 발행 → EKF에 자연스럽게 유입
- 실외 → 실내: GPS fix 상실 시 gnss_quality_filter가 발행 중단
  → navsat_transform 입력 없음 → odom3 타임아웃 → EKF가 자동으로 나머지 센서만 사용

**5) use_gnss launch argument**
- `true` (기본값): GPS 관련 노드 전체 실행
- `false`: GPS 하드웨어 없이 실내 테스트 시 사용

### 25.4 새로 생성된 파일

| 파일 | 역할 |
|------|------|
| `src/jupiter_nav_VO/jupiter_nav_VO/gnss_quality_filter.py` | GNSS 품질 필터 노드 |
| `src/jupiter_nav_VO/config/navsat_params.yaml` | navsat_transform_node 설정 |

### 25.5 수정된 파일

| 파일 | 변경 내용 |
|------|----------|
| `src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` | RTK GPS 섹션 추가 (§11), use_gnss arg, Foxglove whitelist GPS 토픽 |
| `src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml` | odom3 GPS 섹션 추가, 헤더 업데이트 |
| `src/jupiter_nav_VO/setup.py` | `gnss_quality_filter` entry point 추가 |

### 25.6 GPS 안테나 TF 설정

현재 `base_link → gps_link` TF: `(0, 0, 0.15)` (임시값)
⚠️ **실제 GPS 안테나 장착 위치를 측정하여 launch 파일의 `tf_base_to_gps` 값 수정 필요**

### 25.7 빌드 결과

```
colcon build --packages-select jupiter_nav_VO --symlink-install
Summary: 1 package finished [4.41s]
```

### 25.8 사용 방법

```bash
# GPS 포함 실행 (기본)
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py

# GPS 없이 실행 (실내 테스트)
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py use_gnss:=false
```

---

## 26. 휠 슬립 문제 분석 및 해결 방안

### 26.1 문제 상황

- 로버 후진 명령 → 발로 로봇을 막음 → 바퀴는 회전하나 로봇은 이동하지 않음
- **결과**: 엔코더 기반 odom이 후진을 보고 → EKF가 이를 반영 → odom 프레임 상 로봇 위치가 뒤로 이동

### 26.2 근본 원인

EKF 센서별 역할과 휠 슬립 시 상태:

| 센서 | 기여 데이터 | 슬립 시 상태 | EKF 영향 |
|------|------------|-------------|---------|
| **odom0 (Wheel)** | vx, vy, vyaw | **잘못된 vx 보고** (바퀴 회전) | ⚠️ EKF가 vx를 적분 → 위치 드리프트 |
| **odom1 (VSLAM)** | x, y, vx, vy (differential) | 실제 이동 없음 → Δx≈0 | ✅ 위치 변화 없음 보고 |
| **odom2 (RF2O)** | x, y (differential) | 스캔 변화 없음 → Δx≈0 | ✅ 위치 변화 없음 보고 |
| **imu0** | vyaw만 | 회전 없음 → vyaw≈0 | ✅ 정상 |

**핵심 문제**: Wheel odom의 vx 공분산(~0.005 m²/s²)이 낮아 EKF가 이를 높은 신뢰도로 적분.
VSLAM/RF2O는 위치(x, y)를 제공하지만, 속도(vx) 직접 측정은 제한적.
**EKF predict 단계에서 vx 적분이 VSLAM/RF2O의 위치 보정보다 빠르게 누적.**

### 26.3 해결 방안 (우선순위순)

#### 방안 A: IMU 가속도 기반 슬립 감지 (권장)

`odom_covariance_adapter`에 IMU 가속도 데이터를 추가하여 슬립을 감지한다.

**원리:**
- 로봇이 실제로 움직일 때: 초기 가속도 → 정속 → 감속 패턴
- 바퀴만 공회전: 엔코더는 속도 보고하지만 IMU 가속도는 거의 0
- **불일치 감지**: `|wheel_vx| > threshold` AND `|IMU_ax_integral_over_window| ≈ 0` → 슬립

**구현 개요:**
```python
# odom_covariance_adapter.py에 추가
# 1. /imu/data_calibrated 구독
# 2. 슬립 감지 논리
#    - 윈도우(1초) 내 IMU ax 적분 = expected_displacement
#    - 윈도우 내 wheel vx 적분 = encoder_displacement
#    - ratio = expected / encoder
#    - ratio < 0.3 → 슬립 판정 → 공분산 50배 증폭
# 3. 슬립 해제: 5회 연속 정상 → 공분산 복귀
```

**장점**: 독립 센서(IMU)로 검증, VSLAM 상태 무관
**단점**: IMU 가속도 바이어스 주의 (현재 ax 바이어스 ~0.11 m/s²)
→ 바이어스 보정 필요하거나, 속도 변화량(Δvx)으로 비교

#### 방안 B: VSLAM/RF2O 속도 교차 검증

Wheel odom 속도와 VSLAM/RF2O의 Δposition/Δt를 비교한다.

**원리:**
- Wheel vx ≠ 0이지만 VSLAM Δx/Δt ≈ 0 AND RF2O Δx/Δt ≈ 0 → 슬립
- 2개 이상 독립 센서가 "정지"면 wheel을 불신

**장점**: IMU 바이어스 문제 회피, 직관적
**단점**: VSLAM frozen 시 검증 불가, RF2O가 정확한 경우에만 유효

#### 방안 C: cmd_vel 명령 대비 실제 이동 감시

`/cmd_vel` 명령과 실제 EKF 출력(`/odom`) 위치 변화를 비교한다.

**원리:**
- cmd_vel이 후진 명령 중이지만, EKF 출력 위치가 변하지 않으면 정상
- 이미 EKF가 다른 센서로 보정 중이라면 문제 없음
- 문제가 보이면 wheel 공분산을 수동으로 증가

**장점**: 구현 간단
**단점**: EKF 출력은 이미 wheel에 오염된 상태 → 순환 참조

#### 방안 D: 모터 전류 감시 (하드웨어 의존)

MCU에서 모터 전류를 보고하면, 높은 전류 + 낮은 실제 이동 = 스톨/슬립으로 판단.

**장점**: 가장 직접적인 물리적 지표
**단점**: MCU 펌웨어 수정 필요, 현재 지원 여부 미확인

### 26.4 추천: 방안 A (IMU 기반 슬립 감지)

구현 위치: `odom_covariance_adapter.py`

**수정 범위:**
1. `/imu/data_calibrated` 구독 추가
2. IMU ax를 윈도우(~1초) 내 누적하여 "기대 속도 변화량" 계산
3. Wheel encoder "실제 속도 변화량"과 비교
4. 불일치 시 wheel 공분산을 `slip_cov_multiplier`(예: 50배)로 증폭
5. 슬립 해제 조건: 연속 N회 정상 판정

**구현 시 주의사항:**
- IMU ax 바이어스(~0.11 m/s²) 처리: 정지 시 평균을 빼거나, **속도 변화량(Δv)으로 비교** (바이어스 상쇄)
- 정속 주행에서 오탐 방지: 정속 시 ax≈0, encoder Δv≈0이므로 비율 계산 대신 **차이**로 판정
- 슬립 감지 임계값: `|encoder_vx| > 0.05 m/s AND |imu_Δv_window| < 0.02 m/s` → 슬립

### 26.5 즉시 적용 가능한 임시 완화 (코드 수정 없음)

현재 시스템에서 슬립 영향을 줄이는 EKF 파라미터 조정:

1. **Wheel vx 공분산 증가**: `odom_covariance_adapter`의 `base_linear_cov`를 0.005 → 0.02로 증가
   → EKF가 wheel vx를 덜 신뢰 → VSLAM/RF2O의 위치 보정이 더 영향력

2. **VSLAM/RF2O vx 활성화**: EKF에서 VSLAM의 vx, vy를 현재 사용 중이므로 VSLAM이 정상이면 자연스럽게 보정
   → VSLAM frozen 상태에서의 슬립이 가장 취약

3. **RF2O 공분산 감소**: RF2O가 정상 작동 중이면 위치 보정력 증가
   → 회전 중이 아닐 때 rf2o의 position_cov_base를 0.5 → 0.2로 낮출 수 있음

## 27. 수정된 파일 목록 (Part 7 포함 전체)

| 파일 | 변경 내용 | 파트 |
|------|----------|------|
| `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py` | 9개 코드 블록 수정 + adaptive delta cap | Part 1+6 |
| `src/jupiter_nav_VO/jupiter_nav_VO/gnss_quality_filter.py` | **신규 생성** — GNSS 품질 필터 노드 | Part 7 |
| `src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` | Adapter 파라미터 + static TF + **RTK GPS 섹션 추가** | Part 1+4+7 |
| `src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml` | odom0~2 + imu0 + **odom3 GPS 추가** | Part 7 |
| `src/jupiter_nav_VO/config/navsat_params.yaml` | **신규 생성** — navsat_transform 설정 | Part 7 |
| `src/jupiter_nav_VO/setup.py` | gnss_quality_filter entry point 추가 | Part 7 |
| `isaac_ws_ros/.../isaac_ros_vslam_nvblox_realsense.launch.py` | Docker jitter 100ms + nvblox height_bounds | Part 6 |
| `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` | IMU gyro_cov 0.001→0.005 | Part 6 |

### 미적용 항목

| 항목 | 상태 | 사유 |
|------|------|------|
| `enable_ground_constraint_in_odometry: True` | **제외** | 야외 주행 목표 |
| IMU gyro 스케일 재측정 (1.025→~1.2) | **차기** | 실측 필요 |
| 휠 슬립 감지 (IMU 기반, 방안 A) | **차기** | 설계 완료, 구현 대기 — `odom_covariance_adapter.py` 수정 |
| base_link→gps_link TF 실측 | **차기** | GPS 안테나 실제 장착 위치 측정 필요 |
