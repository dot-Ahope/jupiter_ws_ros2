# 진행과정 2026-06-12: D455F + cuVSLAM IMU 융합 fly-away 근본수정(H1 타임스탬프 + Phase3) 및 시각화 거동 진단

## 1. 목표

### 1.1 사용자 요청 (시간순)
1. > `D455F_IMU_cuVSLAM_핸드오프.md`를 보고 제대로 된 분석인지 판단부터 하라. 현재 문제는 isaac_vslam에서 imu를 융합시켰을 때 오도메트리가 조금만 움직여도 날아가버린다. visual only로 하면 날아가는 문제가 없다. 커뮤니티/여러 경로로 이슈 및 해결책을 찾아봐라.
2. > (확정) IMU 융합을 반드시 작동시킨다 / 현재 `/camera/imu` 스트림 상태는 미확인.
3. > (run_dev.sh + VNC 명령 제공) 도커 컨테이너 실행하고 진행하라.
4. > (Phase3 모션 테스트 결과 보고) 예전처럼 조금만 움직여도 멀리 날아가는 모습은 보이지 않는다. + 시각화 객체 중 좌우 이동 시 Y축이 급격히 위를 보다 안정화되는 객체 분석 + 그 이상현상이 일으킬 문제 진단.
5. > (정정) 흔들린 객체는 TF가 아니라 **Velocity 마커**(속도 화살표)였다.
6. > 무엇을 통해 어떤 문제를 해결했고, 인지해야 할 이슈들을 정리하라. 다음 단계 절차를 설명하라.

### 1.2 이전 세션 요약 / 연관 작업
- 핸드오프 문서 `/home/jetson/D455F_IMU_cuVSLAM_핸드오프.md` (작성 2026-06-10): IMU 0Hz(JP6 커널 HID) 중심 분석 + cuVSLAM VIO 초기화 이슈 + autosuspend/RSUSB/Path A 복구경로.
- Allan 측정값: `/home/jetson/allan_ws/imu.yaml` (15h). 모니터: `/home/jetson/isaac_ws_ros/vslam_ready_monitor.py`.
- plan: `/home/jetson/.claude/plans/noble-greeting-backus.md` (본 세션의 판단·분석·실행계획 누적).

## 2. 대상 시스템 구성
- 코드: `isaac_ros_visual_slam` **release-3.2** (cuVSLAM 12.6). `enable_imu_fusion` 사용(`tracking_mode` 아님).
- 컨테이너: `isaac_ros_dev-aarch64`(이미지키 `ros2_humble.realsense.jupiter_user`), 호스트 `/home/jetson/isaac_ws_ros` ↔ 컨테이너 `/workspaces/isaac_ros-dev` 바인드마운트.
- 단독 launch: `isaac_ros_visual_slam_realsense.launch.py` (realsense2_camera + visual_slam_node 컴포저블 + identity gyro→imu static TF + rviz).
- 카메라: D455F (USB `8086:0b5c`), IMU BMI085 200Hz.

## 3. 문제 분석

### 3.1 핸드오프 문서 판단 (요청 1)
- **증상(보고)**: IMU 융합 시 조금만 움직여도 odom 발산, vision-only는 정상.
- **문서 평가**: 유능하나 ① 분량의 ~60%가 "IMU 0Hz=프레임 미流"(커널/RSUSB/autosuspend)에 쏠려 **현재 증상(프레임은 흐르는데 융합 발산)과 우선순위 어긋남**, ② §4 "IMU 축 설정 문제 아님"이 정적 테스트뿐인 **과신**, ③ **핵심 버그(IMU 타임스탬프) 누락**. → 부분적으로 맞음.
- **커뮤니티**: 정설 해결책 없음(#183 init후 점프·#202 init막힘 모두 open), NVIDIA 포럼 t/343427은 IMU 융합 비권장. **단 #178 "barb phenomenon"** 등은 메커니즘 참고.

### 3.2 H1 — IMU 등록 타임스탬프 결함 (근본 원인)
- **증상**: IMU 융합 시 모션 중 catastrophic fly-away(수 m/s 점프).
- **초기 의심 → 정정**: 처음엔 "upstream 동일 코드이니 부차적"으로 깔았으나, **upstream `main`이 `latest_ts`→`imu_ts`로 이미 수정**한 것을 웹 대조로 확인 → release-3.2 회귀로 **승격**.
- **코드상 원인**: `visual_slam_impl.cpp:682-688` 루프가 샘플별 `imu_ts`(685행)를 계산해놓고 버리고 `CUVSLAM_RegisterImuMeasurement(handle, latest_ts, ...)`로 **한 프레임의 모든 IMU 샘플을 이미지 타임스탬프 하나로 등록** → inter-sample dt=0 → 프리인테그레이션 퇴화.
- **확정(코드레벨)**: `cuvslam_ros_conversion.cpp:249-272` `TocuVSLAMImuMeasurement`는 accel/gyro만 채우고 **timestamp 필드 없음**(이미지 경로는 238행 `timestamp_ns` 보유). ⟹ IMU 시각은 등록 함수 인자로만 전달 ⟹ 688행 `latest_ts`→`imu_ts` **한 줄**로 충분. 포럼 t/286630에서 NVIDIA가 "각 stereo 쌍을 직전 쌍 이후 IMU와 각자 타임라인으로 처리" 확인 → 설계 정합.
- **근본 원인**: release-3.2 IMU 타임스탬프 회귀.

### 3.3 Phase3 — IMU 샘플 부족 / 통합방식 (보조 원인)
- **증상**: H1 패치 후 catastrophic fly-away는 사라졌으나 잔여 점프(jump 9, max 189 m/s) + **gravity 정렬 marker 미발행**(중력정렬 미수렴).
- **원인**: standalone launch가 `640x360x90`(90fps) → 200Hz IMU 기준 **2.2 sample/frame**(cuVSLAM 6~7 필요) + `unite_imu_method:2`(realsense-ros #2221: 비주기 timestamp). 샘플 부족 시 optical↔IMU 중력정렬이 수렴 못함.
- **근본 원인**: VIO 비친화 카메라 설정.

### 3.4 시각화 "Y축 흔들림" (요청 4·5)
- **증상**: 좌우 이동 시 어떤 객체의 Y축/화살표가 급격히 위를 보다 안정화.
- **초기 의심 (정정 전)**: 워크플로우(소스+커뮤니티+적대검증)로 "포즈/TF triad의 gravity-tilt transient(횡가속이 중력기울기로 오귀속)"로 추정.
- **bag 실측으로 정정 ①**: 큰 흔들림은 **pitch**(−27°), roll 안정. 피크에서 가속도계가 중력 23° 틸트를 독립 확인하는데 그 순간 |v|=0.06 m/s. accel.z↔pitch **r=−0.986**. 지속 3.8 m/s²+정지 동시성립 불가(병진이면 Δv≈4.4 m/s 필요, 실측 0.06) → **실제 PITCH의 정상 추적**(아티팩트/발산 아님). Δpitch/step 최대 1.5°(글리치 없음), |g|=9.81 유지.
- **사용자 정정 ②**: 흔들린 객체는 TF가 아니라 **Velocity 마커**(`/visual_slam/vis/velocity`).
- **최종 원인**: 속도 화살표가 사방으로 흔들린 것은 **(a) 저속 속도추정 노이즈**(녹화 51%가 |v|<0.05, 방향변화 저속 16°/step vs 명확모션 3°/step) **+ (b) 실제 3D 모션**(pz range 0.516 m). **코스메틱**, 포즈/맵과 무관.

## 4. 적용한 변경

### 4.1 H1 패치 — IMU 등록 타임스탬프
- 파일: `.../src/impl/visual_slam_impl.cpp` (688행)

| 변경 전 | 변경 후 |
|---|---|
| `CUVSLAM_RegisterImuMeasurement(cuvslam_handle, latest_ts, &imu_measurement);` | `CUVSLAM_RegisterImuMeasurement(cuvslam_handle, imu_ts, &imu_measurement);` + 설명 주석 |

- 의도: 각 IMU 샘플을 자기 타임스탬프로 등록(upstream main 동작). 주의: isaac_ros_visual_slam **재빌드 필요**(핸드오프 §5.3 "재빌드 불필요"는 config 변경에만 해당).

### 4.2 Phase3 — 카메라 설정 VIO 친화 + 런치 인자화
- 파일: `.../launch/isaac_ros_visual_slam_realsense.launch.py`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `depth_module.profile` | 하드코딩 `640x360x90` | 런치인자 `stereo_profile`, 기본 `640x360x30` |
| `unite_imu_method` | 하드코딩 `2` | 런치인자 `unite_imu_method`, 기본 `1`(copy) |
| `jitter_threshold_ms` 기본 | `33.0` | `50.0` (30fps 33ms 주기 맞춤) |

- 의도: 6~7 IMU sample/frame 확보 → 중력정렬 수렴. A/B 용이(`stereo_profile:=640x360x90 unite_imu_method:=2`로 즉시 복귀). 트레이드오프: 30fps는 시각 프레임 감소(단 IMU 융합 안정성 우선).

## 5. 빌드 및 검증

### 5.1 빌드 (§0.3)
```bash
colcon build --symlink-install --packages-select isaac_ros_visual_slam --cmake-args -DCMAKE_BUILD_TYPE=Release
# Finished <<< isaac_ros_visual_slam [54.6s]  COLCON_EXIT=0
```
- 재컴파일 `visual_slam_impl.cpp.o`(13:04:38) + 재링크 `libvisual_slam_node.so`(13:04:39, install 심링크 타겟) 확인.
- Phase3 런치: `python3 -m py_compile` → `PY_SYNTAX_OK`(재빌드 불필요).

### 5.2 Phase 0 정지 실측 (§0.5)
- IMU 199.7Hz/드롭0, extrinsic TF non-identity 정상, init 성공, **정지 시 odom 89.9Hz 발행**(핸드오프 §4 "정지→미발행" 재현 안 됨), IMU register 경고 없음.

### 5.3 Phase 3 모션 테스트 (사용자, §0.9)
| 지표 | 1차 90fps/method2 | 2차 30fps/method1 |
|---|---|---|
| odom 안정 | 10 s | 82 s |
| jump 누적 | 9 | 1 |
| max 속도 | 189 m/s | 3.1 m/s |
| gravity 정렬 | 미발행 | **확인됨 z=-9.79 \|g\|=9.81 수신1350** |

### 5.4 bag 정밀 분석 (§0.11)
- IMU 6.7 sample/frame, dt std 0.002ms. "Y축 흔들림" = 실제 pitch 정상추적(accel.z↔pitch r=−0.986, 속도정합성으로 병진 배제) + Velocity 마커 저속 노이즈.

### 5.5 검증 결과
| 기준 | 실측 | 판정 |
|---|---|---|
| catastrophic fly-away | 사라짐 | ✅ H1 |
| gravity 정렬 | \|g\|=9.81 수렴 | ✅ Phase3 |
| IMU sample/frame | 6.7 | ✅ |
| 시각화 흔들림 | 코스메틱(실모션+저속노이즈) | ✅ 문제 아님 |

## 6. 최종 결론

### 6.1 오늘 해결된 항목
- **catastrophic fly-away 근본 해결**: H1(타임스탬프 회귀) 한 줄 패치 + 재빌드.
- **IMU 융합 정상 작동**(사용자 목표 달성): Phase3로 중력정렬 수렴(|g|=9.81), 잔여 점프 대폭 감소.

### 6.2 오늘 새로 확인된 항목
- 핸드오프 §3A(IMU 0Hz)는 현 문제와 무관: IMU는 199.7Hz로 건강. extrinsic TF도 정상(§4 "축 문제 아님"은 결과적으로 맞음).
- 시각화 "Y축 흔들림"은 발산이 아니라 **실제 카메라 pitch의 정상 추적 + Velocity 마커 저속 노이즈**(데이터로 가설 2회 정정).

### 6.3 남은 제약 / 주의 (인지해야 할 이슈)
- **fly-away 잔재는 부분적으로 근본 물리**: 단일 D455 VIO는 순수 병진(저excitation) 구간에서 중력-가속 분리가 약함 → 작은 transient는 최소화는 해도 완전 제거 불가.
- **Velocity 트위스트를 다운스트림에서 소비 시** 저속 노이즈 주의(robot_localization twist fusion / 속도 컨트롤러): LPF·속도 게이팅·포즈미분 권장. 포즈/nvblox엔 무관.
- **`rs-imu-calibration`**: 핸드오프 §6은 "불필요"라 했으나, 잔여 틸트 저감엔 accel bias 캘리브가 #1 증폭원 → **재고 권장**.
- Phase3 30fps는 시각 프레임 감소(빠른 모션/모션블러엔 90fps가 유리). A/B 가능하도록 런치 인자화함.
- vo_state=3("invalidated by IMU") 가능성 — 다운스트림 게이팅 시 참고.

### 6.4 다음 분석 우선순위
1. (선택) 수평 슬라이드 좌우 병진만 재캡처 → pitch 평평 확인 시 아티팩트 전무 확정.
2. `rs-imu-calibration` / Kalibr camera-IMU extrinsic+time-offset → 잔여 틸트 추가 저감.
3. nvblox dense 매핑 통합(emitter_on_off 풀스택의 IMU starvation 재점검 — 핸드오프 §4).
4. Nav2/robot_localization 융합 시 pose-jump/outlier 리젝션 + twist LPF 적용.

## 7. 생성 / 수정 파일 목록

### 7.1 수정된 파일
- `/home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/src/impl/visual_slam_impl.cpp` (H1: 688행 `latest_ts`→`imu_ts`)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_visual_slam_realsense.launch.py` (Phase3: stereo_profile/unite_imu_method 인자화, jitter 50.0)

### 7.2 생성된 문서/스크립트
- `/home/jetson/isaac_ws_ros/analyze_axis_flip.py` (bag 분석 스크립트)
- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_06_12.md` (본 세션 명령어 로그)
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_06_12.md` (본 문서)
- `/home/jetson/.claude/plans/noble-greeting-backus.md` (판단·분석·실행 누적 plan)
- 입력: `/home/jetson/isaac_ws_ros/axis_flip_capture/` (사용자 녹화 bag, 35.3s)

---

## 8. 추가 분석 (세션 연속): 이동 시 odom 발산 → imu_noise_scale 해결

> **정정**: §6.1 "근본 해결"은 **완만/제자리 모션 한정**이었음. 이동(병진) 시나리오에서 발산이 재현되어 추가 분석·해결.

### 8.1 증상
- 어느정도 거리를 이동하면 odom이 점점 멀리 날아감(progressive fly-away). vision-only는 동일 이동에서 안정.

### 8.2 분석 (`analyze_drift.py`, bag 78.8s/28.9s)
- **추정기(자세) 런어웨이 — 실제 모션 아님**: 발산구간 IMU 실가속분 ‖a‖−9.81 평균 0.20/최대 2.43 m/s², 자이로 ≤40°/s(완만)인데 odom \|v\| 최대 **429 m/s**, 시작점서 **135 m** 비행.
- **\|g\|=9.81 유지하나 gravity 방향(tilt) 3.7°→89.8° 단조 붕괴** = 자세 추정 드리프트→중력 누설→양의 피드백 폭주.
- IMU 건강(199.7Hz, gap 0). 방아쇠 = **지속 병진**(제자리 틸트는 안정).
- **근본 원인**: IMU 과신. Allan 정적 노이즈값이 동적 미스캘리브(time-offset/extrinsic)를 못 담아 cuVSLAM이 IMU 과신 → 이동 시 적분 폭주. (vision-only 안정 = IMU가 발산 주체.)

### 8.3 해결 검증 (`imu_noise_scale:=5`)
| 지표 | scale=1 (발산) | scale=5 (해결) |
|---|---|---|
| 시작점서 최대거리 | 135 m | **1.67 m** |
| net 변위 | 101 m | 0.68 m |
| odom \|v\| max | 429 m/s | 1.09 m/s |
| gravity tilt max | 89.8° | 41.8° |

→ 런어웨이 소멸. **IMU 과신이 원인임을 확정.** 잔여: 일시 tilt 스파이크(≤42°, 자가복구) + 위치속도 1회 8 m/s.

### 8.4 적용한 변경
- `.../launch/isaac_ros_visual_slam_realsense.launch.py`: `imu_noise_scale` 기본값 **1.0 → 5.0**(검증값). py_compile 통과.

### 8.5 남은 작업 / 다음
- **진짜 수정 = camera-IMU 캘리브**: Kalibr time-offset+extrinsic + rs-imu-calibration. 미스캘리브 제거 시 noise_scale을 다시 낮춰도 안정(IMU 본래 이득 회복). **컨테이너에 rs-imu-calibration·Kalibr 미설치 → 도구 설치 필요**(rs-enumerate-devices/rs-motion만 있음).
- nvblox 매핑 전 캘리브 권장(잔여 ~38° 틸트가 깊이 역투영 시 맵 번짐).

### 8.6 추가 생성/수정 파일
- `/home/jetson/isaac_ws_ros/analyze_drift.py` (생성, 궤적/발산 분석)
- `.../launch/isaac_ros_visual_slam_realsense.launch.py` (수정, imu_noise_scale 기본 5.0)
