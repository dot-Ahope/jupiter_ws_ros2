# 진단 명령어 2026-04-06: VSLAM Covariance Adapter emitter_on_off 대응 개선

## 배경
- 2026-03-30에 cuVSLAM 오도메트리 진단 완료 (emitter_on_off → 64% frozen 확인)
- 2026-04-06에 vslam_covariance_adapter.py 코드 분석 → 4가지 결함 발견 → 수정 구현

---

## Phase 1: Adapter 코드 분석

### 1.1 Adapter 소스 코드 읽기
```bash
cat /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py
```
**목적**: emitter_on_off 환경(25Hz, 64% frozen)에서 adapter 동작 분석
**파일 경로**: `jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py`
**결과**: 4가지 결함 발견
1. Frozen 프레임이 `_check_anomaly()` + `_update_tracking_state()`에 진입하여 상태머신 오염
2. `_check_frozen()`에서 `prev_odom`이 frozen pose로 업데이트되어 unfreeze 시 거짓 anomaly
3. Frozen 시 10Hz로 stale Δ=0 데이터가 EKF에 투입 (covariance 보정 없음)
4. `good_count_threshold=30`이 64% frozen 환경에서 도달 불가능

### 1.2 EKF 설정 확인
```bash
cat /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml
```
**목적**: Adapter 수정 방향 결정을 위한 EKF 설정 확인
**결과**:
```yaml
odom1: visual_slam/tracking/odometry_adapted
odom1_differential: true      # ← 차분 모드: Δ만 사용, 절대 위치 무관
odom1_config: [true, true, false,    # x, y, z
               false, false, false,   # roll, pitch, yaw (미사용)
               true, true, false,     # vx, vy, vz
               false, false, false]   # vroll, vpitch, vyaw (미사용)
odom1_pose_rejection_threshold: 5.0
odom1_twist_rejection_threshold: 3.0
```
- Differential 모드: frozen Δ=0이 "정지" 신호 → wheel/RF2O와 충돌 위험
- Yaw 미사용 → anomaly의 yaw delta는 EKF에 직접 영향 없음

### 1.3 런타임 파라미터 확인 (코드 기본값 vs 실제 설정)
```bash
grep -A 30 'vslam_covariance_adapter' \
  /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
```
**목적**: Launch 파일에서 실제 적용되는 파라미터 확인 (코드 기본값과 다를 수 있음)
**결과** (수정 전):
```python
'frozen_threshold': 60,         # 코드 기본값=30 → launch에서 60으로 오버라이드
'frozen_publish_rate': 10.0,    # 코드 기본값=5.0 → launch에서 10.0으로 오버라이드
'good_count_threshold': 30,     # 코드 기본값=30과 동일
'recovery_delay': 1.0,          # 코드 기본값=1.0과 동일
```
- `frozen_threshold=60`: 25Hz에서 2.4초 후에야 frozen 판정 (너무 느림)
- `frozen_publish_rate=10.0`: frozen 중 10Hz 발행 (EKF에 stale 데이터 과투입)

---

## Phase 2: Adapter 코드 수정

### 2.1 수정 1 — 새 파라미터 선언 추가
```python
# 파일: vslam_covariance_adapter.py
# 위치: frozen_publish_rate 선언 이후

self.declare_parameter('frozen_cov_multiplier', 50.0)   # frozen 발행 시 공분산 증폭 배율
self.declare_parameter('max_robot_speed', 0.5)           # m/s - adaptive delta 계산용
self.declare_parameter('estimated_frame_rate', 25.0)     # Hz - frozen 시간 계산용
```
**목적**: 3가지 새 기능(frozen 공분산 증폭, adaptive delta, frozen 시간 계산)에 필요한 파라미터

### 2.2 수정 2 — 파라미터 로드 추가
```python
# 위치: frozen_publish_rate 로드 이후

self.frozen_cov_multiplier = self.get_parameter('frozen_cov_multiplier').value
self.max_robot_speed = self.get_parameter('max_robot_speed').value
self.estimated_frame_rate = self.get_parameter('estimated_frame_rate').value
```
**목적**: 선언된 파라미터를 인스턴스 변수로 로드

### 2.3 수정 3 — frozen_duration 상태 변수 추가
```python
# 위치: frozen detection 상태 섹션

self.frozen_duration = 0  # 마지막 frozen 기간 (프레임 수, unfreeze 시 adaptive delta용)
```
**목적**: Frozen 기간을 기록하여 unfreeze 직후 adaptive delta 계산에 사용

### 2.4 수정 4 — 시작 로그 확장
```python
# 기존 log에 추가:
f'frozen_cov_mult={self.frozen_cov_multiplier}x\n'
f'  adaptive delta: max_robot_speed={self.max_robot_speed}m/s, '
f'est_frame_rate={self.estimated_frame_rate}Hz\n'
```
**목적**: 노드 시작 시 모든 설정값 로그 출력 (디버깅 용이)

### 2.5 수정 5 — `_check_frozen()` 재작성
```python
def _check_frozen(self, msg: Odometry) -> bool:
    """
    Frozen data detection (2026-03-04, 2026-04-06 prev_odom 보호 추가).
    중요: frozen 판정 시 prev_odom을 업데이트하지 않음.
    """
    ...
    if pos_delta < tolerance and yaw_delta < tolerance:
        self.frozen_count += 1
        # ★ prev_odom 업데이트하지 않음 — frozen pose로 기준점 오염 방지
        return self.frozen_count >= self.frozen_threshold
    else:
        if self.frozen_count >= self.frozen_threshold:
            # ★ frozen 기간 기록 → unfreeze 직후 adaptive delta에 사용
            self.frozen_duration = self.frozen_count
        self.frozen_count = 0
        return False
```
**핵심 변경 2가지**:
1. `prev_odom` 업데이트 제거 — frozen pose로 기준점이 오염되는 것 방지
2. `frozen_duration` 기록 — unfreeze 직후 adaptive delta threshold 계산에 사용

### 2.6 수정 6 — `_check_anomaly()` adaptive delta threshold
```python
# 위치: pos_delta/yaw_delta 계산 이후, anomaly 판정 이전

effective_max_pos = self.max_pos_delta
effective_max_yaw = self.max_yaw_delta
if self.frozen_duration > 0:
    frozen_time_sec = self.frozen_duration / max(self.estimated_frame_rate, 1.0)
    effective_max_pos = self.max_pos_delta + frozen_time_sec * self.max_robot_speed
    effective_max_yaw = self.max_yaw_delta + frozen_time_sec * 1.0  # ~1 rad/s 가정
    self.frozen_duration = 0  # 한 번만 적용

# 이후 effective_max_pos, effective_max_yaw로 anomaly 판정
```
**목적**: Frozen 기간에 비례하여 delta 임계값을 일시적으로 완화
**예시**: 60프레임(2.4초) frozen → effective_max_pos = 0.5 + 2.4 × 0.5 = 1.7m

### 2.7 수정 7 — `odom_callback()` frozen-anomaly 분리
```python
# 수정 전:
is_frozen = self._check_frozen(msg)
is_anomaly = self._check_anomaly(msg)        # ← frozen이어도 실행
self._update_tracking_state(is_anomaly)       # ← frozen이어도 실행

# 수정 후:
is_frozen = self._check_frozen(msg)
if not is_frozen:
    is_anomaly = self._check_anomaly(msg)
    self._update_tracking_state(is_anomaly)
else:
    is_anomaly = False
    self.last_raw_pose = msg.pose.pose  # auto-recovery용 raw pose만 추적
```
**목적**: Frozen 프레임이 anomaly 상태머신에 영향을 주지 않도록 완전 분리

### 2.8 수정 8 — Frozen 발행에 고공분산 적용
```python
# 수정 전 (odom_callback frozen 발행 분기):
self.odom_pub.publish(msg)  # 원본 covariance로 발행

# 수정 후:
self._publish_with_frozen_covariance(msg)  # 공분산 50배 증폭 후 발행
```
**목적**: Frozen 데이터의 EKF 영향 최소화

### 2.9 수정 9 — `_publish_with_frozen_covariance()` 신규 메서드
```python
def _publish_with_frozen_covariance(self, msg: Odometry):
    """Frozen 발행 시 공분산을 frozen_cov_multiplier 배 증가"""
    pose_cov = list(msg.pose.covariance)
    for i, diag_idx in enumerate(self.pose_diag_indices):
        base_cov = max(abs(pose_cov[diag_idx]), self.min_pose_cov_diag[i])
        pose_cov[diag_idx] = base_cov * self.frozen_cov_multiplier
    msg.pose.covariance = pose_cov

    twist_cov = list(msg.twist.covariance)
    for i, diag_idx in enumerate(self.twist_diag_indices):
        base_cov = max(abs(twist_cov[diag_idx]), self.min_twist_cov_diag[i])
        twist_cov[diag_idx] = base_cov * self.frozen_cov_multiplier
    msg.twist.covariance = twist_cov

    self.odom_pub.publish(msg)
    self.published_count += 1
```
**목적**: Frozen 데이터의 공분산을 50배 증폭하여 EKF가 사실상 무시하도록
**효과**: pose covariance 대각 0.05 → 2.5 m² (EKF rejection 영향권)

---

## Phase 3: Launch 파라미터 수정

### 3.1 파라미터 변경 적용
```python
# 파일: jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
# 위치: vslam_covariance_adapter Node parameters

# 변경된 파라미터:
'good_count_threshold': 10,      # 30 → 10 (25Hz에서 달성 가능)
'frozen_threshold': 10,          # 60 → 10 (0.4초 만에 frozen 감지)
'frozen_publish_rate': 1.0,      # 10.0 → 1.0 (stale 데이터 투입 최소화)
'frozen_cov_multiplier': 50.0,   # 신규: frozen 발행 공분산 50배
'max_robot_speed': 0.5,          # 신규: adaptive delta 계산용
'estimated_frame_rate': 25.0,    # 신규: frozen 시간 계산용
'recovery_delay': 0.5,           # 1.0 → 0.5 (good_count 감소 반영)
```
**목적**: emitter_on_off 환경(25Hz, 64% frozen)에 맞게 파라미터 조정

---

## Phase 4: 빌드 및 검증

### 4.1 패키지 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO --symlink-install
```
**목적**: 수정된 adapter + launch 파일 빌드
**결과**:
```
Starting >>> jupiter_nav_VO
Finished <<< jupiter_nav_VO [4.36s]

Summary: 1 package finished [5.09s]
```
- 빌드 성공, 오류 없음

### 4.2 Python 문법 검증
```bash
cd /home/jetson/jupiter_ws_ros2
python3 -c "import py_compile; py_compile.compile(
  'src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py',
  doraise=True)"
```
**목적**: Python 문법 오류 확인
**결과**: `Syntax OK` — 오류 없음

### 4.3 IDE 오류 확인
```bash
# VS Code에서 vslam_covariance_adapter.py 열어 오류/경고 확인
```
**목적**: 정적 분석 오류가 없는지 확인
**결과**: 오류 0건, 경고 0건

---

## Phase 5: 실테스트 (미실행 — 향후 예정)

### 5.1 전체 시스템 기동
```bash
# Terminal 1 (Docker): cuVSLAM + nvblox
cd /home/jetson/isaac_ws_ros/src/isaac_ros_common
./scripts/run_dev.sh
# 컨테이너 내부:
ros2 launch isaac_ros_visual_slam isaac_ros_vslam_nvblox_realsense.launch.py

# Terminal 2 (Host): EKF + Adapter + SLAM Toolbox
cd /home/jetson/jupiter_ws_ros2
source install/setup.bash
source /home/jetson/isaac_ws_ros/install/setup.bash
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py

# Terminal 3 (Host): Nav2
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```
**목적**: 전체 시스템에서 수정된 adapter 동작 확인

### 5.2 Adapter 통계 모니터링
```bash
# Adapter 30초 주기 통계 로그 실시간 확인
ros2 topic echo /rosout --field msg --filter "msg.name=='vslam_covariance_adapter'" | grep Stats
```
**목적**: frozen/anomaly/published 비율이 개선되었는지 확인
**기대 결과**:
- `frozen_pub` 감소 (10Hz→1Hz 효과)
- `frozen_gated` 대폭 증가 (frozen 데이터 대부분 차단)
- `anomaly_gated` 감소 (frozen-anomaly 분리 효과)
- `tracking_ok=True` 유지 기간 증가

### 5.3 EKF 기여도 확인
```bash
# VSLAM adapted odometry 수신 확인
ros2 topic hz /visual_slam/tracking/odometry_adapted --window 10

# EKF 출력과 wheel odometry 비교
ros2 topic echo /odometry/filtered --field pose.pose.position --once
ros2 topic echo /odom --field pose.pose.position --once
```
**목적**: VSLAM이 EKF에 실제 기여하는지 확인

### 5.4 이동 중 adaptive delta 확인
```bash
# 로봇을 수동 조작하면서 adapter 로그 관찰
ros2 run teleop_twist_keyboard teleop_twist_keyboard

# 별도 터미널에서 DEBUG 레벨 로그:
ros2 service call /vslam_covariance_adapter/set_parameters rcl_interfaces/srv/SetParameters \
  "parameters: [{name: 'rcl_log_level', value: {type: 2, integer_value: 10}}]"
```
**목적**: Unfreeze 직후 adaptive delta가 정상 작동하는지 확인
**기대 로그**:
```
[DEBUG] Adaptive delta: frozen=10 frames (0.4s), max_pos=0.70m, max_yaw=23.0°
[INFO] VSLAM unfrozen after 10 identical msgs (0.4s). pos_delta=0.150000m
```

---

# Part 3: VSLAM orient.z CSV 분석 + nvblox 분석 + EKF 센서 확인

## 6. VSLAM orient.z CSV 데이터 분석

### 6.1 CSV 파일 기본 분석
```bash
# 전체 행 수 확인
wc -l plot_data.csv
# 16034 plot_data.csv

# 컬럼 헤더 확인
head -1 plot_data.csv
# __time,/odom.pose.pose.orientation.z,/odom_adapted.pose.pose.orientation.z,
# /odom_rf2o_adapted.pose.pose.orientation.z,/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z

# 각 토픽별 유효 메시지 수 (빈 값 제외)
awk -F',' 'NR>1 && $2!="" {count++} END {print "Wheel:", count}' plot_data.csv
awk -F',' 'NR>1 && $3!="" {count++} END {print "EKF:", count}' plot_data.csv
awk -F',' 'NR>1 && $4!="" {count++} END {print "RF2O:", count}' plot_data.csv
awk -F',' 'NR>1 && $5!="" {count++} END {print "VSLAM:", count}' plot_data.csv
# Wheel: 4743, EKF: 7768, RF2O: 1608, VSLAM: 1914
```

### 6.2 VSLAM orient.z 범위 및 통계
```bash
# VSLAM orient.z 범위 (min/max)
awk -F',' 'NR>1 && $5!="" {if(min=="" || $5<min) min=$5; if(max=="" || $5>max) max=$5} \
  END {print "VSLAM min:", min, "max:", max}' plot_data.csv
# VSLAM min: -0.19782209396362305 max: 0.00044819451868534088

# 4개 토픽 모두 범위 확인
for col in 2 3 4 5; do
  awk -F',' -v c=$col 'NR>1 && $c!="" {if(min=="" || $c<min) min=$c; if(max=="" || $c>max) max=$c} \
    END {print "col"c, "min:", min, "max:", max}' plot_data.csv
done
# col2 (Wheel): min: -0.1524 max: 0.0813
# col3 (EKF):   min: -0.1148 max: 0.0697
# col4 (RF2O):  min: -0.1514 max: 0.0795
# col5 (VSLAM): min: -0.1978 max: 0.0004
```

### 6.3 VSLAM 0값 비율 확인
```bash
# orient.z가 정확히 0인 비율
awk -F',' 'NR>1 && $5!="" {total++; if($5==0 || $5=="0") zero++} \
  END {print "total:", total, "zero:", zero, "pct:", zero/total*100"%"}' plot_data.csv
# total: 1914 zero: 6 pct: 0.313%

# 절대값 0.001 미만 (거의 0)
awk -F',' 'NR>1 && $5!="" {total++; v=$5; if(v<0) v=-v; if(v<0.001) near++} \
  END {print "near_zero:", near, "/", total, "pct:", near/total*100"%"}' plot_data.csv
# near_zero: 1068 / 1914 pct: 55.8%
```

### 6.4 VSLAM 시간 구간별 분석
```bash
# 시간 범위 확인
awk -F',' 'NR>1 && $5!="" {if(first=="") first=$1; last=$1} \
  END {print "first:", first, "last:", last, "duration:", last-first"s"}' plot_data.csv
# duration: ~172초

# 10초 윈도우별 VSLAM 메시지 수 (Hz 분석)
awk -F',' 'NR>1 && $5!="" {
  if(start=="") start=$1;
  t=int(($1-start)/10);
  count[t]++
} END {
  for(i=0; i<=17; i++) printf "%d~%ds: %d msgs (%.1f Hz)\n", i*10, (i+1)*10, count[i]+0, (count[i]+0)/10
}' plot_data.csv

# VSLAM orient.z=0인 시간대 확인
awk -F',' 'NR>1 && $5!="" && ($5==0 || $5=="0") {print $1, $5}' plot_data.csv

# 데이터 공백 구간 찾기 (연속 메시지 간격 > 5초)
awk -F',' 'NR>1 && $5!="" {
  if(prev!="") {gap=$1-prev; if(gap>5) print "GAP:", gap"s at t="$1}
  prev=$1
}' plot_data.csv
# GAP: 17.03s at t=... (45초~62초 공백)
```

### 6.5 VSLAM orient.z 점프 분석
```bash
# 연속 메시지 간 orient.z 변화량 > 0.01 (≈0.57°)
awk -F',' 'NR>1 && $5!="" {
  if(prev!="") {
    delta=$5-prev_v;
    if(delta<0) delta=-delta;
    if(delta>0.01) print "JUMP:", prev_v, "->", $5, "delta:", delta, "at t="$1
  }
  prev=$1; prev_v=$5
}' plot_data.csv
# JUMP: 0.000149 -> -0.143073 delta: 0.143222 at t=117.05  (16.4° 점프)
# JUMP: -0.143073 -> -0.197822 delta: 0.054749 at t=117.94  (6.3° 추가 점프)

# 동결 구간 (연속 동일 값) 최장길이
awk -F',' 'NR>1 && $5!="" {
  if($5==prev) {run++} else {
    if(run>max_run) {max_run=run; max_val=prev}
    run=1
  }
  prev=$5
} END {print "longest frozen run:", max_run, "msgs, value:", max_val}' plot_data.csv
# longest frozen run: ~220 msgs, value: -0.19421...
```
**목적**: cuVSLAM orient.z 추적 실패 패턴 (frozen, 점프, 공백) 정량 확인

### 6.6 4개 센서 동시 비교 (특정 시점)
```bash
# t=117초 부근 (VSLAM 점프 시점) 4개 센서 동시 비교
awk -F',' 'NR>1 {
  t=$1; if(t>=116.5 && t<=118.5) {
    if($2!="") printf "t=%.2f Wheel=%.6f\n", t, $2
    if($3!="") printf "t=%.2f EKF=%.6f\n", t, $3
    if($4!="") printf "t=%.2f RF2O=%.6f\n", t, $4
    if($5!="") printf "t=%.2f VSLAM=%.6f\n", t, $5
  }
}' plot_data.csv | head -40
# t=117.05 VSLAM=0.000149 → t=117.79 VSLAM=-0.143073
# 같은 시점 EKF≈-0.115, Wheel≈-0.122, RF2O≈-0.121
```
**목적**: VSLAM 점프 시 다른 센서와 비교하여 cuVSLAM 과보정 확인

---

## 7. nvblox 메시 지면 관통 원인 진단

### 7.1 TF 퍼블리셔 확인 (base_link → camera_link)
```bash
# 실행 중 TF 퍼블리셔 3곳 확인

# 1) Host URDF 확인
grep -A2 'camera_joint' ~/jupiter_ws_ros2/src/jupiter_description/urdf/jupiter_simple.urdf
# <origin xyz="0.205 0 0.098" rpy="0 0 0"/>

# 2) Host launch static TF 확인
grep -n 'base_to_camera\|base_link.*camera_link' \
  ~/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
# arguments=['0.274', '0', '0.055', '0', '0', '0', 'base_link', 'camera_link']  ← 구 값!

# 3) Docker launch static TF 확인
grep -n 'base_to_camera\|base_link.*camera_link' \
  ~/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py
# arguments=['0.205', '0', '0.098', '0', '0', '0', 'base_link', 'camera_link']  ← 수정됨
```
**목적**: URDF, Host static TF, Docker static TF 3곳의 camera_link TF 값 비교 → Host static TF에서 구 값 발견

### 7.2 URDF TF chain 높이 계산
```bash
# base_joint 높이 (base_footprint → base_link)
grep -A2 'base_joint' ~/jupiter_ws_ros2/src/jupiter_description/urdf/jupiter_simple.urdf
# <origin xyz="0 0 0.08" rpy="0 0 0"/>

# camera_joint 높이 (base_link → camera_link)
grep -A2 'camera_joint' ~/jupiter_ws_ros2/src/jupiter_description/urdf/jupiter_simple.urdf
# <origin xyz="0.205 0 0.098" rpy="0 0 0"/>

# 실제 카메라 높이 = 0.08(base) + 0.098(camera) = 0.178m from ground
```
**목적**: 정확한 TF chain 높이 계산으로 메시 바닥 z 좌표 검증

### 7.3 nvblox 설정 확인 (workspace bounds)
```bash
# nvblox_base.yaml 전체 확인
cat ~/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/config/nvblox/nvblox_base.yaml
# workspace_bounds_type: "unbounded"  ← 높이 제한 없음
# workspace_bounds_min_height_m: -0.5  (unbounded이므로 미사용)
# workspace_bounds_max_height_m: 2.0   (unbounded이므로 미사용)

# nvblox_realsense.yaml 확인
cat ~/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/config/nvblox/specializations/nvblox_realsense.yaml
# esdf_slice_height: 0.0
# esdf_slice_min_height: -0.1
# esdf_slice_max_height: 0.3

# Docker launch에서 nvblox 파라미터 오버라이드 확인
grep -n 'workspace_bounds\|height' \
  ~/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py
# (workspace_bounds는 오버라이드 없음 → unbounded 유지)
```
**목적**: nvblox가 z<0 영역의 메시 생성을 허용하는지 확인 → unbounded이므로 클리핑 없음

### 7.4 Host launch static TF 수정
```python
# 수정 전 (nav2_vslam_fused.launch.py):
tf_base_to_camera = Node(
    package='tf2_ros',
    executable='static_transform_publisher',
    name='base_to_camera_tf',
    arguments=['0.274', '0', '0.055', '0', '0', '0', 'base_link', 'camera_link']
)

# 수정 후:
tf_base_to_camera = Node(
    package='tf2_ros',
    executable='static_transform_publisher',
    name='base_to_camera_tf',
    arguments=['0.205', '0', '0.098', '0', '0', '0', 'base_link', 'camera_link']
)
```
**목적**: Host static TF를 URDF와 동일 값으로 수정 → TF 3중 충돌 해소

---

## 8. EKF orient.z 센서 구성 확인

### 8.1 EKF 설정 파일 분석
```bash
# odom0 (Wheel) 설정
grep -A20 'odom0_config' ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml
# odom0_config: [false, false, false,   # x, y, z
#                false, false, false,    # roll, pitch, yaw
#                true,  true,  false,    # vx, vy
#                false, false, true,     # vyaw ← 활성!
#                false, false, false]

# odom1 (VSLAM) 설정
grep -A20 'odom1_config' ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml
# odom1_config: [true,  true,  false,   # x, y
#                false, false, false,    # yaw ← 비활성
#                true,  true,  false,    # vx, vy
#                false, false, false,    # vyaw ← 비활성
#                false, false, false]

# odom2 (RF2O) 설정
grep -A20 'odom2_config' ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml
# odom2_config: [true,  true,  false,   # x, y
#                false, false, false,    # yaw ← 비활성
#                false, false, false,
#                false, false, false,    # vyaw ← 비활성
#                false, false, false]

# imu0 (IMU) 설정
grep -A20 'imu0_config' ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml
# imu0_config: [false, false, false,
#               false, false, false,    # yaw ← 비활성
#               false, false, false,
#               false, false, true,     # vyaw ← 활성!
#               false, false, false]
```
**결론**: EKF orient.z = **IMU vyaw + Wheel vyaw** 2개 융합 (VSLAM, RF2O 미기여)

---

## 9. RF2O yaw/vyaw 비활성 영향 분석

### 9.1 RF2O covariance adapter 확인
```bash
# rf2o_covariance_adapter.py 핵심 파라미터
grep -n 'rotation_multiplier\|rotation_threshold\|transition_decay' \
  ~/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/rf2o_covariance_adapter.py
# rotation_multiplier: 20.0  (회전 시 position cov 0.5 → 10.0 m²)
# rotation_threshold: 0.15 rad/s
# transition_decay_time: 1.0s
```

### 9.2 Nav2 회전 관련 설정
```bash
# Nav2 controller 설정 확인
grep -A5 'RotationShimController\|RotateToGoal\|min_rotational_vel\|max_rotational_vel' \
  ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
# primary_controller: "dwb_core::DWBLocalPlanner"
# RotateToGoal.slowing_factor: 5.0
# RotateToGoal.lookahead_time: -1.0
# min_vel_theta: 0.0
# max_vel_theta: 0.5
```
**목적**: Nav2 회전 명령이 EKF yaw (IMU+Wheel)만 사용하며 RF2O에 의존하지 않음을 확인

### 9.3 RF2O EKF rejection threshold
```bash
grep 'odom2.*rejection\|pose_rejection\|twist_rejection' \
  ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml
# odom2_pose_rejection_threshold: 5.0
```
**목적**: RF2O의 큰 위치 오차도 EKF rejection threshold로 거부됨을 확인

---

# Part 5: 2차 테스트 데이터 분석 명령어

## 10. CSV 데이터 기본 분석

### 10.1 CSV 구조 및 통계
```bash
# CSV 헤더와 행 수
head -1 plot_data.csv
# elapsed time,timestamp,topic,value
wc -l plot_data.csv
# 22605

# 시간 범위
awk -F',' 'NR>1 && $1!="" {if(first=="") first=$1; last=$1} END {print "duration:", last-first"s"}' plot_data.csv
# duration: 263.968s

# 토픽별 메시지 수
awk -F',' 'NR>1 {topics[$3]++} END {for(t in topics) print t, topics[t]}' plot_data.csv | sort
# /odom.pose.pose.orientation.z 7207
# /odom_adapted.pose.pose.orientation.z 11816
# /odom_rf2o_adapted.pose.pose.orientation.z 2430
# /visual_slam/tracking/odometry_adapted.pose.pose.orientation.z 1151
```

### 10.2 토픽별 통계 (범위, Hz)
```bash
for topic in "/odom.pose.pose.orientation.z" "/odom_adapted.pose.pose.orientation.z" \
  "/odom_rf2o_adapted.pose.pose.orientation.z" \
  "/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z"; do
  awk -F',' -v t="$topic" 'NR>1 && $3==t {
    if(min=="" || $4<min) min=$4; if(max=="" || $4>max) max=$4;
    sum+=$4; count++; if(first=="") first=$1; last=$1
  } END {
    printf "%-60s count=%5d range=[%+.6f ~ %+.6f] dur=%.1fs avg_hz=%.1f\n", \
      t, count, min, max, last-first, count/(last-first)
  }' plot_data.csv
done
# Wheel: 7207, -0.064~+0.156, 27.1Hz
# EKF:  11816, -0.064~+0.119, 44.4Hz
# RF2O:  2430, -0.067~+0.158,  9.1Hz
# VSLAM: 1151, -0.000~+0.183,  4.5Hz
```

### 10.3 고유값 수 확인 (Frozen 감지)
```bash
for topic in "/odom.pose.pose.orientation.z" "/odom_adapted.pose.pose.orientation.z" \
  "/odom_rf2o_adapted.pose.pose.orientation.z" \
  "/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z"; do
  count=$(awk -F',' -v t="$topic" 'NR>1 && $3==t {print $4}' plot_data.csv | sort -u | wc -l)
  echo "$topic: $count unique values"
done
# Wheel: 7154, EKF: 153(!), RF2O: 1147, VSLAM: 934
```
**목적**: EKF가 153개 고유값만 → 정지 시 완전히 동일한 값 출력 (2회 회전 이벤트만 변화)

---

## 11. 4개 센서 동시 비교

### 11.1 10초 간격 비교 테이블
```bash
awk -F',' 'NR>1 {
  if(start=="") start=$1;
  t=$1-start; bin=int(t/10);
  topic=$3; val=$4;
  if(topic=="/odom.pose.pose.orientation.z") {if(bin!=wb[bin]) {wheel[bin]=val; wb[bin]=bin}}
  if(topic=="/odom_adapted.pose.pose.orientation.z") {if(bin!=eb[bin]) {ekf[bin]=val; eb[bin]=bin}}
  if(topic=="/odom_rf2o_adapted.pose.pose.orientation.z") {if(bin!=rb[bin]) {rf2o[bin]=val; rb[bin]=bin}}
  if(topic=="/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z") {if(bin!=vb[bin]) {vslam[bin]=val; vb[bin]=bin}}
  if(bin>maxbin) maxbin=bin
} END {
  printf "%5s %10s %10s %10s %10s\n", "t(s)", "Wheel", "EKF", "RF2O", "VSLAM"
  for(i=0; i<=maxbin; i++) {
    printf "%3d~%d %+10.5f %+10.5f %+10.5f %+10.5f\n", i*10, (i+1)*10, \
      wheel[i]+0, ekf[i]+0, rf2o[i]+0, vslam[i]+0
  }
}' plot_data.csv
```
**목적**: 4개 센서 orient.z 궤적을 한눈에 비교 → Wheel/RF2O 유사, EKF 과소추정, VSLAM 이탈 확인

### 11.2 회전 후 안정값 비교
```bash
# 회전 1 후 (t=104~106s)
for topic in "/odom.pose.pose.orientation.z" "/odom_adapted.pose.pose.orientation.z" \
  "/odom_rf2o_adapted.pose.pose.orientation.z" \
  "/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z"; do
  awk -F',' -v t="$topic" 'NR>1 && $3==t {
    if(start=="") start=$1; elapsed=$1-start;
    if(elapsed>=104 && elapsed<=106) {val=$4; count++; sum+=val}
  } END {if(count>0) printf "%-60s avg=%+.6f\n", t, sum/count}' plot_data.csv
done
# Wheel: +0.154750, EKF: +0.119226, RF2O: +0.148635, VSLAM: +0.000552

# 회전 2 후 (t=185~200s)
# 동일 구조, elapsed 범위만 185~200으로 변경
# Wheel: -0.058386, EKF: -0.063777, RF2O: -0.057898, VSLAM: +0.182580
```
**목적**: 센서별 절대 정확도 비교 → EKF = Wheel의 76% 수준 (회전 과소추정)

---

## 12. VSLAM 상세 분석

### 12.1 VSLAM 10초 윈도우 Hz
```bash
awk -F',' '$3=="/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z" {
  if(start=="") start=$1; bin=int(($1-start)/10); count[bin]++; if(bin>maxt) maxt=bin
} END {
  for(i=0; i<=maxt; i++) printf "%3d~%3ds: %4d msgs (%5.1f Hz)\n", i*10, (i+1)*10, count[i]+0, (count[i]+0)/10
}' plot_data.csv
```

### 12.2 VSLAM 점프 상세 (t=117s 전후)
```bash
awk -F',' '$3=="/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z" {
  if(start=="") start=$1; t=$1-start;
  if(t>=115 && t<=125) printf "t=%7.3f val=%+.8f\n", t, $4
}' plot_data.csv
# t=116.920 val=+0.00055187  ← frozen
# t=117.203 val=+0.18252511  ← 20.9° 점프 (0.283초 만에)
```

### 12.3 VSLAM 활성 기간 상세 (t=40~65s)
```bash
awk -F',' '$3=="/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z" {
  if(start=="") start=$1; t=$1-start;
  if(t>=40 && t<=65) printf "t=%7.3f val=%+.8f gap=%.3fs\n", t, $4, (prev_t!=""?t-prev_t:0)
  prev_t=t
}' plot_data.csv | head -40
```

### 12.4 VSLAM 데이터 공백 확인
```bash
awk -F',' '$3=="/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z" {
  if(start=="") start=$1; t=$1-start;
  if(prev_t!="") {gap=t-prev_t; if(gap>2.0) printf "GAP: %.1fs at t=%.1f~%.1f\n", gap, prev_t, t}
  prev_t=t
}' plot_data.csv
# GAP: 13.1s at t=61.2~74.3
# GAP: 12.8s at t=122.7~135.5
# GAP: 11.4s at t=139.0~150.3
# GAP: 5.4s at t=154.1~159.5
```

---

## 13. EKF yaw 과소추정 분석

### 13.1 EKF 전환 시점 분석
```bash
# EKF orient.z 변화 > 0.0001인 시점
awk -F',' '$3=="/odom_adapted.pose.pose.orientation.z" {
  if(start=="") start=$1; t=$1-start;
  if(prev!="" && ($4-prev > 0.0001 || $4-prev < -0.0001)) {
    printf "t=%7.2f: %+.6f -> %+.6f (delta=%+.6f)\n", t, prev, $4, $4-prev
  }
  prev=$4
}' plot_data.csv | head -5
# t=102.40: +0.000000 -> +0.000609 (EKF 시작 반응)
# t=102.41: +0.000609 -> +0.001206
# ...
```
**결과**: EKF는 t=102.4~103.0 (0.6초)에 걸쳐 0→+0.119 전환 (약 45 단계)
**목적**: IMU+Wheel vyaw 융합이 Wheel 단독(+0.155)보다 23% 과소추정 확인

### 13.2 Wheel 회전 시작 정확한 시간
```bash
awk -F',' '$3=="/odom.pose.pose.orientation.z" {
  if(start=="") start=$1; t=$1-start;
  if(t>=100 && t<=105) {
    if(prev!="") {d=$4-prev; if(d>0.001||d<-0.001) printf "t=%7.3f %+.6f delta=%+.6f <-- ROTATION\n", t, $4, d}
    prev=$4
  }
}' plot_data.csv
# t=102.409 +0.003283 delta=+0.002162 <-- ROTATION 시작
# ...
# t=103.899 +0.154717    <-- ROTATION 종료
# 회전 시간: 1.49초, 회전량: +0.157 (9.0°)
```

---

## 14. RF2O yaw 융합 평가 분석

### 14.1 정적 구간 드리프트 비교
```bash
for topic in "/odom.pose.pose.orientation.z" "/odom_adapted.pose.pose.orientation.z" \
  "/odom_rf2o_adapted.pose.pose.orientation.z"; do
  awk -F',' -v t="$topic" 'NR>1 && $3==t {
    if(start=="") start=$1; elapsed=$1-start;
    if(elapsed>=200 && elapsed<=265) {
      if(min=="" || $4<min) min=$4; if(max=="" || $4>max) max=$4;
      count++; sum+=$4; if(first_v=="") first_v=$4; last_v=$4;
      if(first_t=="") first_t=elapsed; last_t=elapsed
    }
  } END {
    printf "%-55s mean=%+.6f range=%.6f drift=%.6f/60s\n", \
      t, sum/count, max-min, (last_v-first_v)/(last_t-first_t)*60
  }' plot_data.csv
done
# Wheel: range=0.005887 drift=-0.003217/60s
# EKF:   range=0.000000 drift= 0.000000/60s  ← 완벽 안정
# RF2O:  range=0.007035 drift=-0.005609/60s  ← Wheel 대비 1.7x 드리프트
```

### 14.2 RF2O vs Wheel 시간별 차이
```bash
awk -F',' 'NR>1 {
  if(start=="") start=$1; t=$1-start; bin=int(t/10);
  if($3=="/odom.pose.pose.orientation.z") {wval[bin]=$4}
  if($3=="/odom_rf2o_adapted.pose.pose.orientation.z") {rval[bin]=$4}
  if(bin>maxbin) maxbin=bin
} END {
  printf "%5s %10s %10s %10s\n", "t", "Wheel", "RF2O", "diff"
  for(i=0; i<=maxbin; i++) {
    if(wval[i]!="" && rval[i]!="") {
      d=rval[i]-wval[i]
      printf "%3d~%d %+10.6f %+10.6f %+10.6f %s\n", i*10, (i+1)*10, wval[i], rval[i], d, (d>0.003||d<-0.003?"⚠":"")
    }
  }
}' plot_data.csv
# 대부분 ±0.003 이내, t=230~270에서 RF2O가 Wheel 대비 -0.003~-0.004 드리프트 ⚠
```
**목적**: RF2O yaw가 시간 경과에 따라 Wheel 대비 음의 방향으로 누적 드리프트 확인

---

## 15. cuVSLAM ground constraint 파라미터 확인

```bash
# cuVSLAM ground constraint 파라미터 소스 확인
grep -rn "ground_constraint\|enable_ground\|planar_constraint" \
  ~/isaac_ws_ros/src/isaac_ros_visual_slam/ \
  --include="*.py" --include="*.yaml" --include="*.hpp" --include="*.cpp"

# 핵심 결과:
# visual_slam_node.cpp L57-60:
#   enable_ground_constraint_in_odometry (default: false)
#   enable_ground_constraint_in_slam (default: false)
#
# isaac_ros_visual_slam_hawk.launch.py L115-116:
#   'enable_ground_constraint_in_odometry': True,  ← Hawk 예제에서 활성
#   'enable_ground_constraint_in_slam': False,
#
# visual_slam_impl.cpp L399:
#   configuration.planar_constraints = node.enable_ground_constraint_in_slam_ ? 1 : 0;

# 현재 realsense launch에서 미설정:
grep "ground_constraint" ~/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py
# (결과 없음 → 기본값 False 사용)
```
**목적**: cuVSLAM에 바닥 로봇용 2D 구속 파라미터가 존재하나 미활성 상태 확인 → VSLAM 정확도 + 메시 z 오류 동시 개선 가능

---

## 수정 파일 요약 (전체)

| 파일 | 변경 수 | 내용 |
|------|---------|------|
| `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py` | 9블록 | 파라미터 3개 추가, _check_frozen 재작성, adaptive delta, odom_callback frozen-anomaly 분리, _publish_with_frozen_covariance 신규 |
| `src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` | 2블록 | Adapter 파라미터 4개 변경 + 3개 추가 / **static TF 수정 (0.274,0,0.055 → 0.205,0,0.098)** |
| `isaac_ws_ros/.../isaac_ros_vslam_nvblox_realsense.launch.py` | — | Docker static TF 수정 (사용자 직접 수정) |

---

# Part 6: 패키지 분석 + 권장 수정 적용 명령어

## 16. Isaac VSLAM / nvblox ↔ robot_localization 상호작용 분석

### 16.1 cuVSLAM 소스 분석 명령어

```bash
# cuVSLAM이 robot_localization 관련 패키지를 참조하는지 확인
grep -r "robot_localization\|ekf\|ukf" ~/isaac_ws_ros/src/isaac_ros_visual_slam/ --include="*.cpp" --include="*.hpp" --include="*.py" --include="*.xml" -l
# (결과 없음)

# cuVSLAM TF 퍼블리시 코드 확인
grep -n "publish_map_to_odom_tf\|publish_odom_to_base_tf\|PublishFrameTransform" \
  ~/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/src/impl/visual_slam_impl.cpp

# cuVSLAM 오도메트리 출력 토픽 확인
grep -n "tracking/odometry\|odom_publisher" \
  ~/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/src/impl/visual_slam_impl.cpp

# cuVSLAM TF listener (카메라 외부 파라미터 획득)
grep -n "TransformListener\|lookupTransform\|tf_buffer" \
  ~/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/src/impl/visual_slam_impl.cpp
```

### 16.2 nvblox 소스 분석 명령어

```bash
# nvblox가 robot_localization/EKF 관련 참조하는지 확인
grep -r "robot_localization\|ekf\|ukf\|odom_adapted\|odom_rf2o" ~/isaac_ws_ros/src/isaac_ros_nvblox/ --include="*.cpp" --include="*.hpp" --include="*.py" --include="*.xml" -l
# (결과 없음)

# nvblox Transformer — TF vs Topic 모드
grep -n "use_tf_transforms\|use_topic_transforms\|lookupTransformToGlobalFrame" \
  ~/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/transformer.cpp

# nvblox global_frame 설정 확인
grep -n "global_frame\|pose_frame\|map_clearing_frame" \
  ~/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/include/nvblox_ros/node_params.hpp

# NvbloxCostmapLayer TF lookup
grep -n "lookupTransform\|nav2_costmap_global_frame" \
  ~/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_nav2/src/nvblox_costmap_layer.cpp
```

### 16.3 현재 launch 설정 확인

```bash
# cuVSLAM TF 퍼블리시 설정 확인
grep -n "publish_map_to_odom\|publish_odom_to_base" \
  ~/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py

# nvblox global_frame 확인
grep -n "global_frame\|use_tf_transforms" \
  ~/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/config/nvblox/nvblox_base.yaml
```

**결론:** cuVSLAM과 nvblox 모두 `robot_localization`에 대한 직접적 의존성 없음. 간접 상호작용은 TF tree를 통해서만 발생.

## 17. 권장 수정 적용 (4건)

### 17.1 image_jitter_threshold_ms: 68 → 100

```bash
# 파일: Docker 내부 launch
# ~/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py

# 변경 전:
#   'image_jitter_threshold_ms': 68.00,
# 변경 후:
#   'image_jitter_threshold_ms': 100.0,

# 확인:
grep "image_jitter_threshold" ~/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py
```

### 17.2 nvblox workspace_bounds: height_bounds

```bash
# 동일 파일, nvblox_node 파라미터에 추가:

# 추가 내용:
#   'static_mapper.workspace_bounds_type': 'height_bounds',
#   'static_mapper.workspace_bounds_min_height_m': -0.02,
#   'static_mapper.workspace_bounds_max_height_m': 1.5,

# 확인:
grep "workspace_bounds" ~/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py
```

### 17.3 Adaptive delta cap

```bash
# 파일: vslam_covariance_adapter.py
# ~/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py

# _check_anomaly() 메서드 내, effective_max_yaw/pos 계산 후 추가:
#   MAX_ADAPTIVE_YAW = 0.5   # ~28.6°
#   MAX_ADAPTIVE_POS = 2.0   # 2m
#   effective_max_yaw = min(effective_max_yaw, MAX_ADAPTIVE_YAW)
#   effective_max_pos = min(effective_max_pos, MAX_ADAPTIVE_POS)

# 확인:
grep -n "MAX_ADAPTIVE" ~/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py
```

### 17.4 IMU gyro_cov: 0.001 → 0.005

```bash
# 파일: jupiter_driver_compensated.py
# ~/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py

# 변경 전:
#   gyro_cov = 0.001  # 각속도 측정 불확실성 (rad/s)^2
# 변경 후:
#   gyro_cov = 0.005  # 각속도 측정 불확실성 (rad/s)^2 — 0.001→0.005: Wheel vyaw 비중 ↑

# 확인:
grep "gyro_cov" ~/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```

### 17.5 빌드

```bash
cd ~/jupiter_ws_ros2
source /opt/ros/humble/setup.bash
colcon build --packages-select jupiter_nav_VO jupiter_bringup --symlink-install

# 결과: Summary: 2 packages finished [8.92s]
```

### 17.6 enable_ground_constraint_in_odometry — 제외

```bash
# 야외 주행이 최종 목표이므로 적용하지 않음
# cuVSLAM 파라미터 기본값 확인:
grep -n "enable_ground_constraint" ~/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/include/isaac_ros_visual_slam/visual_slam_node.hpp
# enable_ground_constraint_in_odometry: false (기본)
# enable_ground_constraint_in_slam: false (기본)
```

## 수정 파일 요약 (Part 6 포함 전체)

| 파일 | 변경 수 | 내용 | 파트 |
|------|---------|------|------|
| `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py` | 9블록+1 | 기존 9블록 + **adaptive delta cap** | Part 1+6 |
| `src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` | 2블록 | Adapter 파라미터 + static TF 수정 | Part 1+4 |
| `isaac_ws_ros/.../isaac_ros_vslam_nvblox_realsense.launch.py` | 3건 | Docker static TF + **jitter 100ms** + **nvblox height_bounds** | Part 4+6 |
| `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` | 1건 | **IMU gyro_cov 0.001→0.005** | Part 6 |

### 미적용 항목

| 파일 | 항목 | 상태 | 사유 |
|------|------|------|------|
| `isaac_ros_vslam_nvblox_realsense.launch.py` | `enable_ground_constraint_in_odometry: True` | **제외** | 야외 주행 목표 |
| `jupiter_driver_compensated.py` | IMU gyro 스케일 재측정 (1.025→~1.2) | **차기** | 실측 필요 |

---

# Part 7: RTK GPS 융합 + 휠 슬립 분석 명령어

## 18. RTK GPS 융합 — 생성/수정된 파일

### 18.1 신규 파일 생성

```bash
# 1. GNSS 품질 필터 노드
cat ~/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/gnss_quality_filter.py
# 역할: /fix → 품질 판정 → /fix_filtered (NO_FIX/POOR 게이트아웃)

# 2. NavSat Transform 설정
cat ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/navsat_params.yaml
# 핵심: use_odometry_yaw=true (자력계 없음), zero_altitude=true (2D)
```

### 18.2 EKF 설정 — odom3 GPS 추가

```bash
# 파일: ekf_vslam_fusion.yaml
# 추가 위치: imu0 섹션 뒤, process_noise 앞

# 추가 내용:
#   odom3: /odometry/gps
#   odom3_config: [true, true, false, ...]  ← x, y만
#   odom3_differential: false
#   odom3_pose_rejection_threshold: 10.0

# 확인:
grep -A3 "odom3:" ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml
```

### 18.3 Launch 파일 — GNSS 섹션 추가

```bash
# 파일: nav2_vslam_fused.launch.py
# 추가 내용:
#   - use_gnss launch argument (default: true)
#   - tf_base_to_gps: base_link → gps_link (0,0,0.15 임시)
#   - zed_f9p_node: RTK GPS 드라이버
#   - gnss_quality_filter: 품질 필터
#   - navsat_transform: WGS84→odom 변환 (4초 지연)
#   - Foxglove whitelist: /fix, /fix_filtered, /odometry/gps, /gps/filtered

# 확인:
grep "use_gnss\|zed_f9p\|gnss_quality\|navsat_transform\|tf_base_to_gps" \
  ~/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
```

### 18.4 setup.py — entry point 추가

```bash
# 추가:
#   'gnss_quality_filter = jupiter_nav_VO.gnss_quality_filter:main',

grep "gnss_quality" ~/jupiter_ws_ros2/src/jupiter_nav_VO/setup.py
```

### 18.5 빌드

```bash
cd ~/jupiter_ws_ros2
source /opt/ros/humble/setup.bash
colcon build --packages-select jupiter_nav_VO --symlink-install

# 결과: Summary: 1 package finished [4.41s]
```

### 18.6 실행 명령어

```bash
# GPS 포함 실행 (기본)
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py

# GPS 없이 실행 (실내 전용 테스트)
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py use_gnss:=false

# GPS 품질 모니터링
ros2 topic echo /fix --field status.status  # -1=NO_FIX, 0=FIX, 2=GBAS
ros2 topic echo /fix --field position_covariance  # [cov_x, 0, 0, 0, cov_y, ...]
ros2 topic echo /fix_filtered  # 필터 통과한 GPS만
ros2 topic echo /odometry/gps  # odom 프레임 GPS 위치
ros2 topic hz /odometry/gps    # GPS odom 발행 빈도 (실외에서만)
```

### 18.7 GPS 안테나 TF 실측 방법

```bash
# 현재 임시 TF: base_link → gps_link = (0, 0, 0.15)
# GPS 안테나 실제 위치를 측정하여 launch 파일 수정:
# nav2_vslam_fused.launch.py의 tf_base_to_gps 노드에서:
#   arguments=['X', 'Y', 'Z', '0', '0', '0', 'base_link', 'gps_link']
# X = 앞방향(m), Y = 왼쪽(m), Z = 위(m) — base_link 기준
```

## 19. 휠 슬립 문제 진단

### 19.1 현상 재현 및 확인

```bash
# 1. 로봇 후진 명령
ros2 topic pub /cmd_vel geometry_msgs/Twist '{linear: {x: -0.2}}' -1

# 2. 발로 로봇 막기 (물리적 차단)

# 3. 센서별 상태 확인
# Wheel odom: vx가 -0.2 보고 (바퀴 공회전)
ros2 topic echo /odom_adapted --field twist.twist.linear.x

# VSLAM: 실제 이동 없으므로 Δx ≈ 0
ros2 topic echo /visual_slam/tracking/odometry_adapted --field twist.twist.linear.x

# RF2O: 스캔 변화 없으므로 Δx ≈ 0
ros2 topic echo /odom_rf2o_adapted --field pose.pose.position.x

# EKF 출력: wheel에 의해 x가 감소하면 → 슬립 영향 확인
ros2 topic echo /odom --field pose.pose.position.x

# IMU 가속도: 로봇 정지 시 ax ≈ 0 (또는 바이어스~0.11)
ros2 topic echo /imu/data_calibrated --field linear_acceleration.x
```

### 19.2 즉시 완화 (파라미터 조정만)

```bash
# 방법 1: Wheel vx 공분산 증가 (EKF가 wheel을 덜 신뢰)
# nav2_vslam_fused.launch.py의 odom_covariance_adapter 파라미터:
#   'base_linear_cov': 0.005 → 0.02

# 방법 2: RF2O 직진 시 위치 보정력 증가
# rf2o_covariance_adapter 파라미터:
#   'position_cov_base': 0.5 → 0.2 (직진 시)
```

## 수정 파일 요약 (Part 7 포함 전체)

| 파일 | 변경 수 | 내용 | 파트 |
|------|---------|------|------|
| `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py` | 9블록+1 | 기존 + adaptive delta cap | Part 1+6 |
| `src/jupiter_nav_VO/jupiter_nav_VO/gnss_quality_filter.py` | **신규** | GNSS 품질 필터 노드 | Part 7 |
| `src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` | 5건 | Adapter + TF + **RTK GPS 섹션** | Part 1+4+7 |
| `src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml` | 1섹션 | **odom3 GPS 추가** | Part 7 |
| `src/jupiter_nav_VO/config/navsat_params.yaml` | **신규** | navsat_transform 설정 | Part 7 |
| `src/jupiter_nav_VO/setup.py` | 1행 | gnss_quality_filter entry point | Part 7 |
| `isaac_ws_ros/.../isaac_ros_vslam_nvblox_realsense.launch.py` | 3건 | Docker jitter+nvblox | Part 4+6 |
| `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` | 1건 | gyro_cov 0.005 | Part 6 |

### 미적용 항목

| 항목 | 상태 | 사유 |
|------|------|------|
| `enable_ground_constraint_in_odometry: True` | **제외** | 야외 주행 목표 |
| IMU gyro 스케일 재측정 | **차기** | 실측 필요 |
| 휠 슬립 감지 IMU 기반 구현 | **차기** | 설계 완료, `odom_covariance_adapter.py` 수정 대기 |
| base_link→gps_link TF 실측 | **차기** | GPS 안테나 위치 측정 필요 |
