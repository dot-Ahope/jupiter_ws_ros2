# 진행과정 2026-04-30: Tier 2.5 collision_monitor 도입 + lethal-zone deadlock 새 변종 해결 + 회전 데드존 + path 추종 보강

## 1. 목표

### 1.1 사용자 요청 (시간순)

1. > "jupiter_nvblox v2 검증 / loop jupiter_nvblox 테스트"
2. > "cost map의 가지 못하는 지점에 닿을 시 멈추는 문제 발생"
3. > "현재 방식에서는 후진을 해야 정상적인가?"
4. > (lethal 도달 시 cmd_vel 데이터 첨부) "lethal에 걸린 상황에서 cmd_vel 값이다. nav2도 여러가지 명령이 나오고 있는데, 로버는 전혀 움직이지 않고 있다. 로그도 살펴보고, 코드베이스도 찾아보면서 원인을 찾아라."
5. > "driver 실행 상태 만듦, 구 joy 종료. 현재 lethal 영역에 로버 존재. 로버 nav2 명령에도 불구하고 움직이지 않음. 다시 분석."
6. > "진행하라" (옵션 A — stuck_detector subscription 변경)
7. > "현재 움직이지 못하는 상황이다. 토픽들을 살펴봐라"
8. > (cmd_vel 시퀀스 첨부) "현재 lethal에 도달했을때 움직임. 뒤로 1초동안 가기는 하는데, 다시 전진을 해버리면서, hethal에 도달, 그 후 후진을 다시 하고, path를 따라가려고 하는데 lethal에 도달. 이걸 무한 반복중. … z값이 출력되기는 하지만, 회전하는 모습을 보여주지는 못한다. 데드존 문제일 수도 있을 것 같다."
9. > "조금씩 오른쪽으로 움직이다가, 결국 무한루프에서 탈출했다."
10. > "수행하라. 그리고 또한 path를 정확하게 따라갈 수 있게 만들어달라. path를 벗어나 경향성만 따라가는 경우가 자주 나타난다. 그리고 지금까지 한 것들을 정리하라."

### 1.2 이전 세션 요약 / 연관 작업

- 2026-04-21~28: jupiter_safety 패키지 (Tier 2) 신설 완료 — stuck_detector + forced_recovery + alert_publisher + twist_mux.
- 2026-04-29: nvblox 튜닝 (esdf_slice_max_height 1.2→0.65), inflation_radius 분리 (local 0.20, global 0.25), PoseProgressChecker (5cm OR 0.05rad/20s), DWB BackUp acceleration_limit 0.20/-0.20, simulate_ahead_time 2.0→0.5, BaseObstacle 0.20→0.10.
- 2026-04-30 (오늘 이전 세션): Tier 2.5 nav2_collision_monitor 도입 plan + 1차 적용 — twist_mux 이후 배치 → forced_recovery 차단 deadlock → 재배치하여 nav2 stream 만 gate 하도록 수정 (v1 → v2). 본 세션에서 v2 동작 검증 후 다른 deadlock 변종 발견.

이전 세션 plan 파일: `/home/jetson/.claude/plans/functional-sleeping-starfish.md` (Tier 2.5 → Tier 3 로드맵, 본 세션은 Phase A 실행).

## 2. 대상 시스템 구성

### Launch 통합 (변경 없음)
- `nav2_vslam_fused.launch.py` — 호스트 ROS2 풀스택. driver + EKF + SLAM + safety_layer (collision_monitor 포함) + nav2 + RTK + foxglove.
- `nav2_fused_navigation.launch.py` — nav2 단독. 위 launch 가 prerequisite.

### 본 세션 관련 노드
- `jupiter_driver` — MCU 시리얼 통신, /cmd_vel_safe 구독 → MCU set_car_motion.
- `velocity_smoother` — controller_server 출력을 smooth → /cmd_vel.
- `behavior_server` — BT recovery (Spin/BackUp/Wait) → /cmd_vel.
- `collision_monitor` — /cmd_vel 구독 → polygon trip 시 0, 정상 시 통과 → /cmd_vel_nav_filtered.
- `twist_mux` — /cmd_vel_joy(100) > /cmd_vel_force(80) > /cmd_vel_nav_filtered(50) → /cmd_vel_safe.
- `stuck_detector` — /cmd_vel(수정 후) + /jupiter/get_vel + /jupiter/imu → /stuck_status.
- `forced_recovery` — /stuck_status + /scan_merged → /cmd_vel_force.

### cmd_vel 파이프라인 (Tier 2.5 v2)
```
nav2 controller → velocity_smoother → /cmd_vel ─┐
behavior_server (BackUp/Spin) ─────────────────┼→ collision_monitor → /cmd_vel_nav_filtered ─┐
                                                                                               ├→ twist_mux → /cmd_vel_safe → driver
joy → /cmd_vel_joy ────────────────────────────────────────────────────────────────────────────┤
forced_recovery → /cmd_vel_force ──────────────────────────────────────────────────────────────┘
```

## 3. 문제 분석

### 3.1 [최초 진단] cmd_vel 출력은 있는데 로버 미동

**증상**: 사용자가 cmd_vel echo 출력을 보여줌 (-0.10 / 0.0 패턴). nav2 가 명령을 내고 있다 인지함. 하지만 로버 미동.

**초기 의심**: forced_recovery 의 backup 명령이 driver 까지 안 가는 것 추정.

**데이터로 확인**:
- `ros2 node list` → `/joy_node`, `/jupiter_joy`, `/jupiter_nvblox_container`, `/nvblox_node`, `/rviz` 만 활성. driver / nav2 / safety_layer 전부 부재.
- `ros2 topic info /cmd_vel -v` → Publisher 1 (jupiter_joy), Subscription 0.
- `head -30 launch.log` → "=== Nav2 Navigation (Fused Odometry Mode) ===, Ensure nav2_vslam_fused.launch.py is running first!" — nav2-only launch 만 실행, prerequisite 누락.

**근본 원인**: 잘못된 launch 사용. `nav2_fused_navigation.launch.py` 만 실행 → driver 미기동 → /cmd_vel subscriber 0. 사용자가 본 -0.10 패턴은 forced_recovery 가 아니라 **레거시 `jupiter_joy`** 의 /cmd_vel 직접 publish 였음 (jupiter_joy_new 가 아닌 구 노드, joystick 입력 그대로).

**조치**: 사용자에게 `nav2_vslam_fused.launch.py` 우선 실행 + 구 jupiter_joy 종료 안내. 사용자 적용 후 정상화.

### 3.2 [본 분석] lethal-zone 진입 시 nav2 명령에도 미동 — 새 deadlock 변종

**증상**: driver 정상 기동 후, 로버가 lethal 인근에서 nav2 명령 (linear.x=0.158, angular.z=0.32) 받지만 미동.

**초기 의심**: 이전에 해결한 "collision_monitor 가 forced_recovery 차단" deadlock 의 재발.

**데이터로 확인**:
- `/cmd_vel` echo: linear.x=0.158, angular.z=0.32 (nav2 controller 정상)
- `/cmd_vel_nav_filtered` echo: 0, 0, 0 (collision_monitor PolygonStop trip)
- `/cmd_vel_safe` echo: timeout/무발행 (twist_mux 의 nav 입력이 0 timeout)
- `/stuck_status` echo: `data: false` ← **여기가 이상함**. lethal 인데 stuck 검출 안 됨.

**stuck_detector 코드 확인** ([stuck_detector.py:69](src/jupiter_safety/jupiter_safety/stuck_detector.py#L69)):
```python
self.create_subscription(Twist, '/cmd_vel_safe', self._cb_cmd, 10)
```
→ /cmd_vel_safe (twist_mux 출력, gating 후) 만 구독.

**behavior_server 로그 확인**:
```
Running spin → Exceeded time allowance before reaching the Spin goal - Exiting Spin
Running backup → Exceeded time allowance before reaching the DriveOnHeading goal - Exiting DriveOnHeading
Running wait → completed successfully
```
즉 nav2 BT 의 recovery (Spin, BackUp) 모두 timeout 으로 실패. behavior_server 도 /cmd_vel 에 publish (4 publisher 중 3 개) → collision_monitor 가 같이 차단 → 회전/후진 명령도 driver 까지 안 감.

**근본 원인**:
1. nav2 의 모든 명령 (controller_server 의 path follow + behavior_server 의 BackUp/Spin 모두) 은 `/cmd_vel` 토픽 사용.
2. collision_monitor 가 `/cmd_vel` 구독 → polygon trip 시 0 출력 → behavior_server 의 recovery 도 같이 gate 됨.
3. twist_mux 출력 = 0 → driver 모터 정지.
4. stuck_detector 가 /cmd_vel_safe 만 봐서 |cmd|=0 → "stuck 아님" 판정 → forced_recovery 미발동.
5. 결과적으로 robot 이 nav2 forward 의도에도 불구하고 정지, 어떠한 recovery 도 작동 못함 → **무한 deadlock**.

이전 deadlock 의 정확한 거울. 이전엔 forced_recovery 의 후진이 막혔는데, 이번엔 forced_recovery 자체가 trigger 되지 않음.

**보조 원인**: 모터 교체 후 ANGULAR_SCALE_FACTOR=1.0 + MCU_MIN_ANGULAR=0 로 재캘리브레이션 안된 상태 — 회전 명령 보상 가능 영역 좁음 (3.4 항 이슈와 연결).

### 3.3 [세부 이슈] /scan_merged QoS 불일치로 forced_recovery 의 후방 검사 영구 fail

**증상**: stuck_detector 토픽 변경 후 stuck=True 정상 검출, 그러나 forced_recovery 가 backup 명령을 만들지 않음. /cmd_vel_force 무발행, /cmd_vel_safe 도 0.

**forced_recovery 로그 확인**:
```
[WARN] rear safety: no /scan_merged data → conservative SKIP backup
[ERROR] STUCK detected but rear NOT safe — skipping backup. Operator intervention required.
(2 초 간격으로 무한 반복)
```

**topic info 확인** (`ros2 topic info /scan_merged -v`):
```
scan_merger        PUBLISHER     BEST_EFFORT / VOLATILE
local_costmap      SUBSCRIPTION  BEST_EFFORT / VOLATILE  ← 데이터 받음
collision_monitor  SUBSCRIPTION  BEST_EFFORT / VOLATILE  ← 데이터 받음
slam_toolbox       SUBSCRIPTION  BEST_EFFORT / VOLATILE  ← 데이터 받음
forced_recovery    SUBSCRIPTION  RELIABLE     / VOLATILE ← 데이터 0 수신
```

**근본 원인**: rclpy 기본 QoS 가 RELIABLE. publisher 가 BEST_EFFORT 일 때 RELIABLE 구독은 incompatible → DDS 가 메시지 전달 안 함. forced_recovery 의 `_scan_received` 영구 False → `_is_rear_safe()` 가 보수적으로 False 반환 → backup 항상 skip.

LaserScan / PointCloud 등 sensor 토픽은 BEST_EFFORT 가 관례.

### 3.4 [세부 이슈] DWB 의 작은 angular 명령이 모터 데드존 미만이라 회전 무효

**증상** (사용자 데이터):
```
linear.x: 0.034 → 0.082 → 0.084
angular.z: 0 → -0.16 → -0.16
```
robot 이 z 값은 받지만 시각적으로 회전 안 보임. 천천히 직진 → lethal → backup → 직진 사이클 반복.

**Differential drive 분해**:
- left = vx + ω·L/2 = 0.084 + (-0.16)(0.1225) = 0.060 m/s
- right = vx - ω·L/2 = 0.084 - (-0.16)(0.1225) = 0.108 m/s

**엔코더 분해능**:
- 1320 pulse/rev, 10ms 측정 주기 → 1 tick = 16.3 mm/s = 0.0163 m/s
- left 0.060 m/s = 3.7 ticks/period
- right 0.108 m/s = 6.6 ticks/period
- 회전속도 차 = 0.048 m/s = 2.9 ticks/period

**driver 코드 확인** ([jupiter_driver_compensated.py:295-338](src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py)):
- 정지 회전 (|vx|<0.02) 분기: STARTUP kick 0.37 → RUNNING floor 0.12 적용.
- 이동 + 회전 (|vx|≥0.02) 분기: angular 그대로 패스스루 (코멘트 line 40: "주행 중에는 미적용").

**근본 원인**: 모터 교체 후 (2026-04-17) ANGULAR_SCALE_FACTOR=1.0, MCU_MIN_ANGULAR=0 으로 재캘리브 미완. 이동 중 작은 angular (0.16 같은 값) 은 wheel-level diff (0.048 m/s) 가 모터 데드존 (Motor_Set_Dead_Zone(M3,720,690)) 근처에 머물러 PID 적분 지연 → 실효 회전 거의 안 됨.

**보조 원인**: DWB `min_speed_theta=0.20` 인데도 RotateToGoal critic 이 final yaw 정렬 시 `slowing_factor=2.0` 으로 0.32/2.0=0.16 출력 → 0.20 미만이라 실효 무회전. PathAlign 같은 path-following 출력도 0.20 미만 가능.

### 3.5 [세부 이슈] DWB 가 path 를 벗어나 "경향성만" 따라감

**증상**: lethal-zone 탈출 후에도 plan 의 정확한 line 을 따르지 않고 옆으로 빗겨남. PathDist 가 너무 약하거나 PathAlign 의 lookahead 가 짧음.

**현재 critic 가중치**:
- BaseObstacle.scale: 0.05 (collision_monitor 가 hard gate 라 약하게 둠)
- PathAlign.scale: 20.0
- PathAlign.forward_point_distance: 0.2
- PathDist.scale: 24.0
- GoalDist.scale: 24.0
- GoalAlign.scale: 24.0
- RotateToGoal.scale: 32.0

PathDist:BaseObstacle = 480:1 비율로 path 추종 압도적 우선이지만, 명령 자체가 데드존으로 무효화되어 실제 추종력이 약함. (3.4 와 동기화)

**근본 원인**: DWB 가 simulated trajectory 로 critic score 계산 → robot 이 그 trajectory 대로 갈 거라 가정. 그러나 driver 데드존으로 angular 무효 → simulated rotation ≠ actual rotation → 실제 trajectory 가 path 옆으로 빗겨남.

⇒ 3.4 의 데드존 보상 도입으로 simulated/actual 일치 → critic 가중치 강화로 path 구속 가능.

## 4. 적용한 변경

### 4.1 stuck_detector — subscription 토픽 변경

파일: `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/stuck_detector.py`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| stall cmd source | `/cmd_vel_safe` (twist_mux 출력, gating 후) | `/cmd_vel` (nav2 raw intent, gating 전) |
| stuck 의 의미 | "driver 가 명령받았는데 안 움직임" | "nav2 가 움직이고 싶은데 실제 안 움직임" |

의도: collision_monitor 가 nav2 명령을 차단해 발생하는 deadlock 도 stall 로 검출 → forced_recovery 자체 후방 LiDAR safety 로 backup 시도.

트레이드오프: joystick teleop 중에는 /cmd_vel_joy 가 별도 토픽이라 stall 검출 안 됨. 단 사용자가 직접 조작 중이라 자동 recovery 불필요 — 수용 가능.

### 4.2 forced_recovery — /scan_merged QoS BEST_EFFORT

파일: `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/forced_recovery.py`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| /scan_merged QoS | rclpy 기본 (RELIABLE) | BEST_EFFORT / VOLATILE / KEEP_LAST(10) |

기존 publisher (scan_merger) 가 BEST_EFFORT 라 RELIABLE 구독은 incompatible → 메시지 0 수신. 이제 모든 sensor 구독자와 일관.

### 4.3 driver — 이동 회전 데드존 보상 (MOVING_ROT_MIN_ANGULAR)

파일: `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

신규 클래스 상수:
| 상수 | 값 | 역할 |
|---|---|---|
| `MOVING_ROT_MIN_ANGULAR` | 0.20 rad/s | 이동 중 최소 사용 가능한 angular (DWB min_speed_theta 와 동기화) |
| `MOVING_ROT_VX_THRESHOLD` | 0.02 m/s | 이 이상의 vx 일 때 분기 적용 |
| `MOVING_ROT_RELEASE` | 0.05 rad/s | 이 미만 명령은 0 처리 (smoother deadband 와 일치) |

`cmd_vel_callback` 변경:
| |ω| 범위 (이동 중) | 변경 전 | 변경 후 |
|---|---|---|
| < 0.05 (RELEASE) | 패스스루 (작은 노이즈) | 0 |
| 0.05 ~ 0.20 | 패스스루 → 모터 데드존 미만 | sign × 0.20 으로 boost |
| ≥ 0.20 | 패스스루 | 패스스루 |

의도: DWB 가 실효 가능한 회전 명령만 내도록 floor 보장. simulated trajectory 와 actual trajectory 일치.

주의: near-goal final yaw 정렬은 정지 회전 분기 (|vx|<0.02) 라 영향 없음.

### 4.4 DWB critic — path 구속력 강화

파일: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| PathAlign.scale | 20.0 | 28.0 |
| PathAlign.forward_point_distance | 0.2 | 0.3 |
| PathDist.scale | 24.0 | 32.0 |

의도: 4.3 의 데드존 보상으로 controller sample 의 실효성 확보됐기에, path 이탈 trajectory 에 더 큰 페널티 부과 가능. forward_point_distance 0.3 으로 더 멀리 보고 preemptive turn-in.

PathAlign(28) : BaseObstacle(0.05) = 560:1 비율 — path 추종이 압도적이지만 inflation 영역 진입은 여전히 허용 (좁은 통로 통과 가능).

## 5. 빌드 및 검증

### 5.1 빌드

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_safety --symlink-install
# Finished <<< jupiter_safety [9.41s]

cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_safety --symlink-install
# (forced_recovery QoS 추가 후 재빌드)
# Finished <<< jupiter_safety [9.18s]

cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup jupiter_nav_VO --symlink-install
# Summary: 2 packages finished [10.1s]
```

### 5.2 검증 1 — stuck_detector 토픽 변경 후 stuck 검출

수정 후 launch 재기동 → lethal 도달 → `ros2 topic echo /stuck_status` → `data: true` 확인. 이전에는 false 였음.

### 5.3 검증 2 — forced_recovery 의 QoS 수정 후 backup 명령 발행

forced_recovery 단독 재기동:
```bash
kill 1584831
nohup ros2 run jupiter_safety forced_recovery > /tmp/forced_recovery.log 2>&1 &
# /forced_recovery 노드 등록 확인
# [INFO] ForcedRecovery up — backup -0.1 m/s × 1.0s, rear safety 0.2m × ±30°, cooldown 5.0s
```
이전 로그 무한 반복 패턴 (`rear safety: no /scan_merged data → conservative SKIP backup`) 사라짐. 이후 lethal 재현 시 backup 명령 정상 발행 확인 (사용자 보고: "뒤로 1초동안 가기는 함").

### 5.4 검증 3 — 데드존 + path 추종 보강 후 lethal 탈출

사용자 보고: "조금씩 오른쪽으로 움직이다가, 결국 무한루프에서 탈출했다."

→ 안전 stack 자체는 동작. 데드존 보상 + path 구속 조정으로 backup ↔ forward 사이클이 줄고 path 따라가며 lethal 우회 가능 위치로 이동 가능해질 것으로 예상. 추가 운용 검증 필요.

### 5.5 검증 결과

| 기준 | 실측 |
|---|---|
| /scan_merged 가 forced_recovery 에 도달 | YES (수정 후) |
| /stuck_status 가 collision_monitor 차단 시 true | YES (수정 후) |
| forced_recovery /cmd_vel_force 발행 | YES (수정 후) |
| twist_mux 가 force(80) 우선 선택 | YES (priority 동작 정상) |
| lethal-zone 무한 deadlock | 해소 (backup → forward 사이클로 탈출 가능) |
| 회전 명령 데드존 보상 (이동 중) | 적용 (lab 실측 추가 검증 필요) |
| DWB path 구속력 | 강화 (실주행 검증 필요) |

## 6. 최종 결론

### 6.1 오늘 해결된 항목

- **lethal-zone 무한 deadlock 새 변종** — stuck_detector 가 collision_monitor 이전의 nav2 raw intent (/cmd_vel) 를 보도록 변경. forced_recovery 가 backup 으로 escape 가능.
- **forced_recovery 의 /scan_merged QoS mismatch** — RELIABLE → BEST_EFFORT. 후방 안전 검사 정상 동작.
- **이동 중 회전 무효 (모터 데드존)** — driver 의 MOVING_ROT_MIN_ANGULAR=0.20 floor 추가. DWB 의 작은 angular 명령이 실효 가능 영역으로 boost.
- **DWB path 구속 강화** — PathAlign 20→28, PathDist 24→32, forward_point_distance 0.2→0.3.
- **잘못된 launch 사용 진단** — 사용자가 nav2-only launch 만 실행했던 문제 식별 후 정확한 launch 안내.

### 6.2 오늘 새로 확인된 항목

- **레거시 jupiter_joy 가 /cmd_vel 직접 publish** — twist_mux 의 /cmd_vel_joy 분리 의도와 충돌. 사용자 종료로 임시 해결, 영구 fix 는 jupiter_joy.py 의 publisher 변경 또는 launch 에서 jupiter_joy 자체 제외.
- **behavior_server 가 /cmd_vel 에 3 publisher 등록** — BackUp / Spin / DriveOnHeading 각각. 모두 collision_monitor 의 gate 대상이 됨. 의도된 nav2 동작이지만 안전 layer 와의 상호작용 주의 필요.
- **nav2 BT timeout 시 idle 진입** — controller_server 가 N 회 "Failed to make progress" 후 BT 가 compute_path_to_pose action server timeout → idle. 새 goal 받기 전까지 /cmd_vel 무발행.
- **모터 교체 (2026-04-17) 후 angular 캘리브레이션 미완** — ANGULAR_SCALE_FACTOR=1.0, MCU_MIN_ANGULAR=0 으로 두면 작은 angular 명령이 모터 데드존 미만으로 무효. 본 세션에서 driver-level floor 로 우회.

### 6.3 남은 제약 / 주의

- **MOVING_ROT_MIN_ANGULAR=0.20 의 부작용 가능성** — near-goal 영역에서 yaw 정렬이 정지 회전 분기로 떨어지기 전 (vx 가 0.02 직전) 의 미세 조정 시 0.20 floor 가 over-rotation 유발 가능. 실주행 모니터링 필요.
- **PathDist 32 의 회전 자유도** — 이전 28 시도에서 회전 trajectory 까지 reject 발생. 32 는 더 강한 값 — 좁은 통로에서 회전 어려움 가능. 만약 재발 시 28 로 후퇴 또는 vtheta_samples 11→17 증가.
- **유리/투명 obstacle** — 여전히 미해결. Tier 3 ultrasonic 도입까지 forced_recovery 후 sensor 사후 검출에 의존.
- **모터 angular 캘리브레이션 미완** — 본 세션 floor 는 우회. 정확한 ω 값을 위해서는 calibration 재실행 필요.
- **joystick teleop 의 stall 검출 부재** — stuck_detector 가 /cmd_vel 만 보므로 /cmd_vel_joy 경로의 stuck 은 미검출. 운영자 직접 조작 중이라 의도된 한계지만 향후 필요 시 두 토픽 OR 검사로 확장.

### 6.4 다음 분석 우선순위

1. **path 추종 정확도 실측** — PathAlign/PathDist 강화 효과 측정. 1m 통로 통과 / 코너 회전 시 plan 과 actual trajectory 의 RMS 거리 측정.
2. **MOVING_ROT_MIN_ANGULAR 미세조정** — 0.20 로 부작용 발생 시 0.18 / 0.16 로 단계적 하향, 또는 vx 의존 동적 floor (저속에서만 강 floor).
3. **모터 angular 재캘리브레이션** — calibrate_angular.py 재실행 → ANGULAR_SCALE_FACTOR 갱신 → MOVING_ROT_MIN_ANGULAR 도 함께 조정.
4. **Tier 3 ultrasonic ring** — 유리 detection 위해 HC-SR04/US-100 4 개 + Arduino Nano 도입 (이전 plan 의 Phase B).
5. **레거시 jupiter_joy 영구 처리** — `jupiter_joy.py` 의 publisher topic 을 `/cmd_vel_joy` 로 변경, 또는 `jupiter_joy_new.py` 만 launch 에 포함하도록 정리.

## 7. 생성 / 수정 파일 목록

### 7.1 수정된 파일

- `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/stuck_detector.py` — subscription topic /cmd_vel_safe → /cmd_vel
- `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/forced_recovery.py` — /scan_merged QoS BEST_EFFORT
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` — MOVING_ROT_MIN_ANGULAR floor 추가 + 정지 회전 adaptive kick
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` — PathAlign 28, PathDist 32, forward_point_distance 0.3

### 7.2 생성된 문서

- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_04_30.md` — 본 세션 명령어 로그
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_04_30.md` — 본 문서

---

## 8. 추가 세션 — 정지 회전 startup 지연 (PID buildup 의존) 해결

### 8.1 사용자 요청

> "멈춰있는 상황에서 바로 회전하지 못하고, 시간이 지나야 움직인다. pid 보정이 올라올때까지 기다리는 것 같은데, 해결책을찾아라"

### 8.2 문제 분석

**증상**: 정지 상태에서 회전 명령 (rotation request from rest) 수신 시, 즉각 회전 안 시작. 0.5~1초 정도 지연 후 모터 작동 시작.

**기존 startup kick 코드** ([jupiter_driver_compensated.py:304-347](src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py)):
- 정지 회전 분기 (|vx|<0.02) 진입 시 `MCU_ROT_START_ANGULAR=0.37 rad/s` 의 kick 을 `ROT_STARTUP_KICK_SEC=0.12s` 동안 유지.
- 이후 `ROT_RUNNING_MIN_ANGULAR=0.12` 로 floor 적용한 RUNNING 단계 진입.

**근본 원인**:
1. 모터 교체 (2026-04-17) 후 새 모터의 정지마찰이 더 크거나 ESC 매핑이 달라져 0.37 rad/s × 0.12s 키크가 motor 가동에 부족.
2. MCU PID/FF 의 적분 기반 deadzone 통과가 0.12s 안에 완료 안 됨 → kick 종료 후 RUNNING 단계의 작은 명령 (예 0.16) 으로는 모터 못 깨움.
3. 결과: kick 종료 → RUNNING 의 0.16 명령 → MCU PID 가 0 PWM 부터 다시 적분 시작 → 적분 누적되어 deadzone 통과까지 0.5~1s → 그제야 모터 가동.

**기존 시간 기반 kick 의 한계**: 0.12s 라는 고정 값은 (a) 모터 상태 (b) 부하 (c) 배터리 전압 에 따라 부족할 수 있음. 시간 대신 **encoder 피드백** 으로 motion 실제 발생 여부를 직접 확인해야 robust.

### 8.3 적용한 변경 — adaptive kick (encoder 기반 종료)

파일: `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `MCU_ROT_START_ANGULAR` | 0.37 rad/s | **0.55 rad/s** (kick 강도 상향) |
| `ROT_STARTUP_KICK_SEC` | 0.12s (고정) | **0.40s (안전 상한)** — encoder 확인 시 조기 종료 |
| `ROT_KICK_CONFIRM_THRESHOLD` | (없음) | **0.10 rad/s** (encoder actual_z) |
| 종료 조건 | 시간 만료 (0.12s) | (a) `|actual_z| ≥ 0.10` motion 확인 OR (b) 0.40s 안전 상한 |

**state machine 변경**:
- 신규 변수 `self._rot_motion_confirmed: bool` 추가.
- `_rot_active=True` 진입 시 `_rot_motion_confirmed=False` 로 초기화.
- 매 cmd_vel callback 마다 `self.bot._Rosmaster__vz` (MCU encoder 실측 angular) 직접 read.
- sign-aware 체크: `_rot_sign>0 AND actual_z ≥ 0.10` 또는 `_rot_sign<0 AND actual_z ≤ -0.10` → motion 확인.
- 확인 즉시 RUNNING 진입 (kick 종료) → DWB 의 small angular 명령이 모터 가속도 누리고 시작.
- 0.40s 안에 미확인 → 안전 상한 도달, 강제 RUNNING 전환 (모터 stall 가능성). stuck_detector 가 별도로 stuck 검출 → forced_recovery escalate.

**코드 위치**: cmd_vel_callback 의 정지 회전 분기 (`abs(vx) < 0.02`) 내부, `_rot_active=True` 경로에서 actual_z 기반 confirm 로직 + adaptive kick 종료 분기.

**의도**:
- kick 강도 0.55 (0.37→0.55, +49%): MCU PID/FF 의존도 줄이고 모터에 즉각 큰 PWM 출력하도록 강한 명령 인가.
- kick 시간 안전 상한 0.40s (0.12→0.40, 3.3×): encoder 확인 전 충분한 가동 기회 제공. 정상 시 0.05~0.15s 안에 motion 확인 → 즉시 RUNNING.
- adaptive 종료: 시간 의존성 제거. 배터리 / 부하 / 모터 상태 변동에도 robust.

### 8.4 검증 시나리오

```bash
# driver 재시작 후 정지 상태에서 회전 goal 또는 joystick 회전 입력 → MCU 회전 시작 시간 측정.
ros2 topic echo /cmd_vel_safe         # angular.z 0.55 (kick) → 0.16 (RUNNING) 패턴
ros2 topic echo /jupiter/get_vel      # angular.z 가 0.10 도달까지의 시간 측정
```

기대:
- kick 종료 시간이 cmd_vel 첫 수신 후 0.05~0.15s 사이 (encoder 도달 즉시 종료)
- 시각적으로 회전 시작이 100~200ms 안에 보임 (이전 0.5~1s 대비)

### 8.5 빌드

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup --symlink-install
# Finished <<< jupiter_bringup [13.4s]
```

### 8.6 위험 / 모니터링

- **0.55 rad/s 의 kick 으로 인한 overshoot 가능성**: encoder 가 0.10 도달 시 즉시 RUNNING 의 0.16 으로 떨어지지만 motor inertia 로 actual 이 0.30~0.40 까지 갈 수 있음. settling 시간 확인 필요.
- **MCU 통신 지연 시 actual_z stale**: cmd_vel callback 이 MCU 보고보다 빠를 경우 stale actual_z 로 판단할 위험. publish_velocity 가 50Hz 라 max stale 20ms — 무시 가능 수준.
- **kick 안전 상한 0.40s 의 부작용**: 모터 정상인데 부하 무거운 경우 (예: 카펫) confirm 안 되어 kick 길어짐. 단 0.40s 는 짧고, 만약 정말 stall 이면 stuck_detector 가 1.5s 후 잡음.

### 8.7 추가 수정 종합 (8.x 적용 후 최종 상태)

[jupiter_driver_compensated.py](src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py) 의 회전 보상 종합:

| 단계 | 조건 | 동작 |
|---|---|---|
| 정지 회전 진입 | `|vx|<0.02` & `|ω|≥0.04` & idle | kick=±0.55 startup |
| 정지 회전 STARTUP | active & not motion_confirmed & now<0.40s | kick=±0.55 유지 |
| 정지 회전 STARTUP 종료 | active & motion_confirmed (encoder ≥0.10) | RUNNING 으로 즉시 전환 |
| 정지 회전 RUNNING | active & motion_confirmed | output = sign × max(\|ω\|, 0.12) |
| 정지 회전 종료 | `|ω|<0.03` 또는 sign=0 | output=0, _rot_active=False |
| 이동 회전 데드존 컷 | `|vx|≥0.02` & `|ω|<0.05` | output=0 |
| 이동 회전 floor | `|vx|≥0.02` & `0.05≤|ω|<0.20` | sign × 0.20 |
| 이동 회전 통과 | `|vx|≥0.02` & `|ω|≥0.20` | 패스스루 |
