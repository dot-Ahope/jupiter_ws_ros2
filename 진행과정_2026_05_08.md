# 진행과정 2026-05-08: SLAM strict + Path inflation 조정 + forced_recovery 비활성화 + firmware 보정 환원

## 1. 목표

### 1.1 사용자 요청 (시간순)

1. > "단계 1 진행하라" — slam_toolbox 의 매칭 임계값 strict 화로 yaw drift 차단.
2. > "새로운 bag를 만들었다. 확인해봐라. 알고 싶은 것: 1. path를 만들 때, obstacle에 너무 가까이 만들어서 그런가, 코너를 돌 때, 너무 느려진다. obstacle과 거리를 두고 만들되, 좁은 통로도 지날 수 있게 하고 싶다. 2. 양쪽 바퀴가 같은 cmd_vel일 때, 속도가 다르게 출력되고 있는지 파악해라. 뭔가 쏠림현상이 있는 느낌이다."
3. > "요청1에 대해서 변경 적용하고, 요청 2 같은 경우는, 해당 부분에 영향을 끼치는 보정이, 드라이버 단과 펌웨어 단에 있는지, 혹은 다른 부분에 있는지 조사하라."
4. > "직접 확인하라. 그리고 로버 바퀴의 물리적 문제를 해결하기 전에 만들었던 보정 코드들이 또 있는지 확인해봐라."
5. > "요청 1부터 진행하자"
6. > "[코너 cut 사진] bag를 만들었다. path보다 더 코너에 가깝게 움직이려고 하는 모습을 보이는 것 같다."
7. > "진행하라. 그리고 추가로, backup등을 드라이버단에서 진행되도록 만든 코드들이 있던 것으로 기억하는데, 이것이 nav2와 충돌하는 느낌이 든다. 분석해라."
8. > "사실 없는게 더 나은 부분이 있는지 판단해봐라"
9. > "1번 진행하라" (forced_recovery 비활성화)
10. > "새 bag를 만들었다."
11. > "펌웨어 코드 수정은 한꺼번에 진행하자.(우선순위 2, 3)"

### 1.2 이전 세션 요약 / 연관 작업

- 2026-05-07 진행과정 (`/home/jetson/jupiter_ws_ros2/진행과정_2026_05_07.md`): M3 mechanical 정비 후 baseline + 데모 운영 (조이스틱 / 지도 저장 / localization mode) + Step B (RUNNING_MIN 0.30→0.20) + yaw drift 진단 — 1차 조치.
- 본 세션은 yaw drift 의 매칭 strict 처리 (단계 1) → 운영 시 path/obstacle 부작용 발견 → 코너 cut → safety 충돌 → 보정 layer 가지치기 (forced_recovery, balance correction, deadzone override) 의 **연속된 한 줄기**.

## 2. 대상 시스템 구성

### 2.1 변경 영향 범위

```
[운영]
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py slam_mode:=localization slam_map:=/home/jetson/maps/jupiter_demo

[변경된 layer]
1. SLAM (slam_toolbox)            — slam_params.yaml (단계 1)
2. Costmap & Controller (Nav2)    — nav2_params_fused.yaml (요청 1)
3. Safety layer (jupiter_safety)  — safety_layer.launch.py (forced_recovery 비활성화)
4. MCU firmware                   — app_mecanum.c, app.c (보정 환원, Keil 빌드 필요)
```

### 2.2 cmd_vel 파이프라인 (변경 후)

```
nav2 controller → /cmd_vel ─→ collision_monitor ─→ /cmd_vel_nav_filtered ─→ twist_mux (50)
                  │                                                              ↓
                  └→ stuck_detector (감지만, /stuck_status 발행)               twist_mux out
                                                                                ↓
joy → /cmd_vel_joy ─────────────────────────────────────→ twist_mux (100)     /cmd_vel_safe → driver
                                                                                
(forced_recovery — 비활성화, /cmd_vel_force 발행 안 함)
```

## 3. 문제 분석

### 3.1 이슈 — yaw drift / jump (이전 세션 잔존)

- **증상**: localization mode 운영 중 map→odom yaw 가 stationary 상태에서도 19.58° jump 발생. 2D Pose Estimate 후 정렬 안 됨. lateral RMS 16.7cm 까지 증가.
- **수학적/코드상 원인**: slam_toolbox 의 매칭 임계값 (`link_match_minimum_response_fine 0.50`, `loop_match_minimum_response_coarse 0.55`) 이 약한 매칭도 수용 → SLAM 이 잘못된 매칭으로 map→odom TF 보정 발산.
- **근본 원인**: 매칭 strict 부족 + minimum_travel_heading 0.05 (≈2.9°) 너무 작은 회전마다 매칭 시도 → drift 누적.
- **보조 원인**: correlation_search_space_dimension 0.6 너무 큼 → 후보 영역 넓어 잘못된 매칭 가능성.

### 3.2 이슈 — Path 가 obstacle 에 너무 가까이 그려짐 (사용자 요청 1)

- **증상**: 코너에서 path 가 obstacle 의 inflation 영역 (≤20cm) 안에 들어감 → MPPI 의 ObstaclesCritic 이 자체 감속 → 코너 vx 0.090 m/s (직선 0.110 의 18% 감속).
- **데이터** (bag #1, 단계 1 후):
  - 코너 path 5.6% 가 <20cm
  - 직선 path avg 57.8cm, 코너 avg 53.8cm
- **근본 원인**: `global_costmap.inflation_layer.inflation_radius=0.25` — SmacPlanner2D 가 obstacle 0.25m 밖에서 path 그림. 코너 곡률 작으면 안쪽으로 들어감.

### 3.3 이슈 — 양쪽 wheel 비대칭 / 쏠림 (사용자 요청 2)

- **증상**: 사용자 체감으로 한쪽 쏠림. 직진 명령에도 미세 회전.
- **측정 데이터** (bag #2, 220s):

| 지표 | 측정값 |
|---|---|
| 직진 시 좌/우 wheel speed 평균 차 | -0.0008 (-0.7%) |
| 시간대별 변동 | ±9.0% (90-120s 에 +0.0386) |
| 좌회전 명령 응답 좌/우 차 | 30% (좌측 큼) |
| 우회전 명령 응답 좌/우 차 | 3% |
| IMU 직진 시 yaw rate | -0.33°/s (CW drift) |
| actual/cmd ratio | 0.871 (13% 응답 부족) |

- **수학적 원인 / 보정 위치 조사**:
  - **Driver layer**: ANGULAR_SCALE_FACTOR (1.0, no-op), MCU_ROT_START_ANGULAR/RUNNING_MIN/MOVING_ROT_MIN — 모두 좌/우 동일 적용 (단일 스칼라). 좌/우 비대칭 보정 없음.
  - **EKF / TF**: URDF imu_joint rpy="0 0 0", bringup static_transform 도 rpy=0,0,0 — yaw alignment 보정 없음. **z 위치 이중정의** (URDF 0.05 vs static 0.02).
  - **Firmware**: `app_mecanum.c:130-157` balance correction (M1/M3 encoder delta × 0.02 gain, V_z=0 일 때만), `app.c:158-163` M1/M3 deadzone override 720/690 (default 700 대비 비대칭). `app_pid.c` 4 motor 동일 PID gain.
  - **이상**: **firmware 의 balance correction 만이 좌/우 비대칭 보정 코드**. 그러나 회전 시 비활성 (`balance_prev_vx=0`) → 사용자 측정 데이터의 "좌회전 30% 큼" 증상 정확히 매칭.

- **근본 원인** (잠정): M3 모터 교체 전 시점 만들어진 보정 (balance correction + M1/M3 deadzone 720/690) 의 premise 가 무너졌을 가능성. 보정 자체가 새 모터 환경에서 노이즈 origin 이 됐을 가능성.

### 3.4 이슈 — Path 안쪽 cut (요청 1 v1 부작용)

- **증상**: inflation_radius 0.32 적용 후 path 는 wide 하게 그려졌으나 **robot 이 path 따라가지 않고 안쪽 cut**. 사용자 rviz 사진 — robot 이 path 안쪽 corner 가까이 진행.
- **측정 데이터** (bag #2, v1 적용 후):

| 지표 | Path 자체 | Robot trajectory |
|---|---|---|
| 코너 평균 거리 | 51.3cm | **41.8cm** |
| 코너 p10 거리 | 32.0cm | **22.0cm** |
| 코너 <25cm 비율 | 거의 없음 | **14.2%** |

- **근본 원인**: `ObstaclesCritic.critical_weight 20→15` + `collision_margin_distance 0.10→0.08` 변경이 MPPI 의 obstacle 회피 강도를 약화. path wide 해진 만큼 짧은 trajectory (안쪽 cut) 가 더 좋은 cost 로 채택됨.

### 3.5 이슈 — `forced_recovery` ↔ nav2 충돌

- **증상**: bag #2 분석에서 stuck event 4건, force backup 4 burst, force↔nav 동시 backup 1건 발견. nav2 BackUp burst 가 길어짐 (최장 6.2s).
- **stuck event 분류** (4건):
  - #0 (t=93.1s): cmd ω=-0.15, actual=0 → angular stall (driver ROT_RUNNING_MIN 영역 0.10~0.30 false positive)
  - #1, #2 (108s, 134s): cmd ω=+0.80, actual=0 → angular stall (firmware 가 ω=0.80 못 따라감, nav2 Spin recovery)
  - #3 (206s): cmd vx=-0.098, actual=-0.001 → backward stall (nav2 가 BackUp 진행 중 wall stuck)
- **근본 원인**:
  1. forced_recovery 의 `BACKUP_VX=-0.10, DURATION=1.0` 이 hardcoded — nav2 의 BackUp 명령과 무관한 backup 시도
  2. priority force(80) > nav(50) → forced_recovery 가 nav2 명령 override
  3. 회전 stall 에 후진은 부적절 (회전 안 되는데 후진해도 같은 위치 stuck 반복)
  4. 5s cooldown 동안 nav2 BT 의 다음 단계 (Spin, Wait, replan) 와 시간 충돌
- **결정**: forced_recovery 의 4건 trigger 모두 부적절. nav2 BehaviorTree 가 BackUp/Spin/Wait 으로 같은 일을 더 영리하게 처리. **현재 환경 (유리 없음) 에서 forced_recovery 비활성화가 깔끔**.

### 3.6 이슈 — 모터 mechanical fix 전 만들어진 보정 잔존

전수 조사 결과 8개 항목 식별:

| # | 위치 | 보정 | premise (모터 교체 전) | 모터 교체 후 |
|---|---|---|---|---|
| 1 | `app.c:158-163` | M1/M3 deadzone 720/690 | M3 마찰 비대칭 | premise 무효 |
| 2 | `app_mecanum.c:130-157` | balance correction (gain 0.02) | M3 stuck 보정 | premise 무효 |
| 3 | `bsp_motor.c:14-15` | `MOTOR_FILTER_ALPHA=0.3` | 응답속도 개선 | 무관 (유지) |
| 4 | `jupiter_driver_compensated.py:30` | `ANGULAR_SCALE_FACTOR=1.0` | 0.401 → 1.0 (재캘 필요 명시) | 미실행 |
| 5 | `:40` | `MCU_MIN_ANGULAR=0.0` | 0.12 → 0.0 (비활성화) | dead code |
| 6 | `:125` | `Ki=0.02` | 방안A-4 ESC skip 후 튜닝 (CAR_DIFFERENTIAL) | 현재 CAR_MECANUM 사용, 가정 무관 |
| 7 | `:142` | `Kd=0.3` | 동상 | 동상 |
| 8 | URDF + bringup | imu_link TF 이중 정의 (z=0.05 vs 0.02) | 보드 고정, mechanical 무관 | 충돌 (하나만 남기기) |

## 4. 적용한 변경

### 4.1 단계 1 — slam_params.yaml 매칭 strict

`/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml`

| 라인 | 파라미터 | 변경 전 | 변경 후 | 의미 |
|---|---|---|---|---|
| 41 | `minimum_travel_heading` | 0.05 | **0.20** | 작은 회전마다 매칭하지 않음 — drift 누적 차단 |
| 49 | `link_match_minimum_response_fine` | 0.50 | **0.65** | 인접 scan 약한 매칭 거부 |
| 56 | `loop_search_maximum_distance` | 4.0 | **2.5** | 먼 거리 잘못된 loop closure 차단 |
| 60 | `loop_match_minimum_response_coarse` | 0.55 | **0.70** | loop closure strict |
| 64 | `correlation_search_space_dimension` | 0.6 | **0.3** | 후보 영역 축소 |

의도: yaw jump 19.58° → 5° 이하, drift 자체 발생 빈도 감소. trade-off: 매칭 실패 시 odom 단독 운영 (5~10초 freeze 가능).

### 4.2 요청 1 v1 — nav2_params_fused.yaml (path obstacle 거리)

| 라인 | 파라미터 | 변경 전 | v1 |
|---|---|---|---|
| 215 | `ObstaclesCritic.critical_weight` | 20.0 | 15.0 |
| 218 | `ObstaclesCritic.collision_margin_distance` | 0.10 | 0.08 |
| 437 | `global_costmap.inflation_radius` | 0.25 | **0.32** |
| 436 | `cost_scaling_factor` | 3.0 | **2.5** |

의도: path 가 obstacle 에서 멀리 그려지면 코너 진입 시 MPPI 자체 감속 줄어듦.

### 4.3 요청 1 v2 (부작용 보완)

| 라인 | 파라미터 | v1 | **v2 (최종)** |
|---|---|---|---|
| 215 | `ObstaclesCritic.critical_weight` | 15.0 | **18.0** |
| 218 | `ObstaclesCritic.collision_margin_distance` | 0.08 | **0.10** |
| 226 | `PathAlignCritic.cost_weight` | 14.0 | **20.0** |

의도: critical 약화 부분 환원 + PathAlign 강화 → robot 이 wide path 따라감.

### 4.4 forced_recovery 비활성화

`/home/jetson/jupiter_ws_ros2/src/jupiter_safety/launch/safety_layer.launch.py`

- line 73-91: `forced_recovery_node` 정의 블록 코멘트 처리 + 비활성화 사유 명시 (4/4 trigger 부적절, nav2 BT 가 더 잘 처리)
- line 105-110: LaunchDescription 에서 `forced_recovery_node` 라인 코멘트 (`# forced_recovery_node,`)

유지: `twist_mux`, `stuck_detector`, `alert_publisher`, `collision_monitor_launch`. stuck_detector 는 진단용으로 `/stuck_status` 발행 계속.

### 4.5 Firmware — balance correction 비활성화

`/home/jetson/mcu/extbd/Source/APP/app_mecanum.c`

- line 120-167: balance correction 블록 (`if (V_y == 0 && V_z == 0)` 부터 `else { balance_prev_vx = 0; }` 까지) 전체 주석 처리
- 변수 (`balance_m1_ref`, `balance_m3_ref`, `balance_prev_vx`) 보존 + `(void)` 캐스트로 unused 경고 억제 — 재활성화 시 코드 부활 가능하도록

### 4.6 Firmware — M1/M3 deadzone 환원

`/home/jetson/mcu/extbd/Source/APP/app.c`

| Motor | 변경 전 | 변경 후 |
|---|---|---|
| M1 | 720, 690 | **700, 700** |
| M2 | 780, 750 | 780, 750 (유지 — 별도 motor) |
| M3 | 720, 690 | **700, 700** |

## 5. 빌드 및 검증

### 5.1 빌드 (ROS2 패키지)

```bash
# 단계 1
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_nav --symlink-install
# Finished <<< jupiter_nav [4.63s]

# 요청 1 v1
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_nav_VO --symlink-install
# Finished <<< jupiter_nav_VO [4.70s]

# 요청 1 v2
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_nav_VO --symlink-install
# Finished <<< jupiter_nav_VO [4.61s]

# forced_recovery 비활성화
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_safety --symlink-install
# Finished <<< jupiter_safety [4.53s]
```

모두 성공.

### 5.2 빌드 (Firmware)

**미수행**. Jetson 에 ARM toolchain 없음 (`arm-none-eabi-gcc` 부재). Keil µVision (Windows + `rosmaster.uvprojx`) 필요. 사용자가 Windows 에서 빌드 후 ST-Link 로 플래시 예정.

### 5.3 검증 — bag 분석 (3회 운영)

#### 5.3.1 bag #1 (단계 1 적용 후, 166.6s)

```
Path lateral RMS:    3.1cm
Path lateral p95:    9.1cm
Path lateral max:   18.3cm
평균 signed:        -0.2cm (균형)
코너 path <20cm:    5.6%
코너 vx 평균:       0.090 m/s (직선 0.110 의 82%)
```

→ 단계 1 효과 우수. lateral 16.7cm → 3.1cm (81% 개선). 그러나 path 가 코너에서 obstacle 가까이 그려짐 (5.6% < 20cm) — 사용자 요청 1 의 시작점.

#### 5.3.2 bag #2 (요청 1 v1 적용 후, 219.3s)

```
Path lateral RMS:    3.6cm
Path lateral p95:   12.3cm
Path lateral max:   18.7cm
코너 path <20cm:    0.8% (개선 7배)
코너 vx 평균:       0.098 m/s (8% 향상)
직선 vx 평균:       0.110 m/s (그대로)

Robot↔obstacle (코너):
  평균:             41.8cm  ← path 51.3cm 보다 9.5cm 안쪽
  최소:             17.8cm  @ vx=+0.200 m/s (max)
  <25cm 비율:      14.2%

stuck event:        4건
  - 3건 angular stall (회전 명령 응답 0)
  - 1건 backward stall (nav2 backup 중)
force backup burst:  4
nav2 backup burst:   8 (최장 6.2s)
force↔nav 동시:     1건
```

→ Path 거리는 의도대로 멀어짐. **그러나 robot 이 path 안쪽 cut** (사용자 사진/체감 일치). 동시에 jupiter_safety 충돌 진단 결과 4건 부적절.

#### 5.3.3 bag #3 (v2 + forced_recovery 비활성, 137.1s)

```
Path lateral RMS:    3.8cm
Path lateral max:   23.0cm (증가)
코너 path <20cm:    0.4%
코너 vx 평균:       0.082 m/s (16% 추가 감속)
직선 vx 평균:       0.139 m/s (26% 향상)

Robot↔obstacle (코너):
  평균:             61.3cm  ← v1 41.8cm 대비 19.5cm 개선
  최소:             18.8cm
  <25cm 비율:       5.9%   ← v1 14.2% 의 절반 이하
  <30cm 비율:        7.6%   ← v1 29.4% 의 1/4

stuck event:         0건  ← v1 4건 → 0건
force backup burst:   0    ← v1 4 → 0
nav2 backup burst:   7 (최장 2.55s)  ← v1 8 / 6.2s
```

→ **모든 목표 달성**. forced_recovery 가 빠지자 stuck event 자체가 0건. nav2 의 BackUp burst 도 짧아짐 (6.2s → 2.55s) — 가로채는 force 가 없어 nav2 BT 의 normal flow 유지.

### 5.4 검증 결과 (목표 vs 실측)

| 기준 | 목표 | 실측 (bag #3) | 평가 |
|---|---|---|---|
| 코너 path <20cm | <2% | 0.4% | ✓ |
| 코너 robot↔obstacle 평균 | >50cm | 61.3cm | ✓ |
| 코너 robot↔obstacle <25cm | <10% | 5.9% | ✓ |
| 가장 위험한 시점 | >15cm | 18.8cm @ 0.20 m/s | ✓ |
| 직선 vx | ≥0.10 m/s | 0.139 m/s | ✓ |
| 코너 vx | ≥0.08 m/s | 0.082 m/s | ✓ (간신히) |
| stuck event | 0 | 0 | ✓ |
| force backup | 0 | 0 | ✓ |
| Lateral RMS | ≤5cm | 3.8cm | ✓ |
| Lateral max | ≤25cm | 23.0cm | △ (이전 18.7cm 대비 증가) |

## 6. 최종 결론

### 6.1 오늘 해결된 항목

1. **yaw drift / jump (이전 세션 잔존)**: slam_params.yaml 5개 항목 strict → lateral 16.7cm → 3.1cm.
2. **요청 1 — path obstacle 거리**: inflation 0.25 → 0.32, scaling 3.0 → 2.5 → 코너 path <20cm 5.6% → 0.4%.
3. **path cut 부작용**: PathAlignCritic 14 → 20, ObstaclesCritic critical 18, margin 0.10 → robot↔obstacle 41.8cm → 61.3cm.
4. **forced_recovery ↔ nav2 충돌**: 노드 비활성화 → stuck event 4 → 0건, nav2 backup 길이 6.2s → 2.55s.

### 6.2 오늘 새로 확인된 항목

1. **wheel_speeds 가 2-element** (좌, 우 평균) — 4-wheel 가정 잘못. firmware 에서 합산 후 publish.
2. **좌/우 평균 차이는 0.7%** 작지만 시간대별 ±9% 변동. 좌회전 응답 우회전 대비 30% 큼.
3. **IMU 직진 시 yaw rate -0.33°/s** (CW drift). EKF 가 보정 중이라 lateral 평균 -0.2cm 까지 수렴.
4. **balance correction 이 회전 중 비활성** (V_z=0 일 때만 작동) — 사용자 측정 "좌회전 30% 큼" 의 직접 원인.
5. **모터 mechanical fix 전 보정 8개 항목 식별**:
   - firmware: M1/M3 deadzone 720/690, balance correction
   - driver: ANGULAR_SCALE_FACTOR (no-op), MCU_MIN_ANGULAR (dead code), Ki=0.02 / Kd=0.3 (CAR_DIFFERENTIAL 가정)
   - URDF/launch: imu_link TF 이중 정의 (z=0.05 vs 0.02)
6. **stuck event 4건 모두 부적절**:
   - 3건 angular stall (firmware ω=0.80 응답 0)
   - 1건 nav2 BackUp 중 redundant
7. **회전 stall 의 근본 원인 미해결** — ω=0.80 명령에 actual=0. firmware/electrical 한계 가능성. forced_recovery 빼면서 우회되었으나 nav2 가 같은 stall 만나면 Spin recovery 도 효과 의문.

### 6.3 남은 제약 / 주의

1. **Firmware 변경 (4.5, 4.6) 미플래시**: Jetson 에 ARM toolchain 없음, Keil µVision (Windows) 필요. 미플래시 상태로는 효과 없음.
2. **Lateral max 23.0cm 증가** (bag #2 의 18.7cm 대비) — path wide 한 만큼 일부 시점 큰 편차. 평균은 양호.
3. **회전 stall 잠재 위험**: 같은 위치에서 nav2 의 Spin recovery 도 못 돌면 무한 루프. forced_recovery 비활성화로 backup 자동 우회 차단 — 운영자 개입 필요.
4. **단계 1 의 `minimum_travel_heading 0.20` 은 mapping 모드와 공유**. 새 지도 작성 시 0.05 로 임시 환원 필요 (mapping 효율 차원).
5. **wheel asymmetry 의 시간대별 변동** (±9%) 의 origin 미규명 — balance correction 자체일 수도, 외부 부하일 수도. Firmware 변경 후 측정 비교로 판별.

### 6.4 다음 분석 우선순위

1. **Firmware 빌드 / 플래시 (Windows + Keil)** — 4.5, 4.6 적용
2. 플래시 후 새 bag → wheel_speeds / lateral / IMU drift 비교 (3가지 시나리오):
   - **A**: 결과 좋아짐 → balance correction + deadzone override 영구 제거
   - **B**: 결과 나빠짐 → 새 모터 기준 gain 재튜닝
   - **C**: 변화 미미 → 다른 layer (ANGULAR_SCALE_FACTOR 재캘, Ki/Kd 환원, IMU TF 일원화)
3. 회전 stall 의 근본 원인 조사 — ω=0.80 명령에 actual=0 발생 시점의 MCU PID 출력 / 모터 PWM duty 측정
4. ANGULAR_SCALE_FACTOR 재캘리브레이션 (`calibrate_angular.py` 사용)
5. URDF / bringup 의 imu_link TF 이중정의 정리

## 7. 생성 / 수정 파일 목록

### 7.1 수정된 파일

#### ROS2 패키지
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml` (5개 파라미터)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` (5개 파라미터, v1+v2 두 차례)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/launch/safety_layer.launch.py` (forced_recovery_node 비활성화)

#### Firmware (Keil 빌드 필요)
- `/home/jetson/mcu/extbd/Source/APP/app_mecanum.c` (balance correction 주석 처리)
- `/home/jetson/mcu/extbd/Source/APP/app.c` (M1/M3 deadzone 700/700)

### 7.2 생성된 분석 스크립트

- `/tmp/analyze_path_corners.py` (path 곡률 별 obstacle clearance)
- `/tmp/analyze_wheel_asym.py` (4-wheel 가정 — 데이터 형식 발견용)
- `/tmp/analyze_wheel_asym2.py` (2-element 재분석)
- `/tmp/analyze_robot_vs_path_clearance.py` (robot 자체의 obstacle 거리 + path 대비 cut)
- `/tmp/analyze_safety_conflict.py` (jupiter_safety vs nav2 충돌 정량화)

### 7.3 생성된 문서

- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_05_08.md`
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_05_08.md` (이 파일)
