# 진단 명령어 2026-03-20: 제자리 회전 헌팅 분석

## Phase 1: 시스템 상태 확인

### 1.1 노드 상태 확인
```bash
ros2 node list 2>/dev/null | grep -E "driver|smoother|controller"
```
**목적**: jupiter_driver, velocity_smoother, controller_server 노드 실행 여부 확인
**결과**:
```
/controller_server
/jupiter_driver
/velocity_smoother
```
- jupiter_driver: 이전 세션에서 `ros2 param set` 크래시 후 재시작됨 (launch 재실행)

### 1.2 PID 파라미터 확인
```bash
ros2 param get /jupiter_driver Ki
ros2 param get /jupiter_driver Kd
ros2 param get /jupiter_driver Kp
```
**목적**: MCU에 적용된 실제 PID 값 확인
**결과**:
```
Ki: Double value is: 0.06   ← yaml에서 로드 (사용자가 0.02→0.06 복원)
Kd: Double value is: 0.5    ← yaml에서 로드 (사용자가 0.3→0.5 복원)
Kp: Double value is: 0.8
```
**교훈**: ROS2 `declare_parameter` 기본값은 yaml이 오버라이드함. 코드만 변경하면 적용 안됨.

---

## Phase 2: Nav2 DWB RotateToGoal 소스 분석

### 2.1 RotateToGoal 헤더 파일 확인
```bash
cat /opt/ros/humble/include/dwb_critics/rotate_to_goal.hpp
```
**목적**: RotateToGoal critic의 멤버 변수 및 3단계(Phase 1-3) 구조 파악
**결과**:
- Phase 1: `in_window_=false` → score 0 (xy_goal_tolerance 밖)
- Phase 2: `rotating_=false` + 감속 중 → `speed²×slowing_factor + scoreRotation`
- Phase 3: `rotating_=true` → `vx≠0` 거부, 순수 회전만 `scoreRotation`
- `slowing_factor_`는 Phase 2에서만 사용 (Phase 3 영향 없음)

### 2.2 RotateToGoal 소스코드 확인 (GitHub)
```
https://github.com/ros-planning/navigation2/blob/humble/
  nav2_dwb_controller/dwb_critics/src/rotate_to_goal.cpp
```
**목적**: `scoreTrajectory()`, `scoreRotation()` 알고리즘 정확히 확인
**결과**: Phase 3에서 `scoreRotation` = `fabs(shortest_angular_distance(end_yaw, goal_yaw_))` → sim_time 후 잔여 heading error가 점수

---

## Phase 3: MCU 펌웨어 소스 분석

### 3.1 ESC 데드존 스킵 (방안A-4)
```bash
grep -n "DEADZONE\|Motor_Ignore_Dead_Zone\|ESC_DEADZONE\|esc_value\|active_range" \
  /home/jetson/mcu/extbd/Source/BSP/bsp_motor.c | head -40
```
**목적**: 방안A-4 비례매핑이 정확히 ESC 어떤 값으로 매핑하는지 확인
**결과**:
```c
// pulse > 0: ESC 111 + (pulse × 33 / 3600)
// pulse < 0: ESC 105 + (pulse × 33 / 3600)
// pulse = 0: ESC 108 (중립)
```
- pulse=1 → ESC 111 (데드존 경계), pulse=110 → ESC 112 (첫 초과)
- pulse 1~109 → 모두 ESC 111로 고정 (잔존 미세 데드존)

### 3.2 ESC 상수 확인
```bash
grep -n "MOTOR_FREQ_STOP\|MOTOR_FREQ_FORWARD\|MOTOR_FREQ_BACKWARD\|MOTOR_MAX_PULSE" \
  /home/jetson/mcu/extbd/Source/APP/app_differential.h \
  /home/jetson/mcu/extbd/Source/BSP/bsp_motor.h
```
**목적**: ESC 주파수 범위 확인
**결과**:
```
MOTOR_FREQ_FORWARD_FAST = 144  (2.0ms duty, 최대 전진)
MOTOR_FREQ_FORWARD      = 125  (1.74ms)
MOTOR_FREQ_STOP         = 108  (1.5ms, 중립)
MOTOR_FREQ_BACKWARD     =  90  (1.24ms)
MOTOR_FREQ_BACKWARD_FAST=  72  (1.0ms, 최대 후진)
MOTOR_MAX_PULSE         = 3600
```

### 3.3 모터 출력 필터 확인
```bash
grep -n "Motor_Apply_Filter\|MOTOR_FILTER_ALPHA\|motor_last_pulse" \
  /home/jetson/mcu/extbd/Source/BSP/bsp_motor.c
```
**목적**: PID→ESC 사이의 저역통과 필터 특성 확인
**결과**:
```c
#define MOTOR_FILTER_ALPHA 0.3f
filtered = 0.3 * new_pulse + 0.7 * last_pulse;
```
- 90% 도달 시간: ~77ms (10ms × 6.5 cycles)
- [방안A-3]에서 0.1→0.3으로 변경 (이전 230ms→77ms)

### 3.4 PID 알고리즘 확인
```bash
# app_pid.c 내 PID_Incre_Calc 함수
```
**목적**: incremental PID 구현 및 anti-windup 메커니즘 확인
**결과**:
```c
pid->pwm_output += Kp*(err-err_next) + Ki*err + Kd*(err-2*err_next+err_last);
// Anti-windup: 출력 클램프만 (±3600)
// 적분 freeze 없음 → 방향전환 시 누적 출력 감쇠 필요
```

### 3.5 Differential 제어 확인
```bash
# app_differential.c 내 Differential_Ctrl 함수
```
**목적**: Vz(mrad/s) → 좌/우 바퀴 속도 변환 공식 확인
**결과**:
```c
speed_spin = (V_z / 1000.0f) * 164.555;  // mm/s per wheel
speed_L1 = speed_fb - speed_spin;          // 좌바퀴
speed_R1 = speed_fb + speed_spin;          // 우바퀴
// Vx=0, Vz=500 → spin=82.3mm/s → L=-82, R=+82
```

### 3.6 시리얼 프로토콜 확인
```bash
# Rosmaster_Lib.py 내 set_car_motion 함수
```
**목적**: 드라이버→MCU 직렬 통신의 단위 변환 확인
**결과**:
```python
vz_parms = struct.pack('h', int(v_z * 1000))  # rad/s → mrad/s (int16)
# v_z = 0.12 → 120 mrad/s → MCU Differential_Ctrl 수신
```

---

## Phase 4: DWB 데드존 정량 분석 (Python 계산 스크립트)

### 4.1 현재 설정 (min_speed_theta=0.30) 데드존 계산
```python
import math

# DWB velocity samples
max_vt = 1.0; vtheta_samples = 20; min_speed_theta = 0.30
sim_time = 1.5; acc_lim = 0.8

step = 2.0 * max_vt / (vtheta_samples - 1)  # 0.1053

# 유효 양수 샘플 (min_speed_theta 이상만)
valid = [0, 0.368, 0.474, 0.579, 0.684, 0.789, 0.895, 1.0]
# → 0과 0.368 사이 빈 구간!

# 최소 비영 샘플 시뮬레이션
v = 0.368
t_accel = v / acc_lim  # 0.46s
rotation = 0.5 * v * t_accel + v * (sim_time - t_accel)  # 0.468 rad = 26.8°

# DWB 점수 교차점
# Score(0) = 56 × error,  Score(0.368) = 56 × |error - 0.468| + 5 × 0.368
crossover = (56 * 0.468 + 5 * 0.368) / (2 * 56)  # 0.250 rad = 14.3°

print(f"데드존: {math.degrees(0.15):.1f}° ~ {math.degrees(crossover):.1f}° = {math.degrees(crossover-0.15):.1f}° 폭")
```
**목적**: DWB 속도 샘플 갭이 만드는 데드존 정량화
**결과**:
```
유효 양수 샘플: [0, 0.368, 0.474, 0.579, 0.684, 0.789, 0.895, 1.0]
최소 비영 샘플: 0.368 rad/s
sim_time=1.5s 회전: 26.8°
DWB 교차점: 14.3° (0.250 rad)
yaw_goal_tolerance: 8.6° (0.15 rad)
데드존: 8.6° ~ 14.3° (5.7° 폭) → 이 구간에서 DWB가 정지만 출력 → 헌팅!
```

### 4.2 수정 설정 (min_speed_theta=0.15) 검증
```python
min_speed_theta = 0.15
valid = [0, 0.158, 0.263, 0.368, 0.474, ...]

# 최소 비영 샘플
v = 0.158
t_accel = v / acc_lim  # 0.197s
rotation = 0.5 * v * t_accel + v * (sim_time - t_accel)  # 0.198 rad = 12.7°

crossover = (56 * 0.198 + 5 * 0.158) / (2 * 56)  # 0.118 rad = 6.7°
# 6.7° < 8.6° (yaw_goal_tolerance) → 데드존 제거!
```
**목적**: 수정 후 데드존 해소 확인
**결과**:
```
유효 양수 샘플: [0, 0.158, 0.263, 0.368, 0.474, ...]  (2개 추가)
최소 비영 샘플: 0.158 rad/s
sim_time=1.5s 회전: 12.7°
DWB 교차점: 6.7° < yaw_goal_tolerance(8.6°) → 데드존 완전 제거!
```

### 4.3 물리적 체인 검증
```python
angular_nav2 = 0.158               # DWB 최소 비영 샘플
angular_scaled = 0.158 * 0.401     # = 0.063 (드라이버 스케일)
MCU_MIN_ANGULAR = 0.12
# 0.063 < 0.12 → MCU_MIN clamp
angular_mcu = 0.120                # MCU에 전달되는 실제 값
speed_spin = 0.120 * 164.555       # = 19.7 mm/s
ticks = 19.7 / 16.3               # = 1.21 ticks/10ms (양자화 통과)
nav2_equiv = 0.120 / 0.401         # = 0.299 rad/s (1.9x 증폭)
rot_per_cycle = 0.299 * 0.1        # = 1.71°/DWB cycle
```
**목적**: DWB 0.158 명령이 물리적으로 실행 가능한지 검증
**결과**:
```
MCU speed: 19.7 mm/s → 1.21 ticks (양자화 OK)
Nav2 등가: 0.299 rad/s (MCU_MIN clamp로 1.9x 증폭)
DWB 사이클당: 1.71° 회전
12°→8.6° 도달: ~2 사이클 (0.2초)
```

---

## Phase 5: Nav2 yaml 현재 설정 확인

### 5.1 RotateToGoal 관련 파라미터
```bash
grep -n "min_speed_theta\|yaw_goal_tolerance\|rotate_to_heading\|max_angular_accel\|slowing_factor\|xy_goal_tolerance\|vtheta_samples\|sim_time\|acc_lim_theta\|controller_frequency" \
  /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```
**목적**: 헌팅 관련 모든 Nav2 파라미터 정리
**결과** (적용 후):
```yaml
controller_frequency: 10.0
yaw_goal_tolerance: 0.15         # 0.10→0.15 [2026-03-20]
rotate_to_heading_angular_vel: 0.5  # 0.8→0.5 [2026-03-20]
max_angular_accel: 3.0            # 5.0→3.0 [2026-03-20]
max_vel_theta: 1.0
min_speed_theta: 0.15             # ⭐ 0.30→0.15 [2026-03-20] 핵심 수정
acc_lim_theta: 0.8
vtheta_samples: 20
sim_time: 1.5
xy_goal_tolerance: 0.25
RotateToGoal.slowing_factor: 5.0
```

### 5.2 velocity_smoother 설정
```bash
grep -A20 "velocity_smoother:" \
  /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```
**목적**: smoother의 deadband, 가감속 확인
**결과**:
```yaml
feedback: "OPEN_LOOP"
max_accel: [0.5, 0.0, 2.5]
max_decel: [-0.5, 0.0, -2.5]
deadband_velocity: [0.0, 0.0, 0.05]
velocity_timeout: 1.0
```

---

## Phase 6: 드라이버 코드 확인

### 6.1 ANGULAR_SCALE_FACTOR 및 MCU_MIN_ANGULAR
```bash
grep -n "ANGULAR_SCALE_FACTOR\|MCU_MIN_ANGULAR" \
  /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```
**목적**: 드라이버의 스케일 팩터와 최소 클램프 값 확인
**결과**:
```python
ANGULAR_SCALE_FACTOR = 0.401  # 방안A-4 기준 재캘리브레이션
MCU_MIN_ANGULAR = 0.12        # 19.7 mm/s → 1.21 ticks/period
```

### 6.2 MCU_MIN_ANGULAR 클램프 조건
```bash
grep -A5 "abs(vx) < 0.02" \
  /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```
**목적**: 클램프가 정지 회전에서만 적용되는지 확인
**결과**:
```python
if abs(vx) < 0.02:  # 정지 상태에서만 클램프
    if abs(angular_corrected) > 0.001 and abs(angular_corrected) < self.MCU_MIN_ANGULAR:
        angular_corrected = math.copysign(self.MCU_MIN_ANGULAR, angular_corrected)
```
- vx < 0.02 (정지) + angular_corrected 0.001~0.12 → 0.12로 클램프
- 전진 중에는 클램프 미적용 (올바름)

### 6.3 yaml PID 오버라이드 확인
```bash
cat /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/param/jupiter_driver_params.yaml
```
**목적**: yaml이 코드 기본값을 오버라이드하는지 확인
**결과**:
```yaml
Kp: 0.8   # 코드 0.8, yaml 0.8 (일치)
Ki: 0.06  # 코드 0.02, yaml 0.06 (yaml이 오버라이드!)
Kd: 0.5   # 코드 0.3, yaml 0.5 (yaml이 오버라이드!)
```
**결론**: yaml이 항상 우선 → 코드만 바꾸면 적용 안됨

---

## Phase 7: 빌드 및 적용

### 7.1 패키지 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
source /opt/ros/humble/setup.bash
colcon build --packages-select jupiter_nav_VO --symlink-install 2>&1 | tail -10
```
**목적**: nav2_params_fused.yaml 변경 반영
**결과**:
```
Finished <<< jupiter_nav_VO [4.10s]
Summary: 1 package finished [4.49s]
```

### 7.2 재시작 (사용자 실행)
```bash
# 기존 launch 종료 (Ctrl+C)
cd ~/jupiter_ws_ros2
source install/setup.bash
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py   # 센서 + 드라이버
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py  # Nav2
```

### 7.3 적용 확인
```bash
ros2 param get /controller_server FollowPath.min_speed_theta
# 기대: Double value is: 0.15
```

---

## Phase 8: 검증 테스트 (재시작 후)

### 8.1 test_deadzone_bypass.py 실행
```bash
python /home/jetson/jupiter_ws_ros2/scripts/test_deadzone_bypass.py
```
**목적**: 자동 테스트로 Goal2 (도달 후 회전) 헌팅 여부 확인
**기대**: Goal2 yaw_span < 30° (이전: 285.4°)

### 8.2 Foxglove 수동 테스트
```
Foxglove → 3D Panel → Publish goal → 로봇 옆 뒤쪽 (90° 회전 필요한 목표)
```
**목적**: 실제 사용 시나리오에서 "도달 후 회전" 헌팅 해소 확인
**기대**: 목표 도달 후 1~2초 내 정지 (이전: 30초+ 헌팅)

### 8.3 cmd_vel 모니터링
```bash
ros2 topic echo /cmd_vel --no-arr | grep -E "angular|linear" | head -100
```
**목적**: DWB가 데드존에서 0과 0.368을 교대하지 않는지 확인
**기대**: 목표 근접 시 angular.z가 0.158 수준으로 점진적 감소

---

## 변경 파일 요약

| 파일 | 변경 | 비고 |
|---|---|---|
| nav2_params_fused.yaml | min_speed_theta: 0.30→**0.15** | 핵심 수정 — DWB 데드존 제거 |
| nav2_params_fused.yaml | rotate_to_heading_angular_vel: 0.8→0.5 | 이전 세션 변경 유지 |
| nav2_params_fused.yaml | max_angular_accel: 5.0→3.0 | 이전 세션 변경 유지 |
| nav2_params_fused.yaml | yaw_goal_tolerance: 0.10→0.15 | 이전 세션 변경 유지 |
| jupiter_driver_params.yaml | Ki=0.06, Kd=0.5 | 사용자 복원 (변경 없음) |
