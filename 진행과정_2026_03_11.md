# 진행과정 2026-03-11: VSLAM Adapter Auto-Recovery 및 Nav2 검증

> 이 문서는 2026-03-11 세션의 전체 작업 진행과정을 기록합니다.
> 진행 순서: 센서 검증 → adapter fix 확인 → Nav2 1.5m 테스트 → Nav2 Multi-Goal 테스트 → Permanent Lock-out 발견 → Auto-Recovery 구현 → 검증 테스트

---

## 목차
1. [세션 목표](#1-세션-목표)
2. [스택 재시작 후 센서 검증](#2-스택-재시작-후-센서-검증)
3. [Adapter prev_odom Fix 확인](#3-adapter-prev_odom-fix-확인)
4. [Nav2 1.5m Goal Test](#4-nav2-15m-goal-test)
5. [Nav2 Multi-Goal Test (3 waypoints)](#5-nav2-multi-goal-test-3-waypoints)
6. [Permanent Lock-out 버그 발견](#6-permanent-lock-out-버그-발견)
7. [Auto-Recovery 메커니즘 설계](#7-auto-recovery-메커니즘-설계)
8. [Auto-Recovery 코드 구현](#8-auto-recovery-코드-구현)
9. [Adapter 재시작 및 배포](#9-adapter-재시작-및-배포)
10. [Adapter 재시작 후 정지 상태 검증](#10-adapter-재시작-후-정지-상태-검증)
11. [Auto-Recovery 검증 테스트 (2m 전진+복귀)](#11-auto-recovery-검증-테스트-2m-전진복귀)
12. [cuVSLAM 행동 패턴 분석](#12-cuvslam-행동-패턴-분석)
13. [Nav2 ABORTED 원인 조사 및 수정](#13-nav2-aborted-원인-조사-및-수정)
14. [cuVSLAM 근본 원인 조사](#14-cuvslam-근본-원인-조사)
15. [Auto-Recovery 실제 트리거 검증](#15-auto-recovery-실제-트리거-검증)
16. [현재 상태 요약](#16-현재-상태-요약)
17. [다음 단계](#17-다음-단계)
18. [Nav2 2m 전진+복귀 테스트 — ABORTED 해소 확인](#18-nav2-2m-전진복귀-테스트--aborted-해소-확인)
19. [다음 단계 (갱신)](#19-다음-단계-갱신)
20. [좌측 편향 현상 분석 — 순수 직진 테스트](#20-좌측-편향-현상-분석--순수-직진-테스트)
21. [좌측 편향 현상 분석 — Nav2 주행 진단](#21-좌측-편향-현상-분석--nav2-주행-진단)
22. [좌측 편향 원인 결론 및 다음 단계](#22-좌측-편향-원인-결론-및-다음-단계)
23. [VSLAM ON 재테스트 — 좌측 편향 비교](#23-vslam-on-재테스트--좌측-편향-비교)
24. [종합 분석 및 다음 단계](#24-종합-분석-및-다음-단계)

---

## 1. 세션 목표

- **P1**: 스택 재시작 후 센서 검증 + adapter fix 확인 + Nav2 gating 테스트
- **P2**: anomaly threshold 평가
- **P1 결과 추가**: VSLAM adapter permanent lock-out 버그 발견 및 수정

### 배경

3월 10일 세션에서 VSLAM adapter의 `prev_odom` 항상 업데이트 버그를 발견하여 조건부 업데이트로 원복하였다.
이번 세션은 해당 수정이 제대로 적용되었는지 확인하고, 실제 Nav2 주행으로 gating 동작을 검증하는 것이 목표였다.

사용자가 스택(호스트 Nav2 + Docker VSLAM)을 재시작한 뒤 "다음 단계를 진행하라"라고 지시하여, 계획된 P1 검증 절차를 순차 실행하였다.

---

## 2. 스택 재시작 후 센서 검증

rclpy 스크립트로 10개 토픽을 8초간 구독하여 주파수, 위치, 맵 범위를 검증하였다.

### 결과

모든 9개 센서 토픽 정상 확인:

| 센서 | 토픽 | 주파수 | 상태 |
|------|------|--------|------|
| IMU | `/imu/data` | 85.2 Hz | ✅ 정상 |
| EKF | `/odom` | 29.6 Hz | ✅ (0.029, -0.015) |
| Wheel | `/odom_raw` | 49.9 Hz | ✅ 정상 |
| RF2O | `/odom_rf2o` | 10.0 Hz | ✅ 정상 |
| LiDAR | `/scan` | 10.2 Hz | ✅ 정상 |
| VSLAM raw | `/visual_slam/tracking/odometry` | 59.3 Hz | ✅ 정상 |
| VSLAM adapted | `/visual_slam/tracking/odometry_adapted` | 5.3 Hz | ✅ (9% pass) |
| Map | `/map` | OccupancyGrid 52×132 | ✅ EKF in bounds |
| EKF-RF2O divergence | — | 0.035m | ✅ 정상 범위 |

VSLAM adapted가 9% pass-through인 것은 정지 상태에서 frozen detection의 1Hz downsample 동작이며 정상이다.

---

## 3. Adapter prev_odom Fix 확인

3월 10일 수정한 조건부 prev_odom 업데이트가 배포된 코드에 반영되어 있는지 grep으로 직접 확인하였다.

```bash
grep -n "prev_odom" src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py
```

**확인 결과** — Line 349:
```python
if not is_anomaly:
    self.prev_odom = pose
```
조건부 업데이트 정상 확인. adapter_status 토픽은 메시지 없음 (해당 토픽 미구현 상태).

---

## 4. Nav2 1.5m Goal Test

`MultiThreadedExecutor` 사용 rclpy 스크립트로 Nav2에 1.5m 전진 목표를 전송하고, EKF/VSLAM/cmd_vel을 동시에 모니터링하였다.

### 결과

- **Goal**: ACCEPTED → 스크립트의 rclpy ActionClient 스레드 에러로 결과 timeout
  - `RCLError: Failed to get number of ready entities for action client: wait set index for status subscription is out of bounds`
  - 이것은 ROS 2 Humble의 알려진 이슈 (threaded ActionClient)이며 Nav2 자체 문제 아님
- **실제 로봇 이동 확인**:
  - EKF: (0.029, -0.014) → (1.346, -0.072), travel = **1.318m** ✅
  - VSLAM raw: x range [-0.010, 1.348], **점프 없음** (최대 inter-frame Δ < 0.1m) ✅
  - VSLAM adapted: 287 msgs passed (89% pass-through) ✅
  - cmd_vel: 100 commands ✅
  - Adapter anomaly/gating 로그: **0건** (모든 데이터 정상 통과)

**결론**: Nav2 자체는 정상 작동, VSLAM 텔레포트 없음, adapter 조건부 prev_odom 수정이 정상 동작 확인됨.

---

## 5. Nav2 Multi-Goal Test (3 waypoints)

이전 테스트의 rclpy 스레드 에러를 해결하기 위해 `MultiThreadedExecutor` + polling 기반 goal 관리 스크립트를 작성하여 3개 연속 목표 테스트를 수행하였다.

### 테스트 구성
- Goal 1: 현재 위치에서 1.0m 전진 (heading 방향)
- Goal 2: Goal 1 도착 후 1.0m 좌측 이동 (90° 회전)
- Goal 3: 시작 위치로 복귀

### 결과

| Goal | 목표 좌표 | 결과 | 소요시간 | 목표 잔여거리 |
|------|-----------|------|----------|---------------|
| Goal 1 (1m 전진) | (2.41, 0.31) | **SUCCEEDED** | 4.3s | 0.482m |
| Goal 2 (1m 좌측) | (1.42, 1.10) | **TIMEOUT→CANCELED** | 31.1s | 0.241m |
| Goal 3 (복귀) | (1.47, -0.03) | **SUCCEEDED** | 9.9s | 0.203m |

### VSLAM 상태 (테스트 중)
- **raw**: x=[0.724, 2.619], n=2894 (48s, ~60Hz)
- **adapted**: n=51 (2% pass-through) ⚠️ — 매우 낮음
- **raw 점프 1건**: 1.626 → 2.329 (**Δ=+0.703m**) — adapter가 anomaly로 차단 ✅
- **EKF-VSLAM 최대 x-divergence**: 1.144m — adapter gating 덕분에 EKF 보호됨

### Goal 2 TIMEOUT 원인
Goal 2는 90° 회전 + 1m 이동이 필요한데, 회전 시 costmap 업데이트 지연 또는 DWB 경로 계획 실패로 30초 timeout에 도달한 것으로 추정. VSLAM 관련 이슈 아님.

---

## 6. Permanent Lock-out 버그 발견

### 발견 과정

Multi-Goal 테스트 종료 후 정지 상태에서 adapter pass-through를 확인한 결과:

```
5s check: raw=271 (54.2 Hz), adapted=0 msgs
Pass-through: 0/271 (0%)
⚠️ Adapter is heavily gating — VSLAM likely in anomaly/lost state
```

테스트 중 2% → 테스트 후 **0%**. Adapter가 완전 차단 상태에 진입.

### 원인 분석

```
시간순:
1. VSLAM 텔레포트 발생: position +0.703m 점프
2. _check_anomaly() → is_anomaly=True, prev_odom 동결 (old position)
3. VSLAM은 새 위치에서 안정적으로 추적 계속
4. 매 프레임: delta = |new_pos - old_prev_odom| > 0.5m → 영구 anomaly
5. VSLAM이 old_prev_odom 위치로 돌아오지 않는 한 복구 불가능
6. EKF에 VSLAM 데이터 완전 차단 → 사실상 3-sensor fusion으로 격하
```

이것은 3월 10일 수정의 **설계 한계**. 조건부 prev_odom은 -50m drift를 방지하지만, 텔레포트 후 영구 lock-out이라는 새로운 문제를 생성.

### 핵심 관찰
- EKF는 VSLAM을 **differential 모드** (`odom1_differential: true`)로 사용
- differential 모드에서는 **절대 위치 변화는 무관**, 프레임간 delta 정확성만 중요
- VSLAM 텔레포트 후에도 inter-frame delta가 작으면 안정적 추적을 의미

---

## 7. Auto-Recovery 메커니즘 설계

### 설계 원칙
1. VSLAM 텔레포트 후에도 안정적으로 추적 중이면, baseline을 새 위치로 리셋하여 lock-out 해제
2. 충분한 안정성 검증 후에만 리셋 (5초 anomaly 유지 + 2초 연속 안정)
3. 리셋 후 recovery_delay (1초) + recovery_cov_multiplier (5.0x)로 soft transition
4. EKF differential 모드에서 절대 위치 점프가 EKF에 미치는 영향 없음

### 추가 파라미터 (3개)

| 파라미터 | 기본값 | 설명 |
|----------|--------|------|
| `anomaly_reset_count` | 300 | 연속 anomaly 후 리셋 검토 (~5s @60Hz) |
| `stable_interframe_threshold` | 0.05 | m — 프레임간 안정 판정 기준 |
| `stable_interframe_count` | 120 | 연속 안정 프레임 → 리셋 실행 (~2s @60Hz) |

### 추가 상태 변수 (3개)

| 변수 | 초기값 | 설명 |
|------|--------|------|
| `last_raw_pose` | None | 항상 업데이트되는 최신 VSLAM pose (auto-recovery용) |
| `stable_interframe_counter` | 0 | anomaly 중 연속 안정 inter-frame 수 |
| `auto_recovery_count` | 0 | auto-recovery 발생 횟수 (통계) |

### 메커니즘 동작 시퀀스

```
매 프레임 (_check_anomaly):
  1. prev_odom 기반 anomaly 판정 (기존 로직)
  2. last_raw_pose 기반 inter-frame delta 추적 (신규 — 항상 실행)
     - delta < 0.05m → stable_interframe_counter++
     - delta >= 0.05m → stable_interframe_counter = 0
  3. last_raw_pose = pose (항상 업데이트)

매 프레임 (_update_tracking_state → anomaly 분기):
  4. consecutive_anomaly >= 300 AND stable_interframe_counter >= 120 ?
     → Yes: auto-recovery 실행
       - prev_odom = last_raw_pose (새 baseline)
       - consecutive_anomaly = 0
       - stable_interframe_counter = 0
       - anomaly_detected = False
       - tracking_recovered_time = now
       - 이번 프레임은 gating 유지, 다음부터 새 baseline 판정
```

### 안전장치
- **5초 대기** (anomaly_reset_count=300): 텔레포트 직후 불안정 기간 대기
- **2초 안정성 검증** (stable_interframe_count=120): inter-frame delta 일관성 확인
- **recovery_delay 1초**: 리셋 후 추가 대기 → 총 **8초 이상** 검증 후 발행 재개
- **EKF differential 모드**: 절대 위치 점프가 EKF에 미치는 영향 없음
- **recovery_cov_multiplier 5.0x**: 복구 직후 높은 불확실성 반영

---

## 8. Auto-Recovery 코드 구현

### 파일: `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py`

총 7곳 수정. 각 변경의 이전/이후 코드는 진단_명령어 문서에 기록.

#### 변경 요약:
1. **파라미터 선언 추가** (Line 89-95): `anomaly_reset_count`, `stable_interframe_threshold`, `stable_interframe_count` 3개 파라미터 declare
2. **파라미터 로드 추가** (Line 131-133): 3개 파라미터 get_parameter
3. **상태 변수 추가** (Line 159-161): `last_raw_pose`, `stable_interframe_counter`, `auto_recovery_count`
4. **_check_anomaly() inter-frame 추적** (Line 372-383): 항상 실행되는 inter-frame delta 계산 및 stable 카운터 관리
5. **_update_tracking_state() auto-recovery** (Line 409-427): anomaly 분기에서 조건 확인 및 baseline 리셋 로직
6. **log_stats() 확장** (Line 580-581): `stable_if`, `auto_recov` 카운터 로그 추가
7. **초기화 로그 확장** (Line 213-215): auto-recovery 파라미터 표시

---

## 9. Adapter 재시작 및 배포

### 배포 과정

1. `pip install -e src/jupiter_nav_VO/` — editable install (소스 변경 즉시 반영)
2. `python3 -c "from jupiter_nav_VO.vslam_covariance_adapter import VslamCovarianceAdapter; ..."` — import 및 assertion 검증
3. `kill 52946` — 기존 VSLAM adapter 프로세스 종료 (RF2O adapter PID 53035는 유지)
4. `nohup python3 ... &` — 새 코드로 adapter 재시작

### 재시작 확인

```
[INFO] VslamCovarianceAdapter: /visual_slam/tracking/odometry → /visual_slam/tracking/odometry_adapted
  min_cov: pos=0.05, orient=0.02
  anomaly detection: max_yaw_delta=8.6°, max_pos_delta=0.5m, good_count_threshold=30
  frozen detection: threshold=10 msgs, pos_tol=1e-06m, yaw_tol=1e-06rad, downsample=1.0Hz
  recovery: delay=1.0s, cov_multiplier=5.0x, decay_time=3.0s
  auto-recovery: reset_after=300 anomalies, stable_threshold=0.05m, stable_count=120
  vo_state: UNAVAILABLE (anomaly detection only)
```

auto-recovery 파라미터가 로그에 표시되어 새 코드가 정상 로드됨을 확인.

---

## 10. Adapter 재시작 후 정지 상태 검증

5초간 VSLAM raw/adapted 토픽을 구독하여 adapter 동작 확인:

```
5s check: raw=302 (60Hz), adapted=14 (3Hz), pass=5%
  raw last: (-0.178, 0.120)
  adapted last: (-0.178, 0.120)
✅ Adapter passing through (frozen downsample)
```

0% → **5%** 복구. 재시작 후 frozen 1Hz downsample이 정상 동작.
(기존: prev_odom이 lock-out 상태라 0% 였으나, 재시작 시 prev_odom=None으로 초기화되어 첫 메시지부터 새 baseline 설정)

---

## 11. Auto-Recovery 검증 테스트 (2m 전진+복귀)

`MultiThreadedExecutor` + `NavigateToPose` ActionClient + 5개 토픽 모니터링 + `/rosout` adapter 로그 캡처 스크립트로 검증.

### 테스트 구성
- Goal 1: 현재 위치에서 2.0m 전진
- Goal 2: 시작 위치로 복귀

### 결과

| Goal | 결과 | 소요시간 | 목표 잔여거리 |
|------|------|----------|---------------|
| 2m 전진 | ABORTED | 17.7s | 1.131m |
| 복귀 | ABORTED | 15.9s | 0.109m |

### VSLAM 상태

- **raw**: 2161 msgs (37s, ~58Hz)
- **adapted**: 1535 msgs, **pass-through: 71%** ✅ (이전 0% → 71%)
- **raw 점프 >0.3m**: **0건** — VSLAM 완전 안정 ✅
- **anomaly**: 0건
- **auto-recovery**: 0회 (텔레포트 미발생으로 불필요)
- **cmd_vel**: 703 commands

### Adapter 30초 통계 로그

```
Stats: 12563 msgs | pub=2759 (22%) | gated=9804 (78%)
[anomaly=0, frozen=9804, frozen_pub=190, vo_state=0]
tracking_ok=True frozen_count=29 consec_anomaly=0
stable_if=2534 auto_recov=0 cov_mult=1.0x
```

- **stable_if=2534**: inter-frame 안정 카운터가 정상적으로 누적 (auto-recovery 판정 근거 기능 확인)
- **auto_recov=0**: 텔레포트 미발생으로 recovery 불필요 (대기 상태)
- **22% pub, 78% frozen gate**: cuVSLAM 고유의 burst-duplicate 패턴으로 frozen 감지 정상

### Adapter 로그 이벤트 (74건)

대부분 FROZEN/unfrozen 반복:
```
VSLAM FROZEN: 10 consecutive identical poses
VSLAM unfrozen after 11 identical msgs. pos_delta=0.036885m, yaw_delta=0.566°
VSLAM FROZEN: 10 consecutive identical poses
VSLAM unfrozen after 13 identical msgs. pos_delta=0.093914m, yaw_delta=0.087°
...
```

anomaly 관련 로그 0건 — VSLAM이 이번 테스트에서는 완전히 안정적이었음.

### Nav2 ABORTED 원인 분석
- **2m 전진 ABORTED**: 장애물/costmap expansion으로 경로 계획 실패 추정. 0.869m만 이동 후 중단.
- **복귀 ABORTED**: 0.109m 거리까지 접근 (사실상 도착). goal_tolerance 또는 회전 요구 실패 추정.
- **VSLAM adapter 관련 이슈 아님** — 두 경우 모두 Nav2 planner/controller 문제.

---

## 12. cuVSLAM 행동 패턴 분석

테스트 과정에서 관찰된 cuVSLAM v12.6 특성:

### 12-1. Burst-duplicate 패턴
- 60Hz로 데이터 발행하나 실제 pose 업데이트는 ~1-5Hz
- 10-245 프레임 연속 동일 pose → 실제 업데이트 1건 → 다시 동일 pose 반복
- Frozen detection이 이 패턴을 정상적으로 감지/downsample (frozen_threshold=10)
- 원인: cuVSLAM 내부 keyframe 처리 주기와 ROS publisher 주기 차이

### 12-2. Unfrozen delta 크기
- 대부분 0.001-0.05m (정상: 로봇 속도에 비례)
- 간혹 0.25m (급격한 이동 또는 keyframe 매칭 보정)
- 이번 세션: 0.703m 점프 1건 (테스트 1), 0건 (테스트 2)

### 12-3. 텔레포트 특성
- 세션 1회 (0.703m 점프): Multi-Goal 테스트 중 Goal 2 회전 구간
- 추정 원인: 회전 + 조명 변화 + feature-poor 환경
- 예측 불가능 — random하게 발생

---

## 13. Nav2 ABORTED 원인 조사 및 수정

배터리 문제로 주행 불가 상태에서, Nav2 costmap 및 파라미터 분석으로 ABORTED 원인을 진단하고 수정하였다.

### 13-1. 조사 배경

이전 Multi-Goal 테스트(12절)에서 3개 waypoint 중 Goal 2(좌측 1m)가 TIMEOUT→CANCELED, Goal 3(원점 복귀)이 ABORTED로 실패하였다. Goal 3의 경우, 로봇이 원점 근처(0.109m)까지 도달했음에도 최종 yaw 회전 중 ABORTED가 발생하였다. 이 반복적인 ABORTED 문제의 근본 원인을 파악하기 위해, 주행 없이 정지 상태에서 costmap/map/파라미터 분석을 수행하였다.

### 13-2. Costmap 공간 진단

정지 상태에서 `/odom`, `/map`, `/local_costmap/costmap`, `/global_costmap/costmap` 토픽을 구독하여 로봇 주변 공간 상태를 분석하였다.

#### SLAM Map 상태
- 크기: 58×140 cells (2.9×7.0m @ 0.05m resolution)
- 구성: free 2824(35%), occupied 101(1%), **unknown 5195(64%)** — 매우 작은 탐색 영역
- 로봇 위치: (0.000, 0.000), yaw≈4°
- 맵 경계: x=[-0.03, 2.87], y=[-3.58, 3.42]
- 전방(+x) 여유: 2.87m, **뒤쪽(-x) 여유: 0.03m** — 로봇이 맵 끝단에 위치

#### Global Costmap 8방향 분석

Python 스크립트로 8방향(0.2m~1.5m, 0.1m 간격)의 cost 값을 수집하였다.

| 방향 | Cost 범위 | 첫 free(cost=0) 거리 | 상태 |
|------|----------|---------------------|------|
| Forward (+x) | 60~92 | **1.3m** | 1.2m 전체 inflated corridor |
| Back (-x) | -1 (전부) | — | **맵 바깥** (뒤로 이동 불가) |
| Left (+y) | 60~100 | NONE (2m 이내 없음) | 벽 쪽 inflation |
| Right (-y) | 0~100 | **0.2m** | 가장 빠르게 free zone 진입 |
| FwdLeft | 99~100 | NONE | 벽 모서리 방향 |
| FwdRight | 0~99 | **0.3m** | 차선 이동 방향 |

- **Lethal cells: 0** — global costmap에 lethal(254) 셀이 없음. 벽/장애물은 static layer에서 inflation으로만 표현됨
- **로봇 cell cost: 69** — inscribed(253)은 아니지만, 상당한 inflation zone 내부에 위치
- **전방 경로 cost profile** (10cm 간격):
  ```
  0.0m: 69  →  0.4m: 92  →  0.8m: 77  →  1.2m: 60  →  1.3m: 0 (free)
  ```
  전방 1.2m가 전부 cost 60~92로 채워진 inflation corridor

#### Local Costmap Grid (11×11, ±25cm)
```
    .  .  .  .  .  .  .  .  .  .  .
    .  .  .  .  .  .  .  .  .  .  .
    .  .  .  .  .  .  .  .  . 51 51
    .  .  .  .  .  . 53 56 58 59 59
    .  .  .  .  . 55 60 65 67 69 69
    .  .  .  . 55 62 69 74 78 79 79 ← 로봇
    .  .  . 55 62 69 77 84 90 92 92
    .  . 55 62 69 77 84 95 99 99 99
    . 53 60 69 77 84 95 99 99 99 99
    . 56 65 74 84 95 99 99 99100100
    . 58 67 78 90 99 99 99100100100
```
- 로봇의 **좌상(↖)은 free(cost=0)**, **우하(↘)은 wall inflation(cost 100)**
- cost gradient가 우하→좌상 방향으로 감소 — 벽이 로봇의 우하측에 위치
- DWB의 유효 trajectory 공간이 제한적: 전방+좌측 방향만 가능

### 13-3. ABORTED 3대 원인 상세 분석

#### 원인 1: SimpleProgressChecker의 회전 미인식

**문제**: Nav2의 `SimpleProgressChecker`는 XY 평면상의 **병진(translation) 이동 거리만** 측정하여 progress를 판단한다. 로봇이 제자리에서 yaw 회전만 수행하는 경우, XY 좌표가 변하지 않으므로 "progress 없음"으로 판정된다.

**발생 시나리오**: Multi-Goal 테스트의 Goal 3(원점 복귀)에서 로봇은 목표 지점에 0.109m 거리까지 도달하였다(xy_goal_tolerance=0.15m 이내). 이후 목표 yaw에 맞추기 위해 제자리 회전을 시작하였으나, `required_movement_radius=0.10`을 충족하지 못하여 `movement_time_allowance=15.0s` 이후 progress failure가 발생하였다.

**증거**:
- Goal 3 ABORTED 시간: **15.9초** ≈ `movement_time_allowance(15.0)` + 초기 직선 이동 시간(~0.9s)
- 로봇 최종 위치: 목표로부터 0.109m (xy_tolerance 이내지만 yaw 미달)
- SimpleProgressChecker 코드상 `euclidean_distance(current, prev)` ≥ `required_movement_radius` 비교만 수행

**메커니즘**: 제자리 회전 시작 → 15초간 XY 이동 <0.10m → progress failure → BT recovery action 발동 → recovery 실패 → ABORTED

#### 원인 2: 1.2m Inflation Corridor에 의한 DWB 정체

**문제**: 로봇의 현재 위치에서 전방 1.2m 구간이 전부 inflation zone(cost 60~92)으로 채워져 있다. DWB Local Planner가 trajectory를 sampling할 때, 모든 전방 경로가 높은 cost를 가지므로 최적 trajectory의 속도가 매우 낮아지거나, trajectory 자체가 선택되지 않는다.

**발생 시나리오**: 2m 전방 목표로 이동 시, 로봇은 초기 0.9m까지는 inflated corridor를 통과하였으나, cost가 가장 높은 구간(0.4~0.6m: cost 92)에 진입하면서 속도가 급격히 감소하였다. 이후 15초간 progress 기준(0.10m)을 충족하지 못하여 ABORTED가 발생하였다.

**원인 분석**:
- `cost_scaling_factor=3.0`: inflation 감쇄가 느려서, 벽에서 0.35m(inflation_radius) 이내의 모든 셀이 높은 cost를 유지
- DWB의 `BaseObstacle` critic이 cell cost를 직접 trajectory 비용에 반영 → cost 69~92 구간에서 trajectory 비용 급증
- 로봇 반경(inscribed=0.12m)은 통과 가능하지만, DWB가 cost를 기피하여 실질적으로 정체

**costmap 증거**:
```
전방 0.0m: cost=69 → 0.4m: cost=92(최대) → 0.8m: cost=77 → 1.2m: cost=60 → 1.3m: cost=0(free)
```

#### 원인 3: failure_tolerance 과소 설정 (0.3초)

**문제**: `controller_server`의 `failure_tolerance=0.3`은 DWB가 **0.3초(controller_frequency=10Hz에서 3 cycle)** 동안 유효한 trajectory를 찾지 못하면 즉시 BT에 failure를 보고한다는 의미이다. 좁은 inflated 공간에서 DWB가 일시적으로 모든 trajectory candidate를 reject하는 상황은 빈번하게 발생하며, 이때 3 cycle만에 failure가 보고되면 불필요하게 recovery action이 발동된다.

**발생 시나리오**: inflation corridor 내에서 DWB가 trajectory sampling 시 모든 candidate의 비용이 임계값을 초과하는 순간이 발생한다. 정상적인 상황에서는 다음 cycle에서 약간 다른 위치/속도 상태에서 유효 trajectory를 찾을 수 있으나, `failure_tolerance=0.3`이면 3 cycle 내에 해결되지 않으면 바로 failure가 된다.

**연쇄 효과**: DWB 3 cycle failure → BT failure 보고 → recovery action (backup/spin) 시도 → inflated zone 내에서 recovery도 실패 → ABORTED

**수정 근거**: `failure_tolerance=3.0`으로 변경하면 30 cycle(3초) 동안 DWB가 trajectory를 탐색할 수 있어, 일시적 trajectory failure에 의한 과도한 recovery 발동을 방지한다.

### 13-4. 파라미터 수정 상세 (7개)

수정 대상 파일: `src/jupiter_nav_VO/config/nav2_params_fused.yaml`

#### 수정 1: failure_tolerance (controller_server)

| 항목 | 수정 전 | 수정 후 |
|------|---------|---------|
| 값 | **0.3** | **3.0** |
| 의미 | 0.3s (3 cycles @10Hz) | 3.0s (30 cycles @10Hz) |

**설명**: DWB가 유효 trajectory를 찾지 못하는 일시적 상황에서 즉시 failure를 보고하지 않고, 3초(30 cycle)까지 재시도를 허용한다. Inflation corridor 내에서 일시적 trajectory 탐색 실패 시 recovery를 발동하지 않아 ABORTED 위험을 감소시킨다.

#### 수정 2: required_movement_radius (progress_checker)

| 항목 | 수정 전 | 수정 후 |
|------|---------|---------|
| 값 | **0.10** (10cm) | **0.03** (3cm) |
| 의미 | 10cm 이동 필요 | 3cm 이동 필요 |

**설명**: SimpleProgressChecker가 XY 병진 이동만 감지하므로, 제자리 yaw 회전 시에도 IMU/EKF noise에 의해 약 1~3cm의 XY drift가 발생한다. 이 drift가 progress로 인정되도록 임계값을 3cm으로 낮추었다. 이를 통해 제자리 회전이 "progress 없음"으로 판정되는 문제를 해결한다.

#### 수정 3: movement_time_allowance (progress_checker)

| 항목 | 수정 전 | 수정 후 |
|------|---------|---------|
| 값 | **15.0s** | **20.0s** |
| 의미 | 15초 내 이동 필요 | 20초 내 이동 필요 |

**설명**: Inflated corridor를 저속으로 통과하는 경우 + 목표 도달 후 최종 yaw 회전까지 완료하는 데 15초가 부족할 수 있다. 20초로 확장하여 inflation zone에서의 저속 통과 및 정밀 yaw 정렬에 충분한 시간을 확보한다.

#### 수정 4: xy_goal_tolerance (general_goal_checker)

| 항목 | 수정 전 | 수정 후 |
|------|---------|---------|
| 값 | **0.15** (15cm) | **0.20** (20cm) |
| 의미 | 15cm 이내 도달 | 20cm 이내 도달 |

**설명**: EKF 퓨전 위치의 noise 및 VSLAM adapter의 gating 특성상, 목표 15cm 이내 수렴이 불안정할 수 있다. 20cm으로 완화하여 목표 인근에서의 불필요한 미세 위치 조정 시도를 줄이고, 더 빠르게 goal achieved 판정을 받을 수 있도록 한다.

#### 수정 5: yaw_goal_tolerance (general_goal_checker)

| 항목 | 수정 전 | 수정 후 |
|------|---------|---------|
| 값 | **0.35** (~20°) | **0.50** (~29°) |
| 의미 | ±20° 이내 | ±29° 이내 |

**설명**: Goal 3(원점 복귀) ABORTED의 직접 원인은 최종 yaw 정렬이었다. XY 위치는 tolerance 이내에 도달했으나, yaw 회전 중 progress checker timeout이 발생하였다. yaw tolerance를 ±29°로 완화하여 목표 yaw에 더 빠르게 도달하고, 불필요한 제자리 회전 시간을 줄인다.

#### 수정 6: cost_scaling_factor (inflation_layer, local + global)

| 항목 | 수정 전 | 수정 후 |
|------|---------|---------|
| 값 | **3.0** | **5.0** |
| 적용 | local_costmap + global_costmap 양쪽 동일 |

**설명**: `cost_scaling_factor`는 inflation 감쇄의 지수 계수이다. 값이 클수록 벽에서의 cost가 빠르게 감소한다. 수식: `cost = 253 × e^(-cost_scaling_factor × (distance - inscribed_radius))`. 3.0→5.0으로 변경하면 동일 거리에서 cost가 낮아져, inflated corridor의 실효 cost가 감소한다. 이를 통해 DWB가 inflated zone을 더 적극적으로 통과할 수 있다.

**효과**: 로봇 위치 cost 69→44 (-36%), 전방 0.6m cost 99→89 (-10%)

#### 수정 7: inflation_radius (inflation_layer, local + global)

| 항목 | 수정 전 | 수정 후 |
|------|---------|---------|
| 값 | **0.35m** | **0.30m** |
| 적용 | local_costmap + global_costmap 양쪽 동일 |

**설명**: 로봇의 inscribed radius는 약 0.12m이다. 기존 inflation_radius 0.35m은 벽에서 0.35m 이내의 모든 셀에 inflation cost를 부여한다. 0.30m으로 축소하면 inscribed 0.12m + 안전여유 0.18m으로, 여전히 충분한 안전거리를 확보하면서 inflation 영역의 폭을 줄인다. 이로 인해 기존에 inflated였던 boundary 셀들이 free(cost=0)로 전환된다.

**효과**: free cells 2012→2279 (+267셀, +13%)

### 13-5. 적용 방법

수정사항을 Nav2 재시작 없이 동적으로 적용하였다.

#### Step 1: YAML 파일 수정 및 빌드
`nav2_params_fused.yaml` 파일에서 위 7개 파라미터를 수정하고, colcon build (symlink install)로 빌드하였다. 이는 다음 Nav2 기동 시 반영될 영구 변경이다.

#### Step 2: Controller 파라미터 동적 적용
`ros2 param set` 명령으로 controller_server의 5개 파라미터를 런타임에 동적 적용하였다.
- `failure_tolerance`: 0.3→3.0 ✅
- `progress_checker.required_movement_radius`: 0.10→0.03 ✅
- `progress_checker.movement_time_allowance`: 15.0→20.0 ✅
- `general_goal_checker.xy_goal_tolerance`: 0.15→0.20 ✅
- `general_goal_checker.yaw_goal_tolerance`: 0.35→0.50 ✅

#### Step 3: Costmap Inflation 동적 적용 + Lifecycle Cycle
costmap의 inflation 파라미터는 `ros2 param set`만으로는 inflation layer의 내부 캐시가 갱신되지 않는다. 파라미터 설정 후 **lifecycle transition**(deactivate→cleanup→configure→activate)을 통해 costmap을 완전히 재초기화하여야 한다.
- local_costmap: `cost_scaling_factor` 3.0→5.0, `inflation_radius` 0.35→0.30 설정 후 lifecycle cycle ✅
- global_costmap: 동일 변경 후 lifecycle cycle ✅

#### Step 4: Costmap Clear
`ClearEntireCostmap` service로 양쪽 costmap의 기존 observation 데이터를 초기화하였다.

#### Step 5: 파라미터 검증
`ros2 param get`으로 9개 파라미터 모두 목표값 반영 확인:
```
failure_tolerance=3.0, required_movement_radius=0.03, movement_time_allowance=20.0
xy_goal_tolerance=0.2, yaw_goal_tolerance=0.5
global inflation: cost_scaling_factor=5.0, inflation_radius=0.3
local inflation: cost_scaling_factor=5.0, inflation_radius=0.3
```

### 13-6. 적용 결과 비교

Costmap 공간 진단 스크립트를 동일하게 재실행하여 변경 전후를 비교하였다.

#### Costmap 통계 비교

| 항목 | 변경 전 | 변경 후 | 변화 |
|------|---------|---------|------|
| 로봇 cell cost | 69 | **44** | **-36%** |
| Inflated cells | 1836 | **1816** | -20 cells |
| **Free cells** | 2012 | **2279** | **+267 cells (+13%)** |
| Unknown cells | 4272 | 4025 | -247 |

#### 전방 경로 cost 비교

| 거리 | 변경 전 | 변경 후 | 변화 |
|------|---------|---------|------|
| 0.0m | 69 | **44** | -36% (로봇 위치) |
| 0.2m | 79 | **77** | -3% |
| 0.4m | 92 | **89** | -3% |
| 0.6m | 99 | **89** | **-10%** |
| 0.8m | 84 | **93** | +11% (맵 변동) |
| 1.0m | 99 | **99** | 동일 |
| 1.2m | 65 | **53** | **-18%** |
| 1.4m | 0 | **0** | free 유지 |

#### Progress Checker 비교

| 항목 | 변경 전 | 변경 후 | 효과 |
|------|---------|---------|------|
| 이동 임계 | 0.10m | **0.03m** | 제자리 회전의 EKF drift(~1-3cm)로 progress 통과 가능 |
| 시간 허용 | 15.0s | **20.0s** | inflated corridor 저속 통과에 +5s 여유 |
| failure 허용 | 0.3s | **3.0s** | DWB 일시 실패 시 3초간 재시도 (기존 0.3s=3 cycle) |
| xy 도달 | 0.15m | **0.20m** | 목표 인근 불필요한 위치 조정 감소 |
| yaw 도달 | 0.35 rad | **0.50 rad** | ~20°→~29° 허용 (회전 ABORTED 방지) |

#### 종합 평가
- **로봇 위치 cost 36% 감소**: DWB trajectory 비용이 크게 낮아져 유효 trajectory 확보 용이
- **Free cells 13% 증가**: inflation boundary 축소로 이동 가능 공간 확장
- **Progress/Goal 판정 완화**: 제자리 회전이 progress failure를 유발하지 않도록 보정
- **실 주행 검증 필요**: 배터리 충전 후 동일 구간(2m 전진+복귀) 테스트로 ABORTED 해소 여부 확인 예정

---

## 14. cuVSLAM 근본 원인 조사

> 2026-03-12 세션. cuVSLAM 텔레포트의 근본 원인을 Docker 로그, vo_pose_covariance, 실제 회전 테스트로 분석.

### 14-1. Docker 로그 시간대 분석

컨테이너 전체 로그에서 cuVSLAM 관련 경고/에러를 추출하여 분석하였다.

#### VSLAM 세션 히스토리 (컨테이너 기동 이후)

| 세션 | ROS timestamp | 이벤트 |
|------|------|--------|
| 세션 1 (첫 기동) | 1773276843 | CreateTracker 6.03s (콜드 스타트), jitter 11건 |
| 세션 2 (debug_mode 실험) | 1773281109 | RealSense USB "failed to set power state" → 25s 후 복구 → CreateTracker 0.09s → 2s 후 destroy (debug_mode I/O 과부하) |
| 세션 3 (사용자 수동 재시작) | 1773282129 | RealSense 정상 연결, CreateTracker 시간 미표시 (로그 누락) → jitter 55건 (기동 7초간 burst) |

#### Frame Jitter 경고 통계 (전체 컨테이너 수명)

총 **68건**의 "Delta between current and previous frame" 경고:

| 범위 | 건수 | 비율 |
|------|------|------|
| 34-50ms | 10 | 15% |
| 50-100ms | 37 | 54% |
| 100-200ms | 13 | 19% |
| 200-400ms | 8 | 12% |
| **최대** | **377ms** | — |
| **평균** | **102ms** | — |

#### 시간대별 분포
- **기동 burst**: 세션 3 시작 후 7초간 **55건** 집중 (78.5%)
- **정상 운영**: 3분간 **3건** (4.4%)
- **회전 테스트 중**: 20초간 **2건** (2.9%) — 회전 자체는 jitter를 크게 증가시키지 않음

#### 핵심 발견
1. **jitter는 주로 기동 초기에 집중**: USB 파이프라인 안정화 전에 프레임 타이밍 불규칙
2. **정상 운영 중 jitter는 희소**: ~1건/분 수준으로 추적에 큰 영향 없음
3. **회전 중에도 jitter 추가 증가 미미**: 텔레포트 원인은 jitter가 아닌 다른 요인

### 14-2. vo_pose_covariance 분석

cuVSLAM이 내부적으로 보고하는 추적 불확실성(covariance)을 분석하였다.

#### 정지 상태 baseline (10초)

| 항목 | cov_xx | cov_yy | cov_zz |
|------|--------|--------|--------|
| min | 0.000137 | 0.000131 | 0.000187 |
| max | 0.000137 | 0.000131 | 0.000187 |
| **mean** | **0.000137** | **0.000131** | **0.000187** |
| std | 0.000000 | 0.000000 | 0.000000 |

- 정지 상태에서 covariance는 **완전 상수** (값 변동 0)
- 절대값 ~1.3e-4: cuVSLAM이 매우 높은 자신감으로 추적 중

#### 회전 테스트 중 covariance 변화 (40초)

| Phase | 구간 | cov_xx mean | cov_xx max | 배율 (vs baseline) |
|-------|------|-------------|------------|--------------------|
| 1 (정지) | 5s | 0.046 | 0.058 | 1x (reference) |
| 2 (고속 회전) | 20s | **0.461** | **26.120** | **10x mean, 568x peak** |
| 3 (정지 복귀) | 15s | 0.080 | 0.228 | 1.7x mean |

- **Phase 2**: cov_xx가 최대 **26.12**까지 폭등 → cuVSLAM 스스로 추적 품질이 매우 나쁘다고 보고
- **Phase 3**: 회전 중단 후 covariance가 빠르게 감소하나 baseline 수준으로는 미복귀 (0.080 vs 0.046)
- **Spike 빈도**: >3x mean: 147건, >10x mean: 34건 (Phase 2 집중)

#### 핵심 발견
- vo_pose_covariance는 cuVSLAM의 **실시간 추적 신뢰도 지표**로 활용 가능
- 텔레포트 직전 covariance가 급등하는 패턴: covariance spike → feature matching 실패 → position 점프
- **향후 활용**: adapter에서 covariance 값을 anomaly detection 보조 지표로 사용 가능 (현재는 inter-frame delta만 사용)

### 14-3. cuVSLAM 텔레포트 근본 원인 종합

#### 텔레포트 발생 조건 (관찰 기반)

| 요인 | 영향 | 증거 |
|------|------|------|
| **고속 회전** | 🔴 결정적 | 정지 시 점프 0건, ±2.5~4.0 rad/s 회전 시 60건 (>0.1m) |
| **Feature 부족** | 🟡 기여 | 적외선 stereo에서 회전 시 모션 블러로 feature 소실 |
| **Frame jitter** | 🟢 미미 | 회전 중 jitter 2건 추가뿐 (jitter ≠ 텔레포트 원인) |
| **IMU 퓨전 한계** | 🟡 기여 | IMU가 빠른 회전 보상하나 시각 정보 부재 시 한계 |

#### 메커니즘 모델

```
1. 고속 회전 시작 (>2.5 rad/s)
2. 카메라 프레임 간 큰 시각 변화 → stereo feature matching 실패
3. cuVSLAM 내부 covariance 급등 (cov_xx: 0.05 → 26.12)
4. IMU만으로 position 추정 시도 → drift 누적
5. 회전 감속 시 시각 feature 재감지 → 새 position으로 "점프"
6. Adapter anomaly detection이 점프 감지 → gating
7. 안정화 후 auto-recovery → baseline 리셋 → 정상 복귀
```

#### enable_debug_mode 교훈

전 세션에서 `enable_debug_mode=True`를 시도하여 vo_state를 활성화하려 했으나:
- **치명적 부작용**: 매 프레임 TGA 이미지 저장 → 5.9GB/5분, 26,687파일
- **결과**: disk I/O 포화 → cuVSLAM 완전 중단 → RealSense USB 불안정 → 수동 컨테이너 재진입 필요
- **결론**: Jetson Orin NX에서 `enable_debug_mode`는 **절대 사용 금지**
- **대안**: vo_pose_covariance가 tracking quality의 실용적 대체 지표

---

## 15. Auto-Recovery 실제 트리거 검증

> 2026-03-12. 고속 회전으로 VSLAM 텔레포트를 유발하여 auto-recovery 메커니즘의 실제 동작을 검증.

### 15-1. 테스트 설계

#### 모니터링 토픽 (5개 동시)
| 토픽 | 용도 |
|------|------|
| `/visual_slam/tracking/odometry` | raw VSLAM — 텔레포트 감지 |
| `/visual_slam/tracking/odometry_adapted` | adapted — gating/recovery 확인 |
| `/visual_slam/tracking/vo_pose_covariance` | cuVSLAM 추적 신뢰도 |
| `/odom` | EKF 상태 |
| `/rosout` | adapter anomaly/recovery 로그 |

#### 회전 패턴 (Phase 2, 20초)

| 단계 | 각속도 | 시간 |
|------|--------|------|
| 1 | +2.5 rad/s | 2.0s |
| 2 | -3.5 rad/s | 2.0s |
| 3 | +3.5 rad/s | 2.0s |
| 4 | -2.5 rad/s | 2.0s |
| 5 | +4.0 rad/s | 1.5s |
| 6 | -4.0 rad/s | 1.5s |
| 7 | +3.0 rad/s | 2.0s |
| 8 | -3.0 rad/s | 2.0s |
| 9 | +3.5 rad/s | 1.5s |
| 10 | -3.5 rad/s | 1.5s |

### 15-2. 테스트 결과

#### 메시지 통계 (40초 전체)

| 토픽 | 메시지 수 | 주파수 |
|------|-----------|--------|
| raw VSLAM | 2,299 | 57.5 Hz |
| adapted VSLAM | 1,303 | 32.6 Hz |
| EKF odom | 1,148 | 28.7 Hz |
| vo_pose_cov | 2,292 | 57.3 Hz |
| **Pass-through** | **56.7%** | — |

#### Inter-frame Delta (텔레포트 분석)

| 임계값 | Phase 2 (회전) | Phase 3 (정지) | 전체 |
|--------|---------------|---------------|------|
| >0.1m | **60건** | 0건 | 60건 |
| >0.2m | 35건 | 0건 | 35건 |
| >0.3m | **20건** | 0건 | 20건 |
| >0.5m | **6건** | 0건 | 6건 |
| **최대** | **0.698m** | 0.011m | 0.698m |
| 평균 | 25.93mm | 0.22mm | 12.43mm |

- Phase 2에서 **20건의 텔레포트**(>0.3m) 발생, 최대 0.698m
- Phase 3에서 **0건** — 회전 정지 후 즉시 안정화

#### Raw Position 범위

```
x: [-4.1049, 4.2336]  →  x spread = 8.34m
y: [-6.5461, -5.0743]  →  y spread = 1.47m
Total spread: 8.47m  (실제 로봇은 제자리 회전)
```

제자리 회전인데도 cuVSLAM이 **8.47m 범위**의 허위 위치를 보고 → 텔레포트의 심각성 확인.

### 15-3. Auto-Recovery 동작 로그

| 시점 | 이벤트 | Phase |
|------|--------|-------|
| Phase 2 초반 | `VSLAM tracking LOST (anomaly detected). Gating.` | 2 |
| Phase 2 후반 | `VSLAM auto-recovery #3: stable tracking at new position (584 anomalies, 120 stable interframes). Resetting baseline.` | 2 |
| Phase 3 초반 | `VSLAM auto-recovery #4: stable tracking at new position (300 anomalies, 274 stable interframes). Resetting baseline.` | 3 |
| Phase 3 | `VSLAM recovery delay passed (1.0s). Publishing resumed.` | 3 |

#### Auto-Recovery 동작 시퀀스 상세

1. **Phase 2 시작**: 고속 회전 → anomaly 감지 → gating 시작
2. **Auto-Recovery #3** (Phase 2 중): 584 연속 anomaly (≈10초 @58Hz) 축적 후, 120 stable interframe (≈2초) 확인되어 baseline 리셋. 그러나 회전이 계속되므로 다시 anomaly 발생.
3. **Auto-Recovery #4** (Phase 3 초반): 회전 정지 후 300 anomaly 축적 + 274 stable interframe으로 최종 baseline 리셋.
4. **Publishing 재개**: recovery delay 1.0초 경과 후 정상 발행 재개.

#### 복구 후 상태 (회전 정지 5초 후)

```
raw:     43.4 Hz ✅
adapted: 60.6 Hz ✅ (139.6% — 큐 flush로 일시 burst)
tracking_ok=True, anomaly=0, frozen=0
```

### 15-4. Auto-Recovery 메커니즘 평가

| 평가 항목 | 결과 | 판정 |
|-----------|------|------|
| 텔레포트 감지 | 즉시 감지, gating 개시 | ✅ |
| anomaly 지속 중 EKF 보호 | adapted=0 → EKF에 오염 데이터 미전달 | ✅ |
| 안정화 판정 | 120 stable interframes (2초) 충족 후 리셋 | ✅ |
| Baseline 리셋 | prev_odom을 새 position으로 갱신 | ✅ |
| Recovery delay | 1.0초 추가 대기 후 publishing 재개 | ✅ |
| 복구 후 상태 | tracking_ok=True, anomaly=0, 정상 발행 | ✅ |
| **연속 회전 중 동작** | Recovery #3 후 재차 anomaly → Recovery #4로 최종 복구 | ✅ |

#### 개선 사항 (미래)
- `anomaly_reset_count=300` (5초)이 적절한지 장기 테스트 필요
- 회전 중 auto-recovery 발생(#3) 후 재차 anomaly가 나타나는 패턴 → 회전 종료 판정 로직 추가 고려
- vo_pose_covariance spike를 보조 anomaly indicator로 활용 가능 (현재 미사용)

---

## 16. 현재 상태 요약

### 완료 항목 ✅
- [x] 스택 재시작 후 전체 센서 검증 (9/9 정상)
- [x] prev_odom 조건부 업데이트 확인 (grep Line 349)
- [x] Nav2 1.5m 단일 목표 테스트 (travel=1.318m, VSLAM 안정)
- [x] Nav2 3-waypoint 멀티 목표 테스트 (2/3 SUCCEEDED, 1 TIMEOUT)
- [x] Permanent lock-out 버그 발견 및 원인 분석
- [x] Auto-recovery 메커니즘 설계 (3 파라미터, 3 상태변수)
- [x] Auto-recovery 코드 구현 (7곳 수정)
- [x] Adapter 재시작 및 배포 (pip editable + nohup)
- [x] Auto-recovery 정지 검증 (0% → 5% pass 복구)
- [x] Auto-recovery 주행 검증 (71% pass, stable_if=2534 정상 누적)
- [x] Nav2 ABORTED 원인 분석 (3가지 원인 특정)
- [x] Nav2 파라미터 수정 7개 (동적 적용 + lifecycle cycle)
- [x] **cuVSLAM 근본 원인 조사** (Docker 로그 68건 jitter 분석, vo_pose_covariance 568x spike 확인)
- [x] **Auto-recovery 실제 트리거 검증** (텔레포트 0.698m 유발, recovery #3/#4 정상 동작, 복구 후 정상 상태)

### 미완료 항목
- [ ] Nav2 수정사항 주행 검증 (배터리 충전 후)
- [ ] VSLAM-wheel cross-sensor 검증 (progressive drift 대응)
- [ ] Frozen detection 로그 빈도 감소 (과다 verbose 문제)
- [ ] max_pos_delta 0.5m → 0.3m 임계값 조정 검토
- [ ] vo_pose_covariance 기반 보조 anomaly indicator 구현 검토

---

## 18. Nav2 2m 전진+복귀 테스트 — ABORTED 해소 확인 ✅

### 18.1 테스트 조건
- **시간**: 2026-03-12 (전 세션에서 계속)
- **사전 준비**: global/local costmap ClearEntireCostmap 실행 (이전 teleport 테스트 잔여 costmap 제거)
- **로봇 위치**: (-0.948, 0.058), yaw=5.5° (teleport 테스트 이후 위치)
- **Global costmap**: 141×160 @ 0.05m, lethal=0 (clear 효과), 로봇 셀 cost=86 (inflation zone)
- **전방 경로 cost**: 0.6~1.4m 구간 free(0), 2.0m 지점 cost=44 (통과 가능)

### 18.2 테스트 결과

| 지표 | 수정 전 (Section 11) | 수정 후 (본 테스트) | 변화 |
|------|----------------------|---------------------|------|
| Goal1 (2m 전진) | **ABORTED** (17.7s) | **SUCCEEDED** (9.7s) | ✅ 해소 |
| Goal2 (복귀) | **ABORTED** (15.9s) | **SUCCEEDED** (11.7s) | ✅ 해소 |
| Goal1 잔여 거리 | 1.131m | 0.786m | 개선 |
| Goal2 잔여 거리 | 0.109m | 0.637m | 증가 (경로 차이) |
| VSLAM 점프 >0.3m | 있음 | **0건** | 안정 |
| Pass-through | — | 94.1% | 정상 |
| EKF 총 이동 | — | 4.233m | — |

### 18.3 핵심 변경 파라미터 (효과 확인)

```yaml
# nav2_params_fused.yaml에서 변경된 7개 파라미터
progress_checker:
  required_movement_radius: 0.03    # 0.5 → 0.03 (VSLAM 미세 진동 고려)
  movement_time_allowance: 20.0     # 10.0 → 20.0 (VSLAM frozen 구간 대비)
controller_server:
  failure_tolerance: 3.0            # 0 → 3 (일시적 제어 실패 허용)
goal_checker:
  xy_goal_tolerance: 0.20           # 0.25 → 0.20
  yaw_goal_tolerance: 0.50          # 0.25 → 0.50
costmap (global+local):
  cost_scaling_factor: 5.0          # 3.0 → 5.0 (빠른 cost 감소)
  inflation_radius: 0.30            # 0.55 → 0.30 (inflation 영역 축소)
```

### 18.4 VSLAM 어댑터 상태
- anomaly: 2건, frozen: 11건 (정상 범위)
- 자동 복구(auto-recovery): 0건 (필요 없었음)
- cov_xx: mean=0.000047, max=0.000454 (정상)
- VSLAM raw inter-frame max delta: 0.131m (0.3m 이하, 안정)

### 18.5 분석
1. **ABORTED → SUCCEEDED 전환 핵심 요인**:
   - `required_movement_radius: 0.03` — VSLAM 미세 진동으로도 progress 인정
   - `movement_time_allowance: 20.0` — frozen 구간(~4.5s)도 견딜 수 있는 충분한 여유
   - `failure_tolerance: 3.0` — 일시적 controller 실패 시 즉시 abort하지 않음
2. **Goal2 잔여 거리 0.637m 증가**: 로봇이 다른 경로로 접근했거나, EKF drift로 인해 목표 도달 판정과 실제 위치 차이 발생. Nav2 내부적으로는 goal_checker tolerance 내 도달 판정.
3. **Costmap clear 필요성**: 첫 시도에서 Goal1이 2.9s 만에 ABORTED — costmap에 이전 teleport 잔여 장애물이 남아있었을 가능성. clear 후 정상 작동.

### 18.6 결론
> **Nav2 파라미터 수정 7개 항목 모두 효과 확인됨. ABORTED 이슈 해소.**
> 
> 특히 `required_movement_radius`, `movement_time_allowance`, `failure_tolerance` 3가지가 VSLAM 기반 navigation의 핵심 안정성 파라미터임.

---

## 19. 다음 단계

### P1 (다음 세션)
1. ~~**Nav2 수정사항 주행 검증**: 동일 구간 2m 전진+복귀 테스트로 ABORTED 해소 확인~~ ✅ **완료 (Section 18)**
2. **장기 주행 테스트**: 10분 이상 연속 주행 (복합 환경에서 auto-recovery + Nav2 안정성)

### P2 (이번 주)
3. **vo_pose_covariance 활용**: adapter에서 cov spike를 anomaly 보조 지표로 통합 (현재 inter-frame delta만 사용)
4. **Frozen 로그 정리**: 반복 로그 억제 (cooldown 추가)
5. **max_pos_delta 0.5m → 0.3m 검토**: 현재 0.5m에서 0.698m 점프 차단 확인됨. 0.3m으로 하면 더 빠른 감지 가능

### P3 (다음 주)
6. **VSLAM-wheel cross-sensor**: progressive drift 대응 (per-frame delta 비교)
7. **성능 프로파일링**: adapter CPU 사용량 확인 (고주파 콜백)
8. **회전 중 recovery 억제**: 회전 종료 판정 후에만 auto-recovery 실행하도록 개선

---

## 20. 좌측 편향 현상 분석 — 순수 직진 테스트

### 20.1 배경
Nav2 2m 전진+복귀 테스트(Section 18) 실행 시 관찰된 현상:
- **Goal1**: 일직선으로 전진하나, 도착 후 목표 방향을 보지 못하고 **왼쪽으로 회전**
- **Goal2**: 뒤를 바라본 후 출발하는데, **왼쪽으로 치우친** 전진을 하다가 벽에 부딪힐 것 같을 때 경로를 재설정하여 우회전

→ 좌측 편향의 근본 원인을 분석하기 위해, Nav2 없이 순수 직진 명령(cmd_vel: vx=0.15, wz=0)으로 5초간 주행 테스트 실시

### 20.2 테스트 방법
- **방법**: `/cmd_vel`에 `linear.x=0.15, angular.z=0.0`을 5초간 직접 publish (Nav2 미사용)
- **목적**: 물리적 로봇 자체의 좌측 편향 여부 확인
- **모니터링**: EKF, Wheel odom, VSLAM, RF2O 4개 센서 동시 기록
- **주의**: 초기 시도에서 조이스틱 node가 cmd_vel에 간섭하여 실패 → 조이스틱 중지 후 재시도

### 20.3 순수 직진 테스트 결과

| 센서 | Forward (m) | Lateral (m) | 방향 | Yaw 변화 (°) | 방향 |
|------|-------------|-------------|------|---------------|------|
| **EKF (/odom)** | 0.6973 | -0.0007 | RIGHT | +0.118 | CCW/LEFT |
| **Wheel (/odom_raw)** | 0.6997 | -0.0014 | RIGHT | +0.119 | CCW/LEFT |
| **RF2O (/odom_rf2o)** | 0.6967 | **+0.0209** | **LEFT** | **+1.696** | **CCW/LEFT** |

> **VSLAM**: Docker 컨테이너 중지 상태 → 0건 수신

### 20.4 Wheel Odom Twist 분석
```
vx: mean=0.1393 m/s
wz: mean=0.011 mrad/s (0.0006°/s) → 거의 0 (직진 정상)
wz: std=58.526 mrad/s
```

### 20.5 핵심 발견
1. **물리적 로봇은 완벽히 직진**: Wheel lateral < 2mm, Yaw drift < 0.12°
2. **RF2O만 좌측 편향**: lateral +21mm LEFT, yaw +1.7° LEFT (2.4°/m drift rate)
3. **EKF는 정상**: RF2O의 편향이 EKF에 미미하게만 반영 (lateral < 1mm)
4. **VSLAM Docker 중지**: Isaac ROS 컨테이너가 꺼져있어 odom1 소스 없음

---

## 21. 좌측 편향 현상 분석 — Nav2 주행 진단

### 21.1 테스트 조건
- VSLAM Docker **중지 상태** (0건 메시지) — 가장 정확한 위치 보정 소스 부재
- EKF는 odom0(wheel) + odom2(RF2O) + imu0(IMU)만으로 융합
- Nav2 Goal: 현재 위치에서 heading 방향 2m 전진

### 21.2 Nav2 주행 결과
- **Result**: SUCCEEDED (9.4s)
- Start: (-0.803, -0.087, yaw=8.9°), Goal: (1.173, 0.223)

### 21.3 cmd_vel 분석 (186 forward commands)

```
vx: mean=0.3211 m/s
wz: mean=32.82 mrad/s (1.881°/s) → 전체적으로 LEFT 편향
wz: min=-263.2, max=+473.7 mrad/s
Left(+) cmds: 48, Right(-) cmds: 44
```

**시간대별 wz 추이:**
```
t=0-3s:  wz ≈ 0        (직진)
t=3-4s:  wz = -10.5    (미세 RIGHT)
t=4-6s:  wz = -131~-158 (강한 RIGHT 보정)
t=6-7s:  wz = +5.3     (직진)
t=7-10s: wz = +176~342 (강한 LEFT 회전) ← Goal heading 맞추기
```

### 21.4 EKF Yaw 추이
```
Forward motion yaw: 9.65° → 13.63° (drift: +3.98° LEFT)

Lateral deviation during forward:
  At 0.5m: lateral=-0.005m (거의 직진)
  At 1.0m: lateral=-0.065m (RIGHT 편향 ← 물리적 직진이지만 EKF yaw 감소)
  At 1.5m: lateral=-0.139m (RIGHT 편향 누적)
```

### 21.5 센서별 비교

| 센서 | Yaw Drift | Lateral | 방향 |
|------|-----------|---------|------|
| **EKF** | +3.98° | -0.160m | RIGHT |
| **RF2O** | **+6.03°** | -0.028m | RIGHT |
| **Wheel** | +4.37° | -0.134m | RIGHT |
| **VSLAM** | N/A (0건) | N/A | — |

### 21.6 좌측 편향 메커니즘 분석

#### 1단계: EKF yaw가 떨어짐 (8.9° → ~1.4°)
- RF2O differential x,y가 yaw bias를 포함한 상대 변위를 EKF에 공급
- VSLAM이 없어 보정 불가 → yaw가 실제보다 낮게 추정됨

#### 2단계: DWB controller가 RIGHT 보정 (t=4-6s)
- EKF yaw < goal yaw → controller가 오른쪽으로 수정
- 로봇이 오른쪽으로 회전 → 전체 경로가 오른쪽으로 치우침

#### 3단계: 목표 도달 후 LEFT 과보상 (t=7-10s)
- Goal heading(8.9°) 맞추려면 현재 yaw(1.4°)에서 +7.5° LEFT 회전 필요
- 이것이 "도착 후 왼쪽 회전" 현상

#### Goal2 왼쪽 치우침:
- 복귀 시 반대 방향을 바라보고 출발
- RF2O left yaw drift로 EKF heading이 실제보다 왼쪽으로 편향
- DWB가 이를 기반으로 경로 추종 → 왼쪽으로 치우침
- 벽 감지 → 경로 재계획 → 우회전으로 보정

---

## 22. 좌측 편향 원인 결론 및 다음 단계

### 22.1 근본 원인 (2가지)

#### 원인 1: VSLAM Docker 중지 (치명적)
- Isaac ROS Docker 컨테이너가 꺼져있어 **odom1(VSLAM x,y,vx,vy) = 0건**
- EKF에 가장 정확한 위치 보정 소스가 완전히 빠진 상태
- RF2O drift를 교정할 수 없음

#### 원인 2: RF2O Left Yaw Drift (구조적)
- RF2O 스캔 매칭이 직진 시에도 **+2.4°/m LEFT yaw drift** 발생
- `odom2_differential: true`로 frame-to-frame 변위를 EKF에 공급
- VSLAM이 있으면 x,y 보정으로 상쇄되지만, 없으면 누적

### 22.2 VSLAM 중지 확인 이력
```bash
docker ps --format '{{.Names}} {{.Status}}' | grep isaac
# → (empty) — No isaac container running

timeout 3 ros2 topic echo /visual_slam/tracking/odometry --once
# → Terminated (no messages)
```

### 22.3 영향 범위
- VSLAM 가동 시: RF2O drift는 VSLAM x,y 보정으로 상쇄 → 문제 미미
- VSLAM 중지 시: RF2O drift가 EKF yaw/position에 누적 → 좌측 편향 발생
- 순수 직진 시(Nav2 미사용): wheel odom이 지배적 → 편향 없음 (lateral < 2mm)

### 22.4 다음 단계

#### 즉시 (P0)
1. **VSLAM Docker 컨테이너 재시작** → odom1 소스 복구
2. **VSLAM 활성 상태에서 동일 테스트 재실행** → 좌측 편향 해소 확인

#### 단기 (P1)
3. **RF2O left yaw drift 근본 원인 조사**:
   - LiDAR 물리적 정렬 (base_footprint 대비 회전 offset?)
   - rf2o_laser_odometry 파라미터 (freq, 매칭 알고리즘)
   - scan 토픽의 range 비대칭 여부
4. **VSLAM 중지 시 fallback 전략**: RF2O yaw를 사용하지 않도록 `odom2_config`에서 yaw 관련 필드 검토 (현재 이미 false이지만, differential x,y에 yaw bias가 암묵적으로 포함)

#### 중기 (P2)
5. **장기 주행 테스트**: VSLAM 활성 + 10분 이상 연속 주행
6. **vo_pose_covariance 활용**: adapter anomaly 보조 지표
7. **Frozen 로그 정리**: 반복 억제

---

## 23. VSLAM ON 재테스트 — 좌측 편향 비교

### 23.1 테스트 조건
- **VSLAM Docker**: `isaac_ros_dev-aarch64-container` Up 28분 (재시작 후)
- **VSLAM raw**: 1232 msgs, **adapted**: 1013 msgs (pass-through 82.2%)
- **Costmap**: 테스트 전 ClearEntireCostmap 실행
- **Nav2 Goal**: 2m 전진 + 복귀 (Goal1 + Goal2)

### 23.2 결과 요약

| 지표 | VSLAM OFF (Section 21) | VSLAM ON (본 테스트) |
|------|------------------------|------------------------|
| Goal1 | SUCCEEDED (9.4s) | SUCCEEDED (**6.0s**) |
| Goal2 | (미포함) | SUCCEEDED (11.3s) |
| cmd_vel wz mean | +32.82 mrad/s (LEFT) | **-8.81 mrad/s** (~0) |
| EKF yaw drift (전체) | +3.98° | **+85.66°** |
| RF2O yaw drift | +6.03° | **+49.51°** |
| EKF net displacement | — | 0.245m |
| VSLAM msgs | 0 | 1232 |

### 23.3 cmd_vel 분석 (Goal1 전진 구간)

```
전방 명령 wz 추이 (1s 창):
  t=0-1s: wz=-10.5 mrad/s  (R, 직진)
  t=1-2s: wz=+39.5 mrad/s  (L, 미세 보정)
  t=2-3s: wz=+157.2 mrad/s (L, 보정 시작)
  t=3-4s: wz=+484.2 mrad/s (L, 강한 좌회전!)  ← 핵심 문제
  t=4-5s: wz=+306.5 mrad/s (L)
  t=5-6s: wz=+149.8 mrad/s (L)
```

### 23.4 EKF Yaw 시계열 (전체 주행)

```
  t=0-2s:  7.24°  (정상 heading)
  t=2-4s:  4.50°  (yaw 감소 → 실제보다 낮게 추정)
  t=4-6s:  20.60° (급격한 LEFT 과보상)
  t=6-8s:  42.35° (Goal1 완료 후 heading 정렬)
  t=8-10s: 52.08° (Goal2 시작, 180° 회전 중)
  t=10-14s: ±130° (복귀 주행 중 큰 회전)
  t=20-24s: ~47°  (최종 heading)
```

### 23.5 센서별 비교

| 센서 | Yaw Drift | Forward | Lateral | 방향 |
|------|-----------|---------|---------|------|
| RF2O | +49.51° | 0.183m | -0.246m | RIGHT |
| VSLAM raw | +40.64° | 0.151m | +0.197m | LEFT |
| VSLAM adapted | +53.53° | 0.437m | -0.027m | RIGHT |
| Wheel | +39.78° | 0.185m | -0.042m | RIGHT |

### 23.6 분석

#### 예상과 다른 결과 — VSLAM ON으로도 좌측 편향 미해소

**근본 원인**: VSLAM은 `odom1_config`에서 **yaw 비활성** 상태:
```yaml
odom1_config: [true,  true,  false,    # x, y (O), z (X)
               false, false, false,    # roll, pitch, yaw ← 모두 false!
               true,  true,  false,    # vx, vy (O)
               false, false, false,    # vyaw ← false!
               ...]
```

따라서:
1. **EKF yaw**는 여전히 **wheel(vyaw) + IMU(vyaw)만으로 추정** → VSLAM이 yaw 보정을 하지 않음
2. **RF2O differential x,y**에 암묵적 yaw bias 포함 → EKF position이 실제와 다르게 편향
3. **VSLAM differential x,y**도 EKF에 투입되지만, yaw 불일치 상태에서 position update가 heading을 간접적으로 왕곡
4. **DWB controller**가 이 왓곡된 heading/position 기반으로 경로 추종 → 과도한 회전 명령

#### 왜 VSLAM yaw를 비활성했나? (Section 12 참고)
- 2026-02-27 진단: 빠른 회전 중 VSLAM differential yaw가 Wheel 대비 ±21° 오차 발생
- motion blur/feature loss로 VSLAM yaw가 불안정 → EKF에 누적되어 ~13° 영구 오프셋
- 보수적 판단으로 yaw/vyaw 제거, 회전은 IMU+Wheel에 위임

#### 현재 문제의 근본
> **yaw 소스가 wheel(vyaw) + IMU(vyaw)만으로는 불충분** — 주행 중 yaw drift가 position 편향을 유발하고, controller가 과도하게 보정

---

## 24. 종합 분석 및 다음 단계

### 24.1 좌측 편향 근본 원인 요약

| 계층 | 원인 | 심각도 |
|------|------|--------|
| **EKF yaw** | wheel+IMU만으로 yaw 추정 → drift 발생 | 핵심 |
| **RF2O** | differential x,y에 left yaw bias (+2.4°/m) 포함 | 보조 |
| **VSLAM** | yaw 비활성으로 EKF yaw 보정 불가 | 구조적 |
| **DWB** | 왓곡된 heading 기반으로 과도 보정 | 결과적 |

### 24.2 가능한 해결 방향

#### A. VSLAM yaw 제한적 재활성화 (vyaw만)
```yaml
# ekf_vslam_fusion.yaml
odom1_config: [true,  true,  false,
               false, false, false,    # yaw pose 여전히 false
               true,  true,  false,
               false, false, true,     # vyaw ← true로 변경!
               false, false, false]
```
- **장점**: VSLAM 각속도(vyaw)가 EKF yaw rate를 보정
- **위험**: 회전 중 VSLAM vyaw 오차 (±21°) 재발 가능성
- **완화책**: EKF `odom1_twist_rejection_threshold`를 낮게 설정 (3.0 → 2.0)

#### B. RF2O 비활성화 (VSLAM이 있을 때)
```yaml
# RF2O를 EKF에서 제거 — VSLAM x,y가 동일 역할을 더 정확히 수행
# odom2 제거 또는 dynamic reconfigure로 비활성화
```
- **장점**: RF2O left yaw drift 원천 제거
- **위험**: VSLAM 실패 시 fallback 소스 상실

#### C. RF2O yaw drift 근본 수정
- LiDAR 물리적 정렬 확인 (TF base_footprint → laser_link)
- rf2o 파라미터 튜닝
- 스캔 비대칭 확인

### 24.3 다음 단계

#### P0 (즉시)
1. **방향 A 테스트**: `odom1_config`에서 vyaw=true로 변경 후 동일 2m 전진+복귀 테스트
2. **방향 B 테스트**: RF2O(odom2) 비활성화 후 동일 테스트

#### P1 (단기)
3. **RF2O left yaw drift 근본 원인 조사**
4. **최적 조합 결정**: A와 B 중 더 나은 결과를 기본 설정으로 채택

#### P2 (중기)
5. **장기 주행 테스트**: 최적 설정으로 10분+ 연속 주행
6. **VSLAM 실패 시 fallback 전략 검증**
