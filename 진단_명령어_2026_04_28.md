# 명령어 로그 2026-04-28: Nav2 좁은 공간 + 정지 회전 + 센서 정렬 + Docker 학습

> 진단 및 수정에 사용한 명령어와 스크립트/설정 파일을 요약 없이 전부 기록한다.

---

## 0. 오늘 사용한 터미널 명령어 원문

### 0.1 Isaac ROS Docker 의존성 박제 — 검증 명령

```bash
ls /home/jetson/isaac_ws_ros/src/isaac_ros_common/scripts/
ls /home/jetson/isaac_ws_ros/src/isaac_ros_common/docker/
ls -la ~/.isaac_ros_common-config 2>&1
grep -E "^export ISAAC_ROS_WS|^ISAAC_ROS_WS" ~/.bashrc
```
목적: Isaac ROS Common 의 layered Dockerfile 패턴 + 사용자 config 위치 파악.
결과: `Dockerfile.{aarch64,x86_64,base,ros2_humble,realsense}` 존재. `~/.isaac_ros_common-config` 부재.

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
echo "== depth_image_proc =="
dpkg -l ros-humble-depth-image-proc 2>/dev/null | tail -3
echo
echo "== depth_image_proc composable plugins =="
grep -r "PointCloudXyzNode" /opt/ros/humble/share/depth_image_proc/ 2>/dev/null | head -5
'
```
목적: 학습 자료에서 인용할 PointCloudXyzNode plugin 존재 검증.
결과: `depth_image_proc::PointCloudXyzNode` composable 확인.

### 0.2 Dockerfile.jupiter_user 빌드 검증 — yarn GPG 만료 대응

```bash
cd ${ISAAC_ROS_WS}/src/isaac_ros_common && ./scripts/run_dev.sh
```
목적: 사용자 layer 추가 후 첫 chain 빌드.
결과 (1차): `apt-get update` 가 yarn GPG 만료(`NO_PUBKEY 62D54FD4003F6525`)로 exit 100 → `apt-get install` 차단.

```bash
grep -rnE "yarn|nodesource|node_18|nodejs" /home/jetson/isaac_ws_ros/src/isaac_ros_common/docker/ 2>&1 | head -20
ls /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_foxglove/
find /home/jetson/isaac_ws_ros/src -name "package.json" 2>/dev/null
```
목적: yarn/nodesource 저장소가 NVIDIA 베이스에서 의도적 추가인지 검증 (사용자 지적: "없애도 되는 것인지 검증했는가?").
결과: `Dockerfile.base:201-207` 에서 NVIDIA 가 의도적 추가, `nvblox_foxglove/package.json` 가 yarn 빌드 의존 → 삭제 금지.

### 0.3 Docker 학습 자료 작성 — 사전 조사

```bash
find /home/jetson/isaac_ws_ros -path /proc -prune -o -type d \( -name "nvblox_ros" -o -name "nvblox_examples_bringup" \) -print 2>/dev/null
find /home/jetson/isaac_ws_ros/src/isaac_ros_common -name "workspace-entrypoint*"
mkdir -p /home/jetson/docker_learning
```
목적: 학습 자료 작성용 파일 위치 + 학습 자료 디렉토리 생성.
결과: `/home/jetson/docker_learning/` 신규 생성. `workspace-entrypoint.sh` = `/home/jetson/isaac_ws_ros/src/isaac_ros_common/docker/scripts/workspace-entrypoint.sh`.

### 0.4 Nav2 좁은 공간 통과 진단

```bash
grep -nE "robot_radius|footprint|inflation_radius|cost_scaling_factor|NvbloxCostmapLayer|inscribed" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml 2>&1
grep -nE "forward_sampling_distance|sim_time|vtheta_samples|vx_samples|BaseObstacle.scale|PathAlign.scale|PathDist.scale" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```
목적: 좁은 공간 통과 못하는 원인 진단 (footprint, inflation, DWB critic, RotationShim).
결과: footprint 0.36×0.245 (inscribed 0.1225). NavFn + inflation 0.30 → 1m 문 free 폭 0.40m 빠듯. PathAlign/PathDist 32, BaseObstacle 0.02.

### 0.5 SmacPlanner 패키지 가용성 + Dockerfile chain 매칭 알고리즘

```bash
dpkg -l ros-humble-nav2-smac-planner 2>/dev/null | tail -2
ros2 pkg list 2>/dev/null | grep smac
```
목적: SmacPlannerHybrid 도입 가능 여부.
결과: `ros-humble-nav2-smac-planner 1.1.18-1jammy` 설치 확인.

```bash
docker exec isaac_ros_dev-aarch64-container ls /etc/apt/sources.list.d/ | grep -E "yarn|nodesource"
```
목적: jupiter_user layer 빌드 후 yarn/nodesource 저장소 보존 확인.
결과: `yarn.list` `nodesource.list` 모두 존재 → 무결성 보존.

### 0.6 Rover 방향/회전 문제 진단

```bash
ls /home/jetson/mcu/extbd/Source/APP/
ls /home/jetson/mcu/extbd/Source/BSP/
find /home/jetson/mcu/extbd/Source -type f \( -name "*.c" -o -name "*.h" \) | xargs grep -lE "PID|pid|motor|encoder|wheel" 2>/dev/null
```
목적: MCU 임베디드 코드 구조 파악 (사용자 "/home/jetson/mcu 도 분석하라" 요청).
결과: STM32F1 + FreeRTOS, `app_differential.c`, `app_pid.c`, `app_motion.c`, `bsp_motor.c`, `bsp_encoder.c` 확인.

```bash
grep -nE "FUNC_MOTION|0x12|Differential_Ctrl|adjust" /home/jetson/mcu/extbd/Source/APP/protocol.c 2>/dev/null | head -10
grep -nE "set_car_run|set_car_motion|cmd_vel|FUNC_CAR_RUN|FUNC_MOTION" /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py | head
grep -nE "__CAR_ADJUST|0x80" /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py | head
```
목적: cmd_vel → MCU 흐름 추적 + adjust 플래그 처리 위치 식별.
결과:
```
MCU protocol.c:536  uint8_t adjust = parm & 0x80;
MCU protocol.c:545  Motion_Ctrl(Vx_recv, Vy_recv, Vz_recv, (adjust==0?0:1));
Rosmaster_Lib.py:34 self.__CAR_ADJUST = 0x80
Rosmaster_Lib.py:643 cmd = [..., self.__CAR_TYPE, ...]   ← adjust 비트 미설정
```
→ `set_car_motion` 이 adjust 플래그 미전달 발견.

### 0.7 정지 회전 이력 추적 — 이전 진행과정 md 분석

```bash
ls /home/jetson/jupiter_ws_ros2/진행과정_*.md
grep -nE "회전|rotation|RotationShim|deadzone|ROT_MIN|min_speed_theta|cmd_vel|velocity_smoother|gyro_scale|spin_dist" /home/jetson/jupiter_ws_ros2/진행과정_*.md 2>/dev/null | head -50
grep -nE "rotate|forward_sampling|min_speed_theta|max_angular|deadzone" /home/jetson/jupiter_ws_ros2/진행과정_2026_03_18.md /home/jetson/jupiter_ws_ros2/진행과정_2026_03_19.md /home/jetson/jupiter_ws_ros2/진행과정_2026_03_20.md
grep -nE "min_speed_theta|vtheta|RotationShim|forward_sampling|spin" /home/jetson/jupiter_ws_ros2/진행과정_2026_04_21.md
```
목적: 정지 회전 어려움의 이전 검증된 설정 추적.
결과:
- **03-04**: RotationShim 도입, min_speed_theta 0.30, deadzone 발견.
- **03-18**: forward_sampling_distance 0.5→0.3 (짧은 경로 RotationShim 활성 보장).
- **03-19**: deadband_velocity[2] 0.20→0.05, ANGULAR_SCALE_FACTOR 재캘리브레이션.
- **03-20**: rotate_to_heading_angular_vel 0.8→0.5, max_angular_accel 5→3.
- **04-21**: vtheta_samples 20→11 (1/19=0.0526 병적 최소 샘플 제거), min_speed_theta 0.15→0.20.

### 0.8 EKF + 센서 정렬 진단 — 도구 작성 + 측정

```bash
ls -la /home/jetson/rotation_test.py /home/jetson/orient_diag.py
cat /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml | head -250
```
목적: EKF 가 RF2O yaw 를 사용하는지 확인 + 기존 진단 도구 활용 가능성.
결과: EKF config 에서 RF2O yaw/vyaw, VSLAM yaw/vyaw 모두 비활성. 활성: WHEEL vyaw + IMU vyaw 만. 기존 `~/rotation_test.py` 130줄 존재.

```bash
python3 ~/sensor_yaw_align.py
```
목적: 새로 작성한 RF2O 기준 정렬 진단 도구 (1차, speed=0.3, duration=3s).
결과 (1차): RF2O 0.33° (회전 거의 안 일어남). 명령값이 작아 정적마찰 못 깸.

```bash
python3 ~/sensor_yaw_align.py
```
목적: 도구 수정 후 재측정 (speed=0.6, duration=4s, baseline 3s, /cmd_vel publisher 충돌 검사 추가).
결과 (2차):
```
WHEEL    87.65   -95.85   183.50   (0.858× scale, ALIGNED, L/R 비대칭 8.6%)
RF2O    107.32  -106.49   213.81   (← 기준)
VSLAM     0.00     0.00     0.00   (토픽 미수신)
EKF     105.06  -109.09   214.15   (1.002× scale, ALIGNED)
IMU     109.43  -113.02   222.45   (1.041× scale, ALIGNED)
```
→ 부호는 모두 OK. IMU 4% over-report. WHEEL 14% under-report. WHEEL L/R 비대칭 8.6% (한쪽 바퀴 마찰 차이).

### 0.9 Backup 생성 (변경 전)

```bash
BACKUP_DIR="/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/backup_$(date +%Y_%m_%d)"
mkdir -p "$BACKUP_DIR"
cp /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml "$BACKUP_DIR/nav2_params_fused.yaml.bak"
cp /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/behavior_trees/navigate_to_pose_no_backup.xml "$BACKUP_DIR/navigate_to_pose_no_backup.xml.bak"
md5sum /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml "$BACKUP_DIR/nav2_params_fused.yaml.bak"
```
목적: nav2_params 변경 전 백업 (사용자 요구).
결과: `backup_2026_04_28/` 생성. md5 동일 확인.

```bash
BACKUP_DIR="/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/backup_$(date +%Y_%m_%d)"
mkdir -p "$BACKUP_DIR"
cp /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py "$BACKUP_DIR/Rosmaster_Lib.py.bak"
cp /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py "$BACKUP_DIR/jupiter_driver_compensated.py.bak"
```
목적: driver/Rosmaster_Lib 변경 전 백업.

### 0.10 최종 검증 grep

```bash
grep -nE "imu_gyro_scale" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
grep -nE "def set_car_motion|car_type_byte|__CAR_ADJUST" /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py
grep -nE "set_car_motion.*adjust" /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
grep -nE "forward_point_distance|simulate_ahead_time:|plugin: \"nav2_smac|forward_sampling_distance|vtheta_samples|sim_time:|PathDist.scale|inflation_radius|cost_scaling_factor" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml | head -25
python3 -c "import ast; ast.parse(open('/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py').read())"
python3 -c "import ast; ast.parse(open('/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py').read())"
```
목적: 모든 변경 적용 검증 + Python syntax 무결성.
결과:
```
imu_gyro_scale: 0.984
def set_car_motion(self, v_x, v_y, v_z, adjust=False):
car_type_byte = (self.__CAR_TYPE | self.__CAR_ADJUST) if adjust else self.__CAR_TYPE
set_car_motion(vx, vy, angular_corrected, adjust=True)
plugin: "nav2_smac_planner/SmacPlanner2D"
forward_sampling_distance: 0.3
vtheta_samples: 11
sim_time: 1.0
PathAlign.forward_point_distance: 0.2
GoalAlign.forward_point_distance: 0.2
PathDist.scale: 24.0
simulate_ahead_time: 1.5  (RotationShim)
inflation_radius: 0.20  (local + global)
cost_scaling_factor: 5.0  (local + global)
```

### 0.11 빌드 + 재기동 표준 절차

```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO jupiter_bringup --symlink-install
source install/setup.bash
pkill -INT -f "nav2_vslam_fused\|nav2_fused_navigation\|jupiter_driver"
sleep 2
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py
# 별도 터미널
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```

### 0.12 Docker 컨테이너 재빌드

```bash
docker exec -it isaac_ros_dev-aarch64-container pkill -INT -f component_container_mt
cd ${ISAAC_ROS_WS}/src/isaac_ros_common && ./scripts/run_dev.sh
```
목적: jupiter_user layer 변경 (Dockerfile.jupiter_user 의 yarn fail-soft 수정 + USER admin 제거 + ARG default) 적용.
결과: 모든 layer CACHED + warning 사라짐 → 컨테이너 정상 진입 ("Creating non-root container 'admin' for host user uid=1000:gid=1000").

---

## 1. 분석에 사용한 설정 / 스크립트 파일

### 1.0 오늘 사용/열람/수정/생성한 파일 전체 목록

**호스트 ROS2 워크스페이스 (`/home/jetson/jupiter_ws_ros2`)**:
- `src/jupiter_nav_VO/config/nav2_params_fused.yaml` ← 수정 (planner, inflation, DWB, BT 다수)
- `src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` ← 수정 (imu_gyro_scale 0.984)
- `src/jupiter_nav_VO/behavior_trees/navigate_to_pose_no_backup.xml` ← 수정 (Wait 5s, retries 10, spin 45°+90°, RateController 0.5Hz)
- `src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py` ← 수정 (set_car_motion adjust 인자 추가)
- `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` ← 수정 (set_car_motion adjust=True)
- `src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml` (열람) — RF2O yaw 비활성 확인
- `src/jupiter_bringup/jupiter_bringup/gyro_pid_node.py` (열람) — ROS 측 yaw PID 노드 존재 확인
- `src/jupiter_nav_VO/backup_2026_04_28/nav2_params_fused.yaml.bak` (신규)
- `src/jupiter_nav_VO/backup_2026_04_28/navigate_to_pose_no_backup.xml.bak` (신규)
- `src/jupiter_bringup/backup_2026_04_28/Rosmaster_Lib.py.bak` (신규)
- `src/jupiter_bringup/backup_2026_04_28/jupiter_driver_compensated.py.bak` (신규)

**Isaac ROS Docker 워크스페이스 (`/home/jetson/isaac_ws_ros`)**:
- `src/isaac_ros_common/docker/Dockerfile.jupiter_user` ← 신규 (사용자 layer)
- `src/isaac_ros_common/docker/Dockerfile.{base,ros2_humble,realsense}` (열람)
- `src/isaac_ros_common/scripts/run_dev.sh` (열람)
- `src/isaac_ros_common/scripts/build_image_layers.sh` (열람)
- `src/isaac_ros_common/docker/scripts/workspace-entrypoint.sh` (열람)

**MCU 임베디드 (`/home/jetson/mcu`)**:
- `extbd/Source/APP/app_differential.c` (열람) — Differential_Ctrl, Differential_Yaw_Calc
- `extbd/Source/APP/app_pid.c` (열람) — pid_motor, pid_Yaw (P=0.4, I=0, D=0.1)
- `extbd/Source/APP/protocol.c` (열람) — FUNC_MOTION 핸들러 (parm & 0x80 = adjust)

**홈 디렉토리 (`/home/jetson`)**:
- `~/.isaac_ros_common-config` ← 신규 (CONFIG_IMAGE_KEY)
- `~/sensor_yaw_align.py` ← 신규 (RF2O 기준 정렬 진단 도구)
- `~/rotation_test.py`, `~/orient_diag.py` (열람) — 02-25 작업 기존 도구
- `~/docker_learning/README.md` ← 신규
- `~/docker_learning/01_docker_basics.md` ← 신규
- `~/docker_learning/02_isaac_ros_chain.md` ← 신규
- `~/docker_learning/03_run_dev_workflow.md` ← 신규
- `~/docker_learning/04_jupiter_user_layer.md` ← 신규
- `~/docker_learning/05_practice.sh` ← 신규 (chmod +x)

**이전 진행과정 문서 (참조용 열람)**:
- `진행과정_2026_03_04.md`, `_03_18.md`, `_03_19.md`, `_03_20.md`, `_04_21.md` — 정지 회전 이력 추적

### 1.1 수정된 파일 1 — `nav2_params_fused.yaml`

수정 항목 (이번 세션 누적):

**Planner 교체**:
- L192: NavfnPlanner → **SmacPlannerHybrid** → **SmacPlanner2D** (최종)
  - 사유: NavFn 은 좁은 통로 footprint 회전 미고려. SmacPlannerHybrid + DUBIN 곡선 plan 이 차동구동 + 한쪽 바퀴 이상 환경에서 추종 어려움 → SmacPlanner2D (motion model 없는 holonomic A*) 가 최종 선택. RotationShim 의 정지 회전 동작 회복.

**Inflation**:
- L297, L345 (local+global): `cost_scaling_factor: 10 → 5`, `inflation_radius: 0.30 → 0.20` — 1m 문 free 폭 0.40→0.60m 확보.

**DWB critic**:
- L156: `BaseObstacle.scale: 0.02 → 0.05` — 장애물 회피 비용 소폭 상향.
- L161: `PathAlign.scale: 32 → 20` 유지 — 회전 자유도.
- L165: `PathDist.scale: 32 → 20 → 28 → 24` 절충 — 회전 reject 회피 + 위치 추종.
- **L169: `PathAlign.forward_point_distance: 0.1 → 0.2`** — path 0.2m 앞 heading 으로 정렬 (preemptive turn-in).
- **L171: `GoalAlign.forward_point_distance: 0.1 → 0.2`**.

**RotationShim + DWB 동작**:
- L120: `forward_sampling_distance: 0.3 → 0.6 → 0.3` 회귀 복원 — 짧은 path RotationShim 활성 보장.
- **L123: `simulate_ahead_time: 1.0 → 1.5`** — 회전 후 충돌 검사 시간 확대.
- L146: `vtheta_samples: 11 → 21 → 11` 회귀 복원 — 최소 step 0.10 안정 (04-21 검증값).
- L149: `sim_time: 1.5 → 1.0` — 좁은 통로 trajectory 평가.

**Local costmap**:
- L229-230: `width/height: 3 → 5` — 좁은 통로 회피 trajectory 탐색 공간.

**bt_navigator + behavior_server**:
- L40: `default_nav_to_pose_bt_xml: $(find-pkg-share jupiter_nav_VO)/behavior_trees/navigate_to_pose_no_backup.xml` 추가.
- L88: `controller_server.failure_tolerance: 3.0 → 5.0` — 사람 횡단 50 cycles 대기.

### 1.2 수정된 파일 2 — `nav2_vslam_fused.launch.py`

수정 항목:
- L172: `imu_gyro_scale: 1.025 → 0.984` — sensor_yaw_align.py 측정 결과 IMU/RF2O = 1.041 (4.1% over) → 1.025/1.041 ≈ 0.984 로 정밀화.

### 1.3 수정된 파일 3 — `navigate_to_pose_no_backup.xml`

수정 항목:
- `RecoveryNode number_of_retries: 6 → 10`.
- `RateController hz: 1.0 → 0.5` (BT replanning 2초마다, CPU 절약 + 안정).
- `Wait wait_duration: 2 → 5` (사람 횡단 대기 시간 충분히).
- RoundRobin 에 `<Spin spin_dist="0.785"/>` (45°) 추가 — 카메라 측면 시야 확보.
- 기존 `<Spin spin_dist="1.57"/>` (90°) 는 두 번째 시도로 유지.

### 1.4 수정된 파일 4 — `Rosmaster_Lib.py`

수정 항목:
- L632 `def set_car_motion(self, v_x, v_y, v_z, adjust=False):` ← `adjust` 인자 추가.
- L644 `car_type_byte = (self.__CAR_TYPE | self.__CAR_ADJUST) if adjust else self.__CAR_TYPE` ← 0x80 비트 OR (set_car_run 의 검증된 패턴 적용).
- L648 cmd 배열의 `self.__CAR_TYPE` → `car_type_byte` 변경.

### 1.5 수정된 파일 5 — `jupiter_driver_compensated.py`

수정 항목:
- L362: `self.bot.set_car_motion(vx, vy, angular_corrected, adjust=True)` ← `adjust=True` 추가.
- 효과: MCU 의 `Differential_Yaw_Calc` (Yaw PID, P=0.4) 가 IMU yaw 입력 받아 한쪽 바퀴 마찰로 인한 직진 yaw drift 자동 보정.

### 1.6 신규 파일 1 — `Dockerfile.jupiter_user`

위치: `/home/jetson/isaac_ws_ros/src/isaac_ros_common/docker/Dockerfile.jupiter_user`

핵심 내용:
- `ARG BASE_IMAGE=ubuntu:22.04` (lint 충족, build_image_layers.sh 가 override)
- `USER root`
- `RUN { apt-get update || echo ... ; } && apt-get install -y --no-install-recommends ros-humble-pointcloud-to-laserscan ros-humble-isaac-ros-peoplesemseg-models-install ros-humble-isaac-ros-peoplenet-models-install && rm -rf /var/lib/apt/lists/*` (yarn GPG fail-soft)
- `RUN rosdep update --rosdistro=humble --include-eol-distros || true`
- `USER admin` 디렉티브 금지 주석 — entrypoint 가 동적 생성.

### 1.7 신규 파일 2 — `~/.isaac_ros_common-config`

내용: `CONFIG_IMAGE_KEY=ros2_humble.realsense.jupiter_user`

### 1.8 신규 파일 3 — `~/sensor_yaw_align.py` (218줄)

기능:
- `/cmd_vel` publisher 충돌 검사 (Nav2 활성 시 경고).
- baseline 3s → LEFT (+speed × duration s) → pause 1.5s → RIGHT (-speed × duration s) → 분석.
- 누적 yaw unwrap (±π 경계 처리) 으로 정확 측정.
- RF2O 기준 부호/스케일 비교 + L/R 비대칭 검사.
- 권장 보정값 자동 출력 (imu_gyro_scale, imu_invert_z 등).
- CLI: `--speed`, `--duration`.

### 1.9 신규 파일 — Docker 학습 자료 (`/home/jetson/docker_learning/`)

- `README.md` (69줄) — 학습 진입점, 5단계 구성.
- `01_docker_basics.md` (212줄) — Image/Container, Layer/cache, Dockerfile 명령, CLI.
- `02_isaac_ros_chain.md` (290줄) — 4-layer chain, build_image_layers.sh 매칭 알고리즘.
- `03_run_dev_workflow.md` (314줄) — run_dev.sh 흐름, docker run 옵션, workspace-entrypoint.sh.
- `04_jupiter_user_layer.md` (328줄) — jupiter_user line-by-line + 의존성 무결성 검증 표 + 확장 패턴.
- `05_practice.sh` (218줄, chmod +x) — 8 STEP read-only 실습.

---

## 2. 관찰 기준 요약

### 2.1 sensor_yaw_align.py 측정 (2차, speed=0.6, duration=4s)

| Source | LEFT (+) | RIGHT (-) | scale (vs RF2O) | 부호 |
|---|---:|---:|---:|---|
| WHEEL | 87.65° | -95.85° | 0.858× | OK |
| RF2O (기준) | 107.32° | -106.49° | 1.000 | — |
| VSLAM | 0.00 | 0.00 | (토픽 미수신) | — |
| EKF | 105.06° | -109.09° | 1.002× | OK |
| IMU | 109.43° | -113.02° | 1.041× | OK |

WHEEL L/R 비대칭: `(95.85-87.65)/95.85 = 8.6%` — 한쪽 바퀴 마찰 차이의 정량 측정 (소프트웨어 보정 불가, 기구 문제).

### 2.2 변경된 nav2_params_fused.yaml 핵심 항목 (최종)

| 카테고리 | 파라미터 | 값 | 이전값 |
|---|---|---:|---:|
| Planner | plugin | SmacPlanner2D | NavfnPlanner |
| Planner | expected_planner_frequency | 3.0 | 5.0 |
| RotationShim | forward_sampling_distance | 0.3 | 0.3 |
| RotationShim | simulate_ahead_time | **1.5** | 1.0 |
| DWB | sim_time | 1.0 | 1.5 |
| DWB | vtheta_samples | 11 | 11 |
| DWB | min_speed_theta | 0.20 | 0.20 |
| DWB critic | PathAlign.scale | 20.0 | 32.0 |
| DWB critic | PathAlign.forward_point_distance | **0.2** | 0.1 |
| DWB critic | GoalAlign.forward_point_distance | **0.2** | 0.1 |
| DWB critic | PathDist.scale | 24.0 | 32.0 |
| DWB critic | BaseObstacle.scale | 0.05 | 0.02 |
| controller_server | failure_tolerance | **5.0** | 3.0 |
| local_costmap | width/height | 5 | 3 |
| local/global inflation | cost_scaling_factor | 5.0 | 10.0 |
| local/global inflation | inflation_radius | 0.20 | 0.30 |

### 2.3 Driver / MCU 통신 검증

| 항목 | 값 | 근거 |
|---|---|---|
| MCU `parm & 0x80` = adjust | True | protocol.c:536 |
| Rosmaster_Lib `__CAR_ADJUST` | 0x80 | Rosmaster_Lib.py:34 |
| `set_car_motion` adjust 인자 | True (수정 후) | jupiter_driver:362 |
| MCU `pid_Yaw` (P, I, D) | 0.4, 0, 0.1 | app_pid.c:16 |
| Rosmaster `car_type` | 1 (X3) | jupiter_driver:67 |
| MCU `Differential_Yaw_Calc` 출력 제한 | ±π/6 (30°/s) | app_pid.c:353-356 |

### 2.4 Docker chain 빌드 검증

| 항목 | 결과 |
|---|---|
| `Dockerfile.aarch64` symlink | → `Dockerfile.base` ✓ |
| Chain 빌드 순서 | aarch64 → ros2_humble → realsense → jupiter_user ✓ |
| jupiter_user `[2/3]` apt install | CACHED (재빌드 시) ✓ |
| jupiter_user `[3/3]` rosdep update | CACHED ✓ |
| 컨테이너 진입 시 admin 동적 생성 | "Creating non-root container 'admin' for host user uid=1000:gid=1000" ✓ |
| 박제된 패키지 (`dpkg -l`) | pointcloud-to-laserscan / peoplesemseg-models-install / peoplenet-models-install 모두 ii ✓ |
| yarn / nodesource 저장소 보존 | yarn.list, nodesource.list 존재 ✓ |
