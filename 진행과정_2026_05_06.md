# 진행과정 2026-05-06: MPPI controller 도입 + 펌웨어 wheel_speeds packet 추가 + 모터 캘리브레이션 + M3 mechanical 이슈 정량 진단

## 1. 목표

### 1.1 사용자 요청 (시간순)

1. > "A2. Nav2 안정화 (DWB → MPPI 검토, footprint·rotation_shim 튜닝) 이 수행에 대해서 분석하라"
2. > "좁은 corridor는 nvblox를 사용했더니 발생한 것. (카메라는 전방에 1개) 센서 추가나 패키지 변경으로 해결할 수 있는 부분인가?"
3. > "우선 컨트롤러를 mppi로 변경. 그리고 경로를 잘 만들기 위해 필요한 것들이 무엇이 있는지 제시."
4. > "nav2가 해당 문구를 띄우며, 움직이지 못하고 있다. 원인을 찾아 해결하라." (Controller period more than model dt 에러)
5. > "1m 거리 주행 시 cpu 사용량 상승. 제자리 회전(180도) 후 주행 시 경로만큼 회전하고 이동하는 것이 아닌, 조금만 회전 후 전진했다가 경로 이탈"
6. > "현재 회전 후 지점으로 이동하라고 명령을 내렸는데, 움직이지 못하는 있는 상황이다. 지금 어떤 상황인지 분석하라."
7. > "관찰되는 문제 : 1. path가 로봇의 x축 정면을 기준으로, 왼쪽으로 볼록하면, 그대로 따라가는 것이 아닌, 오른쪽으로 볼록한 경로로 주행한다 ... 2. 회전할 때, 기구의 바퀴 한쪽이 움직이지 않는 경우가 많다 ... 3. bag를 기록하는 것이 좋을까?"
8. > "모터는 현재 2개만 달려있기에, 2개에 대한 데이터만 들어온다는 것을 기억해라. 펌웨어 수정을 진행하라. bag를 자동으로 기록하되, 용량문제가 있으니 매 실행 시 같은 이름으로 저장하게 만드는 것은 어떠한가"
9. > "flashing을 하고 jetson보드를 재부팅하지 않은 것이 원인이었다. ... path를 따라가기 위해서는 왼쪽으로 회전해야 하는데, 로버가 오른쪽으로 회전하게 되고, 그렇게 하면서 path를 벗어나 backup을 수행해버린다."
10. > "옵션A를 진행하면, 크게 움직이게 명령을 내리는 주체가 nav2 컨트롤러가 되는 것인가? 아니면 작은 회전 명령을 내리고, 크게 움직이게 되는 것인가"
11. > "2번으로 진행해보자" (옵션 A 즉시 + 옵션 B 후속)
12. > "좌회전을 못해 backup하는 문제는 어느정도 해결되는 것으로 보인다. 그런데 경로를 제대로 못따라가기는 한다."
13. > "캘리브레이션 노드 작성하라"
14. > "phase1에서 M3를 진행할 때, 속도가 점점 빨라지는데, wheel_speed는 0으로 나오는 이유는 무엇인가?"
15. > "작성 진행하라. ... 오른쪽 휠에 문제가 있다. (손으로 회전시켰을 때, 바퀴에서 드드득 소리가 난다)"
16. > "우선 하드웨어 정비 이전에 측정한 결과이다. 분석하여 알려달라."
17. > "오늘 한 것들을 정리하라. 특히 펌웨어 관련 코드는 잘 모르는 부분이 많아서, 무엇인지, 어떤 의도인지, 어떤 식으로 동작하게 되는지도 적어달라."

### 1.2 이전 세션 요약 / 연관 작업

- **2026-04-30**: jupiter_safety 패키지 (Tier 2: stuck_detector, forced_recovery, alert_publisher, twist_mux, collision_monitor) + DWB 튜닝 (PathAlign 28, PathDist 32) + driver 의 정지 회전 adaptive kick (MCU_ROT_START_ANGULAR=0.55, encoder 기반 종료) + 이동 회전 데드존 보상 (MOVING_ROT_MIN_ANGULAR=0.20) 적용 완료.
- **이전 plan**: `/home/jetson/.claude/plans/functional-sleeping-starfish.md` (Tier 2.5 collision_monitor + Tier 3 ultrasonic 로드맵)
- 본 세션은 위 구조 위에서 controller 자체 (DWB → MPPI) 와 모터 응답 정량 진단 진행.

## 2. 대상 시스템 구성

### 2.1 cmd_vel pipeline (변경 없음, 기존)

```
nav2/MPPI ────> velocity_smoother ─> /cmd_vel ─> collision_monitor ─> /cmd_vel_nav_filtered ┐
behavior_server (Spin/BackUp/DriveOnHeading × 3 publishers) ─────────────────────────────────┤
joy → /cmd_vel_joy ──────────────────────────────────────────────────────────────────────────┼─> twist_mux ─> /cmd_vel_safe ─> jupiter_driver ─> MCU
forced_recovery → /cmd_vel_force ────────────────────────────────────────────────────────────┘
```

twist_mux 우선순위: joy=100 > force=80 > nav=50.

### 2.2 펌웨어 (MCU) 의 시리얼 통신 구조

본 세션의 펌웨어 작업 이해를 위한 기본 구조:

**(가) Packet 형식**

MCU 와 호스트 (Jetson) 는 USB 시리얼 (UART1, 115200 baud) 로 양방향 packet 교환:

```
[HEAD=0x7B] [DEVICE_ID] [LEN-2] [FUNC] [ DATA ... ] [CHECKSUM]
   1 byte      1 byte    1 byte  1 byte                1 byte
```

- **HEAD** = `PTO_HEAD` (0x7B), packet 시작 마커
- **DEVICE_ID** = `PTO_DEVICE_ID-1`, 하드웨어 식별자
- **LEN** = 본 packet 의 byte 수 - 2 (HEAD/DEVICE_ID 제외)
- **FUNC** = 1 byte 의 "기능 코드". 어떤 종류 데이터인지 식별. 0x01~0xFF 범위.
- **DATA** = FUNC 별 가변 길이 페이로드
- **CHECKSUM** = LEN 부터 DATA 까지의 단순 합 (uint8 wrap-around)

**(나) FUNC 코드 영역별 분류** ([protocol.h](mcu/extbd/Source/APP/protocol.h))

| FUNC 코드 | 이름 | 방향 | 용도 |
|---|---|---|---|
| 0x01 | FUNC_AUTO_REPORT | host→MCU | 주기 송신 ON/OFF |
| 0x03~0x06 | FUNC_PWM_SERVO 등 | host→MCU | servo / RGB 제어 |
| **0x08** | **FUNC_REPORT_WHEEL_SPEED** | **MCU→host** | **🆕 좌/우 휠 속도 (오늘 추가)** |
| 0x0A | FUNC_REPORT_SPEED | MCU→host | 차체 vx/vy/vz |
| 0x0B | FUNC_REPORT_MPU_RAW | MCU→host | IMU raw |
| 0x0C | FUNC_REPORT_IMU_ATT | MCU→host | IMU yaw/pitch/roll |
| 0x0D | FUNC_REPORT_ENCODER | MCU→host | 4 모터 encoder 누적 카운트 |
| 0x10 | FUNC_MOTOR | host→MCU | 직접 PWM (open-loop) |
| 0x12 | FUNC_MOTION | host→MCU | velocity 명령 (closed-loop) |

**(다) Auto-report 메커니즘** ([app.c](mcu/extbd/Source/APP/app.c) `vTask_Auto_Report`)

MCU 의 FreeRTOS 태스크 (`vTask_Auto_Report`) 가 MCU→host 데이터를 주기 송출:

```c
while (System_Enable())
{
    if (g_Auto_Report) {
        if (report_count == 1) Motion_Send_Data();        // body Vx/Vy/Vz
        else if (report_count == 6) Motion_Send_Wheel_Speed();  // 🆕 오늘 추가
        else if (report_count == 11) IMU_Send_Raw_Data();
        else if (report_count == 21) IMU_Send_Attitude_Data();
        else if (report_count == 31) Encoder_Send_Count_Now();
    }
    report_count++;
    if (report_count > AUTO_SEND_TIMEOUT /*=40*/) report_count = 0;
    App_Delay_ms(1);
}
```

타이밍:
- `App_Delay_ms(1)` → 1 cycle = 1ms
- `AUTO_SEND_TIMEOUT = 40` → 전체 cycle 40ms = **25Hz**
- 각 report_count 슬롯은 25Hz 1회 발행
- 동일 cycle 안에 여러 슬롯 안 겹치게 분산

각 send 함수는 packet 1개 송출 후 즉시 반환 (UART buffered). 25Hz × 평균 12 byte = 300 byte/s — UART 115200 (≈11.5KB/s) 의 3% 만 사용. 여유 충분.

**(라) 모터 데이터 구조** ([app_pid.h](mcu/extbd/Source/APP/app_pid.h))

```c
typedef struct _motor_data_t
{
    float speed_mm_s[4];   // ENCODER 측정값 — PID feedback (mm/s)
    float speed_pwm[4];    // PID 출력 → 모터 PWM
    int16_t speed_set[4];  // PID 입력 — target 속도 (mm/s)
} motor_data_t;
```

- `speed_set[i]` 는 host 가 set_car_motion() 등으로 보낸 target 을 차륜별 분해한 값
- `speed_mm_s[i]` 는 엔코더 pulse 카운트로부터 계산된 실제 휠 속도
- `speed_pwm[i]` 는 PID 가 두 값 차이로 계산한 PWM 출력 명령

**중요 — 새 함수가 의존하는 부분**: `speed_mm_s[]` 는 PID 루프 안에서 (10ms 주기로) 엔코더 → 속도 변환 후 갱신됨. **PID 가 활성일 때만 최신 값**. `set_motor` 같은 직접 PWM 명령 시엔 PID 우회되어 갱신 안 됨 (Phase 1 v1 실패 원인).

**(마) Jupiter 차동구동 차량의 모터 매핑** ([app_differential.c](mcu/extbd/Source/APP/app_differential.c) line 42-48)

```c
void Differential_Set_Speed(int16_t left_speed, int16_t right_speed)
{
    // Motor 1은 왼쪽, Motor 3는 오른쪽으로 사용
    // Motor 2,4는 사용하지 않음
    Motion_Set_Speed(left_speed, 0, right_speed, 0);
}
```

- **M1 (motor index 0) = LEFT 휠**
- **M3 (motor index 2) = RIGHT 휠**
- M2, M4 = 사용 안 함 (배선 안 됨)

따라서 `speed_mm_s[0]` = LEFT 실측, `speed_mm_s[2]` = RIGHT 실측. 새 packet 도 이 두 값만 송출.

### 2.3 호스트 (Jetson) 의 packet 처리 흐름

```
MCU 펌웨어
  vTask_Auto_Report 25Hz
    └─> Motion_Send_Wheel_Speed()
         └─> USART1_Send_ArrayU8(...)
              └─> UART1 — USB 시리얼
                   ↓
Jetson 호스트
  /dev/myserial 의 receive thread
    └─> Rosmaster_Lib.__receive_data()
         └─> packet 헤더/체크섬 검증
              └─> __parse_data(ext_type=0x08, ext_data=[...])
                   └─> __wheel_left_mps, __wheel_right_mps 갱신
                        ↑
              jupiter_driver_compensated.publish_wheel_speeds() 가 25Hz
                └─> bot.get_wheel_speeds() → [left, right]
                     └─> /jupiter/wheel_speeds (Float32MultiArray) publish
```

## 3. 문제 분석

### 3.1 MPPI 도입 시 controller_period 제약

**증상**:
```
[ERROR] Original error: Controller period more then model dt, set it equal to model dt
[ERROR] Failed to bring up all requested nodes. Aborting bringup.
```

**원인**: MPPI 의 source code 에 `period <= model_dt` 강제 검사 존재.
- `controller_frequency = 10` → period = 100ms
- `model_dt = 0.05` → 50ms
- 100 > 50 → 거부

**근본 원인**: MPPI 가 매 cycle 에서 trajectory rollout 시 `model_dt` 간격으로 forward integration. controller period 가 model_dt 보다 크면 control 출력 사이의 시간이 model 의 step 보다 큰 모순.

**조치**: `controller_frequency: 10 → 20` (period 50ms = model_dt 50ms).

### 3.2 nav2 가 좌회전 못해서 backup 무한 반복

**증상**: path 좌-볼록 (좌회전 필요) → robot 우향 drift → path 이탈 → progress_checker 20s timeout → BT recovery (Spin/BackUp) → Spin 도 회전 안 됨 → BackUp 으로 fallback → backup 후 같은 위치 → 무한 반복.

**bag 분석 (216초, 60 plans, 좌회전 명령 1611 회):**
```
좌회전 명령 (vx<0.05, ω>+0.10):
  평균 cmd ω = +0.204 rad/s
  평균 actual ω = +0.066 rad/s (32%)
  실제 좌회전: 26.1%
  실제 정지:   68.8%   ⚠️
  실제 우회전:  5.1%
```

**근본 원인**: nav2 가 회전 명령 평균 +0.204 rad/s 를 발행. driver 의 정지 회전 RUNNING 단계 floor `ROT_RUNNING_MIN_ANGULAR=0.12` 적용 → max(0.204, 0.12) = 0.204 그대로 → wheel-level diff = 0.204 × 0.1225 = 0.025 m/s = **1.5 tick** (엔코더 분해능 16.3 mm/s = 0.0163 m/s). 마진 거의 없음 → 양쪽 모두 deadzone 미달 → 정지.

**우회전은 잘됨**: 우회전 평균 cmd ω = -0.542 rad/s (좌회전 2.7×). wheel-level diff = 4 tick → deadzone 충분 통과. → bag 의 73% 좌회전 명령 + 좌-볼록 path 시 fail 패턴.

### 3.3 lethal zone 에서 회전 deadlock — stuck_detector 한계

**증상**: 로봇이 lethal zone 진입 시 nav2 가 회전 명령 (linear=0, angular=0.15) 이지만 collision_monitor 가 차단 → /cmd_vel_safe 무발행 → driver 미동.

**stuck_detector 코드 검사** ([stuck_detector.py](src/jupiter_safety/jupiter_safety/stuck_detector.py)):
```python
linear_stall = (cmd_abs > STALL_CMD_MIN) and (actual_abs < STALL_ACTUAL_MAX)
# cmd_abs = abs(cmd.linear.x) — 회전 명령 (linear=0) 일 때 0 → stall 검출 안 됨
```

**근본 원인**: stuck_detector 가 linear 명령만 검사. 회전 명령 시 cmd_abs=0 → stall 조건 미달 → forced_recovery 미발동 → deadlock 영구.

### 3.4 driver flash 후 재부팅 안 함

**증상**: 펌웨어 flash 했지만 `/jupiter/wheel_speeds` 토픽 안 나옴.

**원인**: ROS workspace 빌드는 했지만 jupiter_driver 노드가 launch 시 시작된 OLD binary 를 메모리에 들고 있음. `--symlink-install` 이라도 노드 재시작 필수. + 사용자가 Jetson 보드 재부팅 안 한 상태.

**해결**: Jetson 보드 reboot → 모든 노드/펌웨어 새로 시작 → 토픽 활성.

### 3.5 path 추종 평균 20cm 이탈

**bag 측정**:
```
평균: 19.7 cm, median: 20.7 cm, p95: 32.8 cm, max: 50.5 cm
```

footprint 0.36 × 0.245 m 의 절반 수준 이탈.

**원인 후보 4가지** (Phase 2 측정으로 확정):
- 가설 1 (MCU PID gain 부족): **기각**. 무부하 vx 응답 100%.
- 가설 2 (ANGULAR_SCALE 미캘리브): vx 도 영향 받아야 하므로 부적합.
- 가설 3 (모터 부하 응답 한계): 일부 영향. 그러나 부하만으로 30% 응답률 설명 어려움.
- 가설 4 (driver floor 부작용 over-rotation): cmd_vel ω 부호 반전 0.16Hz 라 진동 없음. 기각.
- **확정 원인**: M3 의 mechanical 비대칭. 아래 §3.8 참조.

### 3.6 한쪽 휠 deadzone 미달

bag 에서 좌회전 시 wheel_L=+0.013, wheel_R=+0.025 (둘 다 forward, ω 못 달성). 회전 명령 시 LEFT wheel 이 reverse 명령 받아도 deadzone 미달로 미동. 같은 패턴이 우회전에서 더 심함.

→ Phase 1 v2 캘리브레이션으로 정량 측정 필요.

### 3.7 캘리브레이션 v1 (set_motor 직접 PWM) 측정 실패

**증상**: M3 PWM sweep 시 휠은 물리적으로 회전하는데 `/jupiter/wheel_speeds` 가 0 으로만 보고. M1 은 계속 +0.0570 의 stale 값.

**근본 원인** (펌웨어 구조 분석):
- `Motion_Send_Wheel_Speed()` 는 `motor_data.speed_mm_s[]` 읽어서 송출
- `motor_data.speed_mm_s[]` 는 **PID 루프 안에서만** 엔코더 → 속도 변환 후 갱신됨
- `set_motor` (FUNC_MOTOR 0x10) 는 **PID 우회 직접 PWM** 명령
- → PID 안 돌아 → speed_mm_s[] 동결 → 송출되는 packet 의 값이 stale

**조치**: v2 — set_car_motion (closed-loop) 사용. PID 활성 시엔 정상 갱신.

### 3.8 M3 mechanical 이슈 — 정량 진단

사용자 보고: "오른쪽 휠에 문제가 있다. 손으로 회전시켰을 때, 바퀴에서 드드득 소리가 난다"

**캘리브레이션 v2 결과 (정비 전 baseline)**:

| 방향 | M1 (LEFT) DZ | M3 (RIGHT) DZ | 비대칭 |
|---|---|---|---|
| Linear forward | 0.005 m/s | 0.030 m/s | 6× |
| Linear reverse | 0.015 m/s | **0.100 m/s** | **6.67×** |
| Rotation CCW (left turn 시 R 의 forward) | 0.05 rad/s | **0.40 rad/s** | **8×** |
| Rotation CW (right turn 시 R 의 reverse) | 0.05 rad/s | **0.30 rad/s** | **6×** |

**M3 의 reverse 방향이 특히 심함**. vx=-0.080 까지 ratio 0% (완전 stuck). 0.100 까지 가야 63% 응답.

**Phase 2 PID rise time @ ω=0.20 = 1228 ms**. PID 적분이 M3 정지마찰 극복에 1.2초 걸림. MPPI 의 50ms cycle 안에서 못 잡음 → control loop 미닫힘.

**근본 원인**: M3 휠의 베어링/기어/축 마찰 비대상 증가. "드드득" 소리는 일반적으로 베어링 손상 또는 이물질 끼임.

## 4. 적용한 변경

### 4.1 펌웨어 — FUNC_REPORT_WHEEL_SPEED 추가

#### 4.1.1 [protocol.h](mcu/extbd/Source/APP/protocol.h) — 새 FUNC 코드 정의

| 변경 전 | 변경 후 |
|---|---|
| (없음) | `#define FUNC_REPORT_WHEEL_SPEED (0x08)` |

**이게 무엇인가**: FUNC 코드는 단지 1바이트 정수 값이다. MCU 가 host 에 보내는 packet 의 4번째 byte 위치 (`data_buffer[3]`) 에 들어가서 "이 packet 은 어떤 종류의 데이터인가" 를 식별한다. 0x08 은 미사용 영역 (0x07~0x09 비어있었음, 0x09 는 호스트 측 Rosmaster_Lib 이 MAG 으로 reserve 했지만 펌웨어에선 미사용 — 충돌 회피 위해 0x08 선택).

**의도**: 새로운 종류의 데이터 송출을 위한 식별자 등록. 향후 호스트가 받은 packet 을 보고 "FUNC=0x08 이면 wheel_speed packet 이구나" 라고 알 수 있게 됨.

**동작**: 정의만 추가. 코드 동작에 직접 영향 없음. 다음 4.1.2 의 송출 함수에서 사용됨.

#### 4.1.2 [app_motion.c](mcu/extbd/Source/APP/app_motion.c) — `Motion_Send_Wheel_Speed()` 함수 추가

```c
void Motion_Send_Wheel_Speed(void)
{
    #define LEN_WS  9
    uint8_t data_buffer[LEN_WS] = {0};
    uint8_t i, checknum = 0;
    int16_t v_left  = (int16_t)motor_data.speed_mm_s[0];   // M1 = LEFT
    int16_t v_right = (int16_t)motor_data.speed_mm_s[2];   // M3 = RIGHT

    data_buffer[0] = PTO_HEAD;                  // 0x7B
    data_buffer[1] = PTO_DEVICE_ID-1;           // 디바이스 식별
    data_buffer[2] = LEN_WS-2;                  // 7 (헤더 2바이트 제외 길이)
    data_buffer[3] = FUNC_REPORT_WHEEL_SPEED;   // 0x08
    data_buffer[4] = v_left & 0xff;             // L int16 LSB
    data_buffer[5] = (v_left >> 8) & 0xff;      // L int16 MSB
    data_buffer[6] = v_right & 0xff;            // R int16 LSB
    data_buffer[7] = (v_right >> 8) & 0xff;     // R int16 MSB

    for (i = 2; i < LEN_WS-1; i++)
        checknum += data_buffer[i];             // checksum: LEN+FUNC+DATA 합
    data_buffer[LEN_WS-1] = checknum;
    USART1_Send_ArrayU8(data_buffer, sizeof(data_buffer));   // UART 송출
}
```

**이게 무엇인가**: 9 byte 의 packet 을 만들어 USART1 (UART hardware peripheral) 로 호스트 (Jetson) 에 송출하는 함수.

**의도**:
1. 차체 vx/vy/vz 만으로는 개별 휠의 deadzone 진단 불가능 (한쪽 휠이 안 도는데 차체 평균 이상하게 보고됨)
2. 좌/우 휠 각각의 실측 속도가 필요 → wheel-level diagnosis 가능

**값의 출처와 형식**:
- `motor_data.speed_mm_s[0]` — M1 (LEFT) 의 실측 속도. PID 루프가 엔코더 pulse 카운트 → 속도 변환 후 갱신한 float (mm/s).
- `motor_data.speed_mm_s[2]` — M3 (RIGHT). 동일.
- int16 으로 캐스팅 → ±32767 mm/s = ±32 m/s 표현 가능 (충분).
- little-endian (LSB 먼저) byte 분해 — STM32 native byte order.

**동작 (한 호출에 일어나는 일)**:
1. `motor_data.speed_mm_s[]` 에서 두 값 읽음
2. int16 캐스팅 + byte 분해
3. checksum 계산 (단순 byte 합, uint8 wrap-around)
4. `USART1_Send_ArrayU8` 호출 → DMA/IT 기반 UART buffer 에 9 byte 적재 → hardware 가 baud rate 으로 시리얼 line 에 전송
5. 함수 즉시 반환 (UART 전송은 buffered, blocking 아님)

**주의 사항**:
- `motor_data.speed_mm_s[]` 가 PID 루프에서만 갱신됨. set_motor (open-loop PWM) 사용 시 stale 값 송출됨. set_car_motion (closed-loop) 시 정상.
- 9 byte × 25Hz = 225 byte/s. UART 115200 baud 의 0.2% 사용. 부하 무시 수준.

#### 4.1.3 [app_motion.h](mcu/extbd/Source/APP/app_motion.h) — 함수 선언

```c
void Motion_Send_Wheel_Speed(void);   // [2026-05-06] 좌/우 휠 mm/s 송출 (M1=L, M3=R)
```

**이게 무엇인가**: 헤더 파일은 다른 .c 파일이 함수를 호출할 수 있도록 함수 시그니처를 노출하는 곳. 본 선언이 있어야 [app.c](mcu/extbd/Source/APP/app.c) 에서 함수를 호출 가능.

**의도**: `Motion_Send_Wheel_Speed()` 를 `vTask_Auto_Report` (app.c 안) 에서 호출할 수 있게 함.

#### 4.1.4 [app.c](mcu/extbd/Source/APP/app.c) — `vTask_Auto_Report` 의 슬롯 추가

| 변경 전 | 변경 후 |
|---|---|
| `if (report_count == 1) Motion_Send_Data();`<br>`else if (report_count == 11) ICM20948_Send_Raw_Data();` | `if (report_count == 1) Motion_Send_Data();`<br>**`else if (report_count == 6) Motion_Send_Wheel_Speed();  // 🆕`**<br>`else if (report_count == 11) ICM20948_Send_Raw_Data();` |

**이게 무엇인가**: `vTask_Auto_Report` 는 FreeRTOS 의 별도 thread (= 태스크) 로 동작하는 무한 루프. 1ms 마다 한 번씩 실행되며, `report_count` 변수 (0~40 cycle) 의 값에 따라 다른 데이터를 송출한다.

**의도**: 0x08 packet 도 25Hz 주기 송출에 포함. count==6 슬롯 비어있어 (count==1 과 11 사이) 배치.

**동작**:
- t=0ms: report_count=0
- t=1ms: report_count=1 → `Motion_Send_Data()` 호출 (vx/vy/vz)
- t=2~5ms: 빈 슬롯
- **t=6ms: report_count=6 → `Motion_Send_Wheel_Speed()` 호출 (L/R wheel speed)** 🆕
- t=7~10ms: 빈 슬롯
- t=11ms: report_count=11 → IMU raw
- ...
- t=40ms: report_count=40 → reset to 0
- t=41ms: report_count=1 → 다시 Motion_Send_Data
- ... (반복)

각 슬롯은 40ms 주기 내 1회만 실행 → 25Hz. 5개 슬롯 (1, 6, 11, 21, 31) 모두 25Hz 발행.

**왜 count==6 인가**: count 1, 11, 21, 31 사이 중간 빈 슬롯이고, body velocity (count==1) 와 IMU raw (count==11) 사이에 5ms 간격으로 끼어있어 UART buffer 부담 분산.

### 4.2 [Rosmaster_Lib.py](src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py) — Python 측 packet 파싱

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| FUNC 코드 정의 | `FUNC_REPORT_MAG = 0x09` | `FUNC_REPORT_WHEEL_SPEED = 0x08`<br>`FUNC_REPORT_MAG = 0x09` |
| 상태 변수 | (없음) | `__wheel_left_mps = 0.0`, `__wheel_right_mps = 0.0` |
| `__parse_data` 의 case | (없음) | 0x08 case 추가 → `__wheel_left_mps, __wheel_right_mps = struct.unpack 'h' / 1000.0` |
| 신규 getter | (없음) | `def get_wheel_speeds(self): return self.__wheel_left_mps, self.__wheel_right_mps` |

**이게 무엇인가**: Rosmaster_Lib 은 MCU 와의 시리얼 통신을 담당하는 Python 라이브러리. background thread 가 `/dev/myserial` 에서 byte stream 을 읽어 packet 으로 조립 → `__parse_data(ext_type=FUNC, ext_data=DATA)` 호출.

**의도**: 펌웨어가 새로 송출하는 0x08 packet 을 받아서 좌/우 휠 속도로 디코드.

**동작**:
1. Background receive thread 가 시리얼에서 9 byte packet 수신
2. HEAD/CHECKSUM 검증
3. `__parse_data(ext_type=0x08, ext_data=[L_lo, L_hi, R_lo, R_hi])` 호출
4. struct.unpack 'h' (signed int16, little-endian) 로 디코드
5. mm/s → m/s 변환 (÷ 1000)
6. `self.__wheel_left_mps`, `self.__wheel_right_mps` 갱신

이제 외부에서 `bot.get_wheel_speeds()` 호출 시 최신 값 반환 가능.

### 4.3 [jupiter_driver_compensated.py](src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py) — ROS2 publisher + 정지 회전 floor 상향

#### 4.3.1 `/jupiter/wheel_speeds` 토픽 publish

추가:
- `Float32MultiArray` import
- `wheel_speeds_pub = create_publisher(Float32MultiArray, '/jupiter/wheel_speeds', 10)`
- `create_timer(0.04, self.publish_wheel_speeds)` — 25Hz timer
- `publish_wheel_speeds()` 메서드: `bot.get_wheel_speeds()` → `Float32MultiArray.data = [L, R]` → publish

**의도**: ROS 생태계 (rviz, plotjuggler, bag, MPPI 등) 가 휠 속도 데이터에 접근 가능하게 함.

**동작**: ROS timer 가 25Hz 마다 `publish_wheel_speeds()` 호출 → Rosmaster_Lib 에서 최신 값 읽어서 ROS topic 으로 publish.

#### 4.3.2 정지 회전 RUNNING floor 상향

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `ROT_RUNNING_MIN_ANGULAR` | 0.12 rad/s | **0.30 rad/s** |

**의도**: bag 분석으로 좌회전 명령 평균 +0.204 rad/s 시 wheel-level diff 가 1.5 tick 으로 deadzone 미달 → 68.8% 정지 확인됨. floor 0.30 으로 상향 시 wheel-level diff = 2.3 tick → 통과 가능.

**부작용**: nav2 가 0.10 rad/s 같은 작은 명령을 내면 driver 가 0.30 으로 boost → encoder 는 0.30 회전 보고 → MPPI 의 trajectory 모델 (0.10 회전 기대) 과 불일치 → 잠재적 진동. 측정 결과 진동 0.16Hz (정상). 단기 적용 OK, 근본 해결은 4.7 캘리브레이션 후 결정.

### 4.4 [nav2_params_fused.yaml](src/jupiter_nav_VO/config/nav2_params_fused.yaml) — DWB → MPPI

| 항목 | 변경 전 (DWB) | 변경 후 (MPPI) |
|---|---|---|
| `controller_frequency` | 10.0 Hz | **20.0 Hz** (MPPI 의 period ≤ model_dt 제약) |
| `FollowPath.primary_controller` | dwb_core::DWBLocalPlanner | nav2_mppi_controller::MPPIController |
| DWB 파라미터 | 30+ 개 (vx_samples, sim_time, BaseObstacle.scale 등) | 모두 제거 |
| MPPI 파라미터 | (없음) | time_steps=48, model_dt=0.05, batch_size=700, vx_std=0.2, wz_std=0.4, vx_max=0.20, vx_min=-0.10, wz_max=1.0, motion_model=DiffDrive |
| Critic | DWB: RotateToGoal, BaseObstacle, PathDist 등 8개 | MPPI: ConstraintCritic, ObstaclesCritic, GoalCritic, GoalAngleCritic, PathAlignCritic, PathFollowCritic, PathAngleCritic, PreferForwardCritic |
| GoalAngleCritic.cost_weight | (DWB RotateToGoal=32) | **8.0** (회전 부족 fix 위해 3 → 8 상향) |
| PathAngleCritic.cost_weight | (DWB Twirling=5) | **5.0** (2 → 5 상향) |
| RotationShim `angular_dist_threshold` | 0.35 (20°) | **0.20 (11°)** — 더 엄격한 path 정렬 후 handover |
| RotationShim `primary_controller` | DWB | MPPI (wrapper 유지) |

**의도**:
- MPPI 가 nonlinear / 비포장 환경에서 DWB 보다 우수 (ROSCon 2023, MDPI 2024)
- 향후 단계 B (야외 농경지) 대비 controller 재구축
- DWB 의 sample-and-score 한계 (좁은 trajectory 탐색 부족) 극복

**주의 사항**:
- MPPI 의 critic 가중치는 DWB 와 직접 매핑 안 됨. 값 재튜닝 필요.
- batch_size 700 + time_steps 48 → 33,600 sim points/cycle × 20Hz = 670K points/sec. Orin Nano CPU 28% peak 사용 (확인됨).
- RotationShim 은 정지→회전 transition 의 명시적 단계 처리용으로 유지 (driver 의 adaptive kick 과 정합).

### 4.5 [nav2_vslam_fused.launch.py](src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py) — 자동 bag 기록

추가:
- `from launch.actions import ExecuteProcess`
- `bag_recorder = TimerAction(period=6.0, actions=[ExecuteProcess(...)])`
- 명령:
  ```
  bash -c 'mkdir -p /home/jetson/bags && rm -rf /home/jetson/bags/latest && exec ros2 bag record -o /home/jetson/bags/latest <topics>'
  ```

**의도**:
- 매 launch 시 동일 경로 (`/home/jetson/bags/latest`) 에 덮어쓰기 → 디스크 누적 방지
- 수동 시작 잊어버림 방지
- 분석 보존 시 사용자 수동 mv 로 백업 (`mv latest keep_NAME_$(date)`)

**동작**:
- launch 시작 + 6초 후 bag_recorder action 발동
- bash 가 기존 dir 삭제 후 ros2 bag record 실행
- `exec` 으로 ros2 bag 가 bash 의 PID 인계 → ctrl-c 시 정상 종료

**기록 토픽 22개** (cmd_vel pipeline + 센서 + costmap + tf 만, 카메라/depth/pointcloud 제외):
```
/plan /local_plan
/cmd_vel /cmd_vel_nav_filtered /cmd_vel_safe /cmd_vel_force /cmd_vel_joy
/jupiter/get_vel /jupiter/imu /jupiter/wheel_speeds   ← 신규
/odom /odometry/global /odometry/local
/tf /tf_static
/scan_merged /scan /scan_back
/goal_pose /stuck_status
/local_costmap/costmap_updates /global_costmap/costmap_updates
/controller_server/transition_event
```

용량 추정: 5분 미션 ~110 MB. 카메라 추가 시 4.5 GB+ → 절대 추가 안함.

### 4.6 [stuck_detector.py](src/jupiter_safety/jupiter_safety/stuck_detector.py) — angular stall 검출

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| 신규 상수 | (없음) | `ANGULAR_STALL_CMD_MIN = 0.10`, `ANGULAR_STALL_ACTUAL_MAX = 0.05` |
| `_tick()` stall 검사 | linear 만 | linear OR angular |

logic:
```python
linear_stall  = (cmd_abs > 0.05) and (actual_abs < 0.02)
angular_stall = (cmd_abs <= 0.05) and (cmd_z_abs > 0.10) and (actual_z_abs < 0.05)
stall_condition = linear_stall or angular_stall
```

**의도**: 회전 명령 시 (linear=0, angular=X) collision_monitor 가 차단해서 발생하는 deadlock 검출. 1.5s 누적되면 stuck_status=true → forced_recovery 가 backup 으로 escape.

**의도된 효과**: lethal zone 에서 회전 goal 줄 때 deadlock 무한 루프 방지.

### 4.7 [wheel_deadzone_calibrator.py](src/jupiter_bringup/jupiter_bringup/wheel_deadzone_calibrator.py) — 신규 캘리브레이션 노드

#### 4.7.1 Phase 1 v2 — Effective deadzone (closed-loop)

set_car_motion 으로 작은 vx, ω sweep:
- Linear: ±[0.005, 0.010, 0.015, 0.020, 0.030, 0.040, 0.050, 0.060, 0.080, 0.100] m/s
- Rotation: ±[0.05, 0.10, 0.15, 0.20, 0.30, 0.40, 0.50] rad/s

각 step:
1. `set_car_motion(target_vx, 0, target_wz)` 발행
2. 1.5s 대기 (PID settling)
3. 마지막 200ms 동안 50Hz sampling — actual_vx, actual_wz, wheel_L, wheel_R
4. 평균값 기록
5. 정지 명령 → 0.3s cooldown

per-wheel ratio = |actual| / |expected|:
- Linear: expected wheel speed = |target_vx| (양 휠 동일)
- Rotation: expected wheel speed = |target_ω| × L/2 = |target_ω| × 0.1225

effective deadzone = ratio ≥ 50% 도달하는 최소 |target|.

#### 4.7.2 Phase 2 — PID step response

target vx 0.20/0.10/0.05, target ω 0.40/0.20/0.10 각 5초 hold:
- rise time (target 의 90% 도달 시간)
- steady-state actual / target ratio
- per-wheel L/R speed

**의도**:
- 모터별 deadzone 정량화 → 펌웨어 Motor_Set_Dead_Zone 적용 데이터 확보
- PID gain 적정성 진단
- M1/M3 비대칭 측정

**필요 조건**:
- 차륜 들어올림 (공중 회전 가능 상태)
- jupiter_driver 종료 (시리얼 포트 점유 해제)

**v1 (open-loop set_motor) 폐기 이유**:
- `motor_data.speed_mm_s[]` 가 PID 루프 안에서만 갱신
- set_motor 는 PID 우회 직접 PWM → speed_mm_s[] 동결
- → wheel_speeds packet 의 값이 stale 또는 0
- v2 (closed-loop set_car_motion) 사용 시 PID 활성으로 정상 측정 가능

## 5. 빌드 및 검증

### 5.1 빌드

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup --symlink-install
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup jupiter_nav_VO --symlink-install
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_safety --symlink-install
```
모두 성공.

펌웨어 (사용자 수동 flash):
- 사용자가 기존 toolchain 으로 빌드 + flash 완료
- Jetson 보드 reboot 후 토픽 활성 확인

### 5.2 MPPI 동작 검증

```bash
ros2 lifecycle get /controller_server   # → active [3]
ros2 topic hz /cmd_vel
```

결과:
- controller lifecycle active
- /cmd_vel rate 20Hz (active goal 시), idle 시 갭 (정상)
- CPU 28.3% (1m 주행 peak)
- "Control loop missed" 경고 간헐 발생 (rviz2 부하 시) → batch_size 1000→700 으로 완화

### 5.3 옵션 A 효과 검증 (RUNNING_MIN 0.30 적용)

bag 분석:

| 지표 | 적용 전 (0.12) | 적용 후 (0.30) |
|---|---|---|
| 좌회전 명령 시 정지 비율 | 68.8% | **8.6%** ✅ |
| 좌회전 follow 비율 | 26.1% | **89.6%** ✅ |
| stuck_status=true | 빈번 | 3.3% |
| Path lateral 평균 | (큼) | 19.7 cm |
| cmd_vel ω 부호 반전 빈도 | (미측정) | 0.16Hz (정상) |

좌회전 못해서 backup 무한 반복 문제 거의 해결. 그러나 path 추종 평균 20cm 이탈 잔존.

### 5.4 캘리브레이션 결과 (정비 전 baseline)

```
M1 forward DZ: 0.005 m/s
M1 reverse DZ: 0.015 m/s
M3 forward DZ: 0.030 m/s   (M1 의 6×)
M3 reverse DZ: 0.100 m/s   (M1 의 6.67× — 매우 비대칭)

M1 CCW DZ: 0.05 rad/s
M3 CCW DZ: 0.40 rad/s   (8×)
M1 CW DZ:  0.05 rad/s
M3 CW DZ:  0.30 rad/s   (6×)

PID rise time @ ω=0.20: 1228 ms (M3 정지마찰 극복 시간)
```

→ M3 mechanical 이슈 정량 확인. 정비 전 baseline 으로 보존.

### 5.5 검증 결과

| 기준 | 실측 |
|---|---|
| /jupiter/wheel_speeds rate | 25Hz ✓ |
| MPPI 동작 | 정상 (active goal 시 20Hz, idle 시 silent) ✓ |
| Control loop missed | rviz2 종료 시 0 ✓ |
| 좌회전 follow 비율 | 89.6% (이전 26.1% → 3.4× 개선) ✓ |
| Path lateral 평균 | 19.7 cm (M3 정비 후 추가 개선 기대) ⚠️ |
| Auto bag 기록 | 동작, 매 launch 덮어쓰기 ✓ |
| 캘리브레이션 노드 | 동작, baseline 측정 완료 ✓ |
| stuck_detector angular stall | 동작 (테스트 필요) ⚠️ |

## 6. 최종 결론

### 6.1 오늘 해결된 항목

- **DWB → MPPI 전환** (controller_period 제약 해결, batch_size 등 튜닝)
- **자동 bag 기록** (매 launch 덮어쓰기, 디스크 누적 방지)
- **펌웨어 wheel_speeds packet 추가** — 25Hz per-wheel 진단 데이터 확보 (M1=L, M3=R)
- **Rosmaster_Lib + jupiter_driver** — 새 packet 파싱 + ROS topic publish
- **stuck_detector 의 angular stall 검출** — pure rotation deadlock 검출 가능
- **driver ROT_RUNNING_MIN 0.30 상향** — 좌회전 정지 68.8% → 8.6% (8× 개선)
- **캘리브레이션 노드 v2** — closed-loop sweep 으로 effective deadzone + PID 응답 정량 측정 가능

### 6.2 오늘 새로 확인된 항목

- **MPPI 의 controller_period ≤ model_dt 강제 제약** — controller_frequency 20Hz 가 model_dt 0.05s 와 정합
- **set_motor (open-loop) vs set_car_motion (closed-loop) 의 데이터 차이** — set_motor 시 motor_data.speed_mm_s[] 가 갱신 안 됨 (펌웨어 한계). 캘리브레이션 시 set_car_motion 사용 강제.
- **M3 (RIGHT) 휠의 mechanical 비대칭** — reverse 방향 deadzone 6.67×, "드드득" 소리 동반. 베어링/기어 정비 필요.
- **PID rise time 1228ms @ ω=0.20** — M3 정지마찰 극복 시간. MPPI 50ms cycle 안에서 못 잡음. control loop 미닫힘의 정량 증거.
- **driver flash 후 Jetson reboot 필요** — 펌웨어 ↔ 호스트 라이브러리 protocol 변경 시 양쪽 모두 재기동 필요.

### 6.3 남은 제약 / 주의

- **M3 mechanical 정비 우선** — 모든 SW 보정보다 우선. 베어링 청소/교체, 기어 윤활 또는 모터 자체 교체.
- **path 추종 lateral 평균 19.7 cm** — M3 정비 후 재측정 필요. 정상 기대치 < 5 cm.
- **MPPI critic 가중치 추가 튜닝 필요** — 현재 GoalAngleCritic 8, PathAngleCritic 5 는 1차 추정. 환경별 미세조정 필요.
- **driver RUNNING_MIN 0.30 의 부작용 잠재** — MPPI 가 작은 ω (0.10) 명령 시 driver 가 0.30 으로 boost → trajectory 모델 ≠ 실거동. 진동 0.16Hz (양호) 라 단기 OK 지만, 캘리브레이션 후 0.12 로 되돌려야 함.
- **rviz2 운영 시 CPU 부하** — 운영 중 rviz2 실행 시 controller_server 의 cycle missing 가능. 분석/디버깅 외엔 닫음 권장.

### 6.4 다음 분석 우선순위

1. **M3 기계 정비** + **calibrator 재실행** → 정비 효과 정량 비교 (baseline yaml 보존됨: `/tmp/deadzone_calib.yaml`)
2. **정비 후 RUNNING_MIN 0.30 → 0.12 복원 시도** → MPPI 모델/실거동 일치 검증
3. **MPPI 추가 튜닝** — 정비 후 path lateral RMS 측정 → critic 가중치 미세조정
4. **stuck_detector angular stall 검증** — lethal zone 에서 회전 goal 시 forced_recovery 발동 확인
5. **펌웨어 deadzone 캘리브레이션 적용** — 정비 후에도 비대칭 잔존 시 `Motor_Set_Dead_Zone()` per-direction 적용
6. **단계 B (야외 농경지) 대비 MPPI 검증** — Velodyne 활성 환경에서 nvblox+LiDAR fusion 으로 360° costmap 확보

## 7. 생성 / 수정 파일 목록

### 7.1 수정된 파일

**펌웨어 (MCU C 코드)**:
- `/home/jetson/mcu/extbd/Source/APP/protocol.h` — `FUNC_REPORT_WHEEL_SPEED (0x08)` 정의 추가
- `/home/jetson/mcu/extbd/Source/APP/app_motion.c` — `Motion_Send_Wheel_Speed()` 함수 추가
- `/home/jetson/mcu/extbd/Source/APP/app_motion.h` — 함수 선언 추가
- `/home/jetson/mcu/extbd/Source/APP/app.c` — `vTask_Auto_Report` 의 count==6 슬롯 추가

**Python**:
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py` — 0x08 파싱 + getter 추가
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` — wheel_speeds publisher + ROT_RUNNING_MIN 0.30
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/setup.py` — calibrator entry point
- `/home/jetson/jupiter_ws_ros2/src/jupiter_safety/jupiter_safety/stuck_detector.py` — angular stall 검출

**ROS2 설정 / launch**:
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` — DWB → MPPI 전환
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` — 자동 bag 기록

### 7.2 신규 파일

- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/wheel_deadzone_calibrator.py` (Phase 1 v2 + Phase 2 캘리브레이션)
- `/tmp/analyze_path_curve.py` (bag 분석 스크립트, 1차)
- `/tmp/analyze_path_followv2.py` (bag 분석 스크립트, 2차 — RUNNING_MIN 적용 후)
- `/tmp/deadzone_calib.yaml` (캘리브레이션 결과, 정비 전 baseline)

### 7.3 생성된 문서

- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_05_06.md`
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_05_06.md` (본 문서)

### 7.4 권장 백업 (사용자 수동)

캘리브레이션 baseline 보존:
```bash
cp /tmp/deadzone_calib.yaml /home/jetson/jupiter_ws_ros2/calib_baseline_pre_M3_repair_2026_05_06.yaml
```

분석 가치 있는 bag 보존 (다음 launch 시 덮어쓰기 방지):
```bash
mv /home/jetson/bags/latest /home/jetson/bags/keep_path_curve_test_2026_05_06_after_RUNMIN_0.30
```
