# 진행과정 2026-03-20: 제자리 회전 헌팅 근본 원인 분석 및 해결

## 1. 문제 보고

### 1.1 사용자 관찰 (Foxglove)
- "회전 이후 전진" (경로 시작 시 RotationShim 회전 → DWB 주행) → **정상**
- "도달 후 회전" (목표 xy 도달 → DWB RotateToGoal 제자리 회전) → **헌팅 발생**
- 로봇이 목표 heading에 도달하지 못하고 좌우로 진동

### 1.2 이전 세션(03-19) 결과
- PID Ki=0.06→0.02, Kd=0.5→0.3 변경이 코드에만 반영, yaml 미수정 → **MCU 미적용**
- 이전 세션에서 `ros2 param set`으로 동적 적용 시도 → jupiter_driver 크래시

### 1.3 PID 직접 적용 시도 결과
- yaml까지 수정하여 Ki=0.02, Kd=0.3 실제 적용 후 → **더 나빠짐**
- 사용자가 PID 변경을 되돌림 (Ki=0.06, Kd=0.5 복원)
- → PID가 근본 원인이 아님을 확인

---

## 2. 분석 접근법

### 2.1 전체 명령 체인 추적
헌팅의 원인을 찾기 위해 전체 체인을 추적:

```
Nav2 controller_server (DWB RotateToGoal, 10Hz)
    → /cmd_vel_nav (angular velocity 명령)
        → velocity_smoother (20Hz, smoothing)
            → /cmd_vel
                → jupiter_driver (ANGULAR_SCALE_FACTOR=0.401)
                    → Rosmaster_Lib.set_car_motion(vx, vy, vz×0.401)
                        → Serial: int(vz×0.401×1000) → MCU (mrad/s)
                            → Differential_Ctrl(): speed_spin = Vz/1000 × 164.555
                                → PID target: L=−speed_spin, R=+speed_spin
                                    → PID_Incre_Calc() → pwm_output
                                        → Motor_Apply_Filter(α=0.3)
                                            → Motor_Ignore_Dead_Zone() (방안A-4)
                                                → ESC PWM → 모터 회전
                                                    → 엔코더 피드백 (1320 pulse/rev, 10ms)
                                                        → /jupiter/get_vel
                                                            → base_node → /odom_raw
                                                                → EKF → /odom
                                                                    → DWB (heading error 계산)
```

### 2.2 각 레이어별 분석 대상
1. **Nav2 DWB RotateToGoal**: 궤적 평가 알고리즘, `min_speed_theta` 영향
2. **velocity_smoother**: 감속 지연, deadband 영향
3. **드라이버**: ANGULAR_SCALE_FACTOR, MCU_MIN_ANGULAR 클램프 영향
4. **MCU PID**: incremental PID 특성, 방향전환 지연
5. **MCU ESC**: 방안A-4 데드존 스킵 경계 문제
6. **엔코더**: 양자화 노이즈

---

## 3. DWB RotateToGoal 소스코드 분석

### 3.1 RotateToGoal 3단계 동작
Nav2 humble 브랜치의 `rotate_to_goal.cpp`를 직접 확인:

```cpp
// Phase 1: xy_goal_tolerance 밖 → score 0 (무시)
if (!in_window_) return 0.0;

// Phase 2: xy_goal_tolerance 안 + 아직 선속도 있음 → 감속 강제
if (!rotating_) {
    if (speed_sq >= current_xy_speed_sq_)
        throw IllegalTrajectoryException("Not slowing down");
    return speed_sq * slowing_factor_ + scoreRotation(traj);
}

// Phase 3: 정지 후 순수 회전 → vx≠0 궤적 거부, 나머지는 scoreRotation
if (fabs(traj.velocity.x) > 0)
    throw IllegalTrajectoryException("Nonrotation command");
return scoreRotation(traj);
```

### 3.2 scoreRotation
```cpp
double scoreRotation(const Trajectory2D& traj) {
    // lookahead_time < 0 → 궤적 마지막 pose 사용
    end_yaw = traj.poses.back().theta;
    return fabs(shortest_angular_distance(end_yaw, goal_yaw_));
}
```

**핵심**: `slowing_factor`는 Phase 2에서만 사용. Phase 3 (정지 후 회전)에서는 **단순히 sim_time 후 잔여 heading error가 점수**. DWB는 이 점수가 최소인 궤적을 선택.

---

## 4. DWB 속도 샘플과 데드존 발견 (핵심)

### 4.1 DWB 각속도 샘플 생성
```
max_vel_theta = 1.0
vtheta_samples = 20
step = 2.0 / 19 = 0.1053 rad/s

전체 샘플: [-1.0, -0.895, -0.789, ..., -0.105, 0.0, +0.105, ..., +1.0]
```

### 4.2 min_speed_theta 필터링
`min_speed_theta=0.30` 적용 시 `0 < |v| < 0.30` 샘플은 **모두 제거**:
```
제거됨: ±0.105, ±0.211 (min_speed_theta 미만)
유효 양수 샘플: [0, 0.368, 0.474, 0.579, 0.684, 0.789, 0.895, 1.0]
                    ↑ 0과 0.368 사이에 선택지가 없음!
```

### 4.3 DWB 궤적 시뮬레이션 (sim_time=1.5s)
`angular=0.368` 궤적의 sim_time 동안 회전량:
```
가속 시간: t_accel = 0.368/0.8 = 0.46s
정속 시간: t_const = 1.5 - 0.46 = 1.04s
총 회전: 0.5 × 0.368 × 0.46 + 0.368 × 1.04 = 0.468 rad = 26.8°
```

### 4.4 교차점 계산 (데드존 경계)
DWB는 RotateToGoal(32), GoalAlign(24), Twirling(5) critic의 합산 점수로 판단:

```
Score(angular=0) = 56 × θ_error       (회전 안 함 → 에러 그대로)
Score(angular=0.368) = 56 × |θ_error − 26.8°| + 5 × 0.368
```

두 점수가 같아지는 교차점:
```
θ_crossover = (56 × 0.468 + 5 × 0.368) / (2 × 56) = 0.250 rad = 14.3°
```

### 4.5 데드존 도출

| heading error | DWB 최적 선택 | 이유 |
|---|---|---|
| > 14.3° | angular=0.368 | 회전하면 에러 감소 |
| **8.6° ~ 14.3°** | **angular=0 (정지!)** | **0.368은 오버슈트 → 에러 오히려 증가** |
| < 8.6° | angular=0 (완료) | yaw_goal_tolerance(0.15=8.6°) 이내 |

**데드존: 8.6° ~ 14.3° (5.7° 폭)**

이 구간에서 DWB는 정지만 출력하지만, yaw_goal_tolerance에 도달하지 못함 → 영원히 대기.
EKF 노이즈(±1~2°)로 error가 14.3° 넘으면 잠시 회전 → 다시 데드존 진입 → 반복 = **헌팅**

---

## 5. 헌팅 메커니즘 상세

```
시간축:
  t=0:   error=30° → DWB 선택: 0.368 rad/s → 매 100ms마다 ~1.7° 감소
  t=0.9: error≈14.3° (교차점) → DWB 선택: 0 (정지!)
  t=1.0: velocity_smoother 감속 중 추가 ~2-3° 회전 → error≈11°
  t=1.1: error 11° (데드존 내부) → DWB는 0만 출력
  t=1.2: yaw_goal_tolerance(8.6°) 미달 → 목표 미도달 판정
  t=1.5: EKF 노이즈로 error 요동 ±1-2°
  t=2.0: error가 14.3° 초과 → DWB가 0.368 잠시 출력
  t=2.1: error 14.3° 이하 → 다시 0 → 반복
  → 교차점(14.3°) 근처에서 채터링 = 헌팅
```

### 5.1 왜 "회전 이후 전진"은 정상인가?
- RotationShim이 경로 시작 시 큰 각도를 0.5 rad/s 고정속도로 처리
- 전진 중 DWB는 `vx>0` + 작은 angular로 헤딩 보정 → `min_speed_theta` 미적용
  (순수 회전이 아니므로 RotateToGoal Phase 3에 진입하지 않음)
- 목표 도달 시 헤딩이 이미 정렬 → 작은 RotateToGoal만 필요

### 5.2 왜 PID 변경(Ki=0.02)이 더 나빠졌는가?
MCU incremental PID에서:
- `Ki`는 비례 역할 (현재 에러에 대한 직접 응답: `Ki × err`)
- `Kd`는 2차 미분 역할 (에러 변화율의 변화 억제)
- Ki 0.06→0.02 = 비례 응답 66% 감소 → MCU 속도 수렴 시간 3x 증가
- **헌팅 근본 원인은 PID가 아니라 DWB 데드존** → PID 변경은 무효
- 오히려 MCU 응답 느려져서 DWB-MCU 불일치 증가 → 더 악화

---

## 6. 해결 방안: min_speed_theta 0.30 → 0.15

### 6.1 원래 0.30으로 올린 이유
- ESC 물리적 데드존(105~111) 때문에 작은 angular 명령이 무시됨
- 0.30 이상이면 MCU PID 출력이 데드존 확실히 초과

### 6.2 방안A-4 이후 0.15 복원 가능한 이유
**방안A-4 ESC 데드존 스킵 펌웨어**가 비례매핑을 적용:
```c
// pulse > 0 → ESC 111~144 (데드존 108~111 건너뜀)
esc_value = 108 + 3 + (int16_t)((float)pulse * 33 / 3600);
```
- 아무리 작은 PID 출력(pulse=1)도 ESC 111로 점프 → 즉시 토크 발생
- 따라서 `min_speed_theta=0.15`도 실제 모터 구동 가능

### 6.3 0.15 복원 시 DWB 샘플 변화
```
유효 양수 샘플: [0, 0.158, 0.263, 0.368, 0.474, 0.579, ...]
                    ↑ 새로 추가된 세밀한 샘플 2개
```

### 6.4 교차점 재계산
최소 비영 샘플 0.158 → sim_time=1.5s 회전: 12.7°
```
θ_crossover = (56 × 0.198 + 5 × 0.158) / (2 × 56) = 0.118 rad = 6.7°
```

| heading error | DWB 최적 선택 |
|---|---|
| > 6.7° | angular ≥ 0.158 |
| < 6.7° | angular = 0 |

**6.7° < 8.6° (yaw_goal_tolerance) → 데드존 완전 제거!**

DWB가 error=8.6° 이상이면 항상 회전 명령을 선택하게 됨.

### 6.5 물리적 체인 검증
```
DWB 0.158 rad/s
  → 드라이버 × 0.401 = 0.063 rad/s
  → MCU_MIN_ANGULAR clamp (0.063 < 0.12) → 0.120 rad/s
  → MCU: speed_spin = 0.120 × 164.555 = 19.7 mm/s
  → 엔코더: 19.7/16.3 = 1.21 ticks/10ms (양자화 통과)
  → Nav2 등가 실효속도: 0.120/0.401 = 0.299 rad/s (1.9x 증폭)
  → DWB 폐루프(10Hz)가 피드백으로 보상
  → 매 DWB 사이클 실제 회전: ~1.71°
  → error 12° → 8.6° 도달: ~2 사이클 (0.2초)
```

---

## 7. 보조 원인 분석

### 7.1 MCU 엔코더 양자화
| 항목 | 값 |
|---|---|
| 분해능 | 1320 pulse/rev, 10ms 주기 |
| 최소 감지 | 1 tick = 16.3 mm/s |
| 최소 angular (Nav2) | 0.12 / 164.555 × 1000 / 0.401 ≈ 0.25 rad/s |
| DWB 최소 (0.15) | 0.158 → MCU 0.120 → 1.21 ticks ✓ |

### 7.2 MCU PID incremental 특성
```c
float PID_Incre_Calc(pid_t *pid, float actual_val) {
    pid->err = pid->target_val - actual_val;
    pid->pwm_output += Kp*(err-err_next) + Ki*err + Kd*(err-2*err_next+err_last);
    clamp(pwm_output, ±3600);  // 유일한 anti-windup
}
```
- Anti-windup이 출력 클램프뿐 → 적분 freeze 없음
- 방향전환 시 누적된 pwm_output 감쇠에 시간 소요
- 하지만 DWB 데드존이 해결되면 이는 보조 요인에 불과

### 7.3 모터 출력 필터
```c
#define MOTOR_FILTER_ALPHA 0.3f
filtered = 0.3 * new_pulse + 0.7 * last_pulse;
```
- 90% 도달: ~77ms (10ms × ln(0.1)/ln(0.7) ≈ 6.5 cycles)
- 방향전환 시 추가 지연이지만 DWB 10Hz (100ms) 대비 수용 가능

### 7.4 방안A-4 ESC 경계
```
pulse=1   → esc = 111 + int(33/3600) = 111 + 0 = 111
pulse=109 → esc = 111 + int(0.999)   = 111 + 0 = 111
pulse=110 → esc = 111 + int(1.008)   = 111 + 1 = 112  ← 첫 111 초과
```
- ESC 111이 물리적 데드존 경계이므로 토크 미약 가능
- PID pulse 1~109 범위에서 ESC 111 고정 = 잔존 데드존
- 그러나 현재 동작 조건에서 PID 비례항(Kp=0.8)이 에러 반응하므로 큰 문제 아님

### 7.5 영향도 종합

| 레이어 | 요인 | 영향도 | 비고 |
|---|---|---|---|
| **Nav2 DWB** | min_speed_theta=0.30 → 데드존 | **★★★ 주원인** | 0.15 복원으로 해결 |
| MCU 엔코더 | 1 tick=16.3mm/s 양자화 | ★★ 보조 | MCU_MIN_ANGULAR이 커버 |
| MCU PID | Ki=0.06 적분 누적 | ★ 경미 | 데드존 해결 시 헌팅 소멸 |
| 모터 필터 | α=0.3 (77ms 지연) | ★ 경미 | DWB 100ms 주기 대비 수용 |
| ESC 경계 | pulse 1~109→ESC 111 | ★ 경미 | PID Kp가 빠르게 탈출 |

---

## 8. 적용된 변경사항

### 8.1 nav2_params_fused.yaml
```yaml
# 변경 전
min_speed_theta: 0.30   # 모터 데드존 초과 보장

# 변경 후 [2026-03-20]
min_speed_theta: 0.15   # 방안A-4 ESC 스킵 후 0.15도 토크 발생. 0.30은 DWB 데드존 생성→헌팅
```

### 8.2 이전 세션(03-20 초) 변경 — 유지
| 파라미터 | 변경 | 상태 |
|---|---|---|
| rotate_to_heading_angular_vel | 0.8→0.5 | 유지 (방안A-4 후 불필요) |
| max_angular_accel | 5.0→3.0 | 유지 (0.5 목표 시 불필요) |
| yaw_goal_tolerance | 0.10→0.15 | 유지 (DWB 교차점 6.7° < 8.6° 확인) |

### 8.3 PID 설정 — 원복
| 파라미터 | 값 | 이유 |
|---|---|---|
| Ki | 0.06 (원본) | PID는 헌팅 주원인 아님. 복원 |
| Kd | 0.5 (원본) | 동상 |

---

## 9. 검증 계획

### 9.1 재시작 방법
```bash
# launch 종료 후
cd ~/jupiter_ws_ros2
source install/setup.bash
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```

### 9.2 확인 사항
1. `ros2 param get /controller_server FollowPath.min_speed_theta` → 0.15 확인
2. Foxglove에서 "도달 후 회전" 목표 전송 → 헌팅 없이 목표 heading 도달 확인
3. test_deadzone_bypass.py 재실행 → Goal2 yaw_span < 30° 확인

---

## 10. 향후 주의사항
- **min_speed_theta 변경 시**: DWB 데드존 재계산 필수 (교차점 < yaw_goal_tolerance 확인)
- **yaml과 코드 기본값**: ROS2 파라미터는 yaml이 코드 기본값을 오버라이드. 반드시 양쪽 동시 수정
- **PID 튜닝 시**: incremental PID에서 Ki는 비례, Kd는 2차 미분 역할. 일반 PID 튜닝 직관과 다름
