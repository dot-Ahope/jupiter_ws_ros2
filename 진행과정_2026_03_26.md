# 진행과정 2026-03-26: nvblox 3D 복셀 토픽 미표시 + Docker↔Host 통신 끊김 분석 및 해결

## 1. 문제 보고

### 1.1 사용자 관찰 (Foxglove)
- **증상 A**: Foxglove에서 nvblox 3D 복셀 시각화 토픽(`/nvblox_node/mesh`, `/nvblox_node/static_esdf_pointcloud` 등)이 보이지 않음
- **증상 B**: Docker↔Host 통신이 자꾸 끊김 (CSV 데이터로 점진적 갭 악화 확인)

### 1.2 제공된 데이터
- Docker 실행 로그: `/home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/exemple_test.txt` (1023행)
- Foxglove CSV 기록: `/home/jetson/jupiter_ws_ros2/plot_data_position_x.csv` (6118행, 4개 토픽, ~207초)

### 1.3 시스템 구성
```
Docker (isaac_ros_dev-aarch64-container):
  - RealSense D455F (depth 640x480@60, infra@60, color@30)
  - RealsenseSplitterNode → cuVSLAM(IR) + nvblox(depth+color)
  - RMW: rmw_fastrtps_cpp
  - --network host

Host (Jetson Orin NX):
  - nav2_vslam_fused.launch.py: EKF + Foxglove bridge + Nav2
  - RMW: rmw_fastrtps_cpp (default)
  - Foxglove bridge: port 8765, send_buffer 10MB, num_threads 2
```

---

## 2. Docker 로그 분석

### 2.1 nvblox 노드 상태 — 정상
Docker 로그(exemple_test.txt)의 nvblox 통계를 10초 주기로 확인:

| 항목 | 속도 | 지연 |
|---|---|---|
| depth_integration | 20-25 Hz | ~0.025s |
| color_integration | 4.8 Hz | — |
| esdf_integration | 9.5-9.6 Hz | ~0.052s |
| tick | 89-101 Hz | — |

- mapping_type: static_tsdf, voxel_size: 0.05, esdf_mode: 2d
- publish_layer_rate_hz: 5.0, publish_debug_vis_rate_hz: 2.0
- **ERROR/FATAL 없음, TF 에러 없음, tracking_lost 없음**

### 2.2 cuVSLAM 상태 — 프레임 간격 경고만
```
"Delta between current and previous frame [~100ms] is above threshold [68ms]"
```
- image_jitter_threshold_ms=68, emitter_on_off 30Hz에서 간헐적 발생
- tracking은 유지됨 (lost 없음)

**결론: Docker 내부에서 nvblox와 cuVSLAM 모두 정상 동작 중**

---

## 3. CSV 데이터 분석 — 통신 끊김 증거

### 3.1 토픽별 데이터 분포
| 토픽 | 데이터 수 | 비고 |
|---|---|---|
| /odom_adapted | 3218 | 정상 |
| /odom | 1913 | 후반 갭 심화 |
| /odom_rf2o_adapted | 646 | 정상 |
| /visual_slam/tracking/odometry_adapted | 340 | **프리징** |

### 3.2 VSLAM 토픽 프리징 발견
```
FROZEN: t=55.0 → t=206.8 (242 samples) value=0.27955493330955505
```
- **t=55초부터 실험 종료(207초)까지 152초간 VSLAM position_x가 동일 값**
- 242개 샘플이 모두 0.27955493330955505 → VSLAM 출력 완전 멈춤
- Docker 로그에서는 cuVSLAM이 정상 동작 → Docker→Host DDS 전달이 끊어진 것

### 3.3 /odom 토픽 점진적 악화
| 시간대 | /odom 갭 |
|---|---|
| 초반 (0~50s) | < 1초 |
| 중반 (100~150s) | 3~5초 |
| 후반 (150~207s) | **9.3초, 10.1초, 9.1초** |

- /odom은 EKF 출력 → EKF 입력(VSLAM, wheel odom)이 끊기면 함께 악화

### 3.4 VSLAM 갭 패턴
```
t=10→14:  3.4초 갭
t=26→32:  5.4초 갭
t=41→51:  9.9초 갭
t=55→80: 25.4초 갭 (프리징 시작)
```
- **점진적 악화**: 갭이 3→5→10→25초로 커짐 → 리소스 소진 패턴

---

## 4. 근본 원인 분석

### 4.1 초기 가설: DDS 대역폭 포화
카메라 이미지(~64 MB/s)가 `--network host`를 통해 Host DDS 참여자에게 노출 → 대역폭 포화 → nvblox 소형 토픽이 Host까지 도달 못함

**검증 결과**: Host 측에 카메라 토픽을 구독하는 노드가 없음. Foxglove whitelist에도 카메라 토픽 없음. 따라서 실제 이미지 데이터가 Host로 전송되지는 않지만, DDS discovery 메타데이터 교환에서 부하 발생 가능.

### 4.2 확정된 핵심 원인: nvblox_msgs 심볼릭 링크 깨짐

`isaac_ws_ros/install/nvblox_msgs/`의 **모든 .msg, .idl, .cmake, 환경설정 파일**이 Docker 빌드 경로 `/workspaces/isaac_ros-dev/...`를 가리키는 심볼릭 링크:

```
Mesh.msg → /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs/msg/Mesh.msg
package.xml → /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs/package.xml
```

호스트에는 `/workspaces/isaac_ros-dev/` 경로가 존재하지 않았음 → **전체 1684개 심볼릭 링크가 깨짐**

이로 인해:
- `ros2 interface show nvblox_msgs/msg/Mesh` → `LookupError: Unknown package 'nvblox_msgs'`
- Foxglove bridge가 `nvblox_msgs/msg/Mesh` 타입을 해석할 수 없어 토픽 구독 실패
- `sensor_msgs/PointCloud2` 기반 nvblox 토픽(esdf_pointcloud)도 같은 패키지 resolving 문제로 영향

### 4.3 통신 끊김의 원인 — 별도 분석 필요
VSLAM 프리징(t=55초 이후)과 /odom 점진적 악화는 nvblox_msgs 심볼릭 링크와 직접 관련 없음.
가능한 원인:
- GPU/CPU 리소스 경합 (cuVSLAM + nvblox + RealSense가 동시 동작)
- DDS FastRTPS의 UDP 전송 버퍼 포화
- RealSense 프레임 드랍 → cuVSLAM 프레임 타이밍 위반

---

## 5. 수정 시도 1: 심볼릭 링크 경로 치환 (실패)

### 5.1 접근법
1684개 깨진 심볼릭 링크의 경로를 일괄 치환:
```
/workspaces/isaac_ros-dev/ → /home/jetson/isaac_ws_ros/
```

### 5.2 결과
- **호스트**: `ros2 interface show nvblox_msgs/msg/Mesh` → 성공
- **Docker**: `PackageNotFoundError: 'nvblox_examples_bringup' not found` → **실패**

### 5.3 실패 원인
Docker 내부에서 `/home/jetson/isaac_ws_ros/` 경로가 존재하지 않음.
Docker 마운트: `/home/jetson/isaac_ws_ros` → `/workspaces/isaac_ros-dev`

install 디렉토리의 심볼릭 링크가 `/home/jetson/isaac_ws_ros/...`를 가리키면:
- 호스트: 해석 가능
- Docker: `/home/jetson/isaac_ws_ros/`가 없으므로 해석 불가

**이 접근법은 Docker와 Host를 동시에 만족시킬 수 없음.**

---

## 6. 수정 시도 2: 호환 심볼릭 링크 (성공)

### 6.1 접근법
심볼릭 링크는 Docker 원래 경로(`/workspaces/isaac_ros-dev/...`)로 유지하되,
호스트에 호환 심볼릭 링크를 생성:

```
/workspaces/isaac_ros-dev → /home/jetson/isaac_ws_ros
```

이렇게 하면 동일한 절대 경로 `/workspaces/isaac_ros-dev/...`가:
- Docker 내부: 실제 경로로 직접 해석
- 호스트: 호환 심볼릭 링크를 통해 `/home/jetson/isaac_ws_ros/...`로 해석

### 6.2 실행
```bash
# 1단계: 수정 시도 1 원복 (Host→Docker 경로)
find /home/jetson/isaac_ws_ros/install -type l -lname '/home/jetson/isaac_ws_ros/*' > /tmp/host_links.txt
while IFS= read -r link; do
  target=$(readlink "$link")
  old_target=$(echo "$target" | sed 's|/home/jetson/isaac_ws_ros/|/workspaces/isaac_ros-dev/|')
  rm "$link"; ln -s "$old_target" "$link"
done < /tmp/host_links.txt
# 1472개 원복 완료

# 2단계: 호환 심볼릭 링크 생성
sudo mkdir -p /workspaces
sudo ln -s /home/jetson/isaac_ws_ros /workspaces/isaac_ros-dev
```

### 6.3 검증 결과

| 검증 항목 | 결과 |
|---|---|
| 호스트 `ros2 interface show nvblox_msgs/msg/Mesh` | 정상 (Mesh 구조 출력) |
| 호스트 `ros2 interface list \| grep nvblox` | 7 msg + 2 srv 모두 인식 |
| Docker `ros2 interface show nvblox_msgs/msg/Mesh` | 정상 |
| Docker `get_package_share_directory('nvblox_examples_bringup')` | 정상 경로 반환 |
| 남은 깨진 심볼릭 링크 | **2개** (Docker 전용 CUDA 바이너리만) |

### 6.4 남은 깨진 심볼릭 링크 (무해)
```
isaac_ros_visual_slam/lib/isaac_ros_visual_slam/cuvslam_api_launcher → /opt/ros/humble/share/isaac_ros_nitros/cuvslam/lib/cuvslam_api_launcher
isaac_ros_visual_slam/lib/libcuvslam.so → /opt/ros/humble/share/isaac_ros_nitros/cuvslam/lib/libcuvslam.so
```
- Docker 전용 CUDA 라이브러리 (`/opt/ros/humble/` 이하, Docker에만 존재)
- nvblox 토픽 시각화와 무관

---

## 7. 적용된 변경사항 요약

### 7.1 호스트 호환 심볼릭 링크 (영구 변경)
```bash
sudo mkdir -p /workspaces
sudo ln -s /home/jetson/isaac_ws_ros /workspaces/isaac_ros-dev
```

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `/workspaces/` 디렉토리 | 존재하지 않음 | 존재 |
| `/workspaces/isaac_ros-dev` | 존재하지 않음 | → `/home/jetson/isaac_ws_ros` 심볼릭 링크 |
| install 내 심볼릭 링크 | Docker 경로 (호스트에서 깨짐) | Docker 경로 유지 (호환 링크로 해석) |
| 깨진 심볼릭 링크 수 | 1684개 | **2개** (CUDA 바이너리) |

### 7.2 영향 분석
| 환경 | 영향 |
|---|---|
| Docker launch | **변화 없음** (심볼릭 링크 경로 그대로) |
| Docker→Host 토픽 구독 | **변화 없음** (DDS 통신 경로 그대로) |
| 카메라 이미지 디버깅 | **변화 없음** (호스트에서 접근 가능) |
| 호스트 nvblox_msgs 타입 해석 | **깨짐→정상** (핵심 수정) |
| Foxglove nvblox 토픽 구독 | **불가→가능** (타입 해석 복구) |

---

## 8. 미해결 사항

### 8.1 통신 끊김 (VSLAM 프리징 + /odom 갭)
CSV 데이터에서 확인된 VSLAM 프리징(t=55초 이후)과 /odom 점진적 갭은 nvblox_msgs 심볼릭 링크 문제와 별개.

**가능한 원인**:
1. GPU 리소스 경합: cuVSLAM + nvblox + RealSense 동시 처리
2. DDS FastRTPS UDP 전송 버퍼 포화 (카메라 DDS discovery 메타데이터)
3. RealSense 프레임 드랍 (`Frames didn't arrived within 5 seconds` 경고)

**다음 단계**:
- Docker 실행 중 `tegrastats` 모니터링으로 GPU/CPU 사용률 확인
- `ros2 topic hz /visual_slam/tracking/odometry` 실시간 모니터링
- DDS 프로파일 최적화 검토

### 8.2 호스트에서 VSLAM/nvblox 직접 실행 불가
호스트에서 launch 실행 시 `libnegotiated.so`, `libisaac_ros_nitros_image_type.so` 누락 에러 발생.
이는 정상 — VSLAM/nvblox 노드는 Docker 전용 CUDA/NITROS 라이브러리가 필요하므로 반드시 Docker 내부에서 실행해야 함.

---

## 9. 향후 주의사항
- **isaac_ws_ros 빌드 시**: Docker 내부(`colcon build --symlink-install`)에서 빌드하므로 install 디렉토리의 심볼릭 링크는 항상 `/workspaces/isaac_ros-dev/...` 경로를 가리킴. 호환 심볼릭 링크 `/workspaces/isaac_ros-dev → /home/jetson/isaac_ws_ros`가 유지되는 한 호스트에서도 해석 가능.
- **호환 심볼릭 링크 삭제 금지**: `/workspaces/isaac_ros-dev` 심볼릭 링크를 삭제하면 호스트에서 nvblox_msgs 해석이 다시 깨짐.
- **재부팅 시**: `/workspaces/` 디렉토리는 `/` 파티션에 있으므로 재부팅 후에도 유지됨.

---

## 10. Foxglove 시각화 느려짐 진단 및 해결 (2026-03-27)

### 10.1 새로운 문제 발생

nvblox_msgs 심볼릭 링크 수정(§6-7)이 성공한 후, Foxglove에서 nvblox 토픽이 정상 표시되기 시작했다.
그러나 **Docker launch(VSLAM+nvblox)를 실행하면 Foxglove 시각화가 극도로 느려지는** 새로운 증상이 발생.

이전에는 nvblox_msgs 타입을 해석하지 못해 Foxglove가 해당 토픽을 아예 구독하지 않았으나,
심볼릭 링크 수정 후 타입 해석이 가능해지면서 **대용량 nvblox 토픽을 전부 WebSocket으로 전달**하게 된 것이 원인.

### 10.2 원인 분석: nvblox 대역폭 과다

Foxglove bridge의 `topic_whitelist`에 포함된 nvblox 토픽 6개의 대역폭을 분석:

| 토픽 패턴 | 매칭 토픽 수 | 대역폭 |
|-----------|-------------|--------|
| `/nvblox_node/mesh` | 1 | 3.5 MB/s |
| `/nvblox_node/.*_layer` | **4** (color_layer, tsdf_layer, color_layer_marker, tsdf_layer_marker) | **16 MB/s** |
| `/nvblox_node/static_esdf_pointcloud` | 1 | 0.1 MB/s |
| `/nvblox_node/esdf_slice_bounds` | 1 | ≈0 |
| `/nvblox_node/combined_esdf_pointcloud` | 0 (static_tsdf 모드 미발행) | 0 |
| `/nvblox_node/human_voxels` | 0 (static_tsdf 모드 미발행) | 0 |
| **합계** | | **~21.7 MB/s** |

**핵심 발견**:
1. `.*_layer` regex가 예상(2개)보다 많은 **4개 토픽**에 매칭 → 16 MB/s
2. `send_buffer_limit: 10MB`를 매 0.5초마다 초과 → WebSocket 버퍼 포화
3. `layer_streamer_bandwidth_limit_mbps: -1.0` (무제한) → DDS 네트워크에도 부담

### 10.3 해결 방안 (2단계)

#### 수정 1: Foxglove whitelist 축소 (Host 측)
**파일**: `jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py`

nvblox 관련 whitelist를 6개 → 2개로 축소:
- ✅ 유지: `static_esdf_pointcloud` (0.1 MB/s) — 2D costmap 시각화용
- ✅ 유지: `esdf_slice_bounds` (≈0) — ESDF 범위 표시
- ❌ 제거: `mesh` (3.5 MB/s), `.*_layer` (16 MB/s), `combined_esdf_pointcloud`, `human_voxels`

**효과**: Foxglove 경유 nvblox 대역폭 **~21.7 MB/s → ~0.1 MB/s** (99.5% 감소)

#### 수정 2: nvblox 퍼블리시 빈도 오버라이드 (Docker 측)
**파일**: `isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py`

nvblox_node에 퍼블리시 매개변수 오버라이드 추가:
```python
'publish_layer_rate_hz': 0.0,       # layer 토픽 발행 완전 중지 (Host 미구독)
'publish_debug_vis_rate_hz': 2.0,    # ESDF pointcloud 발행 유지 (2Hz)
'update_mesh_rate_hz': 5.0,          # mesh 내부 갱신 유지 (costmap용)
```

**효과**: DDS 네트워크 자체에서 layer 토픽 미발행 → Foxglove뿐 아니라 DDS 전체 대역폭 절감.
`update_mesh_rate_hz: 5.0`은 nvblox 내부 메쉬 갱신용이므로 costmap에 영향 없음.

### 10.4 빌드 및 검증

```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO --symlink-install
# → Finished <<< jupiter_nav_VO [5.65s] 성공
```

검증 대기:
- Docker launch 재시작 필요 (`publish_layer_rate_hz: 0.0` 반영)
- Host launch 재시작 후 Foxglove 응답성 확인

### 10.5 문제 발생 경위 정리

```
심볼릭 링크 깨짐 (§2)
  → Foxglove가 nvblox_msgs 타입 해석 실패
  → nvblox 토픽 구독 불가 (대역폭 0)
  → Foxglove 정상 동작 (우연)

심볼릭 링크 수정 (§6-7)
  → Foxglove가 nvblox_msgs 타입 해석 성공
  → nvblox 토픽 6개 패턴 구독 → ~21.7 MB/s
  → WebSocket 포화 → Foxglove 멈춤

whitelist 축소 + 퍼블리시 중지 (§10.3)
  → 경량 토픽 2개만 구독 → ~0.1 MB/s
  → Foxglove 정상 응답 (예상)
```

---

## 11. 미해결 사항 (업데이트)

### 11.1 통신 끊김 (VSLAM 프리징 + /odom 갭)
CSV 데이터에서 확인된 VSLAM 프리징(t=55초 이후)과 /odom 점진적 갭은 nvblox_msgs 심볼릭 링크 문제와 별개.
nvblox 대역폭 최적화(§10.3)로 DDS 부하가 줄어들어 간접적 개선 가능성 있음.

**다음 단계**:
- Docker 재시작 후 VSLAM 프리징 재현 여부 확인
- `tegrastats` 모니터링으로 GPU/CPU 사용률 측정
- `ros2 topic hz /visual_slam/tracking/odometry` 실시간 모니터링

### 11.2 nvblox 3D 메쉬 시각화
현재 Foxglove whitelist에서 제거됨. 향후 3D 메쉬 시각화가 필요한 경우:
- `publish_layer_rate_hz: 0.5` (저빈도 발행)
- Foxglove whitelist에 `/nvblox_node/mesh`만 추가
- 예상 대역폭: 374KB × 0.5Hz ≈ 0.2 MB/s (수용 가능)
