# 진행과정 2026-05-07: 정비 후 baseline + 데모 운영 (조이스틱 / 지도 저장 / localization mode) + Step B (RUNNING_MIN 0.30→0.20) + yaw drift 진단

## 1. 목표

### 1.1 사용자 요청 (시간순)

1. > "기구 문제를 어느정도 해결하고 재실행시킨 결과이다 분석해보라" (정비 후 캘리브 결과)
2. > "옵션 A로 가도록 하자" (보정 코드 model mismatch 수용)
3. > "우선, 데모를 만들기 위해, 지도를 저장 후, 해당 지도에서 주행하도록 만들어보자. 1. 조이스틱으로 조종하고 싶은데, 조이스틱 관련 노드가 실행중인지 확인. 2. 지도를 저장할 방법 확인. 3. 저장된 지도로 실행할 방법 확인."
4. > "권장사항대로 진행하라" (데모 구성)
5. > "조이스틱 속도가 너무 빠르다. 최대 선형속도 0.45, 회전각속도 1.0으로 해라"
6. > "뭔가 합산되는 것 같다." (PublishedDirect: linear=2.450 보고)
7. > "bag를 기록했다. 동작상의 문제점을 파악해라. 뭔가 충돌이 일어나는 모습을 보이는 것 같은데 그렇게 느껴지는 이유에 대해서도 설명하라."
8. > "M3 가 부드럽게 도는중이다. 하드웨어의 문제가 커 보이지는 않는다. M3가 제대로 돌지 않을 때, 수행했던 보정 코드들이 문제를 발생시키는지 확인해봐라. MCU의 코드도 점검해보아라."
9. > "현재 사용중인게 differential이 아니라, mechanum으로 알고 있다.(mechanum을 수정했기에 이름만 메카넘이고, 사실상 코드는 다르다)"
10. > "step b 적용하라"
11. > "실제 보라보는 방향과, 지도와 로버의 x축 정면이, 시작할 때, 로봇 정면이 8~10도가량 기울어져 있다. bag를 새로 만들었다."
12. > "우선 오늘 한 것들을 정리하라"

### 1.2 이전 세션 요약 / 연관 작업

- **2026-05-06** (전일): MPPI controller 도입 + 펌웨어 wheel_speeds packet (FUNC_REPORT_WHEEL_SPEED 0x08) + 캘리브레이션 노드 (wheel_deadzone_calibrator) + M3 mechanical 이슈 정량 진단. M3 정비 전 baseline yaml 저장.
- 본 세션: M3 정비 후 baseline 측정 → 데모 시나리오 구축 → 운영 중 발견된 충돌 사이클 → SW over-correction 진단 → step B 적용 → yaw drift 잔존.

## 2. 대상 시스템 구성

### 2.1 cmd_vel pipeline (변경 없음)

```
nav2 controller (MPPI)
  → velocity_smoother → /cmd_vel
  → collision_monitor → /cmd_vel_nav_filtered
  ↓
twist_mux (joy=100 > force=80 > nav=50)
  ↓
/cmd_vel_safe → jupiter_driver
  ↓ (RUNNING_MIN, MOVING_ROT_MIN floor 적용)
MCU set_car_motion(vx, vy, wz, adjust=False)
  ↓
Mecanum_Ctrl (CAR_MECANUM 모드)
  ↓
PID_Calc_Motor + Motor_Ignore_Dead_Zone (Additive Deadzone +720)
  ↓
Motor PWM
```

### 2.2 시리얼 packet 흐름 (전일 세션의 wheel_speeds 추가 확인)

```
MCU vTask_Auto_Report (25Hz, count==6 슬롯)
  → Motion_Send_Wheel_Speed (FUNC=0x08)
       L = motor_data.speed_mm_s[0] (M1)
       R = motor_data.speed_mm_s[2] (M3)
  → UART
  ↓
Rosmaster_Lib __parse_data
  ↓
bot.get_wheel_speeds() → /jupiter/wheel_speeds (Float32MultiArray, 25Hz)
```

### 2.3 데모 시나리오 (오늘 신규 구성)

```
[Step 1] mapping 모드 launch
  ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py
    (default slam_mode:=mapping)
  + ros2 launch jupiter_ctrl jupiter_joy.launch.py (조이스틱 운영)

[Step 2] 지도 저장
  ros2 service call /slam_toolbox/serialize_map slam_toolbox/srv/SerializePoseGraph
    "{filename: '/home/jetson/maps/jupiter_demo'}"
  → jupiter_demo.posegraph + jupiter_demo.data

[Step 3] localization 모드 launch
  ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py
    slam_mode:=localization slam_map:=/home/jetson/maps/jupiter_demo
  + nav2_fused_navigation.launch.py + jupiter_joy.launch.py
  → rviz "2D Pose Estimate" 로 시작 위치 지정 → goal 발행
```

## 3. 문제 분석

### 3.1 조이스틱 publish topic 잘못 (twist_mux 우회)

**증상**: jupiter_joy 가 launch 되면 nav2 와 충돌 가능 — joy 의 cmd_vel publish 가 nav2 의 명령과 동시 발생.

**원인**:
- `jupiter_joy.py` (OLD): `/cmd_vel` 직접 publish (line 50)
- `jupiter_joy_new.py`: `/cmd_vel_joy` publish (twist_mux priority 100)
- `setup.py` 의 entry_point 에 `jupiter_joy_new` 미등록
- `jupiter_joy.launch.py` 의 executable: `jupiter_joy` (OLD)

→ launch 하면 OLD 노드 실행 → twist_mux 우회.

### 3.2 jupiter_joy_new 의 base_speed 버그

**증상** (사용자 보고):
```
PublishedDirect: linear=2.450, angular=0.000
PublishedDirect: linear=0.000, angular=2.000
```
limit 0.45 / 1.0 인데 출력 2.45 / 2.0.

**원인** ([jupiter_joy_new.py:117-136](src/jupiter_ctrl/jupiter_ctrl/jupiter_joy_new.py)):
```python
base_speed = 2.0  # Linear
linear_val = sign * (base_speed + ratio * limit * Gear)
# stick 임계 (0.2) 만 눌러도 → 2.0 m/s 부터 시작
# stick max (1.0) → 2.0 + 0.45 = 2.45 m/s
```

`linear_speed_limit` 의 의미가 뒤집혀 있음 — limit 은 base 위에 추가되는 양만 제어. base 자체는 hardcoded 2.0.

`publish_direct_cmd_vel` 가 limit 검사 우회 (line 138 코멘트 명시: "제한 없이 직접 cmd_vel 발행").

### 3.3 충돌 사이클 — forced_recovery vs nav2 회전 명령

**증상** (RUNNING_MIN=0.30 운영 중 bag, 270초):
```
forced_recovery backup: 232 회
backup 12 회 시점에 nav2 가 회전 명령 중: 8회 (66.7%)
backup 간 median 6.5s ≈ forced_recovery cooldown(5s) + backup(1s)
```

**증상 시각적**: robot 좌회전 시도 → 멈춤 → 후진 → 정지 → 다시 회전 → 멈춤 → 후진 (사용자 보고 "충돌하는 모습")

**시간 따른 악화**:
```
[60-90s]:    M3 응답 93%, stuck=true 0%
[180-210s]:  M3 응답 27%, stuck=true 0%
[210-240s]:  M3 응답 47%, stuck=true 45%   ← 폭증
[240-270s]:  M3 응답 22%, stuck=true 12%
```

**근본 원인 (사용자 보고 mechanical OK 후 SW 진단)**:
- 차종 = **CAR_MECANUM** (`g_car_type` default, `app_motion.c:34`)
- CAR_MECANUM 모드의 `Motor_Ignore_Dead_Zone` (else 분기) → **`Motor_Set_Dead_Zone(M3, 720, 690)` 작동**
- 작은 PID 출력도 PWM 720+ jump (Additive Deadzone Offset)
- driver `RUNNING_MIN_ANGULAR=0.30` 의 boost 와 결합 → over-correction 누적
- 모터 발열 / 부하 누적 → M3 응답 약화 → angular_stall trigger → forced_recovery → 충돌 사이클

**보조 원인** (검토 후 무관 확인):
- `Motor_Set_Dead_Zone` 자체는 양쪽 wheel 동등 적용. 단 M1 의 실제 deadzone 가 정상 720 근처면 M1 영향 없고, M3 의 실제 deadzone 가 더 낮으면 M3 만 over-shoot
- `Mecanum_Ctrl` 의 balance correction (line 130-153) — 회전 시 `balance_prev_vx=0` 으로 invalidate → 회전에 영향 없음. 직진에만 작용
- `Mecanum_Yaw_Calc` (yaw PID) — driver `set_car_motion(adjust=False)` 라 비활성

### 3.4 yaw drift (localization mode 에서 8-10° 기울어짐)

**증상** (사용자 보고): "실제 보라보는 방향과, 지도와 로버의 x축 정면이, 시작할 때, 로봇 정면이 8~10도가량 기울어져 있다"

**데이터 (Step B 적용 후 bag, 142s)**:
```
시작 (t=3.9s): map→odom yaw = +5.80°  (안정)
[80-100s]:    yaw 5~6°
[100-120s]:   yaw -2.40° ~ +5.00° (range 7.4° 큰 변동)
[120-140s]:   yaw +5.00° ~ +10.20° (사용자 보고와 일치)
```

**근본 원인**:
- localization mode 시작 시 SLAM 의 LiDAR matching error → initial yaw 5.80° offset
- 운영 중 SLAM 이 매칭 재시도하며 yaw 점프 (ICP local minimum)

**충돌 사이클 새 변종 발견**:
- [90-120s] 구간 stuck=true 38% 폭증 (Step B 후에도)
- 이 시점이 yaw 변동 크게 발생한 시점과 일치
- 즉 SLAM 의 yaw 점프 → robot 이 갑자기 path 에서 벗어난 듯 인식 → 큰 회전 보정 명령 → angular_stall

→ Step B 가 driver-side over-correction 은 해결했지만, **localization-side yaw drift** 가 새 forced_recovery 원인.

### 3.5 map_saver QoS mismatch

**증상**:
```
ros2 run nav2_map_server map_saver_cli -f /home/jetson/maps/jupiter_demo2
[ERROR] Failed to spin map subscription
```

**원인**:
- slam_toolbox `/map` publisher: RELIABLE / **TRANSIENT_LOCAL**
- map_saver_cli default: RELIABLE / VOLATILE → incompatible

**해결**:
```bash
ros2 run nav2_map_server map_saver_cli -f ... \
    --ros-args -p map_subscribe_transient_local:=true
```

slam_toolbox 자체 형식 (`.posegraph + .data`) 은 service 로 정상 저장됨.

## 4. 적용한 변경

### 4.1 jupiter_joy entry point + launch executable

#### 4.1.1 [setup.py](src/jupiter_ctrl/setup.py)

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| entry_points | `jupiter_joy = jupiter_ctrl.jupiter_joy:main` | 위 + `jupiter_joy_new = jupiter_ctrl.jupiter_joy_new:main` |

OLD entry 도 보존 (하위 호환). 신규 entry 가 default 로 사용됨 (launch 측에서).

#### 4.1.2 [jupiter_joy.launch.py](src/jupiter_ctrl/launch/jupiter_joy.launch.py)

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| executable | `jupiter_joy` | **`jupiter_joy_new`** |
| linear_speed_limit | 0.6 | 0.25 (사용자 + linter 후속) |
| angular_speed_limit | 3.0 | 1.0 |

### 4.2 jupiter_joy_new.py — base_speed 버그 제거

[jupiter_joy_new.py:110-138](src/jupiter_ctrl/jupiter_ctrl/jupiter_joy_new.py)

| 함수 영역 | 변경 전 | 변경 후 |
|---|---|---|
| `user_jetson` linear | `linear_val = sign * (2.0 + ratio * limit * Gear)` | `linear_val = sign * ratio * limit * Gear` |
| `user_jetson` angular | `angular_val = sign * (1.0 + ratio * limit * Gear)` | `angular_val = sign * ratio * limit * Gear` |

→ stick 0~max 가 0~limit 정상 매핑. base 부풀림 제거.

### 4.3 nav2_vslam_fused.launch.py — slam_mode arg 추가

[nav2_vslam_fused.launch.py](src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py)

신규 LaunchArgument:
- `slam_mode` (default `mapping`) — `mapping` 또는 `localization`
- `slam_map` (default `''`) — 저장 지도 path (확장자 제외)

slam_toolbox_node 의 parameters override:
```python
parameters=[
    slam_params_file,
    {
        'mode': LaunchConfiguration('slam_mode'),
        'map_file_name': LaunchConfiguration('slam_map'),
    },
]
```

### 4.4 nav2_params_fused.yaml — Goal tolerance 강화

[nav2_params_fused.yaml:111-117](src/jupiter_nav_VO/config/nav2_params_fused.yaml#L111)

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `xy_goal_tolerance` | 0.20 m | **0.15 m** |
| `yaw_goal_tolerance` | 0.32 rad (~18°) | **0.25 rad (~14°)** |

검증: 평균 도달 거리 16cm → 11.6cm 개선.

### 4.5 jupiter_driver_compensated.py — Step B (RUNNING_MIN 0.30→0.20)

[jupiter_driver_compensated.py:55-60](src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py#L55)

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `ROT_RUNNING_MIN_ANGULAR` | 0.30 | **0.20** |

이동 회전 floor (`MOVING_ROT_MIN_ANGULAR=0.20`) 와 통일.

검증: forced_recovery 232회 → 4회 (98% 감소). M3 응답 22~50% → 69~88% 회복.

## 5. 빌드 및 검증

### 5.1 빌드

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_ctrl jupiter_nav_VO --symlink-install
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_ctrl --symlink-install
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup --symlink-install
```
모두 성공.

### 5.2 데모 시나리오 검증

#### 5.2.1 매핑 + 지도 저장
- nav2_vslam_fused.launch.py (mapping) + jupiter_joy.launch.py
- 조이스틱 운영 → /slam_toolbox/serialize_map service → 25.7 MB + 7.5 MB 파일 저장 확인

#### 5.2.2 localization mode 운영
- nav2_vslam_fused.launch.py slam_mode:=localization slam_map:=...
- bag 분석:
  - Goal 도달 거리 평균 11.6 cm (이전 16 cm 대비 28% 개선)
  - 좌회전 follow 100%, 우회전 99.4%
  - forced_recovery 232회 → 4회 (Step B 효과)
  - Path RMS 6.7 cm (정비 직후) → 16.7 cm (stale obstacle 영향)

### 5.3 Step B 효과 정량 비교

| 지표 | RUNNING_MIN=0.30 | RUNNING_MIN=0.20 |
|---|---|---|
| forced_recovery 발동 | 232 회 | 4 회 |
| stuck=true 비율 | 10.3% | ~6% |
| 좌회전 명령 시 정지 비율 | 0% | 0% |
| 시간대별 M3 응답 (저점) | 22% | 69% |
| Backup 사이클 median | 6.5s | 6.4s (적은 횟수라 단순 비교 어려움) |

### 5.4 캘리브레이션 baseline (정비 전 vs 후)

| 항목 | 정비 전 | 정비 후 |
|---|---|---|
| M3 forward DZ | 0.030 m/s | 0.020 m/s |
| M3 reverse DZ | **0.100 m/s** | **0.040 m/s** (2.5× 개선) |
| M3 CCW DZ | 0.40 rad/s | 0.15 rad/s |
| M3 CW DZ | 0.30 rad/s | 0.40 rad/s |
| M3/M1 reverse 비율 | 6.67× | 1.33× (정상화) |

### 5.5 yaw drift 측정

| 시간대 | map→odom yaw |
|---|---|
| 시작 (t=3.9~80s) | +5.80° (안정) |
| [100-120s] | -2.40° ~ +5.00° (range 7.4°) |
| [120-140s] | +5.00° ~ +10.20° (사용자 보고 8-10° 일치) |

→ localization mode 시작 시 5.80° offset, 운영 중 ±5° 추가 drift.

## 6. 최종 결론

### 6.1 오늘 해결된 항목

- **조이스틱 launch 정상화** — `/cmd_vel_joy` (twist_mux priority 100) 사용 + base_speed 버그 제거 + 안전 속도 (0.25 m/s, 1.0 rad/s)
- **데모 시나리오 구축** — slam_mode arg 분기 + 지도 저장/로드 흐름 + 검증 명령 정리
- **map_saver QoS 진단** — `map_subscribe_transient_local:=true` 우회법
- **충돌 사이클 정량 진단** — forced_recovery vs nav2 회전의 6.5s 사이클, M3 응답 약화의 시간 누적
- **CAR_MECANUM 모드 보정 코드 검사** — `Motor_Set_Dead_Zone(M3, 720, 690)` 가 실제 작동 (else 분기 Additive Deadzone), driver RUNNING_MIN=0.30 와 결합 시 over-correction
- **Step B 적용 — RUNNING_MIN 0.30 → 0.20** — forced_recovery 98% 감소, M3 약화 회복
- **Goal tolerance 강화** — 0.20→0.15, 평균 도달 거리 16cm → 11.6cm
- **yaw drift 진단** — 시작 5.80° + 운영 중 ±5° drift, [90-120s] 구간 stuck 폭증과 일치 확인

### 6.2 오늘 새로 확인된 항목

- **CAR_MECANUM 의 Motor_Set_Dead_Zone 작동 메커니즘** — `bsp_motor.c:73-85` 의 else 분기 Additive Deadzone Offset (pulse + dz). CAR_DIFFERENTIAL 의 ESC deadzone skip 과 다른 처리.
- **Mecanum_Ctrl 의 balance correction** — 직진 시만 작동 (V_y=V_z=0 분기), 회전 시 invalidate. 회전 약함 무관.
- **Mecanum_Yaw_Calc** — `g_yaw_adjust` 로 활성, driver `adjust=False` 라 비활성 상태.
- **map_saver_cli 의 QoS 요구** — TRANSIENT_LOCAL publisher 와 호환 위해 `map_subscribe_transient_local:=true` 필수.
- **localization mode 의 yaw drift** — initial matching error + 운영 중 SLAM ICP local minimum 점프. 모터/driver 와 무관한 새 충돌 원인.

### 6.3 남은 제약 / 주의

- **yaw drift 미해결** — Step B 후에도 [90-120s] 구간 stuck=38%. localization 의 instability 가 새 충돌 원인. SLAM 매칭 파라미터 튜닝 또는 map quality 향상 필요.
- **Path RMS 회귀** — 6.7 cm (정비 직후) → 16.7 cm (운영 중). stale obstacle + map drift 영향. Manual costmap clear 또는 nvblox 도입으로 해소.
- **Step B 의 RUNNING_MIN=0.20 의 적정성** — wheel-level diff 1.5 tick (분해능 marginal). M3 정비 효과 누적 시 0.15 또는 0.12 까지 추가 하향 시도 가능.
- **펌웨어 Motor_Set_Dead_Zone(M1, M3, 720, 690) 의 적정성** — CAR_MECANUM 에서 Additive +720 가 큰 over-jump. 실제 모터 deadzone 측정 후 재캘리브 필요.
- **데모 운영 가이드 부족** — rviz 의 "2D Pose Estimate" 사용 강조 필요. 사용자가 정확한 시작 pose 지정 안 하면 yaw 5° 자리잡힘.

### 6.4 다음 분석 우선순위

1. **rviz "2D Pose Estimate" 정확화 + 새 bag 측정** — 시작 yaw offset 0° 근처로 가는지 확인
2. **SLAM 매칭 파라미터 튜닝** — `correlation_search_space_*`, `loop_match_minimum_response_*`. 운영 중 yaw 점프 빈도 감소
3. **Map 재매핑** — 사람/동적 obstacle 없는 깨끗한 환경에서 0.10 m/s 저속 + loop closure 명확
4. **Step C — 펌웨어 ESC_DEADZONE_SKIP 또는 Motor_Set_Dead_Zone 값 조정** — Step B 효과 만족스러우면 보류, 잔존 시 시도
5. **단계 B (야외 GPS / 신규 차체)** — 단계 A 검증 충분히 누적 후 진입

## 7. 생성 / 수정 파일 목록

### 7.1 수정된 파일

**ROS 코드 / 설정**:
- `/home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/setup.py` — jupiter_joy_new entry 추가
- `/home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/launch/jupiter_joy.launch.py` — executable 변경 + speed limit
- `/home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/jupiter_ctrl/jupiter_joy_new.py` — base_speed 버그 제거
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` — slam_mode + slam_map arg
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` — goal tolerance 0.20→0.15
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` — RUNNING_MIN 0.30→0.20

### 7.2 신규 생성 (임시 분석)

- `/tmp/analyze_post_repair.py` — 정비 후 첫 bag 검증
- `/tmp/cross_check.py` — 보정 layer 충돌 검사
- `/tmp/analyze_conflict.py` — forced_recovery 충돌 사이클
- `/tmp/analyze_goal_tolerance.py` — goal 도달 거리 (odom-frame)
- `/tmp/check_frames.py` — TF + map drift 측정
- `/tmp/analyze_goal_in_map.py` — goal 도달 거리 (map-frame, 정확)
- `/tmp/analyze_yaw_offset.py` — yaw offset 시계열

### 7.3 신규 저장 (지도)

- `/home/jetson/maps/jupiter_demo.posegraph` (25.7 MB)
- `/home/jetson/maps/jupiter_demo.data` (7.5 MB)

### 7.4 생성된 문서

- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_05_07.md`
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_05_07.md` (본 문서)

### 7.5 권장 백업 (사용자 수동)

```bash
# 정비 후 baseline 보존
cp /tmp/deadzone_calib.yaml /home/jetson/jupiter_ws_ros2/calib_post_repair_2026_05_07.yaml

# Step B 적용 검증 bag 보존 (덮어쓰기 방지)
mv /home/jetson/bags/latest /home/jetson/bags/keep_step_b_2026_05_07_RUNMIN_0.20_yaw_drift
```
