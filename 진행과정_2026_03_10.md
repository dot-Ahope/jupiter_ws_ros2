# 진행과정 2026-03-10

## 1. RF2O `acos` NaN 패치

### 목적
RF2O Laser Odometry에서 `std::acos(acu_trans(0,0))` 호출 시 부동소수점 오차로 인해 인자가 `[-1, 1]` 범위를 벗어나면 NaN이 발생하여 노드 크래시/무한값 출력 가능성 제거.

### 변경 내용
- **파일**: `src/rf2o_laser_odometry/src/CLaserOdometry2D.cpp` (2곳)
  - Line 869–871 (`solveSystemNonLinear` 내)
  - Line 929–931 (`PoseUpdate` 내)
- **수정**: 기존 `> 1.f`만 가드하던 코드를 `std::clamp(acu_trans(0,0), -1.f, 1.f)`로 양방향 클램핑
- **헤더 추가**: `include/rf2o_laser_odometry/CLaserOdometry2D.hpp`에 `#include <algorithm>` 추가

### 이전 코드
```cpp
if (acu_trans(0,0) > 1.f)
    kai_loc_(2) = 0.f;
else
    kai_loc_(2) = fps*std::acos(acu_trans(0,0))*rf2o::sign(acu_trans(1,0));
```

### 수정 코드
```cpp
{
    const float clamped = std::clamp(acu_trans(0,0), -1.f, 1.f);
    kai_loc_(2) = fps*std::acos(clamped)*rf2o::sign(acu_trans(1,0));
}
```

### 빌드
```bash
colcon build --packages-select rf2o_laser_odometry --cmake-args -DCMAKE_BUILD_TYPE=Release
# 44s, 성공
```

---

## 2. VSLAM Adapter Anomaly Lock-out 버그 수정

### 목적
`vslam_covariance_adapter`의 `_check_anomaly()`에서 anomaly 발생 시 `prev_odom`을 업데이트하지 않아 영구적으로 `tracking_ok=False` 상태에 빠지는 버그 수정.

### 원인 분석
- **증상**: Stats에서 `tracking_ok=False`, `anomaly=40693` (29% 게이트), `pub=137080 (71%)`
  - 초반 정상 발행 후, VSLAM 재시작/jitter로 anomaly 1회 발생
  - `prev_odom` 고정 → 이후 매 프레임 delta가 누적 → 영구 anomaly 판정
  - `consecutive_good`이 절대 증가하지 않아 복구 불가
- **근본 원인**: `_check_anomaly()`에서 `if not is_anomaly: self.prev_odom = pose` — anomaly 시 prev_odom 미업데이트

### 변경 내용
- **파일**: `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py`
- **수정**: `self.prev_odom = pose`를 **항상** 실행 (조건 제거)
  - frame-to-frame 비교가 보장되어, anomaly 이후에도 다음 프레임에서 정상 delta로 `consecutive_good` 누적 가능

### 이전 코드
```python
if not is_anomaly:
    self.prev_odom = pose
```

### 수정 코드
```python
# 항상 prev_odom 업데이트 (frame-to-frame 비교 보장)
# anomaly 발생 시에도 업데이트해야 다음 프레임에서 정상 delta로
# consecutive_good을 누적할 수 있음. 미업데이트 시 prev_odom 고정 →
# 누적 delta 증가 → 영구 anomaly lock-out 버그 발생 (2026-03-10 수정)
self.prev_odom = pose
```

---

## 3. LiDAR 데이터 미발행 진단 및 복구

### 증상
- `sllidar_node` alive, `/scan` 토픽 존재, 하지만 **데이터 0Hz**
- RF2O: `Waiting for laser_scans....` 반복
- 시리얼 직접 테스트(`/dev/rplidar`): 응답 없음

### 원인
- 노드는 실행 중이나, LiDAR가 시리얼 통신에 응답하지 않는 상태 (hang)
- `start_motor` 서비스 호출도 무응답

### 복구 조치
```bash
# 1. stuck 노드 강제 종료
sudo fuser /dev/ttyUSB1  # PID 확인
kill -9 <PID>

# 2. sllidar_node 수동 재시작
ros2 run sllidar_ros2 sllidar_node --ros-args \
  -p channel_type:=serial \
  -p serial_port:=/dev/lidar_port \
  -p serial_baudrate:=1000000 \
  -p frame_id:=laser \
  -p inverted:=false \
  -p angle_compensate:=true \
  -p scan_mode:=Standard
```

### 결과
- `/scan`: 10Hz ✅
- `/odom_rf2o`: 10Hz ✅
- `/odom_rf2o_adapted`: 10Hz ✅

---

## 4. 시스템 토픽 모니터링 도구

### DDS Discovery 문제
- VS Code 터미널에서 `ros2 topic hz`가 매번 새 DDS participant를 생성 → discovery timeout으로 NO DATA 오진
- 해결: rclpy 기반 단일 프로세스 모니터링 노드 사용 (모든 토픽 동시 구독, DDS re-discovery 회피)

---

## 5. VSLAM Adapter 수정 후 재시작 검증

### 검증 결과 (호스트 스택 + Nav2 재시작 후)
- rclpy 모니터 노드로 10개 토픽 동시 확인
- **모든 센서 정상**
- VSLAM adapter stats: `tracking_ok=True`, `anomaly=14` (이전 40693), `frozen_pub=305`

| 토픽 | 주파수 | 상태 |
|------|--------|------|
| `/visual_slam/tracking/odometry` | ~63Hz | ✅ |
| `/visual_slam/tracking/odometry_adapted` | **3.6Hz** | ✅ (이전 0Hz) |
| `/scan` | 10Hz | ✅ |
| `/odom_rf2o` | 10Hz | ✅ |
| `/odom_rf2o_adapted` | 10Hz | ✅ |
| `/odom_raw` | 50Hz | ✅ |
| `/odom_adapted` | 50Hz | ✅ |
| `/imu/data_calibrated` | ~91Hz | ✅ |
| `/jupiter/imu` | ~77Hz | ✅ |
| `/odom` (EKF) | 30Hz | ✅ |

---

## 6. Nav2 Goal 테스트 (1차 — 실패)

### 테스트 조건
- 44개 노드 실행 중 (base stack 24 + Nav2 stack ~20)
- Map: 110×139, 5cm 해상도
- 현재 EKF 위치: x=-0.092, y=0.010, yaw=-37.6°

### 테스트 내용
- rclpy ActionClient로 1m 전방 목표 전송
  - 목표: x=0.696, y=-0.604 (map 프레임), yaw=-37.9°
  - 결과: **Goal ACCEPTED → ABORTED** (~28초)

### 관찰
- `dist_remaining=2.572m` 로 고정 (1m 목표인데 2.572m 경로?)
- 로봇 실이동 거리: **0.014m** (거의 움직이지 않음)
- `cmd_vel`: **NO DATA** — Nav2가 속도 명령을 생성하지 않았음

### TF 체인 확인
- map→odom→base_footprint→base_link→laser: 전부 정상
- map→base_link: (-0.333, -0.028, 0.080) — 당시에는 정상으로 보었으나, 이후 발산 확인

---

## 7. Nav2 Abort 근본 원인 분석: RF2O Drift → EKF 발산

### 증상 발견
두 번째 goal 테스트 시도 시 TF lookup 실패 → 상세 조사 시작

### TF 확인 결과
```
map→odom:           (0.047, 0.029, 0.000)
odom→base_footprint: (-2.781, -22.562, 0.000)   ← 비정상!
map→base_link:       (11.774, -19.445, 0.080)    ← 맵 밖!
```
- 로봇이 map 좌표계에서 (11.774, -19.445) — 맵 범위를 완전히 벗어남
- Map 크기: 110×139 cells × 0.05m = 5.5m × 6.95m, origin=(-2.9, -3.6)

### 오도메트리 토픽 비교
| 토픽 | 위치(x, y) | Hz | 분석 |
|------|-----------|-----|------|
| `/odom` (EKF 출력) | (-2.797, -22.550) | 30.8 | RF2O에 지배됨 |
| `/odom_adapted` (wheel) | (-0.235, -0.036) | 47.6 | 정상 (정지 상태) |
| `/odom_rf2o` | **(-2.797, -22.550)** | 10.4 | **22.5m 발산!** |
| `/odom_rf2o_adapted` | (-2.797, -22.550) | 10.4 | RF2O 따라감 |
| `/visual_slam/tracking/odometry_adapted` | (-0.153, 2.760) | 1.0 | 비교적 정상 |
| `/odom_wheel` | NO DATA | — | publisher 없음 |
| `/odometry/filtered` | NO DATA | — | `/odom`으로 remap됨 |

### RF2O Drift 속도 측정 (10초)
- 현재 drift rate: **0.0001 m/s** (실질적 0)
- 22.5m의 drift는 이전에 누적된 것 (활성 발산은 아님)

### 근본 원인
```yaml
# 기존 EKF 설정 (ekf_vslam_fusion.yaml)
odom2: /odom_rf2o_adapted
odom2_differential: false   # ← 문제! 절대 위치 사용
```
- RF2O가 `differential: false` (절대 위치)로 EKF에 입력됨
- RF2O 스캔 매칭 누적 drift (-2.797, -22.550)가 EKF 위치에 직접 반영
- SLAM Toolbox의 map→odom 보정으로도 22.5m 오프셋을 보상 불가능
- 결과: Nav2 planner가 맵 밖 위치에서 경로 계산 → goal abort

### 해결: RF2O EKF Differential 모드 전환
- **파일**: `src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml`
- **변경**: `odom2_differential: false` → `odom2_differential: true`
- RF2O의 frame-to-frame 상대 변위만 EKF에 제공
- 누적 drift는 EKF에 전달되지 않음
- odom frame의 자연 drift는 SLAM Toolbox의 map→odom TF로 보정 (표준 SLAM 접근)

```yaml
# 수정 후
odom2_differential: true    # 2026-03-10: absolute → differential 전환
```

### 빌드
```bash
colcon build --packages-select jupiter_nav_VO --symlink-install
# 5.1s, 성공
```

---

## 8. RF2O Differential 수정 후 재시작 검증

### 검증 결과 (호스트 스택 + Nav2 + VSLAM Docker 전체 재시작 후)

```
=== SENSOR STATUS (8.0s) ===
  /imu/data_calibrated:   82.8 Hz ✅
  /odom (EKF):            22.5 Hz ✅  pos=(-0.002,-0.001) vx=0.000
  /odom_adapted (wheel):  45.2 Hz ✅  pos=(0.000,0.000)
  /odom_raw:              45.2 Hz ✅
  /odom_rf2o:             10.2 Hz ✅  pos=(-0.006,-0.010)
  /odom_rf2o_adapted:     10.2 Hz ✅
  /scan:                  10.1 Hz ✅
  /vslam:                 45.0 Hz ✅  pos=(-0.132,0.040)
  /vslam_adapted:          2.0 Hz ✅

Map: 56×137 @ 0.05m/cell, origin=(0.0, -3.2)
EKF position (map): x=-0.002, y=-0.001, yaw=-2.9°
In map bounds: YES ✅
EKF-RF2O divergence: 0.010m ✅
```

- **EKF 위치가 원점 근처** (-0.002, -0.001) — 이전 22.5m 발산 해소
- RF2O도 원점 근처 (-0.006, -0.010) — differential 모드로 누적 drift 차단
- 모든 센서 정상 발행

---

## 9. Nav2 Goal 테스트 2차 — 성공

### 테스트 조건
- 전체 스택 재시작 완료 (RF2O differential 수정 적용)
- 목표: 현재 heading 방향으로 **0.5m 전방**
- 시작 위치: x=-0.003, y=-0.001, yaw=-9.4°
- 목표 위치: x=0.490, y=-0.084 (map 프레임)

### 결과

| 항목 | 값 |
|------|-----|
| Result | **SUCCEEDED** |
| 소요 시간 | 2.6초 |
| 목표 거리 | 0.5m |
| 실이동 거리 | **0.465m** (93%) |
| cmd_vel 명령 | 50회 |
| 최대 vx | 0.332 m/s |
| 최대 wz | 0.368 rad/s |
| 종료 위치 | (0.461, 0.017), yaw=-7.5° |

### 주행 프로필
1. **가속 단계** (fb#10–#50): vx 0.125→0.332 m/s, wz=0
2. **직진 단계** (fb#50–#130): vx ~0.276 m/s 안정
3. **보정 회전** (fb#145–#195): vx 감소, wz=-0.368 rad/s (경로 보정)
4. **감속/정지** (fb#200–#250): vx 0.092→0.055→0 m/s

### Nav2 로그
```
[controller_server][INFO] Received a goal, begin computing control effort.
[controller_server][INFO] Passing new path to controller.
[controller_server][INFO] Reached the goal!
[bt_navigator][INFO] Goal succeeded
```
경고 1건: "No goal checker was specified" — 기본 `general_goal_checker` 사용, 기능 문제 없음.

---

## 10. 이전 주행 데이터 분석 (plot_data_position_x.csv)

### 데이터 개요
- 파일: `plot_data_position_x.csv`, 19,676행
- 기록 시간: 약 633초 (16~649초), 중간에 425초 gap (녹화 중단)
- 4개 토픽의 position.x 값 기록

| 토픽 | 메시지 수 | 평균 Hz | 값 범위 |
|------|-----------|---------|---------|
| `/odom` (EKF) | 6,227 | 9.8 | [-0.003, 0.255] |
| `/odom_adapted` (wheel) | 10,632 | 16.8 | [0.000, 0.453] |
| `/odom_rf2o_adapted` | 2,066 | 3.3 | [-0.014, 0.462] |
| `/visual_slam/tracking/odometry_adapted` | **751** | **1.2** | [-0.165, 0.207] |

### VSLAM adapted 발행 패턴 분석

**문제**: VSLAM adapted가 평균 1.2Hz로 매우 낮음 (원본 VSLAM은 ~60Hz).

**원인**: `vslam_covariance_adapter`의 **frozen detection** 1Hz 다운샘플링.

```
실제 간격 분포 (총 750 intervals):
  [0.0-0.5s):  549 (73.3%)  ← 정상 발행 (고속 burst)
  [0.5-1.0s):   72 ( 9.6%)
  [1.0-2.0s):  128 (17.1%)  ← frozen downsample (1Hz)
```

- VSLAM이 정지 시 동일 pose를 반복 발행 → `frozen_threshold=10` 후 frozen 판정
- frozen 상태에서는 `frozen_publish_rate=1.0Hz`로 다운샘플링
- **1초 간격 메시지의 98%가 값 변화 없음** (delta < 0.1mm) → 정확히 frozen downsampling
- 정상 burst: 처음 0.42초에 257개 (606Hz), 이후 간쇠적으로 10~38Hz burst

**이것은 정상 동작**. 정지 상태에서 VSLAM이 동일 데이터를 반복 발행하는 것을 감지하여, EKF에 중복 데이터를 과투입하지 않도록 1Hz로 제한한 것임. 문제가 아니라 **설계된 보호 기능**.

### IMU 융합 확인

EKF config (`ekf_vslam_fusion.yaml`)에서:
- **IMU (imu0)**: `/imu/data_calibrated` → `vyaw` (yaw rate)만 사용
- **VSLAM (odom1)**: x, y, vx, vy만 사용 (yaw/vyaw 제거됨)
- VSLAM과 IMU는 **직접 융합되지 않음** — 각각 독립적으로 EKF에 입력
  - IMU: 회전 정보 (vyaw) 담당
  - VSLAM: 병진 위치/속도 (x, y, vx, vy) 담당
  - 역할 분리로 VSLAM 회전 오차(±21°)가 EKF yaw를 오염하지 않음

### 값 차이 분석

**VSLAM vs EKF 일정 오프셋**: 전 구간에서 VSLAM이 EKF보다 약 **-0.11m** 낮음.

| 시점 | VSLAM | EKF | 차이 |
|------|-------|-----|------|
| t=16s (시작) | -0.113 | -0.001 | -0.113m |
| t=31s | -0.117 | -0.001 | -0.116m |
| t=51s | -0.118 | -0.001 | -0.117m |
| t=560s (이동 후) | 0.207 | 0.255 | -0.048m |
| t=640s (정지 후) | 0.195 | 0.254 | -0.060m |

**원인 및 평가**:
- VSLAM 오도메트리의 child_frame은 `camera_link`, 다른 센서들은 `base_footprint`
- VSLAM 자체 원점이 카메라 첫 인식 위치 기준 → 초기 오프셋 발생
- EKF에서 `odom1_differential: true` 설정이므로 **절대값 차이는 EKF에 영향 없음** (frame-to-frame 상대 변위만 사용)
- 이동 후 차이가 -0.11→-0.05m로 줄어듦: 이동 중 VSLAM과 wheel/RF2O의 추정량 차이 반영
- **문제 아님** — differential 모드에서는 절대 오프셋이 EKF에 전달되지 않음

### 주행 구간 (t≈552s) 센서 간 비교

| 시점 | EKF | Wheel | RF2O | VSLAM | VSLAM-EKF |
|------|-----|-------|------|-------|-----------|
| t=550 (직전) | -0.003 | 0.000 | -0.012 | -0.159 | -0.156 |
| t=560 (이동 후) | 0.255 | 0.453 | 0.457 | 0.207 | -0.048 |
| t=640 (정지) | 0.254 | 0.453 | 0.460 | 0.195 | -0.060 |

- Wheel: 0.453m 이동 (가장 큰 값)
- RF2O: 0.457m (wheel과 유사)
- EKF: 0.255m (VSLAM의 낮은 값과 wheel/RF2O의 높은 값을 융합)
- VSLAM: 0.207m (가장 작은 값)
- EKF가 4개 센서의 가중 평균을 적절히 생성한 것으로 **정상 동작**

---

## 11. 실주행 데이터 분석 2차 (Nav2 다중 Goal 주행)

### 데이터 개요
- 파일: `plot_data_position_x.csv` (2차), 23,817행
- 기록 시간: 약 217초, 4개 토픽의 position.x 기록
- Nav2를 통해 **7개 목표 지점** 연속 주행 (foxglove 경유)

| 토픽 | 메시지 수 | 평균 Hz | 값 범위 |
|------|-----------|---------|---------|
| `/odom` (EKF) | 5,939 | 27.4 | [-3.66, 1.65] |
| `/odom_adapted` (wheel) | 9,883 | 45.5 | [-2.50, 2.67] |
| `/odom_rf2o_adapted` | 1,284 | 5.9 | [-1.94, 2.93] |
| `/visual_slam/.../adapted` | **6,711** | **31.5** | **[-50.81, 2.27]** |

### VSLAM 카타스트로픽 실패 발견

**핵심 문제**: VSLAM adapted position.x가 +2.2m에서 **-50.8m으로 폭주**.

**타임라인**:
| 경과시간 | VSLAM x | EKF x | 이벤트 |
|---------|---------|-------|--------|
| t=13 | -0.01 | -0.02 | 정지, 정상 |
| t=47 | +0.06 | +0.01 | 이동 시작 |
| t=53 | **+2.20** | +1.07 | VSLAM 과대추정 (2배) |
| t=68-72 | 1.93→0.79 | 0.93 | VSLAM 급감소 (-1.1 m/s) |
| t=73 | **-8.60** | 0.77 | **점프! Δ=-9.39m** |
| t=81 | -12.63 | 0.84 | 추가 점프 Δ=-4.02m |
| t=89 | -33.50 | 1.39 | 추가 점프 Δ=-18.57m |
| t=158 | **-50.81** | -0.57 | 최저점 도달 |
| t=225 | -48.42 | -0.70 | 끝까지 -48m 유지 |

### 근본 원인: adapter `prev_odom` 무조건 업데이트 버그

이전 세션에서 anomaly lock-out 방지를 위해 `self.prev_odom = pose`를 항상 실행하도록 수정했으나, 이것이 **teleportation bug** 유발:

```
1. VSLAM 0.79 → -8.60 (Δ=9.39m > 0.5m 임계값) → anomaly 감지 ✓ → NOT published
2. prev_odom이 -8.60으로 업데이트됨 (← 버그!)
3. 다음 프레임: VSLAM -8.61 → Δ=0.01m < 0.5m → NOT anomaly → consecutive_good 누적
4. 30프레임 후 (~0.5초): tracking_ok=True → -8.61 발행 시작
5. EKF가 differential로 0.79 → -8.61 = -9.40m delta 수신 → EKF 오염
```

### 수정 내용
- `_check_anomaly()`: `self.prev_odom = pose` → `if not is_anomaly: self.prev_odom = pose`
- anomaly 시 prev_odom을 마지막 정상 위치에 고정 → 나쁜 값이 기준점이 되는 것을 방지
- VSLAM이 정상 위치로 복귀하면 delta가 자연히 작아져 lock-out 해제
- 연속 anomaly 카운터 추가: 300회(~5초) 연속 시 "PERMANENTLY LOST" 경고
- pip editable install 배포 완료

### EKF 오염 평가

| 시점 | EKF | Wheel | EKF-Wheel | 설명 |
|------|-----|-------|-----------|------|
| t=13 | -0.02 | 0.00 | -0.02 | 정상 |
| t=90 | 1.49 | 2.51 | **-1.02** | VSLAM 음방향 drift가 EKF를 끌어내림 |
| t=120 | -2.92 | -1.53 | **-1.39** | 최대 divergence |
| t=130 | -3.43 | -2.50 | -0.93 | EKF 최저점 근처 |
| t=229 | -0.70 | -0.01 | **-0.69** | 종료 시점 |

- EKF 범위: [-3.66, +1.65] (wheel: [-2.50, +2.67])
- VSLAM의 큰 점프 delta가 EKF를 음방향으로 최대 **1.4m** 왜곡
- 그럼에도 Nav2 goal 7개 중 **6개 성공** (wheel+RF2O가 EKF를 보정)

---

## 12. Nav2 Goal 결과 분석

### 7개 Goal 결과 요약

| Goal | 목표 좌표 | 시작 위치 | 결과 | 비고 |
|------|-----------|-----------|------|------|
| 1 | (1.47, 0.09) | (-0.02, 0.00) | **FAIL** → preempt | planner 3회 실패, backup 실패 |
| 2 | (1.68, -2.14) | (1.72, -0.29) | **SUCCESS** | ~9초 |
| 3 | (-0.45, -1.87) | (2.79, -1.90) | **SUCCESS** | ~10초 |
| 4 | (-1.48, -1.71) | (0.84, -1.82) | **SUCCESS** | ~4초 |
| 5 | (-3.76, -1.63) | (-0.24, -1.84) | **SUCCESS** | ~15초 |
| 6 | (1.05, -0.97) | (-2.58, -2.24) | **SUCCESS** | ~19초 |
| 7 | (-0.76, 0.62) | (1.88, -1.45) | **FAIL→SUCCESS** | planner 4회 실패+backup 후 성공 |

**성공률**: 7개 중 **6개 성공** (Goal 1은 Goal 2로 preempt됨)

### Nav2 Planning 실패 분석

**Goal 1 실패** (t≈40-70):
- Planner가 (2.12, 0.15) 경로 계획 실패 (`tolerance 0.15`)
- 시점: costmap이 56×137 → 85×146으로 확장 중
- VSLAM은 아직 정상 단계이며 EKF도 안정 → **costmap 확장 중 경로 무효화**가 원인
- backup behavior 2회 시도 (1회 실패, 1회 성공 → Goal 2로 preempt)

**Goal 7 실패→복구** (t≈159-214):
- Planner가 4개 중간 지점에서 반복 실패: (0.34,0.39), (0.32,0.52), (0.42,0.39), (0.14,0.51)
- 시점: VSLAM이 -50m, EKF는 +0.9m (VSLAM 영향 일부 흡수)
- costmap 254×196 (대형) — 탐사 중 맵이 계속 성장 중
- backup 3회 (2회 성공 + 최종 경로 계획 성공)
- **원인**: costmap 확장 + 탐사 영역 경계의 불완전 장애물 정보
- 최종 55초 후 성공

### BT tick rate 초과 경고
```
[BehaviorTreeEngine]: Behavior Tree tick rate 100.00 was exceeded!
```
- 총 9회 발생
- Planner/controller 처리 지연 시 BT가 100Hz tick을 놓침
- **기능 영향 없음** — BT가 비동기적으로 다음 tick에서 재개

### costmap 계속 성장
```
StaticLayer: Resizing costmap to 56×137 → ... → 254×196 at 0.05m/cell
```
- SLAM Toolbox가 탐사 중 맵을 계속 확장
- 0.05m 해상도에서 254×196 = 12.7m × 9.8m 영역
- **정상 동작**: 탐사 중 costmap 성장은 예상되는 행동
- planner 실패와의 상관: costmap resize 직후 planner 실패 확률 ↑

---

## 13. Foxglove Bridge 경고 (무해)

### 경고 메시지
```
[foxglove_bridge] [WARN] Failed to add service "/visual_slam/set_slam_pose"
(isaac_ros_visual_slam_interfaces/srv/SetSlamPose):
package 'isaac_ros_visual_slam_interfaces' not found
```

### 원인
- `isaac_ros_visual_slam_interfaces` 패키지는 **Docker 컨테이너 내부**에만 설치
- foxglove_bridge는 **호스트**에서 실행 → 호스트의 `AMENT_PREFIX_PATH`에서 해당 패키지 미발견
- foxglove_bridge가 모든 ROS 2 service를 introspect하면서, Docker 내부 service의 인터페이스 정의를 찾지 못해 경고 발생

### 영향
- **기능 영향 없음** — VSLAM은 Docker 내에서 정상 실행되며, DDS 토픽으로 호스트와 통신
- foxglove에서 해당 service를 직접 호출할 수 없을 뿐 (일반적으로 사용하지 않음)
- 토픽 발행/구독은 정상 (interface definition 불필요)

### 권장 조치
- **무시** (기능에 영향 없으므로 수정 불필요)
- 원하면 foxglove_bridge 설정에서 해당 service를 제외 목록에 추가 가능

---

## 현재 상태

### 완료
- [x] RF2O acos NaN 패치 (빌드 완료)
- [x] VSLAM adapter anomaly lock-out 수정 → **재수정 (teleportation bug 발견)**
- [x] LiDAR 복구 (manual sllidar_node)
- [x] 전체 센서 정상 확인 (10개 토픽)
- [x] Nav2 goal 테스트 1차 (abort — RF2O drift 원인 파악)
- [x] RF2O EKF differential 모드 전환 (빌드 완료)
- [x] Nav2 goal 테스트 2차 (성공 — 0.5m 전방)
- [x] 이전 주행 데이터(CSV 1차) 분석 완료
- [x] 실주행 데이터(CSV 2차, 7-goal) 분석 + VSLAM 폭주 발견
- [x] VSLAM adapter `prev_odom` 재수정 배포 (conditional update 복원)
- [x] Nav2 planning 실패/foxglove 경고 원인 분석 완료

### 발견된 문제 요약
1. **VSLAM adapter teleportation bug (치명)** — anomaly 시 prev_odom 갱신이 나쁜 값을 기준으로 전파 → **수정 완료**
2. **cuVSLAM 자체 tracking 실패** — position.x가 +2m→-50m 폭주 → adapter 수정으로 EKF 보호, 근본 원인은 cuVSLAM
3. **Nav2 planner 간헐 실패** — costmap 확장 중 경로 계획 실패 → BT recovery로 자동 복구
4. **Foxglove bridge 경고** — Docker 내 패키지 미발견 → 무해, 무시 권장

---

## 다음 단계 계획

### 즉시 수행 (Priority 1)
1. **스택 재시작 + VSLAM adapter 수정 검증**
   - 호스트 스택 + Nav2 + Docker VSLAM 전체 재시작
   - adapter stats 확인: anomaly 시 prev_odom 고정, tracking_ok=False 동안 gating 확인
   - 센서 전체 정상 확인 (10개 토픽)

2. **VSLAM gating 동작 검증 주행**
   - Nav2 goal 전송 (이전과 같은 경로: 1.5m 전방)
   - VSLAM 폭주 재현 여부 관찰
   - adapter stats에서 consecutive_anomaly 증가 + gating 확인
   - EKF position 안정성 확인 (wheel/RF2O 대비 divergence < 0.5m)

### 단기 개선 (Priority 2)
3. **VSLAM anomaly 임계값 조정 검토**
   - 현재 `max_pos_delta=0.5m` — 이번 데이터에서 0.1~0.3m 점진 drift는 통과
   - `max_pos_delta=0.3m`으로 하향 검토 (정상 이동 속도 고려 필요: 0.3m/s @ 60Hz = 0.005m/frame → 충분한 마진)
   - 또는 sliding-window 기반 cross-sensor velocity check 도입 검토

4. **cuVSLAM tracking 실패 근본 원인 조사**
   - Docker 내 cuVSLAM 로그 확인: `tracking_quality`, `vo_state` 토픽
   - 카메라 프레임 드롭, 노출 설정, 특징점 밀도 확인
   - cuVSLAM이 실패하는 조건 정리: 급회전? 조명 변화? 벽 근접?

5. **Nav2 planner tolerance/costmap 튜닝**
   - `planner_server` tolerance: 0.15m → 0.25m 상향 검토 (좁은 통로 대비)
   - `global_costmap` inflation_radius / cost_scaling_factor 확인
   - planner loop rate 5Hz 유지 여부 확인 (costmap resize 영향)

### 중기 안정화 (Priority 3)
6. **VSLAM-wheel cross-check 추가 (adapter 개선)**
   - adapter에 `/odom_adapted` (wheel) 구독 추가
   - VSLAM 속도 vs wheel 속도 비교 → 10초 이상 divergence 시 VSLAM degraded 판정
   - 점진적 drift (0.1-0.3m/frame) 감지 가능

7. **VSLAM 재시작 메커니즘**
   - adapter에서 "PERMANENTLY LOST" 판정 시 cuVSLAM 노드 재시작 트리거
   - Docker 내 cuVSLAM에 service call 또는 lifecycle 관리
   - 재시작 후 초기 30 프레임 동안 recovery_cov_multiplier 적용

8. **Nav2 장거리 주행 테스트**
   - 맵 전체 영역 왕복 (4-5m)
   - 10+ goal 연속 주행 안정성 확인
   - EKF drift, VSLAM 상태, planner 성공률 종합 평가
