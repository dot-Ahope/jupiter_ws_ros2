# 진단 명령어 2026-03-09

> Foxglove "no data" 문제 진단 → DDS SHM UID 불일치 발견 → Frozen 다운샘플링 수정

---

## Phase 1: 초기 토픽 상태 확인

### 명령 1: 관련 토픽 목록 확인
```bash
ros2 topic list 2>/dev/null | grep -E "adapted|visual_slam|odom"
```
- **목적**: adapted / visual_slam / odom 관련 토픽이 존재하는지 확인
- **결과**:
  ```
  /odom
  /odom_adapted
  /odom_rf2o
  /odom_rf2o_adapted
  /visual_slam/tracking/odometry_adapted
  ```
  - `/visual_slam/tracking/odometry` (raw)가 목록에 없음 → Docker 노드의 토픽이 호스트에서 미발견 가능성

### 명령 2: adapted 토픽 상세 정보 확인
```bash
ros2 topic info /visual_slam/tracking/odometry_adapted -v 2>/dev/null
```
- **목적**: publisher/subscriber QoS, 노드 이름 확인
- **결과**:
  - Type: `nav_msgs/msg/Odometry`
  - Publisher: `/vslam_covariance_adapter` (RELIABLE)
  - Subscriber: `/ekf_filter_node` (BEST_EFFORT)
  - 구조 정상 — 메시지 타입이나 QoS 문제 아님

### 명령 3: adapted 토픽 주파수 확인
```bash
timeout 3 ros2 topic hz /visual_slam/tracking/odometry_adapted 2>/dev/null
```
- **목적**: adapted 토픽에 실제 데이터가 흐르는지 확인
- **결과**: **NO DATA** (타임아웃, 데이터 없음)
- **판정**: adapter가 메시지를 발행하지 않고 있음

### 명령 4: raw VSLAM 토픽 존재 확인
```bash
ros2 topic list 2>/dev/null | grep "visual_slam/tracking/odometry$"
```
- **목적**: Docker에서 발행하는 raw odometry 토픽이 호스트에서 보이는지 확인
- **결과**: `/visual_slam/tracking/odometry` 존재

### 명령 5: raw VSLAM 토픽 주파수 확인
```bash
timeout 3 ros2 topic hz /visual_slam/tracking/odometry 2>/dev/null
```
- **목적**: cuVSLAM이 실제 데이터를 발행하는지 확인
- **결과**: **NO DATA** — raw 자체가 0Hz → 문제는 adapter가 아닌 VSLAM 자체

---

## Phase 2: Docker VSLAM 상태 진단

### 명령 6: Docker 컨테이너 상태 확인
```bash
docker ps --format "{{.Names}} {{.Status}}" 2>/dev/null | grep isaac
```
- **목적**: Isaac ROS Docker 컨테이너 가동 여부
- **결과**: `isaac_ros_dev-aarch64-container Up 12 minutes` — 컨테이너 살아있음

### 명령 7: Docker 내 ROS2 노드 목록
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 node list 2>/dev/null'
```
- **목적**: Docker 내부 노드 생존 확인
- **결과**: `/visual_slam_node`, `/camera/camera` 포함 전체 노드 ALIVE
- **판정**: 노드는 살아있으나 데이터 미발행

### 명령 8: Docker 내 VSLAM 데이터 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 3 ros2 topic hz /visual_slam/tracking/odometry 2>/dev/null'
```
- **목적**: Docker 내부에서 cuVSLAM odometry 발행 여부 직접 확인
- **결과**: **NO DATA** — cuVSLAM 자체가 데이터 미발행

### 명령 9: Docker 내 카메라 스트림 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 3 ros2 topic hz /camera/infra1/image_rect_raw 2>/dev/null'
```
- **목적**: RealSense IR 스트림 확인
- **결과**: **NO DATA** — 카메라 스트림 중단 (노드 alive, 데이터 0)

### 명령 10: Docker 로그 확인
```bash
docker logs --tail 30 isaac_ros_dev-aarch64-container 2>&1 | tail -30
```
- **목적**: cuVSLAM/카메라 에러 메시지 확인
- **결과**:
  - `RealSense Node Is Up!` OK
  - cuVSLAM 초기화 OK (v12.6, GPU warmup)
  - jitter 경고 1건: `Delta between current and previous frame [350.704896 ms] is above threshold [34.000000 ms]`
  - 이후 로그 없음 → 스트리밍 파이프라인 hang
- **판정**: 카메라 시작 직후 스트리밍 hang → cuVSLAM 데이터 없음

---

## Phase 3: VSLAM 프로세스 복구 시도

### 명령 11: Docker 내 VSLAM 프로세스 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'ps aux | grep -E "ros2|launch|realsense|visual_slam" | grep -v grep'
```
- **목적**: PID 확인 및 CPU 상태 파악
- **결과**:
  - `component_container` (cuVSLAM): **CPU 94.6%** (데이터 없이 spin)
  - `realsense2_camera_node`: CPU 21.8%
  - launch 프로세스 (PID 11273) 포함 전체 VSLAM 스택 alive

### 명령 12: VSLAM 프로세스 정상 종료 (SIGINT)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'kill -2 11273 11285 11287 2>/dev/null; sleep 3;
   ps aux | grep -E "component_container|realsense2_camera_node|launch.*visual_slam" | grep -v grep'
```
- **목적**: SIGINT로 정상 종료 시도
- **결과**: launch, realsense 노드만 남음 (component_container 종료됨) → 추가 SIGKILL 필요

### 명령 13: VSLAM 프로세스 강제 종료 (SIGKILL)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'kill -9 11273 11285 11287 2>/dev/null; sleep 2;
   ps aux | grep -E "component_container|realsense2_camera_node|launch.*visual_slam" | grep -v grep || echo "ALL_CLEAN"'
```
- **목적**: 잔존 프로세스 강제 종료
- **결과**: `ALL_CLEAN` — 전체 프로세스 종료 확인

### 명령 14: SHM 파일 정리
```bash
sudo rm -f /dev/shm/fastrtps_*
```
- **목적**: kill -9로 인한 stale DDS SHM 잠금 파일 정리
- **결과**: 0 remaining

### 명령 15: RealSense USB 리셋
```bash
sudo python3 /home/jetson/reset_realsense.py
```
- **목적**: 스트리밍 hang 상태의 카메라 디바이스 리셋
- **결과**: `Successfully reset /dev/bus/usb/002/003`

### 명령 16: VSLAM 재시작 (Docker background)
```bash
docker exec -d isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py'
```
- **목적**: 60fps 설정으로 VSLAM 재시작
- **결과**: 정상 시작 (12초 대기 후 확인)

### 명령 17: Docker 내 VSLAM 데이터 재확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 5 ros2 topic hz /visual_slam/tracking/odometry 2>/dev/null'
```
- **목적**: cuVSLAM 재시작 후 데이터 발행 확인
- **결과**: **average rate: 59.78 Hz** ✅ — Docker 내부 정상

### 명령 18: 호스트에서 raw VSLAM 수신 확인
```bash
timeout 5 ros2 topic hz /visual_slam/tracking/odometry 2>/dev/null
```
- **목적**: Docker→Host DDS 데이터 전달 확인
- **결과**: **NO DATA** ❌ — Docker 내부 60Hz인데 호스트 수신 불가

---

## Phase 4: DDS 통신 장애 진단

### 명령 19: 호스트 DDS 노드 발견 확인
```bash
ros2 node list 2>/dev/null | grep -E "visual_slam_node|camera/camera"
```
- **목적**: Docker VSLAM 노드가 호스트 DDS discovery에 보이는지 확인
- **결과**: `/base_to_camera_tf`만 보임 — Docker 시작 노드 미발견
- **판정**: DDS discovery 실패

### 명령 20: RMW 및 DOMAIN_ID 일치 확인
```bash
echo "HOST_RMW: $RMW_IMPLEMENTATION" && echo "HOST_DOMAIN: $ROS_DOMAIN_ID"
docker exec isaac_ros_dev-aarch64-container bash -c \
  'echo "DOCKER_RMW: $RMW_IMPLEMENTATION" && echo "DOCKER_DOMAIN: $ROS_DOMAIN_ID"'
```
- **목적**: RMW 미들웨어와 도메인 ID 일치 확인
- **결과**:
  - HOST: `rmw_fastrtps_cpp`, DOMAIN_ID=0
  - DOCKER: `rmw_fastrtps_cpp`, DOMAIN_ID=0
  - 설정 일치 — RMW/도메인 문제 아님

### 명령 21: ros2 daemon 재시작
```bash
ros2 daemon stop && sleep 2 && ros2 daemon start
```
- **목적**: DDS discovery 캐시 강제 초기화
- **결과**: 재시작 후에도 Docker 노드 미발견

### 명령 22: SHM 파일 소유자 확인
```bash
ls -la /dev/shm/fastrtps_* 2>/dev/null | head -20
```
- **목적**: SHM 파일 소유자/권한 확인
- **결과**:
  ```
  -rw-r--r-- 1 root   root   549408 /dev/shm/fastrtps_0e842b55...   ← Docker (root)
  -rw-r--r-- 1 root   root        0 /dev/shm/fastrtps_0e842b55..._el
  -rw-r--r-- 1 jetson jetson 549408 /dev/shm/fastrtps_04cc40e8... ← Host (jetson)
  ```
  - **root 소유 (Docker) + jetson 소유 (호스트) 혼재** → SHM 권한 충돌 발견!

### 명령 23: Docker 컨테이너 네트워크/IPC 모드 확인
```bash
docker inspect isaac_ros_dev-aarch64-container \
  --format '{{.HostConfig.NetworkMode}} {{.HostConfig.IpcMode}} {{.HostConfig.PidMode}}'
```
- **목적**: Docker가 호스트와 네트워크/IPC/PID 공유하는지 확인
- **결과**: `host host host` — 전부 호스트 공유 모드

### 명령 24: Docker UID 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c 'id admin; id root'
id jetson
```
- **목적**: Docker admin 사용자와 호스트 jetson 사용자의 UID 매핑 확인
- **결과**:
  - Docker `admin`: UID=1000
  - Docker `root`: UID=0
  - Host `jetson`: UID=1000
  - `docker exec -d` 기본 = root (UID=0) → **호스트 jetson(UID=1000)과 불일치!**

### 명령 25: raw VSLAM topic info 상세
```bash
ros2 topic info /visual_slam/tracking/odometry -v 2>/dev/null
```
- **목적**: Docker publisher의 DDS 메타데이터 확인
- **결과**:
  ```
  Publisher count: 1
  Node name: _NODE_NAME_UNKNOWN_
  Node namespace: _NODE_NAMESPACE_UNKNOWN_
  ```
  - **DDS 데이터 전송 계층 파괴 확인** — discovery OK (토픽 보임), data transport FAIL

---

## Phase 5: DDS SHM UID 불일치 수정

### 명령 26: root 소유 VSLAM 프로세스 종료
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'kill -2 <PID> 2>/dev/null; sleep 5; kill -9 <PID> 2>/dev/null'
```
- **목적**: root로 실행 중인 VSLAM 프로세스 정리
- **결과**: ALL_CLEAN

### 명령 27: SHM 전체 정리 + USB 복구
```bash
sudo rm -f /dev/shm/fastrtps_*
# USB sysfs unbind/rebind
for dev in /sys/bus/usb/devices/*/idProduct; do
  if [[ "$(cat $dev 2>/dev/null)" == "0b5c" ]]; then
    USB_DEV=$(basename $(dirname $dev))
    echo "$USB_DEV" | sudo tee /sys/bus/usb/drivers/usb/unbind
    sleep 2
    echo "$USB_DEV" | sudo tee /sys/bus/usb/drivers/usb/bind
    break
  fi
done
```
- **목적**: SHM 정리 + 카메라 USB 복구
- **결과**: SHM 정리 완료, 카메라 `Bus 002 Device 012` 재열거

### 명령 28: VSLAM 재시작 (`--user admin`)
```bash
docker exec -d --user admin isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py'
```
- **목적**: admin(UID=1000)으로 VSLAM 시작 → SHM 파일 소유자 호스트와 일치
- **결과**: 시작 성공

### 명령 29: 프로세스 UID 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'ps aux | grep -E "component_container|realsense2_camera|launch.*visual_slam" | grep -v grep'
```
- **목적**: 실행 사용자가 admin인지 확인
- **결과**: 전부 `admin` 소유 ✅ (이전: `root`)

### 명령 30: 호스트에서 VSLAM 수신 확인
```bash
timeout 5 ros2 topic hz /visual_slam/tracking/odometry 2>/dev/null
```
- **목적**: --user admin 적용 후 호스트 수신 확인
- **결과**: **average rate: 59.92 Hz** ✅ — 호스트에서 정상 수신!

### 명령 31: SHM 파일 소유자 재확인
```bash
ls -la /dev/shm/fastrtps_* 2>/dev/null | grep root | wc -l
```
- **목적**: root 소유 SHM 파일이 없는지 확인
- **결과**: **0** — 전부 jetson/admin (UID 1000) 소유 ✅

---

## Phase 6: "failed to set power state" 카메라 복구

### 명령 32: Docker 로그에서 카메라 에러 확인
```bash
docker logs isaac_ros_dev-aarch64-container 2>&1 | \
  grep -E "RealSense Node Is Up|failed to set|Error|NOT found" | tail -10
```
- **목적**: 카메라 초기화 성공/실패 확인
- **결과**: `failed to set power state` (반복 6초 간격)

### 명령 33: USB 버스 deauthorize/reauthorize
```bash
echo "0" | sudo tee /sys/bus/usb/devices/usb2/authorized
sleep 3
echo "1" | sudo tee /sys/bus/usb/devices/usb2/authorized
sleep 5
lsusb | grep -i intel
```
- **목적**: USB 3.0 호스트 컨트롤러 레벨에서 전체 리셋 → 카메라 펌웨어 상태 초기화
- **결과**: `Bus 002 Device 012: Intel(R) RealSense(TM) Depth Camera 455f` — 정상 재열거

### 명령 34: 커널 USB 로그 확인
```bash
sudo dmesg | tail -10 | grep -iE "usb|realsense"
```
- **목적**: 커널 레벨 USB 재열거 상태 확인
- **결과**: `Found UVC 1.50 device Intel(R) RealSense(TM) Depth Camera 455f` — 정상 (에러 없음)

### 명령 35: VSLAM foreground 테스트 시작
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 25 ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py 2>&1' | \
  grep -E "RealSense Node Is Up|failed to set|tracker.*success|Delta.*threshold"
```
- **목적**: foreground에서 카메라 open 성공 여부 직접 확인
- **결과**:
  ```
  Open profile: Infra(1), Y8, 640, 360, FPS: 60
  Open profile: Infra(2), Y8, 640, 360, FPS: 60
  RealSense Node Is Up!
  cuVSLAM tracker was successfully initialized.
  Delta between current and previous frame [57.825536 ms] is above threshold [34.000000 ms]
  ```
  - 카메라 정상 open ✅, cuVSLAM 초기화 ✅, jitter 1건(첫 프레임, 정상)

---

## Phase 7: Frozen 다운샘플링 수정 및 검증

### 명령 36: 호스트 스택 재시작
```bash
# 기존 호스트 스택 종료
kill -2 $(pgrep -f "nav2_vslam_fused.launch.py") 2>/dev/null
sleep 8

# SHM 정리 + daemon 재시작
sudo rm -f /dev/shm/fastrtps_*
ros2 daemon stop

# Docker VSLAM 시작 (--user admin)
docker exec -d --user admin isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py'

# VSLAM 60Hz 확인 후 호스트 스택 시작
sleep 15
cd /home/jetson/jupiter_ws_ros2 && source install/setup.bash
ros2 launch src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
```
- **목적**: 수정된 vslam_covariance_adapter.py 로드를 위한 전체 재시작
- **결과**: 24개 노드 정상 가동

### 명령 37: adapted 토픽 주파수 확인
```bash
timeout 5 ros2 topic hz /visual_slam/tracking/odometry_adapted 2>/dev/null
```
- **목적**: frozen 다운샘플링 적용 후 adapted 토픽 발행 확인
- **결과**: **average rate: 0.998 Hz** ✅ — 1Hz 다운샘플 정상 동작

### 명령 38: raw VSLAM 주파수 확인
```bash
timeout 5 ros2 topic hz /visual_slam/tracking/odometry 2>/dev/null
```
- **목적**: raw VSLAM 60Hz 안정성 확인
- **결과**: **average rate: 59.96 Hz** ✅

### 명령 39: 전체 노드 목록 확인
```bash
ros2 node list 2>/dev/null
```
- **목적**: Docker 노드 포함 전체 시스템 정상 동작 확인
- **결과**: 24개 노드 (Docker: `visual_slam_node`, `camera/camera`, `imu_tf_publisher` 포함) ✅

### 명령 40: adapted 토픽 메시지 데이터 확인
```bash
timeout 10 ros2 topic echo /visual_slam/tracking/odometry_adapted --once --no-arr 2>/dev/null
```
- **목적**: Foxglove에서 표시할 `pose.pose.position.x` 필드 존재 및 값 확인
- **결과**: 정상 Odometry 메시지 수신, `pose.pose.position.x` 값 존재 ✅

---

## 명령어 요약 표

| # | 명령 | 목적 | 결과 |
|---|------|------|------|
| 1 | `ros2 topic list \| grep adapted\|visual_slam\|odom` | 관련 토픽 존재 확인 | 5개 토픽 확인 |
| 2 | `ros2 topic info adapted -v` | QoS/노드 정보 확인 | RELIABLE pub, BEST_EFFORT sub |
| 3 | `ros2 topic hz adapted` | adapted 주파수 | **0Hz (NO DATA)** |
| 4 | `ros2 topic list \| grep odometry$` | raw VSLAM 토픽 존재 | 존재 |
| 5 | `ros2 topic hz odometry` | raw VSLAM 주파수 | **0Hz (NO DATA)** |
| 6 | `docker ps` | 컨테이너 상태 | Up 12분 |
| 7 | `docker exec ros2 node list` | Docker 내부 노드 | 전부 ALIVE |
| 8 | `docker exec ros2 topic hz odometry` | Docker 내 VSLAM | **0Hz** |
| 9 | `docker exec ros2 topic hz image_rect_raw` | Docker 내 카메라 | **0Hz** |
| 10 | `docker logs --tail 30` | Docker 로그 | jitter 1건, 스트리밍 hang |
| 11 | `docker exec ps aux` | 프로세스 PID/CPU | component_container 94.6% CPU |
| 12 | `docker exec kill -2` | SIGINT 정상 종료 | 부분 종료 |
| 13 | `docker exec kill -9` | SIGKILL 강제 종료 | ALL_CLEAN |
| 14 | `sudo rm -f /dev/shm/fastrtps_*` | SHM stale 정리 | 0 remaining |
| 15 | `sudo python3 reset_realsense.py` | USB ioctl 리셋 | 성공 |
| 16 | `docker exec -d ... ros2 launch` | VSLAM 재시작 | 시작됨 |
| 17 | `docker exec ros2 topic hz odometry` | Docker 내 재확인 | **59.78 Hz ✅** |
| 18 | `ros2 topic hz odometry` (호스트) | 호스트 수신 확인 | **0Hz ❌** |
| 19 | `ros2 node list \| grep visual_slam` | Docker 노드 발견 | 미발견 |
| 20 | `echo $RMW_IMPLEMENTATION / $ROS_DOMAIN_ID` | RMW/도메인 일치 | 양쪽 일치 |
| 21 | `ros2 daemon stop && start` | DDS 캐시 초기화 | 효과 없음 |
| 22 | `ls -la /dev/shm/fastrtps_*` | SHM 소유자 확인 | **root+jetson 혼재** |
| 23 | `docker inspect NetworkMode/IpcMode` | Docker 네트워크 모드 | host/host/host |
| 24 | `id admin / id jetson` | UID 매핑 | admin=1000, jetson=1000, root=0 |
| 25 | `ros2 topic info odometry -v` | DDS 메타데이터 | **_NODE_NAME_UNKNOWN_** |
| 26 | `docker exec kill -2/-9` | root VSLAM 정리 | ALL_CLEAN |
| 27 | `rm SHM + sysfs unbind/bind` | SHM+USB 정리 | Device 012 재열거 |
| 28 | `docker exec -d --user admin ... launch` | **admin으로 VSLAM 시작** | 시작 성공 |
| 29 | `docker exec ps aux` | 실행 사용자 확인 | **admin ✅** |
| 30 | `ros2 topic hz odometry` (호스트) | 호스트 수신 재확인 | **59.92 Hz ✅** |
| 31 | `ls -la /dev/shm/fastrtps_* \| grep root` | root SHM 확인 | **0개 ✅** |
| 32 | `docker logs \| grep "failed to set"` | 카메라 에러 확인 | failed to set power state |
| 33 | `echo 0/1 > usb2/authorized` | **USB 버스 deauthorize** | 정상 재열거 |
| 34 | `dmesg \| grep usb` | 커널 USB 로그 | UVC device found ✅ |
| 35 | foreground VSLAM launch (timeout 25) | 카메라 open 테스트 | **RealSense Node Is Up ✅** |
| 36 | 전체 스택 재시작 | 수정 코드 적용 | 24개 노드 가동 |
| 37 | `ros2 topic hz adapted` | adapted 주파수 확인 | **0.998 Hz ✅** |
| 38 | `ros2 topic hz odometry` | raw VSLAM 주파수 | **59.96 Hz ✅** |
| 39 | `ros2 node list` | 전체 노드 확인 | 24개 ✅ |
| 40 | `ros2 topic echo adapted --once` | 메시지 데이터 확인 | position.x 값 존재 ✅ |

---

## 핵심 진단 흐름

```
Foxglove "no data"
  ↓ [명령 3] adapted 0Hz 확인
  ↓ [명령 5] raw VSLAM도 0Hz → 문제는 원본
  ↓ [명령 8-9] Docker 내부도 0Hz → 카메라 hang
  ↓ [명령 13-16] 프로세스 정리 + USB 리셋 + 재시작
  ↓ [명령 17] Docker 내부 60Hz OK
  ↓ [명령 18] 호스트 0Hz → DDS 통신 장애
  ↓ [명령 22] SHM 파일: root + jetson 혼재
  ↓ [명령 24] docker exec 기본 = root (UID 0)
  ↓ [명령 25] topic info: _NODE_NAME_UNKNOWN_ (데이터 전송 실패 확인)
  ↓ [명령 28] --user admin으로 재시작
  ↓ [명령 30] 호스트 60Hz OK ✅

  ↓ 그러나 adapted 여전히 0Hz (명령 37 전)
  ↓ adapter 통계: 99% frozen gated
  ↓ 코드 수정: frozen 완전차단 → 1Hz 다운샘플
  ↓ [명령 37] adapted 1Hz OK ✅
```
