# 진행과정 — 2026-02-27 EKF Yaw Overshoot 진단/수정 & Nav2 Backup 문제 해결

**관련 TIL**: `TIL_2026_02_27_EKF_Yaw_Overshoot_Diagnosis.md`  
**이전 작업**: `진행과정_2026_02_25.md`, `TIL_2026_02_25_IMU_Inversion_Nav2_Config.md`

---

## 0. 시작 상태

이전 세션(02-25~26)에서 완료된 상태:
- ✅ IMU gyro Z 반전 수정 (imu_invert_z 적용 누락 버그)
- ✅ MCU encoder vx 반전 수정
- ✅ RF2O yaw를 EKF에서 제거 (제자리 회전 시 ~50° 발산)
- ✅ Nav2 파라미터 튜닝 완료
- ✅ Foxglove 최적화 (topic whitelist)
- ⚠️ VSLAM vo_state 게이팅 시도 → Docker symlink 깨짐으로 실패
- 🔲 EKF yaw overshoot 미해결 (이전 CSV에서 yaw offset +15.7° 확인)

---

## 1. VSLAM 이상치 게이팅 구현 (02-27 초반, 이전 세션 연속)

### 1.1. 문제: VSLAM vo_state 접근 불가

VisualSlamStatus.py가 Docker 내부 symlink → 호스트에서 import 실패.

### 1.2. 해결: odom 기반 이상치 검출로 재설계

`vslam_covariance_adapter.py`를 완전 재작성:

```bash
# 파일 확인
cat ~/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py | wc -l
# ~430줄
```

주요 로직:
- 연속 VSLAM odom 메시지 추적
- 이상치 판정: `|Δyaw| > 0.15 rad/step` 또는 `|Δposition| > 0.5m/step` → 차단
- 복구: 30개 연속 정상 메시지 + 1초 대기 + 5배 공분산 감쇠
- vo_state 구독은 선택적 보조 수단 (Docker 내부에서만 유효)

```bash
# 빌드
cd ~/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO --symlink-install
```

### 1.3. Foxglove에 VSLAM odom 추가

```python
# nav2_vslam_fused.launch.py의 topic_whitelist에 추가
'/visual_slam/tracking/odometry_adapted',  # VSLAM odom (디버깅용)
```

---

## 2. CSV 분석 — EKF Yaw Overshoot 패턴 파악

### 2.1. CSV 구조 확인

```bash
head -5 ~/jupiter_ws_ros2/plot_data.csv
wc -l ~/jupiter_ws_ros2/plot_data.csv
```

```python
# Python으로 토픽별 샘플 수 확인
python3 -c "
import csv
from collections import Counter
topics = []
with open('plot_data.csv') as f:
    r = csv.DictReader(f)
    for row in r:
        topics.append(row['topic'])
c = Counter(topics)
for t, n in c.most_common():
    print(f'{n:8d}  {t}')
"
```

결과 (이전 세션 CSV — 4 토픽):
```
   13214  /visual_slam/tracking/odometry_adapted.pose.pose.orientation.z
    9017  /odom_adapted.pose.pose.orientation.z
    5033  /odom.pose.pose.orientation.z
    1795  /odom_rf2o_adapted.pose.pose.orientation.z
```

### 2.2. 4-소스 Yaw 비교 테이블

```python
python3 << 'EOF'
import csv, math

data = {}
with open('plot_data.csv') as f:
    for row in csv.DictReader(f):
        t = float(row['elapsed time'])
        topic = row['topic']
        val = float(row['value'])
        if topic not in data:
            data[topic] = []
        data[topic].append((t, val))

for k in data:
    data[k].sort()

def get_yaw_at(topic, target_t):
    pts = data.get(topic, [])
    if not pts:
        return None
    lo, hi = 0, len(pts)-1
    while lo < hi:
        mid = (lo+hi)//2
        if pts[mid][0] < target_t:
            lo = mid+1
        else:
            hi = mid
    qz = pts[lo][1]
    qz_clamped = max(-1.0, min(1.0, qz))
    return math.degrees(2.0 * math.asin(qz_clamped))

topics_list = [
    '/odom_adapted.pose.pose.orientation.z',
    '/odom.pose.pose.orientation.z',
    '/odom_rf2o_adapted.pose.pose.orientation.z',
    '/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z',
]
init = {tp: get_yaw_at(tp, 10.0) or 0.0 for tp in topics_list}

for t in range(10, 200, 10):
    vals = {}
    for tp in topics_list:
        v = get_yaw_at(tp, float(t))
        vals[tp] = (v - init[tp]) if v else None
    # ... (출력)
EOF
```

결과에서 핵심 발견:
- t=80-81s: EKF vyaw = 0.574 rad/s, Wheel vyaw = 0.083 rad/s → **6.9배 차이**
- VSLAM, RF2O, Wheel은 모두 정상 범위
- EKF만 비정상적으로 overshoot

### 2.3. 각속도 비율 계산

```python
python3 -c "
# t=80-81s 구간에서 각 센서별 평균 angular velocity 계산
# (CSV에서 연속 2초 구간의 yaw delta → angular velocity 추정)
# 결과:
# Wheel:  avg vyaw = 0.083 rad/s (4.77°/s), total Δyaw = -4.77°
# VSLAM:  avg vyaw = 0.038 rad/s (2.19°/s), total Δyaw = -4.84°
# EKF:    avg vyaw = 0.574 rad/s (32.9°/s), total Δyaw = -14.09°
# EKF/Wheel ratio = 6.9x
"
```

---

## 3. 가설 1: IMU Gyro Scale 오류 조사

### 3.1. Rosmaster_Lib.py ICM20948 gyro_ratio 확인

```bash
grep -n "gyro_ratio" ~/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py
```

```
171: gyro_ratio = 1/1000.0    # ICM20948
148: gyro_ratio = 1/3754.9    # MPU9250 — (32768/500)×(π/180)
```

ICM20948은 `1/1000.0` → MCU가 mrad/s를 보내면 정상, raw INT16이면 `1/3754.9`여야 함.  
비율: `1000/3754.9 = 0.266` → 약 3.75배 overshoot 가능성.

### 3.2. Yahboom 원본과 diff 비교

```bash
diff ~/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py \
     ~/yahboomcar_ros2_ws/yahboomcar_ws/src/yahboomcar_bringup/yahboomcar_bringup/Rosmaster_Lib.py | head -60
```

차이점:
- 원본: `gy = gy * -gyro_ratio` (Y축, Z축 부호 반전)
- Jupiter 복사본: `gy = gy * gyro_ratio` (부호 반전 제거됨)
- `accel_ratio`: 원본=`1/1000`, Jupiter=`1/10000`

### 3.3. imu_calib 확인 — 스케일 보정 없음

```bash
grep -n "gyro\|angular" ~/jupiter_ws_ros2/src/imu_calib/src/apply_calib.cpp
```

```cpp
// apply_calib.cpp — applyCorrectionGyro():
corrected.angular_velocity.x -= gyro_bias_.x();  // 바이어스만 제거
corrected.angular_velocity.y -= gyro_bias_.y();
corrected.angular_velocity.z -= gyro_bias_.z();
// → 스케일 보정 없음!
```

```bash
cat ~/jupiter_ws_ros2/imu_calib.yaml
```

```yaml
gyro_bias:
  x: -0.00183
  y: 0.00264
  z: -0.00810
# → 바이어스만 저장, scale factor 없음
```

### 3.4. 직접 IMU 읽기 시도 (실패)

```bash
# 시도 1: 직접 시리얼 접근 — 드라이버가 점유중
python3 -c "from Rosmaster_Lib import Rosmaster; bot = Rosmaster(car_type=1); ..."
# → 시리얼 포트 busy (드라이버 실행 중)

# 시도 2: ROS 토픽 — 시스템 미실행
ros2 topic echo /jupiter/imu --once
# → 토픽 없음 (시스템 꺼진 상태)
```

### 3.5. EKF process noise 확인

```bash
grep -A 20 "process_noise" ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml
```

핵심 발견:
```yaml
# vyaw(11,11) = 0.0001  ← 극도로 낮음!
# IMU gyro_cov = 0.001
```

→ EKF가 IMU angular_velocity.z를 거의 절대적으로 신뢰 (process noise / measurement noise 비율이 극단적).

---

## 4. IMU Gyro Scale 실측 검증

### 4.1. 진단 스크립트 작성

```bash
# 진단 스크립트 생성 (200줄)
cat ~/jupiter_ws_ros2/scripts/diagnose_imu_gyro_scale.py
chmod +x ~/jupiter_ws_ros2/scripts/diagnose_imu_gyro_scale.py
```

스크립트 로직:
- `/jupiter/imu` (raw), `/imu/data_calibrated` (bias 제거), `/odom_adapted` (wheel) 구독
- 각 소스의 angular_velocity.z를 시간 적분
- 회전 후 `∫IMU_gz / ∫Wheel_vyaw` 비율 계산

### 4.2. imu_gyro_scale 파라미터 추가

```bash
# jupiter_driver_compensated.py에 파라미터 추가
# declare_parameter('imu_gyro_scale', 1.0)
# gx *= gyro_scale; gy *= gyro_scale; gz *= gyro_scale
```

### 4.3. Foxglove에 IMU 토픽 추가

```bash
# nav2_vslam_fused.launch.py topic_whitelist에 추가:
# '/imu/data_calibrated'
# '/jupiter/imu'
```

### 4.4. EKF process noise vyaw 증가

```bash
# ekf_vslam_fusion.yaml에서 vyaw(11,11):
# 0.0001 → 0.01 (100배 증가)
```

### 4.5. 빌드 및 실행

```bash
cd ~/jupiter_ws_ros2
colcon build --packages-select jupiter_bringup jupiter_nav_VO --symlink-install
```

### 4.6. 진단 실행 및 결과

```bash
# 터미널 1: 시스템 실행
ros2 launch ~/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py

# 터미널 2: 진단 스크립트 실행
source /opt/ros/humble/setup.bash && source ~/jupiter_ws_ros2/install/setup.bash
python3 ~/jupiter_ws_ros2/scripts/diagnose_imu_gyro_scale.py
```

로봇을 제자리에서 여러 바퀴 회전 (338초간):

```
================================================================================
IMU Gyro Scale Diagnostic - SUMMARY
================================================================================

Samples collected:
  IMU raw:        33826
  IMU calibrated: 33826
  Wheel odom:     16930

Integrated yaw (total rotation):
  IMU raw:         -356.53°
  IMU calibrated:  -361.03°
  Wheel encoder:   -360.44°

Scale Ratio:
  IMU_raw / Wheel   = 0.9892
  IMU_calib / Wheel = 1.0017

✓ IMU gyro scale looks CORRECT (ratio close to 1.0)

--- Peak Rotation Period Analysis ---
  Peak 1s window:
  IMU avg |gz|:   2.0444 rad/s (117.13°/s)
  Wheel avg |vyaw|: 2.0466 rad/s (117.26°/s)
  Peak ratio: 0.9989
================================================================================
```

**결론: IMU gyro scale 정상** (ratio = 1.0017). `gyro_ratio = 1/1000.0`은 올바른 값.

---

## 5. 새 CSV 분석 — 진짜 원인 발견

### 5.1. 새 CSV 구조 확인 (VSLAM yaw 아직 EKF에 활성 상태)

```bash
head -5 ~/jupiter_ws_ros2/plot_data.csv
wc -l ~/jupiter_ws_ros2/plot_data.csv  # 79650
```

```python
python3 -c "
import csv
from collections import Counter
topics = []
with open('plot_data.csv') as f:
    for row in r:
        topics.append(row['topic'])
c = Counter(topics)
for t, n in c.most_common():
    print(f'{n:8d}  {t}')
"
```

결과 (6 토픽):
```
   23352  /imu/data_calibrated.orientation.z
   18969  /visual_slam/tracking/odometry_adapted.pose.pose.orientation.z
   16161  /odom_adapted.pose.pose.orientation.z
    9703  /odom.pose.pose.orientation.z
    8914  /jupiter/imu.orientation.z
    2550  /odom_rf2o_adapted.pose.pose.orientation.z
```

시간 범위: 8.2 - 331.4s (323.3s)

### 5.2. 6-소스 Yaw 비교 테이블 (10초 간격)

```python
python3 << 'EOF'
import csv, math

data = {}
with open('plot_data.csv') as f:
    for row in csv.DictReader(f):
        t = float(row['elapsed time'])
        topic = row['topic']
        val = float(row['value'])
        if topic not in data:
            data[topic] = []
        data[topic].append((t, val))

for k in data:
    data[k].sort()

def get_yaw_at(topic, target_t):
    pts = data.get(topic, [])
    if not pts:
        return None
    lo, hi = 0, len(pts)-1
    while lo < hi:
        mid = (lo+hi)//2
        if pts[mid][0] < target_t:
            lo = mid+1
        else:
            hi = mid
    qz = pts[lo][1]
    qz_clamped = max(-1.0, min(1.0, qz))
    return math.degrees(2.0 * math.asin(qz_clamped))

topics_list = [
    '/odom_adapted.pose.pose.orientation.z',
    '/odom.pose.pose.orientation.z',
    '/odom_rf2o_adapted.pose.pose.orientation.z',
    '/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z',
    '/imu/data_calibrated.orientation.z',
]
init = {tp: get_yaw_at(tp, 10.0) or 0.0 for tp in topics_list}

print(f"{'Time':>6s} | {'Wheel':>8s} | {'EKF':>8s} | {'RF2O':>8s} | {'VSLAM':>8s} | {'IMU_cal':>8s} | {'EKF-Wheel':>10s}")
for t in range(10, 340, 10):
    vals = {}
    for tp in topics_list:
        v = get_yaw_at(tp, float(t))
        vals[tp] = (v - init[tp]) if v else None
    w = vals.get('/odom_adapted.pose.pose.orientation.z')
    e = vals.get('/odom.pose.pose.orientation.z')
    ew = f"{e-w:+.1f}" if (e is not None and w is not None) else "N/A"
    # ... print
EOF
```

핵심 결과:
```
 Time |   Wheel |     EKF | EKF-Wheel
  120 |    -3.4 |    -1.2 |      +2.3
  190 |    +2.3 |    +8.4 |      +6.1  ← 360° 회전 후 6° offset
  220 |    -2.1 |   +15.6 |     +17.7  ← 2차 회전 peak
  330 |    +1.7 |   +14.5 |     +12.8  ← 영구 고착
```

IMU orientation은 항상 0° (orientation 미제공이므로 정상).

### 5.3. VSLAM/RF2O 데이터 연속성 확인

```python
python3 << 'EOF'
# VSLAM 동결/갭 확인
vslam = data['/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z']
# 결과:
#   VSLAM: ends at t=224.5s (gap at t=213.3-217.0s, 3.7s)
#   RF2O: frozen at t=60.3-62.7 (2.4s)
EOF
```

### 5.4. VSLAM-Wheel Yaw Delta 비교 (핵심 발견)

```python
python3 << 'EOF'
import csv, math

data = {}
with open('plot_data.csv') as f:
    for row in csv.DictReader(f):
        t = float(row['elapsed time'])
        topic = row['topic']
        val = float(row['value'])
        if topic not in data:
            data[topic] = []
        data[topic].append((t, val))
for k in data:
    data[k].sort()

vslam = data['/visual_slam/tracking/odometry_adapted.pose.pose.orientation.z']
wheel = data['/odom_adapted.pose.pose.orientation.z']

def get_closest(pts, target_t):
    lo, hi = 0, len(pts)-1
    while lo < hi:
        mid = (lo+hi)//2
        if pts[mid][0] < target_t:
            lo = mid+1
        else:
            hi = mid
    return pts[lo][1], pts[lo][0]

for t in range(120, 225):
    tf = float(t)
    w1, _ = get_closest(wheel, tf)
    w2, _ = get_closest(wheel, tf + 1.0)
    v1, _ = get_closest(vslam, tf)
    v2, _ = get_closest(vslam, tf + 1.0)
    wd = w2 - w1
    vd = v2 - v1
    diff = vd - wd
    if abs(diff) > 0.005 and abs(diff) < 1.0:
        deg_err = math.degrees(2.0 * math.asin(max(-1, min(1, diff))))
        print(f"{t:6d} | Wh:{wd:+.6f} | VS:{vd:+.6f} | err:{deg_err:+.1f}°")
EOF
```

핵심 결과:
```
   165 | Wh:-0.051898 | VS:+0.059331 | err:+12.8°   ← 180° 회전 중
   183 | Wh:+0.055499 | VS:-0.067566 | err:-14.1°   ← 360° 회전 마무리
   210 | Wh:+0.195987 | VS:+0.111081 | err: -9.7°   ← 2차 회전
   215 | Wh:-0.185133 | VS:+0.000000 | err:+21.3°   ← VSLAM 동결!
```

### 5.5. EKF-Wheel Offset 점프 분석

```python
python3 << 'EOF'
# 매초 EKF-Wheel offset delta 계산
# |delta| > 0.005인 순간만 출력
# 핵심 이벤트:
#   t=170: +180° 점프 (quaternion wrapping)
#   t=178: -180° 복귀
#   t=186: +6.3° (360° 회전 후 VSLAM 복귀)
#   t=210: +10.6° (2차 회전)
#   t=220: +17.7° (피크)
EOF
```

---

## 6. 수정 적용: VSLAM yaw/vyaw 제거

### 6.1. EKF 설정 수정

```bash
# ekf_vslam_fusion.yaml의 odom1_config 수정
# 변경 전:
#   odom1_config: [true, true, false,   # x, y
#                  false, false, true,   # yaw ← 활성
#                  true, true, false,    # vx, vy
#                  false, false, true,   # vyaw ← 활성
#                  false, false, false]
# 변경 후:
#   odom1_config: [true, true, false,   # x, y
#                  false, false, false,  # yaw ← 제거
#                  true, true, false,    # vx, vy
#                  false, false, false,  # vyaw ← 제거
#                  false, false, false]
```

### 6.2. 빌드

```bash
cd ~/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO --symlink-install
```

---

## 7. 수정 파일 요약

| 파일 | 변경 | 이유 |
|------|------|------|
| `config/ekf_vslam_fusion.yaml` | odom1 yaw/vyaw 제거 | VSLAM 회전 중 ±21° 오차 → EKF 누적 |
| `config/ekf_vslam_fusion.yaml` | process_noise vyaw: 0.0001→0.01 | IMU 과신뢰 해소 |
| `jupiter_driver_compensated.py` | `imu_gyro_scale` 파라미터 추가 | 향후 캘리브레이션 대비 (기본 1.0) |
| `nav2_vslam_fused.launch.py` | Foxglove에 IMU 토픽 추가 | 실시간 IMU 진단용 |
| `scripts/diagnose_imu_gyro_scale.py` | 신규 생성 | IMU gyro scale 진단 도구 |

---

## 8. 최종 EKF 센서 역할 배분

```
Yaw 제어:  IMU vyaw (100Hz) + Wheel vyaw (50Hz)
  └ ratio = 1.0017 확인됨 (진단 스크립트 실결과)

위치(x, y): Wheel vx,vy + VSLAM x,y,vx,vy + RF2O x,y
  └ VSLAM: differential 모드 (위치 변위)
  └ RF2O: 절대 위치 (LiDAR 기반)

VSLAM 역할: x, y, vx, vy만 (yaw/vyaw 완전 제거)
  └ 이전: yaw도 사용했으나 빠른 회전 시 motion blur로 ±21° 오차
  └ 현재: 위치/병진속도만 기여 (VSLAM의 원래 강점)
```

---

## 9. 미해결 / 향후 과제

- ✅ ~~VSLAM yaw 제거 후 실주행 검증 테스트 필요~~ → 섹션 10에서 완료
- ⏳ process_noise vyaw=0.01이 적절한지 실측 후 재조정
- ⏳ VSLAM이 t=224.5s에서 종료됨 (tracking lost?) — 원인 조사 필요
- ⏳ RF2O가 t=264.6s에서 종료됨 — LiDAR 연결 이슈?
- 💡 vslam_covariance_adapter의 이상치 게이팅이 VSLAM yaw용이었으므로, yaw 제거 후 위치 이상치 게이팅도 점검 필요

---

## 10. VSLAM yaw 제거 후 검증 — 새 CSV 분석

### 10.1. 새 CSV 기록

VSLAM yaw/vyaw 제거 후 Foxglove에서 새 주행 데이터 기록 (로봇 다방향 회전 포함).

```bash
wc -l ~/jupiter_ws_ros2/plot_data.csv  # 55293
```

### 10.2. 토픽 구조 확인

```python
python3 -c "
import csv
from collections import Counter
topics = []
with open('plot_data.csv') as f:
    r = csv.DictReader(f)
    for row in r:
        topics.append(row['topic'])
c = Counter(topics)
for t, n in c.most_common():
    print(f'{n:8d}  {t}')
"
```

결과 (4 토픽, 317.7s):
```
   16090  /odom_adapted.pose.pose.orientation.z       (Wheel)
   15804  /odom.pose.pose.orientation.z               (EKF)
   12814  /odom_rf2o_adapted.pose.pose.orientation.z  (RF2O)
   10585  /visual_slam/tracking/odometry_adapted...    (VSLAM)
```

### 10.3. Unwrapped Yaw 비교 분석

qz → yaw 변환 시 ±180° wrapping을 해제(unwrap)하여 누적 회전 추적:

```python
python3 << 'EOF'
import csv, math

data = {}
with open('plot_data.csv') as f:
    for row in csv.DictReader(f):
        t = float(row['elapsed time'])
        topic = row['topic']
        val = float(row['value'])
        if topic not in data:
            data[topic] = []
        data[topic].append((t, val))
for k in data:
    data[k].sort()

def unwrap_series(pts):
    result = []
    offset = 0.0
    prev = None
    for t, qz in pts:
        qz_c = max(-1.0, min(1.0, qz))
        yaw = math.degrees(2.0 * math.asin(qz_c))
        if prev is not None:
            diff = yaw - prev
            if diff > 180:
                offset -= 360
            elif diff < -180:
                offset += 360
        prev = yaw
        result.append((t, yaw + offset))
    return result
# ... (각 토픽별 unwrap 후 비교)
EOF
```

핵심 결과:
```
 Time |   Wheel |     EKF |    RF2O | EKF-Wheel
   50 |  -21.8° |  -22.3° |  -20.3° |    -0.5°
  100 |  -99.1° |  -94.7° |  -88.1° |    +4.4°
  150 |  +16.3° |  +17.5° |  +24.7° |    +1.2°
  200 | -338.3° | -333.1° | -308.5° |    +5.2°
  250 | -337.4° | -336.6° |     N/A |    +0.8°
```

### 10.4. 결론

- **EKF-Wheel 오차 최대 +5.2°** (이전: +17.7°) → 3배 이상 개선
- EKF가 RF2O와 잘 추적 (VSLAM yaw 없이도 안정)
- 큰 |EKF-Wheel| 값은 wheel odom 자체 드리프트 (타이어 슬립)
- **VSLAM yaw 제거로 EKF yaw overshoot 해결 확인** ✅

---

## 11. Nav2 Backup 반복 실행 문제 진단 및 수정

### 11.1. 문제 현상

EKF 수정 후 Nav2 내비게이션 테스트 시, **모든 goal에서 backup behavior 실행**:
```
[behavior_server-7]: Attempting backup
[behavior_server-7]: Running backup
```

Foxglove에서 목표점 클릭 → Planner 실패 → BT Recovery → Backup → 재시도 반복.

### 11.2. 로그 분석 — TF Extrapolation 오류

planner_server 로그에서 반복되는 패턴:

```
[planner_server-6]: Extrapolation Error looking up robot pose:
  Lookup would require extrapolation into the future.
  Requested time 1740661028.214... but the latest data is at time 1740661025.806...
  when looking up transform from frame [base_link] to frame [map]
```

시간 차이: `1740661028.214 - 1740661025.806 = 2.41초`

### 11.3. 근본 원인 분석

Foxglove PC의 시계가 Jetson보다 **~2.4초 빠름**:

```bash
# 6개 goal의 시계 차이 분석
python3 << 'EOF'
goals = [
    ("Goal 1", 1740661025.80, 1740661028.21, 2.41),
    ("Goal 2", 1740661123.42, 1740661125.67, 2.25),
    ("Goal 3", 1740661215.81, 1740661217.81, 2.00),
    ("Goal 4", 1740661279.02, 1740661280.02, 1.00),
    ("Goal 5", 1740661382.29, 1740661384.38, 2.09),
    ("Goal 6", 1740661425.96, 1740661428.40, 2.44),
]
for name, bt_time, goal_stamp, diff in goals:
    print(f"{name}: BT={bt_time:.2f}, Goal={goal_stamp:.2f}, Δ={diff:+.2f}s")
EOF
```

**실패 cascade**:
1. `goal_pose.header.stamp` = Jetson 시간 + 2.4s (PC 시계 기준)
2. planner_server가 해당 stamp으로 TF 조회 시도
3. TF 버퍼에 미래 데이터 없음 → "Extrapolation into the future"
4. 대기 후 재시도 → 이번엔 TF 버퍼에서 만료 → "Extrapolation into the past"
5. NavfnPlanner 2회 실패 → BT recoveryFallback → backup 실행

### 11.4. 기존 설정 확인

```bash
grep -n "transform_tolerance" \
  ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```

기존 상태:
```
  124: transform_tolerance: 1.0    (controller_server DWB)
  174: transform_tolerance: 0.1    (behavior_server)
  // planner_server, local_costmap, global_costmap: 미설정 (기본 0.3?)
```

→ 최대 tolerance가 1.0초인데, 시계 차이가 2.4초 → 허용 범위 초과.

### 11.5. 수정 적용

```bash
# nav2_params_fused.yaml에서 5개 컴포넌트 모두 transform_tolerance: 3.0으로 설정:
# - line 124: controller_server (DWB)     1.0 → 3.0
# - line 148: planner_server              (추가) 3.0
# - line 174: behavior_server             0.1 → 3.0
# - line 198: local_costmap               (추가) 3.0
# - line 237: global_costmap              (추가) 3.0

cd ~/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO --symlink-install
```

빌드 성공:
```
Starting >>> jupiter_nav_VO
Finished <<< jupiter_nav_VO [3.99s]
```

### 11.6. 검증

```bash
grep -n "transform_tolerance" \
  ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```

```
124:      transform_tolerance: 3.0
148:    transform_tolerance: 3.0
174:    transform_tolerance: 3.0
198:      transform_tolerance: 3.0
237:      transform_tolerance: 3.0
```

5곳 모두 3.0으로 확인 완료. ✅

---

## 12. 수정 파일 최종 요약 (전체 세션)

| # | 파일 | 변경 | 이유 |
|---|------|------|------|
| 1 | `config/ekf_vslam_fusion.yaml` | odom1 yaw/vyaw 제거 | VSLAM 회전 중 ±21° 오차 → 누적 |
| 2 | `config/ekf_vslam_fusion.yaml` | process_noise vyaw: 0.0001→0.01 | IMU 과신뢰 해소 |
| 3 | `config/nav2_params_fused.yaml` | transform_tolerance: 3.0 (5곳) | PC-Jetson 시계 차이 ~2.4s |
| 4 | `jupiter_driver_compensated.py` | `imu_gyro_scale` 파라미터 추가 | 캘리브레이션 대비 (기본 1.0) |
| 5 | `nav2_vslam_fused.launch.py` | Foxglove에 IMU 토픽 추가 | 실시간 진단용 |
| 6 | `scripts/diagnose_imu_gyro_scale.py` | 신규 생성 | IMU gyro scale 진단 도구 |

---

## 13. 향후 과제

### 즉시 필요
- ⏳ Nav2 backup 수정 후 실주행 검증 — transform_tolerance 3.0으로 backup 해소 확인
- ⏳ Nav2 재런치 후 Foxglove에서 goal 전송 테스트

### 단기 과제
- ⏳ PC-Jetson NTP 시간 동기화 (`sudo apt install chrony`) → transform_tolerance 기본값 복원
- ⏳ process_noise vyaw=0.01 적정성 검증 (실주행 CSV 분석)
- ⏳ VSLAM 트래킹 손실 원인 조사 (t=224.5s에서 종료)
- ⏳ RF2O 종료 원인 조사 (t=264.6s에서 종료)

### 장기 과제
- 💡 vslam_covariance_adapter 위치 이상치 게이팅 점검 (yaw 제거 후)
- 💡 장시간 주행 시 wheel odom 드리프트 보상 전략 검토
- 💡 VSLAM Docker symlink 문제 → vo_state 기반 게이팅은 향후 Docker 통합 시 재시도
