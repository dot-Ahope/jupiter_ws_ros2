# 명령어 로그 2026-05-11: 회전 / 주행 부드러움 종합 개선 (Stage 0~8 + Ki 환원)

> 진단 및 수정에 사용한 명령어와 스크립트/설정 파일을 요약 없이 전부 기록한다.

## 0. 오늘 사용한 터미널 명령어 원문

### 0.1 새 bag 정보 확인 (각 시연 후 반복)

```bash
ls -la /home/jetson/bags/latest/ && ros2 bag info /home/jetson/bags/latest 2>&1 | head -8
```

목적: 사용자가 시연 후 만든 새 bag 의 size / duration / topic 확인.
결과: 11개 bag 분석 (158s ~ 323s, 21~67MB 범위).

### 0.2 사용자 시나리오 timeline 분석

```bash
source /opt/ros/humble/setup.bash && python3 << 'PYEOF'
# inline analysis script — 회전 transition / latency / over-rotation /
# premature forward / decay 측정
PYEOF
```

목적: 매 변경 후 bag 분석. cmd_vel ω / actual ω timing, latency 분포, premature forward 검출.
결과: 각 Stage 마다 효과 측정. Stage 6 sign-flip 후 over-rotation 6.41 → 3.61x 등 누적 효과 입증.

### 0.3 rotation latency / over-rotation 측정

```bash
source /opt/ros/humble/setup.bash && python3 << 'PYEOF'
# 정지 회전 transition 검출 + threshold (0.05, 0.10, 0.20) 별 도달 시간
# cmd_ω ∈ [0.04, 0.30] 영역의 over-rotation ratio
PYEOF
```

목적: kick state machine 동작 / RUNNING_MIN floor 효과 측정.
결과: Stage 0 latency 1641ms → Stage 1 (CONFIRM 0.06) 후 13ms 까지 단축.

### 0.4 firmware 플래시 상태 검증 (Stage 0)

```bash
source /opt/ros/humble/setup.bash && python3 << 'PYEOF'
# 직진 시 좌/우 wheel diff + 우측 wheel latency + IMU drift 측정
PYEOF
```

목적: balance correction + M1/M3 deadzone (720/690) 플래시 여부 추정.
결과: 좌/우 차 -0.21%, IMU drift +0.42°/s → 플래시 됨 추정.

### 0.5 회전 transition 종료 후 decay 시간 측정

```bash
source /opt/ros/humble/setup.bash && python3 << 'PYEOF'
# 회전 phase 종료 후 actual ω < 0.10 도달까지 시간
PYEOF
```

목적: motor inertia 잔류 / filter α 효과 측정.
결과: Stage 6 (sign-flip) 후 avg 642ms → 356ms. Stage 7 (filter α 0.5) 후 76ms.

### 0.6 패키지 빌드 (변경 적용)

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup --symlink-install 2>&1 | tail -5
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_nav_VO --symlink-install 2>&1 | tail -5
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_nav --symlink-install 2>&1 | tail -5
```

목적: 각 stage 의 코드 변경 후 빌드.
결과: 모든 빌드 성공 (각 4.5~5s).

### 0.7 firmware 빌드 환경 확인

```bash
ls /home/jetson/mcu/ | head && find /home/jetson/mcu -maxdepth 3 -name "*.uvprojx" 2>/dev/null
which arm-none-eabi-gcc
```

목적: Jetson 에 ARM toolchain 있는지 확인.
결과: `arm-none-eabi-gcc` 없음. Keil µVision (Windows) 필요.

### 0.8 calibrate_angular 실행 (Stage 8)

```bash
# 터미널 1
ros2 launch jupiter_bringup bringup.launch.py
# 터미널 2
ros2 run jupiter_bringup calibrate_angular
# 터미널 3
ros2 param set /calibrate_angular test_angle 360.0
ros2 param set /calibrate_angular speed 0.5
ros2 param set /calibrate_angular tolerance 1.0
ros2 param set /calibrate_angular start_test true
```

목적: ANGULAR_SCALE_FACTOR 재캘리브레이션.
결과: script wraparound bug 로 무한 루프. 로그 데이터에서 직접 ratio 산출 → 5 cycle 평균 11.64s/360° → ratio 1.079 (8% over).

### 0.9 정지 명령 (캘리브레이션 중단 후)

```bash
ros2 topic pub --once /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.0, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
```

목적: motor 강제 정지.
결과: robot 정지.

### 0.10 ROS 로그 확인 — nav2 controller / BT

```bash
ls -lht /home/jetson/.ros/log/controller_server_*.log 2>/dev/null | head -3
ls -lht /home/jetson/.ros/log/bt_navigator_*.log 2>/dev/null | head -3
ls -lht /home/jetson/.ros/log/behavior_server_*.log 2>/dev/null | head -3

grep -inE "WARN|ERROR|FATAL|FAIL|abort|exception|spin|recovery|cancel" \
    /home/jetson/.ros/log/controller_server_<pid>_*.log | head -50

grep -c "Aborting handle" /home/jetson/.ros/log/controller_server_<pid>_*.log
grep -c "Control loop missed" /home/jetson/.ros/log/controller_server_<pid>_*.log
grep -c "Unable to transform" /home/jetson/.ros/log/controller_server_<pid>_*.log

cat /home/jetson/.ros/log/bt_navigator_<pid>_*.log
cat /home/jetson/.ros/log/behavior_server_<pid>_*.log
```

목적: 사용자 보고 "이상 동작 / spin 이상" 원인 추적.
결과:
- `Lookup would require extrapolation into the future. Requested time X but the latest data is at time Y, when looking up transform from frame [map] to frame [odom]` — 132s 에 12회 발생
- `[follow_path] [ActionServer] Aborting handle` — 12회
- `Control loop missed its desired rate of 20.0000Hz` — 21회
- → SLAM TF publish 10Hz vs controller 20Hz mismatch 의 직접 증거

### 0.11 controller_frequency / model_dt 확인

```bash
grep -n "controller_frequency\|model_dt" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml | head
grep -n "transform_publish_period\|transform_tolerance" /home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml
```

목적: 옵션 B 적용 가능 여부 확인.
결과:
- controller_frequency: 20.0
- model_dt: 0.05
- MPPI 제약: controller_period ≤ model_dt (10Hz 만 변경 시 lifecycle configure 실패)

### 0.12 SLAM TF publish period 변경 후 결과 비교

```bash
grep -c "Aborting handle" /home/jetson/.ros/log/controller_server_47319_*.log
# TF 0.05 → 7회 (감소했지만)
grep -c "Control loop missed" /home/jetson/.ros/log/controller_server_47319_*.log
# TF 0.05 → 48회 (악화)
```

목적: SLAM TF 20Hz 변경 효과 평가.
결과: BT tick 100Hz 못 따라감, action server timeout 빈발 → CPU 과부하로 환원.

### 0.13 Ki 환원 후 검증

```bash
grep -c "Aborting handle" /home/jetson/.ros/log/controller_server_57360_*.log
# Ki 0.06 → 6회
grep -c "Control loop missed" /home/jetson/.ros/log/controller_server_57360_*.log
# Ki 0.06 → 27회

cat /home/jetson/.ros/log/bt_navigator_57374_*.log
# Begin navigating → Goal succeeded ×4
# BT tick rate exceeded only 1회
# spin recovery 0 회

cat /home/jetson/.ros/log/behavior_server_57371_*.log
# Spin/BackUp/Wait 모두 활성화, 호출 없음
```

목적: Ki 환원 후 path 추종 정상화 검증.
결과: Goal succeeded 4/4, Spin recovery 0회 (이전 1회 잘못 trigger). 편향만 잔존.

### 0.14 Plan mode 진입 + 종합 plan 작성

```bash
# Plan mode 에서 inline bag 분석 + Explore agent (3개) + Plan agent (1개)
# Plan file: /home/jetson/.claude/plans/functional-sleeping-starfish.md
```

목적: 사용자 보고 "180° 회전 후 전진 시 path 벗어남" 의 종합 plan 설계.
결과: Stage 0~9 의 단계적 진행 plan 작성. 사용자 승인 후 implementation 진입.

## 1. 분석에 사용한 설정 / 스크립트 파일

### 1.0 오늘 사용/열람한 스크립트 및 파일 전체 경로

#### 분석 (inline python via bash heredoc)
- bag analysis (rotation latency, over-rotation, premature forward, decay, lateral drift)
- nav2 log analysis (Aborting handle, Control loop missed, transform errors)
- IMU drift 측정
- 좌/우 wheel asymmetry 측정

#### 수정된 파일
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` ← 수정 (Stage 1, 2, 5*, 6, 8, A — Ki 환원)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` ← 수정 (Stage 3, 4, 4 보강)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml` ← 수정 (TF publish period 0.05 시도 후 0.1 환원)
- `/home/jetson/mcu/extbd/Source/BSP/bsp_motor.c` ← 수정 (Stage 7 — filter α 0.3 → 0.5, 사용자 Keil 플래시 완료)

#### 열람한 파일 (read-only)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` (열람: line 55-90, 280-410, 425-465, 600-650)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` (열람: line 80-220, 460-481)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml` (열람 전체)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` (열람: line 100-200, 570-600)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/calibrate_angular.py` (열람: line 1-150)
- `/home/jetson/mcu/extbd/Source/APP/app_mecanum.c` (열람: balance correction)
- `/home/jetson/mcu/extbd/Source/APP/app.c` (열람: M1/M3 deadzone)
- `/home/jetson/mcu/extbd/Source/APP/app_pid.c` (열람: PID gain)
- `/home/jetson/mcu/extbd/Source/BSP/bsp_motor.c` (열람: filter α 위치)
- `/home/jetson/.claude/plans/functional-sleeping-starfish.md` ← 수정 (Plan 진행 로그 추가)

### 1.1 수정된 파일 1 — `jupiter_driver_compensated.py` (Driver layer)

`/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

| 라인 | 파라미터 | 변경 전 → 변경 후 | Stage |
|---|---|---|---|
| 30 | `ANGULAR_SCALE_FACTOR` | 1.0 → **0.93** | 8 |
| 60 | `MCU_ROT_START_ANGULAR` | 0.55 → **0.30** | 0 (이전 세션) |
| 77 | `ROT_RUNNING_MIN_ANGULAR` | 0.0 → **0.30** | 5* update 2 |
| 80 | `ROT_STARTUP_KICK_SEC` | 0.40 (Stage 2 0.25 시도 후 환원) | (Stage 2 환원) |
| 84 | `ROT_KICK_CONFIRM_THRESHOLD` | 0.10 → **0.06** | 1 |
| 166 | `Ki` (declare_parameter default) | 0.02 → **0.06** | A (Ki 환원) |
| 286+ | `_last_cmd_omega` 초기화 + sign-flip 0 cycle 코드 | 신규 추가 | 6 |
| 425+ | cmd_vel_callback 의 set_car_motion 직전 sign-flip 검출 | 신규 로직 | 6 |

### 1.2 수정된 파일 2 — `nav2_params_fused.yaml` (Nav2 layer)

`/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

| 라인 | 파라미터 | 변경 전 → 변경 후 | Stage |
|---|---|---|---|
| 136 | `RotationShim.angular_dist_threshold` | 0.20 → **0.30** | 3 |
| 163 | `MPPI.wz_max` | 1.0 → **0.6** | 4 |
| 209 | `PreferForwardCritic.cost_weight` | 5.0 → **3.0** | 3 |
| 329 | `behavior_server.max_rotational_vel` | 0.8 → **0.6** | 4 보강 |
| 474 | `velocity_smoother.max_velocity[z]` | 1.0 → **0.6** | 4 보강 |
| 475 | `velocity_smoother.min_velocity[z]` | -1.0 → **-0.6** | 4 보강 |
| 476 | `velocity_smoother.max_accel[z]` | 2.5 → **1.5** | 4 |
| 477 | `velocity_smoother.max_decel[z]` | -2.5 → **-1.5** | 4 |

### 1.3 수정된 파일 3 — `slam_params.yaml` (SLAM TF publish rate)

`/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml`

| 라인 | 파라미터 | 변경 시도 | 결과 |
|---|---|---|---|
| 27 | `transform_publish_period` | 0.1 → 0.05 (20Hz) | CPU 과부하 → BT tick 100Hz 못 따라감, action server timeout → **0.1 환원** |

### 1.4 수정된 파일 4 — `bsp_motor.c` (Firmware filter α)

`/home/jetson/mcu/extbd/Source/BSP/bsp_motor.c`

| 라인 | 파라미터 | 변경 전 → 변경 후 | Stage |
|---|---|---|---|
| 15 | `MOTOR_FILTER_ALPHA` | 0.3f → **0.5f** | 7 (사용자 Keil 플래시 완료) |

### 1.5 Plan 파일

`/home/jetson/.claude/plans/functional-sleeping-starfish.md`

- Plan mode 진입 (2026-05-11 ~11:15)
- Phase 1: Explore agent (3개 병렬) — driver / nav2 / firmware 탐색
- Phase 2: Plan agent (1개) — Stage 0~9 implementation plan 설계
- Phase 3: AskUserQuestion — firmware 플래시 상태, 진행 단위, A3.4 도입 확인
- Phase 4: Plan file 작성
- Phase 5: ExitPlanMode → 사용자 승인 → implementation 진입
- Implementation 중 plan 우회: Stage 5* (Plan 의 0.08 대신 0.30), Ki 환원 (옵션 A)

## 2. 관찰 기준 요약

### 2.1 측정 지표 변화 (시간순)

| Stage / 변경 | Latency med | Over-rot max | Decay avg | Premature fwd | Spin 이상 |
|---|---|---|---|---|---|
| Stage 0 baseline | 1641ms (n=2) | — | — | — | — |
| Stage 1 (CONFIRM 0.06) | **13ms** | 5.5x | — | — | — |
| Stage 2 (KICK_SEC 0.25) → 환원 | 20ms | 5.39x | — | — | motion 안 시작 13/19 (회귀) |
| Stage 5* (RUNNING_MIN 0.20) | 111ms | 12.54x | — | 8건 | — |
| Stage 3 (shim 0.30) | 16ms | 12.13x | — | 9건 | — |
| Stage 5* update 2 (RUNNING_MIN 0.30) | 10ms | 7.43x | 642ms | 7건 | — |
| Stage 4 (wz_max 0.6) | 17ms | 7.35x | 356ms | 9건 | — |
| Stage 4 보강 (smoother 0.6 hard cap) | 17ms | 6.41x | 317ms | 8건 (잔류>1.0 = 0) | — |
| Stage 6 (sign-flip) | 79ms | **3.61x** | 896ms (표본 7) | 5건 | — |
| Stage 7 (filter α 0.5) + 8 (SCALE 0.93) bag1 | 100ms | 8.62x | 198ms | 6건 | — |
| Stage 7+8 bag2 (localization) | 19ms | 5.41x | **76ms** | 8건 | — |
| TF 0.05 (시도) → 환원 | — | — | — | — | **이상 spin** (CPU 과부하) |
| TF 환원 후 | — | — | — | — | spin 1회 발생 (Ki 영향) |
| **Ki 0.06 환원 (최종)** | — | — | — | — | **0회** ✓ |

### 2.2 사용자 보고 핵심 증상 vs 해결 여부

| 사용자 보고 | 해결 단계 | 비고 |
|---|---|---|
| 8초 무응답 | Stage 5* update 2 | RUNNING_MIN 0.20 → 0.30 |
| Premature forward (회전 미완료 전 전진) | Stage 4 보강 + Stage 6 | 큰 잔류 ω (>1.0) 0건 달성 |
| Over-correction oscillation (cmd ±1.000 panic) | Stage 4 보강 | hard cap 0.6 |
| 회전을 이상하게 / 이상한 상황에 spin | Ki 0.06 환원 | progress 정상화 → BT Recovery 미발동 |
| 오른쪽 편향 | **잔존** | IMU drift -2.05°/s, lateral 15cm/70cm |

### 2.3 핵심 발견 — TF publish rate mismatch

```
[ERROR] controller_server: Exception in transformPose: Lookup would require extrapolation 
into the future. Requested time X but the latest data is at time Y, 
when looking up transform from frame [map] to frame [odom]
```

- SLAM TF publish 10Hz (transform_publish_period: 0.1)
- nav2 controller 20Hz (controller_frequency: 20.0)
- 매 50ms 마다 controller query, 100ms 마다 TF update
- → 매 두 번째 controller tick 이 future timestamp 로 lookup → 실패
- → follow_path Aborting → cmd_vel 미발사 → robot 이상 동작

해결 시도: TF 0.05 (20Hz) — CPU 과부하 야기 → 0.1 환원.
근본 해결 (옵션 B): controller_frequency 10Hz + model_dt 0.1 + time_steps 24 + TF 0.1 — 보류.

### 2.4 localization mode vs mapping mode

- mapping mode (default): SLAM 가 map 누적 → 매 session 거동 다름, 측정 변동성 큼
- localization mode (slam_map 지정): 안정적
- 사용자가 launch arg 없이 실행 → mapping mode (default)
- 사용자 추론대로 mapping mode 가 측정 변동성 origin

권장 운영:
```bash
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py \
    slam_mode:=localization \
    slam_map:=/home/jetson/maps/jupiter_demo
```
