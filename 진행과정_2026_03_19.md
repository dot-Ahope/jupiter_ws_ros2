# 진행과정 2026-03-19: 방안A-4 ESC 데드존 스킵 + 방안B 완전 제거 + ANGULAR_SCALE_FACTOR 재캘리브레이션

> 이 문서는 2026-03-19 세션의 전체 작업 진행과정을 기록합니다.
> 03-18에서 방안B 여러 변형(flat boost → 감속감지 → 방향지속성)이 모두 헌팅을 해결하지 못한 상태에서,
> 사용자가 "방안B를 고치는 방향 말고, 없애고 방안A만 유지하는 방법으로" 가라고 지시.
> MCU 펌웨어에 ESC 데드존 스킵(방안A-4)을 적용하고, 방안B를 완전 제거.
> 이후 ANGULAR_SCALE_FACTOR 재캘리브레이션, velocity_smoother 파라미터 조정,
> 테스트 스크립트 버그 수정, Nav2 파라미터 튜닝을 순차 수행.

---

## 목차
1. [문제 배경: 방안B 모든 변형 실패](#1-문제-배경-방안b-모든-변형-실패)
2. [사고 과정: ESC 100:1 압축의 근본 인식](#2-사고-과정-esc-1001-압축의-근본-인식)
3. [방안A-4 설계: ESC 데드존 스킵](#3-방안a-4-설계-esc-데드존-스킵)
4. [방안B 완전 제거](#4-방안b-완전-제거)
5. [1차 테스트: 방안A-4 플래싱 직후](#5-1차-테스트-방안a-4-플래싱-직후)
6. [사고 과정: velocity_smoother 충돌 인식](#6-사고-과정-velocity_smoother-충돌-인식)
7. [velocity_smoother + Nav2 파라미터 조정](#7-velocity_smoother--nav2-파라미터-조정)
8. [2차 테스트: smoother 변경 후](#8-2차-테스트-smoother-변경-후)
9. [사고 과정: 테스트 스크립트 토픽 버그 발견](#9-사고-과정-테스트-스크립트-토픽-버그-발견)
10. [ANGULAR_SCALE_FACTOR 재캘리브레이션](#10-angular_scale_factor-재캘리브레이션)
11. [3차 테스트: 토픽 수정 + SCALE 재캘리브레이션 후](#11-3차-테스트-토픽-수정--scale-재캘리브레이션-후)
12. [사고 과정: deadband 0.20이 DWB 헤딩보정을 차단하는 인식](#12-사고-과정-deadband-020이-dwb-헤딩보정을-차단하는-인식)
13. [4차 수정: deadband 0.05 + yaw_goal_tolerance 0.10](#13-4차-수정-deadband-005--yaw_goal_tolerance-010)
14. [현재 적용 상태 요약](#14-현재-적용-상태-요약)

---

## 1. 문제 배경: 방안B 모든 변형 실패

03-18~19에 걸쳐 방안B의 3가지 변형을 시도했으나 모두 헌팅 해결 실패:

| 변형 | 동작 | 실패 원인 |
|------|------|-----------|
| Flat boost (0.25) | 0.005~0.25 → 0.25 부스트 | velocity_smoother 감속 차단 → 오버슈트 → 무한회전 |
| 감속감지 boost | abs(curr) < abs(prev)-0.01 이면 부스트 금지 | 타이밍 불안정 — DWB 방향전환과 감속 구분 실패 |
| 방향지속성 boost | 같은 방향 20 cycle(1초) 유지 후에만 부스트 | PID 적분 지연(1.08초)과 DWB 주기(1.5초) 본질적 충돌 |

사용자 지시: "방안A를 언제든지 수행할 수 있다. 방안B를 고치는 방향 말고, 없애고 방안A만 유지하는 방법으로 가는 게 더 효율적이라면, 그렇게 하도록 해라."

---

## 2. 사고 과정: ESC 100:1 압축의 근본 인식

### 2.1 문제 인식 경로

3차례의 방안B 실패가 모두 같은 패턴을 보임:
- 부스트 ON → 오버슈트 → 방향 반전 → 부스트 ON → 반복
- 부스트 OFF → 데드존 → 모터 무응답 → DWB 방향전환 → 반복

**핵심 질문**: 왜 어떤 타이밍으로 부스트하든 헌팅이 발생하는가?

### 2.2 MCU PID→ESC 매핑 분석

MCU 코드(bsp_motor.c)의 Motor_Ignore_Dead_Zone()에서 ESC 변환 공식 추출:

```
esc_value = MOTOR_FREQ_STOP + pulse × (MOTOR_FREQ_FORWARD_FAST - MOTOR_FREQ_STOP) / MOTOR_MAX_PULSE
         = 108 + pulse × 36 / 3600
         = 108 + pulse / 100
```

**발견: 100:1 압축**
- PID 출력 범위: ±3600 → ESC 변화: ±36 ticks
- PID 1 → ESC 0.01 tick 변화 (물리적 의미 없음)
- PID 300 → ESC 3 ticks → **ESC 111** (물리 데드존 탈출 최소값)

### 2.3 DWB 최소 명령 → PID → ESC 프로세스 추적

```
DWB 0.0526 rad/s → driver ×0.353 → MCU 0.0186 → PID target 3.06
→ P = Kp × 3.06 = 0.8 × 3.06 = 2.45
→ ESC = 108 + 2.45/100 = 108.02 → 데드존 내 → 토크 없음!
```

RotationShim 0.8 rad/s도 추적:
```
0.8 → ×0.353 → MCU 0.2824 → PID target 46.5
→ P = 0.8 × 46.5 = 37.2
→ ESC = 108 + 37.2/100 = 108.37 → 여전히 데드존 내!
→ I가 300 도달까지: 300 / (0.06 × 46.5) ÷ 100Hz = ~1.08초
```

### 2.4 근본 인식

**PID 비례항만으로는 ESC 물리 데드존(±3 ticks)을 절대 탈출할 수 없다.**
- 최대 비례 출력: P = 0.8 × (max_cmd × 0.353 / 1000 × 164.555) = ~37 → ESC 108.37
- 적분 buildup 1.08초 ≈ DWB 방향전환 주기 1.5초의 72%
- 이 본질적 타이밍 충돌은 어떤 소프트웨어 부스트로도 해결 불가

**결론**: 방안B(소프트웨어)는 ESC 100:1 압축의 물리적 한계를 우회할 수 없다.
ESC 매핑 자체를 수정해야 한다 → **방안A-4: ESC 데드존 스킵**.

---

## 3. 방안A-4 설계: ESC 데드존 스킵

### 3.1 설계 원리

PID 출력이 0이 아니면, ESC 값을 중립(108)에서 시작하지 않고 **즉시 물리 데드존을 건너뛰어** 111(전진) 또는 105(후진)에서 시작.

```
이전(방안A-1~3):
  pulse: -3600 ────── 0 ────── +3600
  ESC:     72 ────── 108 ────── 144
                  ↑     ↑
              물리 데드존 (105~111)

방안A-4:
  pulse: -3600 ────── 0 ────── +3600
  ESC:     72 ── 105 ← | → 111 ── 144
                     108(중립)
                  데드존 건너뜀!
```

### 3.2 코드 변경

**파일**: `mcu/extbd/Source/BSP/bsp_motor.c` — `Motor_Ignore_Dead_Zone()` CAR_DIFFERENTIAL 분기

```c
#define ESC_DEADZONE_SKIP  3
int16_t esc_half = MOTOR_FREQ_FORWARD_FAST - MOTOR_FREQ_STOP;  // 36
int16_t active_range = esc_half - ESC_DEADZONE_SKIP;           // 33

if (pulse > 0) {
    // pulse +1~+3600 → ESC 111~144 (활성 범위 33 ticks)
    esc_value = MOTOR_FREQ_STOP + ESC_DEADZONE_SKIP +
        (int16_t)((float)pulse * active_range / MOTOR_MAX_PULSE);
} else if (pulse < 0) {
    // pulse -1~-3600 → ESC 105~72 (활성 범위 33 ticks)
    esc_value = MOTOR_FREQ_STOP - ESC_DEADZONE_SKIP +
        (int16_t)((float)pulse * active_range / MOTOR_MAX_PULSE);
} else {
    esc_value = MOTOR_FREQ_STOP;  // pulse=0 → 108(중립)
}
```

### 3.3 ESC 값 변환 테이블

| PID pulse | 이전 ESC (방안A-1) | 방안A-4 ESC | 토크 |
|-----------|-------------------|------------|------|
| +1 | 108.01 (데드존) | **111** | ✅ 즉시 |
| +100 | 109.00 (데드존) | **111.92** | ✅ |
| +300 | 111.00 (데드존 탈출) | **113.75** | ✅ |
| +1800 | 126.00 | **127.50** | ✅ |
| +3600 | 144 | **144** | ✅ |
| -1 | 107.99 (데드존) | **105** | ✅ 즉시 |
| 0 | 108 | **108** | — |

### 3.4 효과 계산

검증 — DWB 최소 명령:
```
DWB 0.0526 → ×0.353 → MCU 0.0186 → PID target 3.06
→ P = 2.45 → pulse 2.45
→ 방안A-4: ESC = 111 + 2.45×33/3600 = 111.02
→ 즉시 토크 발생! (적분 대기 0초)
```

---

## 4. 방안B 완전 제거

### 4.1 삭제: `__init__` 상태변수

```python
# 삭제됨:
self._prev_angular_cmd = 0.0
self._boost_dir = 0
self._boost_hold_count = 0
```

### 4.2 삭제: `cmd_vel_callback` 방안B 블록

약 20줄의 방향지속성 부스트 로직 전체 삭제. 대신 2줄 주석으로 대체:

```python
# ESC 데드존은 펌웨어 방안A-4 (ESC deadzone skip)가 처리
# PID 출력≠0이면 즉시 ESC 111/105로 점프 → 적분 지연 없음
```

### 4.3 유지: ANGULAR_SCALE_FACTOR

`ANGULAR_SCALE_FACTOR = 0.353`은 캘리브레이션 값(워크어라운드 아님)이므로 유지.
(이후 Section 10에서 0.401로 재캘리브레이션)

---

## 5. 1차 테스트: 방안A-4 플래싱 직후

### 5.1 테스트 결과

```
Part 1: Yaw change: 2.89 deg → FAIL (로봇 미움직임)
Part 2: Yaw change: 75.67 deg → PASS (0.5×3초=85.9° 대비 88%)
Goal1: SUCCEEDED 1.7s
Goal2: TIMEOUT 61.1s, yaw_span: 271.8° — 여전히 회전 문제
```

### 5.2 분석

- Part 1 FAIL (0.1 rad/s, 3초, 2.89° 회전):
  - 이 시점에선 "데드존 문제 지속"으로 판단했으나...
  - **실은 테스트 스크립트 버그** (Section 9에서 발견)
- Part 2 (88% 정확도):
  - ANGULAR_SCALE_FACTOR 0.353이 방안A-4 이후 부적합
  - ESC 활성 범위 36→33 ticks 변경으로 MCU 실효 게인 변화
- Goal2 yaw_span 271.8°:
  - 여전히 큰 회전 → velocity_smoother 설정 필요 (Section 6)

---

## 6. 사고 과정: velocity_smoother 충돌 인식

### 6.1 추론 경로

Goal2 실패 패턴을 분석:
1. RotationShimController가 180° 방향 전환 → 0.8 rad/s 명령
2. 목표 헤딩 도달 → 정지 명령 → velocity_smoother가 0.8→0 감속
3. **감속 중 0.30 이하 angular 값들이 MCU에 전달**
4. 방안A-4 적용 후에도 MCU 엔코더 분해능 한계 (1 tick/10ms ≈ 0.28 rad/s)
5. PID가 ESC 111↔108 사이를 진동 → 오도메트리 노이즈 → Nav2 혼란 → 헌팅

**핵심**: `deadband_velocity = [0.0, 0.0, 0.0]` — velocity_smoother가 데드존 영역 값을 그대로 통과시킴

### 6.2 해결 방향

velocity_smoother의 angular 관련 파라미터 조정:
- `deadband_velocity[2]`: MCU 추적 불가 영역 차단
- `max_accel[2]`, `max_decel[2]`: 데드존 통과 시간 최소화

---

## 7. velocity_smoother + Nav2 파라미터 조정

### 7.1 velocity_smoother 변경

| 파라미터 | 이전 | 변경 | 이유 |
|---------|------|------|------|
| `max_accel[2]` | 0.8 | **2.5** | 데드존 구간 빠르게 통과 |
| `max_decel[2]` | -0.8 | **-2.5** | 회전 정지 시 데드존 통과 최소화 |
| `deadband_velocity[2]` | 0.0 | **0.20** | MCU 추적 불가 영역 차단 |

### 7.2 Nav2 컨트롤러 변경

| 파라미터 | 이전 | 변경 | 이유 |
|---------|------|------|------|
| `angular_dist_threshold` | 0.785 (45°) | **0.35 (20°)** | RotationShim이 160° 담당 |
| `yaw_goal_tolerance` | 0.50 (28.6°) | **0.25 (14.3°)** | foxglove 헤딩 부족 해소 |
| `RotateToGoal.slowing_factor` | 2.0 | **5.0** | 오버슈트 억제 |

---

## 8. 2차 테스트: smoother 변경 후

```
Part 1: Yaw change: 2.48 deg → FAIL
Part 2: Yaw change: 75.59 deg → PASS (여전히 88% — 아직 SCALE 미수정)
Goal1: SUCCEEDED 3.7s
Goal2: TIMEOUT 61.1s, yaw_span: 115.4° — 개선되었으나 여전히 TIMEOUT
```

yaw_span 271.8° → 115.4° 개선 (angular_dist_threshold + slowing_factor 효과).

**추가 관찰**: foxglove에서 직접 goal_pose_raw로 이동 명령 시 좌표까지 잘 이동하나, **제자리 회전 시 15~30° 부족**.

---

## 9. 사고 과정: 테스트 스크립트 토픽 버그 발견

### 9.1 발견 경위

Part 1과 Part 2 결과의 비일관성에 주목:
- Part 1: 0.1 rad/s × 3초 = 17.2° 예상 → **2.48°** (14%)
- Part 2: 0.5 rad/s × 3초 = 85.9° 예상 → **75.59°** (88%)

만약 ANGULAR_SCALE_FACTOR 문제라면 두 테스트 모두 같은 비율로 줄어야 함.
**14% vs 88% 불균형은 SCALE 문제로 설명 불가.**

### 9.2 근본 원인

테스트 스크립트가 `/cmd_vel`에 직접 publish:
```python
self.pub = self.create_publisher(Twist, '/cmd_vel', 10)
```

그런데 velocity_smoother도 `/cmd_vel`에 20Hz로 publish 중!

```
테스트 → /cmd_vel (20Hz, 0.1 rad/s)
smoother → /cmd_vel (20Hz, 0.0 rad/s)  ← 경합!
드라이버 → 50%는 0.1, 50%는 0.0 수신
```

**Part 2가 88%인 이유**: 0.5 rad/s는 smoother에도 전달되므로 smoother가 0이 아닌 값을 출력하기 시작하지만, smoother의 자체 cmd_vel_nav 입력은 0이므로 결과가 불안정.

### 9.3 수정

```python
# 변경 전
self.pub = self.create_publisher(Twist, '/cmd_vel', 10)

# 변경 후
self.pub = self.create_publisher(Twist, '/cmd_vel_nav', 10)
```

`/cmd_vel_nav`는 velocity_smoother의 입력 토픽 → smoother를 경유하여 `/cmd_vel`로 출력 → 경합 없음.

### 9.4 교훈

> **Nav2 시스템에서 수동 테스트할 때 반드시 `/cmd_vel_nav`로 publish해야 한다.**
> `/cmd_vel`은 velocity_smoother의 출력 토픽이므로, 직접 publish하면 smoother와 경합하여
> 명령의 50%+ 가 손실된다. 이는 결과를 심각하게 왜곡한다.

---

## 10. ANGULAR_SCALE_FACTOR 재캘리브레이션

### 10.1 필요성

방안A-4가 ESC 활성 범위를 36→33 ticks로 변경:
- 기존: pulse 1 → ESC 0.01 tick
- 방안A-4: pulse 1 → ESC 0.0092 tick (33/3600)
- **같은 명령에 대해 ESC 변화량이 8.3% 감소 → MCU 실효 게인 변화**

### 10.2 캘리브레이션 데이터

토픽 수정 후 Part 2 (3차 테스트):
```
0.5 rad/s × 3초 = 85.9° 예상
실측: 86.22° (SCALE 0.401 적용 후)
```

계산:
```
old_scale = 0.353
expected = 85.9°
measured_with_old = 75.59° (2차 테스트)
new_scale = 0.353 × (85.9 / 75.59) = 0.401
```

### 10.3 피드백 경로 확인

`publish_velocity()`에서 역스케일링:
```python
vz_scaled = vz / self.ANGULAR_SCALE_FACTOR
```

명령(×SCALE)과 피드백(÷SCALE) 모두 같은 값을 사용하므로, SCALE 변경 시 자동으로 양쪽 일치.

### 10.4 검증 (3차 테스트 Part 2)

```
accuracy: 86.22° / 85.9° = 100%
```

**ANGULAR_SCALE_FACTOR 0.401 적합 확인.**

---

## 11. 3차 테스트: 토픽 수정 + SCALE 재캘리브레이션 후

```
Part 1: Yaw change: 0.22 deg → PASS (smoother deadband=0.20이 0.1 차단)
Part 2: Yaw change: 86.22 deg (accuracy 100%) → PASS
Goal1: TIMEOUT 31.0s → ⚠️ 신규 문제 발생!
Goal2: TIMEOUT 61.1s, yaw_span: 140.2°
```

**Goal1 TIMEOUT은 예기치 않은 결과** — 직진 명령인데 왜 TIMEOUT?

---

## 12. 사고 과정: deadband 0.20이 DWB 헤딩보정을 차단하는 인식

### 12.1 추론 경로

Goal1은 0.5m 직진인데 TIMEOUT:
- 직진 중 DWB가 헤딩 보정을 위해 **미세 angular 명령**을 보냄
- DWB 헤딩보정 angular: 0.05 ~ 0.15 rad/s (min_speed_theta=0.30 미만 값)
- `deadband_velocity[2] = 0.20` → **0.20 미만 angular 전부 0으로 스냅!**

```
DWB 출력: angular = 0.12 (헤딩보정)
  → velocity_smoother: 0.12 < 0.20 → 0.0으로 변환
  → 드라이버: angular = 0 → 로봇 직진만 → 헤딩 보정 불가
  → 경로 이탈 → 재계획 반복 → TIMEOUT
```

### 12.2 확인

Part 1 결과가 증거:
```
0.1 rad/s → smoother → 0 (deadband 0.20에 걸림) → yaw change 0.22°
```

0.1 rad/s가 차단되었다는 것은, DWB의 0.05~0.15 범위 헤딩보정도 전부 차단됨을 의미.

### 12.3 교훈

> **velocity_smoother deadband는 DWB 출력의 최소 유효값보다 작아야 한다.**
> DWB의 최소 non-zero angular = 1/19 = 0.0526 rad/s.
> deadband 0.20은 이보다 4배 크므로 DWB의 모든 미세 보정을 차단한다.
> 반면 deadband 0.05는 노이즈(0.01 미만)만 필터링하면서 DWB 보정은 통과시킨다.

---

## 13. 4차 수정: deadband 0.05 + yaw_goal_tolerance 0.10

### 13.1 파라미터 변경

| 파라미터 | 이전 | 변경 | 이유 |
|---------|------|------|------|
| `deadband_velocity[2]` | 0.20 | **0.05** | 0.20이 DWB 헤딩보정 차단→Goal1 TIMEOUT |
| `yaw_goal_tolerance` | 0.25 (14.3°) | **0.10 (5.7°)** | foxglove 10° 부족 해소 |

### 13.2 foxglove 10° 부족 분석

사용자 보고: "foxglove에서 제자리 회전 시 실제보다 10도 정도 덜 회전"

원인: `yaw_goal_tolerance = 0.25 rad = 14.3°`
- Nav2 SimpleGoalChecker는 헤딩 오차 14.3° 이내면 성공 판정 → 즉시 정지
- RotationShim이 목표의 ~10° 전에서 정지해도 "성공"으로 처리

```
goal heading = 150°
actual heading = 140° (error = 10°)
yaw_goal_tolerance = 14.3°
→ |10°| < 14.3° → "Goal reached!" → 회전 정지 → 10° 부족
```

yaw_goal_tolerance = 0.10 (5.7°)으로 줄이면 최대 5.7° 이내까지 회전해야 성공.

### 13.3 테스트 스크립트 업데이트

deadband 0.05로 변경되어 0.1 rad/s > 0.05이므로 Part 1이 ESC 데드존 스킵 테스트로 변경:
```python
print("Part 1: ESC 데드존 스킵 테스트 (angular.z = 0.1 rad/s, 3초)")
# 0.1 > deadband(0.05) → smoother 통과 → MCU → 방안A-4 ESC 스킵 → 회전 예상
```

---

## 14. 현재 적용 상태 요약

### 14.1 활성 변경사항

| 항목 | 값 | 파일 | 유형 |
|------|----|------|------|
| **방안A-4** (ESC 데드존 스킵) | ESC_DEADZONE_SKIP=3 | bsp_motor.c (MCU) | ✅ 펌웨어 (영구) |
| ANGULAR_SCALE_FACTOR | **0.401** (0.353에서) | jupiter_driver_compensated.py | 보정 (영구) |
| angular_dist_threshold | **0.35** (0.785에서) | nav2_params_fused.yaml | 튜닝 |
| yaw_goal_tolerance | **0.10** (0.50에서) | nav2_params_fused.yaml | 튜닝 |
| RotateToGoal.slowing_factor | **5.0** (2.0에서) | nav2_params_fused.yaml | 튜닝 |
| deadband_velocity[2] | **0.05** (0.0에서) | nav2_params_fused.yaml | 튜닝 |
| max_accel/decel[2] | **2.5/-2.5** (0.8/-0.8에서) | nav2_params_fused.yaml | 튜닝 |
| forward_sampling_distance | 0.3 (0.5에서) | nav2_params_fused.yaml | 튜닝 (03-18) |
| velocity_smoother 설정 | 신규 추가 | nav2_params_fused.yaml | 설정 (03-18) |
| min_speed_theta | 0.30 | nav2_params_fused.yaml | 튜닝 (기존) |
| imu_gyro_scale | 1.025 | nav2_vslam_fused.launch.py | 보정 (기존) |

### 14.2 제거된 항목

| 항목 | 제거일 | 이유 |
|------|--------|------|
| **방안B 전체** (모든 변형) | 2026-03-19 | 방안A-4가 ESC 데드존을 펌웨어에서 해결 |
| `_prev_angular_cmd` 상태변수 | 2026-03-19 | 방안B 제거 |
| `_boost_dir` 상태변수 | 2026-03-19 | 방안B 제거 |
| `_boost_hold_count` 상태변수 | 2026-03-19 | 방안B 제거 |

### 14.3 Nav2 토픽 흐름 (최종)

```
controller_server (DWB/RotationShim)
    │ /cmd_vel_nav
    ▼
velocity_smoother
    │ deadband [0.0, 0.0, 0.05]
    │ max_accel [0.5, 0.0, 2.5]
    │ /cmd_vel
    ▼
jupiter_driver_compensated
    │ ×ANGULAR_SCALE_FACTOR (0.401)
    │ 데드밴드 0.001 (노이즈만)
    ▼
MCU (STM32, 방안A-4)
    │ PID → Motor_Ignore_Dead_Zone()
    │ pulse≠0 → ESC 111/105 즉시 점프
    ▼
ESC → 물리 모터
```

### 14.4 다음 단계

- [ ] Nav2 + 드라이버 재시작 후 PID Ki=0.02/Kd=0.3 + MCU_MIN_ANGULAR=0.12 테스트
- [ ] 장거리 경로 테스트 (복도, 방 이동)

---

## 15. 방안C: MCU 엔코더 양자화 분석 및 최소 각속도 보정

### 15.1 사고 과정: 왜 0.1 rad/s에서 로봇이 안 돌았나

Test 4차(deadband 0.05, yaw_tol 0.10)에서 Part 1 PASS로 판정되었지만 실제 0.22°만 회전.
방안A-4 ESC 데드존 스킵이 작동하는데 왜 0.1 rad/s에서 회전이 안 되는가?

**MCU 내부 변환 경로 추적:**
```
Nav2 0.1 → driver ×0.401 → MCU 0.040 rad/s
→ set_car_motion(): int(0.040 × 1000) = 40 (V_z mrad/s)
→ Differential_Ctrl(): speed_spin = (40/1000) × 164.555 = 6.58 mm/s per wheel
→ Motion_Set_Speed(): L=-6.58, R=+6.58 mm/s
→ PID target: 6.58 mm/s
```

**엔코더 분해능 계산:**
```
1320 pulse/rev × 10ms 측정주기:
1 tick/10ms = 1 tick × 100Hz × (215.2 mm/rev) / (1320 pulse/rev) = 16.3 mm/s
```

**PID가 6.58 mm/s 타겟에서 실제로 하는 일:**
```
● 바퀴 0 ticks 읽음 → speed = 0 mm/s → error = +6.58 → PID 증가 → ESC 111 (전진)
● 바퀴 1 tick 회전 → speed = 16.3 mm/s → error = -9.72 → PID 급감 → ESC 105 (후진!)
● 바퀴 정지 → speed = 0 → error = +6.58 → PID 증가 → ESC 111 (전진)
→ 결과: 전진↔후진 반복 = 순회전 ≈ 0°
```

| Nav2 cmd | MCU 각속도 | per-wheel 속도 | encoder ticks | PID 상태 | 결과 |
|----------|----------|---------------|--------------|---------|------|
| 0.1 | 0.040 | 6.6 mm/s | **0.40** | 0↔1 진동 | ❌ 순회전 0 |
| 0.25 | 0.100 | 16.5 mm/s | **1.01** | 경계선 | ⚠️ |
| 0.30 | 0.120 | 19.7 mm/s | **1.21** | 안정 시작 | ✅ |
| 0.50 | 0.200 | 33.0 mm/s | **2.02** | 안정 | ✅ |
| 0.80 | 0.321 | 52.8 mm/s | **3.24** | 안정 | ✅ |

### 15.2 방안C 설계: MCU 최소 각속도 클램프

정지(vx≈0)에서 non-zero angular가 MCU_MIN_ANGULAR 미만이면 최소값으로 클램프:

```python
MCU_MIN_ANGULAR = 0.15  # (초기) 24.7 mm/s, 1.5 ticks → PID 안정

if abs(vx) < 0.02:  # 정지 상태에서만 클램프
    if abs(angular_corrected) > 0.001 and abs(angular_corrected) < self.MCU_MIN_ANGULAR:
        angular_corrected = math.copysign(self.MCU_MIN_ANGULAR, angular_corrected)
```

**중요: `abs(vx) < 0.02` 조건** — 전진 중에는 양 바퀴 모두 다수 ticks를 읽으므로
작은 angular 차이도 PID가 추적 가능. 전진 중 클램프하면 DWB 헤딩보정이 증폭되어 헌팅!

### 15.3 테스트 5차: 방안C (MCU_MIN_ANGULAR=0.15, 무조건 클램프)

```
Part 1: 34.36° PASS (0.1→클램프 0.15→실효 0.374→3초=64.3° 예상, 34° 실측)
Part 2: 89.11° (104%)
Goal1: SUCCEEDED 1.9s
Goal2: TIMEOUT 61.1s, yaw_span: 357.8° — 전진 중 헤딩보정 증폭→헌팅 악화!
```

### 15.4 테스트 6차: 방안C (MCU_MIN_ANGULAR=0.15, vx<0.02 조건부)

```
Part 1: 63.01° PASS (0.1→클램프 0.15→실효 0.374)
Part 2: 93.06° (108%)
Goal1: SUCCEEDED 4.2s
Goal2: TIMEOUT 61.1s, yaw_span: 285.4° — 개선 but 여전히 헌팅
```

**cmd_vel_nav 진단 (Goal2):**
```
총 603 msgs, 572 angular non-zero
avg angular: 0.283, max: 1.000
총 회전량: 0.283 × 572/10 ≈ 929° (yaw_span 285° → 3회+ 왕복 진동)
linear: 40 non-zero (6.6%) — 거의 100% 회전 명령
```

---

## 16. 사고 과정: MCU incremental PID 관성 분석

### 16.1 Goal2 헌팅의 3가지 원인

**원인 ①: MCU PID 적분 관성**
RotationShim 0.8 rad/s로 3+초 회전 후, DWB 0.30으로 전환 시:
```
MCU incremental PID: pwm_output ≈ 980 (0.8 tracking steady state)
→ 타겟 급변: 52.8 → 19.7 mm/s
→ 첫 cycle Δpwm ≈ -45 → pwm=935 (여전히 고속!)
→ Ki=0.06: 각 cycle -1.99 감소 → 500 cycle(5초)에 걸쳐 안정화
→ 이 5초간 로봇은 의도보다 55° 이상 추가 회전
```

**원인 ②: 방안C 0.30 클램프 증폭**
DWB RotateToGoal(vx=0) → min_speed_theta=0.30 → MCU 0.120:
```
MCU_MIN_ANGULAR=0.15 > 0.120 → 클램프 0.15 → 실효 0.374 (24% 증폭)
매 DWB cycle(100ms): 0.374×0.1 = 2.14° (vs DWB 기대 1.72°)
→ 지속적 오버슈트 → 방향 반전 → 다시 증폭 → 리밋 사이클
```

**원인 ③: Incremental PID에서 Kd가 방향전환 저항**
```
err 변화(복구 중): err_n > err_n-1 (덜 음수)
→ Kd × (err - 2×err_prev + err_prev2) = 양수
→ pwm 증가(감속 저항!) → 방향전환 지연
```

### 16.2 PID 튜닝 설계

**Ki: 0.06 → 0.02** — 적분 누적 3배 감소
- 방안A-4 이후 ESC 데드존 탈출에 적분 불필요 (ESC 즉시 111/105 점프)
- 방향전환 시 PID 누적 출력 해소 시간: ~2초 → ~0.7초
- 정상상태 정밀도: Nav2가 10Hz로 지속 보정하므로 약간의 steady-state error 허용

**Kd: 0.5 → 0.3** — 출력 변경 저항 감소
- Incremental PID Kd는 2차 미분 항 → 출력 변화 자체를 감쇠
- 값이 클수록 방향전환 시 감속이 느림
- 0.3: 진동 억제 능력 약간 감소하지만, Nav2가 10Hz로 외부 제어 → 허용

**Kp: 0.8 유지** — 즉각 응답 유지

### 16.3 MCU_MIN_ANGULAR 하향: 0.15 → 0.12

```
min_speed_theta(0.30) × ANGULAR_SCALE_FACTOR(0.401) = 0.1203
MCU_MIN_ANGULAR = 0.12
→ 0.1203 ≥ 0.12 → DWB 0.30 명령에 클램프 미적용!
→ RotateToGoal 24% 증폭 해소
```

0.12 MCU → 19.7 mm/s → 1.21 ticks/period → PID 추적 가능 (Ki=0.02로 안정성 개선)

---

## 17. 현재 적용 상태 요약 (최종)

### 17.1 활성 변경사항

| 항목 | 값 | 파일 | 유형 |
|------|----|------|------|
| **방안A-4** (ESC 데드존 스킵) | ESC_DEADZONE_SKIP=3 | bsp_motor.c (MCU) | ✅ 펌웨어 (영구) |
| **방안C** (MCU 최소 각속도) | MCU_MIN_ANGULAR=0.12, vx<0.02 조건 | jupiter_driver_compensated.py | 보정 |
| PID Ki | **0.02** (0.06에서) | jupiter_driver_compensated.py → MCU EEPROM | 튜닝 |
| PID Kd | **0.3** (0.5에서) | jupiter_driver_compensated.py → MCU EEPROM | 튜닝 |
| ANGULAR_SCALE_FACTOR | **0.401** (0.353에서) | jupiter_driver_compensated.py | 보정 |
| angular_dist_threshold | **0.35** (0.785에서) | nav2_params_fused.yaml | 튜닝 |
| yaw_goal_tolerance | **0.10** (0.50에서) | nav2_params_fused.yaml | 튜닝 |
| RotateToGoal.slowing_factor | **5.0** (2.0에서) | nav2_params_fused.yaml | 튜닝 |
| deadband_velocity[2] | **0.05** (0.0에서) | nav2_params_fused.yaml | 튜닝 |
| max_accel/decel[2] | **2.5/-2.5** (0.8/-0.8에서) | nav2_params_fused.yaml | 튜닝 |
| forward_sampling_distance | 0.3 (0.5에서) | nav2_params_fused.yaml | 튜닝 (03-18) |
| min_speed_theta | 0.30 | nav2_params_fused.yaml | 튜닝 (기존) |
| imu_gyro_scale | 1.025 | nav2_vslam_fused.launch.py | 보정 (기존) |

### 17.2 Nav2 토픽 흐름 (최종)

```
controller_server (DWB/RotationShim)
    │ /cmd_vel_nav
    ▼
velocity_smoother
    │ deadband [0.0, 0.0, 0.05]
    │ max_accel [0.5, 0.0, 2.5]
    │ /cmd_vel
    ▼
jupiter_driver_compensated
    │ ×ANGULAR_SCALE_FACTOR (0.401)
    │ 방안C: vx<0.02일 때 angular < 0.12 → 클램프 0.12
    │ 데드밴드 0.001 (노이즈만)
    ▼
MCU (STM32, 방안A-4, PID Kp=0.8/Ki=0.02/Kd=0.3)
    │ PID → Motor_Ignore_Dead_Zone()
    │ pulse≠0 → ESC 111/105 즉시 점프
    ▼
ESC → 물리 모터
```

### 17.3 다음 단계

- [ ] 드라이버 + Nav2 재시작 후 PID Ki=0.02/Kd=0.3 + MCU_MIN_ANGULAR=0.12 테스트
- [ ] 장거리 경로 테스트 (복도, 방 이동)
