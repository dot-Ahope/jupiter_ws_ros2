# 진행과정 2026-03-16: Direction D — RF2O 회전인식 동적 공분산 + IMU gyro_scale 보정

> 이 문서는 2026-03-16 세션의 작업 진행과정을 기록합니다.
> 이전 세션(2026-03-13)에서 Direction C (RF2O cov 0.5)가 최적으로 확인되었고,
> 이번 세션에서는 180° 회전 시 EKF yaw 오차를 추가 개선하는 Direction D를 구현 및 테스트합니다.

---

## 목차
1. [배경 및 이전 세션 요약](#1-배경-및-이전-세션-요약)
2. [Direction D 설계](#2-direction-d-설계)
3. [구현 내역](#3-구현-내역)
4. [테스트 결과](#4-테스트-결과)
5. [발견된 문제: VSLAM 완전 중단](#5-발견된-문제-vslam-완전-중단)
6. [핵심 결과 요약 및 결론](#6-핵심-결과-요약-및-결론)
7. [VSLAM Frozen Detection 수정 및 재테스트](#7-vslam-frozen-detection-수정-및-재테스트)
8. [MCU 모터 데드존 분석](#8-mcu-모터-데드존-분석)
9. [데드존 해결방안 구현](#9-데드존-해결방안-구현)

---

## 1. 배경 및 이전 세션 요약

### 1.1 Direction C 기준선 (2026-03-13)
| 지표 | 값 |
|------|-----|
| Nav2 Goal1 (0.5m 전진) | SUCCEEDED 6.3s |
| Nav2 Goal2 (복귀) | SUCCEEDED 11.8s |
| 0.5m 직진 lat | -3mm (거의 직진) |
| 180° 회전 EKF yaw 오차 | **-3.91°** |
| RF2O 공분산 | 정적 0.5 m² (항상 동일) |
| IMU gyro_scale | 1.0 (보정 없음) |

### 1.2 Direction D 목표
1. **180° 회전 yaw 정확도 개선**: -3.91° → < -2° (IMU gyro 보정)
2. **회전 중 RF2O 잡음 억제**: 정적 0.5 → 회전 시 10.0 (동적 공분산)
3. **직진 성능 보존**: Goal1 6.3s 유지

### 1.3 근본 원인 분석 (Direction D 이전 측정)
180° 회전 시 모든 센서가 yaw를 과소측정:
| 센서 | Yaw 측정 | 오차 | 오차율 |
|------|----------|------|--------|
| EKF | 176.09° | **-3.91°** | -2.17% |
| Wheel | 176.37° | -3.63° | -2.02% |
| RF2O | 175.45° | -4.55° | -2.53% |
| VSLAM | 172.65° | -7.35° | -4.08% |
| IMU gyro 적분 | ~176° | ~-4° | **~-2.3%** |

→ IMU gyro가 ~2.3% 과소측정 → EKF yaw 오차의 핵심 원인
→ 회전 후 -3.91° 오차 → 1m 직진 시 ~68mm 좌편향 발생 (기하학적)

---

## 2. Direction D 설계

### 2.1 변경 1: rf2o_covariance_adapter (NEW)
기존 `vslam_covariance_adapter`가 RF2O에 **정적 공분산(항상 0.5 m²)** 을 부여하던 것을
새로운 `rf2o_covariance_adapter`로 교체하여 **회전 인식 동적 공분산**을 적용:

```
직진 중: pose_cov = 0.5 m²   → RF2O 약한 기여 (Wheel 0.001의 500배)
회전 중: pose_cov = 10.0 m²  → RF2O 사실상 무시 (Wheel ~0.003의 3333배)
전환 시: exponential decay 1초 → 급격한 EKF 점프 방지
```

감지 방식: `/cmd_vel`의 `angular.z` 모니터링
- |wz| > 0.15 rad/s → 회전 상태
- 회전 종료 시 → 3τ(=3초) 동안 exponential decay

### 2.2 변경 2: IMU gyro_scale 1.025
`jupiter_driver_compensated.py`의 `imu_gyro_scale` 파라미터를 1.0 → 1.025로 변경:
- IMU raw gyro 값에 1.025 배수를 곱하여 2.5% 보정
- 2.3% 과소측정 → 보정 후 0.2% 수준으로 감소 예상

---

## 3. 구현 내역

### 3.1 새 파일: rf2o_covariance_adapter.py
- 경로: `src/jupiter_nav_VO/jupiter_nav_VO/rf2o_covariance_adapter.py`
- 220줄, `RF2OCovarianceAdapter` ROS 2 노드
- 주요 파라미터:
  | 파라미터 | 값 | 설명 |
  |----------|-----|------|
  | position_cov_base | 0.5 | 직진 시 pose x,y 공분산 (m²) |
  | rotation_threshold | 0.15 | 회전 감지 임계값 (rad/s) |
  | rotation_multiplier | 20.0 | 회전 중 공분산 배수 |
  | transition_decay_time | 1.0 | 회전→직진 전환 시간 (초) |
  | cmd_vel_timeout | 0.5 | cmd_vel 수신 없으면 정지 판단 (초) |

### 3.2 수정 파일: setup.py
```python
# console_scripts에 추가
'rf2o_covariance_adapter = jupiter_nav_VO.rf2o_covariance_adapter:main',
```

### 3.3 수정 파일: nav2_vslam_fused.launch.py
- RF2O adapter 변경: `executable='vslam_covariance_adapter'` → `executable='rf2o_covariance_adapter'`
- 신규 파라미터 추가: rotation_threshold, rotation_multiplier, transition_decay_time 등
- jupiter_driver에 `'imu_gyro_scale': 1.025` 추가

### 3.4 빌드 및 배포
```bash
cd ~/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO --symlink-install
source install/setup.bash
# nav2_vslam_fused.launch.py 재시작
```

---

## 4. 테스트 결과

### 4.1 시스템 상태 확인
Direction D 배포 후 파라미터 확인:
```
rf2o_covariance_adapter: rotation_threshold = 0.15  ✓
jupiter_driver: imu_gyro_scale = 1.025               ✓
RF2O adapted cov at rest: 0.5 m²                     ✓
```

### 4.2 테스트 A: Nav2 Goal 테스트 (test_direction_d.py)

| 지표 | Direction C | Direction D | 비교 |
|------|-------------|-------------|------|
| Goal1 (0.5m 전진) | SUCCEEDED 6.3s | SUCCEEDED 3.0s | ✅ 2배 빠름 |
| Goal2 (복귀) | SUCCEEDED 11.8s | SUCCEEDED 35.7s | ❌ 3배 느림 |

- **Goal2 35.7s의 원인**: VSLAM 100% frozen (후술) → Direction D 변경과 무관

### 4.3 테스트 B: 180° 독립 회전 테스트 (핵심 테스트) ✅

test_direction_d.py의 회전 테스트에 Nav2 cmd_vel 충돌 버그가 있어 독립 스크립트 실행:

**1차 테스트:**
| 센서 | Yaw 변화 | 오차 | vs C |
|------|----------|------|------|
| EKF | 178.45° | **-1.55°** | C: -3.91° → **2.5배 개선** |
| Wheel | 178.46° | -1.54° | C: -3.63° |
| RF2O | N/A (adapted) | — | — |

**2차 테스트 (3-part comprehensive):**
| 센서 | Yaw 변화 | 오차 | vs C |
|------|----------|------|------|
| EKF | 178.80° | **-1.20°** | C: -3.91° → **3.2배 개선** |

RF2O 동적 공분산 검증:
```
회전 중 RF2O cov 분포 (88 samples):
  base (≤0.6):   0개  (0%)   ← 직진 수준 없음 ✓
  high (>5.0):  70개 (80%)   ← 회전 중 10.0 m² 적용 ✓
  mid:          18개 (20%)   ← 전환 구간 (decay 중)
```
→ 회전 중 RF2O 영향력이 정상적으로 억제됨

### 4.4 테스트 C: 회전 전후 직진 좌편향 측정 (3-part comprehensive)

**회전 전 직진 1.0m:**
| 거리 | Lateral | 방향 |
|------|---------|------|
| 0.3m | +9.3mm | 좌 |
| 1.0m (종점) | +10.0mm | 좌 |

**180° 회전 후 직진 0.5m:**
| 거리 | Lateral | 방향 |
|------|---------|------|
| 0.3m | **-1.3mm** | 우 (≈0) |
| 0.5m (종점) | **-4.7mm** | 우 |

→ **회전 후 좌편향이 우편향으로 전환** (-4.7mm, 거의 0에 가까움)
→ Direction C에서는 회전 후 명확한 좌편향이 있었으나, D에서는 해소

### 4.5 테스트 D: VSLAM 정상 시 Nav2 Goal 테스트

VSLAM Docker 재시작 후 실행:
| 지표 | 값 | 비교 |
|------|-----|------|
| VSLAM Hz | 0Hz (비정상) | 카메라 스트림 끊김 |
| Goal1 | SUCCEEDED 4.3s | - |
| Goal2 | TIMEOUT 60s | EKF yaw span 13.8° |

→ VSLAM이 완전히 중단되어 공정한 Nav2 비교 불가 (아래 Section 5 참조)

---

## 5. 발견된 문제: VSLAM 완전 중단

### 5.1 증상
- VSLAM 토픽 수신: 0Hz → 0 messages
- Docker 컨테이너: Running (Up 36 minutes)
- VSLAM C++ 노드 (visual_slam_node): **프로세스 종료됨**
- launch 프로세스: 남아있으나 stuck 상태
- 이전 reset service call 3개가 hung 상태

### 5.2 타임라인
1. 세션 초반: VSLAM 정상 시작 → 점진적 frozen 증가 (79% → 100%)
2. `ros2 service call /visual_slam/reset` 2회 시도 → 둘 다 hung
3. VSLAM C++ 노드 완전 종료 → 토픽 발행 중단
4. Docker 재시작 후에도 카메라 스트림 수신 불가

### 5.3 원인 추정
- cuVSLAM v12.6의 기존 알려진 불안정성 (frozen detection이 존재하는 이유)
- RealSense D455f USB 연결 문제 또는 Docker 내부 리소스 충돌
- reset service call이 내부 mutex deadlock 유발 가능성

### 5.4 영향
- **Nav2 Goal 비교 불가**: VSLAM이 절대 위치 보정을 못 하므로 Goal2 성능 비교 무효
- **Direction D 핵심 개선은 확인됨**: 180° 회전 테스트는 VSLAM 무관 (IMU + Wheel + RF2O)
- **후속 조치 필요**: 카메라 USB 재연결 또는 시스템 재부팅 후 재테스트

---

## 6. 핵심 결과 요약 및 결론

### 6.1 Direction C vs D 비교

| 지표 | Direction C | Direction D | 변화 |
|------|-------------|-------------|------|
| **180° EKF yaw 오차** | -3.91° | **-1.20°** | ✅ **3.2배 개선** |
| 180° Wheel yaw 오차 | -3.63° | -1.54° | 2.4배 개선 |
| RF2O cov (직진) | 0.5 정적 | 0.5 동적 | 동일 |
| RF2O cov (회전) | 0.5 정적 | **10.0 동적** | ✅ 회전 억제 |
| 회전 후 직진 lat | 좌편향 | **-4.7mm 우** | ✅ 좌편향 해소 |
| Nav2 Goal1 | 6.3s | 3.0~4.3s | ✅ 개선 |
| Nav2 Goal2 | 11.8s | 35.7~TIMEOUT | ❌ VSLAM 중단 (별도 이슈) |

### 6.2 결론

1. **Direction D의 핵심 개선 확인됨**:
   - IMU gyro_scale 1.025가 yaw 정확도를 3.2배 개선 (-3.91° → -1.20°)
   - RF2O 동적 공분산이 회전 중 정상 작동 (80% high cov)
   - 회전 후 좌편향이 실질적으로 해소됨

2. **Nav2 Goal 비교는 보류**:
   - VSLAM 완전 중단으로 Goal2 공정 비교 불가
   - VSLAM 정상 시 재테스트 필요

3. **후속 과제**:
   - VSLAM Docker + 카메라 정상화 후 Nav2 Goal 재테스트
   - Goal2 성능이 C의 11.8s 수준 유지 확인 필요
   - 필요시 gyro_scale 미세 조정 (1.020~1.030 범위)
   - test_direction_d.py 회전 테스트 B 버그 수정 (Nav2 cmd_vel 충돌)

### 6.3 현재 적용 중인 설정 (Direction D)
```yaml
# nav2_vslam_fused.launch.py
rf2o_covariance_adapter:
  position_cov_base: 0.5
  rotation_threshold: 0.15    # rad/s
  rotation_multiplier: 20.0   # 회전 중 cov = 0.5 × 20 = 10.0
  transition_decay_time: 1.0  # 초

jupiter_driver:
  imu_gyro_scale: 1.025       # 2.5% 보정
```

---

## 7. VSLAM Frozen Detection 수정 및 재테스트

### 7.1 문제 발견: VSLAM "frozen" 오탐

시스템 재부팅 후 VSLAM이 정상 동작함에도 진단 스크립트가 "frozen"으로 보고.

**근본 원인**: `vslam_covariance_adapter`의 frozen 판정 파라미터 문제
- cuVSLAM은 ~60Hz로 publish하나, 실제 새 pose는 ~10-15Hz (나머지는 반복)
- `frozen_threshold=10` (0.17초 = 60Hz × 10) → 너무 짧아서 정상 동작도 frozen으로 판정
- `frozen_publish_rate=1.0Hz` → frozen 판정 시 1Hz로 다운샘플 (사실상 VSLAM 데이터 거의 차단)

**증거**: 유저의 Foxglove CSV에서 604개의 VSLAM adapted 메시지가 실제 변하는 값으로 확인됨.

### 7.2 수정 내역

`nav2_vslam_fused.launch.py`에 다음 파라미터 추가:
```yaml
vslam_covariance_adapter:
  frozen_threshold: 60       # 10 → 60 (1초, 기존 0.17초)
  frozen_publish_rate: 10.0  # 1.0 → 10.0 Hz
  frozen_pos_tolerance: 1e-6
```

### 7.3 재테스트 결과 (Frozen Fix 적용 후)

| 지표 | Frozen Fix 전 | Frozen Fix 후 |
|------|---------------|---------------|
| VSLAM adapted msgs (Goal2) | ~0 | **2574** |
| Goal1 | SUCCEEDED 3.0~4.3s | SUCCEEDED **2.4s** |
| Goal2 | TIMEOUT 60s | TIMEOUT **60s** |
| Goal2 yaw span | 13.8° | **359°** |
| Goal2 max wz | N/A | **-748.8 mrad/s** |

**분석**:
- VSLAM 데이터 전달 대폭 개선 (0 → 2574 msgs)
- Goal1 시간 단축 (3~4s → 2.4s) ✅
- Goal2 여전히 TIMEOUT — **yaw span 359° = 통제불능 회전** ❌
- 문제는 VSLAM이 아니라 **모터 제어** 쪽

---

## 8. MCU 모터 데드존 분석

### 8.1 증상

사용자 보고: `cmd_vel angular.z=0.5` 회전 명령을 줘도 정지 상태에서 로봇이 움직이지 못함.
Nav2 Goal2 재테스트에서 yaw span 359° (통제불능 회전) 발생.

### 8.2 신호 흐름 추적

```
ROS cmd_vel (m/s, rad/s)
→ jupiter_driver (ANGULAR_SCALE_FACTOR=0.353, ×1000 → mm/s, mrad/s)
→ UART → protocol.c FUNC_MOTION (V_x, V_y, V_z as int16)
→ Differential_Ctrl: L=V_x-(V_z/1000)*164.555, R=V_x+(V_z/1000)*164.555
→ PID_Set_Motor_Target (target mm/s)
→ [10ms loop] PID_Incre_Calc → pwm_output (±3600)
→ Motor_Set_Pwm → Motor_Apply_Filter(α=0.1) → Motor_Ignore_Dead_Zone
  → CAR_DIFFERENTIAL: 0/72/144 이산값만 반환 (비례제어 불가)
→ TIM compare registers (ESC pulse width)
```

### 8.3 수치 추적: `cmd_vel angular.z = 0.5`

```
cmd_vel angular.z = 0.5 rad/s
  → ANGULAR_SCALE_FACTOR(0.353) → 0.1765 rad/s → V_z = 176 mrad/s
  → speed_spin = (176/1000) × 164.555 = 29 mm/s
  → Left target = -29 mm/s, Right target = +29 mm/s
```

목표 속도 29mm/s는 매우 작은 값 → PID 출력이 서서히 증가 → 데드존에 갇힘.

### 8.4 근본 원인 3가지

#### 문제 1: `Motor_Ignore_Dead_Zone` — 비례 제어 완전 파괴

`bsp_motor.c`의 `CAR_DIFFERENTIAL` 분기 (수정 전):
```c
if (pulse > MOTOR_FREQ_FORWARD_FAST) return MOTOR_FREQ_FORWARD_FAST;  // >144 → 144
if (pulse < MOTOR_FREQ_BACKWARD_FAST) return MOTOR_FREQ_BACKWARD_FAST; // <72 → 72
return 0;  // 72~144 → 0
```

PID 출력 (±3600)에 대해 3개의 이산값만 반환:
| PID 출력 범위 | Dead zone 반환 | ESC 의미 | 결과 |
|---|---|---|---|
| `> 144` | 144 | 전진 최고속 | 풀스피드 점프 |
| `72 ~ 144` | 0 | 무신호 | 정지 (데드존) |
| `0 ~ 71` | 72 | **후진 최고속** | **역방향!** |
| `< 0` (전부) | 72 | 후진 최고속 | 후진 풀스피드 |

#### 문제 2: `Motor_Set_Pwm` M3 방향 반전 — ESC 신호 경로 파괴

M3(우측 모터)에서 `pulse = -pulse` 반전 후 H-bridge 로직 수행:
- Dead zone이 72 반환 → 반전 -72 → `PWM_M3_A = 0` → ESC가 TIM1_CCR4에서 0 수신
- 우측 모터 **영구 정지** (순수 회전 시)

#### 문제 3: `MOTOR_FILTER_ALPHA = 0.1` — 과도한 저역통과 필터

- `filtered = 0.1 × new + 0.9 × old`
- 90% 도달에 ~23회 반복 = **230ms** 소요
- 데드존과 결합 시, 임계치 도달까지 0.5~1초 이상

### 8.5 결론

**MCU 데드존이 Nav2 Goal2 실패의 근본 원인**:
- 순수 회전 시 우측 모터가 영구 정지 → 좌측만 구동 → 통제불능 회전
- 작은 회전 명령이 데드존에 갇혀 모터 무반응
- Nav2가 경로 추종 실패 → 더 큰 보정 명령 → 풀스피드 점프 → 발산

---

## 9. 데드존 해결방안 구현

### 9.1 방안 A — 펌웨어 소스 수정 (3건, 미적용 상태)

> **상태**: 소스코드 수정 완료. MCU 플래시 미적용 — 나중에 펌웨어 빌드/업로드 필요.

**수정 파일**: `mcu/extbd/Source/BSP/bsp_motor.c`

| 태그 | 수정 내용 | 위치 |
|------|-----------|------|
| `[방안A-1]` | `Motor_Ignore_Dead_Zone` PID→ESC 비례 매핑 | Dead zone 함수 내 CAR_DIFFERENTIAL 분기 |
| `[방안A-2]` | `Motor_Set_Pwm` M1/M3에서 ESC값 직접 출력 (M3 반전 제거) | Motor_Set_Pwm 함수 M1/M3 case |
| `[방안A-3]` | `MOTOR_FILTER_ALPHA` 0.1 → 0.3 (응답속도 3배) | define문 |

**방안A-1 비례 매핑 수식**:
```
ESC값 = MOTOR_FREQ_STOP + (PID출력 × esc_half / MOTOR_MAX_PULSE)
     = 108 + (pulse × 36 / 3600)

PID=0     → ESC=108 (중립, 1.5ms)
PID=+3600 → ESC=144 (최대전진, 2.0ms)
PID=-3600 → ESC=72  (최대후진, 1.0ms)
PID=+1800 → ESC=126 (절반 전진)
```

### 9.2 방안 B — ROS 드라이버 우회 (즉시 적용)

> **상태**: 적용 완료, 빌드 완료.
> **주의**: 방안 A(펌웨어) 적용 후 반드시 제거할 것.

**수정 파일**: `jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

`cmd_vel` 콜백에서 각속도 데드존 우회:
```python
# ── 방안B 시작 ──
MIN_ANGULAR_DEADZONE = 0.15  # rad/s (scaled) — MCU 데드존 탈출 최소값
if abs(angular_corrected) > 0.01 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
    angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
# ── 방안B 끝 ──
```

**동작**: Nav2가 0.01~0.15 rad/s 사이의 작은 회전을 명령하면, MCU 데드존을 넘길 수 있도록 0.15로 올림.

**한계**: M3 방향 반전 문제(문제 2)는 해결 불가 → 방안 A 적용이 근본 해결.

### 9.3 적용 상태 요약

| 방안 | 수정 상태 | 빌드 상태 | 런타임 적용 |
|------|-----------|-----------|-------------|
| A-1: Dead zone 비례매핑 | ✅ 소스 수정 | ❌ MCU 빌드 필요 | ❌ 플래시 필요 |
| A-2: M1/M3 ESC 직접출력 | ✅ 소스 수정 | ❌ MCU 빌드 필요 | ❌ 플래시 필요 |
| A-3: Filter alpha 0.3 | ✅ 소스 수정 | ❌ MCU 빌드 필요 | ❌ 플래시 필요 |
| B: 최소 회전속도 보장 | ✅ 수정 완료 | ✅ colcon 빌드 완료 | ⏳ 재시작 시 적용 |

### 9.4 후속 과제

1. **MCU 펌웨어 빌드 및 플래시** → 방안 A 적용
2. **방안 A 적용 후 방안 B 제거** (주석 `── 방안B 시작/끝 ──` 사이 코드 삭제)
3. **Nav2 Goal2 재테스트** → 통제불능 회전 해소 확인
4. **MIN_ANGULAR_DEADZONE 값 튜닝** (방안 B 사용 시, 0.10~0.20 범위)
