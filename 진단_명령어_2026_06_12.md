# 명령어 로그 2026-06-12: D455F + cuVSLAM IMU 융합 fly-away 수정 (H1 타임스탬프 + Phase3 fps/method) 및 시각화 진단

> 진단 및 수정에 사용한 명령어와 스크립트/설정 파일을 요약 없이 전부 기록한다.

## 0. 오늘 사용한 터미널 명령어 원문

### 0.1 cuVSLAM IMU 타임스탬프 결함(H1) 코드 확인 — 호스트
```bash
find /home/jetson/isaac_ws_ros / -name 'cuvslam.h' -o -name 'cuvslam*.h' 2>/dev/null | head -20
```
목적: cuVSLAM C-API 헤더(`CUVSLAM_RegisterImuMeasurement` 시그니처) 위치 확인.
결과: 출력 없음 → 헤더는 컨테이너 전용(호스트 소스트리에 없음).

```bash
cd /home/jetson/isaac_ws_ros; grep -rl "CUVSLAM_RegisterImuMeasurement\|CUVSLAM_ImuMeasurement" /home/jetson/isaac_ws_ros 2>/dev/null | head; echo "---headers---"; find /home/jetson/isaac_ws_ros -path '*cuvslam*' \( -name '*.h' -o -name '*.hpp' \) 2>/dev/null | head -30
```
목적: IMU API 참조 파일 + cuvslam 헤더 탐색.
결과: `cuvslam_ros_conversion.{hpp,cpp}`, `visual_slam_impl.cpp` 만 참조. C-API 헤더는 install에도 없음.

```bash
cd /home/jetson/isaac_ws_ros; grep -rn "CUVSLAM_RegisterImuMeasurement" . 2>/dev/null | grep -v "\.cpp:" | head; echo "=== struct def search ==="; grep -rn "struct CUVSLAM_ImuMeasurement\|} CUVSLAM_ImuMeasurement\|CUVSLAM_ImuMeasurement {" . 2>/dev/null | head; echo "=== any cuvslam c-api header ==="; find . -name '*.h' 2>/dev/null | xargs grep -l "CUVSLAM_" 2>/dev/null | head
```
목적: `CUVSLAM_ImuMeasurement` 구조체에 timestamp 필드가 있는지 확인(한 줄 패치 충분성 검증).
결과: 구조체 정의/헤더 호스트에 없음 → 컨테이너 전용 확정. (단 `TocuVSLAMImuMeasurement`는 accel/gyro만 채움 = timestamp는 함수 인자로만 전달 → 688행 한 줄 패치로 충분.)

### 0.2 isaac_ros dev 컨테이너 환경 확인 / detached 기동
```bash
echo "ISAAC_ROS_WS=${ISAAC_ROS_WS:-<unset>}"; echo "--- docker ps ---"; docker ps --format '{{.ID}}  {{.Image}}  {{.Names}}  {{.Status}}' 2>&1 | head -20; echo "--- run_dev.sh path ---"; ls -la /home/jetson/isaac_ws_ros/src/isaac_ros_common/scripts/run_dev.sh 2>&1
```
목적: 워크스페이스/컨테이너 상태/스크립트 경로 확인.
결과: `ISAAC_ROS_WS=/home/jetson/isaac_ws_ros`, 떠 있는 컨테이너 0개, run_dev.sh 존재.

```bash
echo "=== ~/.isaac_ros_common-config ==="; cat ~/.isaac_ros_common-config 2>&1; echo; echo "=== ROOT .isaac_ros_common-config ==="; cat /home/jetson/isaac_ws_ros/src/isaac_ros_common/.isaac_ros_common-config 2>&1; echo; echo "=== dockerargs file (home) ==="; cat ~/.isaac_ros_dev-dockerargs 2>&1 | head -40; echo; echo "=== docker images (isaac_ros_dev) ==="; docker image ls | grep -i isaac_ros_dev 2>&1; echo "=== all containers (incl stopped) ==="; docker ps -a --format '{{.Names}}  {{.Status}}  {{.Image}}' 2>&1 | head
```
목적: 이미지키/컨테이너명/추가 마운트 확인.
결과: `CONFIG_IMAGE_KEY=ros2_humble.realsense.jupiter_user`, suffix 없음 → 컨테이너명 `isaac_ros_dev-aarch64-container`, 이미지 `isaac_ros_dev-aarch64:latest` 존재.

```bash
docker run -d \
  --privileged --network host --ipc=host \
  -e NVIDIA_VISIBLE_DEVICES=nvidia.com/gpu=all,nvidia.com/pva=all \
  -e NVIDIA_DRIVER_CAPABILITIES=all \
  -e ISAAC_ROS_WS=/workspaces/isaac_ros-dev \
  -e HOST_USER_UID=$(id -u) -e HOST_USER_GID=$(id -g) \
  -v /usr/bin/tegrastats:/usr/bin/tegrastats \
  -v /tmp/:/tmp/ \
  -v /usr/lib/aarch64-linux-gnu/tegra:/usr/lib/aarch64-linux-gnu/tegra \
  -v /usr/src/jetson_multimedia_api:/usr/src/jetson_multimedia_api \
  --pid=host \
  -v /usr/share/vpi3:/usr/share/vpi3 \
  -v /home/jetson/isaac_ws_ros:/workspaces/isaac_ros-dev \
  -v /etc/localtime:/etc/localtime:ro \
  --name isaac_ros_dev-aarch64-container \
  --runtime nvidia \
  --entrypoint /usr/local/bin/scripts/workspace-entrypoint.sh \
  --workdir /workspaces/isaac_ros-dev \
  isaac_ros_dev-aarch64 \
  sleep infinity 2>&1
echo "--- exit: $? ---"
sleep 3
docker ps --format '{{.Names}}  {{.Status}}' 2>&1
```
목적: run_dev.sh 신규 경로가 대화형(블로킹)이라, 동일 마운트로 컨테이너를 detached 기동(이후 사용자 run_dev.sh가 재사용 attach). 호스트 `/home/jetson/isaac_ws_ros` → 컨테이너 `/workspaces/isaac_ros-dev` 바인드마운트로 H1 패치 즉시 노출.
결과: `isaac_ros_dev-aarch64-container Up`.

### 0.3 H1 패치 확인 + 패키지 빌드 — 컨테이너
```bash
docker exec -u admin isaac_ros_dev-aarch64-container bash -lc '
echo "=== id ==="; id
echo "=== patched line (should be imu_ts) ==="; sed -n "686,694p" /workspaces/isaac_ros-dev/src/isaac_ros_visual_slam/isaac_ros_visual_slam/src/impl/visual_slam_impl.cpp
echo "=== ros / colcon ==="; echo "ROS_DISTRO=$ROS_DISTRO"; which colcon ros2 2>&1
echo "=== prior build/install present? ==="; ls -d /workspaces/isaac_ros-dev/install/isaac_ros_visual_slam 2>&1; ls /workspaces/isaac_ros-dev/install 2>/dev/null | wc -l; echo "install pkgs count ^"
echo "=== visual_slam deps installed? ==="; ls -d /workspaces/isaac_ros-dev/install/isaac_ros_nitros 2>&1
'
```
목적: 패치 노출 확인 + admin(uid 1000) + ROS/colcon + 기존 빌드 확인.
결과: 패치 `imu_ts` 정상 노출, `uid=1000(admin)`, ROS humble, install 41패키지(nitros는 apt). 

```bash
docker exec -u admin isaac_ros_dev-aarch64-container bash -lc '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash 2>/dev/null
cd /workspaces/isaac_ros-dev
echo "=== build start ==="
colcon build --symlink-install --packages-select isaac_ros_visual_slam --cmake-args -DCMAKE_BUILD_TYPE=Release 2>&1 | tail -n 40
echo "COLCON_EXIT=${PIPESTATUS[0]}"
'
```
목적: H1 패치된 isaac_ros_visual_slam 재빌드.
결과:
```
Finished <<< isaac_ros_visual_slam [54.6s]
Summary: 1 package finished [55.6s]
COLCON_EXIT=0
```

```bash
docker exec -u admin isaac_ros_dev-aarch64-container bash -lc '
echo "=== now ==="; date
echo "=== installed visual_slam .so files (mtime) ==="
find /workspaces/isaac_ros-dev/install/isaac_ros_visual_slam -name "*.so" -printf "%t  %p\n" 2>/dev/null | sort
echo "=== build object mtime (visual_slam_impl) ==="
find /workspaces/isaac_ros-dev/build/isaac_ros_visual_slam -name "visual_slam_impl.cpp.o" -printf "%t  %p\n" 2>/dev/null
'
```
목적: 재컴파일/설치 반영 확인.
결과: `visual_slam_impl.cpp.o` Jun 11 13:04:38 재컴파일. install `.so`는 심링크(mtime 옛날) → 타겟 확인 필요.

```bash
docker exec -u admin isaac_ros_dev-aarch64-container bash -lc '
echo "=== install .so: symlink? ==="; ls -la /workspaces/isaac_ros-dev/install/isaac_ros_visual_slam/lib/libvisual_slam_node.so
echo "=== resolved target ==="; readlink -f /workspaces/isaac_ros-dev/install/isaac_ros_visual_slam/lib/libvisual_slam_node.so
echo "=== build-tree .so mtime ==="; find /workspaces/isaac_ros-dev/build/isaac_ros_visual_slam -name "libvisual_slam_node.so" -printf "%t  %p\n" 2>/dev/null
echo "=== resolved target mtime ==="; T=$(readlink -f /workspaces/isaac_ros-dev/install/isaac_ros_visual_slam/lib/libvisual_slam_node.so); stat -c "%y  %n" "$T" 2>&1
'
```
목적: `--symlink-install` 이므로 install `.so`가 빌드트리를 가리키는 심링크인지 + 타겟 재링크 확인.
결과: install `.so` → `build/.../libvisual_slam_node.so` 심링크, 타겟 mtime **Jun 11 13:04:39 재링크** = 패치 반영 확정.

### 0.4 RealSense 연결/유휴 확인 (무침습)
```bash
echo "=== RealSense USB (passive) ==="; lsusb 2>/dev/null | grep -i "8086\|Intel\|RealSense" || echo "lsusb: no Intel/RealSense match (or lsusb unavailable)"
echo "=== any ROS/realsense/vslam process already running (host pid namespace) ==="; ps -eo pid,comm,args 2>/dev/null | grep -iE "realsense2_camera|visual_slam|component_container|ros2 launch" | grep -v grep | head
echo "=== container still up ==="; docker ps --format '{{.Names}} {{.Status}}' | grep isaac
```
목적: 카메라 연결/점유 확인.
결과: `Bus 002 Device 003: ID 8086:0b5c ... Depth Camera 455f` 연결, 점유 프로세스 없음(유휴).

### 0.5 Phase 0 실측 — 패치본 스택 런치 + IMU/TF/odom 측정 (백그라운드, 자동 정리)
```bash
docker exec -u admin isaac_ros_dev-aarch64-container bash -lc '
set +e
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash 2>/dev/null
cd /workspaces/isaac_ros-dev
LOG=/tmp/vslam_phase0.log
echo "=== launching patched stack (enable_imu_fusion:=true) ==="
ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py enable_imu_fusion:=true use_rviz:=false enable_visualization:=false > "$LOG" 2>&1 &
LP=$!
sleep 32
echo "=== node list ==="; ros2 node list 2>&1 | head
echo "=== /camera/imu hz (9s) ==="; timeout 9 ros2 topic hz /camera/imu 2>&1 | tail -4
echo "=== /camera/infra1/image_rect_raw hz (6s) ==="; timeout 6 ros2 topic hz /camera/infra1/image_rect_raw 2>&1 | tail -3
echo "=== tf camera_link -> camera_imu_optical_frame ==="; timeout 5 ros2 run tf2_ros tf2_echo camera_link camera_imu_optical_frame 2>&1 | head -16
echo "=== tf camera_link -> camera_gyro_optical_frame ==="; timeout 5 ros2 run tf2_ros tf2_echo camera_link camera_gyro_optical_frame 2>&1 | head -16
echo "=== /visual_slam/tracking/odometry hz STATIONARY (7s) ==="; timeout 7 ros2 topic hz /visual_slam/tracking/odometry 2>&1 | tail -3
echo "=== vslam log: init/imu/gravity/warn ==="; grep -iE "cuvslam|initializ|imu|gravity|fail|threshold|register|odom|reset" "$LOG" | tail -45
echo "=== cleanup ==="; kill -INT $LP 2>/dev/null; sleep 6; pkill -INT -f realsense2_camera_node 2>/dev/null; pkill -INT -f visual_slam 2>/dev/null; pkill -f component_container 2>/dev/null; sleep 2
echo DONE_PHASE0
'
```
목적: 사용자가 "모름"이라 한 IMU 스트림 상태 + extrinsic TF + 정지 시 odom 발행을 움직임 없이 측정.
결과(핵심 토막):
```
/camera/imu        average rate: 199.747  std dev: 0.00015s window: 1401
/camera/infra1     average rate: 89.873
tf camera_link->camera_imu_optical_frame: Translation [-0.016,-0.030,0.007] Rotation RPY[-90,-0,-90]°
/visual_slam/tracking/odometry  average rate: 89.877   (정지 상태에서 발행됨)
[visual_slam_node]: cuVSLAM tracker was successfully initialized.  Enable IMU Fusion: true
```

### 0.6 잔여 launch 프로세스 정리 (cleanup 미완 → 호스트 직접 종료)
```bash
echo "=== lingering ros/realsense/vslam processes? (host pid ns) ==="; ps -eo pid,comm,args 2>/dev/null | grep -iE "realsense2_camera|visual_slam|component_container|ros2 launch|robot_state" | grep -v grep | head
echo "=== container still up ==="; docker ps --format '{{.Names}} {{.Status}}' | grep isaac
```
결과: `ros2 launch ...`(PID 11757) 잔존.

```bash
# (docker exec pkill 은 "context canceled" 로 실패 → 호스트에서 직접 종료)
PIDS=$(ps -eo pid,args 2>/dev/null | grep -iE "isaac_ros_visual_slam_realsense|realsense2_camera_node|component_container" | grep -v grep | awk '{print $1}')
echo "target PIDs: $PIDS"
for p in $PIDS; do kill -INT "$p" 2>/dev/null; done
for i in 1 2 3 4 5 6; do
  left=$(ps -o pid= -p ${PIDS// /,} 2>/dev/null | wc -l)
  [ "$left" -eq 0 ] && break
done
PIDS2=$(ps -eo pid,args 2>/dev/null | grep -iE "isaac_ros_visual_slam_realsense|realsense2_camera_node|component_container" | grep -v grep | awk '{print $1}')
for p in $PIDS2; do kill -KILL "$p" 2>/dev/null; done
echo "=== final ==="; ps -eo pid,args 2>/dev/null | grep -iE "realsense2_camera|visual_slam|component_container|isaac_ros_visual_slam_realsense" | grep -v grep | head || true
echo "exit_ok"
```
목적: 카메라 점유 해제. 결과: 전부 종료.

### 0.7 빌드용 최소 컨테이너 제거 (X11 없음 → 사용자 run_dev.sh 새로 띄우게)
```bash
echo "=== host-side patched .so (persisted via bind mount) ==="
ls -la --time-style=+%Y-%m-%d_%H:%M /home/jetson/isaac_ws_ros/build/isaac_ros_visual_slam/libvisual_slam_node.so 2>&1
echo "=== host-side patched source line ==="
sed -n '687,688p' /home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/src/impl/visual_slam_impl.cpp
echo "=== removing minimal build container (artifacts persist on host) ==="
docker rm -f isaac_ros_dev-aarch64-container 2>&1
echo "=== containers now ==="; docker ps -a --format '{{.Names}} {{.Status}}' | grep isaac || echo "none"
```
결과: `.so` 호스트 영속(2026-06-11_13:04, jetson 소유), 컨테이너 제거.

### 0.8 Phase 3 런치 수정 문법 검증
```bash
cd /home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch && python3 -m py_compile isaac_ros_visual_slam_realsense.launch.py && echo "PY_SYNTAX_OK" || echo "PY_SYNTAX_FAIL"
echo "=== changed lines preview ==="
grep -nE "stereo_profile|unite_imu_method|jitter_threshold_ms|depth_module.profile" isaac_ros_visual_slam_realsense.launch.py
```
결과: `PY_SYNTAX_OK`.

### 0.9 (사용자 실행) Phase 3 모션 테스트 — 새 컨테이너에서 런치
```bash
# 사용자: cd ${ISAAC_ROS_WS}/src/isaac_ros_common && ./scripts/run_dev.sh  (VNC: vncserver :1, export DISPLAY=:1, xhost +local:docker)
# 1차(90fps/method2 — 발산 잔여): ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py enable_imu_fusion:=true   (Phase3 적용 전)
# 2차(30fps/method1 — Phase3 적용): ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py enable_imu_fusion:=true
# 모니터: python3 vslam_ready_monitor.py
```
결과(2차): odom 82s 안정, jump 누적 1, max 3.1 m/s, **gravity 정렬 확인됨 z=-9.79 |g|=9.81 수신 1350**.

### 0.10 (사용자 실행) 시각화 진단용 rosbag 녹화
```bash
ros2 bag record -o /workspaces/isaac_ros-dev/axis_flip_capture \
  /visual_slam/tracking/odometry \
  /visual_slam/tracking/vo_pose \
  /visual_slam/vis/gravity \
  /camera/imu /tf /tf_static
# (좌우+3D 모션 35초 재현 후 Ctrl+C)
```
결과: `/home/jetson/isaac_ws_ros/axis_flip_capture/axis_flip_capture_0.db3` (35.3s).

### 0.11 bag 위치 확인 + 정밀 분석 (odom RPY ↔ IMU ↔ gravity)
```bash
echo "=== running containers ==="; docker ps --format '{{.Names}}  {{.Status}}' 2>&1 | grep -i isaac || echo "(none running)"
echo "=== bag dirs on HOST (/home/jetson, isaac_ws_ros) ==="
find /home/jetson -maxdepth 2 \( -name 'rosbag2_*' -o -name '*axis_flip*' -o -name 'metadata.yaml' \) 2>/dev/null | grep -iE "bag|axis_flip|metadata" | head -20 || true
echo "=== quick look: recent dirs in ~ with .db3/.mcap ==="
find /home/jetson -maxdepth 3 \( -name '*.mcap' -o -name '*.db3' \) -printf '%t  %p\n' 2>/dev/null | sort | tail -10 || true
```
결과: 컨테이너 `Up About an hour`(사용자 run_dev.sh), bag `/home/jetson/isaac_ws_ros/axis_flip_capture/axis_flip_capture_0.db3` Jun 11 15:01.

```bash
docker exec -u admin isaac_ros_dev-aarch64-container bash -lc '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash 2>/dev/null
python3 /workspaces/isaac_ros-dev/analyze_axis_flip.py
'
```
목적: bag 파싱 → IMU rate/dt, odom roll/pitch transient, gravity tilt, 상관 분석. (스크립트 = §1.1 참조)
결과(핵심):
```
IMU count=7040 rate≈199.7Hz dt mean=5.007ms std=0.002ms  → samples/odom-frame ≈ 6.7
odom pitch range=[-27.1,+16.4]°  roll range=[-3.1,+3.7]°  max|w|=0.95 max|v|=0.40
top transient t=18.61s pitch=-27.1° |v|=0.04m/s  gravity tilt=23.0° |g|=9.81
```

```bash
docker exec -u admin isaac_ros_dev-aarch64-container bash -lc '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash 2>/dev/null
python3 /workspaces/isaac_ros-dev/analyze_axis_flip.py 2>/dev/null | sed -n "/axis-paired/,\$p"
'
```
목적: 판별 — 실제 pitch vs 병진가속 아티팩트.
결과:
```
accel.z(forward) vs pitch r=-0.986
peak pitch=-27.1° window±0.6s max|v|=0.063 m/s
measured accel.z=+3.80 vs 9.81·sin(pitch)=+4.47 → 중력투영으로 설명(실제 틸트)
IF 병진이면 1.2s 동안 Δv≈4.4 m/s 필요 — 실측 0.063 m/s → 실제 PITCH(정상 추적)
max sample-to-sample position-speed=0.53 m/s ; max Δpitch/step=1.5° (글리치 없음)
```

```bash
docker exec -u admin isaac_ros_dev-aarch64-container bash -lc '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash 2>/dev/null
python3 /workspaces/isaac_ros-dev/analyze_axis_flip.py 2>/dev/null | sed -n "/VELOCITY 마커/,/done/p"
'
```
목적: 사용자 정정(흔들린 객체 = Velocity 마커) 후, 속도 화살표 거동 분석.
결과:
```
|v| mean=0.078 max=0.399 m/s ; 저속(<0.05) 51% ; 명확모션(>0.15) 17%
화살표 방향변화/step: 저속 16° vs 명확모션 3°
실제 수직이동 pz range=0.516 m
→ 저속 속도추정 노이즈 + 실제 3D 모션 = 코스메틱, 포즈와 무관
```

## 1. 분석에 사용한 설정 / 스크립트 파일

### 1.0 오늘 사용/열람한 스크립트 및 파일 전체 경로
- `/home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/src/impl/visual_slam_impl.cpp` ← 수정 (H1)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_visual_slam_realsense.launch.py` ← 수정 (Phase3)
- `/home/jetson/isaac_ws_ros/analyze_axis_flip.py` ← 생성 (bag 분석 스크립트)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/src/impl/cuvslam_ros_conversion.cpp` (열람)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/rviz/realsense.cfg.rviz` (열람)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_common/scripts/run_dev.sh` (열람)
- `~/.isaac_ros_common-config` (열람)
- `/home/jetson/allan_ws/imu.yaml` (열람)
- `/home/jetson/isaac_ws_ros/axis_flip_capture/axis_flip_capture_0.db3` (사용자 녹화, 분석 입력)

### 1.1 수정된 파일 1 — visual_slam_impl.cpp (cuVSLAM IMU 등록)
- 경로: `.../src/impl/visual_slam_impl.cpp` 688행 부근
- 변경: `CUVSLAM_RegisterImuMeasurement(cuvslam_handle, latest_ts, ...)` → `... imu_ts, ...` + 설명 주석. release-3.2 회귀(모든 IMU 샘플이 이미지 타임스탬프로 등록되던 결함)를 upstream main 동작으로 백포트.

### 1.2 수정된 파일 2 — isaac_ros_visual_slam_realsense.launch.py (Phase3)
- 경로: `.../launch/isaac_ros_visual_slam_realsense.launch.py`
- 변경: `depth_module.profile`·`unite_imu_method` 를 런치 인자로 승격(A/B 용이). 기본값 `stereo_profile=640x360x30`, `unite_imu_method=1`(copy), `jitter_threshold_ms` 33.0→50.0.

### 1.3 생성된 파일 — analyze_axis_flip.py
- 경로: `/home/jetson/isaac_ws_ros/analyze_axis_flip.py`
- 내용: rosbag2(sqlite3) 파싱 → odom 쿼터니언→RPY, IMU dt/rate/samples-per-frame, gravity 마커 틸트, accel.z↔pitch 상관, 속도 정합성 판별, Velocity 마커 거동.

## 2. 관찰 기준 요약
| 기준 | 실측 (Phase3 패치본) |
|---|---|
| `/camera/imu` rate | 199.7 Hz, dt std 0.002 ms, 드롭 0 |
| IMU samples / stereo frame | 6.7 (cuVSLAM 권장 6~7) |
| extrinsic TF camera_link→camera_imu_optical_frame | trans[-0.016,-0.030,0.007], RPY[-90,0,-90]° (non-identity, 정상) |
| gravity 정렬 | 확인됨, base z=-9.79, \|g\|=9.81 |
| odom 안정(모니터) | 82 s, jump 누적 1, max 3.1 m/s |
| bag 최대 위치 점프속도 | 0.53 m/s (글리치 없음) |
| COLCON_EXIT | 0 |

---

## 3. 추가 (세션 연속): 이동 발산 분석 및 imu_noise_scale 해결

### 3.0 거리누적 발산 분석 (analyze_drift.py 생성 후 실행)
```bash
docker exec -u admin isaac_ros_dev-aarch64-container bash -lc '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash 2>/dev/null
python3 /workspaces/isaac_ros-dev/analyze_drift.py
'
```
목적: 새 bag(이동 시나리오)의 점진 발산 원인 분석 — 궤적/속도/gravity tilt/IMU 실가속·자이로.
결과(scale=1):
```
시작점서 최대거리=135.1m  net=101.2m  총경로=390.5m
twist |v| max=429.8 m/s  위치미분속도 max=3749 m/s
gravity tilt mean=38.1° max=89.8°  (|g| 9.81 유지)
DISCRIMINATOR 발산구간: IMU |accel| mean=9.73, 실가속분 ||a|-9.81| mean=0.20 max=2.43 m/s^2
→ 실가속 거의 0인데 odom 수백 m/s = 자세발산 중력누설 '환상속도'(실모션 아님)
```

### 3.1 imu_noise_scale=5 재검증
```bash
# (사용자) ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py enable_imu_fusion:=true imu_noise_scale:=5.0
# 같은 이동 재현 후 bag 재녹화 → 동일 스크립트로 분석
docker exec -u admin isaac_ros_dev-aarch64-container bash -lc '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash 2>/dev/null
python3 /workspaces/isaac_ros-dev/analyze_drift.py
'
```
결과(scale=5):
```
시작점서 최대거리=1.67m  net=0.68m  총경로=14.3m
twist |v| max=1.09 m/s  gravity tilt max=41.8°
→ 런어웨이 소멸 (IMU 과신이 원인임을 확정)
```

### 3.2 launch 기본값 변경 + 캘리브 도구 가용성 확인
```bash
cd /home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch && python3 -m py_compile isaac_ros_visual_slam_realsense.launch.py && echo "PY_SYNTAX_OK"
docker exec -u admin isaac_ros_dev-aarch64-container bash -lc '
echo "--- rs-imu-calibration ---"; which rs-imu-calibration 2>/dev/null; find / -name "rs-imu-calibration*" 2>/dev/null | head -3
echo "--- librealsense tools ---"; which rs-enumerate-devices rs-motion 2>/dev/null
echo "--- Kalibr ---"; ros2 pkg list 2>/dev/null | grep -i kalibr || echo "kalibr: 미설치"
'
```
목적: imu_noise_scale 기본값 1.0→5.0 변경 검증 + 캘리브 도구 확인.
결과: `PY_SYNTAX_OK`. `rs-enumerate-devices`/`rs-motion` 있음, **`rs-imu-calibration`·Kalibr 미설치**.

### 3.3 추가 수정/생성 파일
- `.../launch/isaac_ros_visual_slam_realsense.launch.py` ← 수정 (imu_noise_scale 기본 1.0→5.0)
- `/home/jetson/isaac_ws_ros/analyze_drift.py` ← 생성 (궤적/발산 분석 스크립트)
