# 진행과정 2026-03-30: nvblox ↔ Nav2 연동 + cuVSLAM 오도메트리 진단 + 설정 테스트

## 1. 목표

### 1.1 사용자 요청
> "nvblox가 nav2와 slam과 연동되도록 하고 싶다."

nvblox가 생성하는 3D ESDF 맵 슬라이스(`static_map_slice`)를 Nav2 costmap에 반영하여,
Nav2 경로 계획 및 장애물 회피에 nvblox 3D 장애물 정보가 활용되도록 연동.

### 1.2 기존 상태 (이전 세션 요약)
- 03-26: nvblox_msgs 심볼릭 링크 수정, Foxglove whitelist 최적화 (21.7→0.1 MB/s)
- 03-27: nvblox Foxglove extension 기반 3D 시각화 완성 (mesh + color_layer)
- Docker: cuVSLAM + nvblox 실행 (`--network host`)
- Host: Nav2 + EKF + SLAM Toolbox + Foxglove 실행

---

## 2. 현재 설정 분석

### 2.1 nvblox costmap 플러그인 — 이미 구성됨
소스 분석 결과, `nav2_params_fused.yaml`에 `nvblox::nav2::NvbloxCostmapLayer` 플러그인이 **이미 구성**되어 있음을 확인.

플러그인 라이브러리: `/home/jetson/isaac_ws_ros/install/nvblox_nav2/lib/libnvblox_nav2.so` (926KB, 2026-03-23 빌드)

**local_costmap 설정**:
```yaml
plugins: ["obstacle_layer", "nvblox_layer", "inflation_layer"]
nvblox_layer:
  plugin: "nvblox::nav2::NvbloxCostmapLayer"
  enabled: True
  nav2_costmap_global_frame: "odom"
  nvblox_map_slice_topic: "/nvblox_node/static_map_slice"
  convert_to_binary_costmap: True
  max_obstacle_distance: 1.0
  inflation_distance: 0.5
```

**global_costmap 설정**:
```yaml
plugins: ["static_layer", "obstacle_layer", "nvblox_layer", "inflation_layer"]
nvblox_layer:
  plugin: "nvblox::nav2::NvbloxCostmapLayer"
  enabled: True
  nav2_costmap_global_frame: "map"
  nvblox_map_slice_topic: "/nvblox_node/static_map_slice"
  convert_to_binary_costmap: True
  max_obstacle_distance: 1.0
  inflation_distance: 0.5
```

### 2.2 전제 조건 확인

| 항목 | 상태 | 세부 |
|------|------|------|
| `libnvblox_nav2.so` | ✅ 존재 | 926504 bytes, Mar 23 빌드 |
| `AMENT_PREFIX_PATH` | ✅ 포함 | `nvblox_nav2` 경로 3회 등장 |
| `nvblox_msgs/msg/DistanceMapSlice` | ✅ Host 사용 가능 | `ros2 interface show` 성공 |
| `/nvblox_node/static_map_slice` | ✅ 발행 중 | 9.1Hz, 136×128, resolution 0.05, frame_id=odom |
| TF chain: map→odom→base_footprint | ✅ 정상 | SLAM Toolbox → EKF → URDF |

### 2.3 결론
**코드 수정 없이** 기존 설정으로 nvblox↔Nav2 연동이 가능한 상태.
Nav2 navigation stack만 실행하면 nvblox_layer가 자동 로드됨.

---

## 3. Nav2 실행 및 연동 확인

### 3.1 Nav2 navigation stack 실행
```bash
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```
`navigation_launch.py` + `goal_pose_restamper` 실행.

### 3.2 노드 활성화 확인
12개 Nav2 노드 모두 `active [3]` 상태:
- `/controller_server`, `/planner_server`, `/bt_navigator`
- `/local_costmap/local_costmap`, `/global_costmap/global_costmap`
- `/behavior_server`, `/smoother_server`, `/velocity_smoother`, `/waypoint_follower`

### 3.3 nvblox_layer 로드 확인
```
local_costmap plugins: ['obstacle_layer', 'nvblox_layer', 'inflation_layer']
global_costmap plugins: ['static_layer', 'obstacle_layer', 'nvblox_layer', 'inflation_layer']
```
양쪽 costmap 모두 nvblox_layer를 정상 로드.

### 3.4 static_map_slice 구독 확인
```
/nvblox_node/static_map_slice → Subscription count: 2
```
local_costmap + global_costmap이 모두 구독 중.

---

## 4. costmap 발행 상태 분석

### 4.1 local_costmap
| 토픽 | 상태 | 주파수 |
|------|------|--------|
| `costmap_raw` | ✅ 발행 | ~1.7 Hz |
| `costmap_updates` | ✅ 발행 | ~1.7 Hz (TRANSIENT_LOCAL QoS) |
| `published_footprint` | ✅ 발행 | ~5 Hz |
| `costmap` (full) | 미발행 | `always_send_full_costmap: False` (정상) |

### 4.2 global_costmap
| 토픽 | 상태 | 주파수 |
|------|------|--------|
| `costmap_raw` | ✅ 발행 | ~0.4 Hz |
| `costmap_updates` | ⚠️ 간헐적 | ~0.4 Hz (TRANSIENT_LOCAL, publish_frequency=0.5) |
| `published_footprint` | ✅ 발행 | ~1 Hz |
| `costmap` (full) | 미발행 | `always_send_full_costmap: False` (정상) |

### 4.3 발견: global_costmap TF 오류
Nav2 실행 로그에서 **반복적 TF 경고** 발생:
```
[WARN] [global_costmap.global_costmap]: [NvbloxCostmapLayer] Can't transform: map to odom.
Error: Lookup would require extrapolation into the future.
Requested time 1774848199.928240 but the latest data is at time 1774848199.921837,
when looking up transform from frame [odom] to frame [map]
```
- 약 6~30ms의 시간차이
- nvblox static_map_slice의 timestamp가 SLAM Toolbox의 map→odom TF timestamp보다 미래값
- Docker nvblox → Host SLAM Toolbox 간의 clock 동기화 차이로 인한 문제
- **local_costmap은 odom→base_footprint TF만 필요하므로 영향 없음** (EKF가 빠르게 발행)
- **global_costmap은 map→odom TF 필요** (SLAM Toolbox 발행) → 간헐적 실패

---

## 5. TF 오류 수정

### 5.1 원인 분석
`NvbloxCostmapLayer` 소스 코드 (`nvblox_costmap_layer.cpp` L250):
```cpp
T_G_S_msg = tf_buffer_->lookupTransform(
    nav2_costmap_global_frame_, slice_frame, timestamp).transform;
```
slice 메시지의 정확한 timestamp로 TF lookup → Docker clock skew 시 미래 시간 요청 발생.

### 5.2 수정 내용
`/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_nav2/src/nvblox_costmap_layer.cpp`:
```cpp
// 수정 전 (L250):
tf_buffer_->lookupTransform(nav2_costmap_global_frame_, slice_frame, timestamp).transform;

// 수정 후:
tf_buffer_->lookupTransform(nav2_costmap_global_frame_, slice_frame,
    tf2::TimePointZero).transform;
```
`tf2::TimePointZero`는 **최신 가용 TF**를 사용 → 미래 시간 extrapolation 오류 제거.

### 5.3 빌드 및 배포
```bash
# 권한 수정 (Docker에서 root로 빌드된 디렉토리)
sudo chown -R jetson:jetson /home/jetson/isaac_ws_ros/install/nvblox_nav2/ \
    /home/jetson/isaac_ws_ros/build/nvblox_nav2/

# 재빌드
cd /home/jetson/isaac_ws_ros
colcon build --packages-select nvblox_nav2 --symlink-install
# → Finished <<< nvblox_nav2 [36.4s]
```

symlink-install이므로 `install/nvblox_nav2/lib/libnvblox_nav2.so` → `build/nvblox_nav2/libnvblox_nav2.so` 심볼릭 링크.

### 5.4 Nav2 재시작 후 결과
```bash
# Nav2 프로세스 종료
pkill -f "nav2_fused_navigation.launch.py"
pkill -9 -f "controller_server|planner_server|behavior_server|bt_navigator|smoother_server|waypoint_follower|velocity_smoother|lifecycle_manager_navigation"

# 재시작 (rebuild된 라이브러리 포함)
source install/setup.bash
source /home/jetson/isaac_ws_ros/install/setup.bash
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```

**결과**:
- **TF 경고 완전 제거됨** — Nav2 로그에 NvbloxCostmapLayer 관련 경고 0건
- 모든 Nav2 노드 정상 `active [3]`
- local_costmap: footprint 5Hz, costmap_raw 1.7Hz
- global_costmap: footprint 1Hz, costmap_raw 0.4Hz

---

## 6. 연동 데이터 검증

### 6.1 costmap 장애물 데이터 분석

**local_costmap_raw** (60×60 cells):
- 128개 셀 샘플: 비어 있지 않음 (nonzero: 81, lethal≥253: 50, inflated: 31)
- 고유 값: [0, 109, 113, 118, 136, 140, 159, 169, 174, 179, 198, 221, 230, 241, 253, 254]
- **nvblox + LiDAR obstacle 데이터가 inflation과 함께 반영됨**

**global_costmap_raw** (25×34 cells):
- 128개 셀 샘플: 대부분 장애물 (nonzero: 128, lethal≥253: 124, inflated: 4)
- 고유 값: [198, 221, 230, 241, 253, 254]
- **static_layer(SLAM map) + obstacle_layer(LiDAR) + nvblox_layer(3D ESDF) 합산**

### 6.2 Python 직접 구독 검증
```python
# nav2_msgs/msg/Costmap 직접 구독 (RELIABLE/VOLATILE QoS)
# 15초 측정:
local_costmap_raw:  60×60, res=0.050, 26 msgs (1.72Hz)
global_costmap_raw: 25×34, res=0.050, 5 msgs (0.33Hz)
```

### 6.3 입출력 토픽 주파수 요약

| 토픽 | 주파수 | 비고 |
|------|--------|------|
| `/nvblox_node/static_map_slice` (입력) | 9.4 Hz | Docker nvblox → Host costmap |
| `/scan` (입력) | 10.2 Hz | LiDAR → obstacle_layer |
| `/map` (입력) | TRANSIENT_LOCAL | SLAM Toolbox → static_layer |
| `local_costmap/costmap_raw` (출력) | 1.7 Hz | 60×60, update_freq=5, publish_freq=2 |
| `global_costmap/costmap_raw` (출력) | 0.4 Hz | 25×34, update_freq=1, publish_freq=0.5 |
| `local_costmap/published_footprint` | 5.0 Hz | 로봇 footprint |
| `global_costmap/published_footprint` | 1.0 Hz | 로봇 footprint |

---

## 7. 아키텍처 요약

### 7.1 데이터 흐름
```
[Docker]                                    [Host]
D455F Camera                               SLAM Toolbox
  ↓ depth + color                            ↓ map→odom TF
cuVSLAM → pose                              ↓ /map topic
  ↓                                          ↓
nvblox_node ──/static_map_slice──→ NvbloxCostmapLayer
  │ (9.4Hz, DistanceMapSlice)         ↓ (tf2::TimePointZero)
  │                                    ↓
  │                             local_costmap = obstacle + nvblox + inflation
  │                             global_costmap = static + obstacle + nvblox + inflation
  │                                    ↓
  │                               Nav2 Planner → Controller → cmd_vel
  │
  └──/mesh, /color_layer──→ Foxglove (via nvblox extension)
```

### 7.2 TF Chain
```
map (SLAM Toolbox)
  └── odom (EKF fused: cuVSLAM + wheel + imu + rf2o)
       └── base_footprint (EKF)
            └── base_link (URDF +8cm Z)
                 ├── laser (URDF -3cm X, +13cm Z, 180° yaw)
                 └── camera_link → camera_depth_optical_frame
```

### 7.3 파라미터 정리

| 파라미터 | local_costmap | global_costmap |
|----------|---------------|----------------|
| global_frame | odom | map |
| update_frequency | 5.0 Hz | 1.0 Hz |
| publish_frequency | 2.0 Hz | 0.5 Hz |
| resolution | 0.05 m | 0.05 m |
| rolling_window | True | False |
| transform_tolerance | 0.5 s | 0.5 s |
| footprint | 36×24.5cm | 36×24.5cm |
| nvblox_layer.nav2_costmap_global_frame | odom | map |
| nvblox_layer.max_obstacle_distance | 1.0 m | 1.0 m |
| nvblox_layer.inflation_distance | 0.5 m | 0.5 m |
| nvblox_layer.convert_to_binary_costmap | True | True |

---

## 8. 수행한 변경 사항

### 8.1 코드 수정 (1건)
| 파일 | 변경 | 이유 |
|------|------|------|
| `isaac_ws_ros/src/isaac_ros_nvblox/nvblox_nav2/src/nvblox_costmap_layer.cpp` L250 | `timestamp` → `tf2::TimePointZero` | Docker/Host 간 clock skew로 인한 TF extrapolation 오류 제거 |

### 8.2 파일 권한 변경 (1건)
| 대상 | 변경 | 이유 |
|------|------|------|
| `isaac_ws_ros/install/nvblox_nav2/`, `build/nvblox_nav2/` | `root:root` → `jetson:jetson` | Docker 빌드 잔존 권한 → Host colcon build 허용 |

### 8.3 이전 설정 변경 없음
- `nav2_params_fused.yaml`: 수정 없음 (이미 nvblox_layer 구성 완료)
- Docker launch: 수정 없음
- Host launch: 수정 없음

---

## 9. 핵심 발견 사항

### 9.1 nvblox→Nav2 costmap 연동 — 이미 구성됨
- 이전 작업에서 `nav2_params_fused.yaml`에 nvblox_layer가 구성되어 있었으나 실제 연동을 검증한 적이 없었음
- 이번 세션에서 **Nav2 실행 + 장애물 데이터 검증**을 통해 연동이 실제로 작동함을 확인

### 9.2 `tf2::TimePointZero` 수정의 효과
- NvbloxCostmapLayer가 Docker의 message timestamp으로 TF lookup → 간헐적 미래 시간 extrapolation 오류
- `tf2::TimePointZero`(최신 가용 TF) 사용으로 **완전 해결**
- 실시간 시스템에서 정확한 시간 매칭보다 최신 TF 사용이 더 안정적
- 특히 Docker↔Host 간 멀티-프로세스 시스템에서 clock skew 불가피

### 9.3 costmap_updates vs costmap_raw
- `costmap_updates`: TRANSIENT_LOCAL QoS → `ros2 topic hz`로 측정 시 잡히지 않을 수 있음
- `costmap_raw`: nav2_msgs/Costmap 타입, VOLATILE QoS → 직접 구독으로 확인 가능
- 내부적으로 costmap은 **정상 업데이트** 중 — 경로 계획에는 문제 없음

---

## 10. 미해결 사항 / 향후 작업

### 10.1 실주행 내비게이션 테스트
- Foxglove 또는 rviz2에서 Goal Pose 설정 → Nav2 경로 생성 → 이동 확인
- nvblox 3D 장애물 감지 → costmap 반영 → 경로 우회 검증

### 10.2 global_costmap 크기 확장
- 현재 global_costmap: 25×34 cells (1.25×1.7m) — SLAM map 크기에 따라 자동 확장
- 넓은 환경에서 자동 확장 확인 필요

### 10.3 nvblox_layer 파라미터 튜닝
- `max_obstacle_distance: 1.0m` — 환경에 따라 조정 가능
- `inflation_distance: 0.5m` — Nav2 inflation_layer와 중복 가능성 검토
- `convert_to_binary_costmap: True` — False로 변경 시 연속 코스트값 사용 가능

### 10.4 통신 안정성 모니터링
- Docker→Host static_map_slice (9.4Hz) 장기 안정성 확인
- TF TimePointZero 수정 후 TF 경고 완전 소멸 확인 (장기)

---
---

# Part 2: cuVSLAM 오도메트리 문제 진단

## 11. 배경

### 11.1 문제 발생
nvblox ↔ Nav2 연동(Part 1) 완료 후 cuVSLAM 오도메트리 데이터를 분석한 결과,
VSLAM이 로봇의 실제 움직임을 **전혀 추적하지 못하는** 심각한 문제 발견.

### 11.2 CSV 데이터 기반 분석
Foxglove에서 Export한 CSV 데이터 (`plot_data_position_x.csv`, `plot_data.csv`)를 분석:

| 지표 | 정상 기대값 | VSLAM 실측 | 판정 |
|------|-----------|-----------|------|
| 전진 추적 (1.6m) | ~1.6m | 0.033m (2%) | ❌ 실패 |
| 회전 추적 (~130°) | ~130° | ~15° (12%) | ❌ 실패 |
| 초기화 시간 | <5초 | ~30초 | ⚠️ 비정상 |
| Frozen 발생 | 없음 | 3회 (최대 106 samples) | ❌ tracking lost |
| 큰 점프 | 없음 | 7회 (최대 Δ1.45m) | ❌ 재위치추정 실패 |
| 14초+ 데이터 공백 | 없음 | 2회 | ❌ 장기 추적 실패 |

### 11.3 근본 원인
**cuVSLAM 비주얼 추적 자체가 실패** — 프레임/좌표계 문제가 아닌 입력 이미지 품질 문제.

주요 원인 후보:
1. **emitter_on_off 모드**: emitter OFF IR 이미지에서 텍스처 부족 (가능성 높음)
2. **초기화 지연 30초**: tracker 생성에 비정상적으로 오래 걸림 (가능성 높음)
3. **image_jitter_threshold_ms**: 프레임 간격 불규칙으로 인한 jitter 경고 (가능성 중간)

### 11.4 상세 진단 보고서
→ [진단_VSLAM_오도메트리_2026_03_30.md](진단_VSLAM_오도메트리_2026_03_30.md)

---

## 12. 권장 설정 테스트 3건

진단 보고서에서 도출한 3가지 설정 변경 테스트:

| 테스트 | 변경사항 | 목적 |
|--------|---------|------|
| A | VSLAM-only, emitter OFF, 60Hz | emitter_on_off 비활성화 시 추적 성능 확인 |
| B | base_frame='base_link' | TF chain 변경 시 동작 차이 확인 |
| C | image_jitter_threshold_ms=100 | 느슨한 jitter 임계값이 추적에 미치는 영향 확인 |

---
---

# Part 3: cuVSLAM 설정 테스트 실행 및 결과

## 13. Test A: VSLAM-only (emitter OFF, 60Hz, nvblox 없음)

### 13.1 목적
emitter_on_off를 비활성화하고 **순수 IR 스테레오 60Hz**로 VSLAM만 실행.
nvblox 없이 cuVSLAM 추적 능력 자체를 검증하는 **기준선(baseline) 테스트**.

### 13.2 준비: Docker 프로세스 정리
기존 nvblox+VSLAM 프로세스를 정리하고, RealSense 카메라 리셋:
```bash
# Docker 내 기존 프로세스 종료
docker exec isaac_ros_dev-aarch64-container bash -c "
  pkill -f 'isaac_ros_vslam_nvblox_realsense' 2>/dev/null
  sleep 1
  pkill -f 'component_container' 2>/dev/null
  pkill -f 'realsense2_camera_node' 2>/dev/null
"
sleep 3

# RealSense USB 리셋 (카메라 상태 초기화)
python3 /home/jetson/reset_realsense.py
sleep 5
```

### 13.3 사용한 Launch 파일
`isaac_ros_visual_slam_realsense.launch.py` — VSLAM-only (nvblox 없음):
```python
# 주요 설정값
'enable_depth_module': True,
'depth_module.profile': '640x360x60',   # 60fps (emitter 교대 아닌 전량)
'emitter_enabled': 0,                   # emitter 항상 OFF
'enable_depth': False,                 # depth 맵 비활성화
'enable_color': False,                 # color 비활성화
'infra_width': 640,
'infra_height': 360,
'infra_fps': 60,                       # IR 스테레오 60Hz
'image_jitter_threshold_ms': 34.00,    # 60Hz(16.7ms) × 2배
'base_frame': 'camera_link',
'camera_frame': 'camera_link',
'enable_imu_fusion': True,
'publish_odom_to_base_tf': True,       # VSLAM-only이므로 TF 발행
```

### 13.4 실행
```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c "
  source /workspaces/isaac_ros-dev/install/setup.bash &&
  ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py
" 2>&1 | tee /home/jetson/isaac_ws_ros/test_A_vslam_only.log
```

### 13.5 오도메트리 데이터 수집 (10초)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
python3 -c \"
import rclpy
from rclpy.node import Node
from nav_msgs.msg import Odometry
import time, math

rclpy.init()
node = Node('test_a')
msgs = []
first_pos = None

def cb(msg):
    global first_pos
    p = msg.pose.pose.position
    if first_pos is None:
        first_pos = (p.x, p.y, p.z)
    msgs.append((time.time(), p.x, p.y, p.z))

node.create_subscription(Odometry, '/visual_slam/tracking/odometry', cb, 10)
start = time.time()
while time.time() - start < 10:
    rclpy.spin_once(node, timeout_sec=0.05)
elapsed = time.time() - start

if msgs:
    rate = len(msgs) / elapsed
    dx = msgs[-1][1] - msgs[0][1]
    dy = msgs[-1][2] - msgs[0][2]
    dz = msgs[-1][3] - msgs[0][3]
    disp = math.sqrt(dx*dx + dy*dy + dz*dz)
    frozen = sum(1 for i in range(1, len(msgs))
                 if abs(msgs[i][1]-msgs[i-1][1]) < 1e-6
                 and abs(msgs[i][2]-msgs[i-1][2]) < 1e-6)
    print(f'Msgs: {len(msgs)}, Rate: {rate:.1f} Hz, Displacement: {disp:.6f}m')
    print(f'Frozen frames: {frozen}/{len(msgs)-1}')
    print(f'First: ({msgs[0][1]:.6f}, {msgs[0][2]:.6f}, {msgs[0][3]:.6f})')
    print(f'Last:  ({msgs[-1][1]:.6f}, {msgs[-1][2]:.6f}, {msgs[-1][3]:.6f})')
else:
    print('No messages received')
rclpy.shutdown()
\"
"
```

### 13.6 결과

**핵심 수치:**
| 지표 | 값 | 평가 |
|------|-----|------|
| 발행 주파수 | **54 Hz** | ✅ 60fps 근접 (정상) |
| 10초 변위 | **0.04mm** | ✅ 노이즈 수준 (로봇 정지 상태) |
| Frozen 프레임 | **0/540** | ✅ 없음 |
| 초기화 시간 | ~10초 | ⚠️ tracker 생성 9.8초 |
| IMU HW 에러 | 2건 | Motion Module force pause (시작 시, 비치명적) |

**Jitter 경고:**
- 임계값 34ms 초과 경고 **71건** 발생
- 최대 jitter: **351ms** (시작 시), 100ms, 66ms, 50ms
- 시작 시 큰 jitter 이후 안정화 → 대부분 50~66ms 범위

**결론: emitter_on_off 없이 60Hz IR로 VSLAM을 실행하면 추적이 정상적으로 작동함.**
Jitter 경고가 있지만 추적에는 영향 없음 (54Hz 유지, frozen 없음).

### 13.7 로그 저장
```bash
# 로그는 실행 시 tee로 자동 저장됨
# /home/jetson/isaac_ws_ros/test_A_vslam_only.log
```

---

## 14. Test B: base_frame='base_link' (emitter_on_off, nvblox)

### 14.1 목적
cuVSLAM의 `base_frame`을 `'camera_link'`에서 `'base_link'`로 변경.
TF chain 구성이 VSLAM 동작에 미치는 영향 확인.

### 14.2 Launch 파일 수정
`isaac_ros_vslam_nvblox_realsense.launch.py` 변경:
```python
# 수정 전:
'base_frame': 'camera_link',

# 수정 후 (TEST B):
'base_frame': 'base_link',
```

### 14.3 준비
```bash
# Test A 프로세스 종료
docker exec isaac_ros_dev-aarch64-container bash -c "
  pkill -f 'isaac_ros_visual_slam_realsense' 2>/dev/null
  sleep 1
  pkill -f 'component_container' 2>/dev/null
  pkill -f 'realsense2_camera_node' 2>/dev/null
"
sleep 3

# RealSense 리셋
python3 /home/jetson/reset_realsense.py
sleep 5
```

### 14.4 실행
```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c "
  source /workspaces/isaac_ros-dev/install/setup.bash &&
  ros2 launch isaac_ros_visual_slam isaac_ros_vslam_nvblox_realsense.launch.py
" 2>&1 | tee /home/jetson/isaac_ws_ros/test_B_base_link.log
```

### 14.5 오도메트리 데이터 수집 시도 (10초)
동일한 Python 스크립트로 수집 시도:
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
python3 -c \"
import rclpy
from rclpy.node import Node
from nav_msgs.msg import Odometry
import time, math
# (Test A와 동일한 수집 스크립트)
...
\"
"
```

### 14.6 결과: ❌ FAIL — 오도메트리 미발행

| 지표 | 값 | 평가 |
|------|-----|------|
| 발행 주파수 | **0 Hz** | ❌ 메시지 없음 |
| cuVSLAM 초기화 | tracker 생성 0.33초 | ✅ 빠른 초기화 |
| nvblox 상태 | "couldn't look up frame: camera_link" | ❌ TF 실패 |

**cuVSLAM 로그:**
```
[visual_slam_node]: cuVSLAM tracker was created
```
- tracker는 정상 생성 (0.33초 — Test A보다 훨씬 빠름)
- 하지만 **오도메트리 토픽에 메시지가 발행되지 않음**

**nvblox 로그:**
```
[nvblox_node]: Transformer - lookTransform failed from frame 'camera_link' for sensor with frame_id 'camera_link'. Error is: "camera_link" passed to lookupTransform argument target_frame does not exist.
```
- nvblox가 camera_link TF를 찾지 못함

**원인 분석:**
- `base_frame='base_link'`로 변경 시, cuVSLAM은 `odom→base_link` TF를 발행해야 함
- 그런데 `publish_odom_to_base_tf=False` (프로덕션 설정)이므로 TF 미발행
- 결과적으로 TF tree에 `camera_link`이 **odom 아래에 존재하지 않게 됨**
- 닭과 달걀 문제: base_link TF를 발행하려면 VSLAM이 작동해야 하고, VSLAM이 작동하려면 TF가 필요

**Static TF는 정상:**
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
  ros2 topic echo /tf_static --once --no-arr 2>/dev/null | head -30
"
```
```
base_link → camera_link: translation(0.274, 0, 0.055)
```
- base_link→camera_link static TF는 존재하지만 odom→base_link dynamic TF가 없어 chain 단절

### 14.7 로그 저장
```bash
# /home/jetson/isaac_ws_ros/test_B_base_link.log
```

---

## 15. Test C: image_jitter_threshold_ms=100 (emitter_on_off, nvblox)

### 15.1 목적
`image_jitter_threshold_ms`를 68ms→100ms로 느슨하게 변경.
emitter_on_off 모드에서 프레임 간격 불규칙이 VSLAM에 미치는 영향 확인.

### 15.2 Launch 파일 수정
`isaac_ros_vslam_nvblox_realsense.launch.py` 변경:
```python
# base_frame을 원래대로 복원:
'base_frame': 'camera_link',

# jitter threshold 변경:
# 수정 전:
'image_jitter_threshold_ms': 68.00,  # ⭐ 34→68ms: 30Hz(33ms) × 2배 마진

# 수정 후 (TEST C):
'image_jitter_threshold_ms': 100.00,  # TEST C: 68→100ms (더 느슨한 임계값)
```

### 15.3 준비
```bash
# Test B 프로세스 종료
docker exec isaac_ros_dev-aarch64-container bash -c "
  pkill -f 'isaac_ros_vslam_nvblox_realsense' 2>/dev/null
  sleep 1
  pkill -f 'component_container' 2>/dev/null
  pkill -f 'realsense2_camera_node' 2>/dev/null
"
sleep 3

# RealSense 리셋
python3 /home/jetson/reset_realsense.py
sleep 5
```

### 15.4 실행
```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c "
  source /workspaces/isaac_ros-dev/install/setup.bash &&
  ros2 launch isaac_ros_visual_slam isaac_ros_vslam_nvblox_realsense.launch.py
" 2>&1 | tee /tmp/test_C_jitter100.log
```
(로그는 이후 `/workspaces/isaac_ros-dev/test_C_jitter100.log`로 복사)

### 15.5 오도메트리 데이터 수집 (10초)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
python3 -c \"
import rclpy
from rclpy.node import Node
from nav_msgs.msg import Odometry
import time, math

rclpy.init()
node = Node('test_c')
msgs = []
first_pos = None

def cb(msg):
    global first_pos
    p = msg.pose.pose.position
    if first_pos is None:
        first_pos = (p.x, p.y, p.z)
    msgs.append((time.time(), p.x, p.y, p.z))

node.create_subscription(Odometry, '/visual_slam/tracking/odometry', cb, 10)
start = time.time()
while time.time() - start < 10:
    rclpy.spin_once(node, timeout_sec=0.05)
elapsed = time.time() - start

if msgs:
    rate = len(msgs) / elapsed
    dx = msgs[-1][1] - msgs[0][1]
    dy = msgs[-1][2] - msgs[0][2]
    dz = msgs[-1][3] - msgs[0][3]
    disp = math.sqrt(dx*dx + dy*dy + dz*dz)
    frozen = sum(1 for i in range(1, len(msgs))
                 if abs(msgs[i][1]-msgs[i-1][1]) < 1e-6
                 and abs(msgs[i][2]-msgs[i-1][2]) < 1e-6)
    max_jump = max(
        math.sqrt((msgs[i][1]-msgs[i-1][1])**2 + (msgs[i][2]-msgs[i-1][2])**2)
        for i in range(1, len(msgs))
    )
    print(f'Msgs: {len(msgs)}, Rate: {rate:.1f} Hz, Displacement: {disp:.6f}m')
    print(f'Frozen frames: {frozen}/{len(msgs)-1}')
    print(f'Max inter-frame jump: {max_jump:.6f}m')
    print(f'First: ({msgs[0][1]:.6f}, {msgs[0][2]:.6f}, {msgs[0][3]:.6f})')
    print(f'Last:  ({msgs[-1][1]:.6f}, {msgs[-1][2]:.6f}, {msgs[-1][3]:.6f})')
else:
    print('No messages received')
rclpy.shutdown()
\"
"
```

### 15.6 결과: ⚠️ 작동하지만 심각하게 열화

| 지표 | 값 | Test A 대비 | 평가 |
|------|-----|-----------|------|
| 발행 주파수 | **25.2 Hz** | 54→25 Hz (54% 감소) | ⚠️ 열화 |
| 수신 메시지 | 247개/10초 | 540→247 (54% 감소) | ⚠️ 열화 |
| 10초 변위 | **0.5mm** | 0.04→0.5mm | ⚠️ 미세 drift |
| Frozen 프레임 | **158/246 (64%)** | 0%→64% | ❌ 심각 |
| 최대 프레임간 점프 | **0.28mm** | - | ✅ 작음 |
| IMU HW 에러 | 2건 | 동일 | - |

**Jitter 경고 분석:**
```bash
# 경고 수
docker exec isaac_ros_dev-aarch64-container bash -c "
  grep -c 'Delta between' /workspaces/isaac_ros-dev/test_C_jitter100.log
"
# → 114건

# 최대 jitter 값 (ms)
docker exec isaac_ros_dev-aarch64-container bash -c "
  grep -oP 'Delta between current and previous frame \[\K[0-9.]+' \
    /workspaces/isaac_ros-dev/test_C_jitter100.log | sort -rn | head -5
"
# → 354.854144, 233.677312, 206.473216, 200.528896, 200.491776

# 최소 jitter 값 (ms, 100ms 초과만 기록됨)
docker exec isaac_ros_dev-aarch64-container bash -c "
  grep -oP 'Delta between current and previous frame \[\K[0-9.]+' \
    /workspaces/isaac_ros-dev/test_C_jitter100.log | sort -n | head -5
"
# → 100.144896, 100.149248, 100.149504, 100.150272, 100.155648
```

- 100ms 임계값에서도 **114건** 경고 발생 (68ms 기본값에서 96건이었던 이전 측정 대비 증가 — 로그 시간이 더 길어서)
- 최대 jitter: **354ms** — emitter_on_off 프레임 전환으로 인한 극단적 불규칙
- 대부분 100~200ms 범위에 분포

**결론:**
- `image_jitter_threshold_ms`를 68→100ms로 높여도 근본적 개선 없음
- emitter_on_off 모드 자체가 **최대 354ms**의 프레임 간격 불규칙을 유발
- 64% frozen 프레임 → cuVSLAM이 절반 이상의 프레임에서 새로운 pose를 계산하지 못함
- 이 파라미터는 "경고 기준"일 뿐, 실제 추적 성능의 근본 원인이 아님

### 15.7 로그 저장
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
  cp /tmp/test_C_jitter100.log /workspaces/isaac_ros-dev/
"
# → /home/jetson/isaac_ws_ros/test_C_jitter100.log
```

---

## 16. 설정 테스트 종합 비교

### 16.1 결과 요약표

| 테스트 | 구성 | 발행 주파수 | Frozen | Jitter 경고 | 최대 Jitter | 결과 |
|--------|------|-----------|--------|------------|------------|------|
| **A: VSLAM-only 60Hz** | emitter OFF, threshold=34ms | **54 Hz** | **0%** | 71건 (max 351ms) | 351ms | ✅ 정상 작동 |
| **B: base_frame=base_link** | emitter_on_off, threshold=68ms | **0 Hz** | N/A | 0건 | N/A | ❌ 오도메트리 미발행 |
| **C: jitter=100ms** | emitter_on_off, threshold=100ms | **25 Hz** | **64%** | 114건 (max 354ms) | 354ms | ⚠️ 심각 열화 |

### 16.2 핵심 발견

#### emitter_on_off가 근본 원인
- **Test A** (emitter OFF, 60Hz): 54Hz로 정상 추적, frozen 0%
- **Test C** (emitter_on_off, 30Hz 실효): 25Hz, frozen 64%
- **결론**: emitter_on_off 모드가 IR 프레임 전달 간격을 극도로 불규칙하게 만들어 cuVSLAM 추적을 방해

#### emitter_on_off의 구체적 문제
1. **실효 프레임률 저하**: 60fps 중 절반만 VSLAM용 (emitter OFF) → 30fps
2. **프레임 간격 불규칙**: emitter ON/OFF 교대로 인한 비균일 타이밍
3. **최대 354ms 지연**: 카메라 내부 버퍼/동기화 문제로 간헐적 극단 지연
4. **Splitter 오버헤드**: RealsenseSplitterNode가 프레임을 분리하는 과정에서 추가 지연

#### base_frame 설정의 함정
- `base_frame='base_link'` + `publish_odom_to_base_tf=False` = **TF 닭과 달걀 문제**
- tracker 자체는 빠르게 생성되지만 (0.33초 vs Test A의 9.8초) 오도메트리 발행 불가
- 이 조합은 사용 불가능

### 16.3 Launch 파일 원상복구
테스트 후 `isaac_ros_vslam_nvblox_realsense.launch.py`를 원래 설정으로 복원:
```python
'image_jitter_threshold_ms': 68.00,  # ⭐ 34→68ms: 30Hz(33ms) × 2배 마진
'base_frame': 'camera_link',
```

---

## 17. 향후 작업 방향

### 17.1 근본 해결: emitter 모드 재설계 필요
Test A의 결과가 증명하듯, emitter OFF 60Hz에서 cuVSLAM은 정상 작동.
문제는 nvblox가 depth(emitter ON)를 필요로 하므로 emitter_on_off가 불가피하다는 점.

**가능한 접근:**
1. **듀얼 카메라 구성**: VSLAM용 카메라(emitter OFF) + nvblox용 카메라(emitter ON) 분리
2. **Software depth 보간**: emitter OFF 프레임에서 이전 depth 맵을 재사용
3. **nvblox 업데이트 주기 낮추기**: depth 프레임을 간격을 두고 수집, IR은 매 프레임 VSLAM 전달
4. **cuVSLAM 대안**: emitter_on_off에 더 강건한 VIO 사용 (ORB-SLAM3, RTAB-Map 등)

### 17.2 단기 개선
- cuVSLAM `image_jitter_threshold_ms` 를 매우 높은 값(500ms)으로 설정하여 경고 억제
- RealsenseSplitter의 프레임 버퍼링/타이밍 최적화 검토
- emitter 교대 주기를 2:1 (OFF:ON = 2:1)로 변경하여 VSLAM 프레임 비율 증가

### 17.3 이전 미해결 사항 (Part 1)
- 실주행 내비게이션 테스트 (nvblox障害물 → Nav2 경로 우회)
- global_costmap 크기 확장 확인
- nvblox_layer 파라미터 튜닝
