# 진단 명령어 2026-03-27: Foxglove nvblox 시각화 — mesh 렌더링 실패 + 대역폭 최적화

## 배경
- 2026-03-26에 nvblox_msgs 심볼릭 링크 문제 해결 및 Foxglove whitelist 초기 최적화 완료
- 2026-03-27에 Docker launch 실행 후 Foxglove에서 실제 시각화 검증 시작

---

## Phase 1: mesh 토픽 렌더링 문제 진단

### 1.1 Docker launch 실행 후 Foxglove 오류 확인
**사용자 보고**:
- `PointCloud2 data length 0 is not a multiple of point_step 0` 오류
- `/nvblox_node/mesh` 토픽이 Foxglove에 도달하지만 "matches no data" 표시

### 1.2 nvblox 토픽 퍼블리시 주파수 확인
```bash
ros2 topic hz /nvblox_node/mesh --window 5 &
ros2 topic hz /nvblox_node/static_esdf_pointcloud --window 5 &
sleep 6; kill $PID1 $PID2
```
**목적**: mesh와 static_esdf_pointcloud 토픽의 실제 발행 빈도 확인
**결과**:
```
/nvblox_node/mesh: WARNING: topic does not appear to be published yet
/nvblox_node/static_esdf_pointcloud: average rate: 9.451 (min: 0.091s max: 0.122s)
                                     average rate: 9.544 (min: 0.100s max: 0.111s)
```
- mesh: Host에서 수신 불가 (Docker 발행 확인 필요)
- static_esdf_pointcloud: ~9.5Hz로 정상 발행

### 1.3 mesh 토픽 데이터 수신 시도
```bash
ros2 topic echo /nvblox_node/mesh --once
```
**목적**: mesh 메시지 실제 수신 확인
**결과**:
```
WARNING: topic [/nvblox_node/mesh] does not appear to be published yet
Could not determine the type for the passed topic
```
- Host에서 mesh 토픽 구독 시 타입 결정 실패

### 1.4 nvblox 관련 전체 토픽 목록 확인
```bash
ros2 topic list | grep nvblox
```
**목적**: Docker에서 발행 중인 nvblox 토픽 전체 목록 확인
**결과**:
```
/nvblox_node/back_projected_depth/camera_depth_optical_frame
/nvblox_node/color_layer
/nvblox_node/color_layer_marker
/nvblox_node/combined_occupancy_grid
/nvblox_node/dynamic_occupancy_grid
/nvblox_node/esdf_slice_bounds
/nvblox_node/mesh
/nvblox_node/pessimistic_static_esdf_pointcloud
/nvblox_node/pessimistic_static_map_slice
/nvblox_node/shapes_to_clear
/nvblox_node/static_esdf_pointcloud
/nvblox_node/static_map_slice
/nvblox_node/static_occupancy_grid
/nvblox_node/tsdf_layer
/nvblox_node/tsdf_layer_marker
/nvblox_node/workspace_bounds
```
- 총 16개 nvblox 토픽 발행 확인
- mesh 토픽 존재하나 Host 수신에 문제 있음

### 1.5 mesh 토픽 상세 정보 확인
```bash
ros2 topic info /nvblox_node/mesh
```
**목적**: mesh 토픽 타입, publisher/subscriber 수 확인
**결과**:
```
Type: nvblox_msgs/msg/Mesh
Publisher count: 1
Subscription count: 1
```
- Publisher 1개 (Docker nvblox_node), Subscriber 1개 (Host foxglove_bridge)
- DDS 레벨에서 연결은 되어있음

### 1.6 mesh 토픽 메시지 구조 확인 (배열 제외)
```bash
ros2 topic echo /nvblox_node/mesh --once --no-arr
```
**목적**: mesh 메시지가 실제로 도착하는지 확인 (배열 데이터 제외)
**결과**: 타임아웃 — 메시지 미수신
- DDS discovery는 되었으나 실제 데이터가 Host까지 도달하지 않음

### 1.7 cuVSLAM 오도메트리 상태 확인
```bash
timeout 5 ros2 topic hz /visual_slam/tracking/odometry --window 3
```
**목적**: nvblox에 pose를 제공하는 cuVSLAM이 정상 동작 중인지 확인
**결과**:
```
average rate: 28.098 (min: 0.031s max: 0.042s)
average rate: 30.134 (min: 0.032s max: 0.035s)
```
- cuVSLAM 정상 ~30Hz 발행 중

### 1.8 static_esdf_pointcloud 메시지 구조 확인
```bash
timeout 8 ros2 topic echo /nvblox_node/static_esdf_pointcloud --once --no-arr
```
**목적**: PointCloud2 메시지의 실제 구조 확인 (PointCloud2 data length 0 오류 원인 조사)
**결과**:
```yaml
header:
  stamp: {sec: 1774594709, nanosec: 510504937}
  frame_id: odom
height: 1
width: 3417
fields: '<sequence type: sensor_msgs/msg/PointField, length: 4>'
is_bigendian: false
point_step: 16
row_step: 54672
data: '<sequence type: uint8, length: 54672>'
is_dense: false
```
- point_step=16, width=3417, data=54672 bytes → 정상 데이터
- PointCloud2 length 0 오류는 nvblox 시작 직후 맵이 비어있을 때 발생한 것으로 추정

### 1.9 실행 중인 nvblox 매개변수 확인
```bash
ros2 param get /nvblox_node update_mesh_rate_hz
ros2 param get /nvblox_node publish_layer_rate_hz
```
**목적**: Docker launch에서 설정한 매개변수 오버라이드가 실제 적용되었는지 확인
**결과**:
```
update_mesh_rate_hz: Double value is: 2.0
publish_layer_rate_hz: Double value is: 0.0
```
- 이전 세션(03-26)의 설정이 적용된 상태로 실행 중

### 1.10 mesh 관련 전체 매개변수 탐색
```bash
ros2 param list /nvblox_node | grep -i mesh
```
**목적**: mesh 발행에 영향을 주는 매개변수 전체 확인
**결과**:
```
dynamic_mapper.mesh_integrator_min_weight
dynamic_mapper.mesh_integrator_weld_vertices
static_mapper.mesh_integrator_min_weight
static_mapper.mesh_integrator_weld_vertices
update_mesh_rate_hz
```
- `update_mesh_rate_hz`만이 mesh 발행 주파수 제어

### 1.11 퍼블리시 관련 전체 매개변수 탐색
```bash
ros2 param list /nvblox_node | grep -i 'publish\|output'
```
**목적**: 퍼블리시 제어 관련 매개변수 전체 확인
**결과**:
```
output_pessimistic_distance_map
publish_debug_vis_rate_hz
publish_esdf_distance_slice
publish_layer_rate_hz
```

### 1.12 mesh 토픽 QoS 프로파일 확인
```bash
ros2 topic info /nvblox_node/mesh --verbose
```
**목적**: Publisher(Docker)와 Subscriber(Host foxglove_bridge) 간 QoS 호환성 확인
**결과**:
```
Publisher (nvblox_node):
  Reliability: RELIABLE
  Durability: VOLATILE
  Lifespan: Infinite

Subscription (foxglove_bridge):
  Reliability: RELIABLE
  Durability: VOLATILE
  Lifespan: Infinite
```
- QoS 완전 일치 — QoS 미스매치가 아님
- mesh 데이터 미수신 원인은 DDS 대형 메시지 전송 문제로 추정

### 1.13 color_layer_marker 토픽 타입 확인
```bash
ros2 topic info /nvblox_node/color_layer_marker
ros2 topic info /nvblox_node/mesh_marker
```
**목적**: Foxglove에서 표준 메시지로 시각화 가능한 대안 토픽 탐색
**결과**:
```
/nvblox_node/color_layer_marker:
  Type: visualization_msgs/msg/Marker
  Publisher count: 1
  Subscription count: 0

/nvblox_node/mesh_marker:
  Unknown topic (존재하지 않음)
```
- `color_layer_marker`는 표준 Marker 타입 → Foxglove 기본 렌더러 사용 가능

---

## Phase 2: mesh "matches no data" 원인 분석

### 2.1 원인 확인
- `/nvblox_node/mesh` 타입: `nvblox_msgs/msg/Mesh` (NVIDIA 커스텀 메시지)
- Foxglove 기본 3D Panel은 커스텀 메시지 렌더링 불가
- Foxglove에 nvblox extension을 설치해야 `nvblox_msgs/msg/Mesh` → `foxglove.SceneUpdate` 변환 가능
- **이 시점에서 사용자는 아직 nvblox Foxglove extension 미설치 상태**

### 2.2 초기 대안: color_layer_marker 사용
- `visualization_msgs/msg/Marker` 타입 → Foxglove 기본 렌더링 가능
- `publish_layer_rate_hz`를 0.0→0.5로 변경 필요 (layer 발행 활성화)

---

## Phase 3: mesh 대신 color_layer_marker 사용 시도 (1차 수정)

### 3.1 Docker launch 수정
```bash
# 파일: isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/
#       isaac_ros_vslam_nvblox_realsense.launch.py
```
**변경 내용**:
```python
# 변경 전:
'publish_layer_rate_hz': 0.0,       # layer 토픽 발행 중지 (Host 미구독)
'publish_debug_vis_rate_hz': 2.0,    # ESDF pointcloud 발행 유지 (2Hz)
'update_mesh_rate_hz': 2.0,          # mesh 발행 2Hz (374KB×2=0.75MB/s)

# 변경 후:
'publish_layer_rate_hz': 0.5,       # layer+marker 0.5Hz (color_layer_marker용)
'publish_debug_vis_rate_hz': 2.0,    # ESDF pointcloud 발행 유지 (2Hz)
'update_mesh_rate_hz': 2.0,          # mesh 내부 갱신 유지 (costmap용)
```

### 3.2 Host launch Foxglove whitelist 수정
```bash
# 파일: jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
```
**변경 내용**:
```python
# 변경 전:
'/nvblox_node/mesh',                    # 3D 복셀 메쉬 (374KB×2Hz=0.75MB/s)
'/nvblox_node/static_esdf_pointcloud',  # 2D ESDF 포인트클라우드 (58KB×2Hz=0.1MB/s)
'/nvblox_node/esdf_slice_bounds',       # ESDF 범위 마커 (0.5KB×2Hz≈0)

# 변경 후:
'/nvblox_node/color_layer_marker',      # 3D 복셀 시각화 (Marker, ~600KB×0.5Hz=0.3MB/s)
'/nvblox_node/static_esdf_pointcloud',  # 2D ESDF 포인트클라우드 (58KB×2Hz=0.1MB/s)
'/nvblox_node/esdf_slice_bounds',       # ESDF 범위 마커 (0.5KB×2Hz≈0)
```

### 3.3 Host 패키지 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
source install/setup.bash
colcon build --packages-select jupiter_nav_VO --symlink-install
```
**결과**: `Finished <<< jupiter_nav_VO [5.81s]` 성공

---

## Phase 4: nvblox Foxglove extension 발견 및 분석

### 4.1 nvblox 패키지 내 Foxglove 관련 파이썬 스크립트 검색
```bash
find /home/jetson/isaac_ws_ros -path '*/nvblox*' -name '*.py' \
  | xargs grep -li -i 'foxglove\|websocket\|extension'
```
**목적**: nvblox 패키지에 Foxglove 관련 유틸리티가 있는지 확인
**결과**:
```
.../nvblox_examples_bringup/launch/realsense_example.launch.py
.../nvblox_examples_bringup/launch/visualization/foxglove_bridge.launch.py
.../nvblox_examples_bringup/launch/visualization/visualization.launch.py
.../nvblox_ros/nvblox_core/python/scripts/nvblox_visualize_voxel_grid/__main__.py
```

### 4.2 NVIDIA 공식 Foxglove bridge launch 파일 확인
```bash
cat /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/launch/visualization/foxglove_bridge.launch.py
```
**목적**: NVIDIA가 권장하는 Foxglove bridge 설정 및 whitelist 확인
**결과**: NVIDIA 공식 whitelist:
```python
TOPIC_WHITELIST = [
    '/nvblox_node/combined_esdf_pointcloud',
    '/nvblox_node/.*_layer',
    '/nvblox_node/esdf_slice_bounds',
    '/nvblox_node/mesh',
    '/nvblox_node/static_esdf_pointcloud',
    '/nvblox_node/human_voxels',
    '/tf', '/tf_static',
    '/visual_slam/status',
    '/visual_slam/tracking/odometry',
    '/visual_slam/tracking/slam_path',
    '/visual_slam/tracking/vo_path',
    '/visual_slam/tracking/vo_pose',
    '/visual_slam/vis/landmarks_cloud',
    '/visual_slam/vis/observations_cloud',
]
```
- 주요 설정: `send_buffer_limit: 10000000`, `max_qos_depth: 1`, `use_compression: False`
- `--log-level ERROR` 사용 (send_buffer_limit 경고 억제)

### 4.3 NVIDIA 시각화 launch 파일 확인
```bash
cat /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_examples/nvblox_examples_bringup/launch/visualization/visualization.launch.py
```
**목적**: nvblox 공식 시각화 구성 확인
**결과**: `run_foxglove` 옵션으로 foxglove_bridge + `use_foxglove_whitelist`로 대역폭 제한, rviz 선택 가능

### 4.4 nvblox Foxglove extension TypeScript 소스 검색
```bash
find /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox -type f \
  \( -name '*.ts' -o -name '*.tsx' -o -name '*.js' -o -name '*.json' \) \
  -path '*foxglove*'
```
**목적**: nvblox Foxglove extension 소스 코드 위치 확인
**결과**:
```
nvblox_foxglove/tsconfig.json
nvblox_foxglove/package.json
nvblox_foxglove/package-lock.json
nvblox_foxglove/src/index.ts
```

### 4.5 nvblox Foxglove extension 소스 분석 ⭐
```bash
cat /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_foxglove/src/index.ts
```
**목적**: extension이 지원하는 메시지 변환 확인
**결과**: 두 가지 메시지 변환기 등록:

**변환기 1: Mesh → SceneUpdate**
```typescript
extensionContext.registerMessageConverter({
    fromSchemaName: "nvblox_msgs/msg/Mesh",
    toSchemaName: "foxglove.SceneUpdate",
    converter: foxgloveFromRosMesh,
});
```
- `nvblox_msgs/msg/Mesh` → 삼각형 기반 3D 메쉬 렌더링
- block별로 vertices, normals, colors, triangles 추출
- `BLOCK_LIFETIME: 10초` (10초간 유지 후 자동 삭제)

**변환기 2: VoxelBlockLayer → SceneUpdate**
```typescript
extensionContext.registerMessageConverter({
    fromSchemaName: "nvblox_msgs/msg/VoxelBlockLayer",
    toSchemaName: "foxglove.SceneUpdate",
    converter: foxgloveFromRosVoxelBlockLayer,
});
```
- `nvblox_msgs/msg/VoxelBlockLayer` → 복셀 큐브(CubePrimitive) 기반 3D 시각화
- 복셀 크기: `block_size_m / 8`
- block별로 centers, colors 추출하여 큐브 생성
- `BLOCK_LIFETIME: 10초`

### 4.6 extension package.json 확인
```bash
cat /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_foxglove/package.json
```
**목적**: extension 버전, 빌드 방법 확인
**결과**:
```json
{
  "name": "nvblox_foxglove",
  "displayName": "Nvblox Foxglove",
  "version": "2.2.0",
  "publisher": "Nvidia",
  "description": "Message converter to visualize nvblox meshes in foxglove."
}
```
- 빌드: `foxglove-extension build`
- 로컬 설치: `foxglove-extension install` (또는 `local-install`)

---

## Phase 5: 실제 대역폭 측정

### 5.1 주요 토픽 퍼블리시 주파수 동시 측정
```bash
timeout 8 ros2 topic hz /nvblox_node/mesh --window 5 &
timeout 8 ros2 topic hz /nvblox_node/color_layer --window 5 &
timeout 8 ros2 topic hz /nvblox_node/static_esdf_pointcloud --window 5 &
timeout 8 ros2 topic hz /visual_slam/tracking/odometry --window 5 &
timeout 8 ros2 topic hz /odom --window 5 &
wait
```
**목적**: Docker launch 실행 중 주요 토픽의 실제 발행 주파수 측정
**결과**:
```
/nvblox_node/mesh:                     WARNING: does not appear to be published
/nvblox_node/color_layer:              ~8.254 Hz (min: 0.083s max: 0.166s)
/nvblox_node/static_esdf_pointcloud:   ~9.635 Hz (min: 0.098s max: 0.109s)
/visual_slam/tracking/odometry:        ~30.840 Hz (min: 0.023s max: 0.039s)
/odom:                                 WARNING: does not appear to be published
```
- mesh는 Host에서 `ros2 topic hz`로도 수신 불가
- color_layer: 0.5Hz 설정인데 ~8Hz로 측정 → 이전 Docker launch의 설정이 아직 반영 안 됨

### 5.2 주요 nvblox 토픽 대역폭 측정
```bash
timeout 8 ros2 topic bw /nvblox_node/mesh --window 5 &
timeout 8 ros2 topic bw /nvblox_node/color_layer --window 5 &
timeout 8 ros2 topic bw /nvblox_node/color_layer_marker --window 5 &
timeout 8 ros2 topic bw /nvblox_node/static_esdf_pointcloud --window 5 &
wait
```
**목적**: Foxglove 대역폭 산정을 위한 실측 대역폭 확인
**결과**:
```
/nvblox_node/mesh:                 ~438.10 KB/s (mean: 38.10 KB × ~5 msgs)
/nvblox_node/color_layer:          ~255.44 KB/s (mean: 102.47 KB, min: 0.51 KB max: 204.43 KB)
/nvblox_node/color_layer_marker:   ~113.71 KB/s (mean: 102.47 KB, min: 0.51 KB max: 204.43 KB)
/nvblox_node/static_esdf_pointcloud: ~398.01 KB/s (mean: 38.07 KB × ~5 msgs)
```

### 5.3 실행 중인 nvblox 매개변수 확인 (새 터미널)
```bash
ros2 param get /nvblox_node update_mesh_rate_hz
ros2 param get /nvblox_node publish_layer_rate_hz
ros2 param get /nvblox_node publish_debug_vis_rate_hz
```
**목적**: 현재 실행 중인 Docker launch의 실제 매개변수값 확인
**결과**:
```
update_mesh_rate_hz: 2.0
publish_layer_rate_hz: 0.0  (이전 설정이 여전히 적용됨)
publish_debug_vis_rate_hz: (출력 잘림)
```

---

## Phase 6: nvblox extension 기반 최종 수정 (2차 수정)

사용자가 nvblox Foxglove extension을 설치하여 `nvblox_msgs/msg/Mesh`와 `nvblox_msgs/msg/VoxelBlockLayer` 모두 렌더링 가능해짐.

### 6.1 Docker launch 수정
```bash
# 파일: isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/
#       isaac_ros_vslam_nvblox_realsense.launch.py
```
**변경 내용**:
```python
# 변경 전 (Phase 3 수정):
# Host Foxglove: color_layer_marker + static_esdf_pointcloud + esdf_slice_bounds 구독
# nvblox_msgs/Mesh는 Foxglove가 렌더링 불가 → color_layer_marker(Marker) 사용
'publish_layer_rate_hz': 0.5,       # layer+marker 0.5Hz (color_layer_marker용)
'publish_debug_vis_rate_hz': 2.0,    # ESDF pointcloud 발행 유지 (2Hz)
'update_mesh_rate_hz': 2.0,          # mesh 내부 갱신 유지 (costmap용)

# 변경 후:
# nvblox Foxglove extension 설치됨:
#   nvblox_msgs/Mesh → SceneUpdate (3D 메쉬)
#   nvblox_msgs/VoxelBlockLayer → SceneUpdate (3D 복셀)
# Host Foxglove: mesh + color_layer + static_esdf_pointcloud 구독
# 대역폭: mesh(~438KB/s) + color_layer(~255KB/s) + esdf(~398KB/s) ≈ 1.1MB/s
'publish_layer_rate_hz': 0.5,       # color_layer 0.5Hz (VoxelBlockLayer→SceneUpdate)
'publish_debug_vis_rate_hz': 2.0,    # ESDF pointcloud 발행 유지 (2Hz)
'update_mesh_rate_hz': 2.0,          # mesh 발행 2Hz (Mesh→SceneUpdate)
```
**핵심**: 주석/설명만 업데이트, 매개변수 값은 동일

### 6.2 Host launch Foxglove whitelist 최종 수정
```bash
# 파일: jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
```
**변경 내용**:
```python
# 변경 전 (Phase 3 수정):
'/nvblox_node/color_layer_marker',      # 3D 복셀 시각화 (Marker, ~600KB×0.5Hz=0.3MB/s)
'/nvblox_node/static_esdf_pointcloud',  # 2D ESDF 포인트클라우드 (58KB×2Hz=0.1MB/s)
'/nvblox_node/esdf_slice_bounds',       # ESDF 범위 마커 (0.5KB×2Hz≈0)

# 변경 후:
'/nvblox_node/mesh',                    # 3D 메쉬 (extension→SceneUpdate, ~438KB/s@2Hz)
'/nvblox_node/color_layer',             # 3D 복셀 지도 (extension→SceneUpdate, ~255KB/s@0.5Hz)
'/nvblox_node/static_esdf_pointcloud',  # 2D ESDF 포인트클라우드 (~398KB/s)
'/nvblox_node/esdf_slice_bounds',       # ESDF 범위 마커 (≈0)
```
**핵심 변경**:
- `color_layer_marker` (표준 Marker) → `color_layer` (VoxelBlockLayer, extension이 변환)
- `mesh` 복원 (extension이 nvblox_msgs/Mesh → SceneUpdate 변환)
- 예상 총 대역폭: ~1.1 MB/s (send_buffer_limit 10MB 이내)

### 6.3 Host 패키지 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
source install/setup.bash
colcon build --packages-select jupiter_nav_VO --symlink-install
```
**결과**: `Finished <<< jupiter_nav_VO [7.52s]` 성공

---

## Phase 7: 최종 설정 요약

### 7.1 Docker launch 최종 nvblox 매개변수
```python
'publish_layer_rate_hz': 0.5,       # color_layer/tsdf_layer 발행 0.5Hz
'publish_debug_vis_rate_hz': 2.0,    # ESDF pointcloud 발행 2Hz
'update_mesh_rate_hz': 2.0,          # mesh 발행 2Hz
```

### 7.2 Host Foxglove whitelist 최종 nvblox 토픽
```python
'/nvblox_node/mesh',                    # nvblox_msgs/Mesh → extension → SceneUpdate
'/nvblox_node/color_layer',             # nvblox_msgs/VoxelBlockLayer → extension → SceneUpdate
'/nvblox_node/static_esdf_pointcloud',  # sensor_msgs/PointCloud2 (기본 렌더링)
'/nvblox_node/esdf_slice_bounds',       # visualization_msgs/Marker (기본 렌더링)
```

### 7.3 예상 대역폭 (실측 기반)
| 토픽 | 타입 | 대역폭 | 렌더링 |
|------|------|--------|--------|
| mesh | nvblox_msgs/Mesh | ~438 KB/s | extension → SceneUpdate |
| color_layer | nvblox_msgs/VoxelBlockLayer | ~255 KB/s | extension → SceneUpdate |
| static_esdf_pointcloud | PointCloud2 | ~398 KB/s | Foxglove 기본 |
| esdf_slice_bounds | Marker | ≈0 | Foxglove 기본 |
| **합계** | | **~1.1 MB/s** | send_buffer_limit 10MB OK |

### 7.4 검증 대기
- Docker launch 재시작 필요 (publish_layer_rate_hz, update_mesh_rate_hz 반영)
- Host launch 재시작 필요 (whitelist 변경 반영)
- Foxglove 3D Panel에서 mesh + color_layer SceneUpdate 렌더링 확인 예정
