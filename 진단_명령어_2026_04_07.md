# 진단 명령어 2026-04-07: RTK NavSatStatus 수정 + GNSS 간소화 + 휠 슬립 감지

## 배경
- 2026-04-06 Part 7에서 RTK GPS 융합 파이프라인 구축 완료
- 2026-04-07에 NavSatStatus 매핑 버그 수정, GNSS 필터 간소화, IMU 기반 슬립 감지 구현

---

## Phase 1: RTK NavSatStatus 버그 수정

### 1.1 수정 전 코드 확인
```bash
grep -n "STATUS_FIX\|STATUS_GBAS\|STATUS_SBAS\|STATUS_NO_FIX" \
  /home/jetson/jupiter_ws_ros2/src/zed_f9p_rtk/zed_f9p_rtk/zed_f9p_rtk_node.py
```
**목적**: 기존 NavSatStatus 매핑 확인
**결과**: qual=4,5 모두 STATUS_GBAS_FIX(2)로 매핑 → qual=5는 STATUS_SBAS_FIX(1)이어야 함

### 1.2 공분산 값 확인
```bash
grep -n "cov" /home/jetson/jupiter_ws_ros2/src/zed_f9p_rtk/zed_f9p_rtk/zed_f9p_rtk_node.py
```
**목적**: 기존 공분산 값 확인  
**결과**:
- qual=4: cov=0.01 → 실제: σ²=0.0004 (2cm 정밀도)
- qual=5: cov=0.1 → 실제: σ²=0.25 (50cm 정밀도)
- else: cov=5.0 → 실제: σ²=6.25 (2.5m 정밀도)

### 1.3 수정 내용
**파일**: `src/zed_f9p_rtk/zed_f9p_rtk/zed_f9p_rtk_node.py` → `process_gga()` 메서드

올바른 매핑:
```
qual=0 → STATUS_NO_FIX (-1), cov=9999.0
qual=1,2 → STATUS_FIX (0), cov=6.25
qual=5 → STATUS_SBAS_FIX (1), cov=0.25
qual=4 → STATUS_GBAS_FIX (2), cov=0.0004
```

---

## Phase 2: GNSS 파이프라인 복잡도 분석

### 2.1 기존 gnss_quality_filter 분석
```bash
cat /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/gnss_quality_filter.py
```
**목적**: 파이프라인이 과도하게 복잡한지 분석
**결과**:
- 공분산 기반 RTK Fixed/Float 구분 (raw_cov ≤ 0.05) → **취약**: zed_f9p 공분산 값 변경 시 깨짐
- 공분산 override → **불필요**: 소스 노드가 이미 올바른 값을 설정하면 됨
- 6개 파라미터 중 3개 (override 관련)가 불필요

### 2.2 간소화 판단
| 구성 요소 | 필요 여부 | 이유 |
|:---|:---:|:---|
| gnss_quality_filter 자체 | ✅ 필요 | navsat_transform이 NO_FIX를 게이트하지 않음 |
| 공분산 override | ❌ 불필요 | zed_f9p_rtk_node가 올바른 공분산 설정 |
| RTK Fixed/Float 구분 | ❌ 불필요 | NavSatStatus로 직접 구분 가능 |

### 2.3 간소화 결과
```bash
cat /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/gnss_quality_filter.py
```
**변경 후**: 품질 판정 → NavSatStatus 기반, 통과 시 → `self.fix_pub.publish(msg)` 원본 그대로

---

## Phase 3: IMU 기반 휠 슬립 감지 구현

### 3.1 기존 odom_covariance_adapter 확인
```bash
cat /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/odom_covariance_adapter.py
```
**목적**: 기존 동적 공분산 로직 확인, IMU 구독 추가 위치 파악
**결과**: 속도/회전/가속도 기반 공분산 있음, IMU 구독 없음

### 3.2 IMU 토픽 확인 (실행 시)
```bash
ros2 topic echo /imu/data_calibrated --once --field linear_acceleration
```
**목적**: IMU x축 가속도 확인 (바이어스 ~0.11 m/s²)
**예상**: `x: ~0.11` (정지 시), `y: ~0.0`, `z: ~9.81`

### 3.3 구현 내용
**파일**: `src/jupiter_nav_VO/jupiter_nav_VO/odom_covariance_adapter.py`
- IMU 구독 추가: `/imu/data_calibrated`
- 슬립 감지: `|encoder_ax - imu_ax - bias| > 0.5 m/s²`
- 적응형 바이어스: EMA (α=0.005)
- 동적 배율: `1 + (mismatch/threshold) × 10`, max 50x
- 감쇠: `multiplier *= 0.92` (슬립 해제 시)

---

## Phase 4: 빌드 및 검증

### 4.1 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO zed_f9p_rtk --symlink-install
```
**결과**:
```
WARNING: Package name "jupiter_nav_VO" does not follow the naming conventions. It should start with a lower case letter and only contain lower case letters, digits, underscores, and dashes.
Starting >>> zed_f9p_rtk
Starting >>> jupiter_nav_VO
/home/jetson/.local/lib/python3.10/site-packages/setuptools/_distutils/dist.py:270: UserWarning: Unknown distribution option: 'tests_require'
  warnings.warn(msg)
/home/jetson/.local/lib/python3.10/site-packages/setuptools/_distutils/dist.py:270: UserWarning: Unknown distribution option: 'tests_require'
  warnings.warn(msg)
--- stderr: jupiter_nav_VO
/home/jetson/.local/lib/python3.10/site-packages/setuptools/_distutils/dist.py:270: UserWarning: Unknown distribution option: 'tests_require'
  warnings.warn(msg)
---
Finished <<< jupiter_nav_VO [4.88s]
--- stderr: zed_f9p_rtk
/home/jetson/.local/lib/python3.10/site-packages/setuptools/_distutils/dist.py:270: UserWarning: Unknown distribution option: 'tests_require'
  warnings.warn(msg)
---
Finished <<< zed_f9p_rtk [4.92s]

Summary: 2 packages finished [5.32s]
  2 packages had stderr output: jupiter_nav_VO zed_f9p_rtk
```

---

## Phase 5: 실테스트 진단 명령어

### 5.1 RTK NavSatStatus 확인
```bash
# NavSatStatus 값 확인 (RTK Fixed=2, RTK Float=1, GPS=0, No Fix=-1)
ros2 topic echo /fix --field status.status

# 공분산 확인 (RTK Fixed≈0.0004, RTK Float≈0.25, GPS≈6.25)
ros2 topic echo /fix --field position_covariance
```

### 5.2 GNSS 품질 필터 동작 확인
```bash
# 필터 로그 확인 (품질 전환 시 로그)
ros2 topic echo /fix_filtered --field status.status

# 필터 통과율 비교
ros2 topic hz /fix
ros2 topic hz /fix_filtered
```

### 5.3 슬립 감지 테스트
```bash
# 오도메트리 공분산 모니터링
ros2 topic echo /odom_adapted --field twist.covariance --once

# 슬립 감지 로그 확인 (logger)
ros2 topic echo /rosout --field msg | grep -i slip

# 실시간 엔코더 vs IMU 비교
ros2 topic echo /odom_raw --field twist.twist.linear.x &
ros2 topic echo /imu/data_calibrated --field linear_acceleration.x &
```

### 5.4 슬립 유발 테스트 시나리오
```bash
# 시나리오 1: 바퀴 들어올리기 (공전 테스트)
# → 엔코더: vx > 0, IMU: ax ≈ 0 → SLIP DETECTED

# 시나리오 2: 미끄러운 바닥 급출발
# → 엔코더 급증, IMU 소폭 증가 → SLIP DETECTED

# 시나리오 3: 급정지 (바퀴 록)
# → 엔코더: vx=0 급정지, IMU: ax < 0 (감속) → SLIP DETECTED
```

### 5.5 EKF 영향 확인
```bash
# odom0(wheel) 공분산 변화 모니터링
ros2 topic echo /odom_adapted --field twist.covariance[0]

# EKF 융합 결과 비교
ros2 topic echo /odom --field twist.twist.linear.x

# 슬립 시 EKF가 VSLAM에 더 의존하는지 확인
ros2 topic echo /odom --field header.stamp &
ros2 topic echo /visual_slam/tracking/odometry --field twist.twist.linear.x &
```

---

## Phase 6: 파라미터 튜닝 가이드

### 6.1 슬립 감지 민감도 조절
```bash
# 현재 파라미터 확인
ros2 param get /odom_covariance_adapter slip_mismatch_threshold
ros2 param get /odom_covariance_adapter slip_max_multiplier
ros2 param get /odom_covariance_adapter slip_decay

# 런타임 파라미터 조절 (재시작 불필요)
# 너무 민감하게 감지 시 → threshold 증가
ros2 param set /odom_covariance_adapter slip_mismatch_threshold 1.0

# 슬립 시 완전 무시 → max_multiplier 증가
ros2 param set /odom_covariance_adapter slip_max_multiplier 100.0

# 슬립 후 복귀가 너무 느릴 때 → decay 증가
ros2 param set /odom_covariance_adapter slip_decay 0.95
```

### 6.2 GNSS max_covariance 조절
```bash
# 현재 max_covariance 확인
ros2 param get /gnss_quality_filter max_covariance

# 3D Fix (cov=6.25)도 통과시키려면 현재 50.0이면 충분
# 3D Fix를 차단하고 RTK만 통과시키려면:
ros2 param set /gnss_quality_filter max_covariance 1.0
```
