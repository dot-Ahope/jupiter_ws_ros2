# 진단 명령어 및 결과 기록 — 2026-03-10

> 이 문서는 2026-03-10 세션에서 사용한 **모든** 진단 명령어/코드, 그 목적, 결과를 시간순으로 기록합니다.

---

## 목차
1. [RF2O acos NaN 패치](#1-rf2o-acos-nan-패치)
2. [VSLAM Adapter Anomaly Lock-out 진단 및 수정](#2-vslam-adapter-anomaly-lock-out-진단-및-수정)
3. [DDS Discovery 문제 진단](#3-dds-discovery-문제-진단)
4. [LiDAR 미발행 진단 및 복구](#4-lidar-미발행-진단-및-복구)
5. [rclpy 모니터링 노드 도구](#5-rclpy-모니터링-노드-도구)
6. [수정 후 센서 검증](#6-수정-후-센서-검증)
7. [Nav2 Goal 테스트 1차](#7-nav2-goal-테스트-1차)
8. [Nav2 Abort 근본 원인 분석](#8-nav2-abort-근본-원인-분석)
9. [RF2O EKF Differential 수정](#9-rf2o-ekf-differential-수정)
10. [RF2O Differential 수정 후 재시작 및 검증](#10-rf2o-differential-수정-후-재시작-및-검증)
11. [Nav2 Goal 테스트 2차 — 성공](#11-nav2-goal-테스트-2차--성공)
12. [이전 주행 데이터 CSV 분석](#12-이전-주행-데이터-csv-분석)
13. [실주행 데이터 CSV 2차 분석 — VSLAM 폭주 발견](#13-실주행-데이터-csv-2차-분석--vslam-폭주-발견)
14. [VSLAM Adapter prev_odom 재수정](#14-vslam-adapter-prev_odom-재수정)
15. [Nav2 Planning 실패 분석](#15-nav2-planning-실패-분석)
16. [Foxglove Bridge 경고 분석](#16-foxglove-bridge-경고-분석)

---

## 1. RF2O acos NaN 패치

### 1-1. 소스 코드 확인
**목적**: RF2O 코드에서 `std::acos` 호출 위치 확인
```bash
grep -n "acos" src/rf2o_laser_odometry/src/CLaserOdometry2D.cpp
```
**결과**: 2곳 발견
- Line 872: `solveSystemNonLinear()` 내
- Line 936: `PoseUpdate()` 내
```
872:    kai_loc_(2) = fps*std::acos(acu_trans(0,0))*rf2o::sign(acu_trans(1,0));
936:    kai_loc_(2) = fps*std::acos(acu_trans(0,0))*rf2o::sign(acu_trans(1,0));
```

### 1-2. 코드 수정
**목적**: `acu_trans(0,0)` 값이 부동소수점 오차로 `[-1, 1]` 범위 벗어날 때 NaN 방지

**변경 1** — `CLaserOdometry2D.cpp` Line 869–871:
```cpp
// Before
if (acu_trans(0,0) > 1.f)
    kai_loc_(2) = 0.f;
else
    kai_loc_(2) = fps*std::acos(acu_trans(0,0))*rf2o::sign(acu_trans(1,0));

// After
{
    const float clamped = std::clamp(acu_trans(0,0), -1.f, 1.f);
    kai_loc_(2) = fps*std::acos(clamped)*rf2o::sign(acu_trans(1,0));
}
```

**변경 2** — `CLaserOdometry2D.cpp` Line 929–931: 동일 패턴

**변경 3** — `CLaserOdometry2D.hpp`: `#include <algorithm>` 추가

### 1-3. 빌드
**목적**: RF2O 패키지 빌드 확인
```bash
cd /home/jetson/jupiter_ws_ros2
source /opt/ros/humble/setup.bash
colcon build --packages-select rf2o_laser_odometry --cmake-args -DCMAKE_BUILD_TYPE=Release
```
**결과**: 44초, 성공 (WARNING 없음)

---

## 2. VSLAM Adapter Anomaly Lock-out 진단 및 수정

### 2-1. VSLAM adapted 토픽 확인 (rclpy 모니터)
**목적**: 전체 10개 센서 토픽 동시 확인 — `/visual_slam/tracking/odometry_adapted`가 NO DATA일 가능성
```python
# rclpy 노드: 10개 토픽을 동시 구독, 5초간 count
topics = ['/visual_slam/tracking/odometry', '/visual_slam/tracking/odometry_adapted',
          '/scan', '/odom_rf2o', '/odom_rf2o_adapted', '/odom_raw', '/odom_adapted',
          '/odom', '/imu/data_calibrated', '/jupiter/imu']
```
**결과**:
```
/visual_slam/tracking/odometry:         65.2 Hz ✅
/visual_slam/tracking/odometry_adapted: NO DATA ❌
/scan:                                  10.2 Hz ✅
/odom_rf2o:                             10.2 Hz ✅
(나머지 모두 정상)
```

### 2-2. Adapter Stats 토픽 확인
**목적**: `vslam_covariance_adapter` 내부 상태 확인
```python
# /vslam_adapter_stats (String 타입) 구독
```
**결과**:
```
tracking_ok=False
anomaly=40693 (29%)
pub=137080 (71%)
total=193093
frozen_detect=0
frozen_pub=0
```
- anomaly 29%: 거의 1/3 프레임이 anomaly로 gate됨
- tracking_ok=False: adapter가 VSLAM을 불량으로 판정

### 2-3. 소스 코드 분석 — anomaly lock-out 버그 발견
**목적**: `_check_anomaly()` 로직에서 `prev_odom` 업데이트 조건 확인
```python
# vslam_covariance_adapter.py 의 _check_anomaly() 내:
if not is_anomaly:
    self.prev_odom = pose    # ← 문제: anomaly 시 prev_odom 미업데이트
```
**분석**:
1. anomaly 1회 발생 → `prev_odom` 고정됨
2. 다음 프레임: current - prev_odom = 더 큰 delta → anomaly 재판정
3. prev_odom 여전히 고정 → delta 계속 증가 → **영구 anomaly lock-out**
4. `consecutive_good` 카운터가 절대 증가하지 않음 → 복구 불가

### 2-4. 수정 적용
**목적**: anomaly 상태와 무관하게 항상 `prev_odom` 업데이트
```python
# 수정: self.prev_odom = pose를 조건 밖으로 이동
self.prev_odom = pose   # 항상 업데이트 → frame-to-frame 비교 보장
```

### 2-5. pip editable 설치
**목적**: 수정된 adapter를 즉시 반영
```bash
cd /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO
pip install -e .
```
**결과**: 성공

---

## 3. DDS Discovery 문제 진단

### 3-1. `ros2 topic hz` 무응답 진단
**목적**: VS Code 터미널에서 `ros2 topic hz`가 데이터를 수신하지 못하는 원인 파악

```bash
echo $RMW_IMPLEMENTATION
```
**결과**: (빈 문자열)
- 모든 ROS2 노드는 `rmw_fastrtps_cpp`를 사용하는데, 에이전트 터미널은 RMW가 미설정

### 3-2. RMW 설정 및 daemon 재시작
```bash
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
ros2 daemon stop && ros2 daemon start
```
**결과**: 일부 토픽 수신 가능해졌으나, 매번 subprocess 생성 시 DDS discovery 지연으로 불안정

### 3-3. 단일 프로세스 rclpy 모니터링으로 전환
**목적**: subprocess 기반 ros2 CLI 대신, rclpy 단일 프로세스로 모든 토픽 구독 → DDS re-discovery 회피
```python
# rclpy 노드 1개에서 10개 토픽 동시 구독
# QoS: BEST_EFFORT (센서 데이터), RELIABLE (Log/DiagnosticArray)
```
**결과**: 모든 토픽 안정적으로 수신 성공

---

## 4. LiDAR 미발행 진단 및 복구

### 4-1. /scan 토픽 상태 확인
**목적**: sllidar_node가 실행 중인데 /scan이 발행되지 않는 원인 파악
```python
# rclpy 모니터: /scan 구독 5초
```
**결과**: `/scan`: NO DATA (0 msgs)

### 4-2. 시리얼 포트 직접 테스트
**목적**: LiDAR 하드웨어가 시리얼에 응답하는지 확인
```python
import serial
ser = serial.Serial('/dev/rplidar', 1000000, timeout=2)
# health request 전송
ser.write(b'\xa5\x52')
response = ser.read(10)
print(f"Response: {len(response)} bytes")
```
**결과**: `Response: 0 bytes` — LiDAR 하드웨어 응답 없음

### 4-3. 시리얼 포트 점유 프로세스 확인
**목적**: 다른 프로세스가 `/dev/ttyUSB1`을 점유하고 있는지 확인
```bash
sudo fuser /dev/ttyUSB1
```
**결과**: `PID 61827` — 이전 yahboomcar_ros2_ws에서 남은 구식 sllidar_node

### 4-4. 구식 프로세스 종료
```bash
kill -9 61827
```
**결과**: 포트 해제 완료

### 4-5. sllidar_node 수동 재시작
**목적**: 올바른 파라미터로 LiDAR 노드 재시작
```bash
ros2 run sllidar_ros2 sllidar_node --ros-args \
  -p channel_type:=serial \
  -p serial_port:=/dev/lidar_port \
  -p serial_baudrate:=1000000 \
  -p frame_id:=laser \
  -p inverted:=false \
  -p angle_compensate:=true \
  -p scan_mode:=Standard
```
**결과**: `/scan` 10Hz 복구 ✅

### 4-6. start_motor 서비스 호출 (이전 시도 — 실패)
**목적**: LiDAR가 멈춘 상태에서 서비스로 재시작 시도
```bash
ros2 service call /start_motor std_srvs/srv/Empty
```
**결과**: 무응답 (시리얼 포트가 다른 프로세스에 점유되어 있었으므로)

---

## 5. rclpy 모니터링 노드 도구

### 5-1. 10-토픽 동시 모니터
**목적**: DDS discovery 문제를 우회하여 모든 센서 토픽 상태를 한번에 확인
```python
import rclpy
from rclpy.node import Node
from rclpy.qos import QoSProfile, ReliabilityPolicy, HistoryPolicy
from nav_msgs.msg import Odometry
from sensor_msgs.msg import Imu, LaserScan
from std_msgs.msg import String

class Monitor(Node):
    def __init__(self):
        super().__init__('monitor')
        qos = QoSProfile(depth=5, reliability=ReliabilityPolicy.BEST_EFFORT,
                          history=HistoryPolicy.KEEP_LAST)
        self.counts = {}
        topics = [
            ('/visual_slam/tracking/odometry', Odometry),
            ('/visual_slam/tracking/odometry_adapted', Odometry),
            ('/scan', LaserScan),
            ('/odom_rf2o', Odometry),
            ('/odom_rf2o_adapted', Odometry),
            ('/odom_raw', Odometry),
            ('/odom_adapted', Odometry),
            ('/odom', Odometry),
            ('/imu/data_calibrated', Imu),
            ('/jupiter/imu', Imu),
        ]
        for topic, msg_type in topics:
            self.create_subscription(msg_type, topic,
                lambda m, t=topic: self.cb(t), qos)
            self.counts[topic] = 0

    def cb(self, topic):
        self.counts[topic] += 1

# 5초 spin 후 rate 계산
```
**결과**: 전체 세션에서 반복 사용하여 센서 상태 확인

### 5-2. Adapter Stats 모니터
**목적**: VSLAM adapter의 내부 통계 (anomaly 횟수, tracking 상태 등) 확인
```python
# /vslam_adapter_stats, /rf2o_adapter_stats (String) 구독
```

---

## 6. 수정 후 센서 검증

### 6-1. 호스트 스택 + Nav2 재시작 후 전체 확인
**목적**: VSLAM adapter 수정 적용 확인
```python
# rclpy 10-토픽 모니터 (5초)
```
**결과**:
```
/visual_slam/tracking/odometry:         63.0 Hz ✅
/visual_slam/tracking/odometry_adapted:  3.6 Hz ✅  (이전 0Hz → 수정 성공!)
/scan:                                  10.0 Hz ✅
/odom_rf2o:                             10.0 Hz ✅
/odom_rf2o_adapted:                     10.0 Hz ✅
/odom_raw:                              50.0 Hz ✅
/odom_adapted:                          50.0 Hz ✅
/odom (EKF):                            30.0 Hz ✅
/imu/data_calibrated:                   91.0 Hz ✅
/jupiter/imu:                           77.0 Hz ✅
```

### 6-2. Adapter Stats 확인 (수정 후)
```
tracking_ok=True
anomaly=14
pub=... 
frozen_pub=305
```
- `tracking_ok=True`: 복구 성공
- `anomaly=14` (이전 40693): lock-out 해소 확인

### 6-3. 노드 수 확인
**목적**: 전체 시스템 노드가 모두 실행 중인지 확인
```python
nodes = node.get_node_names_and_namespaces()
print(f"Total: {len(nodes)} nodes")
```
**결과**: **44 노드** (base stack 24 + Nav2 stack 20)

### 6-4. Map 정보 확인
**목적**: SLAM Toolbox 맵이 정상적으로 생성되었는지 확인
```python
from nav_msgs.msg import OccupancyGrid
# /map 구독 (TRANSIENT_LOCAL QoS)
```
**결과**: 110×139 cells, resolution=0.05m, origin=(-2.9, -3.6)

### 6-5. 현재 EKF 위치 확인
**목적**: Nav2 goal 테스트를 위한 현재 위치 파악
```python
import tf2_ros
t = buf.lookup_transform('map', 'base_link', rclpy.time.Time())
```
**결과**: x=-0.092, y=0.010, yaw=-37.6°

---

## 7. Nav2 Goal 테스트 1차

### 7-1. 1m 전방 목표 전송
**목적**: 현재 yaw 방향으로 1m 전방에 Nav2 goal 전송 → 로봇 실주행 확인
```python
from nav2_msgs.action import NavigateToPose
from rclpy.action import ActionClient

# 현재 위치: x=-0.092, y=0.011, yaw=-37.9°
goal_x = -0.092 + 1.0 * cos(-37.9°) = 0.696
goal_y = 0.011  + 1.0 * sin(-37.9°) = -0.604

goal = NavigateToPose.Goal()
goal.pose.header.frame_id = 'map'
goal.pose.pose.position.x = 0.696
goal.pose.pose.position.y = -0.604
# yaw=-37.9° → quaternion
```
**결과**:
```
Goal ACCEPTED
Feedback: 275회, dist_remaining=2.572m (고정)
Result: ABORTED (약 28초)
Distance traveled: 0.014m (거의 0)
```

### 7-2. cmd_vel 확인
**목적**: Nav2가 속도 명령을 실제로 생성했는지 확인
```python
# /cmd_vel 구독 (BEST_EFFORT, 5초)
```
**결과**: **NO DATA** — 속도 명령이 0개. Nav2 controller가 cmd_vel을 생성하지 않음.

### 7-3. Nav2 rosout 로그 확인
**목적**: controller_server, bt_navigator, planner_server 등의 에러 메시지 확인
```python
from rcl_interfaces.msg import Log
# /rosout 구독, 필터: controller_server|bt_navigator|planner_server|behavior_server
```
**결과**: "No Nav2 log messages" — 시스템 idle 상태 (abort 후 로그 없음)

### 7-4. TF 체인 검증
**목적**: 모든 TF가 정상적으로 연결되어 있는지 확인
```python
import tf2_ros
buf = tf2_ros.Buffer()
# 4개 TF 조회
```
**결과**:
```
map→odom:            (0.043, 0.027, 0.000) q=(0,0,0.326,0.945) ✅
odom→base_footprint: (-0.331, 0.189, 0.000) q=(0,0,-0.299,0.954) ✅
base_footprint→base_link: (0, 0, 0.080) q=(0,0,0,1) ✅
base_link→laser:     (-0.030, 0, 0.130) q=(0,0,1,0) ✅
map→base_link (전체): (-0.333, -0.028, 0.080) ✅
```
- 이 시점에는 모든 TF 정상으로 보였음
- (이후 시간이 지남에 따라 RF2O drift로 EKF 발산)

---

## 8. Nav2 Abort 근본 원인 분석

### 8-1. TF Lookup (2차 — 발산 발견)
**목적**: 두 번째 goal 테스트 전 현재 위치 확인
```python
# map, odom, base_footprint, base_link 전체 TF 조회
# 5초 spin 후 lookup
```
**결과**:
```
map→odom:            (0.047, 0.029, 0.000)
odom→base_footprint: (-2.781, -22.562, 0.000)   ← 22.5m 발산!
map→base_link:       (11.774, -19.445, 0.080)    ← 맵 완전 이탈!
```
- **EKF가 odom→base_footprint를 (-2.781, -22.562)로 발행** — 정지 상태인데 22.5m 이동
- map→base_link가 (11.774, -19.445) — 맵(5.5m×7m) 밖

### 8-2. 오도메트리 토픽 값 비교
**목적**: 어떤 센서가 EKF 발산의 원인인지 특정
```python
# 7개 odom 토픽 동시 구독 (5초)
topics = ['/odom', '/odom_raw', '/odom_adapted', '/odom_rf2o', '/odom_rf2o_adapted',
          '/visual_slam/tracking/odometry', '/visual_slam/tracking/odometry_adapted']
```
**결과**:
```
/odom (EKF):          (-2.797, -22.550) 30.8Hz → RF2O와 동일!
/odom_adapted (wheel): (-0.235, -0.036) 47.6Hz → 정상 (정지)
/odom_rf2o:           (-2.797, -22.550) 10.4Hz → 22.5m 발산
/odom_rf2o_adapted:   (-2.797, -22.550) 10.4Hz → RF2O 복사
/vslam adapted:       (-0.153,  2.760)  1.0Hz  → 비교적 정상
/odom_wheel:          NO DATA
/odometry/filtered:   NO DATA (→ /odom으로 remap됨)
```
- **EKF 출력 = RF2O 값** — RF2O가 EKF를 완전히 지배

### 8-3. 노드 및 퍼블리셔 확인
**목적**: 실행 중인 노드 수와 핵심 토픽의 퍼블리셔 확인
```python
nodes = node.get_node_names_and_namespaces()  # DDS discovery에 따라 결과 변동
pubs = node.get_publishers_info_by_topic('/odometry/filtered')
pubs = node.get_publishers_info_by_topic('/odom_wheel')
```
**결과** (1차): 21 nodes (DDS partial discovery)
```
/odometry/filtered: NO PUBLISHERS
/odom_wheel:        NO PUBLISHERS
/odom:              UNKNOWN publisher
/cmd_vel:           behavior_server (3개)
```
**결과** (2차): 11 nodes (DDS partial discovery — 다름)
```
controller_server, planner_server, behavior_server 등 Nav2 노드 확인
jupiter_driver, odom_covariance_adapter 등 확인
```
**분석**: DDS discovery가 불안정하여 매번 다른 노드 집합이 보이지만, `/odometry/filtered`는 `/odom`으로 remap되므로 별도 publisher 없는 것이 정상

### 8-4. 상세 토픽 rate + 값 + 프레임 확인
**목적**: EKF 출력과 각 센서 입력을 비교하여 drift 원인 특정
```python
# 7개 odom 토픽 + IMU + diagnostics 동시 구독
# 5초간 rate, 최신 position, velocity, frame_id, child_frame_id 수집
```
**결과**:
```
/imu/data_calibrated: 91.0 Hz
/odom:                30.8 Hz  pos=(-2.797,-22.550)  vx=-0.000 wz=-0.004  [odom->base_footprint]
/odom_adapted:        47.6 Hz  pos=(-0.235,-0.036)   vx=-0.000 wz=-0.006  [odom->base_footprint]
/odom_raw:            48.0 Hz  pos=(-0.235,-0.036)   vx=-0.000 wz=-0.006  [odom->base_footprint]
/odom_rf2o:           10.4 Hz  pos=(-2.797,-22.550)  vx=-0.000 wz= 0.000  [odom->base_footprint]
/odom_rf2o_adapted:   10.4 Hz  pos=(-2.797,-22.550)  vx=-0.000 wz= 0.000  [odom->base_footprint]
/vslam adapted:        1.0 Hz  pos=(-0.180, 2.764)   vx= 0.000 wz=-0.000  [odom->camera_link]

Diagnostics: ekf_filter_node: odometry/filtered topic status
```
**핵심 발견**:
- EKF 출력 위치 = RF2O 위치 (동일값!)
- wheel odom: 정상 (-0.235, -0.036)
- RF2O: 발산 (-2.797, -22.550) — 정지 상태인데 22.5m
- EKF diagnostics: 에러 없음 (정상 동작으로 인식)

### 8-5. RF2O Drift 속도 측정
**목적**: RF2O가 현재도 actively drifting하는지, 아니면 이전에 누적된 것인지 확인
```python
# 10초간 RF2O + EKF odom position 샘플링 (stationary)
# first sample vs last sample distance
```
**결과**:
```
RF2O drift in 9.9s:
  Start: (-2.799, -22.542)
  End:   (-2.798, -22.542)
  Delta: dx=0.0005, dy=0.0002, dist=0.0005m
  Rate:  0.0001 m/s = 0.00 m/min
  Samples: 100

EKF /odom drift in 10.0s:
  Start: (-2.798, -22.542)
  End:   (-2.798, -22.542)
  Delta: dx=0.0001, dy=0.0001, dist=0.0001m
  Rate:  0.0000 m/s = 0.00 m/min
```
**결론**: RF2O는 현재 actively drifting하지 않음 (0.0001 m/s). 22.5m drift는 이전에 누적된 것.

### 8-6. EKF 설정 파일 확인
**목적**: RF2O가 EKF에 어떤 모드로 인가되는지 확인
```bash
cat src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml | grep -A5 "odom2"
```
**결과**:
```yaml
odom2: /odom_rf2o_adapted
odom2_config: [true, true, false, ...]    # x, y 절대 위치 사용
odom2_differential: false                  # ← 문제! 절대 위치 모드
```
**분석**: RF2O의 누적 drift가 절대 위치로 EKF에 직접 인가 → EKF 위치 = RF2O 위치

---

## 9. RF2O EKF Differential 수정

### 9-1. 설정 변경
**목적**: RF2O의 절대 위치 대신 frame-to-frame 상대 변위만 EKF에 제공
**파일**: `src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml`
```yaml
# Before
odom2_differential: false   # 절대 위치 사용 (RF2O 자체 적분값)

# After
odom2_differential: true    # 2026-03-10: absolute → differential 전환
# 원인: RF2O 스캔 매칭 누적 drift가 EKF 절대 위치를 오염 (정지 중 22.5m 발산)
# 해결: differential 모드에서는 frame-to-frame 상대 변위만 사용
#       odom frame drift는 SLAM Toolbox map→odom TF가 보정 (표준 접근)
```

### 9-2. 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
source /opt/ros/humble/setup.bash
colcon build --packages-select jupiter_nav_VO --symlink-install
```
**결과**: 5.1초, 성공

---

## 10. RF2O Differential 수정 후 재시작 및 검증

### 10-1. 전체 스택 재시작
**목적**: RF2O differential 수정이 적용된 상태로 호스트 스택 + Nav2 + VSLAM Docker 전부 재시작
```bash
# 호스트 스택 정지 (기존 ros2 launch 프로세스 종료)
# Nav2 스택 정지
# VSLAM Docker 내 launch 정지 후 재시작

# 호스트 스택 재시작
cd /home/jetson/jupiter_ws_ros2
source install/setup.bash
ros2 launch jupiter_bringup jupiter_bringup.launch.py

# Nav2 재시작
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py

# Docker 내 VSLAM 재시작
docker exec -it isaac_ros_dev-aarch64-container /bin/bash
ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam.launch.py
```

### 10-2. 센서 상태 확인 (rclpy 통합 모니터링)
**목적**: 재시작 후 10개 토픽 모두 정상 발행 중인지, EKF 위치가 map 내부(원점 근처)인지 확인
```python
# (rclpy 단일 프로세스에서 10개 토픽 동시 구독 + /map 읽기)
# 구독 대상: /imu/data_calibrated, /odom, /odom_adapted, /odom_raw,
#            /odom_rf2o, /odom_rf2o_adapted, /scan,
#            /visual_slam/tracking/odometry, /visual_slam/tracking/odometry_adapted
# 추가: /map (OccupancyGrid) 읽기, 좌표 bounds 판정

# 핵심 확인 사항:
# 1) 모든 토픽이 0 Hz가 아닐 것
# 2) EKF (odom) position이 map 원점 근처 (0,0) — RF2O drift 해소 확인
# 3) EKF-RF2O divergence < 1m
```
**결과**:
```
=== SENSOR STATUS (8.0s) ===
  /imu/data_calibrated:   82.8 Hz ✅
  /odom (EKF):            22.5 Hz ✅  pos=(-0.002,-0.001) vx=0.000
  /odom_adapted (wheel):  45.2 Hz ✅  pos=(0.000,0.000)
  /odom_raw:              45.2 Hz ✅
  /odom_rf2o:             10.2 Hz ✅  pos=(-0.006,-0.010)
  /odom_rf2o_adapted:     10.2 Hz ✅
  /scan:                  10.1 Hz ✅
  /vslam:                 45.0 Hz ✅  pos=(-0.132,0.040)
  /vslam_adapted:          2.0 Hz ✅
  Map: 56×137 @ 0.05m/cell, origin=(0.0, -3.2)
  EKF position → map 내부 ✅, 원점 근처 ✅
  EKF-RF2O divergence: 0.010m ✅
```
**분석**: RF2O differential 전환으로 EKF 위치 발산 완전 해소.

---

## 11. Nav2 Goal 테스트 2차 — 성공

### 11-1. 테스트 실행 (rclpy Action Client)
**목적**: EKF 위치 정상화 후 Nav2가 실제 로봇 이동을 성공시키는지 확인
```python
import rclpy, math, time
from rclpy.node import Node
from rclpy.action import ActionClient
from nav2_msgs.action import NavigateToPose
from geometry_msgs.msg import PoseStamped
from nav_msgs.msg import Odometry
from rclpy.qos import QoSProfile, ReliabilityPolicy, HistoryPolicy

qos = QoSProfile(depth=5, reliability=ReliabilityPolicy.BEST_EFFORT,
                  history=HistoryPolicy.KEEP_LAST)

# rclpy.init()  — (이전 단계에서 이미 초기화됨)

# 1) 현재 EKF 위치/yaw 읽기
# 2) heading 방향으로 0.5m 전방 좌표 계산
# 3) NavigateToPose goal 전송
# 4) 피드백 로그 기록 (2Hz sampling)
# 5) 최종 결과 출력

# goal 좌표:
#   target_x = cur_x + 0.5 * cos(yaw) = -0.003 + 0.5*cos(-0.164) = 0.490
#   target_y = cur_y + 0.5 * sin(yaw) = -0.001 + 0.5*sin(-0.164) = -0.084

goal = NavigateToPose.Goal()
goal.pose.header.frame_id = 'map'
goal.pose.pose.position.x = 0.490
goal.pose.pose.position.y = -0.084
goal.pose.pose.orientation.z = math.sin(-0.164 / 2.0)   # yaw 유지
goal.pose.pose.orientation.w = math.cos(-0.164 / 2.0)

client = ActionClient(node, NavigateToPose, 'navigate_to_pose')
client.wait_for_server()
future = client.send_goal_async(goal, feedback_callback=fb_cb)
```
- `send_goal_async()`: Nav2 bt_navigator에 비동기 goal 전송, feedback_callback으로 진행 상황 수신
- `feedback.current_pose`: 현재 로봇 위치 (5Hz)
- `result.result`: 최종 성공/실패 상태

**결과**:
```
Goal ACCEPTED
fb #10 pos=(0.030,-0.001) dist=0.033m
fb #30 pos=(0.073,-0.002) dist=0.076m
fb #50 pos=(0.131,-0.004) dist=0.134m
fb #100 pos=(0.263,-0.008) dist=0.271m
fb #150 pos=(0.352,-0.012) dist=0.355m
fb #200 pos=(0.427,-0.015) dist=0.431m
fb #250 pos=(0.461,-0.017) dist=0.465m
Goal RESULT: SUCCEEDED (status=4) — 2.6s elapsed
```

### 11-2. cmd_vel 분석 (50개 명령)
**목적**: Nav2가 실제로 몇 번 속도 명령을 보냈는지, 최대 속도/각속도 확인
```python
# /cmd_vel 구독하여 (linear.x, angular.z) 기록
# Nav2가 goal 수행 중에만 cmd_vel 발행 → goal 시작~종료 간 기록

# 수집: 50 commands over 2.6s
# max vx = 0.332 m/s,  max wz = 0.368 rad/s
# vx profile: 0→0.125→0.332→0.276(순항)→0.092→0 (가속/순항/감속)
# wz profile: 대부분 0, fb#145~#195 구간에서 -0.368 (경로 보정 회전)
```
**결과**: 50개 cmd_vel, 정상 속도 프로필 (가속→순항→보정→감속→정지)

---

## 12. 이전 주행 데이터 CSV 분석

> 파일: `/home/jetson/jupiter_ws_ros2/plot_data_position_x.csv`
> 기록: plotjuggler에서 추출한 `pose.pose.position.x` 데이터 (4개 토픽)

### 12-1. CSV 구조 확인
**목적**: 파일 크기, 열 구조, 토픽 목록 파악
```bash
wc -l plot_data_position_x.csv            # 행 수 확인
head -5 plot_data_position_x.csv          # 헤더 + 첫 데이터 확인
```
- `wc -l`: 파일의 총 줄 수를 세는 명령 (word count, `-l`은 line count 옵션)
- `head -5`: 파일의 처음 5줄을 출력 (`-5` = 5줄)

**결과**: 19,677행 (헤더 + 19,676 데이터), 4개 열:
```
elapsed time,timestamp,field_name,value
16.074,...,/odom/pose/pose/position/x,-0.000803...
```

### 12-2. 토픽별 통계
**목적**: 각 토픽의 메시지 수, 평균 발행률, 값 범위 파악
```python
import csv
from collections import defaultdict

topics = defaultdict(list)

with open('plot_data_position_x.csv') as f:
    reader = csv.DictReader(f)
    for row in reader:
        t = float(row['elapsed time'])     # plotjuggler 기록 경과 시간
        topic = row['field_name'].split('/pose/')[0]  # 토픽명 추출
        val = float(row['value'])          # position.x 값
        topics[topic].append((t, val))

for topic, data in sorted(topics.items()):
    ts = [d[0] for d in data]
    vals = [d[1] for d in data]
    duration = ts[-1] - ts[0]             # 토픽 기록 총 시간
    avg_hz = len(data) / duration         # 평균 주파수 = 메시지수 / 초
    print(f"{topic}: {len(data)} msgs, {avg_hz:.1f} Hz, "
          f"range=[{min(vals):.3f}, {max(vals):.3f}]")
```
- `csv.DictReader`: CSV 파일을 딕셔너리로 읽음 (열 이름으로 접근)
- `field_name.split('/pose/')[0]`: `/odom/pose/pose/position/x`에서 토픽명 `/odom` 추출
- `len(data) / duration`: 총 메시지 수를 기록 시간으로 나눠 평균 Hz 계산

**결과**:
| 토픽 | 메시지 수 | 평균 Hz | 값 범위 |
|------|-----------|---------|---------|
| `/odom` (EKF) | 6,227 | 9.8 Hz | [-0.003, 0.255] |
| `/odom_adapted` (wheel) | 10,632 | 16.8 Hz | [0.000, 0.453] |
| `/odom_rf2o_adapted` | 2,066 | 3.3 Hz | [-0.014, 0.462] |
| `/visual_slam/tracking/odometry_adapted` | **751** | **1.2 Hz** | [-0.165, 0.207] |

### 12-3. VSLAM adapted 발행 간격 분석
**목적**: VSLAM이 "띄엄띄엄" 발행되는 패턴의 정확한 분포 파악 (burst vs silence)
```python
import numpy as np

vslam_times = [d[0] for d in topics['/visual_slam/tracking/odometry_adapted']]
intervals = np.diff(vslam_times)  # 연속 메시지 간 시간 차이 (초)

# 기본 통계
print(f"count={len(intervals)}, mean={np.mean(intervals):.3f}s, "
      f"median={np.median(intervals):.3f}s, "
      f"std={np.std(intervals):.3f}s")
print(f"min={np.min(intervals):.6f}s, max={np.max(intervals):.3f}s")

# 구간별 분포 (히스토그램)
bins = [(0, 0.5), (0.5, 1.0), (1.0, 2.0), (2.0, 10.0), (10.0, float('inf'))]
for lo, hi in bins:
    cnt = np.sum((intervals >= lo) & (intervals < hi))
    pct = cnt / len(intervals) * 100
    print(f"  [{lo:.1f}-{hi:.1f}s): {cnt} ({pct:.1f}%)")
```
- `np.diff(times)`: 연속된 두 타임스탬프의 차이 계산 → 메시지 간 간격
- `np.mean() / np.median()`: 평균 vs 중앙값 비교로 분포 편향 파악
  - mean >> median → 소수의 큰 gap이 평균을 끌어올림 (bimodal)
- 구간별 분포: burst(고속, <0.5s)와 silence(저속, ≥1s) 비율 파악

**결과**:
```
count=750, mean=0.843s, median=0.015s, std=15.51s
min=0.000004s, max=425.31s

  [0.0-0.5s):  549 (73.3%)   ← 고속 burst
  [0.5-1.0s):   72 (9.6%)
  [1.0-2.0s):  128 (17.1%)   ← frozen downsampling (1Hz)
  [2.0-10s):     0 (0.0%)
  [10s+):        1 (0.1%)    ← 녹화 중단 gap (425s)
```
**분석**: 73%가 0.5초 미만 burst, 17%가 1초 근처 (frozen 다운샘플링). Bimodal distribution.

### 12-4. Burst/Silence 패턴 분류
**목적**: VSLAM adapted의 고속 burst → 침묵 → 1Hz 반복 패턴을 정량적으로 분류
```python
# "burst"를 정의: 연속 메시지 중 간격이 0.5s 미만인 것들을 하나의 burst로 묶음
bursts = []
current_burst = [vslam_times[0]]

for i in range(1, len(vslam_times)):
    dt = vslam_times[i] - vslam_times[i-1]
    if dt < 0.5:  # 같은 burst에 속함
        current_burst.append(vslam_times[i])
    else:          # 새 burst 시작
        bursts.append(current_burst)
        current_burst = [vslam_times[i]]
bursts.append(current_burst)

# burst별 통계
for i, b in enumerate(bursts[:5]):   # 처음 5개 burst만 출력
    duration = b[-1] - b[0] if len(b) > 1 else 0
    rate = (len(b) - 1) / duration if duration > 0 else 0
    print(f"Burst #{i}: {len(b)} msgs / {duration:.2f}s "
          f"({rate:.1f} Hz) @ t={b[0]:.1f}s")
```
- burst 분류: 간격 < 0.5s이면 같은 burst, ≥ 0.5s이면 새 burst
- 목적: VSLAM이 데이터를 생성할 때의 실제 속도 vs 침묵 기간 파악
- `rate = (len-1) / duration`: 메시지 간 평균 속도를 Hz로 계산

**결과**:
```
총 202 bursts
Burst #0: 257 msgs / 0.42s (606.4 Hz) @ t=16.1s  ← 초기 대량 burst
Burst #1: 1 msgs @ t=17.0s
Burst #2: 1 msgs @ t=18.0s
Burst #3: 1 msgs @ t=19.0s
...
대부분 1~2 msgs, 간격 1.0s (frozen downsampled)
```
**분석**: 초기에 257개 burst 후, 대부분 1초 간격의 단일 메시지 (frozen 상태에서 1Hz 다운샘플링).

### 12-5. Frozen Detection 확인
**목적**: 1초 간격 메시지들이 실제로 값 변화가 없는 "frozen" 반복인지 검증
```python
vslam_data = topics['/visual_slam/tracking/odometry_adapted']

# 1초 근처 간격만 추출 (0.8~1.2s)
frozen_candidates = []
for i in range(1, len(vslam_data)):
    dt = vslam_data[i][0] - vslam_data[i-1][0]
    if 0.8 <= dt <= 1.2:
        delta_val = abs(vslam_data[i][1] - vslam_data[i-1][1])
        frozen_candidates.append((dt, delta_val))

total_near_1s = len(frozen_candidates)
no_change = sum(1 for _, dv in frozen_candidates if dv < 0.0001)
pct = no_change / total_near_1s * 100

print(f"Intervals near 1s: {total_near_1s}")
print(f"Value unchanged (delta < 0.1mm): {no_change} ({pct:.0f}%)")
```
- `0.8 <= dt <= 1.2`: `frozen_publish_rate=1.0Hz`에 해당하는 간격 필터
- `delta_val < 0.0001` (0.1mm): 위치 변화가 사실상 0인지 판정
- 목적: adapter의 `_check_frozen()` → `_on_frozen_tick()` 경로 동작 확인

**결과**:
```
Intervals near 1s: 186
Value unchanged (delta < 0.1mm): 183 (98%)
```
**분석**: 1초 간격 메시지의 98%가 값 변화 없음 — `frozen_publish_rate=1.0Hz` 다운샘플링 확인.
adapter 설정: `frozen_threshold=10`, `frozen_pos_tolerance=1e-6`, `frozen_publish_rate=1.0Hz`
→ VSLAM이 10회 연속 동일 pose를 발행하면 frozen 판정, 이후 1Hz로 repeat → NAV2에 중복 투입 방지.

### 12-6. VSLAM-EKF 값 대조
**목적**: 동일 시점에서 VSLAM adapted와 EKF(odom) position.x의 차이를 측정하여 이상 유무 확인
```python
# 시간 매칭: 각 VSLAM 메시지에 가장 가까운 EKF 메시지를 찾음 (0.1초 이내)
import bisect

ekf_data = topics['/odom']
ekf_times = [d[0] for d in ekf_data]
ekf_vals = [d[1] for d in ekf_data]

diffs = []
for t_vslam, v_vslam in vslam_data:
    idx = bisect.bisect_left(ekf_times, t_vslam)  # 이진 탐색으로 근접 시점 찾기
    # 앞뒤 후보 중 더 가까운 것 선택
    best_idx = idx
    if idx > 0 and (idx >= len(ekf_times) or
        abs(ekf_times[idx-1] - t_vslam) < abs(ekf_times[idx] - t_vslam)):
        best_idx = idx - 1
    dt = abs(ekf_times[best_idx] - t_vslam)
    if dt < 0.1:  # 0.1초 이내만 매칭
        diff = v_vslam - ekf_vals[best_idx]
        diffs.append((t_vslam, diff))

print(f"Matched pairs: {len(diffs)}")
print(f"Mean diff: {np.mean([d[1] for d in diffs]):.4f}m")
print(f"Std diff: {np.std([d[1] for d in diffs]):.4f}m")
print(f"All >5cm discrepancy: {all(abs(d[1]) > 0.05 for d in diffs)}")
```
- `bisect.bisect_left()`: 정렬된 배열에서 이진 탐색으로 O(log n) 시간에 가장 가까운 시점 찾기
- `dt < 0.1`: 0.1초 이내에 매칭되는 쌍만 사용 (시간 차이가 크면 비교 의미 없음)
- `v_vslam - ekf_val`: 동일 시점의 position.x 차이 → 일정하면 오프셋, 변동이 크면 문제

**결과**:
```
Matched pairs: 271
Mean diff: -0.1127m
Std diff: 0.0279m
All >5cm discrepancy: True
```

### 12-7. VSLAM-EKF 오프셋 원인 분석
**목적**: -0.113m 일정 오프셋의 원인 규명
```python
# 시간 구간별 차이 분포
for t_start, t_end, label in [(16, 60, "초반"), (486, 552, "중간"), (552, 649, "이동")]:
    subset = [(t, d) for t, d in diffs if t_start <= t <= t_end]
    if subset:
        vals = [d for _, d in subset]
        print(f"{label} (t={t_start}-{t_end}): "
              f"n={len(subset)}, mean={np.mean(vals):.4f}m, "
              f"std={np.std(vals):.4f}m")
```
- 시간 구간 분리: 정지(초반/중간)와 이동 시 오프셋 변화 관찰
- 정지 시 일정 → 초기 오프셋, 이동 시 변화 → 센서 간 추정량 차이

**결과**:
```
초반 (t=16-60):   n=251, mean=-0.1164m, std=0.0018m  (거의 상수)
중간 (t=486-552): n=4,   mean=-0.1576m, std=0.0015m
이동 (t=552-649): n=16,  mean=-0.0553m, std=0.0328m  (줄어듦)
```
**분석**:
- 정지 시: VSLAM이 카메라 원점에서 시작하여 `-0.116m` 오프셋 유지
- 이동 시: VSLAM 병진 추정이 wheel/RF2O보다 보수적 → 차이 감소
- `odom1_differential: true` 설정 때문에 EKF는 **절대값이 아닌 frame-to-frame 변위**만 사용 → 오프셋이 EKF에 전달되지 않음 → **문제 아님**

### 12-8. 주행 구간 4센서 교차 비교
**목적**: 이동 시 4개 센서의 position.x 변화를 비교하여 센서 융합 적절성 판단
```python
# 각 토픽에서 특정 시점의 값 추출 (nearest neighbor)
checkpoints = [550, 555, 560, 580, 600, 640]

for t_check in checkpoints:
    row = f"t={t_check}: "
    for topic_name in ['/odom', '/odom_adapted',
                       '/odom_rf2o_adapted',
                       '/visual_slam/tracking/odometry_adapted']:
        data = topics[topic_name]
        # nearest neighbor: 가장 가까운 시점의 값
        nearest = min(data, key=lambda d: abs(d[0] - t_check))
        row += f"{topic_name.split('/')[-1]}={nearest[1]:.3f}  "
    print(row)
```
- `min(data, key=lambda d: abs(d[0] - t_check))`: 목표 시점에서 가장 가까운 데이터 포인트 선택
- 4개 토픽을 같은 시점에서 비교 → 센서 간 일관성 및 EKF 융합 결과 평가

**결과**:
| 시점 | EKF | Wheel | RF2O | VSLAM | VSLAM-EKF |
|------|-----|-------|------|-------|-----------|
| t=550 | -0.003 | 0.000 | -0.012 | -0.159 | -0.156 |
| t=560 | 0.255 | 0.453 | 0.457 | 0.207 | -0.048 |
| t=640 | 0.254 | 0.453 | 0.460 | 0.195 | -0.060 |

**분석**: EKF(0.255)는 wheel(0.453)/RF2O(0.457)과 VSLAM(0.207)의 가중 평균 — 정상 융합.

### 12-9. 425초 Gap 분석
**목적**: t=60.6→485.9의 425초 대규모 gap이 센서 문제인지 녹화 중단인지 판별
```python
# gap 구간(t=60-486)의 토픽별 메시지 수 확인
for topic_name, data in sorted(topics.items()):
    in_gap = [d for d in data if 60 <= d[0] <= 486]
    print(f"{topic_name}: {len(in_gap)} msgs in gap")
```
**결과**:
```
/odom:                28 msgs in gap
/odom_adapted:        48 msgs in gap
/odom_rf2o_adapted:    4 msgs in gap
/visual_slam:          0 msgs in gap
```
**분석**: **모든** 토픽이 동시에 거의 데이터 없음 → 센서 문제가 아니라 **녹화(plotjuggler) 중단**. VSLAM 특정 문제 아님.

### 12-10. EKF IMU 융합 설정 확인
**목적**: IMU가 VSLAM과 어떻게 융합되는지, VSLAM 간격 문제와 관련이 있는지 확인
```bash
# EKF 설정에서 IMU 관련 부분 확인
grep -A 5 "imu0" src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml
```
- `grep -A 5`: 매칭 줄과 그 **다음 5줄** (`-A` = After) 함께 출력

**결과**:
```yaml
imu0: /imu/data_calibrated
imu0_config: [false, false, false,   # x, y, z 위치 ✗
              false, false, false,    # roll, pitch, yaw 방향 ✗
              false, false, false,    # vx, vy, vz 속도 ✗
              false, false, true,     # vroll, vpitch, vyaw ← vyaw만 ✓
              false, false, false]    # ax, ay, az 가속도 ✗
imu0_differential: false
imu0_relative: false
```
**분석**:
- IMU는 **vyaw (yaw rate, angular_velocity.z)만** EKF에 제공
- 방향(orientation), 선가속도(ax,ay,az), 위치/속도 모두 사용하지 않음
- VSLAM(odom1)은 x, y, vx, vy만 제공 → **IMU와 VSLAM은 직접 융합되지 않음**
- 각각 독립적으로 EKF에 입력: IMU=회전, VSLAM=병진 → 역할 분리
- **VSLAM의 발행 간격(1Hz downsampling)은 IMU와 무관**

### 12-11. Adapter frozen detection 코드 확인
**목적**: VSLAM adapter의 frozen detection 로직을 소스 코드에서 직접 확인
```bash
grep -n "frozen" src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py
```
- `grep -n`: 줄 번호 포함 출력 (`-n` = line number)
- frozen 관련 변수/함수 위치 파악

**결과** (핵심 부분):
```python
# 파라미터 선언
self.frozen_threshold = 10        # frozen 판정까지 필요한 연속 동일 위치 횟수
self.frozen_publish_rate = 1.0    # frozen 중 발행 주기 (Hz)
self.frozen_pos_tolerance = 1e-6  # 위치 동일 판정 임계값 (m)

# _check_frozen(): VSLAM odom 수신 시마다 호출
def _check_frozen(self, pose):
    # 이전 pose와의 거리 계산
    dist = sqrt((pose.x - self._prev_x)**2 + (pose.y - self._prev_y)**2 + ...)
    if dist < self.frozen_pos_tolerance:     # 0.000001m 이내
        self._frozen_count += 1              # 연속 동일 카운트 증가
    else:
        self._frozen_count = 0               # 변화 감지 시 리셋
        self._is_frozen = False

    if self._frozen_count >= self.frozen_threshold:  # 10회 이상
        self._is_frozen = True               # frozen 상태 진입

# _on_frozen_tick(): Timer 콜백 (1Hz)
def _on_frozen_tick(self):
    if self._is_frozen:
        self._publish_adapted(self._last_odom)  # 마지막 odom을 1Hz로 재발행
```
**분석**: VSLAM이 동일 위치를 10번 연속 발행하면 frozen 판정 → 이후 1Hz(1초 간격)로만 중복 발행. CSV에서 관찰된 "1초 간격 + 값 변화 없음" 패턴과 **정확히 일치**. 정상 보호 기능.

---

## 부록: 사용 rclpy 패턴 정리

### A. QoS 설정 (센서 데이터)
```python
from rclpy.qos import QoSProfile, ReliabilityPolicy, HistoryPolicy
qos_sensor = QoSProfile(
    depth=5,
    reliability=ReliabilityPolicy.BEST_EFFORT,
    history=HistoryPolicy.KEEP_LAST
)
```
- 센서 토픽(odom, IMU, scan)은 BEST_EFFORT QoS 필수
- RELIABLE로 구독 시 데이터 수신 불가 (DDS QoS 불일치)

### B. QoS 설정 (Map)
```python
from rclpy.qos import DurabilityPolicy
qos_map = QoSProfile(
    depth=1,
    reliability=ReliabilityPolicy.RELIABLE,
    history=HistoryPolicy.KEEP_LAST,
    durability=DurabilityPolicy.TRANSIENT_LOCAL
)
```
- `/map` 토픽은 TRANSIENT_LOCAL durability 필수 (late-joining subscriber에게 마지막 메시지 전달)

### C. Nav2 Action Client
```python
from nav2_msgs.action import NavigateToPose
from rclpy.action import ActionClient

client = ActionClient(node, NavigateToPose, 'navigate_to_pose')
goal = NavigateToPose.Goal()
goal.pose.header.frame_id = 'map'
goal.pose.pose.position.x = target_x
goal.pose.pose.position.y = target_y
goal.pose.pose.orientation.z = math.sin(yaw / 2.0)
goal.pose.pose.orientation.w = math.cos(yaw / 2.0)
```

### D. DDS Discovery 문제 우회
- VS Code 터미널에서 `ros2 topic hz/echo`는 매번 DDS participant 생성 → discovery timeout
- **해결**: rclpy 단일 프로세스에서 모든 토픽 동시 구독
- 환경변수 필수: `export RMW_IMPLEMENTATION=rmw_fastrtps_cpp`

---

## 13. 실주행 데이터 CSV 2차 분석 — VSLAM 폭주 발견

> 파일: `/home/jetson/jupiter_ws_ros2/plot_data_position_x.csv` (2차, Nav2 다중 goal 주행)
> 기록: foxglove에서 Nav2를 통해 7개 목표 지점 연속 주행

### 13-1. CSV 구조 및 토픽 통계
**목적**: 새 CSV 데이터의 규모, 토픽 목록, 값 범위 파악
```bash
wc -l plot_data_position_x.csv
head -3 plot_data_position_x.csv
awk -F',' 'NR>1{print $3}' plot_data_position_x.csv | sort -u
```
- `wc -l`: 파일의 총 줄 수 (23,818행)
- `head -3`: 헤더 + 첫 2행의 데이터 구조 확인
- `awk -F',' 'NR>1{print $3}'`: CSV 열 3(topic)만 추출
  - `-F','`: 콤마를 필드 구분자로 설정
  - `NR>1`: 1행(헤더) 건너뜀
  - `{print $3}`: 3번째 필드 출력
- `sort -u`: 정렬 후 중복 제거 (`-u` = unique)

**Python 토픽별 통계**:
```python
import csv
from collections import defaultdict
import numpy as np

topics = defaultdict(list)
with open('plot_data_position_x.csv') as f:
    reader = csv.DictReader(f)
    for row in reader:
        t = float(row['elapsed time'])
        topic = row['topic'].split('.pose')[0]  # 토픽명만 추출
        val = float(row['value'])
        topics[topic].append((t, val))

for name in sorted(topics.keys()):
    data = topics[name]
    ts = [d[0] for d in data]
    vs = [d[1] for d in data]
    dur = ts[-1] - ts[0]                    # 기록 총 시간
    hz = (len(data)-1)/dur if dur > 0 else 0  # 평균 발행률
    print(f'{name}: count={len(data)}, {hz:.1f}Hz, '
          f'range=[{min(vs):.4f}, {max(vs):.4f}]')
```
**결과**:
| 토픽 | 메시지 수 | 평균 Hz | 값 범위 |
|------|-----------|---------|---------|
| `/odom` (EKF) | 5,939 | 27.4 | [-3.66, 1.65] |
| `/odom_adapted` (wheel) | 9,883 | 45.5 | [-2.50, 2.67] |
| `/odom_rf2o_adapted` | 1,284 | 5.9 | [-1.94, 2.93] |
| `/visual_slam/.../adapted` | **6,711** | **31.5** | **[-50.81, 2.27]** |

**핵심 발견**: VSLAM adapted의 position.x 범위가 **-50.8m**까지 도달 — 카타스트로픽 실패.

### 13-2. VSLAM 발행 간격 분석
**목적**: VSLAM adapted의 발행 패턴 확인 (이전 1차 CSV: 1.2Hz vs 이번: 31.5Hz)
```python
vslam = topics['/visual_slam/tracking/odometry_adapted']
vt = [d[0] for d in vslam]
dt = np.diff(vt)   # 연속 메시지 간 시간 차이

print(f'count={len(dt)}, mean={np.mean(dt):.3f}s, '
      f'median={np.median(dt):.4f}s')
bins = [(0,0.1),(0.1,0.5),(0.5,1.0),(1.0,2.0),(2.0,5.0),(5.0,30.0)]
for lo,hi in bins:
    c = np.sum((dt>=lo)&(dt<hi))
    print(f'  [{lo:.1f}-{hi:.1f}s): {c} ({c/len(dt)*100:.1f}%)')
```
- `np.diff()`: 연속 타임스탬프 차이 계산 → 메시지 간격
- 구간별 히스토그램: burst(고속) vs silence(저속) 분포

**결과**: 98.3%가 <0.1s (고속), 대형 gap 5개 (5~26초). 이전 CSV와 달리 frozen downsampling 비율 낮음 → 로봇이 지속적으로 이동하여 frozen 판정 안됨.

### 13-3. VSLAM 값 점프 검출
**목적**: position.x에서 0.1m 이상의 급격한 값 변화 감지
```python
vv = [d[1] for d in vslam]
dv = np.diff(vv)   # 연속 값 차이

big_jumps = [(vt[i+1], dv[i], vv[i], vv[i+1])
             for i in range(len(dv)) if abs(dv[i]) > 0.1]
for t, d, v0, v1 in big_jumps:
    print(f't={t:.1f}: {v0:.4f}→{v1:.4f} (delta={d:+.4f}m)')
```
- `np.diff(values)`: 연속 position.x 값의 차이 계산
- `abs(dv) > 0.1`: 0.1m 이상 점프 필터

**결과**: 29개 점프 감지.
- t=68-69: 0.3m 급감소 반복 (VSLAM 불안정기)
- t=73.4: **-9.39m 점프** (0.79→-8.60) ← 카타스트로픽 실패
- t=80.6: -4.02m 점프
- 이후 지속적 음방향 drift → -50.8m

### 13-4. VSLAM drift rate 시간별 추적
**목적**: VSLAM position.x의 시간별 변화율 추적으로 파괴 시점 정확히 파악
```python
for t_start in range(13, 230, 5):
    t_end = t_start + 5
    subset = [(t,v) for t,v in zip(vt,vv)
              if t_start <= t < t_end]
    if len(subset) >= 2:
        rate = (subset[-1][1] - subset[0][1]) / \
               (subset[-1][0] - subset[0][0])
        print(f't={t_start:3d}-{t_end:3d}: '
              f'x=[{subset[0][1]:10.4f}→{subset[-1][1]:10.4f}] '
              f'rate={rate:+.3f} m/s')
```
- 5초 구간별 시작/끝 값과 평균 변화율 계산
- `rate = Δvalue / Δtime`: m/s 단위 drift rate

**결과** (핵심 구간):
```
t= 48- 53: +0.29→+2.20  rate=+0.394 m/s  ← 정상 이동 (과대추정)
t= 68- 73: +1.93→+0.79  rate=-1.092 m/s  ← 급격한 감소!
t= 73- 78: -8.60→-8.61  rate=-0.002 m/s  ← 점프 후 안정 (-8.6에서)
t= 78- 83: -8.61→-13.12 rate=-0.914 m/s  ← 2차 폭주
t= 88- 93: -33.50→-33.46 rate=+0.021 m/s ← 3차 점프 후 안정
t=158-163: -50.81→-50.75 rate=+0.012 m/s ← 최종 안정화 (-50m)
```

### 13-5. Nav2 이벤트 vs 센서 상태 상관분석
**목적**: Nav2 goal 성공/실패가 VSLAM 상태와 어떻게 연관되는지 파악
```python
import bisect

# Nav2 이벤트 ROS 타임스탬프 → CSV elapsed time 변환
# CSV: t=12.8 = ROS 1773129564.0 → offset = 1773129551.2
offset = 1773129551.2

nav2_events = [
    (1773129591.8, 'Goal1 START'),
    (1773129597.98,'Goal1 planner FAIL'),
    (1773129628.8, 'Goal2 START'),
    (1773129637.3, 'Goal2 SUCCESS'),
    # ... (전체 목록)
]

ekf_t = [d[0] for d in topics['/odom']]
ekf_v = [d[1] for d in topics['/odom']]
vs_t = [d[0] for d in vslam]
vs_v = [d[1] for d in vslam]

for ros_t, label in nav2_events:
    elapsed = ros_t - offset
    # bisect.bisect_left: 정렬된 배열에서 이진 탐색
    ei = bisect.bisect_left(ekf_t, elapsed)
    vi = bisect.bisect_left(vs_t, elapsed)
    # min(idx, len-1): 배열 범위 초과 방지
    ev = ekf_v[min(ei, len(ekf_v)-1)]
    vv = vs_v[min(vi, len(vs_v)-1)]
    print(f't={elapsed:6.1f} EKF={ev:8.4f} '
          f'VSLAM={vv:10.4f} diff={vv-ev:+10.4f} {label}')
```
**결과** (핵심):
| Event | t(s) | EKF | VSLAM | diff | 비고 |
|-------|------|-----|-------|------|------|
| Goal1 START | 40.6 | -0.02 | -0.01 | +0.01 | 정상 |
| Goal1 FAIL | 46.8 | -0.01 | +0.02 | +0.03 | 정상 (costmap 원인) |
| Goal2 SUCCESS | 86.1 | 0.98 | **-14.26** | -15.25 | VSLAM 이미 폭주 |
| Goal5 SUCCESS | 123.5 | -2.94 | -28.77 | -25.83 | EKF도 -2.9m 오염 |
| Goal7 FAIL | 162.5 | 0.94 | **-50.76** | -51.71 | VSLAM -50m |
| Goal7 SUCCESS | 213.7 | 0.34 | -48.19 | -48.53 | 55초 후 성공 |

### 13-6. EKF 오염 정량화
**목적**: VSLAM drift가 EKF를 얼마나 왜곡시켰는지 wheel과 비교하여 평가
```python
for t_check in [13,50,90,120,130,140,165,200,229]:
    e = min(ekf, key=lambda d: abs(d[0]-t_check))
    w = min(wheel, key=lambda d: abs(d[0]-t_check))
    r = min(rf2o, key=lambda d: abs(d[0]-t_check))
    print(f't={t_check:3d}: EKF={e[1]:8.4f} '
          f'Wheel={w[1]:8.4f} RF2O={r[1]:8.4f} '
          f'EKF-Whl={e[1]-w[1]:+8.4f}')
```
- `min(data, key=lambda d: abs(d[0]-t_check))`: 목표 시점에 가장 가까운 데이터 포인트

**결과**: **EKF-Wheel 최대 divergence -1.39m** (t=120). VSLAM의 -50m drift가 differential 모드를 통해 EKF를 ~1.4m 음방향으로 왜곡.

---

## 14. VSLAM Adapter prev_odom 재수정

### 14-1. 버그 분석
**목적**: 이전 수정 (11. prev_odom 항상 갱신)이 teleportation bug를 유발한 원인 확인

```python
# 이전 수정 (버그):
self.prev_odom = pose  # 항상 갱신 → anomaly 값이 새 기준점이 됨

# 문제 시나리오:
# VSLAM 0.79 → -8.60 (Δ=9.39m) → anomaly ✓, NOT published ✓
# prev_odom = -8.60 (갱신됨 ← 문제!)
# 다음: VSLAM -8.61 → Δ=0.01m < 0.5m → NOT anomaly → good count++
# 30 frames 후: tracking_ok=True → -8.61 published
# EKF: differential delta = -8.61 - 0.79(마지막 발행값) = -9.40m ← 오염!
```

### 14-2. 수정 내용
**파일**: `src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py`
```python
# Before (버그):
self.prev_odom = pose  # 항상 갱신

# After (수정):
if not is_anomaly:
    self.prev_odom = pose  # 정상 시에만 갱신
```
- anomaly 시 prev_odom 유지 → 나쁜 위치가 기준점이 되는 것 방지
- VSLAM이 정상 위치로 복귀 시 prev_odom과의 delta가 작아져 자연 해제
- VSLAM이 영구 이탈 시 영구 lock-out → **의도된 보호 동작**

### 14-3. 연속 anomaly 카운터 추가
```python
self.consecutive_anomaly = 0  # __init__에 추가

# _update_tracking_state():
if is_anomaly:
    self.consecutive_anomaly += 1
    if self.consecutive_anomaly == 300:  # ~5초 @60Hz
        self.get_logger().error(
            'VSLAM appears PERMANENTLY LOST. '
            'VSLAM data is being fully gated.')
else:
    self.consecutive_anomaly = 0
```
- 300회 = 약 5초간 연속 anomaly → "PERMANENTLY LOST" 경고
- 진단용: log_stats에도 `consec_anomaly` 출력 추가

### 14-4. 배포
```bash
pip install -e src/jupiter_nav_VO/ --quiet
```
**결과**: 성공 (Python editable install — 소스 변경 즉시 반영)

---

## 15. Nav2 Planning 실패 분석

### 15-1. 실패 패턴 정리
**목적**: Nav2 planner 실패의 원인 분류
```
Goal 1: planner FAIL at (2.12,0.15) — t=46.8, VSLAM 정상, costmap 85×146
Goal 7: planner FAIL at (0.34,0.39),(0.32,0.52),(0.42,0.39),(0.14,0.51)
         — t=162-194, VSLAM -50m, costmap 254×196
```

### 15-2. 원인
1. **Costmap 확장 중 경로 무효화**: SLAM Toolbox가 탐사 중 맵을 계속 확장 → costmap resize가 planner와 동시 실행 → 일시적 경로 무효
2. **탐사 경계의 불완전 장애물**: 새로 확장된 영역에 LiDAR 스캔이 완전히 채워지기 전 planner 호출 → "장애물 있는 것으로 판정"
3. **Nav2 BT recovery가 정상 작동**: planner 실패 시 `backup` → `costmap clear` → `replan` 순서로 자동 복구

### 15-3. planner loop rate 저하
```
[planner_server]: Planner loop missed its desired rate of 5.0 Hz.
  Current loop rate is 1.04 Hz
```
- costmap resize로 planner 처리 시간 증가 → 5Hz 유지 실패
- 기능 영향: 경로 갱신 주기 저하 (1Hz) → 장애물 회피 반응 느려짐

---

## 16. Foxglove Bridge 경고 분석

### 16-1. 경고 내용
```bash
[foxglove_bridge] [WARN] Failed to add service
  "/visual_slam/set_slam_pose"
  (isaac_ros_visual_slam_interfaces/srv/SetSlamPose):
  package 'isaac_ros_visual_slam_interfaces' not found
```

### 16-2. 원인
- `isaac_ros_visual_slam_interfaces`: Docker 컨테이너 내부에만 설치된 ROS 2 인터페이스 패키지
- `foxglove_bridge`: 호스트에서 실행 → 호스트의 `AMENT_PREFIX_PATH` 검색
- 호스트에는 해당 패키지 미설치 → service introspection 실패

### 16-3. 영향도
- **기능 영향 없음**: VSLAM 토픽 발행/구독은 DDS를 통해 정상 작동 (interface definition 불필요)
- foxglove에서 `set_slam_pose` service를 직접 호출할 수 없지만, 일반적으로 사용하지 않음
- **권장 조치**: 무시 (경고 수준, 반복 출력이나 동작에 영향 없음)
