# 진행과정 2026-04-28: Nav2 좁은 공간 + 정지 회전 + 센서 정렬 + Docker 학습

## 1. 목표

### 1.1 사용자 요청 (시간순)

1. > "isaac 가상환경 컨테이너에 의존성 무결성에 변경이 있었는지 검토. docker container 의 기초부터 구성이 어떻게 되는지에 대한 학습도구를 만들고, 새로 추가한 layer 가 어떻게 동작하는지 이해할 수 있도록 학습도구를 만들어달라."
2. > "확인없이 진행하라" — yarn/nodesource 저장소 검증 후 Dockerfile.jupiter_user + ~/.isaac_ros_common-config 작성.
3. > "없애도 되는 것인지 검증했는가?" — 사용자 지적, yarn 저장소 NVIDIA 의도 추가 확인 후 fail-soft 패턴으로 변경.
4. > "로버가 문을 잘 찾지 못하는 문제. 카메라가 벽 정면일 때 문 인식 못함. 사람을 피해가거나 멈추고 경로를 다시 설정하기 위한 방법도 계획하고 수행하라."
5. > "로버가 제대로된 방향을 잡지 못하는 것 같다. 기존 설정에서는 잘 잡았는데 지금은 그러지 못함. 바퀴 한쪽이 기구적으로 문제가 있어 path 추종 보정 기능이 작동하는지 모르겠다."
6. > "/home/jetson/mcu — 컨트롤러 보드의 임베디드 코드. 분석하여 plan 을 세우라. 현재 로버는 바퀴가 2개 달린 상태이다."
7. > "멈춰있는 상태에서 회전하는 것에 상당한 어려움을 겪고 있다. 전체 구성에서 cmd_vel 과 관련된 구성을 파악하고, 이전의 진행과정 md 파일들을 분석하여 해당 원인을 찾아라."
8. > "센서들이 만드는 오도메트리의 방향이 서로 어긋난 것 같다. rf2o 의 방향을 기반으로 센서의 방향을 정렬하라."
9. > "회전 명령 값이 너무 작아서 안움직이는 것 같다." — 도구 명령 크기 조정.
10. > "바퀴가 이상한건 기구문제이기 때문에 소프트웨어로 조정이 어려울 것 같다. imu 스케일 미세 조정만 하고 마무리하라. 그리고 nav2 관련 path 를 잘 따라가지 못하는 문제를 수행하라."
11. > "지금까지 한 것들을 정리하라" — 본 문서 트리거.

### 1.2 이전 세션 요약 / 연관 작업

- 2026-04-23/24 작업: nvblox people detection 전환, 3D voxel ↔ Nav2 slice 분리, scan_merger (depth z-slice), nav2_params 의 NvbloxCostmapLayer inflation 0 + cost_scaling 10 → 좁은 통로 통과 위한 1차 튜닝.
- 2026-03-04/18/19/20: RotationShim 도입 + 정지 회전 deadzone 해소 (이번 세션의 정지 회전 회귀 분석에 결정적 참조).
- 2026-04-21: vtheta_samples 11, min_speed_theta 0.20, driver ROT_MIN 게이트 도입 (이번 세션 회귀 복원의 검증 기준).
- 2026-02-25: IMU gyro Z 반전 발견 + driver 코드 보정 — 이번 RF2O 기준 정렬 측정에서 부호 일치로 검증됨.

---

## 2. 대상 시스템 구성

### 2.1 cmd_vel 흐름도 (이번 세션 분석으로 확정)

```
Nav2 controller_server (RotationShim → DWB)
    ├ RotationShim: angular_dist_threshold=0.35 ≥ 0.5 rad/s 고정 회전
    └ DWB: vtheta_samples=11, min_speed_theta=0.20
          PathAlign.scale=20, PathDist.scale=24, BaseObstacle.scale=0.05
   ↓ /cmd_vel_nav
velocity_smoother
   │ deadband_velocity[2]=0.05 (작은 헤딩보정 통과)
   ↓ /cmd_vel
jupiter_driver_compensated.py
   ├ ANGULAR_SCALE_FACTOR=1.0 (모터 교체 후 1:1)
   ├ ROT_MIN_REQUEST_ANGULAR=0.04 / ROT_RUNNING_MIN_ANGULAR=0.12
   ├ MCU_ROT_START_ANGULAR=0.37 (시작 0.12s 키크)
   └ self.bot.set_car_motion(vx, vy, angular_corrected, adjust=True)  ← 2026-04-28 변경
   ↓ Rosmaster_Lib.set_car_motion → 시리얼 (FUNC_MOTION 0x12)
       cmd 의 byte[4] = CAR_TYPE | 0x80 (adjust 비트 ON)
   ↓
MCU (STM32F1)
   ├ parm & 0x80 = adjust = 1 → Motion_Ctrl(Vx, Vy, Vz, 1)
   ├ Differential_Ctrl: speed_L = Vx - Vz, speed_R = Vx + Vz
   ├ Differential_Yaw_Calc: PID_Yaw(P=0.4, I=0, D=0.1) 출력 ±π/6 으로 좌우 모터 보정
   └ Motor PID (모터별 개별 PID) + ESC 방안A-4 deadzone skip
   ↓
좌·우 모터 + 인코더
   ↓ /jupiter/get_vel (50Hz), /jupiter/imu (100Hz)
   ↓ imu_calib → /imu/data_calibrated (gyro_scale 0.984)
   ↓
robot_localization EKF (50Hz)
   입력: /odom_adapted (vx, vy, vyaw)
         /visual_slam/.../odometry_adapted (x, y, vx, vy)  — yaw 비활성
         /odom_rf2o_adapted (x, y, differential)  — yaw 비활성
         /imu/data_calibrated (vyaw)  — 핵심 yaw rate 소스
   출력: /odom (EKF 융합)
   ↓ map → odom 은 SLAM Toolbox 가 보정
```

### 2.2 4-Layer Docker chain 구성 (이번 세션 학습 자료 작성으로 명확화)

```
Dockerfile.aarch64 (= Dockerfile.base symlink)
    NVIDIA CUDA 12.6.11 L4T base, Node.js 18 + yarn, VPI/cuDSS/CV-CUDA
        ↓
Dockerfile.ros2_humble
    ROS 2 Humble + nav2 + MoveIt2, rosdep init/update, source 빌드 (rclcpp 패치)
        ↓
Dockerfile.realsense
    librealsense v2.55.1 소스 빌드, udev rules, realsense-ros bloom debian
        ↓
Dockerfile.jupiter_user (← 2026-04-28 추가)
    pointcloud-to-laserscan, peoplesemseg/peoplenet-models-install apt 박제
    rosdep update DB 캐시 박제
```

활성화: `~/.isaac_ros_common-config` 의 `CONFIG_IMAGE_KEY=ros2_humble.realsense.jupiter_user`.

---

## 3. 문제 분석

### 3.1 [이슈 A] Isaac ROS 컨테이너 매번 재설치 부담

- **증상**: `run_dev.sh` 실행 시마다 `apt install ros-humble-isaac-ros-peoplesemseg-models-install` 등을 수동 실행해야 함.
- **데이터**: `run_dev.sh:186-187` 에서 `docker rm` 으로 종료된 컨테이너 자동 삭제 — apt 변경분 휘발.
- **근본 원인**: Isaac ROS Common 의 컨테이너 관리 정책이 stateless (한 번 종료되면 image 까지 새로 시작).
- **조치 방향**: `Dockerfile.<key>` chain 패턴으로 베이스 이미지에 영구 박제.

### 3.2 [이슈 B] yarn 저장소 GPG 만료로 빌드 실패

- **증상**: jupiter_user layer 의 `apt-get update` 가 exit 100 → `apt install` 차단.
- **데이터**: `NO_PUBKEY 62D54FD4003F6525`, yarn 저장소 GPG 키 만료.
- **초기 시도** (잘못된 접근): yarn/nodesource 저장소 file 삭제. **사용자 지적** "없애도 되는 것인지 검증했는가?" 후 재검증.
- **검증 결과**:
  - `Dockerfile.base:201-207` 에서 NVIDIA 가 의도적으로 yarn/nodesource 추가.
  - `extra_rosdeps.yaml:9263-9270` 에서 `nodejs`, `yarnpkg` rosdep keys 등록.
  - `nvblox_foxglove/package.json` 가 yarn 빌드 의존.
- **근본 원인**: yarn 저장소는 NVIDIA 의도 + 우리 트리 사용 중 — 삭제 금지.
- **조치 방향**: `apt-get update` 의 부분 실패를 그룹화로 흡수 (`{ apt-get update || echo ... ; } && apt-get install`).

### 3.3 [이슈 C] `USER admin` 디렉티브로 docker run 실패

- **증상**: 빌드는 성공했으나 `docker run` 시 `unable to find user admin: no matching entries in passwd file`.
- **근본 원인**: admin 사용자는 Dockerfile build 시점에 없음. `workspace-entrypoint.sh` 가 컨테이너 시작 시 동적 생성 (HOST_USER_UID/GID 일치).
- **조치 방향**: Dockerfile.jupiter_user 에서 `USER admin` 디렉티브 제거. root 로 layer 종료해도 entrypoint 가 admin 으로 attach.

### 3.4 [이슈 D] Nav2 좁은 공간 (1m 문 / 1.8m 통로 + 사람) 통과 실패

- **증상**: 직전 세션 (`nvblox_layer.inflation_distance: 0`, `cost_scaling 10`, `inflation_radius 0.30`) 적용 후에도 backup recovery 발화.
- **데이터** (이번 세션 진단):
  - footprint 0.36×0.245 (inscribed 0.1225), 1m 문 free 폭 = 1.0 - 2×0.30 = 0.40m → 빠듯.
  - Global planner = NavFn (grid-based), footprint 회전 미고려.
  - PathDist=32 강함 + 좁은 통로 → trajectory reject.
  - `RotationShim.forward_sampling=0.3`, `vtheta_samples=11` 검증값.
- **근본 원인 (1차)**: NavFn 의 footprint 회전 미인식.
- **조치 방향 (1차)**: SmacPlannerHybrid + DUBIN motion model 도입.

### 3.5 [이슈 E] SmacPlannerHybrid 도입 후 "방향 못 잡음" 회귀

- **증상**: SmacPlannerHybrid 적용 후 로봇이 정지 시 회전을 못 함, plan 추종 불안정.
- **데이터**:
  - Hybrid + DUBIN 의 `minimum_turning_radius=0.20` → plan 자체가 곡선.
  - 차동구동 + 한쪽 바퀴 기구 이상 → 곡선 추종 시 양쪽 모터 속도차 명령 불일치 → 누적 이탈.
  - RotationShim 의 정지 회전 (0.5 rad/s 고정) 처리가 plan 의 곡선에 의해 무력화.
- **근본 원인**: Hybrid 의 motion model 곡선 plan 이 정지 회전 기능을 회피.
- **조치 방향**: SmacPlanner2D (motion model 없는 holonomic A*) 로 다운그레이드. NavFn 의 RotationShim 정지 회전 동작 회복 + SmacPlanner 의 footprint collision check 정확성 유지.

### 3.6 [이슈 F] 정지 회전 어려움 — 이번 세션 회귀 발견

- **증상**: 사용자 "멈춰있는 상태에서 회전하는 것에 상당한 어려움".
- **데이터** (이전 진행과정 md 추적):
  - 2026-03-04: deadzone 발견, RotationShim + min_speed_theta 0.30 도입.
  - 2026-03-18: forward_sampling_distance 0.5→0.3 (짧은 path RotationShim 활성 보장).
  - 2026-04-21: vtheta_samples 20→11 (1/19=0.0526 병적 최소 샘플 제거).
  - 이번 세션 04-28 변경: forward_sampling 0.3→0.6 (벽 inflation 오인 방지 명목), vtheta_samples 11→21 (좁은 통로 분해능 명목), PathDist 20→28.
- **근본 원인**: 이번 세션 변경이 03-18/04-21 검증값을 회귀시켜 RotationShim 활성 판단이 부정확 + vtheta sample 평가 timing 압박.
- **조치 방향**: 검증값으로 부분 롤백 (forward_sampling 0.3, vtheta_samples 11, PathDist 24).

### 3.7 [이슈 G] 센서 yaw 정렬 — RF2O 기준 측정

- **증상**: 사용자 "센서들이 만드는 오도메트리의 방향이 서로 어긋난 것 같다".
- **데이터** (sensor_yaw_align.py 측정, speed=0.6 × 4s):
  - WHEEL: 0.858× (under-report), L/R 비대칭 8.6%.
  - RF2O: 1.000 (기준).
  - VSLAM: 0.000 (토픽 미수신, Docker cuVSLAM 미실행 추정).
  - EKF: 1.002× (거의 완벽 정렬).
  - IMU: 1.041× (4.1% over-report).
  - **부호: 모든 활성 센서 OK** (LEFT/RIGHT 부호 일관).
- **근본 원인 분석**:
  - 부호 반전 / 명시적 미스얼라인먼트는 없음.
  - EKF 가 1.002× 인 이유: IMU(1.041) over + WHEEL(0.858) under 가 우연히 상쇄.
  - WHEEL L/R 비대칭 8.6% — 한쪽 바퀴 마찰 차이의 정량 측정 (사용자 언급 "한쪽 바퀴 기구 이상").
  - VSLAM 토픽 미수신 — Docker cuVSLAM 미실행 (EKF 에서 yaw 비활성이라 navigation 무영향).
- **조치 방향**: IMU 4% 정밀화 (소프트웨어). WHEEL 8.6% 비대칭은 기구 문제 (사용자 결정: 소프트 보정 어려움 — MCU yaw PID 자동 보정 활성으로 우회).

### 3.8 [이슈 H] MCU yaw PID 자동 보정이 비활성화된 상태

- **증상**: WHEEL L/R 비대칭으로 직진 yaw drift 가 MCU 측에서 보정되지 않음.
- **데이터** (cmd_vel chain 추적):
  - MCU `app_pid.c:16`: `pid_Yaw = {P=0.4, I=0, D=0.1}` 가 `Differential_Yaw_Calc(yaw)` 에서 IMU yaw 입력으로 좌우 모터 속도 보정.
  - MCU `protocol.c:530-545`: `FUNC_MOTION` 핸들러가 `parm & 0x80 = adjust` 로 yaw 보정 활성/비활성 결정.
  - `Rosmaster_Lib.py:34`: `__CAR_ADJUST = 0x80` 상수 정의.
  - `Rosmaster_Lib.py:613-615` (set_car_run): `car_type | self.__CAR_ADJUST` 패턴 이미 사용.
  - `Rosmaster_Lib.py:643` (set_car_motion): `self.__CAR_TYPE` 만 보냄, **0x80 비트 미설정**.
  - `jupiter_driver_compensated.py:357`: `set_car_motion(vx, vy, angular_corrected)` 호출 — adjust 미전달.
- **근본 원인**: `set_car_motion` 이 set_car_run 의 검증된 adjust 패턴을 누락 — MCU yaw 보정 루프가 cmd_vel 채널에선 항상 OFF.
- **조치 방향**: Rosmaster_Lib.set_car_motion 에 adjust 인자 추가 + driver 호출 시 adjust=True. set_car_run 의 동일 패턴 (CAR_TYPE | __CAR_ADJUST=0x80).

---

## 4. 적용한 변경

### 4.1 Docker 의존성 박제 + 학습 자료

**파일**:
- `~/.isaac_ros_common-config` (신규): `CONFIG_IMAGE_KEY=ros2_humble.realsense.jupiter_user`
- `Dockerfile.jupiter_user` (신규):
  ```dockerfile
  ARG BASE_IMAGE=ubuntu:22.04
  FROM ${BASE_IMAGE}
  USER root
  RUN { apt-get update || echo "yarn GPG fail-soft"; } && \
      apt-get install -y --no-install-recommends \
          ros-humble-pointcloud-to-laserscan \
          ros-humble-isaac-ros-peoplesemseg-models-install \
          ros-humble-isaac-ros-peoplenet-models-install \
      && rm -rf /var/lib/apt/lists/*
  RUN rosdep update --rosdistro=humble --include-eol-distros || true
  # USER admin 디렉티브 사용 금지 (workspace-entrypoint.sh 가 동적 생성)
  ```
- `/home/jetson/docker_learning/` 5개 마크다운 + 1개 셸 (총 1431줄):
  - `README.md` (69), `01_docker_basics.md` (212), `02_isaac_ros_chain.md` (290),
  - `03_run_dev_workflow.md` (314), `04_jupiter_user_layer.md` (328), `05_practice.sh` (218).

**의존성 무결성 검증 결과** (`04_jupiter_user_layer.md` 의 §4.4):

| 항목 | 결과 | 판정 |
|---|---|---|
| 3개 APT 패키지 base/humble/realsense 중복 | 신규 추가 (매치 0) | ✓ |
| Yarn/Node.js 저장소 보존 | rm 명령 없음 | ✓ |
| rosdep update 멱등성 | 캐시 갱신만 | ✓ |
| USER admin 동적 생성 | entrypoint 에서 처리 | ✓ |
| 소스 목록 파괴 | 없음 | ✓ |

### 4.2 Nav2 좁은 공간 통과 — 5단계 변경 누적

**파일**: `nav2_params_fused.yaml` + `behavior_trees/navigate_to_pose_no_backup.xml`.

| 단계 | 항목 | 변경 | 근거 |
|---|---|---|---|
| Stage 1 | Planner | NavfnPlanner → SmacPlannerHybrid → **SmacPlanner2D** | footprint-aware + 정지 회전 동작 회복 |
| Stage 2 | inflation_radius (local+global) | 0.30 → **0.20** | 1m 문 free 폭 0.40→0.60m |
| Stage 2 | cost_scaling_factor | 10 → **5** | 감쇠 완만 |
| Stage 3 | controller_server.failure_tolerance | 3.0 → **5.0** | 사람 횡단 50 cycles 대기 |
| Stage 3 | BT `Wait wait_duration` | 2 → **5s** | 사람 횡단 인내 |
| Stage 3 | BT `number_of_retries` | 6 → **10** | 사람 길게 머물러도 |
| Stage 3 | BT `RateController hz` | 1.0 → **0.5** | replanning 안정 |
| Stage 3 | BT recovery `<Spin>` | 90° 만 → **45°+90°** | 카메라 측면 시야 우선 |
| Stage 4 | local_costmap width/height | 3 → **5** | 회피 trajectory 탐색 공간 |

### 4.3 정지 회전 회귀 복원

**근거**: 03-18/04-21 검증값 vs 이번 세션 회귀 변경 비교.

| 파라미터 | 회귀 직전 (이번 세션 04-24~28) | **복원 후** | 검증 시점 |
|---|---:|---:|---|
| `forward_sampling_distance` | 0.6 | **0.3** | 2026-03-18 |
| `vtheta_samples` | 21 | **11** | 2026-04-21 |
| `PathDist.scale` | 28 | **24** (절충) | — |

### 4.4 IMU 스케일 정밀화

**파일**: `nav2_vslam_fused.launch.py:172`.
- `imu_gyro_scale: 1.025 → 0.984`
- 근거: sensor_yaw_align.py 측정 IMU/RF2O = 1.041 → 1.025/1.041 ≈ 0.984.
- 효과: IMU vyaw 가 RF2O 의 절대 회전과 정확히 매칭. EKF /odom yaw 추정 정밀도 향상.

### 4.5 MCU Yaw PID 자동 보정 활성화 (핵심)

**파일 1**: `Rosmaster_Lib.py:632-650`.
- `def set_car_motion(self, v_x, v_y, v_z, adjust=False):` — adjust 인자 추가.
- `car_type_byte = (self.__CAR_TYPE | self.__CAR_ADJUST) if adjust else self.__CAR_TYPE` — set_car_run 의 검증된 패턴.
- cmd 배열의 `self.__CAR_TYPE` → `car_type_byte` 변경.

**파일 2**: `jupiter_driver_compensated.py:362`.
- `self.bot.set_car_motion(vx, vy, angular_corrected)` → `set_car_motion(..., adjust=True)`.

**메커니즘**:
```
WHEEL 비대칭 8.6% (좌회전 한쪽 모터 약함) → 직진 yaw drift
   ↓ adjust=True 활성
MCU Differential_Yaw_Calc(IMU_yaw) 가 PID(P=0.4) 로 좌우 모터 PWM 차이 자동 조절
   ↓ MCU 1차 보정 (출력 ±π/6 제한)
   잔여 drift → DWB PathAlign forward_point=0.2 가 path 0.2m 앞 heading 정렬
```

### 4.6 DWB Path 추종 강화

**파일**: `nav2_params_fused.yaml`.

| 파라미터 | 변경 전 | 변경 후 |
|---|---:|---:|
| `PathAlign.forward_point_distance` | 0.1 | **0.2** |
| `GoalAlign.forward_point_distance` | 0.1 | **0.2** |
| `RotationShim.simulate_ahead_time` | 1.0 | **1.5** |

효과: path 0.2m 앞 heading 으로 정렬 (preemptive turn-in) + 회전 후 충돌 검사 시간 확대.

### 4.7 신규 진단 도구 — `~/sensor_yaw_align.py`

기능:
- /cmd_vel publisher 충돌 검사 (Nav2 활성 시 경고).
- baseline → LEFT → pause → RIGHT → 분석.
- 누적 yaw unwrap (±π 경계 처리).
- RF2O 기준 부호/스케일 비교 + L/R 비대칭 검사.
- 권장 보정값 자동 출력.
- CLI: `--speed`, `--duration`.

---

## 5. 빌드 및 검증

### 5.1 빌드 명령

```bash
# 호스트 ROS2 워크스페이스
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO jupiter_bringup --symlink-install
source install/setup.bash

# Docker 컨테이너 (jupiter_user layer 적용)
cd ${ISAAC_ROS_WS}/src/isaac_ros_common && ./scripts/run_dev.sh
# 첫 빌드 시 chain 4-layer + jupiter_user 박제 (~3-5분), 이후 CACHED
```

### 5.2 검증 명령 — 변경 사항 확인

```bash
grep -nE "imu_gyro_scale" .../nav2_vslam_fused.launch.py
# → 'imu_gyro_scale': 0.984

grep -nE "set_car_motion.*adjust" .../jupiter_driver_compensated.py
# → set_car_motion(vx, vy, angular_corrected, adjust=True)

grep -nE "plugin: \"nav2_smac|forward_sampling_distance|vtheta_samples|PathDist.scale|forward_point_distance" .../nav2_params_fused.yaml
# → SmacPlanner2D, forward_sampling 0.3, vtheta_samples 11, PathDist 24, forward_point 0.2
```

### 5.3 검증 — sensor_yaw_align.py 측정

| Source | LEFT (+) | RIGHT (-) | scale | 부호 |
|---|---:|---:|---:|---|
| WHEEL | 87.65° | -95.85° | 0.858× | OK |
| RF2O | 107.32° | -106.49° | 1.000 (기준) | — |
| EKF | 105.06° | -109.09° | 1.002× | OK |
| IMU | 109.43° | -113.02° | 1.041× | OK |

### 5.4 빌드 결과

| 항목 | 결과 |
|---|---|
| Python syntax (Rosmaster_Lib.py, jupiter_driver_compensated.py) | OK ✓ |
| YAML syntax (nav2_params_fused.yaml) | OK ✓ |
| Docker 컨테이너 진입 | "Creating non-root container 'admin' for host user uid=1000:gid=1000" ✓ |
| Docker 박제 패키지 | pointcloud-to-laserscan / peoplesemseg-models / peoplenet-models 모두 ii ✓ |
| Yarn / nodesource 저장소 보존 | yarn.list, nodesource.list 존재 ✓ |
| 학습 자료 5_practice.sh | 모든 STEP read-only 동작 ✓ |

---

## 6. 최종 결론

### 6.1 오늘 해결된 항목

- **Isaac ROS 컨테이너 의존성 박제**: Dockerfile.jupiter_user + ~/.isaac_ros_common-config 로 매번 apt 재설치 부담 영구 해소. NVIDIA 베이스 의존성 무결성 검증 완료 (안전).
- **Docker 학습 자료 5단계** (`/home/jetson/docker_learning/`): Image/Container 기초 → Isaac ROS chain → run_dev.sh 흐름 → jupiter_user line-by-line + 무결성 보고서 → read-only 실습 셸. 1431줄.
- **Nav2 좁은 공간 통과**: SmacPlanner2D + inflation 0.20 + BT custom (Wait 5s, retries 10, spin 45°+90°) + failure_tolerance 5s. 1m 문 free 폭 0.60m 확보.
- **정지 회전 회귀 복원**: 이전 03-18/04-21 검증값으로 forward_sampling 0.3, vtheta_samples 11, PathDist 24 복원 → RotationShim 활성 판단 + sample 평가 timing 안정.
- **센서 yaw 정렬 측정**: RF2O 기준 모든 활성 센서 부호 OK 확인. IMU 4% over → 0.984 로 정밀화.
- **MCU Yaw PID 보정 활성화** (가장 큰 변화): driver `set_car_motion(adjust=True)` 로 한쪽 바퀴 마찰 (WHEEL L/R 비대칭 8.6%) 의 직진 yaw drift 를 MCU 가 IMU 기반 자동 보정.
- **DWB path 추종 강화**: PathAlign/GoalAlign forward_point_distance 0.2 + RotationShim simulate_ahead 1.5 — preemptive turn-in.

### 6.2 오늘 새로 확인된 항목

- **NVIDIA Isaac ROS Dockerfile chain 매칭 알고리즘**: `build_image_layers.sh` 가 dot-separated key 를 longest-suffix 로 풀어내고 reverse 빌드. `Dockerfile.aarch64` 와 `.x86_64` 는 `Dockerfile.base` 의 symlink.
- **`workspace-entrypoint.sh` 의 admin 동적 생성**: HOST_USER_UID/GID 와 일치하는 사용자 동적 생성 + sudoers NOPASSWD + video/sudo/plugdev 그룹 추가 + udev 재시작.
- **MCU 가 차동구동 자동 yaw 보정 기능 보유**: `app_differential.c:63-88` 의 `Differential_Yaw_Calc(yaw)` 가 IMU yaw PID 로 좌우 모터 속도 차이 자동 조절 — 그러나 driver 측에서 활성화 채널 (CAR_TYPE 의 0x80 비트) 누락 상태였음.
- **`set_car_run` vs `set_car_motion` 의 비대칭**: set_car_run 은 이미 `car_type | self.__CAR_ADJUST` 패턴 사용 중이었으나 set_car_motion 이 누락 — yahboom 표준 라이브러리의 미완성 부분.
- **EKF 의 우연한 정렬**: IMU 1.041× over + WHEEL 0.858× under 가 상쇄되어 EKF/RF2O = 1.002× — 운 좋게 정렬된 상태였음.
- **WHEEL L/R 비대칭 8.6%**: 사용자 언급 "한쪽 바퀴 기구 이상" 의 정량 측정 — 좌회전 시 한쪽 모터 약함.
- **VSLAM 토픽 미수신**: Docker container 의 cuVSLAM 미실행 추정. EKF 에서 VSLAM yaw 는 이미 비활성이라 navigation 무영향.

### 6.3 남은 제약 / 주의

- **WHEEL 좌우 비대칭은 기구 문제로 소프트웨어 보정 한계**: MCU yaw PID 가 IMU 기반 1차 보정 + DWB path 추종 2차 보정으로 우회. 완벽 해결은 기구 점검 필요.
- **MCU `pid_Yaw` 의 I 항 = 0**: 정상상태 yaw 오차가 누적 보정되지 않고 잔존. P=0.4 만으로 약간의 잔여 drift 가능. 펌웨어 재플래시로 I=0.05 추가 시 완전 보정 가능 (현 세션에선 미진행).
- **VSLAM 미동작 상태**: nvblox depth 통합은 별도 채널이라 무관하지만 VSLAM 의 위치 보정이 빠진 상태. cuVSLAM 노드 진단 필요 시 별도 작업.
- **첫 빌드 시 Hybrid → 2D 다운그레이드**: SmacPlannerHybrid 의 motion model 곡선 plan 이 차동구동 + 한쪽 바퀴 이상 환경에서 부적합. Hybrid 의 footprint-aware 정밀도가 필요한 상황이면 motion_model `REEDS_SHEPP` (전후진 허용) 으로 재시도 가능.
- **이번 세션 변경이 많음**: 회귀 가능성 모니터링 필요. backup 디렉토리 (`backup_2026_04_28/`) 로 즉시 롤백 가능.

### 6.4 다음 분석 우선순위

1. **현장 검증 — MCU yaw 보정 효과**: 5m 직진 명령 → /odom yaw 변화 측정. 이전 (~5°/m drift) 대비 1°/m 미만 기대.
2. **현장 검증 — 1m 문 통과 + 사람 회피**: SmacPlanner2D + inflation 0.20 + BT 강화 의 누적 효과.
3. **현장 검증 — 정지 회전**: forward_sampling 0.3 + vtheta_samples 11 + PathDist 24 복원 효과.
4. **sensor_yaw_align.py 재측정** (선택): IMU/RF2O 가 1.001 ± 0.02 로 좁혀지는지.
5. **VSLAM 노드 상태 진단**: `docker exec ... ros2 node list | grep visual_slam`.
6. **MCU `pid_Yaw` I 항 추가** (펌웨어 재플래시 필요): MCU yaw 보정의 정상상태 잔여 drift 해소.
7. **Docker 학습 자료 검토**: 사용자가 5단계 가이드 따라가면서 이해도 점검.

---

## 7. 생성 / 수정 파일 목록

### 7.1 수정된 파일

- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` (Planner 교체, inflation, DWB critic, RotationShim, BT default, 좁은 공간 + 정지 회전 + path 추종 누적 변경)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` (`imu_gyro_scale: 1.025 → 0.984`)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/behavior_trees/navigate_to_pose_no_backup.xml` (Wait 5s, retries 10, spin 45°+90°, RateController 0.5Hz)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py` (set_car_motion adjust 인자 추가)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` (set_car_motion adjust=True)
- `/home/jetson/isaac_ws_ros/src/isaac_ros_common/docker/Dockerfile.jupiter_user` (yarn fail-soft, USER admin 제거, ARG default)

### 7.2 생성된 파일 (신규)

- `/home/jetson/.isaac_ros_common-config` (CONFIG_IMAGE_KEY)
- `/home/jetson/sensor_yaw_align.py` (RF2O 기준 정렬 진단 도구, 218줄)
- `/home/jetson/docker_learning/README.md` (69줄)
- `/home/jetson/docker_learning/01_docker_basics.md` (212줄)
- `/home/jetson/docker_learning/02_isaac_ros_chain.md` (290줄)
- `/home/jetson/docker_learning/03_run_dev_workflow.md` (314줄)
- `/home/jetson/docker_learning/04_jupiter_user_layer.md` (328줄)
- `/home/jetson/docker_learning/05_practice.sh` (218줄, chmod +x)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/backup_2026_04_28/nav2_params_fused.yaml.bak`
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/backup_2026_04_28/navigate_to_pose_no_backup.xml.bak`
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/backup_2026_04_28/Rosmaster_Lib.py.bak`
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/backup_2026_04_28/jupiter_driver_compensated.py.bak`

### 7.3 생성된 문서

- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_04_28.md` (본 세션 명령어 로그)
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_04_28.md` (본 문서)

### 7.4 Plan 파일 (참조)

- `/home/jetson/.claude/plans/functional-sleeping-starfish.md` (이번 세션 plan, Docker 학습 자료 + Nav2 작업 통합)
