# 명령어 로그 2026-05-07: 캘리브레이션 baseline + 데모 준비 (지도 저장/로드, 조이스틱) + Step B (RUNNING_MIN 0.30→0.20) + yaw drift 진단

> 진단 및 수정에 사용한 명령어와 스크립트/설정 파일을 요약 없이 전부 기록한다.

## 0. 오늘 사용한 터미널 명령어 원문

### 0.1 캘리브레이션 — 정비 후 baseline

```bash
pkill -f nav2_vslam_fused.launch.py ; pkill -f jupiter_driver_compensated ; sleep 2
ros2 run jupiter_bringup wheel_deadzone_calibrator
```
목적: M3 정비 후 effective deadzone + PID 응답 재측정.
결과 핵심:
```
=== Phase 1 v2 SUMMARY ===
[Linear effective deadzone]
  M1 forward DZ: vx ≥ 0.005
  M3 forward DZ: vx ≥ 0.020   (정비 전 0.030)
  M1 reverse DZ: vx ≥ 0.030
  M3 reverse DZ: vx ≥ 0.040   (정비 전 0.100, 6.67×)

[Rotation effective deadzone]
  M1 CCW DZ: ω ≥ 0.05
  M3 CCW DZ: ω ≥ 0.15        (정비 전 0.40)
  M1 CW DZ:  ω ≥ 0.30
  M3 CW DZ:  ω ≥ 0.40

[좌-우 비대칭 진단]
  M3/M1 forward DZ 비율: 4.00× (정비 전 6×)
  M3/M1 reverse DZ 비율: 1.33× (정비 전 6.67×) ← 정비 효과 확인
```

### 0.2 조이스틱 노드 + 토픽 진단

```bash
source /opt/ros/humble/setup.bash 2>/dev/null && ros2 node list 2>&1 | grep -iE "joy|teleop"
ros2 topic list 2>&1 | grep -iE "joy|cmd_vel"
ls /dev/input/js* 2>/dev/null
lsusb 2>/dev/null | grep -i "logi\|joy\|gamepad\|controller"
```
목적: 조이스틱 노드/디바이스 확인.
결과:
```
/dev/input/js0
Bus 001 Device 007: ID 0079:181c DragonRise Inc. Controller
```

```bash
grep -nE "create_publisher.*Twist|cmd_vel" /home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/jupiter_ctrl/jupiter_joy.py
cat /home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/setup.py | tail -25
```
결과: `jupiter_joy.py:50` publishes to `/cmd_vel` (OLD), `jupiter_joy_new.py:48` publishes to `/cmd_vel_joy` (twist_mux priority 100). entry_point 는 jupiter_joy 만 등록 → 잘못된 기본값.

### 0.3 빌드 — joy 수정 + slam_mode arg

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_ctrl jupiter_nav_VO --symlink-install
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_ctrl --symlink-install
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup --symlink-install
```
결과:
```
Finished <<< jupiter_ctrl [5.68s]
Finished <<< jupiter_nav_VO [...]
Finished <<< jupiter_bringup [4.52s]
```

### 0.4 지도 저장

```bash
mkdir -p /home/jetson/maps
ros2 service call /slam_toolbox/serialize_map slam_toolbox/srv/SerializePoseGraph \
    "{filename: '/home/jetson/maps/jupiter_demo'}"
```
결과:
```
response: slam_toolbox.srv.SerializePoseGraph_Response(result=0)
```
파일 저장 확인:
```
-rw-rw-r-- jetson jetson 25,706,305 jupiter_demo.posegraph
-rw-rw-r-- jetson jetson  7,507,624 jupiter_demo.data
```

```bash
ros2 run nav2_map_server map_saver_cli -f /home/jetson/maps/jupiter_demo2
```
결과: 실패 — `Failed to spin map subscription`. QoS mismatch (TRANSIENT_LOCAL).

진단:
```bash
ros2 topic info /map -v 2>&1 | grep -E "Reliability|Durability|count"
```
```
Publisher count: 1
  Reliability: RELIABLE
  Durability: TRANSIENT_LOCAL          ← slam_toolbox
Subscription count: 2
  Reliability: RELIABLE
  Durability: TRANSIENT_LOCAL          ← OK 구독자
  Reliability: RELIABLE
  Durability: VOLATILE                 ← map_saver_cli (mismatch)
```

해결:
```bash
ros2 run nav2_map_server map_saver_cli \
    -f /home/jetson/maps/jupiter_demo2 \
    --ros-args -p map_subscribe_transient_local:=true
```

### 0.5 데모 운영 — localization mode

```bash
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py \
    slam_mode:=localization \
    slam_map:=/home/jetson/maps/jupiter_demo
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
ros2 launch jupiter_ctrl jupiter_joy.launch.py
```
목적: 저장 지도로 데모 운영.

### 0.6 bag 분석 — 충돌 사이클 진단

```bash
ros2 bag info /home/jetson/bags/latest
```

```bash
cd /tmp && python3 /tmp/analyze_post_repair.py
```
결과 (정비 후 첫 bag, RUNNING_MIN=0.30):
```
좌회전 명령 시 정상 방향 100%, 정지 0%
우회전 명령 시 정상 방향 99.4%, 정지 0%
Path RMS 평균 16.7 cm (이전 6.7 의 2.5×)
sustained 회전 segment 시계열 분석 — wheel_R decay 일관성 없음
forced_recovery backup 0회
stuck_status=true 0%
```

```bash
python3 /tmp/cross_check.py    # 보정 layer 충돌 검사
python3 /tmp/analyze_conflict.py    # forced_recovery 충돌 시점
```

데모 운영 bag 분석 결과 (RUNNING_MIN=0.30):
```
forced_recovery backup 232 회 발동
backup 12 회 중 8 회 (66.7%) 가 nav2 회전 명령 중 발동
backup 간 median 6.5s = forced_recovery cooldown 5s + backup 1s
시간대별 stuck=true: [60-90s] 12% → [210-240s] 45%
시간대별 M3 응답 (좌회전): [60-90s] 93% → [240-270s] 22%
```

### 0.7 펌웨어 코드 분석 — Motor_Set_Dead_Zone 작동 여부

```bash
grep -rn "Motor_Set_Dead_Zone(" /home/jetson/mcu/extbd/Source/ | grep -v "\.bak\|\.h:"
grep -n "g_car_type\s*=\|Motion_Set_Car_Type\|CAR_MECANUM\|CAR_DIFFERENTIAL" /home/jetson/mcu/extbd/Source/APP/app_motion.c
grep -nE "ENABLE_YAW_ADJUST" /home/jetson/mcu/extbd/Source/APP/config.h
```
결과:
- app.c:158-163 에 Motor_Set_Dead_Zone(M1, 720, 690), (M2, 780, 750), (M3, 720, 690) 호출
- app_motion.c:34: `g_car_type = CAR_MECANUM` (default)
- config.h:26: `ENABLE_YAW_ADJUST = 1`

→ CAR_MECANUM 모드에서는 [bsp_motor.c:73-85](mcu/extbd/Source/BSP/bsp_motor.c#L73) 의 else 분기 (Additive Deadzone) 사용. **720/690 이 실제 작동 중**.

### 0.8 Step B 검증 bag 분석

```bash
python3 /tmp/cross_check.py     # Step B 후 bag
python3 /tmp/analyze_conflict.py
```
결과 (RUNNING_MIN=0.20 적용 후):
```
forced_recovery backup: 4 회 (이전 232 → 98% 감소)
좌회전 명령 시 정상 방향 100%, 정지 0%
M3 응답 (안정 구간): 69~88% (이전 22~50% 대비 회복)
시간대별 stuck=true: [0-30s] 0% → [90-120s] 38% (yaw drift 시점만 폭증)
```

### 0.9 yaw drift 진단

```bash
python3 /tmp/analyze_yaw_offset.py
```
결과 (사용자 보고 "8-10° 기울어짐" 일치):
```
시작 (t=3.9s 부터): map→odom yaw = +5.80° 안정
시간대별 yaw 변동:
  [  0- 80s] yaw +5.80° 안정
  [ 80-100s] yaw 5~6° (약간 변동)
  [100-120s] yaw -2.40° ~ +5.00° (range 7.4°)
  [120-140s] yaw +5.00° ~ +10.20° (사용자 보고와 일치)
  [140-160s] yaw +8.40°
```

## 1. 분석에 사용한 설정 / 스크립트 파일

### 1.0 오늘 사용/열람한 스크립트 및 파일 전체 경로

**ROS 코드 / 설정**:
- `/home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/setup.py` ← 수정 (entry point)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/launch/jupiter_joy.launch.py` ← 수정 (executable + speed limit)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/jupiter_ctrl/jupiter_joy_new.py` ← 수정 (base_speed 버그)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/jupiter_ctrl/jupiter_joy.py` (열람)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` ← 수정 (slam_mode arg)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` ← 수정 (goal tolerance)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml` (열람)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` ← 수정 (RUNNING_MIN)

**펌웨어 (열람만)**:
- `/home/jetson/mcu/extbd/Source/APP/app.c` (열람)
- `/home/jetson/mcu/extbd/Source/APP/app_motion.c` (열람)
- `/home/jetson/mcu/extbd/Source/APP/app_mecanum.c` (열람) — Mecanum_Ctrl + balance correction
- `/home/jetson/mcu/extbd/Source/APP/app_differential.c` (열람) — 본 robot 미사용 확인
- `/home/jetson/mcu/extbd/Source/APP/protocol.c` (열람) — adjust 비트 처리
- `/home/jetson/mcu/extbd/Source/APP/app_pid.h` (열람)
- `/home/jetson/mcu/extbd/Source/APP/config.h` (열람) — ENABLE_YAW_ADJUST
- `/home/jetson/mcu/extbd/Source/BSP/bsp_motor.c` (열람) — Motor_Ignore_Dead_Zone

**임시 분석 스크립트 (생성)**:
- `/tmp/analyze_post_repair.py` — 정비 후 첫 bag 검증
- `/tmp/cross_check.py` — 보정 layer 충돌 검사
- `/tmp/analyze_conflict.py` — forced_recovery 충돌 사이클 분석
- `/tmp/analyze_goal_tolerance.py` — goal 도달 거리 (odom-frame, 부정확)
- `/tmp/check_frames.py` — TF frame_id + map drift 측정
- `/tmp/analyze_goal_in_map.py` — goal 도달 거리 (map-frame, 정확)
- `/tmp/analyze_yaw_offset.py` — yaw offset 시계열

**Bag**:
- `/home/jetson/bags/latest/` (자동 기록, 매 launch 덮어씀)

**저장된 지도**:
- `/home/jetson/maps/jupiter_demo.posegraph` (25.7 MB, slam_toolbox)
- `/home/jetson/maps/jupiter_demo.data` (7.5 MB, slam_toolbox)

### 1.1 수정된 파일 1 — setup.py (jupiter_joy_new entry 등록)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/setup.py`

수정 항목 (line 27-35):
- entry_points 의 console_scripts 에 `jupiter_joy_new = jupiter_ctrl.jupiter_joy_new:main` 추가
- OLD `jupiter_joy = jupiter_ctrl.jupiter_joy:main` 도 보존 (하위 호환)

배경: jupiter_joy_new.py 의 publish topic 이 `/cmd_vel_joy` (twist_mux priority 100) 인데 entry_point 미등록으로 실행 불가 상태였음.

### 1.2 수정된 파일 2 — jupiter_joy.launch.py

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/launch/jupiter_joy.launch.py`

수정 항목:
- executable: `jupiter_joy` → **`jupiter_joy_new`** (line 33)
- linear_speed_limit: 0.6 → 0.45 → 0.25 (linter 후속 수정 0.25)
- angular_speed_limit: 3.0 → 1.0 (사용자 요청 안전 운영 속도)

### 1.3 수정된 파일 3 — jupiter_joy_new.py (base_speed 버그)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_ctrl/jupiter_ctrl/jupiter_joy_new.py`

수정 항목 — `user_jetson` 함수의 base_speed 제거:
- 이전: `linear_val = sign * (2.0 + ratio * limit * Gear)` — stick 임계 (0.2) 만 눌러도 2.0 m/s 부터 시작 → max 시 2.45 m/s
- 이후: `linear_val = sign * ratio * limit * Gear` — 0 ~ limit 정상 매핑
- angular 도 동일 (base 1.0 제거)

배경: 사용자 보고 `PublishedDirect: linear=2.450, angular=0.000` 같은 값 — limit 0.45 인데 2.45 로 출력. base_speed 버그.

### 1.4 수정된 파일 4 — nav2_vslam_fused.launch.py

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py`

수정 항목:
- 신규 LaunchArgument 2 개:
  - `slam_mode` (default `mapping`) — `mapping` 또는 `localization`
  - `slam_map` (default `''`) — 저장된 지도 경로 (확장자 제외)
- slam_toolbox_node 의 parameters 에 LaunchConfiguration override 추가:
  - `mode: LaunchConfiguration('slam_mode')`
  - `map_file_name: LaunchConfiguration('slam_map')`
- `return LaunchDescription([...])` 에 두 arg 추가

배경: 데모 시나리오 (mapping → 지도 저장 → localization 모드) 분기 위해.

### 1.5 수정된 파일 5 — nav2_params_fused.yaml (goal tolerance)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

수정 항목 (line 111-117):
- `xy_goal_tolerance: 0.20 → 0.15` (5cm 강화)
- `yaw_goal_tolerance: 0.32 → 0.25` (~14°)

배경: 사용자 보고 "일정 수준까지만 도달하면 success". 이전 bag 측정 결과 5/5 goal 이 13~21cm 범위에서 success — tolerance 직전. 0.15 로 강화 시 평균 도달 거리 16cm → 11.6cm 로 개선 확인됨.

### 1.6 수정된 파일 6 — jupiter_driver_compensated.py (RUNNING_MIN 0.30→0.20)

경로: `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

수정 항목:
- `ROT_RUNNING_MIN_ANGULAR: 0.30 → 0.20`

배경: CAR_MECANUM 모드의 `Motor_Set_Dead_Zone(M3, 720, 690)` 가 Additive Deadzone 으로 작동 (작은 PID 출력 → 720+ PWM jump). driver RUNNING_MIN=0.30 boost 와 결합 시 over-correction 누적. 모터 부하 증가 → 시간 따라 M3 응답 약화 → forced_recovery 폭증.

검증: RUNNING_MIN 0.20 적용 후 forced_recovery 232회 → 4회 (98% 감소).

## 2. 관찰 기준 요약

### 2.1 Step B (RUNNING_MIN 0.30→0.20) 효과

| 지표 | 이전 (0.30) | 이후 (0.20) |
|---|---|---|
| forced_recovery backup | 232 회 | **4 회** |
| stuck=true 비율 (전체) | 10.3% | ~6% |
| 좌회전 명령 시 정지 비율 | 0% | 0% |
| 회전 follow 비율 | 좌 100% / 우 99.4% | 좌 100% / 우 99.4% |
| 시간대별 M3 응답 약화 | 93% → 22% | 안정 시 69~88% |

### 2.2 Goal tolerance (0.20→0.15) 효과

| 지표 | 이전 (0.20) | 이후 (0.15) |
|---|---|---|
| Goal 도달 거리 평균 | 16 cm | 11.6 cm |
| 최정밀 도달 | 5.4 cm | 3.0 cm |
| Tolerance 초과 횟수 | 2/5 | 1/5 |

### 2.3 캘리브레이션 baseline (정비 전 vs 후)

| 항목 | 정비 전 | 정비 후 |
|---|---|---|
| M3 forward DZ | 0.030 m/s | 0.020 m/s |
| M3 reverse DZ | 0.100 m/s | 0.040 m/s |
| M3 CCW DZ | 0.40 rad/s | 0.15 rad/s |
| M3 CW DZ | 0.30 rad/s | 0.40 rad/s |
| M3/M1 forward 비율 | 6× | 4× |
| M3/M1 reverse 비율 | 6.67× | 1.33× |

### 2.4 yaw drift (localization mode)

| 시간대 | map→odom yaw |
|---|---|
| 시작 (t=3.9~80s) | +5.80° (안정) |
| [100-120s] | -2.40° ~ +5.00° (range 7.4°) |
| [120-140s] | +5.00° ~ +10.20° (사용자 보고와 일치) |

### 2.5 SLAM map drift

| 측정 | 이전 bag (mapping) | 이번 bag (localization) |
|---|---|---|
| map→odom x range | 0.57 m | 0.27 m (작음) |
| map→odom y range | 0.42 m | 0.34 m |
| map→odom yaw range | (큰 변동) | 12.6° (-2.40°~+10.20°) |
