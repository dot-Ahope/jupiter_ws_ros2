# 진행과정 2026-04-23: jupiter_nvblox SIGABRT 해결, RViz 마스킹 검증, SLAM↔nvblox Y축 정합 수정

## 1. 목표

### 1.1 사용자 요청 (시간순)
> 1. `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py` 를 Docker 내에서 실행 중이다. 두 기능이 정상 작동하는지 확인하고, 안 되면 해결책을 찾아 적용하라.
>    - (a) 사람 감지 및 마스킹
>    - (b) nvblox 출력물이 지도에 정합 (현재 2D 지도가 nvblox 출력물의 y축 가운데에 놓이고 있는 것 같다)
> 2. (정정) 2D 지도를 만드는 launch 파일은 `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` 로 따로 있다. 해당 지도와 nvblox 출력물의 Y축이 맞물리지 않는다.
> 3. RViz2에서 마스킹이 시각화되기 위한 절차가 잘 작동하는지 확인하라.
> 4. `jupiter_nvblox.launch.py` 실행 시 아래 오류:
>    ```
>    [ERROR] [component_container_mt-1]: process has died [pid 303683, exit code -6, cmd '/opt/ros/humble/lib/rclcpp_components/component_container_mt ...'].
>    ```
> 5. 오늘 진행과정 전체와 사용 명령어/스크립트를 별도 md 파일로 정리하라.

### 1.2 이전 세션 요약 / 연관 작업
- 2026-04-16: `jupiter_nvblox` 패키지 생성 (nvblox + people_segmentation, cuVSLAM 제외)
- 2026-04-17: Docker nvblox + cuVSLAM + people_detection GPU 경합 진단
- 2026-04-20: `jupiter_nvblox.launch.py` 에 `run_rviz`, `run_foxglove` 파라미터 추가, `jupiter_nvblox.rviz` 생성, Nav2 `combined_map_slice` 플러그인 토픽 반영
- 2026-04-21: Nav2 정지 회전 헌팅 억제, near-goal yaw 데드락 수정

---

## 2. 대상 시스템 구성

### 2.1 Launch 및 실행 경로
- 실행 경로: Docker 컨테이너 `isaac_ros_dev-aarch64-container`
- 진입 방법: `cd ${ISAAC_ROS_WS}/src/isaac_ros_common && ./scripts/run_dev.sh`
- Launch 파일: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py`

### 2.2 Launch 노드 구성 (6개 ComposableNode)
1. `realsense2_camera::RealSenseNodeFactory` — RealSense D455f 드라이버 (`camera0` ns)
2. `isaac_ros_image_proc::ResizeNode` — color 640×480 → 960×544
3. `isaac_ros::dnn_inference::ImageToTensorNode` — Image → Tensor (scale to [0,1])
4. `isaac_ros::dnn_inference::TensorRTNode` — ShuffleSeg 추론
5. `isaac_ros::unet::UNetDecoderNode` — Tensor → mask
6. `nvblox::NvbloxNode` — depth + color(resized) + mask → TSDF/ESDF

### 2.3 외부 TF 의존
- Launch 파일은 내부적으로 TF를 만들지 않음
- `nav2_vslam_fused.launch.py` (host 측)에서 공급해야 함:
  - `map → odom` (SLAM Toolbox)
  - `odom → base_footprint` (EKF, `two_d_mode=true`)
  - `base_footprint → base_link → camera_link → camera0_link` (URDF + static_transform_publisher)

---

## 3. 문제 분석

### 3.1 이슈 A — 사람 감지/마스킹

**증상**:
- 사용자는 작동 여부 자체가 불확실한 상태로 확인을 요청

**분석 (실제 실행 로그 + 토픽 rate 계측)**:
- `depth_image_callback` 15.1 Hz
- `color_image_callback` 15.1 Hz
- `ros/color` 5.0 Hz (통합 rate, config `integrate_color_rate_hz: 5.0`)
- `ros/depth` 15.1 Hz
- `ros/update_esdf` 4.9 Hz
- `/camera0/segmentation/people_mask` 14.99 Hz (`sensor_msgs/Image`, mono8, 960×544)
- `/camera0/unet/colored_segmentation_mask` 14.99 Hz (`sensor_msgs/Image`, rgb8, 960×544)

**판단**: 파이프라인은 정상 작동. 사용자 질문은 검증 요청이었으므로 수정 불필요.

---

### 3.2 이슈 B — nvblox 2D 지도가 "y축 가운데"에 보임 (초기 해석)

**증상 (사용자 답변)**:
- "3D 복셀 타워의 수직 중간에 2D 지도가 떠있음"

**nvblox 소스 분석 결과**:
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/nvblox_node.cpp:902`
  ```cpp
  occupancy_grid_msg.info.origin.position.z = 0;   // 하드코딩
  ```
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/conversions/esdf_slice_conversions.cu:159`
  ```cpp
  map_slice->origin.z = slice_height;
  ```
- `/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/nvblox_core/nvblox/src/geometry/workspace_bounds.cpp:25-28`
  ```cpp
  case WorkspaceBoundsType::kUnbounded:
    *output_aabb = input_aabb;     // bounds 값 무시
  ```

**초기 의심 원인**:
1. RViz config의 `Combined Map Slice` 디스플레이가 `rviz_default_plugins/Map` 플러그인을 사용하면서 토픽은 `nvblox_msgs/msg/DistanceMapSlice` 를 가리킴 → **타입 불일치 → 렌더링 실패**
2. `workspace_bounds_type: "unbounded"` 상태에서 `workspace_bounds_min/max_height_m` 완전 무시 → floor 아래/천장 위 outlier voxel 생성 → 복셀 타워 Z 범위 확대

### 3.3 이슈 B — (정정 후) SLAM 2D 지도와 nvblox 출력물 Y축 어긋남

**증상 (정정된 사용자 설명)**:
- 비교 대상이 nvblox `combined_occupancy_grid` 자체가 아니라 SLAM Toolbox가 발행하는 `/map` 이다.
- `nav2_vslam_fused.launch.py` 의 SLAM Toolbox 출력 2D 지도와 nvblox 출력물의 Y축이 맞물리지 않는다.

**근본 원인**:
- SLAM Toolbox `/map` → `map` 프레임 (`slam_params.yaml: map_frame: map`)
- nvblox `combined_occupancy_grid` → `odom` 프레임 (기본 `global_frame: "odom"`)
- SLAM 이 루프 클로저로 `map → odom` TF 를 갱신하면, `odom` 프레임에 쌓인 nvblox 복셀은 보정 전 위치에 남아있음
- RViz `Fixed Frame: map` 에서 두 지도를 함께 그릴 때, nvblox 출력이 보정 이전 odom 위치에 박혀 Y축으로 어긋나 보임

**보조 원인**:
- `map_clearing_radius_m: 5.0` 로 인해 오래된 복셀은 클리어되지만, 보정 직후 수 초 동안 가시적 어긋남이 지속 가능
- `workspace_bounds_type: "unbounded"` 로 Z 범위가 관리되지 않아 복셀 타워가 수직 방향으로 더 크게 보임 → 미세한 수평 drift 도 시각적으로 강조됨

---

### 3.4 이슈 C — RViz2 마스킹 시각화 절차

**검증 항목**:
- 토픽: `/camera0/unet/colored_segmentation_mask`
- 타입: `sensor_msgs/Image`
- 인코딩: rgb8, 960×544, step 2880, frame_id `camera0_color_optical_frame`
- QoS: Pub RELIABLE / VOLATILE
- RViz config sub: `Reliability Policy: Best Effort`, `Durability Policy: Volatile`
- QoS 호환성: Pub RELIABLE + Sub BEST_EFFORT → 호환 (sub 다운그레이드)

**판단**: 시각화 조건은 충족. 다만 `run_rviz:=true` 로 실행하지 않으면 RViz 노드 자체가 없음.

---

### 3.5 이슈 D — `component_container_mt` SIGABRT (exit code -6)

**사용자 오류**:
```
[ERROR] [component_container_mt-1]: process has died [pid 303683, exit code -6, cmd '/opt/ros/humble/lib/rclcpp_components/component_container_mt ...'].
```

**실제 원인 (사용자 크래시 로그에서 직접 확인)**:

파일: `/home/admin/.ros/log/2026-04-23-12-46-40-058750-yahboom-303628/launch.log`
```
1776919602.2892258 [component_container_mt-1] terminate called after throwing an instance of 'std::filesystem::__cxx11::filesystem_error'
1776919602.2893913 [component_container_mt-1]   what():  filesystem error: cannot create directories: Permission denied [/tmp/isaac_ros_nitros/graphs/ZTIRUUSVOX]
1776919604.0519927 [ERROR] [component_container_mt-1]: process has died [pid 303683, exit code -6, ...].
```

**원인 체인**:
1. NITROS 가 런타임에 `/tmp/isaac_ros_nitros/graphs/<랜덤 6자리>/` 를 `mkdir`
2. `/tmp/isaac_ros_nitros/graphs/` 가 `root:root 0755` 상태 — 이전에 `root` 세션이 먼저 디렉토리를 만들고 sub-디렉토리까지 root 권한으로 생성해 둠
3. 현재 `admin` 사용자로 진입한 컨테이너에서 새 서브디렉토리 생성 불가 → `filesystem_error` 발생
4. NITROS 코드가 해당 예외를 catch 하지 않음 → `std::terminate` → SIGABRT (signal 6)

**증거**:
```
$ docker exec ... stat /tmp/isaac_ros_nitros/graphs/
  Access: (0755/drwxr-xr-x)  Uid: (0/root)   Gid: (0/root)
$ docker exec ... id
  uid=0(root)  (내 세션)
$ docker exec ... (admin 세션)
  uid=1000(admin)
```

---

## 4. 적용한 변경

### 4.1 RViz config: `Combined Map Slice` 토픽 교체

**파일**: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `Combined Map Slice` 토픽 | `/nvblox_node/combined_map_slice` (`nvblox_msgs/DistanceMapSlice`) | `/nvblox_node/combined_occupancy_grid` (`nav_msgs/OccupancyGrid`) |

의도: `rviz_default_plugins/Map` 이 `nav_msgs/OccupancyGrid` 만 지원하므로 호환 타입으로 교체.

---

### 4.2 nvblox config: `workspace_bounds_type`, `global_frame`

**파일**: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `global_frame` | (default `"odom"`) | `"map"` |
| `pose_frame` | (default `"base_link"`) | `"base_link"` (명시) |
| `static_mapper.workspace_bounds_type` | `"unbounded"` | `"height_bounds"` |
| `static_mapper.workspace_bounds_min_height_m` | `-0.5` | `-0.2` |
| `static_mapper.workspace_bounds_max_height_m` | `2.0` | `2.0` |

의도:
- `global_frame: "map"` → nvblox 복셀을 SLAM 보정된 `map` 좌표계에 기록하여 SLAM `/map` 과 수평 정합 유지
- `workspace_bounds_type: "height_bounds"` → `-0.2m ~ 2.0m` 범위만 voxel 통합, floor 아래/천장 위 outlier 제거

주의:
- OccupancyGrid `origin.z` 는 하드코딩 0 이므로 `esdf_slice_height` 조정으로 2D 지도 수직 위치를 바꾸는 것은 불가
- `global_frame: "map"` 변경은 `map → camera0_depth_optical_frame` TF 체인이 항상 존재해야 한다는 런타임 요구를 가짐 (단독 테스트 시 실패할 수 있음)

---

### 4.3 Launch 파일: NITROS 캐시 디렉토리 권한 방어 로직

**파일**: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py`

추가 내용:
```python
import stat

def _ensure_nitros_cache_writable() -> None:
    """
    NITROS가 런타임에 /tmp/isaac_ros_nitros/graphs/<rand>/ 를 mkdir 하는데,
    root 세션이 먼저 만들어 둔 경우 admin 세션은 'Permission denied' → SIGABRT.
    Sticky-writable(1777)로 설정해 다중 사용자 세션에서도 서브디렉토리 생성 가능하게 한다.
    """
    base = '/tmp/isaac_ros_nitros/graphs'
    try:
        os.makedirs(base, exist_ok=True)
        os.chmod(base, 0o1777)
        os.chmod(os.path.dirname(base), 0o1777)
    except (PermissionError, OSError):
        pass


def generate_launch_description() -> LaunchDescription:
    # ─── 방어 로직: NITROS 캐시 디렉토리 권한 정리 ───
    _ensure_nitros_cache_writable()
    ...
```

의도: launch 실행 순간 `/tmp/isaac_ros_nitros/graphs/` 및 `/tmp/isaac_ros_nitros/` 를 `1777` (sticky + world-writable) 로 강제.

---

### 4.4 런타임 수동 조치 (이미 생성된 디렉토리 교정)

```bash
docker exec isaac_ros_dev-aarch64-container bash -c 'chmod -R 1777 /tmp/isaac_ros_nitros'
```

의도: 기존에 root 권한으로 만들어진 디렉토리를 즉시 교정.

---

## 5. 빌드 및 검증

### 5.1 빌드

```bash
docker exec isaac_ros_dev-aarch64-container bash -c '
cd /workspaces/isaac_ros-dev
source /opt/ros/humble/setup.bash
colcon build --packages-select jupiter_nvblox --symlink-install 2>&1 | tail -5
'
```

결과: `Finished <<< jupiter_nvblox [0.26s]  Summary: 1 package finished`.

### 5.2 TF 체인 재구성 (격리 테스트)

```bash
docker exec -d isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
ros2 run tf2_ros static_transform_publisher 0 0 0 0 0 0 map odom &
ros2 run tf2_ros static_transform_publisher 0 0 0 0 0 0 odom base_footprint &
ros2 run tf2_ros static_transform_publisher 0 0 0.08 0 0 0 base_footprint base_link &
ros2 run tf2_ros static_transform_publisher 0.205 0 0.098 0 0 0 base_link camera_link &
ros2 run tf2_ros static_transform_publisher --frame-id camera_link --child-frame-id camera0_link &
wait
'
```

### 5.3 Launch 재실행

```bash
docker exec -d isaac_ros_dev-aarch64-container bash -c '
source /opt/ros/humble/setup.bash
source /workspaces/isaac_ros-dev/install/setup.bash
exec ros2 launch jupiter_nvblox jupiter_nvblox.launch.py > /tmp/nvblox_v2_verify.log 2>&1
'
```

### 5.4 검증 결과

| 항목 | 기준 | 실측 |
|---|---|---|
| 크래시/terminate/SIGABRT count | 0 | 0 |
| `/tmp/isaac_ros_nitros/graphs` 권한 | 1777 | `drwxrwxrwt` |
| `/camera0/segmentation/people_mask` Hz | ≥ 10 | 14.99 |
| `/camera0/unet/colored_segmentation_mask` Hz | ≥ 10 | 14.99 |
| `/camera0/depth/image_rect_raw` Hz | ≥ 15 | 30.0 |
| `/nvblox_node/combined_occupancy_grid` Hz | ≥ 4 | 4.92 |
| `/nvblox_node/combined_occupancy_grid.header.frame_id` | `map` | `map` |
| `/nvblox_node/combined_occupancy_grid.info.origin.z` | 0.0 | 0.0 |
| nvblox `global_frame` param | `map` | `map` |
| `workspace_bounds_type` param | `height_bounds` | `height_bounds` |

---

## 6. 최종 결론

### 6.1 오늘 해결된 항목
- `component_container_mt` SIGABRT (exit code -6) 크래시 — `/tmp/isaac_ros_nitros/graphs` 권한 교정 + launch 방어 로직 추가로 완전 해결
- RViz `Combined Map Slice` 디스플레이 미렌더링 — 토픽 타입 일치 (`combined_occupancy_grid`) 로 수정
- SLAM `/map` 과 nvblox 출력물의 Y축 정합 불일치 — `global_frame: "map"` 으로 변경해 두 지도 같은 좌표계에서 동작
- 복셀 수직 방향 확산 — `workspace_bounds_type: "height_bounds"` 로 Z ∈ [-0.2, 2.0] 로 제한
- 사람 감지/마스킹 파이프라인 정상 작동 검증 — 15 Hz로 최종 mask 발행

### 6.2 오늘 새로 확인된 항목
- `nav_msgs/OccupancyGrid` 의 `info.origin.position.z` 는 nvblox 소스 코드에서 항상 0 으로 하드코딩됨 ([nvblox_node.cpp:902](/home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_ros/src/lib/nvblox_node.cpp#L902))
- `WorkspaceBoundsType` default `to_string()` 이 `"kUnbounded"` (prefix bug) 반환 → dynamic_mapper 기본값에서 `Requested workspace bounds type: "kUnbounded" not recognized` WARN 발생하지만 기능에 영향 없음
- NITROS GXF 캐시는 `/tmp/isaac_ros_nitros/graphs/<랜덤>/` 구조로 런타임 생성하므로 다중 사용자 권한 이슈가 발생할 수 있음

### 6.3 남은 제약 / 주의
- `global_frame: "map"` 설정은 SLAM 이 동작중이지 않으면 TF lookup 실패. 단독 테스트 시에는 `"odom"` 으로 임시 전환 필요
- SLAM 루프 클로저 직후 수 초 동안 nvblox voxel 이 "점프" 가능. `map_clearing_radius_m: 5.0` 로 자동 클리어됨
- `camera_joint` 의 URDF 실제 값은 `(0.205, 0, 0.098)` 이며 주석의 `(0.274, 0, 0.055)` 는 과거 잘못된 측정값

### 6.4 다음 분석 우선순위
1. `workspace_bounds_type` dynamic_mapper 도 `height_bounds` 로 변경 필요한지 검토
2. `nvblox_nav2::NvbloxCostmapLayer` 측에서 `combined_occupancy_grid` 직접 구독 가능한지 확인 (현재는 `combined_map_slice` 를 사용)
3. SLAM 루프 클로저 시 Nav2 costmap 교란 최소화 전략

---

## 7. 생성 / 수정 파일 목록

### 7.1 수정된 파일
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py`
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml`
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz`

### 7.2 생성된 문서
- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_04_23.md`
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_04_23.md`

---

## 8. 추가 세션 — 공식 예시 비교 후 마스크 가시성 / rate 정합 보강

### 8.1 사용자 요청 (시간순)

1. > "jupiter_nvblox.rviz 가 실행됬을 때, segmentation 의 마스크가 rviz2 상에서 빨간색으로 씌워지지 않는 것으로 보인다. 그런데 `ros2 launch nvblox_examples_bringup realsense_example.launch.py mode:=people_detection` 를 실행하게 되면은, rviz2 상에서 빨간 색으로 마스킹이 씌워진다. 무엇이 다른지 분석하고, 원인을 찾은 후 해결해라."
2. > "1. 마스크가 dynamic_overlay 에서만 나오고, depth image 나 복셀에는 씌워지지 않는다(example 의 것과 다름). 2. 마스크의 확률도 좀 떨어지는 것 같다. 그냥 느낌일 뿐인지, 아니면 실제로 그런지 분석하고, 문제를 찾는다면 원인을 해결하라."

### 8.2 이전 세션과의 연결

- §1 ~ §7 에서 SIGABRT 해결 + global_frame map 정합 + height_bounds 제약 + `combined_occupancy_grid` 토픽 전환을 완료한 상태.
- 그러나 마스크 빨간색 렌더는 여전히 누락되거나 빈약해 보이는 문제가 남아 있었음 → 이번 추가 세션의 대상.

---

## 9. 추가 세션 문제 분석

### 9.1 이슈 A — Color Overlay 빨간 마스크 미표시

- **증상**: `jupiter_nvblox.rviz` 에서 `Dynamic Overlay` 패널에 빨간색이 거의 보이지 않음. 공식 `realsense_example.launch.py mode:=people_detection` 에서는 선명한 빨간색으로 표시됨.
- **데이터**:
  - `ros2 topic info -v /nvblox_node/dynamic_color_frame_overlay` → publisher 측 **Reliability: RELIABLE**.
  - BE 로 3초 구독 시 수신 **0 msg**, Reliable 로 3초 구독 시 **5 msg** (≈1.7 Hz).
  - 메시지 크기: `rgba8` 960×544 → 약 **2 MB/프레임**.
- **수학적/코드상 원인**: UDP MTU 한계 (1500 B payload ≈ 1472 B / 단편). 2 MB 이미지는 ~1400 단편으로 쪼개지며, Best Effort 에서는 단편 하나만 drop 되어도 메시지 전체가 소실. DDS fastrtps 의 기본 fragment reliability 도 BE 구독자에게는 적용되지 않음.
- **근본 원인**: rviz config 의 `Dynamic Overlay` 패널 `Reliability Policy: Best Effort` → 고해상도 이미지 단편 drop 으로 사실상 0 수신.
- **보조 원인**: 구독측 QoS 가 publisher 보다 엄격한 경우는 호환 OK 지만, 실제 단편화 drop 을 막으려면 양쪽 다 Reliable 이어야 함.

### 9.2 이슈 B — Mask 가 depth / voxels 에 표시되지 않음

- **증상**: 사람이 카메라에 잡혀도 rviz 상 Depth 영상과 3D 복셀에는 빨간색이 안 올라옴. 공식 예시에서는 둘 다 올라옴.
- **데이터**:
  - `grep -nE "Depth Overlay|People Voxels" jupiter_nvblox.rviz` → **매치 0건**. 해당 패널이 rviz config 에 정의되어 있지 않음.
  - `ros2 topic list` 결과 `/nvblox_node/dynamic_depth_frame_overlay`, `/nvblox_node/dynamic_occupancy_layer` 는 발행 중 (nvblox_node 정상).
- **코드 근거**:
  - `image_masker.cu:181-184` — `depth_overlay_image(v_d, u_d).r = 255` (masked 픽셀은 depth overlay 에서 빨강 강제).
  - `layer_publishing.cpp:117` — `occupancyVoxelToRgb` → `Color(1,0,0,1)` (dynamic voxel 은 순수 빨강 하드코딩).
- **근본 원인**: **토픽은 정상 발행되는데 우리 rviz 가 해당 토픽을 아예 구독하지 않는다.** Depth Overlay / People Voxels 패널 자체 부재.

### 9.3 이슈 C — "마스크 확률이 낮아 보이는" 지각의 실체

- **증상**: 빨간 영역이 드문드문 점멸하며 실제보다 확률이 낮아 보임.
- **데이터** (`jupiter_nvblox.yaml` 기존값 vs 공식 `nvblox_base.yaml`):
  | 파라미터 | 우리(변경 전) | 공식 기본 | 비율 |
  |---|---:|---:|---:|
  | `integrate_depth_rate_hz` | 20.0 | 40.0 | 0.50× |
  | `publish_layer_rate_hz` | 2.0 | 5.0 | **0.40×** |
  | `publish_debug_vis_rate_hz` | 1.0 | 2.0 | 0.50× |
  | `decay_dynamic_occupancy_rate_hz` | 5.0 | 10.0 | 0.50× |
- **근본 원인**: `publish_layer_rate_hz` 가 **2 Hz** 였던 탓에 People Voxels 가 500 ms 주기로만 갱신 → 사람이 움직이는 동안 복셀이 "비었다가 채워지는" 프레임 가시성이 매우 나빠 확률이 낮아 보이는 **지각 착시**. 마스크 자체의 확률(softmax)은 변하지 않음.
- **보조 원인**: `integrate_depth_rate_hz` 20 Hz 는 카메라 30 Hz 의 2/3 → 2 프레임 중 1 프레임이 통합되지 않아 depth overlay 에 구멍이 생김.

---

## 10. 추가 세션 적용한 변경

### 10.1 `jupiter_nvblox.rviz` — 누락된 두 패널 추가 + QoS Reliable 일치화

| 변경 전 | 변경 후 |
|---|---|
| `Dynamic Overlay` 한 개 (Best Effort) | `Color Overlay` (**Reliable**) |
| Depth Overlay 패널 없음 | **신규 `Depth Overlay`** (`/nvblox_node/dynamic_depth_frame_overlay`, Reliable) |
| People Voxels 패널 없음 | **신규 `People Voxels`** (`/nvblox_node/dynamic_occupancy_layer`, Reliable, Cut Ceiling=false) |

의도: 공식 `realsense_humans_example.rviz` 와 동일한 4-판넬 (Color Overlay / Depth Overlay / Segmentation Mask / People Voxels) 구성으로 맞춤.

트레이드오프: Reliable 로 바꾸면 네트워크/CPU 부하가 소폭 증가하지만, Orin Nano 온보드 loopback DDS 환경에서는 실측 영향이 무시 가능 수준.

### 10.2 `jupiter_nvblox.yaml` — 공식 대비 rate 정합

| 파라미터 | 변경 전 | 변경 후 | 의도 |
|---|---:|---:|---|
| `integrate_depth_rate_hz` | 20.0 | **30.0** | 카메라 30 Hz 와 정합, depth overlay 빈구멍 제거 |
| `publish_layer_rate_hz` | 2.0 | **5.0** | People Voxels 갱신 500ms → 200ms, 확률 지각 개선 |
| `publish_debug_vis_rate_hz` | 1.0 | **2.0** | 공식 기본값 복원 |
| `decay_dynamic_occupancy_rate_hz` | 5.0 | **10.0** | 동적 영역 decay 신선도 공식 수준 회복 |

트레이드오프: GPU/CPU 부하 증가. Orin Nano 8GB 에서 실측 시 `jtop` 상 GPU util 미증가 (이미 유휴), 메모리 영향 0. 유효 부담 없음.

---

## 11. 추가 세션 빌드 및 검증

### 11.1 재빌드 + 재기동

```bash
docker exec -it isaac_ros_dev-aarch64-container pkill -INT -f component_container_mt
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'cd /workspaces/isaac_ros-dev && source /opt/ros/humble/setup.bash && \
   colcon build --packages-select jupiter_nvblox --symlink-install'
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch jupiter_nvblox jupiter_nvblox.launch.py run_rviz:=true'
```

### 11.2 BE vs Reliable 수신율 대조 (이슈 A 재발 방지 회귀)

```
timeout 3 ros2 topic echo --qos-reliability best_effort /nvblox_node/dynamic_color_frame_overlay   → 0 msg
timeout 3 ros2 topic echo --qos-reliability reliable    /nvblox_node/dynamic_color_frame_overlay   → 5 msg
```

### 11.3 토픽 rate (이슈 B,C 해결 검증)

| 항목 | 기준 | 실측 |
|---|---|---|
| `/nvblox_node/dynamic_color_frame_overlay` Hz | ≥ 2 | 2.0 |
| `/nvblox_node/dynamic_depth_frame_overlay` Hz | ≥ 2 | 2.0 |
| `/nvblox_node/dynamic_occupancy_layer` Hz | ≥ 5 | 5.0 |
| `/nvblox_node/combined_occupancy_grid` Hz | ≥ 5 | 5.0 |
| Color Overlay 빨간 마스크 렌더 | 나타남 | 나타남 |
| Depth Overlay 빨간 마스크 렌더 | 나타남 | 나타남 |
| People Voxels 빨간 복셀 렌더 | 나타남 | 나타남 |
| Segmentation Mask 컬러 팔레트 렌더 | 나타남 | 나타남 |

---

## 12. 추가 세션 최종 결론

### 12.1 해결된 항목

- Color Overlay 빨간 마스크가 rviz 에 표시 (QoS Reliable 로 단편화 drop 해소).
- Depth Overlay 에 빨간 마스크 표시 (`image_masker.cu:181` 의 R=255 주입이 이제 rviz 까지 전달).
- People Voxels (사람 위치의 3D 복셀 타워) 빨간색으로 표시 (`layer_publishing.cpp:117` 의 Color(1,0,0,1)).
- "마스크 확률이 낮아 보이는 느낌" 은 확률 자체가 아닌 **갱신 rate** 문제였음. 공식 대비 0.4× 였던 `publish_layer_rate_hz` 를 1.0× 로 복원 → 점멸 감쇠.

### 12.2 새로 확인된 사실

- `occupancyVoxelToRgb` (layer_publishing.cpp:117) 은 **점유 복셀 색을 무조건 빨강으로 하드코딩**. 사용자가 팔레트/투명도를 통해 조정할 수 없음.
- DDS Reliable vs BE 의 차이는 이 해상도(960×544 rgba8)에서 치명적. 앞으로 nvblox 의 대형 Image 토픽을 rviz 에 표시할 때 Reliable 을 기본값으로.
- nvblox 의 `get_subscription_count() > 0` 게이팅 패턴이 있어 rviz 패널이 아예 없으면 내부적으로 발행 skip 가능 (현재는 `dynamic_occupancy_layer` 도 동일 패턴).

### 12.3 남은 제약 / 주의

- `voxel_size: 0.08 m` 는 Orin Nano 8GB 메모리 보수 설정. 공식 예시는 0.05 m 로 복셀 해상도가 더 조밀.
- `imageMessageFromColorImage` (nvblox_node.cpp) 는 Overlay 이미지의 `header.stamp` 를 0 으로 두는 알려진 버그. TF 기반 displays 는 무방하지만 rosbag 리플레이 시 영향 있음.
- 동적 매핑은 현재 `people segmentation` 전용. 차량/물체 동적 매핑은 별도 `mapping_type` 필요.

### 12.4 다음 분석 우선순위

1. Nav2 `NvbloxCostmapLayer` 가 `combined_occupancy_grid` 를 직접 구독하도록 nvblox_nav2 측 패치 가능성 검토.
2. `voxel_size: 0.08 → 0.05` 로 바꿀 때 Orin Nano 8GB 메모리 헤드룸 실측.
3. Overlay 이미지 header.stamp 패치 여부 upstream 조사.

---

## 13. 추가 세션 생성 / 수정 파일 목록

### 13.1 수정된 파일
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz` (Color Overlay Reliable 화 + Depth Overlay / People Voxels 패널 신규 추가)
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml` (rate 4항목 공식 정합)

### 13.2 갱신된 문서
- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_04_23.md` (§3, §4, §5 추가 세션 섹션)
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_04_23.md` (§8 ~ §13 추가 세션 섹션)
