# 진행과정 2026-03-17: 방안B-2 Anti-Hunting 필터 구현 및 Nav2 파라미터 튜닝

> 이 문서는 2026-03-17 세션의 작업 진행과정을 기록합니다.
> 이전 세션(2026-03-16→17 연속)에서 방안B(MIN_ANGULAR_DEADZONE)로 Goal2 성공률 50~67%를 달성했으나,
> 로봇이 좌우 진동(헌팅) 후 급격한 회전(오버슈트)을 반복하는 문제가 관찰되었고,
> 이를 해결하기 위한 방안B-2를 구현합니다.

---

## 목차
1. [배경 및 문제 진단](#1-배경-및-문제-진단)
2. [방안B-2 설계: Anti-Hunting Filter](#2-방안b-2-설계-anti-hunting-filter)
3. [구현 내역](#3-구현-내역)
4. [Nav2 파라미터 튜닝](#4-nav2-파라미터-튜닝)
5. [펌웨어 업데이트 후 되돌릴 항목 (Revert Checklist)](#5-펌웨어-업데이트-후-되돌릴-항목-revert-checklist)
6. [현재 적용 상태 요약](#6-현재-적용-상태-요약)

---

## 1. 배경 및 문제 진단

### 1.1 이전 세션 결과 (방안B, MIN_ANGULAR_DEADZONE)
| 조건 | Goal2 성공률 | Yaw span | 비고 |
|------|-------------|----------|------|
| 변경 전 (기본) | 0% | N/A | 데드존으로 회전 불가 |
| MIN_DZ=0.15 | 2/4 (50%) | ~360° | 성공 시에도 360° 회전 |
| MIN_DZ=0.25 | 2/3 (67%) | ~358° | 소폭 개선 |

### 1.2 물리적 관찰 (핵심 진단)
Goal2 실행 중 로봇의 실제 동작:
1. **좌우 진동(헌팅)**: 로봇이 좌우로 진동하면서 회전하려고 노력
2. **갑작스런 풀스피드 회전(오버슈트)**: 어느 순간 확 회전해버림
3. **Nav2 방향 전환 → 재발**: 오버슈트 후 Nav2가 반대 명령 → 같은 사이클 반복

### 1.3 근본 원인 분석: MCU 데드존 → PID 윈드업 → 오버슈트

MCU `Motor_Ignore_Dead_Zone` 함수가 PID 출력을 **0/72/144 세 값으로만 양자화**하여 비례 제어를 불가능하게 만듦:

```
[MCU 내부 동작 추적]

1) Nav2 → 작은 회전 명령 (예: 0.3 rad/s)
2) MCU Differential_Ctrl → PID 목표 = 약 30mm/s
3) PID 출력 점진 상승: 0 → 6 → 15 → 55  (incremental PID, 10ms 주기)
4) Motor filter α=0.1: 출력 극도로 느리게 추종
5) Motor_Ignore_Dead_Zone: filter 출력 < 72 → return 72 (후진!)
                                                  ↑ 이것이 핵심 문제
6) 모터 후진 이동 → 에러 증가 → PID 적분 누적 (윈드업)
7) filter 출력 72~144 진입 → Dead zone return 0 (정지)
8) PID 적분 계속 누적... 
9) filter 출력 > 144 → Dead zone return 144 (풀 전진!)
   → 갑작스런 오버슈트!
10) Nav2 반대 명령 → 같은 사이클 반대 방향으로 반복 → 발산
```

**ESC 제어 특성** (서보 펄스 기반):
- 72 = 후진 풀스피드 (1.0ms 펄스)
- 108 = 중립/정지 (1.5ms 펄스)
- 144 = 전진 풀스피드 (2.0ms 펄스)
- Dead zone이 72 리턴 = **전진 명령에 후진 응답** → 에러 급증

---

## 2. 방안B-2 설계: Anti-Hunting Filter

### 2.1 전략

MCU 펌웨어를 수정할 수 없는 현 상황에서, ROS 드라이버 단에서 헌팅→윈드업 사이클을 끊는 필터:

| 기법 | 설명 | 효과 |
|------|------|------|
| **방향 전환 감지** | angular sign flip 카운트 | 헌팅 상태 인식 |
| **제동 삽입** | 0.8초 내 2회 전환 시 150ms zero 명령 | MCU에 (0,0,0) → `Motion_Stop` → PID 리셋 |
| **Rate Limiting** | 각속도 변화율 2.0 rad/s² 제한 | MCU PID 적응 시간 확보, 급변 방지 |

### 2.2 제동 삽입의 원리

MCU에 `set_car_motion(0, 0, 0)` 전송 시:
1. `Differential_Ctrl()` → `Motion_Stop(STOP_BRAKE)` 호출
2. 모터 전기적 제동 (ESC 108=중립)
3. `Motion_Set_Speed(0,0,0,0)` → PID 목표=0
4. PID 적분항이 리셋됨 (incremental PID에서 목표=0이면 출력 감쇠)
5. 150ms 후 정상 명령 재개 → 깨끗한 상태에서 재시작

---

## 3. 구현 내역

### 3.1 파일: `jupiter_driver_compensated.py`

**변경 1: `__init__`에 상태 변수 추가** (line ~181)
```python
# [2026-03-17 방안B-2] Anti-hunting angular velocity filter state
# MCU 데드존(0/72/144) + PID 적분 윈드업으로 인한
# 헌팅→오버슈트→발산 방지용 상태 변수
# ※ 방안A(펌웨어) 적용 후 아래 초기화 및 _anti_hunting_filter() 삭제할 것
self._ah_last_angular = 0.0
self._ah_last_time = time.time()
self._ah_reversal_count = 0
self._ah_last_reversal_time = 0.0
self._ah_brake_until = 0.0  # 제동 해제 시점
```

**변경 2: `_anti_hunting_filter()` 메서드 추가** (line ~193)
```python
def _anti_hunting_filter(self, angular_corrected):
    now = time.time()
    dt = max(now - self._ah_last_time, 0.001)
    self._ah_last_time = now
    
    # 제동 구간 → zero 출력
    if now < self._ah_brake_until:
        self._ah_last_angular = 0.0
        return 0.0
    
    # 방향 전환 감지
    if (angular_corrected * self._ah_last_angular < -0.001 and
            abs(self._ah_last_angular) > 0.03):
        self._ah_reversal_count += 1
        self._ah_last_reversal_time = now
    else:
        if now - self._ah_last_reversal_time > 1.0:
            self._ah_reversal_count = max(0, self._ah_reversal_count - 1)
    
    # 헌팅 감지: 0.8초 내 2회 이상 전환 → 150ms 제동
    if (self._ah_reversal_count >= 2 and
            now - self._ah_last_reversal_time < 0.8):
        self._ah_brake_until = now + 0.15
        self._ah_reversal_count = 0
        self._ah_last_angular = 0.0
        return 0.0
    
    # Rate limiting: 2.0 rad/s² 이하로 변화율 제한
    MAX_ANG_RATE = 2.0
    max_change = MAX_ANG_RATE * dt
    diff = angular_corrected - self._ah_last_angular
    if abs(diff) > max_change:
        angular_corrected = self._ah_last_angular + math.copysign(max_change, diff)
    
    self._ah_last_angular = angular_corrected
    return angular_corrected
```

**변경 3: `cmd_vel_callback`에 필터 호출 추가** (line ~313)
```python
# [2026-03-17 방안B-2] Anti-hunting filter: 방향전환 감쇠 + rate limiting
# ※ 방안A(펌웨어) 적용 후 아래 1줄 삭제할 것
angular_corrected = self._anti_hunting_filter(angular_corrected)
```

---

## 4. Nav2 파라미터 튜닝

### 4.1 파일: `nav2_params_fused.yaml`

DWB controller 파라미터를 강화하여 Nav2 단에서도 오버슈트/진동 억제:

| 파라미터 | 변경 전 | 변경 후 | 목적 |
|----------|---------|---------|------|
| `max_vel_theta` | 1.0 | **0.7** | MCU 데드존 오버슈트 방지 — 최대 회전속도 하향 |
| `acc_lim_theta` | 0.8 | **0.5** | 각가속도 제한 — MCU PID 적응 시간 확보 |
| `decel_lim_theta` | -0.8 | **-0.5** | 각감속도 제한 — 급감속 방지 |
| `Twirling.scale` | 5.0 | **15.0** | 스핀 방지 critic 페널티 3배 증가 |
| `Oscillation.scale` | 1.0 | **5.0** | 진동(헌팅) critic 페널티 5배 증가 |

### 4.2 변경하지 않은 파라미터 (유지)
| 파라미터 | 현재값 | 이유 |
|----------|--------|------|
| `rotate_to_heading_angular_vel` | 0.8 | RotationShim 초기 정렬 — 0.5에서 실패 사례 있음 |
| `min_speed_theta` | 0.30 | 데드존 탈출 최소 보장 — 이미 적절 |
| `max_angular_accel` | 5.0 | RotationShim용 — DWB와 별도 제어 |

---

## 5. 펌웨어 업데이트 후 되돌릴 항목 (Revert Checklist)

> **중요**: 방안A(MCU 펌웨어 `bsp_motor.c`)가 플래시되면, 아래 ROS 워크어라운드들은 반드시 제거해야 합니다.
> 펌웨어가 데드존을 비례매핑으로 해결하면, 드라이버 단의 임시 우회가 오히려 정상 제어를 방해합니다.

### 5.1 드라이버: `jupiter_driver_compensated.py`

#### ❌ 삭제 항목 1: 방안B — 최소 회전속도 강제 (2026-03-16)
- **위치**: `cmd_vel_callback()` 내부, `── 방안B 시작 ──` ~ `── 방안B 끝 ──` 주석 사이
- **내용**: MIN_ANGULAR_DEADZONE = 0.25 및 조건문
- **삭제할 코드**:
```python
# [2026-03-16 방안B] MCU 데드존 우회: 최소 회전 속도 보장
# ── 방안B 시작 ──
MIN_ANGULAR_DEADZONE = 0.25
if abs(angular_corrected) > 0.01 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
    angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
# ── 방안B 끝 ──
```
- **이유**: 펌웨어가 비례매핑하면 데드존이 없어지므로, 작은 각속도를 강제로 증폭할 필요 없음

#### ❌ 삭제 항목 2: 방안B-2 — Anti-Hunting Filter (2026-03-17)
- **위치 1**: `__init__()` 끝부분, `self._ah_*` 상태 변수 초기화 (5줄)
- **위치 2**: `_anti_hunting_filter()` 메서드 전체 (~55줄)
- **위치 3**: `cmd_vel_callback()` 내부, `self._anti_hunting_filter()` 호출 (1줄)
- **삭제할 코드 요약**:
```python
# __init__ 에서 삭제:
self._ah_last_angular = 0.0
self._ah_last_time = time.time()
self._ah_reversal_count = 0
self._ah_last_reversal_time = 0.0
self._ah_brake_until = 0.0

# 메서드 전체 삭제:
def _anti_hunting_filter(self, angular_corrected):
    ... (전체)

# cmd_vel_callback 에서 삭제:
angular_corrected = self._anti_hunting_filter(angular_corrected)
```
- **이유**: 펌웨어 비례매핑 후 0/72/144 양자화가 사라지면 헌팅→윈드업 사이클 자체가 발생하지 않음

### 5.2 Nav2 파라미터: `nav2_params_fused.yaml`

#### ⚠️ 원래 값으로 복원할 파라미터 (2026-03-17 변경분)
| 파라미터 | 현재(방안B-2) | 원래값 | 복원 이유 |
|----------|--------------|--------|-----------|
| `max_vel_theta` | 0.7 | **1.0** | 펌웨어 비례매핑 후 full range 사용 가능 |
| `acc_lim_theta` | 0.5 | **0.8** | MCU PID가 정상 추종하면 빠른 가속 허용 |
| `decel_lim_theta` | -0.5 | **-0.8** | 정상 감속 허용 |
| `Twirling.scale` | 15.0 | **5.0** | 과도한 스핀 페널티 제거 |
| `Oscillation.scale` | 5.0 | **1.0** | 정상 경로 탐색 허용 |

#### ✅ 유지할 파라미터 (데드존과 무관한 이전 튜닝)
| 파라미터 | 값 | 변경일 | 이유 |
|----------|-----|--------|------|
| `min_speed_theta` | 0.30 | 2026-03-11 | 모터 특성상 최소 회전속도 보장 (펌웨어와 무관) |
| `rotate_to_heading_angular_vel` | 0.8 | 2026-03-11 | RotationShim 정렬 속도 (테스트로 확정) |
| `max_angular_accel` | 5.0 | 2026-03-11 | RotationShim 가속 보장 |

### 5.3 MCU 펌웨어: `bsp_motor.c` (방안A — 플래시만 하면 됨)

> 이 항목들은 이미 소스 수정 완료 상태이며, MCU에 빌드+플래시가 필요합니다.

| 변경 | 위치 | 내용 |
|------|------|------|
| **방안A-1** | `Motor_Ignore_Dead_Zone()` | PID 출력(±3600) → ESC(72~144) 비례매핑 |
| **방안A-2** | `Motor_Set_Pwm()` M1/M3 | Differential: ESC값 직접 A채널 출력 |
| **방안A-3** | `MOTOR_FILTER_ALPHA` | 0.1 → 0.3 (응답속도 개선) |

### 5.4 Revert 절차 요약

```
펌웨어 플래시 후 순서:
1. bsp_motor.c 빌드 → STM32 플래시 (방안A 적용)
2. jupiter_driver_compensated.py:
   - ── 방안B 시작/끝 ── 블록 삭제
   - self._ah_* 초기화 5줄 삭제
   - _anti_hunting_filter() 메서드 전체 삭제
   - self._anti_hunting_filter() 호출 1줄 삭제
3. nav2_params_fused.yaml:
   - max_vel_theta: 0.7 → 1.0
   - acc_lim_theta: 0.5 → 0.8
   - decel_lim_theta: -0.5 → -0.8
   - Twirling.scale: 15.0 → 5.0
   - Oscillation.scale: 5.0 → 1.0
4. colcon build --packages-select jupiter_bringup jupiter_nav_VO --symlink-install
5. 시스템 재시작 후 Nav2 Goal2 테스트
```

---

## 6. 현재 적용 상태 요약

### 6.1 활성 워크어라운드 (방안A 플래시 전까지 유지)

| 항목 | 값 | 파일 | 제거 시점 |
|------|----|------|-----------|
| 방안B: MIN_ANGULAR_DEADZONE | 0.25 | jupiter_driver_compensated.py | 방안A 플래시 후 |
| 방안B-2: Anti-hunting filter | 활성 | jupiter_driver_compensated.py | 방안A 플래시 후 |
| max_vel_theta | 0.7 | nav2_params_fused.yaml | 방안A 플래시 후 |
| acc_lim_theta | 0.5 | nav2_params_fused.yaml | 방안A 플래시 후 |
| decel_lim_theta | -0.5 | nav2_params_fused.yaml | 방안A 플래시 후 |
| Twirling.scale | 15.0 | nav2_params_fused.yaml | 방안A 플래시 후 |
| Oscillation.scale | 5.0 | nav2_params_fused.yaml | 방안A 플래시 후 |

### 6.2 영구 변경 (방안A와 무관하게 유지)

| 항목 | 값 | 파일 | 변경일 |
|------|----|------|--------|
| ANGULAR_SCALE_FACTOR | 0.353 | jupiter_driver_compensated.py | 2026-02-11 |
| imu_gyro_scale | 1.025 | nav2_vslam_fused.launch.py | 2026-03-16 |
| frozen_threshold | 60 | nav2_vslam_fused.launch.py | 2026-03-16 |
| frozen_publish_rate | 10.0 | nav2_vslam_fused.launch.py | 2026-03-16 |
| min_speed_theta | 0.30 | nav2_params_fused.yaml | 2026-03-11 |
| rotate_to_heading_angular_vel | 0.8 | nav2_params_fused.yaml | 2026-03-11 |

### 6.3 전체 방안 상태

| 방안 | 내용 | 상태 | MCU 적용 |
|------|------|------|----------|
| A-1 | Dead zone 비례매핑 | ✅ 소스 수정 | ❌ 빌드+플래시 필요 |
| A-2 | M1/M3 ESC 직접출력 | ✅ 소스 수정 | ❌ 빌드+플래시 필요 |
| A-3 | Filter alpha 0.3 | ✅ 소스 수정 | ❌ 빌드+플래시 필요 |
| B | 최소 회전속도 강제 | ✅ 적용 중 | — (ROS 워크어라운드) |
| B-2 | Anti-hunting filter | ✅ 적용 중 | — (ROS 워크어라운드) |
| Nav2 튜닝 | θ 제한 + critic 강화 | ✅ 적용 중 | — (Nav2 파라미터) |
