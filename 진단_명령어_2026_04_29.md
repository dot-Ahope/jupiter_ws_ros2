# 명령어 로그 2026-04-29: 상태 의존적 cmd_vel 응답 — balance correction stale refs 진단 + 수정

> 진단 및 수정에 사용한 명령어와 스크립트/설정 파일을 요약 없이 전부 기록한다.

## 0. 오늘 사용한 터미널 명령어 원문

### 0.1 시리얼 충돌 진단 (legacy rosmaster_main.py)

```bash
ls -la /dev/myserial /dev/ttyTHS1 /dev/ttyUSB* 2>&1 | head -10
lsof /dev/ttyTHS1 2>/dev/null | head -10
```
목적: 시리얼 포트와 점유 프로세스 확인.
결과: `/dev/myserial -> ttyUSB0`, `/dev/ttyUSB0` 존재. `ttyTHS1` 은 점유자 없음.

```bash
lsof /dev/ttyUSB0 2>&1 | head -10
lsof /dev/myserial 2>&1 | head -10
pgrep -fa jupiter_driver 2>&1
timeout 3 ros2 node list 2>&1 | head -20
```
목적: ttyUSB0 점유자와 ROS 노드 상태 확인.
결과 (핵심):
```
COMMAND      PID   USER   FD   TYPE DEVICE SIZE/OFF NODE NAME
python3     2958 jetson   11u   CHR  188,0      0t0  895 /dev/ttyUSB0
jupiter_d 225895 jetson   22u   CHR  188,0      0t0  895 /dev/ttyUSB0
```
**두 프로세스가 동시 점유 = 시리얼 충돌**.

```bash
ps -p 2958 -o pid,user,etime,cmd 2>&1
ps -p 225895 -o pid,user,etime,cmd 2>&1
cat /proc/2958/cmdline 2>&1 | tr '\0' ' '
ls -l /proc/2958/fd 2>/dev/null | head -20
```
목적: PID 2958 정체 식별.
결과:
```
2958 jetson 04:36:20 python3 /home/jetson/Rosmaster/rosmaster/rosmaster_main.py
225895 jetson 09:03 /usr/bin/python3 .../jupiter_driver_compensated --ros-args ...
```
PID 2958 = 야붐 레거시 드라이버, 4시간 36분째 실행 중. FD 11 = `/dev/ttyUSB0`.

```bash
systemctl list-units --type=service --state=running 2>&1 | grep -iE "rosmaster|yahboom" | head -5
grep -rE "rosmaster_main\.py|/home/jetson/Rosmaster" /etc/systemd/system /etc/rc.local /etc/xdg/autostart ~/.config/autostart 2>/dev/null | head -10
```
목적: 자동시작 확인.
결과:
```
yahboom_oled.service          loaded active running transbot_oled start service
/home/jetson/.config/autostart/bash.desktop:Exec=bash /home/jetson/Rosmaster/rosmaster/start_app.sh
```
GNOME 자동시작 (`~/.config/autostart/bash.desktop`) 이 `start_app.sh` → `rosmaster_main.py` 실행.

```bash
cat /home/jetson/Rosmaster/rosmaster/start_app.sh 2>&1
cat /home/jetson/.config/autostart/bash.desktop 2>&1
```
목적: 자동시작 스크립트 내용 확인.
결과: `gnome-terminal -- bash -c "...python3 /home/jetson/Rosmaster/rosmaster/rosmaster_main.py..."`.

(사용자가 시스템 재부팅으로 충돌 해소 — 추가 명령 미사용)

### 0.2 path_curvature_diag.py ratio 버그 수정 + 재측정

```bash
python3 -c "import ast; ast.parse(open('/home/jetson/path_curvature_diag.py').read()); print('syntax OK')"
```
목적: 수정 후 syntax 검증.
결과: `syntax OK`.

(사용자 측 별도 터미널에서 실행:)
```bash
python3 ~/path_curvature_diag.py --window 5
# joy 로 회전 명령 발행 후 Ctrl-C
```
결과 (final summary):
```
cmd_angular     n= 769 avg= -0.3492 std=0.1870 peak= -1.0000
odom_angular    n=1264 avg= -0.2997 std=0.3097 peak= -0.9497
rf2o_angular    n= 252 avg= -0.3092 std=0.3180 peak= -0.9523
imu_angular     n=2813 avg= -0.2808 std=0.3175 peak= -0.9802

  ★ cmd → rf2o (전체 |z| 평균비) = 0.981  (|cmd|=0.3523, |rf2o|=0.3455)
    → ratio 정상 범위 (0.85~1.15). 회전 자체는 정상.
```

### 0.3 scenario3_diag.py 신규 작성 + 측정

```bash
python3 -c "import ast; ast.parse(open('/home/jetson/scenario3_diag.py').read()); print('syntax OK')"
chmod +x /home/jetson/scenario3_diag.py
ls -l /home/jetson/scenario3_diag.py
```
목적: 신규 도구 syntax + 실행권한 부여.
결과: `syntax OK`, `-rwxrwxr-x 1 jetson jetson 9098 ...`.

(사용자 측 측정:)
```bash
python3 ~/scenario3_diag.py
# joy 로 좌회전 4회 (cmd_peak +2.000) 후 Ctrl-C
```
결과:
```
event 1-4: rf2o_back +0.0000, imu_back +0.0000
rf2o_n_resid: 48 / 9 / 9 / 9
평균 복귀 peak: rf2o=+0.0000, imu=+0.0000
→ 자동 복귀 미관측 (잔류 0.0000 < 0.05)
```
(나중에 idle stop 우회로 인한 false negative 임이 판명)

### 0.4 scenario1_diag.py 신규 작성

```bash
python3 -c "import ast; ast.parse(open('/home/jetson/scenario1_diag.py').read()); print('syntax OK')"
chmod +x /home/jetson/scenario1_diag.py
```
목적: 신규 직진 드리프트 측정 도구 syntax + 실행권한.
결과: syntax OK.

### 0.5 jupiter_driver_compensated.py 백업 버전 swap 도구 작성

```bash
ls -la /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/backup_2026_04_28/ 2>&1
diff -q /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/backup_2026_04_28/jupiter_driver_compensated.py.bak /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py 2>&1
```
목적: 백업 폴더 내용 + 현재 src 와 가장 오래된 백업 일치 여부.
결과:
```
total 160
-rw-rw-r--  1 jetson jetson 27569 Apr 28 15:55 jupiter_driver_compensated.py.bak
-rw-rw-r--  1 jetson jetson 28015 Apr 29 08:21 jupiter_driver_compensated.py.bak2
-rw-rw-r--  1 jetson jetson 31743 Apr 29 08:56 jupiter_driver_compensated.py.bak3
-rw-r--r--  1 jetson jetson 62257 Apr 28 15:55 Rosmaster_Lib.py.bak
Files ... differ
```

```bash
for f in /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/backup_2026_04_28/jupiter_driver_compensated.py.bak{,2,3} /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py; do
  wc -l "$f"
done
diff <(head -60 .../jupiter_driver_compensated.py.bak) <(head -60 .../jupiter_driver_compensated.py.bak2) 2>&1 | head -30
diff <(head -100 .../jupiter_driver_compensated.py.bak2) <(head -100 .../jupiter_driver_compensated.py.bak3) 2>&1 | head -30
```
목적: 각 백업 버전의 차이점 매핑.
결과:
- bak (589 lines, Apr 28 15:55): 가장 오래된 — adjust 인자 없음
- bak2 (594 lines, Apr 29 08:21): `adjust=True` 하드코드 추가
- bak3 (658 lines, Apr 29 08:56): linear STARTUP 키크 추가
- current (670 lines): `ANGULAR_THRESHOLD_FOR_MCU_ADJUST=0.15` dynamic 분기 추가

```bash
diff <(grep -A1 "set_car_motion" .../jupiter_driver_compensated.py.bak | head -20) <(grep -A1 "set_car_motion" .../jupiter_driver_compensated.py.bak2 | head -20)
diff /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/backup_2026_04_28/Rosmaster_Lib.py.bak /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py 2>&1 | head -30
ls -la /home/jetson/jupiter_ws_ros2/install/jupiter_bringup/lib/python3*/site-packages/jupiter_bringup/jupiter_driver_compensated.py 2>&1 | head -5
find /home/jetson/jupiter_ws_ros2/install/jupiter_bringup -name "jupiter_driver_compensated*" 2>&1
find /home/jetson/jupiter_ws_ros2/install -name "*.egg-link" -o -name "*.pth" 2>&1 | head -5
cat /home/jetson/jupiter_ws_ros2/install/jupiter_bringup/lib/python3.10/site-packages/jupiter-bringup.egg-link 2>&1
ls -la /home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py 2>&1
md5sum /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py /home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/backup_2026_04_28/jupiter_driver_compensated.py.bak{,2,3} 2>&1
```
목적: install/build 구조 추적, 어느 버전이 실제 실행되는지 확인.
결과: egg-link → `/home/jetson/jupiter_ws_ros2/build/jupiter_bringup`. src 와 build 의 md5 동일 (`b3e8fa14...`) → 현재 threshold 버전이 실제 실행 중. bak/bak2/bak3 는 모두 다른 md5.

```bash
chmod +x /home/jetson/swap_driver.sh
bash -n /home/jetson/swap_driver.sh
/home/jetson/swap_driver.sh status
```
목적: swap 도구 syntax + 현재 상태 확인.
결과:
```
syntax OK
src:    (매칭 없음)  (670 lines, 2026-04-29 08:57:28)
build:  (매칭 없음)  (670 lines, 2026-04-29 08:57:28)
```

### 0.6 펌웨어 코드 분석 — Mecanum_Ctrl, balance correction, Motor_Ignore_Dead_Zone

```bash
grep -n "Motion_Ctrl\|Motion_Set_Yaw_Adjust\|Motion_Stop\|FUNC_MOTION\|PID_Yaw_Reset" /home/jetson/mcu/extbd/Source/APP/app_motion.c | head -50
grep -rn "Motion_Yaw_Calc\|Mecanum_Yaw_Calc" /home/jetson/mcu/extbd/Source/ 2>/dev/null
grep -n "g_speed_setup" /home/jetson/mcu/extbd/Source/APP/*.c | head -20
grep -n "PID_Yaw_Reset\|PID_Yaw_Calc\|g_yaw_adjust" /home/jetson/mcu/extbd/Source/APP/app_pid.c | head -30
grep -n "PID_Set_Motor_Target\|PID_Clear_Motor\|PID_Calc_Motor" /home/jetson/mcu/extbd/Source/APP/app_pid.c | head -20
grep -rn "MOTOR_MAX_PULSE\|MOTOR_IGNORE_PULSE\|PID_FF_GAIN\|PID_FF_THRESHOLD" /home/jetson/mcu/extbd/Source/ 2>/dev/null | grep -E "#define|=\s*[0-9]" | head -20
grep -n "Motor_Ignore_Dead_Zone" /home/jetson/mcu/extbd/Source/BSP/bsp_motor.c | head -5
grep -rn "CAR_MECANUM\|CAR_DIFFERENTIAL\|CAR_FOURWHEEL\|CAR_ACKERMAN\|CAR_SUNRISE" /home/jetson/mcu/extbd/Source/APP/*.h /home/jetson/mcu/extbd/Source/BSP/*.h 2>/dev/null | grep -E "#define|enum|=.*[0-9]" | head -30
grep -rn "motor_dead_zone_fwd\|motor_dead_zone_bwd\|Motor_Set_Dead_Zone" /home/jetson/mcu/extbd/Source/BSP/bsp_motor.c /home/jetson/mcu/extbd/Source/APP/*.c 2>&1 | head -20
```
목적: 코드베이스의 stateful 요소 / 차종 enum / deadzone 설정 추적.
핵심 결과:
```
app_mecanum.c:25:static uint16_t g_speed_setup = 0;
app_mecanum.c:154:    g_offset_yaw = yaw_offset * g_speed_setup;
app_mecanum.c:214:    g_speed_setup = speed;       ← Mecanum_State 에서만 갱신
```
→ FUNC_MOTION 경로에선 g_speed_setup 절대 갱신 안 됨, MCU yaw PID 의 모터 영향 = 0 확정.

```
CAR_MECANUM = 0x01,         CAR_DIFFERENTIAL = 0x07
app.c:158: Motor_Set_Dead_Zone(MOTOR_ID_M1, 720, 690);
app.c:159: Motor_Set_Dead_Zone(MOTOR_ID_M2, 780, 750);
bsp_motor.c:19: motor_dead_zone_fwd[MAX_MOTOR] = {700, 760, 700, 700};
bsp_motor.c:20: motor_dead_zone_bwd[MAX_MOTOR] = {700, 760, 700, 700};
```
→ M1: 720/690, M3: default 700/700 → 직진 시 R 가 10 PWM 더 강함 = 좌측 편향.

```bash
git -C /home/jetson/mcu/extbd log --oneline -15 2>&1
git -C /home/jetson/mcu/extbd status 2>&1 | head -30
git -C /home/jetson/jupiter_ws_ros2 log --oneline -10 -- src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py 2>&1
git -C /home/jetson/jupiter_ws_ros2 status 2>&1 | head -40
diff /home/jetson/mcu/extbd/Source/APP/app_mecanum.c.bak_2026_04_28 /home/jetson/mcu/extbd/Source/APP/app_mecanum.c
```
목적: 펌웨어/드라이버 변경 이력 + 현재 백업 vs 라이브 차이 확인.
결과: 4개 차종 \_Ctrl 함수만 modified. 백업은 `.bak_2026_04_28` 4개. driver 도 1 file modified.

### 0.7 펌웨어 패치 적용

(Read 와 Edit 도구 사용 — bash 명령은 검증용만)

```bash
echo "=== app_mecanum.c brace count ==="; awk 'BEGIN{ob=0;cb=0;op=0;cp=0} {for(i=1;i<=length($0);i++){c=substr($0,i,1); if(c=="{")ob++; if(c=="}")cb++; if(c=="(")op++; if(c==")")cp++}} END{printf "{ %d / } %d   ( %d / ) %d\n", ob, cb, op, cp}' /home/jetson/mcu/extbd/Source/APP/app_mecanum.c
echo "=== app.c brace count ==="; awk 'BEGIN{ob=0;cb=0;op=0;cp=0} {for(i=1;i<=length($0);i++){c=substr($0,i,1); if(c=="{")ob++; if(c=="}")cb++; if(c=="(")op++; if(c==")")cp++}} END{printf "{ %d / } %d   ( %d / ) %d\n", ob, cb, op, cp}' /home/jetson/mcu/extbd/Source/APP/app.c
grep -n "balance_prev_vx" /home/jetson/mcu/extbd/Source/APP/app_mecanum.c
grep -n "Motor_Set_Dead_Zone" /home/jetson/mcu/extbd/Source/APP/app.c
```
목적: 패치 후 brace/paren 균형 + balance_prev_vx 갱신 위치 + deadzone 라인 확인.
결과:
```
app_mecanum.c: { 16 / } 16   ( 106 / ) 106
app.c:         { 101 / } 101   ( 351 / ) 351
balance_prev_vx 라인 (mecanum.c): 32 (decl), 66 (stop branch invalidate, 신규), 132 (reset 조건), 152 (if 안 갱신, 이동), 156 (else invalidate, 신규)
deadzone 라인 (app.c): 158 (M1), 159 (M2), 163 (M3, 신규)
```

```bash
diff /home/jetson/mcu/extbd/Source/APP/app_mecanum.c.bak_2026_04_28 /home/jetson/mcu/extbd/Source/APP/app_mecanum.c | head -80
git -C /home/jetson/mcu/extbd diff Source/APP/app.c | head -25
```
목적: 최종 패치 diff 확인.
결과:
- `app_mecanum.c`: 2026-04-28 edge-trigger 패치 + 2026-04-29 balance invalidate (V=0 stop 분기 + else 분기)
- `app.c`: M3 deadzone 한 줄 추가 (line 163 영역)

(사용자가 본인 PC 에서 STM32 IDE 로 펌웨어 빌드 + 플래시 — Jetson 측에서는 실행 안 함)

### 0.8 사용자 측 검증 (재현 테스트)

(사용자가 직접 joy 사용:)
```
# 시나리오 2 재현 — 끊김 없는 직진 → 좌회전 → 직진
joy hold forward + apply right turn briefly + release
```
T1 적용 전: 큰 자동 복귀 거동 (수 도 단위로 회전 반대 방향)
T1 적용 후 (사용자 보고): "증상이 남아있기는 하나 많이 개선되었다."
잔류: "전진 시 오른쪽으로 회전을 줄 시, 왼쪽으로 복귀하는 현상이 있는데, 미세하다."

---

## 1. 분석에 사용한 설정 / 스크립트 파일

### 1.0 오늘 사용/열람한 스크립트 및 파일 전체 경로

#### 펌웨어 (mcu/extbd)
- `/home/jetson/mcu/extbd/Source/APP/app_mecanum.c` ← 수정 (T1: balance refs invalidate)
- `/home/jetson/mcu/extbd/Source/APP/app.c` ← 수정 (T2: M3 deadzone)
- `/home/jetson/mcu/extbd/Source/APP/app_motion.c` (열람)
- `/home/jetson/mcu/extbd/Source/APP/app_pid.c` (열람)
- `/home/jetson/mcu/extbd/Source/APP/app_differential.c` (열람)
- `/home/jetson/mcu/extbd/Source/APP/app_fourwheel.c` (열람)
- `/home/jetson/mcu/extbd/Source/APP/app_ackerman.c` (열람)
- `/home/jetson/mcu/extbd/Source/BSP/bsp_motor.c` (열람)
- `/home/jetson/mcu/extbd/Source/APP/app_mecanum.h` (열람)
- `/home/jetson/mcu/extbd/Source/APP/app_motion.h` (열람)
- `/home/jetson/mcu/extbd/Source/APP/app_pid.h` (열람)
- `/home/jetson/mcu/extbd/Source/BSP/bsp_motor.h` (열람)
- `/home/jetson/mcu/extbd/Source/APP/app_mecanum.c.bak_2026_04_28` (diff 비교)

#### Driver / ROS workspace
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` (열람)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py` (열람)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/backup_2026_04_28/jupiter_driver_compensated.py.bak` (열람, 비교)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/backup_2026_04_28/jupiter_driver_compensated.py.bak2` (열람, 비교)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/backup_2026_04_28/jupiter_driver_compensated.py.bak3` (열람, 비교)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/backup_2026_04_28/Rosmaster_Lib.py.bak` (diff 비교)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` (열람)
- `/home/jetson/jupiter_ws_ros2/install/jupiter_bringup/lib/jupiter_bringup/jupiter_driver_compensated` (열람)
- `/home/jetson/jupiter_ws_ros2/install/jupiter_bringup/lib/python3.10/site-packages/jupiter-bringup.egg-link` (열람)
- `/home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` (열람)

#### 진단 도구 (홈 디렉토리)
- `/home/jetson/path_curvature_diag.py` ← 수정 (`mag_nz` 추가)
- `/home/jetson/scenario3_diag.py` ← 신규
- `/home/jetson/scenario1_diag.py` ← 신규
- `/home/jetson/swap_driver.sh` ← 신규

#### 시스템 / 자동시작
- `/dev/myserial`, `/dev/ttyTHS1`, `/dev/ttyUSB0`, `/dev/ttyUSB1` (점검)
- `/proc/2958/cmdline`, `/proc/2958/fd` (열람)
- `/home/jetson/Rosmaster/rosmaster/start_app.sh` (열람)
- `/home/jetson/.config/autostart/bash.desktop` (열람)

#### 플랜 / 문서
- `/home/jetson/.claude/plans/functional-sleeping-starfish.md` ← 수정 (전면 재작성)

### 1.1 수정된 파일 1 — `app_mecanum.c` (Mecanum 차종 cmd_vel 처리)

`/home/jetson/mcu/extbd/Source/APP/app_mecanum.c`

수정 항목:
- L60-68 (V=0 stop 분기): `balance_prev_vx = 0;` 한 줄 추가
- L120-157 (balance correction 블록): if-else 재구성
  - if (V_y==0 && V_z==0) 안: `balance_prev_vx = V_x;` 이동 (구 line 142 의 무조건 갱신 제거)
  - else: `balance_prev_vx = 0;` (회전/측방 진입 시 invalidate)
- 패치 의도/메커니즘 주석 추가 (`[2026-04-29 Patch]` 태그)

### 1.2 수정된 파일 2 — `app.c` (FreeRTOS task 초기화 + 모터 deadzone 설정)

`/home/jetson/mcu/extbd/Source/APP/app.c`

수정 항목:
- L163 부근: `Motor_Set_Dead_Zone(MOTOR_ID_M3, 720, 690);` 한 줄 추가
- M1 과 동일 패턴으로 M3 도 명시 → 좌우 deadzone 대칭

### 1.3 수정된 파일 3 — `path_curvature_diag.py` (cmd vs rf2o ratio 측정 도구)

`/home/jetson/path_curvature_diag.py`

수정 항목:
- `_stats()` 에 `mag_nz` (절댓값 평균) 필드 추가
- `_report()` 와 `final_summary()` 의 ratio 계산이 `avg_nz` (부호) → `mag_nz` (절댓값) 사용
- 컬럼 헤더 `avg|>0.05|` → `mag|>0.05|`
- final summary 출력에 `|cmd|`, `|rf2o|` 명시

### 1.4 신규 파일 1 — `scenario3_diag.py` (회전 종료 후 자동 복귀 검증)

`/home/jetson/scenario3_diag.py`

핵심 로직:
- 상태기계 IDLE → ROTATING (cmd_z>0.30) → SETTLE (cmd_z≤0.05 후 3초)
- SETTLE 중 50ms 마다 `cmd_x/cmd_z/rf2o_z/imu_z/mcu_z` 출력
- 복귀 peak (반대 방향 angular_z) 자동 산출

### 1.5 신규 파일 2 — `scenario1_diag.py` (직진 드리프트 정량화)

`/home/jetson/scenario1_diag.py`

핵심 로직:
- 상태기계 IDLE → FORWARD (vx>0.05, |angular_cmd|≤0.10)
- FORWARD segment 별로 ∫rf2o yaw [°], ∫imu yaw [°], drift per meter [°/m] 적분
- direction (LEFT/RIGHT/NONE) 분류

### 1.6 신규 파일 3 — `swap_driver.sh` (driver 백업 버전 swap)

`/home/jetson/swap_driver.sh`

기능:
- `bak/bak2/bak3/current/status` 인자
- src/ + build/ 동시 갱신 (colcon build 없이도 다음 launch 부터 적용)
- swap_save 로 현재 버전 보관 (current 명령으로 복원 가능)

---

## 2. 관찰 기준 요약

| 기준 | 값 / 측정 방법 | 실측 결과 |
|---|---|---|
| 시리얼 포트 점유 | `lsof /dev/ttyUSB0` | 두 프로세스 (jupiter_driver + legacy rosmaster_main) 동시 점유 — 충돌 |
| path_curvature ratio | `mag_nz` 기반 ★ ratio | 0.981 (정상 0.85~1.15) |
| scenario3 복귀 peak (idle stop 포함) | rf2o_back [rad/s] | 0.0000 (false negative) |
| 시나리오 2/3 자동 복귀 (T1 적용 전) | 시각적 정성 | 회전 양만큼 정확히 반대 방향으로 되돌아옴 |
| 시나리오 2/3 자동 복귀 (T1 적용 후) | 시각적 정성 | 큰 거동 사라짐, 미세 잔류만 |
| 시나리오 1 직진 드리프트 | 시각적 정성 (T2 적용 후) | 개선 확인, 정량 측정은 미실시 |
| brace/paren 균형 (app_mecanum.c) | awk count | 16/16, 106/106 |
| brace/paren 균형 (app.c) | awk count | 101/101, 351/351 |
| 펌웨어 git 변경 파일 | `git status` | 4개 차종 _Ctrl 함수 (Mecanum/Differential/Fourwheel/Ackerman) + app.c |
| driver 변경 파일 | `git status` | jupiter_driver_compensated.py (threshold 분기) |
