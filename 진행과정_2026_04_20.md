# 진행과정 2026-04-20: 모터 비대칭 + 정지 회전 데드존 분석 및 설정

## 1. 목표

### 1.1 사용자 요청 (시간순)
> 1. 후진 시 왼쪽 바퀴가 더 많이 돌아 쏠림 현상이 발생한다. Nav2에서 보정해가면서 해결하는 것이 최선인가?
> 2. 정지 상태에서 회전 시작 최소 angular이 0.37이다. 주행 중에는 더 적은 값으로도 회전한다. Nav2가 이를 보정하는가?
> 3. 설정 방법론을 제시하라
> 4. Nav2 시스템과 충돌이 일어나지 않을 것들만 적용시켜라

### 1.2 이전 세션 요약
- 2026-04-17: 모터/인코더 교체 후 오도메트리 캘리브레이션 수정
  - `ANGULAR_SCALE_FACTOR = 1.0` (이전 0.401)
  - `MCU_MIN_ANGULAR = 0.0` (이전 0.12)
  - `vx = -vx` 복원
  - `imu_invert_z` 로직 빌드 적용

---

## 2. 문제 분석

### 2.1 후진 쏠림 (왼쪽 바퀴 과회전)

**증상**: 물리적 모터 비대칭으로 후진 시 왼쪽 바퀴가 더 많이 회전 → 직진 대신 한쪽으로 쏠림

**Nav2 보정 동작**:
1. Nav2가 직선 후진 `cmd_vel` 발행
2. 물리적 비대칭으로 로봇 경로 이탈
3. EKF가 이탈 감지 (wheel + IMU + RF2O 융합)
4. 다음 제어 사이클 (100ms @10Hz)에서 DWB가 보정 명령 생성

**한계**: 제어 주기마다 "이탈→감지→보정" 반복 → 지그재그 주행. 쏠림이 클수록 진동 폭 증가.

**보정 계층 분석**:

| 계층 | 방법 | 제어 주기 | 효과 | Nav2 충돌 |
|------|------|----------|------|----------|
| 펌웨어 PID | 모터별 PID 게인 비대칭 보정 | 10ms | 최상 | 없음 |
| 펌웨어 밸런스 | `Mecanum_Ctrl()` 직진 밸런스 (이미 구현, 전진 전용) | 10ms | 양호 | 없음 |
| 드라이버 | `cmd_vel_callback()`에서 후진 시 좌우 오프셋 | 50Hz | 중간 | 없음 |
| Nav2 DWB | 피드백 루프 자동 보정 | 10Hz (100ms) | 느림 | - |

**결론**: 모터 교체 전까지 Nav2 피드백 루프에 의존. 물리적 문제이므로 소프트웨어만으로는 근본 해결 불가.

### 2.2 정지 회전 데드존 (0.37 rad/s)

**증상**:
- 정지 상태: angular < 0.37 → 모터 무반응
- 주행 중: angular 0.15로도 회전 가능 (동적 마찰 < 정적 마찰)

**Nav2 동작 분석**:

| 상황 | Nav2 출력 | 실제 동작 | 문제 |
|------|----------|----------|------|
| RotationShim (제자리 회전) | `rotate_to_heading_angular_vel: 0.5` | 0.5 > 0.37 → **정상** | 없음 |
| DWB 정지 회전 | `min_speed_theta: 0.15` ~ 0.36 가능 | 0.15~0.36 < 0.37 → **무반응** | 저크 발생 |
| DWB 주행 중 조향 | 0.15 이상 | 동적 마찰 낮아 **정상** | 없음 |

→ 문제는 DWB가 정지 상태에서 0.15~0.36 angular를 보낼 때만 발생

---

## 3. 적용한 변경

### 3.1 충돌 위험 분석

| 변경 | Nav2 충돌 위험 | 판단 |
|------|--------------|------|
| `MCU_MIN_ANGULAR = 0.37` | **없음** — 드라이버 레이어, 정지 시만 적용 | ✅ 적용 |
| `controller_frequency: 20.0` | **있음** — 10Hz로 낮춘 이유 (Jetson 부하) 존재 | ❌ 보류 |
| `velocity_smoother CLOSED_LOOP` | **있음** — EKF 오도메트리 피드백 경로 변경 | ❌ 보류 |

### 3.2 MCU_MIN_ANGULAR = 0.37 적용

**파일**: `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `MCU_MIN_ANGULAR` | `0.0` (비활성) | `0.37` (정지 상태 최소 회전 실측값) |

**동작 로직** (기존 코드 line 271-273, 변경 없음):
```python
if abs(vx) < 0.02:  # 정지 상태에서만 클램프
    if abs(angular_corrected) > 0.001 and abs(angular_corrected) < self.MCU_MIN_ANGULAR:
        angular_corrected = math.copysign(self.MCU_MIN_ANGULAR, angular_corrected)
```

- `|vx| < 0.02` (정지): angular 0.001~0.37 → 0.37로 클램프
- `|vx| ≥ 0.02` (주행): 클램프 미적용 → 그대로 전달
- angular 0 또는 0.37 이상: 클램프 미적용

**colcon build 완료** ✅

---

## 4. 보류 항목

### 4.1 controller_frequency 증가 (10→20Hz)
- 쏠림 보정 반응 속도 2배 향상 가능
- Jetson Orin Nano Super 부하 측정 후 적용 여부 결정 필요

---

## 5. 시스템 종합 분석

### 5.1 사용자 요청
> jupiter_ws_ros2 + MCU 펌웨어 전체를 종합 분석하라

### 5.2 수행 내용
- jupiter_ws_ros2 전체 ROS2 드라이버/설정 파일 분석
- MCU 펌웨어 전체 분석 (app_mecanum.c, bsp_encoder.c, app_motion.c, app_pid.c, bsp_motor.c)
- 분석 결과를 `시스템_분석_2026_04_20.md`로 저장

### 5.3 사용자 피드백
- `--symlink-install` 사용 중이므로 source = installed (별도 빌드 불일치 없음)
- 전진은 정상, 후진 드리프트는 물리적 모터 비대칭 (Nav2가 자체 보정)
- 물리 상수는 이미 검증 완료
- ANGULAR_SCALE_FACTOR 캘리브레이션은 현재 불필요

---

## 6. Nav2 속도 제한 (0.35 → 0.20 m/s)

### 6.1 사용자 요청
> max_vel_x를 0.20으로 줄여라

### 6.2 변경 파일
**파일**: `src/jupiter_nav_VO/config/nav2_params_fused.yaml`

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| `controller_server.FollowPath.max_vel_x` | 0.35 | **0.20** |
| `controller_server.FollowPath.max_speed_xy` | 0.35 | **0.20** |
| `velocity_smoother.max_velocity` | [0.35, 0.0, 1.0] | **[0.20, 0.0, 1.0]** |
| `velocity_smoother.min_velocity` | [-0.35, 0.0, -1.0] | **[-0.20, 0.0, -1.0]** |

---

## 7. nvblox 사람 회피 통합

### 7.1 사용자 요청
> nvblox 사람 감지 기능을 Nav2에 통합하라

### 7.2 기존 구성 확인
- `jupiter_nvblox` 패키지가 isaac_ws_ros에 이미 존재 (people_segmentation 모드)
- nav2_params_fused.yaml에 nvblox_layer가 이미 설정되어 있었으나 `static_map_slice` 사용 중

### 7.3 변경 파일
**파일**: `src/jupiter_nav_VO/config/nav2_params_fused.yaml`

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| local_costmap nvblox_map_slice_topic | `/nvblox_node/static_map_slice` | **`/nvblox_node/combined_map_slice`** |
| global_costmap nvblox_map_slice_topic | `/nvblox_node/static_map_slice` | **`/nvblox_node/combined_map_slice`** |

**이유**: `combined_map_slice`는 정적 장애물 + 사람 감지 결과를 합산한 토픽

---

## 8. EKF ↔ Nav2 설정 일관성 검증

### 8.1 사용자 요청
> ekf_vslam_fusion.yaml과 nav2_params_fused.yaml의 프레임/파라미터 비교

### 8.2 결과
- frame 불일치: 없음 (odom, base_footprint, map 일관)
- 파라미터 충돌: 없음
- EKF 5센서 구성 (odom0~3, imu0) 정상

---

## 9. TF 트리 camera_link ↔ camera0_link 연결

### 9.1 문제
- Host URDF: `camera_link` 프레임 발행
- Docker RealSense: `camera0` namespace → `camera0_link` 프레임 사용
- nvblox가 `camera0_depth_optical_frame` → `odom` TF 체인을 찾지 못함

### 9.2 해결
**파일**: `src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py`

identity static TF 추가:
```
camera_link → camera0_link (0,0,0, 0,0,0)
```

### 9.3 검증
```
map → odom → base_footprint → base_link → camera_link → camera0_link → camera0_depth_optical_frame
```
- `/nvblox_node/combined_map_slice` ~5Hz 발행 확인
- `/nvblox_node/dynamic_color_frame_overlay` ~5Hz 발행 확인

---

## 10. nvblox 시각화 (RViz + Foxglove) 구성

### 10.1 사용자 요청
> jupiter_nvblox.launch.py에 foxglove와 rviz를 파라미터로 추가하고, RViz config 파일을 만들어라

### 10.2 생성 파일
**파일**: `isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz`

| 그룹 | 디스플레이 | 토픽 | 기본 상태 |
|------|-----------|------|----------|
| Nvblox | NvbloxMesh | `/nvblox_node/mesh` | disabled |
| Nvblox | NvbloxVoxelBlockLayer | `/nvblox_node/color_layer` | enabled |
| Nvblox | Map Slice Bounds | `/nvblox_node/esdf_slice_bounds` | enabled |
| Nvblox | ESDF Slice | `/nvblox_node/static_esdf_pointcloud` | enabled |
| Nvblox | Combined Map Slice | `/nvblox_node/combined_map_slice` | enabled |
| Nvblox | Dynamic Occupancy Grid | `/nvblox_node/dynamic_occupancy_grid` | disabled |
| Sensors | Dynamic Overlay | `/nvblox_node/dynamic_color_frame_overlay` | enabled |
| Sensors | Segmentation Mask | `/camera0/unet/colored_segmentation_mask` | enabled |
| Sensors | Depth/Color Image | `/camera0/depth/...`, `/camera0/color/...` | disabled |
| Navigation | Local/Global Costmap | `/local_costmap/costmap`, `/global_costmap/costmap` | disabled |

- Fixed Frame: `odom`
- Target Frame: `base_link`
- isaac_sim_example.rviz 기반으로 토픽을 실제 환경에 맞게 수정

### 10.3 수정 파일
**파일**: `isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py`

| 추가 항목 | 설명 |
|----------|------|
| `run_rviz` 파라미터 (default: false) | `rviz2` 노드 조건부 실행 |
| `run_foxglove` 파라미터 (default: false) | `foxglove_bridge` 노드 조건부 실행 |
| `IfCondition` import | 조건부 실행 지원 |
| `Node` import | 비-composable 노드 지원 |

**사용법** (Docker 내):
```bash
ros2 launch jupiter_nvblox jupiter_nvblox.launch.py run_rviz:=true
ros2 launch jupiter_nvblox jupiter_nvblox.launch.py run_foxglove:=true
ros2 launch jupiter_nvblox jupiter_nvblox.launch.py run_rviz:=true run_foxglove:=true
```
- 보류 이유: 이전에 의도적으로 10Hz로 낮춘 기록 존재

### 4.2 velocity_smoother CLOSED_LOOP
- 실제 오도메트리 기반 스무딩으로 전환
- EKF → velocity_smoother 피드백 경로가 추가되어 기존 제어 흐름 변경
- 보류 이유: EKF 발산 등 부작용 가능성 테스트 필요

### 4.3 ANGULAR_SCALE_FACTOR 재캘리브레이션
- 현재 1.0 (passthrough) — 새 모터에 맞는 정확한 값 미측정
- 회전 정확도 향상을 위해 실측 필요 (e.g., 1.0 rad/s × 10s → 실제 회전각 측정)

---

## 5. 변경된 파일 목록

| 파일 | 변경 내용 |
|---|---|
| `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` | `MCU_MIN_ANGULAR = 0.0` → `0.37` |
