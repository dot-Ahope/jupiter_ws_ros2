# 진단 명령어 2026-03-16: Direction D 테스트 및 VSLAM 진단

> 이 문서는 2026-03-16 세션에서 사용한 모든 명령어, 스크립트, 결과를 기록합니다.

---

## 목차
1. [시스템 상태 확인](#1-시스템-상태-확인)
2. [test_direction_d.py 실행](#2-test_direction_dpy-실행)
3. [VSLAM frozen 진단](#3-vslam-frozen-진단)
4. [독립 180° 회전 테스트](#4-독립-180-회전-테스트)
5. [Nav2 Goal2 상세 분석](#5-nav2-goal2-상세-분석)
6. [VSLAM reset 시도](#6-vslam-reset-시도)
7. [3-part comprehensive 진단](#7-3-part-comprehensive-진단)
8. [VSLAM 재시작 및 Nav2 재테스트](#8-vslam-재시작-및-nav2-재테스트)

---

## 1. 시스템 상태 확인

### 1.1 노드 확인
```bash
ros2 node list
```
```
/amcl
/bt_navigator
/controller_server
/ekf_filter_node
/jupiter_driver
/lifecycle_manager_navigation
/map_server
/planner_server
/rf2o_covariance_adapter      ← NEW (Direction D)
/rf2o_laser_odometry
/slam_toolbox
/velocity_smoother
/vslam_covariance_adapter
/waypoint_follower
```

### 1.2 Direction D 파라미터 확인
```bash
ros2 param get /rf2o_covariance_adapter rotation_threshold
# Double value is: 0.15

ros2 param get /jupiter_driver imu_gyro_scale
# Double value is: 1.025
```

### 1.3 RF2O adapted 공분산 확인 (정지 상태)
```bash
ros2 topic echo /odom_rf2o_adapted --once
```
```
pose:
  covariance:
    [0]: 0.5    ← position_cov_base ✓
    [35]: 0.05  ← orientation_cov_base ✓
twist:
  covariance:
    [0]: 0.01   ← linear_vel_cov_base ✓
    [35]: 0.01  ← angular_vel_cov_base ✓
```

---

## 2. test_direction_d.py 실행

### 2.1 명령어
```bash
cd ~/jupiter_ws_ros2
python3 scripts/test_direction_d.py
```

### 2.2 결과
```
=== Direction D 테스트 시작 ===
시작 위치: (-0.531, 0.016), yaw=-173.5°

=== Test A: Nav2 0.5m 전진 + 복귀 ===
Goal1: 0.5m forward → (-1.022, -0.041)
SUCCEEDED in 3.0s
 EKF 종점: (-0.999, -0.038)
 Goal dist err: 0.023m

Goal2: Return → (-0.531, 0.016)
SUCCEEDED in 35.7s
 EKF 종점: (-0.199, 0.069)
 Start dist: 0.337m

=== Test B: 180° 제자리 회전 ===
회전 시작 yaw: -172.1°
Rotating... timeout
회전 종료: 3.0s

[결과] ← 모든 센서 +0.00° (Nav2 cmd_vel 충돌로 실패)
  EKF yaw 변화:   +0.00° (err=+180.00°)
  Wheel yaw 변화: +0.00° (err=+180.00°)
  RF2O yaw 변화:  +0.00° (err=+180.00°)

=== RF2O Adapted 공분산 분포 ===
Total samples: 101
  base (≤0.6):   57 (56.4%)
  high (>5.0):   32 (31.7%)
  medium:        12 (11.9%)
```

### 2.3 분석
- Goal1: 3.0s (C: 6.3s) → 성능 향상
- Goal2: 35.7s (C: 11.8s) → VSLAM frozen이 원인
- Test B: **FAIL** — Nav2 controller가 여전히 cmd_vel 점유 → 스크립트의 회전 명령 무효

---

## 3. VSLAM frozen 진단

### 3.1 VSLAM 토픽 rate 확인
```bash
timeout 5 ros2 topic hz /visual_slam/tracking/odometry --window 20
```
```
average rate: 26.289
```

### 3.2 Frozen 상태 확인 스크립트
```python
python3 -c "
import rclpy, time
from rclpy.node import Node
from rclpy.qos import QoSProfile, ReliabilityPolicy, HistoryPolicy
from nav_msgs.msg import Odometry

rclpy.init()
n=rclpy.create_node('vcheck')
qos=QoSProfile(reliability=ReliabilityPolicy.BEST_EFFORT,
               history=HistoryPolicy.KEEP_LAST, depth=5)
data=[]
def cb(m):
    p=m.pose.pose.position
    data.append((time.time(),p.x,p.y,p.z))
n.create_subscription(Odometry,'/visual_slam/tracking/odometry',cb,qos)
t0=time.time()
while time.time()-t0<5:
    rclpy.spin_once(n, timeout_sec=0.1)
if len(data)<2:
    print(f'VSLAM: {len(data)} msgs in 5s')
else:
    hz=len(data)/5
    frozen=sum(1 for i in range(1,len(data))
               if abs(data[i][1]-data[i-1][1])<1e-6
               and abs(data[i][2]-data[i-1][2])<1e-6)
    pct=frozen/(len(data)-1)*100
    print(f'VSLAM: {hz:.1f}Hz, {len(data)} msgs')
    print(f'Frozen: {frozen}/{len(data)-1} ({pct:.0f}%)')
n.destroy_node(); rclpy.shutdown()
"
```
```
초기: Frozen: 272/273 (100%)    ← 완전 frozen
1차 reset 후: Frozen: 124/180 (69%)
2차 확인: Frozen: 123/155 (79%)
```

---

## 4. 독립 180° 회전 테스트

### 4.1 1차 회전 테스트 (Nav2 미사용)
```python
python3 -c "
import rclpy, time, math
from rclpy.node import Node
from rclpy.qos import QoSProfile, ReliabilityPolicy, HistoryPolicy
from nav_msgs.msg import Odometry
from geometry_msgs.msg import Twist

rclpy.init()

class RotTest(Node):
    def __init__(self):
        super().__init__('rot_test')
        oqos = QoSProfile(reliability=ReliabilityPolicy.BEST_EFFORT,
                          history=HistoryPolicy.KEEP_LAST, depth=5)
        self.ekf=[]
        self.wheel=[]
        self.rf2o=[]
        self.vslam=[]
        self.create_subscription(Odometry,'/odom',self._ekf,10)
        self.create_subscription(Odometry,'/odom_raw',self._wh,10)
        self.create_subscription(Odometry,'/odom_rf2o_adapted',self._rf,oqos)
        self.create_subscription(Odometry,'/visual_slam/tracking/odometry',self._vs,oqos)
        self.pub=self.create_publisher(Twist,'/cmd_vel',10)
    def _q(self,m):
        q=m.pose.pose.orientation
        return math.atan2(2*(q.w*q.z+q.x*q.y),1-2*(q.y*q.y+q.z*q.z))
    def _ekf(self,m): self.ekf.append((time.time(),self._q(m)))
    def _wh(self,m): self.wheel.append((time.time(),self._q(m)))
    def _rf(self,m):
        self.rf2o.append((time.time(),self._q(m),m.pose.covariance[0]))
    def _vs(self,m): self.vslam.append((time.time(),self._q(m)))

n=RotTest()
time.sleep(1)
for _ in range(50): rclpy.spin_once(n,timeout_sec=0.05)

e0=n.ekf[-1][1] if n.ekf else 0
w0=n.wheel[-1][1] if n.wheel else 0
r0i=len(n.rf2o)

# 180도 회전 (0.5 rad/s, ~6.3초)
t=Twist(); t.angular.z=0.5
dur=math.pi/0.5
t0=time.time()
while time.time()-t0<dur:
    n.pub.publish(t)
    rclpy.spin_once(n,timeout_sec=0.02)
t.angular.z=0.0
n.pub.publish(t)
time.sleep(0.5)
for _ in range(30): rclpy.spin_once(n,timeout_sec=0.05)

# 결과 수집
def ad(a,b):
    d=a-b
    while d>math.pi: d-=2*math.pi
    while d<-math.pi: d+=2*math.pi
    return d

ekf_d = math.degrees(ad(n.ekf[-1][1],e0))
wh_d = math.degrees(ad(n.wheel[-1][1],w0))

# RF2O adapted cov 분포
rf_data = n.rf2o[r0i:]
base=sum(1 for _,_,c in rf_data if c<=0.6)
high=sum(1 for _,_,c in rf_data if c>5.0)

print(f'EKF yaw 변화:   {ekf_d:+.2f}° (오차: {ekf_d-180:+.2f}°)')
print(f'Wheel yaw 변화: {wh_d:+.2f}° (오차: {wh_d-180:+.2f}°)')
print(f'RF2O adapted cov: base={base}, high={high} / {len(rf_data)}')

n.destroy_node(); rclpy.shutdown()
"
```

### 4.2 1차 결과
```
EKF yaw 변화:   +178.45° (오차: -1.55°)    ← C: -3.91° → 2.5배 개선
Wheel yaw 변화: +178.46° (오차: -1.54°)
RF2O adapted cov: base=23, high=69 / 92      ← 75% 회전 cov 적용 ✓
```

---

## 5. Nav2 Goal2 상세 분석

### 5.1 Goal2 35.7s 원인 분석 스크립트
```python
python3 -c "
import rclpy, time, math
from rclpy.node import Node
from rclpy.qos import QoSProfile, ReliabilityPolicy, HistoryPolicy
from nav_msgs.msg import Odometry
from geometry_msgs.msg import Twist

rclpy.init()
n=rclpy.create_node('g2check')
oqos=QoSProfile(reliability=ReliabilityPolicy.BEST_EFFORT,
                history=HistoryPolicy.KEEP_LAST,depth=5)
ekf=[]; cmds=[]; vslam_raw=[]; t0=time.time()
def _e(m):
    p=m.pose.pose.position; q=m.pose.pose.orientation
    yaw=math.atan2(2*(q.w*q.z+q.x*q.y),1-2*(q.y*q.y+q.z*q.z))
    ekf.append({'t':time.time()-t0,'x':p.x,'y':p.y,'yaw':math.degrees(yaw)})
def _c(m): cmds.append({'t':time.time()-t0,'vx':m.linear.x,'wz':m.angular.z})
def _v(m):
    p=m.pose.pose.position
    vslam_raw.append({'t':time.time()-t0,'x':p.x,'y':p.y})
n.create_subscription(Odometry,'/odom',_e,10)
n.create_subscription(Twist,'/cmd_vel',_c,10)
n.create_subscription(Odometry,'/visual_slam/tracking/odometry',_v,oqos)

# 60s 수집
dur=60
while time.time()-t0<dur:
    rclpy.spin_once(n,timeout_sec=0.05)

# VSLAM frozen 분석
frozen=0
for i in range(1,len(vslam_raw)):
    if abs(vslam_raw[i]['x']-vslam_raw[i-1]['x'])<1e-6:
        frozen+=1
pct=frozen/(len(vslam_raw)-1)*100 if len(vslam_raw)>1 else 0

# cmd_vel 분석
stops=sum(1 for c in cmds if abs(c['vx'])<0.01 and abs(c['wz'])<0.01)
stop_pct=stops/len(cmds)*100 if cmds else 0

print(f'EKF: {len(ekf)} msgs, yaw range [{min(e[\"yaw\"] for e in ekf):.1f}, {max(e[\"yaw\"] for e in ekf):.1f}]')
print(f'cmd_vel: {len(cmds)} msgs, stops={stops} ({stop_pct:.0f}%)')
print(f'VSLAM: {len(vslam_raw)} raw, frozen={frozen}/{max(1,len(vslam_raw)-1)} ({pct:.0f}%)')
n.destroy_node(); rclpy.shutdown()
"
```

### 5.2 분석 결과
- VSLAM: **100% frozen** (272/273 frames identical)
- cmd_vel: 85% stop (로봇 대부분 정지)
- EKF yaw 범위: -154.8° ~ 목표 방향 (큰 진동)
- Nav2 로그: "Failed to make progress" 반복 (~15회)

---

## 6. VSLAM reset 시도

### 6.1 Docker 확인
```bash
docker ps --format "{{.Names}} {{.Status}}"
# isaac_ros_dev-aarch64-container Up 25 minutes
```

### 6.2 Reset service 호출
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  "source /opt/ros/humble/setup.bash && \
   ros2 service call /visual_slam/reset \
   isaac_ros_visual_slam_interfaces/srv/Reset '{}'" 2>&1
```
→ **hung** (응답 없음, Ctrl+C로 중단)

### 6.3 Reset 후 frozen 상태
```
1차 reset 후: 69% frozen (272→124)
2차 확인: 79% frozen (개선 미미)
```

---

## 7. 3-part comprehensive 진단

### 7.1 실행 스크립트 (직진 1.0m + 180° 회전 + 직진 0.5m)
```python
python3 -c "
import rclpy, time, math
from rclpy.node import Node
from rclpy.qos import QoSProfile, ReliabilityPolicy, HistoryPolicy
from nav_msgs.msg import Odometry
from geometry_msgs.msg import Twist
import numpy as np

rclpy.init()

class Diag(Node):
    def __init__(self):
        super().__init__('diag')
        oqos=QoSProfile(reliability=ReliabilityPolicy.BEST_EFFORT,
                         history=HistoryPolicy.KEEP_LAST,depth=5)
        self.ekf=[]; self.rf2o=[]; self.vslam=[]
        self.t0=time.time()
        self.create_subscription(Odometry,'/odom',self._e,10)
        self.create_subscription(Odometry,'/odom_rf2o_adapted',self._r,oqos)
        self.create_subscription(Odometry,'/visual_slam/tracking/odometry',self._v,oqos)
        self.pub=self.create_publisher(Twist,'/cmd_vel',10)
    def _q(self,m):
        q=m.pose.pose.orientation
        return math.atan2(2*(q.w*q.z+q.x*q.y),1-2*(q.y*q.y+q.z*q.z))
    def _e(self,m):
        p=m.pose.pose.position
        self.ekf.append({'t':time.time()-self.t0,'x':p.x,'y':p.y,'yaw':self._q(m)})
    def _r(self,m):
        self.rf2o.append({'t':time.time()-self.t0,'cov':m.pose.covariance[0]})
    def _v(self,m):
        p=m.pose.pose.position
        self.vslam.append({'t':time.time()-self.t0,'x':p.x,'y':p.y})

n=Diag()
time.sleep(1)
for _ in range(50): rclpy.spin_once(n,timeout_sec=0.05)

sp=n.ekf[-1]; yaw0=sp['yaw']

def ad(a,b):
    d=a-b
    while d>math.pi: d-=2*math.pi
    while d<-math.pi: d+=2*math.pi
    return d

def drive(vx,wz,dur):
    t=Twist(); t.linear.x=vx; t.angular.z=wz
    t0=time.time()
    while time.time()-t0<dur:
        n.pub.publish(t)
        rclpy.spin_once(n,timeout_sec=0.02)
    t.linear.x=0.0; t.angular.z=0.0; n.pub.publish(t)
    time.sleep(0.5)
    for _ in range(20): rclpy.spin_once(n,timeout_sec=0.05)

def lat_at(ekf_slice,yaw_ref,dist):
    for e in ekf_slice:
        dx=e['x']-ekf_slice[0]['x']; dy=e['y']-ekf_slice[0]['y']
        fwd=dx*math.cos(yaw_ref)+dy*math.sin(yaw_ref)
        if fwd>=dist:
            lat=-dx*math.sin(yaw_ref)+dy*math.cos(yaw_ref)
            return lat
    return None

# Test 1: 직진 1.0m
print('=== Test 1: 직진 1.0m ===')
e1s=len(n.ekf); r1s=len(n.rf2o)
drive(0.15, 0.0, 7.0)
e1e=len(n.ekf)
ep=n.ekf[-1]
fwd=((ep['x']-sp['x'])**2+(ep['y']-sp['y'])**2)**0.5
lat03=lat_at(n.ekf[e1s:e1e],yaw0,0.3)
print(f'fwd={fwd:.3f}m, lat@종점={((ep[\"x\"]-sp[\"x\"])*(-math.sin(yaw0))+(ep[\"y\"]-sp[\"y\"])*math.cos(yaw0))*1000:.1f}mm')
print(f'lat@0.3m={lat03*1000:.1f}mm' if lat03 else 'lat@0.3m=N/A')

# Test 2: 180° 회전
print('\\n=== Test 2: 180° 회전 ===')
pre_yaw=n.ekf[-1]['yaw']
r2s=len(n.rf2o)
drive(0.0, 0.5, math.pi/0.5)
post_yaw=n.ekf[-1]['yaw']
yaw_change=math.degrees(ad(post_yaw,pre_yaw))
yaw_err=yaw_change-180
r2e=len(n.rf2o)
rf_slice=n.rf2o[r2s:r2e]
base=sum(1 for r in rf_slice if r['cov']<=0.6)
high=sum(1 for r in rf_slice if r['cov']>5.0)
print(f'EKF yaw 변화: {yaw_change:.2f}° (오차: {yaw_err:+.2f}°)')
print(f'RF2O cov: base={base}, high={high} / {len(rf_slice)}')

# Test 3: 회전 후 직진 0.5m
print('\\n=== Test 3: 회전 후 직진 0.5m ===')
post_yaw_ref=n.ekf[-1]['yaw']
e3s=len(n.ekf)
drive(0.15, 0.0, 4.0)
e3e=len(n.ekf)
ep3=n.ekf[-1]; sp3=n.ekf[e3s]
lat_end=-((ep3['x']-sp3['x'])*math.sin(post_yaw_ref))+((ep3['y']-sp3['y'])*math.cos(post_yaw_ref))
lat03_post=lat_at(n.ekf[e3s:e3e],post_yaw_ref,0.3)
print(f'lat@종점={lat_end*1000:.1f}mm')
print(f'lat@0.3m={lat03_post*1000:.1f}mm' if lat03_post else 'lat@0.3m=N/A')

# VSLAM frozen
vf=0
for i in range(1,len(n.vslam)):
    if abs(n.vslam[i]['x']-n.vslam[i-1]['x'])<1e-6: vf+=1
tt=time.time()-n.t0
print(f'\\nVSLAM: frozen={vf}/{max(1,len(n.vslam)-1)} ({vf/max(1,len(n.vslam)-1)*100:.0f}%), {len(n.vslam)/tt:.1f}Hz')

n.destroy_node(); rclpy.shutdown()
"
```

### 7.2 결과
```
=== Test 1: 직진 1.0m ===
fwd=0.443m, lat@종점=+10.0mm (좌)
lat@0.3m=+9.3mm (좌)

=== Test 2: 180° 회전 ===
EKF yaw 변화: 178.80° (오차: -1.20°)    ← Direction C: -3.91° → 3.2배 개선
RF2O cov: base=0, high=70 / 88           ← 80% 회전 cov 적용 ✓

=== Test 3: 회전 후 직진 0.5m ===
lat@종점=-4.7mm (우)                     ← 좌편향 → 우편향으로 전환 (거의 0)
lat@0.3m=-1.3mm (우)

VSLAM: frozen=122/155 (79%), 25.2Hz
```

### 7.3 핵심 해석
- **180° yaw 오차 -1.20°**: Direction C(-3.91°) 대비 3.2배 개선 (IMU gyro_scale 효과)
- **RF2O 동적 cov**: 회전 중 100% high (base=0), 전환 구간 20% → 정상 동작
- **회전 후 직진**: **좌편향(-) → 우편향(-4.7mm)** 전환 → 좌편향 해소
- **VSLAM 79% frozen**: 절대 위치 보정 부족 (환경 이슈)

---

## 8. VSLAM 재시작 및 Nav2 재테스트

### 8.1 VSLAM 프로세스 정리
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  "ps aux | grep visual_slam_node | grep -v grep"
# → empty (C++ 노드 이미 종료)

docker exec isaac_ros_dev-aarch64-container bash -c \
  "ps aux | grep -i visual_slam | grep -v grep"
# PID 34975: ros2 launch isaac_ros_visual_slam... (stuck)
# PID 50314, 50327, 50871: reset service calls (hung)

docker exec isaac_ros_dev-aarch64-container bash -c \
  "kill 34975 50314 50327 50871 2>/dev/null"
```

### 8.2 VSLAM 재시작
```bash
docker exec -d isaac_ros_dev-aarch64-container bash -c \
  "source /opt/ros/humble/setup.bash && \
   source /workspaces/isaac_ros-dev/install/setup.bash 2>/dev/null; \
   ros2 launch isaac_ros_visual_slam \
   isaac_ros_visual_slam_realsense.launch.py > /tmp/vslam_log.txt 2>&1"
```

### 8.3 VSLAM 상태 확인
```bash
ros2 topic list | grep visual_slam
# /visual_slam/initial_pose
# /visual_slam/status
# /visual_slam/tracking/odometry
# /visual_slam/tracking/odometry_adapted
# /visual_slam/tracking/slam_path

# VSLAM health check
python3 -c "..." (위 frozen 확인 스크립트 동일)
```
```
VSLAM: 0 msgs in 5s - NOT RUNNING
```
→ 토픽 존재하나 데이터 없음 (카메라 스트림 수신 불가)

### 8.4 Nav2 Goal 재테스트
```python
python3 -c "
# Nav2 0.5m forward + return with VSLAM monitoring
# (전체 스크립트는 Section 2와 유사, VSLAM Hz 모니터링 추가)
"
```
```
VSLAM: 0Hz, frozen=0/-1
Goal1: SUCCEEDED in 4.3s
Goal2: TIMEOUT in 60.0s, dist=1.209m
  EKF yaw range: [-169.7d, -155.9d], span=13.8d
VSLAM 전체: 0 msgs (0.0Hz)
```
→ VSLAM 완전 중단으로 Goal2 TIMEOUT (Direction D 변경과 무관)

### 8.5 카메라 USB 확인
```bash
lsusb | grep -i intel
# Bus 001 Device 005: ID 8086:0b5c Intel Corp. Intel(R) RealSense(TM) Depth Camera 455
```
→ USB 하드웨어 감지됨, Docker 내부 카메라 드라이버 문제 추정
→ 시스템 재부팅 또는 Docker 컨테이너 재생성 필요

---

## 9. VSLAM Frozen Detection 파라미터 수정

### 9.1 원인 분석
```bash
# cuVSLAM publish 빈도 vs 실제 새 pose 빈도 확인
ros2 topic hz /visual_slam/tracking/odometry --window 100
# ~60 Hz (but only 10-15 Hz are new poses, rest are repeats)
```

frozen_threshold=10 → 60Hz에서 0.17초 = 정상 동작도 frozen 판정

### 9.2 launch 파일 수정
```python
# nav2_vslam_fused.launch.py 내 vslam_covariance_adapter 파라미터
'frozen_threshold': 60,          # 10 → 60 (1초)
'frozen_publish_rate': 10.0,     # 1.0 → 10.0 Hz
'frozen_pos_tolerance': 1e-6,
```

### 9.3 수정 후 확인
```bash
ros2 param get /vslam_covariance_adapter frozen_threshold
# Integer value is: 60

ros2 param get /vslam_covariance_adapter frozen_publish_rate
# Double value is: 10.0
```

---

## 10. Nav2 재테스트 (Frozen Fix 적용 후)

### 10.1 재테스트 실행
```bash
ros2 launch /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
ros2 launch /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_fused_navigation.launch.py
```

### 10.2 결과
```
Goal1: SUCCEEDED 2.4s ✓
Goal2: TIMEOUT 60s, yaw span=359°, wz=-748.8mrad/s ❌
VSLAM adapted: 2574 msgs (개선됨, 이전 ~0)
```
→ VSLAM 데이터 정상 전달되나 Goal2 여전히 실패 → **모터 데드존 문제**

---

## 11. MCU 펌웨어 데드존 분석

### 11.1 분석 대상 파일
```bash
ls mcu/extbd/Source/BSP/bsp_motor.c  # 모터 PWM 제어 + 데드존
ls mcu/extbd/Source/BSP/bsp_motor.h  # 상수 정의
ls mcu/extbd/Source/APP/app_pid.c    # PID 제어
ls mcu/extbd/Source/APP/app_pid.h    # PID 상수
ls mcu/extbd/Source/APP/app_differential.c  # 차동 구동 제어
ls mcu/extbd/Source/APP/app_differential.h  # 차동 상수
ls mcu/extbd/Source/APP/app_motion.c        # 모션 핸들러
```

### 11.2 핵심 상수
```bash
grep -n "MOTOR_MAX_PULSE\|MOTOR_IGNORE_PULSE\|MOTOR_FILTER_ALPHA\|MOTOR_FREQ" \
  mcu/extbd/Source/BSP/bsp_motor.h mcu/extbd/Source/BSP/bsp_motor.c \
  mcu/extbd/Source/APP/app_differential.h
```
```
bsp_motor.h:  MOTOR_IGNORE_PULSE  (1600)
bsp_motor.h:  MOTOR_MAX_PULSE     (3600)
bsp_motor.c:  MOTOR_FILTER_ALPHA  0.1f → 0.3f (수정 후)
app_differential.h: MOTOR_FREQ_FORWARD_FAST  (144)  // 2.0 msec
app_differential.h: MOTOR_FREQ_STOP          (108)  // 1.5 msec
app_differential.h: MOTOR_FREQ_BACKWARD_FAST (72)   // 1.0 msec
```

### 11.3 PID 상수
```bash
grep -n "PID_DEF" mcu/extbd/Source/APP/app_pid.h
```
```
PID_DEF_KP  (0.8f)
PID_DEF_KI  (0.06f)
PID_DEF_KD  (0.5f)
```

### 11.4 ROS 드라이버 스케일 팩터
```bash
grep -n "ANGULAR_SCALE_FACTOR" \
  jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```
```
26: ANGULAR_SCALE_FACTOR = 0.353
234: angular_corrected = angular * self.ANGULAR_SCALE_FACTOR
```

---

## 12. 데드존 해결방안 적용

### 12.1 방안 A — 펌웨어 소스 수정 (MCU 빌드/플래시 필요)

수정 파일: `mcu/extbd/Source/BSP/bsp_motor.c`

**[방안A-1] Motor_Ignore_Dead_Zone 비례 매핑**:
```c
// 수정 전 (3개 이산값):
if (pulse > MOTOR_FREQ_FORWARD_FAST) return MOTOR_FREQ_FORWARD_FAST;
if (pulse < MOTOR_FREQ_BACKWARD_FAST) return MOTOR_FREQ_BACKWARD_FAST;
return 0;

// 수정 후 (비례 매핑):
int16_t esc_half = MOTOR_FREQ_FORWARD_FAST - MOTOR_FREQ_STOP;  // 36
int16_t esc_value = MOTOR_FREQ_STOP +
    (int16_t)((float)pulse * esc_half / MOTOR_MAX_PULSE);
// clamp to [72, 144]
```

**[방안A-2] Motor_Set_Pwm M1/M3 ESC 직접 출력**:
```c
// M1, M3 case에서 CAR_DIFFERENTIAL일 때:
if (Motion_Get_Car_Type() == CAR_DIFFERENTIAL) {
    PWM_Mx_A = pulse;  // ESC값 직접 출력, 반전 없음
    PWM_Mx_B = 0;
}
// 기존 H-bridge 로직은 else 블록으로 이동
```

**[방안A-3] MOTOR_FILTER_ALPHA 증가**:
```c
// 수정 전:
#define MOTOR_FILTER_ALPHA  0.1f
// 수정 후:
#define MOTOR_FILTER_ALPHA  0.3f  // 90% 도달: 230ms → 77ms
```

### 12.2 방안 B — ROS 드라이버 우회 (즉시 적용)

수정 파일: `jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

```python
# cmd_vel 콜백 내, angular_corrected 계산 직후:
# ── 방안B 시작 ──
MIN_ANGULAR_DEADZONE = 0.15
if abs(angular_corrected) > 0.01 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
    angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
# ── 방안B 끝 ──
```

### 12.3 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_bringup --symlink-install
# Summary: 1 package finished [4.85s]
```

### 12.4 방안 B 제거 방법 (방안 A 적용 후)
```bash
# jupiter_driver_compensated.py에서 아래 주석 사이 코드를 삭제:
# "── 방안B 시작 ──" ~ "── 방안B 끝 ──"
grep -n "방안B" jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
# 해당 라인 범위 삭제 후 재빌드
```

---

## 13. 방안 B 테스트 (2026-03-17)

### 13.1 데드존 우회 확인 테스트 스크립트
```bash
# 테스트 스크립트 실행 (Part1: 데드존 + Part2: 회전 + Part3: Nav2 Goal)
cd /home/jetson/jupiter_ws_ros2
python3 scripts/test_deadzone_bypass.py

# Nav2 제외 회전만 테스트
python3 scripts/test_deadzone_bypass.py --skip-nav
```

### 13.2 MIN_ANGULAR_DEADZONE 값 변경 (0.15 → 0.25)
```bash
# 소스 수정 후 재빌드
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_bringup --symlink-install

# build 경로에 반영 확인
python3 -c "
f = open('build/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py')
for i, line in enumerate(f, 1):
    if 'MIN_ANGULAR' in line: print(f'{i}: {line.rstrip()}')
f.close()
"
# 241: MIN_ANGULAR_DEADZONE = 0.25

# 현재 드라이버 프로세스 시작시간 확인 (빌드 이후인지)
ps -eo pid,start,args | grep jupiter_driver_comp | grep -v grep
```

### 13.3 테스트 결과 요약

MIN_ANGULAR_DEADZONE=0.15 (4회):
```
Test 1: Goal1 SUCCEEDED 1.3s | Goal2 SUCCEEDED 18.0s | yaw_span: 77.7°
Test 2: Goal1 SUCCEEDED 4.2s | Goal2 TIMEOUT  61.1s | yaw_span: 359.9°
Test 3: Goal1 SUCCEEDED 4.2s | Goal2 SUCCEEDED 17.7s | yaw_span: 359.0°
Test 4: Goal1 SUCCEEDED 4.0s | Goal2 TIMEOUT  61.0s | yaw_span: 360.0°
→ 성공률: 2/4 (50%)
```

MIN_ANGULAR_DEADZONE=0.25 (3회):
```
Test 5: Goal1 SUCCEEDED 2.4s | Goal2 TIMEOUT  61.0s | yaw_span: 359.8°
Test 6: Goal1 SUCCEEDED 2.1s | Goal2 SUCCEEDED 11.4s | yaw_span: 358.6°
Test 7: Goal1 SUCCEEDED 4.3s | Goal2 SUCCEEDED 33.1s | yaw_span: 357.8°
→ 성공률: 2/3 (67%)
```
