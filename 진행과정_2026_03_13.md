# 진행과정 2026-03-13: 좌측 편향 원인 분석 및 EKF yaw 보정 테스트

> 이 문서는 2026-03-13 세션의 작업 진행과정을 기록합니다.
> 이전 세션(2026-03-11~12)에서 발견된 좌측 편향 현상의 근본 원인을 해결하기 위한 테스트 기록.

---

## 목차
1. [배경 및 이전 세션 요약](#1-배경-및-이전-세션-요약)
2. [방향 A: VSLAM vyaw 재활성화 테스트](#2-방향-a-vslam-vyaw-재활성화-테스트)
3. [방향 B: RF2O 비활성화 테스트](#3-방향-b-rf2o-비활성화-테스트)
4. [결과 비교 및 결론](#4-결과-비교-및-결론)

---

## 1. 배경 및 이전 세션 요약

### 1.1 발견된 문제
Nav2 2m 전진+복귀 테스트에서 **좌측 편향** 관찰:
- Goal1 도착 후 목표 방향을 보지 못하고 **왼쪽으로 회전**
- Goal2 복귀 시 **왼쪽으로 치우친** 전진 → 벽 근접 → 경로 재설정

### 1.2 확인된 근본 원인 (Section 20-23, 2026-03-12)

| 계층 | 원인 | 심각도 |
|------|------|--------|
| **EKF yaw** | wheel+IMU만으로 yaw 추정 → drift 발생 | 핵심 |
| **RF2O** | differential x,y에 left yaw bias (+2.4°/m) 포함 | 보조 |
| **VSLAM** | yaw 비활성으로 EKF yaw 보정 불가 | 구조적 |
| **DWB** | 왜곡된 heading 기반으로 과도 보정 | 결과적 |

### 1.3 순수 직진 테스트 결과 (VSLAM OFF, 2026-03-12)
| 센서 | Forward | Lateral | Yaw 변화 |
|------|---------|---------|----------|
| Wheel | 0.700m | -0.001m (≈0) | +0.12° |
| EKF | 0.697m | -0.001m (≈0) | +0.12° |
| RF2O | 0.697m | **+0.021m LEFT** | **+1.70° LEFT** |

→ 물리적 로봇은 직진 정상, RF2O만 LEFT drift

### 1.4 VSLAM ON 테스트 결과 (2026-03-12)
- VSLAM ON으로도 좌측 편향 미해소 (EKF yaw drift +85.66°)
- 원인: `odom1_config`에서 yaw/vyaw 모두 false → VSLAM이 yaw 보정 불가

### 1.5 현재 EKF 설정
```yaml
# ekf_vslam_fusion.yaml
odom1_config: [true,  true,  false,    # x, y, z
               false, false, false,    # roll, pitch, yaw ← false
               true,  true,  false,    # vx, vy, vz
               false, false, false,    # vroll, vpitch, vyaw ← false!
               false, false, false]    # ax, ay, az
```

### 1.6 이번 세션 목표
두 가지 방향을 비교 테스트:
- **방향 A**: VSLAM vyaw 재활성화 (`odom1_config`에서 vyaw=true)
- **방향 B**: RF2O 비활성화 (odom2 제거)

---

## 2. 방향 A: VSLAM vyaw 재활성화 테스트

### 2.1 설정 변경
```yaml
# ekf_vslam_fusion.yaml 수정 내용
odom1_config: [..., false, false, true, ...]  # vyaw: false → true
odom1_twist_rejection_threshold: 2.0          # 3.0 → 2.0
```

### 2.2 검증
- `ros2 param get /ekf_filter_node odom1_config` → vyaw(인덱스 11) = **True** 확인
- VSLAM 메시지 수신 확인 (`/visual_slam/tracking/odometry`)

### 2.3 테스트 실행 결과: ❌ 실패 (EKF 발산)

**Nav2 2m 전진+복귀 테스트**:
```
Start: (0.358, 0.145, yaw=-0.5°)
Goal1: 2m forward → (2.358, 0.129)
결과: TIMEOUT (60.0s) — 목표 미도달
```

**EKF 상태 확인**:
```
현재 EKF 위치: (1.165, -14.357)  ← y축으로 -14.5m 발산!
실제 이동 거리: ~2m (정상)
```

### 2.4 실패 원인 분석

| 요인 | 상세 |
|------|------|
| **VSLAM adapted 희소** | raw 171msg vs adapted **3msg** (3초간) — 98% 여과됨 |
| **VSLAM twist.angular.z 오프셋** | -0.04 rad/s 상시 존재 (정지 중에도) |
| **Mahalanobis 통과** | cov=0.005 → MD=0.04/√0.005=0.57 < 2.0 → 거부 못함 |
| **differential 대형 점프** | adapted 1Hz 수준 → 프레임간 차분이 과도하게 큼 |
| **결과** | EKF가 VSLAM vyaw를 신뢰하여 y축 -14.5m 발산 |

### 2.5 결론
> **방향 A 불가**: VSLAM vyaw를 직접 활성화하면 covariance adapter의 희소 출력과
> VSLAM twist의 상시 오프셋으로 인해 EKF가 발산한다.
> 과거(2026-02-27) vyaw를 비활성화한 결정이 정당했음을 재확인.

### 2.6 설정 복원
```yaml
# 원복
odom1_config: [..., false, false, false, ...]  # vyaw: true → false
odom1_twist_rejection_threshold: 3.0           # 2.0 → 3.0 원복
```

---

## 3. 방향 B: RF2O 비활성화 테스트

### 3.1 변경 근거
- RF2O의 differential x,y에 +2.4°/m left yaw drift가 내재
- EKF가 이 편향된 변위를 수용하여 좌측 편향 유발
- RF2O를 제거하면 EKF는 wheel + VSLAM(x,y) + IMU(vyaw)만으로 추정
- VSLAM이 절대 위치(x,y) 보정을 제공하므로 RF2O 없이도 drift 억제 가능

### 3.2 설정 변경
```yaml
# ekf_vslam_fusion.yaml - odom2 완전 비활성화
odom2_config: [false, false, false,    # x, y, z ← 비활성화
               false, false, false,    # roll, pitch, yaw
               false, false, false,    # vx, vy, vz
               false, false, false,    # vroll, vpitch, vyaw
               false, false, false]    # ax, ay, az
```

### 3.3 예상 효과
- RF2O left yaw drift 제거 → 좌측 편향 감소 예상
- EKF 소스: wheel(vx,vy,vyaw) + VSLAM(x,y,vx,vy) + IMU(vyaw)
- 리스크: VSLAM tracking lost 시 wheel+IMU만으로 drift 억제 필요

### 3.4 테스트 결과: △ 부분 성공

**Nav2 2m 전진+복귀 테스트**:
```
Start: (-0.010, -0.005, yaw=3.0°)
Goal1: SUCCEEDED (6.0s)   ← 매우 빠름! (기존 9.7~11.7s 대비)
Goal2: SUCCEEDED (55.9s)  ← 매우 느림 (180° 회전 중 EKF yaw 발산)
End:   (-0.737, -0.192, yaw=0.5°)
Travel: 4.715m  Disp: 0.751m (시작점 대비)
```

### 3.5 세부 분석

#### cmd_vel (전진 구간)
```
wz mean: -23.38 mrad/s (RIGHT bias!) ← 기존 LEFT → RIGHT로 반전
L(+)=91, R(-)=61, neutral=101
```
- 전진 초기 0-6s: wz 0-246 mrad/s (점진 좌회전 → Goal1 도달)
- 복귀 중 11-18s: wz ±700 mrad/s (180° 회전 시 큰 진동)

#### EKF yaw Timeline (핵심)
```
t=0-6s:   yaw 3° → 3.4°   (전진 중 안정 ✓)
t=6-12s:  yaw 3° → 96°    (180° 회전 시작 → yaw 폭주!)
t=12-16s: yaw 96° → -155°  (overshoot, wrap-around)
t=16-24s: yaw -16° → 24°   (불안정 수렴)
t=24-56s: yaw ~21-24°       (정지/저속 중 drift 잔류)
t=56-68s: yaw 24° → 0°     (재전진으로 Goal2 완료)
```

### 3.6 결과 해석

| 측면 | 평가 | 상세 |
|------|------|------|
| **전진 직진성** | ✅ 크게 개선 | Goal1 6.0s, yaw drift 안정적 |
| **좌측 편향** | ✅ 해소 | wz=-23.38 (RIGHT) → 좌측 편향 제거됨 |
| **180° 회전** | ❌ EKF yaw 발산 | 96° → -155° 폭주 (RF2O 없이 회전 중 위치 보정 부족) |
| **복귀 시간** | ❌ 55.9초 | RF2O ON 시 ~12초 대비 대폭 증가 |

### 3.7 문제 원인
- RF2O 제거 시 **회전 중** EKF에 위치 보정이 부족
- wheel odom의 회전 적분 오차가 VSLAM이 보정하기 전에 누적
- 특히 180° 대회전 시 VSLAM의 x,y만으로는 heading 보정 불가
- 결과: DWB가 잘못된 heading으로 과잉 보정 → 55.9s 소요

---

## 4. 결과 비교 및 결론

### 4.1 3-way 비교

| 지표 | VSLAM OFF | ON (vyaw=F, RF2O ON) | ON (RF2O OFF) |
|------|-----------|---------------------|---------------|
| **cmd_vel wz (mrad/s)** | +32.82 (L) | -8.81 (R) | -23.38 (R) |
| **EKF yaw drift (deg)** | +3.98 | +85.66 | -2.67 |
| **EKF lat max (m)** | 0.160 | 0.351 | 0.338 |
| **Goal1 시간** | ~10s | ~10s | **6.0s** |
| **Goal2 시간** | ~12s | ~12s | **55.9s** |
| **RF2O yaw drift** | +6.03° | +49.51° | +3.98° (미사용) |
| **VSLAM msgs** | 0 | 1232 | 3907 |

### 4.2 핵심 발견

1. **좌측 편향의 주범은 RF2O**: RF2O 제거 시 좌측 편향이 완전히 사라짐
2. **RF2O 제거의 부작용**: 180° 회전 시 EKF yaw가 발산하여 복귀 55.9s
3. **방향 A(vyaw 재활성화)는 불가**: EKF 발산 (-14.5m)
4. **정방향 직진은 RF2O OFF이 최선**: Goal1 6.0s (최고 성능)

### 4.3 최적 방안 제안: 방향 C — RF2O 부분 비활성화

방향 A도 B도 완벽하지 않은 현 상황에서, 절충안이 필요:

**옵션 C1: RF2O differential 가중치 감소**
- odom2_config의 x,y를 유지하되, EKF에서 RF2O 공분산을 크게 증가
- RF2O의 영향력을 줄이면서도 회전 중 최소한의 위치 보정 유지

**옵션 C2: 현재 RF2O OFF 유지 + Nav2 회전 파라미터 튜닝**
- RF2O OFF를 유지하고, 180° 회전 시 DWB/RotationShim 파라미터 최적화
- 직진 성능은 이미 검증됨, 회전만 개선하면 됨

**옵션 C3: RF2O 노드에서 yaw bias 보정**
- rf2o_covariance_adapter에 yaw bias 보정 로직 추가
- 근본적 해결이나 RF2O 내부 수정 필요

---

## 5. 방향 C1: RF2O 공분산 5배 증가 (min_position_cov 0.1→0.5)

### 5.1 설정 변경
```yaml
# ekf_vslam_fusion.yaml
odom2_config: [true,  true,  false, ...]  # x,y 재활성화

# nav2_vslam_fused.launch.py
min_position_cov: 0.5   # 0.1 → 0.5 m² (5배 증가)
```

### 5.2 테스트 결과: ✅ 최선의 균형

```
Goal1: SUCCEEDED (6.3s)  ← 전진 최고 성능 유지
Goal2: SUCCEEDED (11.8s) ← 복귀 정상! (B의 55.9s 대비 대폭 개선)
```

### 5.3 전진 구간 분석
```
0.5m 지점: lat=-0.003m, yaw=-0.54°  ← 거의 완벽 직진
1.0m 지점: lat=+0.015m, yaw=+1.42°  ← 미세 좌편향 (기존 대비 대폭 감소)
```

### 5.4 180° 회전 분석
```
t=8-10s:  yaw 17° (회전 시작)
t=10-12s: yaw 91° (진행 중)
t=12-14s: yaw 137° (overshoot)
t=16-18s: yaw -81° (반대로 보정)
t=20-24s: yaw ~39° (수렴)
```
- 여전히 회전 중 EKF yaw 진동 있으나 costmap을 벗어나지 않음
- backup 미발생, 정상 경로 계획 → 11.8s 복귀

### 5.5 4-way 비교

| 지표 | VSLAM OFF | ON(RF2O ON) | RF2O OFF | **cov 0.5** |
|------|-----------|-------------|----------|-------------|
| wz mean (mrad/s) | +32.8 (L) | -8.8 (R) | -23.4 (R) | **-86.0 (R)** |
| EKF yaw drift | +3.98° | +85.66° | -2.67° | **+48.38°** |
| EKF lat max | 0.160m | 0.351m | 0.338m | **0.281m** |
| 0.5m 지점 lat | - | - | +0.187m | **-0.003m** |
| 1.0m 지점 lat | - | - | +0.083m | **+0.015m** |
| Goal1 시간 | ~10s | ~10s | 6.0s | **6.3s** |
| Goal2 시간 | ~12s | ~12s | 55.9s | **11.8s** |
| backup 발생 | N | N | Y | **N** |

### 5.6 결론
> **Direction C (cov 0.5)가 현재 최적 설정**:
> - 전진 직진성: 0.5m에서 lat 3mm (최고)
> - Goal1: 6.3s (최빠름)
> - Goal2: 11.8s (정상)
> - backup 미발생
>
> 잔여 과제: 180° 회전 중 EKF yaw 진동 (+48°), wz=-86mrad/s (RIGHT)
> → 추가 개선 시 cov를 1.0으로 더 올리거나, DWB 회전 파라미터 최적화 가능

