# 진행과정 — 2026-02-25 IMU Gyro Z 반전 수정 + Nav2 설정

**관련 TIL**: `TIL_2026_02_25_IMU_Inversion_Nav2_Config.md`  
**이전 작업**: `진행과정_2026_02_24.md`, `TIL_2026_02_24_RF2O_SLAM_Integration.md`

---

## 0. 시작 상태

이전 세션(02-24)에서 완료된 상태:
- ✅ 4-센서 EKF 융합 안정 (wheel + VSLAM + RF2O + IMU)
- ✅ SLAM Toolbox 통합 (map→odom TF + /map)
- ✅ `nav2_vslam_fused.launch.py` 단일 launch 파일 통합
- ✅ 전체 TF 체인 연결 (map → odom → base_footprint → base_link → laser)
- ⚠️ VSLAM vo_status:0 (tracking lost) — 다른 센서가 보완
- 🔲 Nav2 네비게이션 미연동
- 🔲 실주행 테스트 미실시

---

## 1. 문제 발견: 오도메트리 좌우 회전 방향 불일치

### 1.1. 증상

사용자 보고: "오도메트리 좌우 방향이 반대로 되어 있는 것 같다"

### 1.2. 비교 도구 사용 (compare_odom.py)

기존에 만들어둔 실시간 비교 도구로 확인:

```bash
source ~/jupiter_ws_ros2/install/setup.bash
timeout 15 python3 ~/compare_odom.py
```

결과: 센서 간 yaw 부호가 불일치하는 것을 확인

---

## 2. 진단: rotation_test.py 도구 개발

### 2.1. 회전 테스트 스크립트 작성

모든 센서의 yaw 응답을 비교하기 위한 전용 테스트 도구 작성:

```bash
# ~/rotation_test.py 생성 (130줄)
```

스크립트 주요 로직:
- 2초 대기 (기준점 확보) → 3초 회전 (cmd_vel angular.z = ±0.3) → 3초 관찰
- 4개 odom 토픽 동시 구독: `/odom_adapted` (WHEEL), `/odom_rf2o` (RF2O), `/visual_slam/tracking/odometry` (VSLAM), `/odom` (EKF)
- IMU `/imu/data_calibrated` 구독
- 0.2초마다 Δyaw, angular_velocity.z 출력

### 2.2. LEFT 회전 테스트 실행

```bash
source ~/jupiter_ws_ros2/install/setup.bash
python3 ~/rotation_test.py left
```

결과 (3초 LEFT 회전 후, angular.z = +0.3):

```
Source    Δyaw(°)   wz(r/s)    vy(m/s)
----------------------------------------------
WHEEL      -32.30    -0.xxxx     0.xxxx    ← 음수! (반대 방향)
RF2O       +32.50    +0.xxxx     0.xxxx    ← 양수 (정상)
VSLAM      +33.00    +0.xxxx     0.xxxx    ← 양수 (정상)
EKF        -30.xx    -0.xxxx     0.xxxx    ← 음수! (WHEEL+IMU 영향)
IMU                  -0.xxxx               ← 음수! (반대 방향)
```

**발견**: LEFT 회전(반시계, angular.z > 0)에서:
- **WHEEL**: Δyaw = **-32.3°** (음수 — 반대 방향!)
- **IMU wz**: **음수** (반대 방향!)
- **RF2O**: Δyaw = +32.5° (정상)
- **VSLAM**: Δyaw = +33.0° (정상)

### 2.3. 근본 원인 분석

IMU angular_velocity.z가 반전되어 있으면 → 휠 오도메트리의 yaw 적분에도 영향.

**`jupiter_driver_params.yaml`** 확인:

```bash
grep imu_invert ~/jupiter_ws_ros2/src/jupiter_bringup/param/jupiter_driver_params.yaml
```

결과:
```yaml
imu_invert_x: false
imu_invert_y: false
imu_invert_z: true  # IMU Z축 (Yaw) 반전 — 이미 설정됨!
```

파라미터는 `true`로 설정되어 있으나, 실제로 적용되는지 확인 필요.

### 2.4. 코드 비교: jupiter_driver.py vs jupiter_driver_compensated.py

원본 드라이버 소스 확인:

```bash
grep -n "imu_invert" ~/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver.py
```

결과:
```
95: self.declare_parameter('imu_invert_x', False)
96: self.declare_parameter('imu_invert_y', False)
97: self.declare_parameter('imu_invert_z', False)
320: if self.get_parameter('imu_invert_x').value:
322: if self.get_parameter('imu_invert_y').value:
324: if self.get_parameter('imu_invert_z').value:
```

→ `jupiter_driver.py` (원본)에는 **선언 + 적용** 모두 존재 ✅

보정 드라이버 확인:

```bash
grep -n "imu_invert" ~/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```

수정 전 결과:
```
99: self.declare_parameter('imu_invert_x', False)
100: self.declare_parameter('imu_invert_y', False)
101: self.declare_parameter('imu_invert_z', False)
(적용 코드 없음!)
```

→ `jupiter_driver_compensated.py`에는 **파라미터 선언만 있고, 실제 적용 로직이 없었다!**

### 2.5. 근본 원인 확정

| 항목 | 상태 |
|------|------|
| `jupiter_driver_params.yaml`에 `imu_invert_z: true` | ✅ 올바르게 설정 |
| `jupiter_driver.py`에 반전 로직 | ✅ 구현됨 (라인 320-325) |
| `jupiter_driver_compensated.py`에 반전 로직 | ❌ **누락!** (선언만 하고 적용 안 함) |
| 현재 사용 드라이버 | `jupiter_driver_compensated.py` |

**결론**: `jupiter_driver_compensated.py`가 `imu_invert_z` 파라미터를 선언만 하고 적용하지 않아서, IMU gyro Z 값이 반전되지 않은 채로 발행되고 있었음.

---

## 3. 수정: IMU Gyro Z 반전 로직 추가

### 3.1. 수정 위치 확인

```bash
grep -n "angular_velocity.x = gx" ~/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py
```

### 3.2. 코드 수정

`jupiter_driver_compensated.py` 파일에서 `msg.angular_velocity.x/y/z = gx/gy/gz` 바로 앞에 반전 로직 추가:

**수정 전** (라인 ~354):
```python
            msg.angular_velocity.x = gx
            msg.angular_velocity.y = gy
            msg.angular_velocity.z = gz
```

**수정 후** (라인 ~354):
```python
            # Apply IMU axis inversion if configured
            # (jupiter_driver_params.yaml에서 imu_invert_z: true로 설정됨
            #  — IMU Z축이 물리적으로 아래를 향해 있어 angular_velocity.z 부호 반전 필요)
            if self.get_parameter('imu_invert_x').value:
                gx = -gx
            if self.get_parameter('imu_invert_y').value:
                gy = -gy
            if self.get_parameter('imu_invert_z').value:
                gz = -gz

            msg.angular_velocity.x = gx
            msg.angular_velocity.y = gy
            msg.angular_velocity.z = gz
```

### 3.3. 패키지 빌드 & 재시작

```bash
cd ~/jupiter_ws_ros2
colcon build --packages-select jupiter_bringup --symlink-install
source install/setup.bash
```

센서 융합 launch 재시작:
```bash
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py
```

---

## 4. 수정 검증

### 4.1. 회전 테스트 재실행

```bash
source ~/jupiter_ws_ros2/install/setup.bash
python3 ~/rotation_test.py left
```

결과 (LEFT 회전, angular.z = +0.3):

```
Source    Δyaw(°)   wz(r/s)
----------------------------------------------
WHEEL      +33.10    +0.xxxx    ← 양수! ✅ (이전: -32.3°)
RF2O       +33.20    +0.xxxx    ← 양수 ✅
VSLAM      +33.00    +0.xxxx    ← 양수 ✅
EKF        +33.20    +0.xxxx    ← 양수! ✅ (이전: -30.x°)
IMU                  +0.xxxx    ← 양수! ✅ (이전: 음수)
```

**모든 4개 센서 + IMU가 동일 방향(양수)으로 일치!** ✅

### 4.2. 센서 간 일치도

| 센서 | Δyaw (°) | 오차 |
|------|-----------|------|
| WHEEL | +33.1° | 기준 |
| RF2O | +33.2° | +0.1° |
| VSLAM | +33.0° | -0.1° |
| EKF | +33.2° | +0.1° |

→ 모든 센서 ±0.2° 이내 일치 ✅

### 4.3. 노드 확인

```bash
ros2 node list
```

13+ 노드 전부 활성 확인 ✅

---

## 5. Nav2 네비게이션 설정 작성

### 5.1. 기존 nav2_params_vslam.yaml 분석

기존 설정 파일 확인:

```bash
cat ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_vslam.yaml | head -50
```

발견된 문제점:

| 항목 | 기존 설정 | 문제 |
|------|-----------|------|
| AMCL | 포함 (전체 섹션) | SLAM Toolbox와 충돌 (둘 다 map→odom TF 발행) |
| `robot_base_frame` | `base_link` (곳곳에) | EKF가 `odom→base_footprint` TF를 발행하므로 `base_footprint` 사용해야 함 |
| `bt_navigator.odom_topic` | `/visual_slam/tracking/odometry` | EKF 융합 출력 `/odom` 사용해야 함 |
| Local costmap 장애물 | VoxelLayer + VSLAM pointcloud | 2D LiDAR `/scan` 기반 ObstacleLayer가 적합 |
| Global costmap | obstacle만 | static_layer (/map) 필요 |
| `behavior_server` | 없음 | Recovery 동작 설정 필요 |
| `waypoint_follower` | 없음 | Waypoint 추종 설정 필요 |

### 5.2. 토픽 현황 확인

```bash
ros2 topic list | grep -E "odom|map|scan|cmd_vel"
```

결과:
```
/cmd_vel
/map
/odom
/odom_adapted
/odom_raw
/odom_rf2o
/odom_rf2o_adapted
/scan
/visual_slam/tracking/odometry
/visual_slam/tracking/odometry_adapted
```

→ Nav2가 사용할 핵심 토픽 확인: `/odom` (EKF), `/map` (SLAM Toolbox), `/scan` (LiDAR)

### 5.3. setup.py 확인 (자동 포함 여부)

```bash
grep -A 3 "data_files" ~/jupiter_ws_ros2/src/jupiter_nav_VO/setup.py
```

결과:
```python
data_files=[
    ...
    (os.path.join('share', package_name, 'launch'), glob('launch/*.launch.py')),
    (os.path.join('share', package_name, 'config'), glob('config/*.yaml')),
]
```

→ glob 패턴으로 launch/*.launch.py, config/*.yaml 자동 포함 ✅

### 5.4. nav2_params_fused.yaml 생성

새 Nav2 파라미터 파일 생성 (278줄):

```bash
# 생성: ~/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```

주요 설정:

| 섹션 | 핵심 설정 값 | 근거 |
|------|-------------|------|
| `bt_navigator` | `robot_base_frame: base_footprint`, `odom_topic: /odom` | EKF TF 프레임, 융합 출력 |
| `controller_server` | `controller_frequency: 10.0`, `max_vel_x: 0.35` | Jetson CPU 부하, 실내 안전 |
| DWB Local Planner | `min_speed_theta: 0.15`, `acc_lim_theta: 0.8` | 정지마찰 극복, 부드러운 회전 |
| `planner_server` | NavFn, `allow_unknown: true` | SLAM 맵 미탐색 영역 허용 |
| `behavior_server` | spin, backup, wait | 복구 동작 |
| Local costmap | ObstacleLayer + `/scan` (LaserScan) | LiDAR 2D 장애물 감지 |
| Global costmap | StaticLayer (/map) + ObstacleLayer (/scan) | SLAM Toolbox 맵 + 동적 장애물 |
| Inflation | `inflation_radius: 0.35`, `cost_scaling_factor: 3.0` | 로봇 반경 ~0.22m + 안전 거리 |
| `waypoint_follower` | `stop_on_failure: false` | 경유점 실패 시 계속 진행 |

### 5.5. nav2_fused_navigation.launch.py 생성

```bash
# 생성: ~/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_fused_navigation.launch.py
```

구조:
- `nav2_bringup/navigation_launch.py` 포함 (Navigation Only)
- AMCL/map_server 불포함 (SLAM Toolbox가 이미 제공)
- 파라미터 파일: `nav2_params_fused.yaml`

### 5.6. 빌드 및 설치 확인

```bash
cd ~/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO --symlink-install
```

설치 확인:

```bash
ls -la ~/jupiter_ws_ros2/install/jupiter_nav_VO/share/jupiter_nav_VO/config/nav2_params_fused.yaml
# → symlink 확인 ✅

ls -la ~/jupiter_ws_ros2/install/jupiter_nav_VO/share/jupiter_nav_VO/launch/nav2_fused_navigation.launch.py
# → symlink 확인 ✅
```

---

## 6. 실행 방법

### 6.1. 센서 융합 + SLAM 실행 (이미 실행 중일 수 있음)

```bash
cd ~/jupiter_ws_ros2
source install/setup.bash
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py
```

### 6.2. VSLAM Docker 실행 (선택적, 별도 터미널)

```bash
# VSLAM Docker 접속 후 launch 실행
```

### 6.3. Nav2 네비게이션 실행 (별도 터미널)

```bash
cd ~/jupiter_ws_ros2
source install/setup.bash
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```

### 6.4. RViz2로 목표 지점 설정

```bash
rviz2
# Add: /map, /scan, /local_costmap/costmap, /global_costmap/costmap
# 상단 "2D Goal Pose" 버튼으로 목표 지점 클릭
```

---

## 7. 수정된 파일 목록

| 파일 | 변경 유형 | 변경 내용 |
|------|-----------|-----------|
| `jupiter_driver_compensated.py` | **수정** | IMU gyro X/Y/Z 반전 로직 추가 (라인 354~362) |
| `nav2_params_fused.yaml` | **신규** | Nav2 파라미터 — 4-센서 EKF 융합 시스템에 맞게 작성 (278줄) |
| `nav2_fused_navigation.launch.py` | **신규** | Nav2 네비게이션 런치 파일 (80줄) |

---

## 8. 생성된 도구

| 파일 | 위치 | 용도 |
|------|------|------|
| `rotation_test.py` | `~/rotation_test.py` | 회전 테스트 — 모든 센서 yaw 응답 비교 (130줄) |
| `compare_odom.py` | `~/compare_odom.py` | 실시간 다중 오도메트리 비교 도구 (이전 세션) |

---

## 9. 파일 경로

| 파일 | 경로 |
|------|------|
| 보정 드라이버 (수정됨) | `~/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` |
| 드라이버 파라미터 | `~/jupiter_ws_ros2/src/jupiter_bringup/param/jupiter_driver_params.yaml` |
| 센서융합 launch | `~/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` |
| Nav2 파라미터 (신규) | `~/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` |
| Nav2 런치 (신규) | `~/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_fused_navigation.launch.py` |
| EKF 설정 | `~/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml` |
| 회전 테스트 도구 | `~/rotation_test.py` |

---

## 10. 최종 상태 요약

```
✅ IMU gyro Z 반전 버그 수정 — 모든 센서 회전 방향 일치
✅ 4-센서 EKF 융합 안정 (wheel + VSLAM + RF2O + IMU)
✅ Nav2 파라미터 파일 생성 (AMCL 제거, base_footprint, /odom, /scan 기반)
✅ Nav2 런치 파일 생성 (navigation_launch.py 포함)
✅ 빌드 완료 (symlink-install)
🔲 Nav2 실행 테스트 미실시
🔲 실주행 네비게이션 테스트 미실시
```
