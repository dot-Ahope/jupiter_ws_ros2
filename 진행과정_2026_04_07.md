# 진행과정 2026-04-07: RTK NavSatStatus 수정 + GNSS 파이프라인 간소화 + IMU 기반 휠 슬립 감지

## 1. 목표

### 1.1 사용자 요청
> 1. "md파일에 대한 정리는 4월 7일자로 만들어라"
> 2. RTK NavSatStatus 매핑 수정 + 파이프라인 복잡도 분석
> 3. "휠 슬립 문제에 대해 추천 방안을 수행하라. 다만 공분산의 경우 동적 공분산으로 수행되도록 해라"

### 1.2 이전 세션 요약 (2026-04-06 Part 7까지)
- Part 1~5: VSLAM adapter emitter_on_off 수정, CSV 분석, TF 수정, RF2O 분석
- Part 6: 패키지 상호작용 분석 → 4건 권장 수정 적용 (jitter, nvblox height, delta cap, gyro_cov)
- Part 7: RTK GPS 융합 아키텍처 구축 (zed_f9p_rtk_node → gnss_quality_filter → navsat_transform → EKF odom3), 휠 슬립 분석 4가지 방안 제시

---

## 2. RTK NavSatStatus 매핑 버그 수정

### 2.1 문제: 잘못된 NavSatStatus 매핑

**파일**: `src/zed_f9p_rtk/zed_f9p_rtk/zed_f9p_rtk_node.py`

기존 코드의 `process_gga()`:
```python
# 버그: qual=4, qual=5 모두 STATUS_GBAS_FIX(2)로 매핑
if qual > 0:
    fix.status.status = STATUS_FIX if qual == 1 else STATUS_GBAS_FIX
```

**문제점**:
| GGA qual | 의미 | 기존 매핑 | 기존 cov |
|:---:|:---:|:---:|:---:|
| 0 | No Fix | STATUS_NO_FIX (-1) | N/A |
| 1 | GPS Fix | STATUS_FIX (0) | 5.0 |
| 4 | RTK Fixed | STATUS_GBAS_FIX (2) ✓ | 0.01 ✗ |
| 5 | RTK Float | STATUS_GBAS_FIX (2) ✗ | 0.1 |

- qual=5 (RTK Float)가 STATUS_GBAS_FIX(2)로 잘못 매핑됨 → STATUS_SBAS_FIX(1)이어야 함
- RTK Fixed 공분산 0.01은 실제 정밀도(1~3cm, σ²=0.0004)와 차이가 큼
- 3D Fix 공분산 5.0은 실제 정밀도(~2.5m, σ²=6.25)보다 과신

### 2.2 수정: 올바른 NavSatStatus 매핑

ublox ZED-F9P 기준 올바른 매핑:

| GGA qual | 의미 | NavSatStatus | 값 | 공분산 σ² | 정밀도 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 0 | No Fix | STATUS_NO_FIX | -1 | 9999.0 | 사용 불가 |
| 1,2 | 3D Fix | STATUS_FIX | 0 | 6.25 | ~2.5m |
| 5 | RTK Float | STATUS_SBAS_FIX | 1 | 0.25 | 20cm~1m |
| 4 | RTK Fixed | STATUS_GBAS_FIX | 2 | 0.0004 | 1~3cm |

수정된 코드:
```python
if qual == 4:
    fix.status.status = NavSatStatus.STATUS_GBAS_FIX   # RTK Fixed
elif qual == 5:
    fix.status.status = NavSatStatus.STATUS_SBAS_FIX   # RTK Float
elif qual > 0:
    fix.status.status = NavSatStatus.STATUS_FIX         # 3D Fix
else:
    fix.status.status = NavSatStatus.STATUS_NO_FIX
```

고도 공분산은 수평 공분산의 4배(RTK) 또는 2배(GPS)로 설정 (수직 정밀도가 수평보다 낮음).

---

## 3. GNSS 파이프라인 복잡도 분석 및 간소화

### 3.1 기존 파이프라인 분석

```
ZED-F9P → gnss_quality_filter → navsat_transform → EKF odom3
```

**기존 gnss_quality_filter의 문제**:
1. **공분산 기반 RTK Fixed/Float 구분** — `raw_cov ≤ 0.05`로 구분 → 취약하고 불안정
2. **공분산 override** — 소스 노드가 설정한 공분산을 무시하고 하드코딩 값으로 대체
3. **중복 로직** — zed_f9p_rtk_node가 이미 설정한 공분산을 다시 판정+재설정

**과도한가?**:
- gnss_quality_filter 자체는 **필요함** — navsat_transform은 STATUS_NO_FIX를 게이트하지 않음
- 하지만 **공분산 override 로직은 불필요** — zed_f9p_rtk_node가 올바른 공분산을 설정하면 됨

### 3.2 간소화 결과

gnss_quality_filter를 **단순 게이트**로 축소:
- STATUS_NO_FIX → 게이트아웃
- `position_covariance > 50.0` → 게이트아웃 (POOR)
- 나머지 → **원본 메시지 그대로 통과** (공분산 override 제거)

제거된 파라미터:
- ~~`rtk_fixed_cov_override`~~
- ~~`rtk_float_cov_override`~~  
- ~~`gps_cov_override`~~

NavSatStatus에서 직접 품질 문자열로 변환 (공분산 기반 추정 불필요):
```python
_STATUS_NAME = {
    STATUS_NO_FIX: 'NO_FIX',
    STATUS_FIX: 'GPS',
    STATUS_SBAS_FIX: 'RTK_FLOAT',
    STATUS_GBAS_FIX: 'RTK_FIXED',
}
```

### 3.3 간소화 전/후 비교

| 항목 | 간소화 전 | 간소화 후 |
|:---|:---|:---|
| RTK Fixed/Float 구분 | 공분산 기반 (≤0.05) | NavSatStatus 기반 (정확) |
| 공분산 설정 | filter가 override | zed_f9p 원본 통과 |
| 파라미터 수 | 6개 | 3개 |
| 코드 복잡도 | ~120줄 | ~85줄 |
| 의존성 | zed_f9p 공분산 값에 의존 | NavSatStatus만 의존 |
| 메시지 복사 | 전체 필드 복사+재구성 | `publish(msg)` 원본 통과 |

---

## 4. IMU 기반 휠 슬립 감지 (Method A) 구현

### 4.1 설계 원리

**핵심 아이디어**: 엔코더와 IMU의 가속도 비교

| 상황 | 엔코더 가속도 | IMU 가속도 | 불일치 |
|:---|:---|:---|:---|
| 정상 주행 | 일치 | 일치 | 낮음 |
| 휠 스핀 (공전) | 높음 (바퀴 회전) | 낮음 (차체 정지) | **높음** |
| 휠 록 (미끄러짐) | 낮음 (바퀴 정지) | 높음 (차체 관성) | **높음** |

**알고리즘**:
1. 엔코더 가속도: `encoder_ax = (vx_now - vx_prev) / dt`
2. IMU 가속도: `imu_ax = linear_acceleration.x` (실시간)
3. 바이어스 보정: `corrected_mismatch = |encoder_ax - imu_ax - bias|`
4. 적응형 바이어스: 슬립 아닌 상태에서만 EMA 업데이트
5. 슬립 판정: `corrected_mismatch > threshold`이 N회 연속
6. 동적 배율: `multiplier = 1 + (mismatch / threshold) * scale`

### 4.2 적응형 바이어스 추정

IMU ax에는 ~0.11 m/s² 바이어스가 존재.
정적 보정이 아닌 **적응형 EMA**로 자동 추적:

```python
# 슬립이 아닌 상태에서만 바이어스 업데이트
if corrected_mismatch < threshold:
    bias = 0.995 * bias + 0.005 * (encoder_ax - imu_ax)
```

α=0.005로 느리게 수렴 → 급격한 변화에 안정적, 장시간 드리프트에 적응.

### 4.3 동적 공분산 전략

기존의 속도/회전/가감속 기반 동적 공분산에 **슬립 배율을 곱셈 적용**:

```
최종 공분산 = (속도 기반 공분산) × slip_cov_multiplier
```

| 상태 | slip_cov_multiplier | 의미 |
|:---|:---|:---|
| 정상 | 1.0 | 기존 동적 공분산 그대로 |
| 경미한 슬립 | 5~15x | EKF가 휠 오도메트리 가중치 감소 |
| 심한 슬립 | 15~50x | EKF가 휠 오도메트리 거의 무시 |

**감쇠 동작**:
- 슬립 시: 즉시 최대 배율 적용 (빠른 반응)
- 슬립 해제: `multiplier *= 0.92` (점진적 복귀, ~30 사이클에서 50x → 1.0)

### 4.4 파라미터 목록

| 파라미터 | 기본값 | 단위 | 설명 |
|:---|:---:|:---:|:---|
| `slip_mismatch_threshold` | 0.5 | m/s² | 슬립 판정 불일치 임계값 |
| `slip_max_multiplier` | 50.0 | 배 | 최대 공분산 배율 |
| `slip_scale` | 10.0 | - | 불일치→배율 변환 계수 |
| `slip_decay` | 0.92 | - | 슬립 해제 시 배율 감쇠 |
| `slip_detect_count` | 3 | 회 | 연속 N회 불일치 시 슬립 확정 |
| `slip_bias_alpha` | 0.005 | - | 바이어스 EMA 학습률 |
| `imu_topic` | `/imu/data_calibrated` | - | IMU 토픽 |

### 4.5 로그 출력

슬립 감지/해제 시 로그 발생 (상태 변경 시에만):
```
[WARN] SLIP DETECTED: mismatch=1.23 m/s² (enc_ax=0.52, imu_ax=-0.42, bias=0.082), cov_mult=34.6x
[INFO] SLIP CLEARED: cov_mult=1.08x, bias=0.085
```

---

## 5. EKF 관점에서의 슬립 감지 효과

### 5.1 슬립 감지 없는 경우
```
EKF 입력:
  odom0 (wheel):  vx=0.5 m/s, cov=0.006  ← 신뢰 (실제로는 슬립)
  odom1 (VSLAM):  vx=0.1 m/s, cov=0.01   ← 신뢰
  imu0:           ax≈0 m/s²               ← 차체 정지

→ EKF: 3개 소스 가중 평균 → 잘못된 위치 추정 (wheel에 과도한 가중치)
```

### 5.2 슬립 감지 있는 경우
```
EKF 입력:
  odom0 (wheel):  vx=0.5 m/s, cov=0.30 (×50)  ← 불신뢰
  odom1 (VSLAM):  vx=0.1 m/s, cov=0.01         ← 신뢰
  imu0:           ax≈0 m/s²                      ← 차체 정지

→ EKF: VSLAM + IMU 가중치 증가, wheel 무시 → 정확한 위치 추정
```

---

## 6. 수정된 파일 목록

| 파일 | 변경 내용 |
|:---|:---|
| `src/zed_f9p_rtk/zed_f9p_rtk/zed_f9p_rtk_node.py` | NavSatStatus 매핑 수정 (qual=5→SBAS_FIX), 공분산 값 교정 |
| `src/jupiter_nav_VO/jupiter_nav_VO/gnss_quality_filter.py` | 간소화 — 공분산 override 제거, 단순 게이트로 축소 |
| `src/jupiter_nav_VO/jupiter_nav_VO/odom_covariance_adapter.py` | IMU 기반 휠 슬립 감지 추가 (동적 공분산 배율) |
| `src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` | gnss_quality_filter 파라미터 간소화 |

---

## 7. 빌드 결과

```
colcon build --packages-select jupiter_nav_VO zed_f9p_rtk --symlink-install
Summary: 2 packages finished [5.32s]
```
정상 빌드 완료 (기존 성격의 경고만 존재).

---

## 8. 향후 작업

### 8.1 실테스트 필요 사항
1. **RTK NavSatStatus 확인**: `ros2 topic echo /fix --field status.status` — qual=4일 때 2, qual=5일 때 1 출력 확인
2. **GNSS 공분산 확인**: `ros2 topic echo /fix --field position_covariance` — RTK Fixed 시 0.0004 확인
3. **슬립 감지 테스트**: 바퀴를 들어올리거나 미끄러운 바닥에서 주행 → `SLIP DETECTED` 로그 확인
4. **슬립 시 EKF 동작**: `ros2 topic echo /odom_adapted --field twist.covariance` — 슬립 시 공분산 증가 확인

### 8.2 파라미터 튜닝
- `slip_mismatch_threshold=0.5`: 너무 민감하면 증가, 둔하면 감소
- `slip_max_multiplier=50.0`: EKF가 wheel을 완전 무시해야 하면 유지, 부분 무시면 감소
- `slip_decay=0.92`: 슬립 후 복귀가 너무 느리면 증가 (0.95→빠른 복귀), 너무 빠르면 감소
