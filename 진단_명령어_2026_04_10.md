# 명령어 로그 2026-04-10: Rosmaster_Lib 비교 + VNC 경량화

> 진단 및 구축에 사용한 명령어와 그 목적 및 결과를 요약 없이 전부 기록한다.

---

## 1. Rosmaster_Lib.py 비교 분석

### 1.1 src와 build 파일 동일 여부 및 underlay install 비교

**목적**: 런타임에 실제 사용되는 파일 경로 + src/build/install 간 차이 확인

```bash
file /home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py && echo "===" && diff /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py /home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py 2>&1; echo "exit=$?" && echo "===" && diff /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py /home/jetson/install/jupiter_bringup/lib/python3.10/site-packages/jupiter_bringup/Rosmaster_Lib.py 2>&1 | head -30
```

**결과**:
```
/home/jetson/jupiter_ws_ros2/build/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py: Python script, Unicode text, UTF-8 text executable, with CRLF line terminators
===
exit=0
===
20,25c20
<         self.__com = com
<         self.__baudrate = 115200
<         self.ser = serial.Serial(com, self.__baudrate)
<         self.__serial_ok = True
<         self.__reconnect_lock = threading.Lock()
<         self.__last_reconnect_time = 0
---
>         self.ser = serial.Serial(com, 115200)
269,296c264
<     # USB 시리얼 재연결 시도
<     def __try_reconnect(self):
<         """USB disconnect 후 자동 재연결 시도. 성공 시 True, 실패 시 False."""
<         now = time.time()
<         if now - self.__last_reconnect_time < 2.0:
<             return False
<         with self.__reconnect_lock:
<             self.__last_reconnect_time = now
<             self.__serial_ok = False
<             try:
<                 try:
<                     self.ser.close()
<                 except Exception:
<                     pass
<                 time.sleep(0.5)
<                 self.ser = serial.Serial(self.__com, self.__baudrate)
<                 self.__serial_ok = True
<                 time.sleep(0.2)
<                 self.set_auto_report_state(True, forever=False)
<                 time.sleep(0.1)
```

- src와 build 파일은 **완전히 동일** (exit=0)
- `/home/jetson/install/` 경로의 파일은 **오래된 버전** (reconnect 로직 등 없음)

---

### 1.2 수정본 vs 원본 (yahboom V3.3.1) 전체 diff

**목적**: 수정본(jupiter_bringup)과 원본(yahboom V3.3.1)의 모든 차이점 확인

```bash
diff /home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py /home/jetson/yahboomcar_ros2_ws/software/py_install_V3.3.1/Rosmaster_Lib/Rosmaster_Lib.py 2>&1 | head -200
```

**결과**:
```
20,25c20
<         self.__com = com
<         self.__baudrate = 115200
<         self.ser = serial.Serial(com, self.__baudrate)
<         self.__serial_ok = True
<         self.__reconnect_lock = threading.Lock()
<         self.__last_reconnect_time = 0
---
>         self.ser = serial.Serial(com, 115200)
42d36
<         self.FUNC_PORT_ON_OFF = 0x07
44d37
<         self.FUNC_REPORT_MAG = 0x09
161,162c154,155
<             self.__gy = struct.unpack('h', bytearray(ext_data[2:4]))[0]*gyro_ratio
<             self.__gz = struct.unpack('h', bytearray(ext_data[4:6]))[0]*gyro_ratio
---
>             self.__gy = struct.unpack('h', bytearray(ext_data[2:4]))[0]*-gyro_ratio
>             self.__gz = struct.unpack('h', bytearray(ext_data[4:6]))[0]*-gyro_ratio
181c174
<             accel_ratio = 1 / 10000.0
---
>             accel_ratio = 1 / 1000.0
196,202d188
< 
<         #지자계 센서 데이터
<         elif ext_type == self.FUNC_REPORT_MAG:
<             self.__magx = struct.unpack('h', bytearray(ext_data[0:2]))[0] / 10000.0
<             self.__magy = struct.unpack('h', bytearray(ext_data[2:4]))[0] / 10000.0
<             self.__magz = struct.unpack('h', bytearray(ext_data[4:6]))[0] / 10000.0
< 
269,296c255
<     # USB 시리얼 재연결 시도
<     def __try_reconnect(self):
<         """USB disconnect 후 자동 재연결 시도. 성공 시 True, 실패 시 False."""
<         now = time.time()
<         if now - self.__last_reconnect_time < 2.0:
<             return False
<         with self.__reconnect_lock:
<             self.__last_reconnect_time = now
<             self.__serial_ok = False
<             try:
<                 try:
<                     self.ser.close()
<                 except Exception:
<                     pass
<                 time.sleep(0.5)
<                 self.ser = serial.Serial(self.__com, self.__baudrate)
<                 self.__serial_ok = True
<                 time.sleep(0.2)
<                 self.set_auto_report_state(True, forever=False)
<                 time.sleep(0.1)
<                 self.set_auto_report_state(True, forever=False)
<                 print(f"[Rosmaster] Serial reconnected: {self.__com}")
<                 return True
<             except Exception as e:
<                 print(f"[Rosmaster] Reconnect failed ({self.__com}): {e}")
<                 return False
< 
<     # 접收数据 receive data
---
>     # 接收数据 receive data
299,322c258,273
<             try:
<                 read_data = self.ser.read()
<                 if not read_data:
<                     continue
<                 head1 = bytearray(read_data)[0]
<                 if head1 == self.__HEAD:
<                     head2 = bytearray(self.ser.read())[0]
<                     check_sum = 0
<                     rx_check_num = 0
<                     if head2 == self.__DEVICE_ID - 1:
<                         ext_len = bytearray(self.ser.read())[0]
<                         ext_type = bytearray(self.ser.read())[0]
<                         ext_data = []
<                         check_sum = ext_len + ext_type
<                         data_len = ext_len - 2
<                         while len(ext_data) < data_len:
<                             value = bytearray(self.ser.read())[0]
<                             ext_data.append(value)
<                             if len(ext_data) == data_len:
<                                 rx_check_num = value
<                             else:
<                                 check_sum = check_sum + value
<                         if check_sum % 256 == rx_check_num:
<                             self.__parse_data(ext_type, ext_data)
---
>             head1 = bytearray(self.ser.read())[0]
>             if head1 == self.__HEAD:
>                 head2 = bytearray(self.ser.read())[0]
>                 check_sum = 0
>                 rx_check_num = 0
>                 if head2 == self.__DEVICE_ID - 1:
>                     ext_len = bytearray(self.ser.read())[0]
>                     ext_type = bytearray(self.ser.read())[0]
>                     ext_data = []
>                     check_sum = ext_len + ext_type
>                     data_len = ext_len - 2
>                     while len(ext_data) < data_len:
>                         value = bytearray(self.ser.read())[0]
>                         ext_data.append(value)
>                         if len(ext_data) == data_len:
>                             rx_check_num = value
324,334c275,280
<                             if self.__debug:
<                                 print("check sum error:", ext_len, ext_type, ext_data)
<             except (serial.SerialException, OSError) as e:
<                 print(f"[Rosmaster] Serial RX error (attempting reconnect): {e}")
<                 self.__serial_ok = False
<                 if self.__try_reconnect():
<                     continue
<                 time.sleep(2.0)
<             except Exception as e:
<                 print("Serial receive error:", e)
<                 time.sleep(0.1)
---
>                             check_sum = check_sum + value
>                     if check_sum % 256 == rx_check_num:
>                         self.__parse_data(ext_type, ext_data)
>                     else:
>                         if self.__debug:
>                             print("check sum error:", ext_len, ext_type, ext_data)
383,386d328
<     def is_serial_ok(self):
<         """시리얼 연결 상태 확인"""
<         return self.__serial_ok
< 
462,480d403
<     # 라이트 스위치(1번 포트) 온 오프
<     def set_port(self, on_time):
<         try:
<             if on_time < 0:
<                 print("port input error!")
<                 return
<             value = bytearray(struct.pack('h', int(on_time)))
< 
<             cmd = [self.__HEAD, self.__DEVICE_ID, 0x05, self.FUNC_PORT_ON_OFF, value[0], value[1]]
<             checksum = sum(cmd, self.__COMPLEMENT) & 0xff
<             cmd.append(checksum)
<             self.ser.write(cmd)
<             if self.__debug:
<                 print("port:", cmd)
<             time.sleep(self.__delay_time)
<         except:
<             print('---set_port error!---')
<             pass
< 
635c558
<         X3: v_x=[-1.0, 1.0], v_y=[-1.0, 1.0], v_z=[-5, 5] # 바꿨음 v_x = [-0.45, 0.45], v_y = [-2.0, 2.0]
---
>         X3: v_x=[-1.0, 1.0], v_y=[-1.0, 1.0], v_z=[-5, 5]
652,657c575,577
<         except (serial.SerialException, OSError) as e:
<             print(f'[Rosmaster] set_car_motion serial error: {e}')
<             self.__serial_ok = False
<             self.__try_reconnect()
<         except Exception as e:
<             print(f'---set_car_motion error: {e}---')
---
>         except:
>             print('---set_car_motion error!---')
>             pass
```

---

## 2. VNC 환경 분석

### 2.1 현재 디스플레이/VNC 프로세스 확인

**목적**: 현재 VNC 서버 종류, 데스크탑 환경, 시스템 부하 확인

```bash
echo "=== Current display env ===" && echo "DISPLAY=$DISPLAY" && echo "=== Running VNC/display processes ===" && ps aux | grep -iE 'vnc|x11vnc|vino|xrdp|xpra|turbovnc|tigervnc|nomachine|nxserver' | grep -v grep && echo "=== systemd vnc services ===" && systemctl list-units --type=service --all 2>/dev/null | grep -iE 'vnc|vino|xrdp|display|xpra|nomachine' && echo "=== Desktop session ===" && echo "$XDG_CURRENT_DESKTOP" && echo "$XDG_SESSION_TYPE" && echo "=== GPU usage ===" && tegrastats 2>/dev/null | head -1 || echo "no tegrastats"
```

**결과**:
```
=== Current display env ===
DISPLAY=
=== Running VNC/display processes ===
jetson    271603  0.0  0.1  24428 10592 ?        Ss   10:16   0:00 /usr/bin/perl /usr/bin/vncserver :1 -localhost=0 -geometry 1920x1200 -depth 24
jetson    271604  6.4  1.2 254556 97572 ?        Ss   10:16   1:54 /usr/bin/Xtigervnc :1 -localhost=0 -desktop yahboom:1 (jetson) -rfbport 5901 -PasswordFile /home/jetson/.vnc/passwd -SecurityTypes VncAuth,TLSVnc -auth /home/jetson/.Xauthority -geometry 1920x1200 -depth 24
=== systemd vnc services ===
  gdm.service                                           loaded    active   running GNOME Display Manager
  nvmemwarning.service                                  loaded    active   running Display Low memory warning
=== Desktop session ===

tty
=== GPU usage ===
04-10-2026 10:45:36 RAM 4826/7620MB (lfb 14x4MB) SWAP 6/12002MB (cached 0MB) CPU [3%@1728,3%@1728,8%@1728,1%@1728,1%@1728,0%@1728] GR3D_FREQ 0% cpu@49.312C soc2@47.906C soc0@48.156C gpu@49.093C tj@49.125C soc1@48.781C VDD_IN 6946mW/6946mW VDD_CPU_GPU_CV 1693mW/1693mW VDD_SOC 2382mW/2382mW
```

---

### 2.2 VNC xstartup 및 데스크탑 환경 확인

**목적**: xstartup 내용, backup vnc 디렉토리, 설치된 데스크탑 환경 확인

```bash
echo "=== VNC startup script ===" && cat /home/jetson/.vnc/xstartup 2>/dev/null && echo "=== VNC config ===" && cat /home/jetson/.vnc/config 2>/dev/null && echo "=== backup vnc ===" && ls -la /home/jetson/backup/vnc/ 2>/dev/null && echo "=== Desktop manager ===" && dpkg -l | grep -iE 'gnome-session|gdm|lightdm|xfce|lxde|openbox' | head -20
```

**결과**:
```
=== VNC startup script ===
#!/bin/sh
# Robust VNC xstartup: try to start a full desktop, fall back to lighter DEs or xterm
unset DBUS_SESSION_BUS_ADDRESS
unset XDG_RUNTIME_DIR

# Prefer dbus-launch for GNOME/KDE session startup
if command -v dbus-launch >/dev/null 2>&1; then
        if command -v gnome-session >/dev/null 2>&1; then
                exec dbus-launch --exit-with-session gnome-session
        fi
fi

# XFCE
if command -v startxfce4 >/dev/null 2>&1; then
        exec startxfce4
fi

# LXDE
if command -v startlxde >/dev/null 2>&1; then
        exec startlxde
fi

# Fallback: launch a simple xterm so the VNC session stays alive
if command -v xterm >/dev/null 2>&1; then
        exec /usr/bin/xterm
fi

[Allow Colord all Users]
Identity=unix-user:*
Action=org.freedesktop.color-manager.create-device;org.freedesktop.color-manager.create-profile;org.freedesktop.color-manager.delete-device;org.freedesktop.color-manager.delete-profile;org.freedesktop.color-manager.modify-device;org.freedesktop.color-manager.modify-profile
ResultAny=yes # Allow all actions : 한국어로는 "모든 작업 허용"
ResultInactive=yes # 비활성 세션에 대해서도 허용
ResultActive=yes # 활성 세션에 대해서도 허용

# If nothing else, sleep to keep session alive briefly
sleep 10

=== VNC config ===
(없음)
```

---

### 2.3 설치된 DE 및 메모리 상위 프로세스 확인

**목적**: 현재 GNOME 관련 프로세스의 리소스 소비량 확인

```bash
echo "=== backup vnc ===" && ls -la /home/jetson/backup/vnc/ 2>/dev/null && echo "=== installed DEs ===" && dpkg -l 2>/dev/null | grep -iE 'gnome-session|gnome-shell|gdm3|xfce4-session|openbox' | awk '{print $1,$2,$3}' && echo "=== RAM breakdown (top GPU consumers) ===" && ps aux --sort=-%mem | head -15 && echo "=== Xtigervnc version ===" && Xtigervnc -version 2>&1 | head -3
```

**결과**:
```
=== backup vnc ===
total 24
drwxrwxr-x 2 jetson jetson 4096 Nov 26 12:31 .
drwxrwxr-x 3 jetson jetson 4096 Nov 26 12:31 ..
-rw-rw-r-- 1 jetson jetson    8 Nov 26 12:31 passwd
-rw-rw-r-- 1 jetson jetson 6527 Nov 26 12:31 VNC_SETUP_GUIDE.md
-rw-rw-r-- 1 jetson jetson  706 Nov 26 12:31 xstartup
=== installed DEs ===
ii gdm3 42.0-1ubuntu7.22.04.4
ii gnome-session-bin 42.0-1ubuntu2
ii gnome-session-canberra 0.30-10ubuntu1.22.04.1
ii gnome-session-common 42.0-1ubuntu2
ii gnome-shell 42.9-0ubuntu2.2
ii gnome-shell-common 42.9-0ubuntu2.2
ii gnome-shell-extension-appindicator 42-2~fakesync1
ii gnome-shell-extension-desktop-icons-ng 43-2ubuntu1
ii gnome-shell-extension-manager 0.3.0-0ubuntu2.1
ii gnome-shell-extension-ubuntu-dock 72~ubuntu5.22.04.3
ii yaru-theme-gnome-shell 22.04.5
=== RAM breakdown (top GPU consumers) ===
USER         PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND
jetson      4736  0.9 11.3 65700648 886048 ?     Sl   07:59   1:38 /home/jetson/.vscode-server/cli/servers/Stable-e7fb5e96c0730b9deb70b33781f98e2f35975036/server/node --dns-result-order=ipv4first ...
jetson    271667 46.8  4.9 4795644 387424 ?      Sl   10:16  13:57 /usr/bin/gnome-shell
jetson      2247  0.3  2.7 3550548 213040 ?      Ssl  07:50   0:32 /usr/bin/gnome-shell
jetson      4012  0.3  2.5 1068900 201624 ?      SNl  07:52   0:31 /usr/bin/python3 /usr/bin/update-manager --no-update --no-focus-on-map
jetson      2996  1.2  2.4 1852016 191048 pts/0  Sl+  07:50   2:15 python3 /home/jetson/Rosmaster/rosmaster/rosmaster_main.py
jetson     41308  0.0  2.0 838588 162268 ?       Sl   09:22   0:03 ...node... tsserver.js ...
jetson      4629  0.3  2.0 1065368 161640 ?      Sl   07:59   0:33 ...node... fileWatcher
jetson      4610  0.2  1.6 11586224 125988 ?     Sl   07:59   0:21 ...node... server-main.js ...
jetson      2612  0.1  1.5 728948 119956 ?       Sl   07:50   0:11 /usr/bin/gnome-software --gapplication-service
jetson    272001  0.5  1.2 728552 99644 ?        Sl   10:16   0:09 /usr/bin/gnome-software --gapplication-service
jetson    271604  6.3  1.2 254556 97572 ?        Ss   10:16   1:54 /usr/bin/Xtigervnc :1 ...
root        1079  0.0  1.1 190968 88372 ?        Ss   07:50   0:02 /usr/bin/python3 /usr/local/bin/jupyter-lab
root        2527  0.5  1.1 385740 88244 ?        Ssl  07:50   0:53 /usr/libexec/packagekitd
=== Xtigervnc version ===

Xvnc TigerVNC 1.12.0 - built 2024-01-23 19:20
Copyright (C) 1999-2021 TigerVNC Team and many others (see README.rst)
```

---

### 2.4 Xfce4 설치 여부 확인

**목적**: Xfce4가 이미 설치되어 있는지 확인

```bash
dpkg -l | grep -iE 'xfce4-session|xfce4 ' | awk '{print $1,$2,$3}' && echo "=== check available ===" && apt-cache policy xfce4 2>/dev/null | head -5
```

**결과**:
```
=== check available ===
xfce4:
  Installed: (none)
  Candidate: 4.16
  Version table:
     4.16 500
```

---

## 3. Xfce4 설치 및 구성

### 3.1 Xfce4 최소 설치

**목적**: 경량 데스크탑 환경 설치 (불필요 추천 패키지 제외)

```bash
sudo apt-get update -qq && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends xfce4 xfce4-terminal dbus-x11 2>&1 | tail -20
```

**결과**: (apt 출력 — 다수의 패키지 설치됨, 정상 완료)

설치 확인:
```bash
dpkg -l | grep xfce4-session | awk '{print $1,$2,$3}'
```

**결과**:
```
ii xfce4-session 4.16.0-1ubuntu2
```

---

### 3.2 xstartup 백업

**목적**: 현재 GNOME xstartup 원본 보존

```bash
cp /home/jetson/.vnc/xstartup /home/jetson/.vnc/xstartup.bak.gnome
```

**결과**: 성공 (backed up to xstartup.bak.gnome)

---

### 3.3 xstartup 변경

**목적**: Xfce4 우선 실행으로 변경

변경 전 (`xstartup`):
```sh
#!/bin/sh
# Robust VNC xstartup: try to start a full desktop, fall back to lighter DEs or xterm
unset DBUS_SESSION_BUS_ADDRESS
unset XDG_RUNTIME_DIR

# Prefer dbus-launch for GNOME/KDE session startup
if command -v dbus-launch >/dev/null 2>&1; then
	if command -v gnome-session >/dev/null 2>&1; then
		exec dbus-launch --exit-with-session gnome-session
	fi
fi

# XFCE
if command -v startxfce4 >/dev/null 2>&1; then
	exec startxfce4
fi

# LXDE
if command -v startlxde >/dev/null 2>&1; then
	exec startlxde
fi

# Fallback: launch a simple xterm so the VNC session stays alive
if command -v xterm >/dev/null 2>&1; then
	exec /usr/bin/xterm
fi

[Allow Colord all Users]
Identity=unix-user:*
Action=org.freedesktop.color-manager.create-device;...
ResultAny=yes
ResultInactive=yes
ResultActive=yes

# If nothing else, sleep to keep session alive briefly
sleep 10
```

변경 후 (`xstartup`):
```sh
#!/bin/sh
# Lightweight VNC xstartup - Xfce4 preferred for low resource usage
unset DBUS_SESSION_BUS_ADDRESS
unset XDG_RUNTIME_DIR

# Xfce4 - lightweight desktop (preferred)
if command -v startxfce4 >/dev/null 2>&1; then
	exec dbus-launch --exit-with-session startxfce4
fi

# GNOME fallback
if command -v dbus-launch >/dev/null 2>&1; then
	if command -v gnome-session >/dev/null 2>&1; then
		exec dbus-launch --exit-with-session gnome-session
	fi
fi

# Fallback: xterm
if command -v xterm >/dev/null 2>&1; then
	exec /usr/bin/xterm
fi

sleep 10
```

---

### 3.4 Colord PolicyKit 규칙 분리

**목적**: xstartup에 잘못 포함되어 있던 PolicyKit 규칙을 올바른 파일로 이동

```bash
sudo tee /etc/polkit-1/localauthority/50-local.d/45-allow-colord.pkla > /dev/null << 'EOF'
[Allow Colord all Users]
Identity=unix-user:*
Action=org.freedesktop.color-manager.create-device;org.freedesktop.color-manager.create-profile;org.freedesktop.color-manager.delete-device;org.freedesktop.color-manager.delete-profile;org.freedesktop.color-manager.modify-device;org.freedesktop.color-manager.modify-profile
ResultAny=yes
ResultInactive=yes
ResultActive=yes
EOF
```

**결과**: polkit rule created (정상 생성)

---

### 3.5 VNC 세션 확인 및 재시작

**목적**: 현재 GNOME VNC 세션 종료 후 Xfce4로 재시작

```bash
vncserver -list
```

**결과**:
```
TigerVNC server sessions:

X DISPLAY #     RFB PORT #      RFB UNIX PATH   PROCESS ID #    SERVER
1               5901                            271604          Xtigervnc
```

```bash
vncserver -kill :1 2>&1; echo "---"; vncserver :1 -localhost=0 -geometry 1920x1080 -depth 24 2>&1; echo "---done"
```

**결과**:
```
Killing Xtigervnc process ID 374973... success!
---

New Xtigervnc server 'yahboom:1 (jetson)' on port 5901 for display :1.
Use xtigervncviewer -SecurityTypes VncAuth,TLSVnc -passwd /home/jetson/.vnc/passwd yahboom:1 to connect to the VNC server.

---done
```

---

### 3.6 Xfce4 실행 확인

**목적**: GNOME 대신 Xfce4가 VNC 세션에서 실행되고 있는지 확인

```bash
ps aux | grep -iE 'xfce|gnome-shell' | grep -v grep
```

**결과**:
```
jetson      2247  0.3  2.7 3550548 214412 ?      Ssl  07:50   0:33 /usr/bin/gnome-shell
jetson      2439  0.0  0.2 584736 16920 ?        Sl   07:50   0:00 /usr/libexec/gnome-shell-calendar-server
jetson      2541  0.1  0.3 2807624 24200 ?       Sl   07:50   0:12 /usr/bin/gjs /usr/share/gnome-shell/org.gnome.Shell.Notifications
jetson      2780  0.0  0.2 2807680 23360 ?       Sl   07:50   0:00 /usr/bin/gjs /usr/share/gnome-shell/org.gnome.ScreenSaver
jetson    375358  2.0  0.7 449772 58512 ?        Ssl  10:54   0:00 xfce4-session
jetson    375361  0.0  0.0   7996  2120 ?        S    10:54   0:00 dbus-launch --exit-with-session startxfce4
jetson    375388  0.6  0.0 230992  7540 ?        Sl   10:54   0:00 /usr/lib/aarch64-linux-gnu/xfce4/xfconf/xfconfd
jetson    375480  6.2  0.4 470304 37440 ?        Sl   10:54   0:00 xfce4-panel
jetson    375505  1.2  0.3 438400 29428 ?        Sl   10:54   0:00 /usr/lib/aarch64-linux-gnu/xfce4/panel/wrapper-2.0 ... libsystray.so ...
jetson    375506  1.5  0.4 463092 38356 ?        Sl   10:54   0:00 /usr/lib/aarch64-linux-gnu/xfce4/panel/wrapper-2.0 ... libpulseaudio-plugin.so ...
jetson    375571  1.0  0.3 354676 24928 ?        Sl   10:54   0:00 /usr/lib/aarch64-linux-gnu/xfce4/panel/wrapper-2.0 ... libactions.so ...
```

- gnome-shell PID 2247: 로컬 디스플레이용 (VNC 아님)
- xfce4-session PID 375358: **VNC :1 세션** (새로 시작됨)

---

## 4. Xfce4 최적화

### 4.1 Compositor 비활성화

**목적**: GPU 부하 감소를 위한 창 합성기 비활성화

```bash
DISPLAY=:1 xfconf-query -c xfwm4 -p /general/use_compositing -s false 2>&1; echo "rc=$?"
```

**결과**:
```
rc=0
```

확인:
```bash
DISPLAY=:1 xfconf-query -c xfwm4 -p /general/use_compositing
```

**결과**:
```
false
```

---

### 4.2 autostart 비활성화

**목적**: 불필요한 GNOME 서비스 자동 실행 방지

```bash
mkdir -p /home/jetson/.config/autostart

cat > /home/jetson/.config/autostart/update-notifier.desktop << 'EOF'
[Desktop Entry]
Hidden=true
EOF

cat > /home/jetson/.config/autostart/org.gnome.Software.desktop << 'EOF'
[Desktop Entry]
Hidden=true
EOF

cat > /home/jetson/.config/autostart/gnome-initial-setup-copy-worker.desktop << 'EOF'
[Desktop Entry]
Hidden=true
EOF
```

**결과**: autostart entries disabled (정상 생성)

---

### 4.3 VNC 세션 리소스 확인 (최적화 후)

**목적**: 최적화 후 Xfce4 프로세스별 메모리 사용량 확인

```bash
ps aux | grep -E 'xfce|Xtigervnc' | grep -v grep | awk '{printf "PID:%-8s CPU:%-5s RSS:%-8sKB %s\n", $2, $3, $6, $11}'
```

**결과**:
```
PID:375353   CPU:0.6   RSS:109308  KB /usr/bin/Xtigervnc
PID:375358   CPU:0.4   RSS:58520   KB xfce4-session
PID:375361   CPU:0.0   RSS:2120    KB dbus-launch
PID:375388   CPU:0.1   RSS:7620    KB /usr/lib/aarch64-linux-gnu/xfce4/xfconf/xfconfd
PID:375480   CPU:1.3   RSS:37840   KB xfce4-panel
PID:375505   CPU:0.2   RSS:27512   KB /usr/lib/aarch64-linux-gnu/xfce4/panel/wrapper-2.0
PID:375506   CPU:0.4   RSS:38356   KB /usr/lib/aarch64-linux-gnu/xfce4/panel/wrapper-2.0
PID:375571   CPU:0.2   RSS:24928   KB /usr/lib/aarch64-linux-gnu/xfce4/panel/wrapper-2.0
PID:377027   CPU:0.0   RSS:5476    KB /usr/lib/aarch64-linux-gnu/xfce4/xfconf/xfconfd
```

---

## 5. rviz2 동작 테스트

### 5.1 rviz2 실행 테스트

**목적**: Xfce4 VNC 세션에서 rviz2가 OpenGL로 정상 동작하는지 확인

```bash
DISPLAY=:1 bash -c 'source /opt/ros/humble/setup.bash && timeout 8 rviz2' 2>&1; echo "EXIT=$?"
```

**결과**:
```
[INFO] [1775789755.014409916] [rviz2]: Stereo is NOT SUPPORTED
[INFO] [1775789755.014582915] [rviz2]: OpenGl version: 4.5 (GLSL 4.5)
[INFO] [1775789755.740634515] [rviz2]: Stereo is NOT SUPPORTED
[INFO] [1775789762.036004521] [rclcpp]: signal_handler(signum=15)
EXIT=124
```

- OpenGL 4.5 정상 인식
- timeout 후 SIGTERM으로 정상 종료 (EXIT=124 = timeout)

---

## 6. 최종 리소스 비교

### 6.1 전체 시스템 메모리 및 tegrastats

**목적**: GNOME 대비 Xfce4 VNC의 전체 리소스 사용량 비교

```bash
free -m | head -2; echo ""; echo "=== VNC Xfce4 total RSS ==="; ps aux | grep -E 'xfce|xfwm|Xtigervnc' | grep -v grep | awk '{sum+=$6; printf "  %6dKB %s\n", $6, $11} END {printf "\n  Total: %d MB\n", sum/1024}'; echo ""; echo "=== tegrastats (GPU) ==="; tegrastats 2>/dev/null | head -1 || echo "no tegrastats available"
```

**결과**:
```
               total        used        free      shared  buff/cache   available
Mem:            7619        3924         843          38        2851        3424

=== VNC Xfce4 total RSS ===
  109320KB /usr/bin/Xtigervnc
   58520KB xfce4-session
    2120KB dbus-launch
    7620KB /usr/lib/aarch64-linux-gnu/xfce4/xfconf/xfconfd
   79492KB xfwm4
   37840KB xfce4-panel
   27512KB /usr/lib/aarch64-linux-gnu/xfce4/panel/wrapper-2.0
   38356KB /usr/lib/aarch64-linux-gnu/xfce4/panel/wrapper-2.0
   24928KB /usr/lib/aarch64-linux-gnu/xfce4/panel/wrapper-2.0
    5476KB /usr/lib/aarch64-linux-gnu/xfce4/xfconf/xfconfd

  Total: 382 MB

=== tegrastats (GPU) ===
04-10-2026 10:56:25 RAM 4351/7620MB (lfb 14x4MB) SWAP 6/12002MB (cached 1MB) CPU [1%@1728,0%@1728,0%@1728,4%@1728,0%@1728,0%@1728] GR3D_FREQ 0% cpu@49C soc2@48.218C soc0@48.562C gpu@49.468C tj@49.468C soc1@49.25C VDD_IN 6754mW/6754mW VDD_CPU_GPU_CV 1534mW/1534mW VDD_SOC 2382mW/2382mW
```

---

## 7. RDP 분석 (OpenGL 렌더러 확인)

### 7.1 mesa-utils 설치 및 glxinfo 확인

**목적**: VNC 가상 디스플레이의 OpenGL 렌더러가 GPU인지 소프트웨어인지 확인 (RDP 전환 판단)

```bash
dpkg -l | grep glxinfo; echo "---"; apt-cache policy mesa-utils 2>&1 | head -3
```

**결과**:
```
---
mesa-utils:
  Installed: (none)
  Candidate: 8.4.0-1ubuntu1
```

```bash
sudo apt-get install -y -qq mesa-utils 2>&1 | tail -5
```

**결과**: 설치 완료

```bash
DISPLAY=:1 glxinfo 2>&1 | grep -iE 'renderer|vendor|version|direct' | head -10
```

**결과**:
```
direct rendering: Yes
server glx vendor string: SGI
server glx version string: 1.4
client glx vendor string: Mesa Project and SGI
client glx version string: 1.4
    GLX_MESA_copy_sub_buffer, GLX_MESA_query_renderer, GLX_MESA_swap_control, 
GLX version: 1.4
    GLX_MESA_query_renderer, GLX_OML_swap_method, GLX_SGIS_multisample, 
Extended renderer info (GLX_MESA_query_renderer):
    Vendor: Mesa (0xffffffff)
```

```bash
DISPLAY=:1 glxinfo 2>&1 | grep -i 'opengl renderer\|opengl version\|opengl vendor'
```

**결과**:
```
OpenGL vendor string: Mesa
OpenGL renderer string: llvmpipe (LLVM 15.0.7, 128 bits)
OpenGL version string: 4.5 (Compatibility Profile) Mesa 23.2.1-1ubuntu3.1~22.04.3
```

- VNC 가상 디스플레이(:1)는 **llvmpipe (소프트웨어 렌더러)** 사용
- Jetson GPU는 VNC에서 직접 사용되지 않음
- xrdp로 전환해도 동일하게 llvmpipe 사용 → GL 성능 차이 없음

---

### 7.2 NVIDIA 드라이버 확인

**목적**: Jetson GPU 드라이버 버전 확인

```bash
cat /proc/driver/nvidia/version 2>/dev/null | head -2 || echo "no nvidia proc"
```

**결과**:
```
NVRM version: NVIDIA UNIX Open Kernel Module for aarch64  540.4.0  Release Build  (buildbrain@mobile-u64-6336-d8000)  Tue Jan  7 17:35:12 PST 2025
GCC version:  collect2: error: ld returned 1 exit status
```
