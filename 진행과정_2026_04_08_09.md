# 진행과정 2026-04-08~09: USB 시리얼 재연결 + udev 수정 + NavSatStatus 분석

## 1. 목표

### 1.1 사용자 요청 (시간순)
> 1. (04-08) Nav2에서 로봇이 움직이지 않는다 → 진단하라
> 2. (04-08) MCU USB 시리얼이 끊어지면 물리적 재연결 없이 복구되지 않는다
> 3. (04-08) RTK GPS도 USB 시리얼로 연결 중인데 같은 문제가 있다
> 4. (04-09) 재부팅 후 컨트롤러 보드(CH340)가 인식되지 않는다 — 부팅 시 인식 될 때가 있고 안 될 때가 있는 원인을 찾아라
> 5. (04-09) 물리적 접촉 문제가 맞았다. 시스템을 복잡하게 하는 것들을 제거하라
> 6. (04-09) NavSatStatus에 3이 존재하지 않는다 — 패키지 분석을 통해 확인하고 RTK 코드를 수정하라
> 7. (04-09) 진행 과정을 md파일로 정리하라

### 1.2 이전 세션 요약
- Part 7 (04-07): RTK GPS 융합 아키텍처 구축
- Part 8 (04-07): NavSatStatus 매핑 수정, GNSS 필터 간소화, IMU 기반 휠 슬립 감지

---

## 2. Nav2 로봇 미동 진단 (04-08)

### 2.1 증상
Nav2에서 골 명령을 보내면 cmd_vel이 발행되지만 로봇이 움직이지 않음.

### 2.2 원인 1: Global Costmap "Sensor origin out of map bounds"

```bash
ros2 topic echo /global_costmap/costmap_updates
```
LiDAR base_link → base_footprint 오프셋이 -3cm인데, 이것이 맵 경계와 정확히 일치하여 raytrace skip 발생.

**수정**: global_costmap에서 obstacle_layer 제거 (static_layer + nvblox_layer로 충분, local_costmap이 실시간 장애물 담당)

**파일**: `src/jupiter_nav_VO/config/nav2_params_fused.yaml`
```yaml
# 수정 전
global_costmap:
  plugins: ["static_layer", "obstacle_layer", "nvblox_layer", "inflation_layer"]

# 수정 후
global_costmap:
  plugins: ["static_layer", "nvblox_layer", "inflation_layer"]
```

### 2.3 원인 2: goal_pose_restamper shutdown crash

```bash
ros2 launch nav2_vslam_fused.launch.py
# Ctrl+C 시 rclpy.shutdown() 이중호출로 크래시
```

**수정**: `src/jupiter_nav_VO/jupiter_nav_VO/goal_pose_restamper.py`
```python
# 수정 전
finally:
    node.destroy_node()
    rclpy.shutdown()

# 수정 후
finally:
    node.destroy_node()
    try:
        rclpy.shutdown()
    except Exception:
        pass
```

### 2.4 원인 3: MCU USB 시리얼 끊김
cmd_vel이 0.33 m/s로 발행되지만 엔코더가 0.0을 보고 → MCU가 명령을 수신 못함.

```bash
sudo dmesg | grep -iE "ch34|disconnect"
```
```
[5573.123456] usb 1-2.3: USB disconnect, device number 6
[5573.234567] ch34x ttyUSB0: ch34x converter now disconnected from ttyUSB0
```

물리적 USB 재연결 후 정상 동작 확인.

---

## 3. MCU USB 시리얼 자동 재연결 구현 (04-08)

### 3.1 문제: Rosmaster_Lib.py의 `except: pass`

기존 코드가 모든 시리얼 에러를 `except: pass`로 무시 → USB disconnect 후 에러를 감지조차 못함.

### 3.2 수정: `__try_reconnect()` 메서드 추가

**파일**: `src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py`

추가된 인스턴스 변수:
```python
self.__com = com
self.__baudrate = 115200
self.__serial_ok = True
self.__reconnect_lock = threading.Lock()
self.__last_reconnect_time = 0
```

추가된 메서드:
```python
def __try_reconnect(self):
    """USB disconnect 후 자동 재연결 시도. 성공 시 True, 실패 시 False."""
    now = time.time()
    if now - self.__last_reconnect_time < 2.0:
        return False
    with self.__reconnect_lock:
        self.__last_reconnect_time = now
        self.__serial_ok = False
        try:
            try:
                self.ser.close()
            except Exception:
                pass
            time.sleep(0.5)
            self.ser = serial.Serial(self.__com, self.__baudrate)
            self.__serial_ok = True
            time.sleep(0.2)
            self.set_auto_report_state(True, forever=False)
            time.sleep(0.1)
            self.set_auto_report_state(True, forever=False)
            print(f"[Rosmaster] Serial reconnected: {self.__com}")
            return True
        except Exception as e:
            print(f"[Rosmaster] Reconnect failed ({self.__com}): {e}")
            return False
```

`__receive_data()` 수정:
```python
except (serial.SerialException, OSError) as e:
    print(f"[Rosmaster] Serial RX error (attempting reconnect): {e}")
    self.__serial_ok = False
    if self.__try_reconnect():
        continue
    time.sleep(2.0)
```

`set_car_motion()` 수정:
```python
except (serial.SerialException, OSError) as e:
    print(f'[Rosmaster] set_car_motion serial error: {e}')
    self.__serial_ok = False
    self.__try_reconnect()
```

### 3.3 jupiter_driver 헬스 모니터 추가

**파일**: `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

```python
# 5초 간격 시리얼 헬스 체크 타이머
self._serial_was_ok = True
self.create_timer(5.0, self.check_serial_health)

def check_serial_health(self):
    ok = self.bot.is_serial_ok()
    if not ok and self._serial_was_ok:
        self.get_logger().error('MCU serial disconnected — reconnecting...')
    elif ok and not self._serial_was_ok:
        self.get_logger().info('MCU serial reconnected')
    self._serial_was_ok = ok
```

### 3.4 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_bringup --symlink-install
```
```
Starting >>> jupiter_bringup
Finished <<< jupiter_bringup [5.11s]

Summary: 1 package finished [5.56s]
```

---

## 4. RTK GPS USB 시리얼 진단 및 수정 (04-08)

### 4.1 장치 상태 확인

```bash
ls -la /dev/myserial /dev/rtk_gps /dev/ttyUSB*
```
```
lrwxrwxrwx 1 root root 7 Apr  8 10:51 /dev/myserial -> ttyUSB1
lrwxrwxrwx 1 root root 7 Apr  8 10:51 /dev/rtk_gps -> ttyUSB0
crwxrwxrwx 1 root dialout 188, 0 Apr  8 10:51 /dev/ttyUSB0
crwxrwxrwx 1 root dialout 188, 1 Apr  8 10:51 /dev/ttyUSB1
crwxrwxrwx 1 root dialout 188, 2 Apr  8 10:51 /dev/ttyUSB2
```

### 4.2 udev 규칙 버그 발견

```bash
cat /home/jetson/jupiter_ws_ros2/99-jupiter-devices.rules
```
```
KERNEL=="ttyUSB*", ATTRS{idVendor}=="1a86", ATTRS{idProduct}=="7523", MODE="0777", SYMLINK+="myserial"
KERNEL=="ttyUSB*", ATTRS{idVendor}=="0403", ATTRS{idProduct}=="6015", MODE="0777", SYMLINK+="myserial"
KERNEL=="ttyUSB*", ATTRS{idVendor}=="10c4", ATTRS{idProduct}=="ea60", MODE="0777", SYMLINK+="lidar_port"
```

**문제**: `0403:6015` (FTDI FT231X, RTK GPS)이 `myserial` symlink을 생성 → MCU의 `1a86:7523`과 충돌.

```bash
udevadm info -q property /dev/ttyUSB0
```
```
ID_VENDOR_ID=0403
ID_MODEL_ID=6015
ID_SERIAL_SHORT=D30JR9UK
DEVLINKS=/dev/serial/by-id/usb-FTDI_FT231X_USB_UART_D30JR9UK-if00-port0 /dev/serial/by-path/... /dev/myserial /dev/rtk_gps
```
→ ttyUSB0 (RTK GPS)에 `/dev/myserial`과 `/dev/rtk_gps` 모두 할당됨.

### 4.3 udev 규칙 수정

```bash
# 99-jupiter-devices.rules 수정: 0403:6015 → rtk_gps
sed -i 's/ATTRS{idVendor}=="0403".*SYMLINK+="myserial"/ATTRS{idVendor}=="0403", ATTRS{idProduct}=="6015", MODE="0777", SYMLINK+="rtk_gps"/' \
  /home/jetson/jupiter_ws_ros2/99-jupiter-devices.rules
```

```bash
sudo cp /home/jetson/jupiter_ws_ros2/99-jupiter-devices.rules /etc/udev/rules.d/
sudo udevadm control --reload-rules
sudo udevadm trigger
```

```bash
ls -la /dev/myserial /dev/rtk_gps
```
```
lrwxrwxrwx 1 root root 7 Apr  8 10:51 /dev/myserial -> ttyUSB1
lrwxrwxrwx 1 root root 7 Apr  8 10:51 /dev/rtk_gps -> ttyUSB0
```
→ 충돌 해소: `/dev/myserial` → MCU만, `/dev/rtk_gps` → RTK만.

### 4.4 RTK 노드 재연결 로직 추가

**파일**: `src/zed_f9p_rtk/zed_f9p_rtk/zed_f9p_rtk_node.py`

기존 코드:
```python
try:
    self.serial = serial.Serial(self.port, self.baudrate, timeout=1.0)
except Exception as e:
    self.get_logger().error(f"Failed to open serial port {self.port}: {e}")
    sys.exit(1)  # ← 노드 영구 사망
```

수정 후:
```python
# Serial port with retry
self.serial = None
self._serial_lock = threading.Lock()
self._serial_ok = False
self._connect_serial()

def _connect_serial(self):
    """시리얼 포트 연결 시도. 실패 시 백그라운드에서 재시도."""
    try:
        if self.serial and self.serial.is_open:
            self.serial.close()
    except Exception:
        pass
    try:
        self.serial = serial.Serial(self.port, self.baudrate, timeout=1.0)
        self._serial_ok = True
        self.get_logger().info(f"Opened serial port {self.port} at {self.baudrate} bps.")
    except Exception as e:
        self._serial_ok = False
        self.serial = None
        self.get_logger().error(f"Failed to open serial port {self.port}: {e}")

def _try_reconnect(self):
    """시리얼 포트 재연결 시도 (lock 내부에서 호출)."""
    self._serial_ok = False
    try:
        if self.serial and self.serial.is_open:
            self.serial.close()
    except Exception:
        pass
    try:
        self.serial = serial.Serial(self.port, self.baudrate, timeout=1.0)
        self._serial_ok = True
        self.get_logger().info(f"Serial reconnected: {self.port}")
        return True
    except Exception as e:
        self.serial = None
        self.get_logger().warn(f"Serial reconnect failed: {e}")
        return False
```

`read_nmea()` 수정:
```python
def read_nmea(self):
    while self.running:
        if not self._serial_ok or self.serial is None:
            time.sleep(2)
            with self._serial_lock:
                self._try_reconnect()
            continue
        try:
            if self.serial.in_waiting > 0:
                line = self.serial.readline().decode('ascii', errors='ignore').strip()
                if line.startswith('$GNGGA') or line.startswith('$GPGGA'):
                    self.last_gga = line
                    self.process_gga(line)
        except (serial.SerialException, OSError) as e:
            self.get_logger().error(f"Serial read error: {e}. Attempting reconnect...")
            with self._serial_lock:
                self._try_reconnect()
            time.sleep(2)
        except Exception as e:
            pass
```

`ntrip_client()` RTCM 쓰기 보호:
```python
if self._serial_ok and self.serial and self.serial.is_open:
    try:
        self.serial.write(data)
    except (serial.SerialException, OSError):
        self.get_logger().warn("Serial write failed during RTCM injection")
        with self._serial_lock:
            self._try_reconnect()
```

### 4.5 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select zed_f9p_rtk --symlink-install
```
```
Starting >>> zed_f9p_rtk
Finished <<< zed_f9p_rtk [4.31s]

Summary: 1 package finished [4.71s]
  1 package had stderr output: zed_f9p_rtk
```

---

## 5. 부팅 시 CH340 간헐적 미인식 진단 (04-09)

### 5.1 증상
재부팅 후 MCU 컨트롤러 보드(CH340)가 `/dev/myserial`로 인식되지 않음. 라이다(CP210x)와 RTK(FTDI)는 정상.

### 5.2 USB 장치 확인

```bash
lsusb
```
```
Bus 002 Device 002: ID 0bda:0489 Realtek Semiconductor Corp. 4-Port USB 3.0 Hub
Bus 002 Device 001: ID 1d6b:0003 Linux Foundation 3.0 root hub
Bus 001 Device 003: ID 13d3:3549 IMC Networks Bluetooth Radio
Bus 001 Device 006: ID 0079:181c DragonRise Inc. Controller
Bus 001 Device 005: ID 0403:6015 Future Technology Devices International, Ltd Bridge(I2C/SPI/UART/FIFO)
Bus 001 Device 007: ID 10c4:ea60 Silicon Labs CP210x UART Bridge
Bus 001 Device 004: ID 1a40:0101 Terminus Technology Inc. Hub
Bus 001 Device 002: ID 0bda:5489 Realtek Semiconductor Corp. 4-Port USB 2.0 Hub
Bus 001 Device 001: ID 1d6b:0002 Linux Foundation 2.0 root hub
```
→ `1a86:7523` (CH340) **없음** — USB 버스에서 아예 미감지.

### 5.3 USB 토폴로지 확인

```bash
lsusb -t
```
```
/:  Bus 02.Port 1: Dev 1, Class=root_hub, Driver=tegra-xusb/4p, 10000M
    |__ Port 1: Dev 2, If 0, Class=Hub, Driver=hub/4p, 10000M
/:  Bus 01.Port 1: Dev 1, Class=root_hub, Driver=tegra-xusb/4p, 480M
    |__ Port 2: Dev 2, If 0, Class=Hub, Driver=hub/4p, 480M
        |__ Port 1: Dev 4, If 0, Class=Hub, Driver=hub/4p, 480M
            |__ Port 1: Dev 7, If 0, Class=Vendor Specific Class, Driver=cp210x, 12M
        |__ Port 4: Dev 6, If 0, Class=Human Interface Device, Driver=usbhid, 12M
        |__ Port 2: Dev 5, If 0, Class=Vendor Specific Class, Driver=ftdi_sio, 12M
    |__ Port 3: Dev 3, If 0, Class=Wireless, Driver=rtk_btusb, 12M
```
→ Port 3에 CH340이 있어야 하나 없음 (Port 2=RTK, Port 1→서브허브→Port 1=LiDAR, Port 4=컨트롤러).

### 5.4 dmesg USB 열거 확인

```bash
sudo dmesg | grep "1-2" | head -30
```
```
[    5.308632] usb 1-2: new high-speed USB device number 2 using tegra-xusb
[    5.487062] hub 1-2:1.0: USB hub found
[    5.488183] hub 1-2:1.0: 4 ports detected
[    6.200626] usb 1-2.1: new high-speed USB device number 4 using tegra-xusb
[    6.308500] hub 1-2.1:1.0: USB hub found
[    6.308975] hub 1-2.1:1.0: 4 ports detected
[    6.392627] usb 1-2.2: new full-speed USB device number 5 using tegra-xusb
[    6.596627] usb 1-2.4: new full-speed USB device number 6 using tegra-xusb
[    6.896819] usb 1-2.1.1: new full-speed USB device number 7 using tegra-xusb
[   12.732778] usbcore: registered new interface driver ftdi_sio
[   12.741606] usbcore: registered new interface driver cp210x
[   12.748444] usb 1-2.1.1: cp210x converter now attached to ttyUSB1
[   12.789890] usb 1-2.2: FTDI USB Serial Device converter now attached to ttyUSB0
```
→ CH340 관련 메시지 0건. 포트 1-2.3이 열거되지 않음.

```bash
sudo dmesg | grep -iE "ch34"
```
→ 출력 없음 (exit code 1).

### 5.5 CH340 커널 모듈 확인

```bash
lsmod | grep ch34
```
→ 출력 없음 — 모듈 미로드 상태.

```bash
find /lib/modules/$(uname -r) -name "ch34*"
```
```
/lib/modules/5.15.148-tegra/kernel/drivers/usb/serial/ch34x.ko
```

```bash
sudo modprobe ch34x
lsmod | grep ch34
```
```
ch34x                  20480  0
usbserial              40960  7 ftdi_sio,cp210x,ch34x
```

```bash
modinfo ch34x
```
```
filename:       /lib/modules/5.15.148-tegra/kernel/drivers/usb/serial/ch34x.ko
author:         <tech@wch.cn>
description:    WCH CH34x USB to serial adaptor driver
license:        GPL
alias:          usb:v1A86p7522d*dc*dsc*dp*ic*isc*ip*in*
alias:          usb:v1A86p5523d*dc*dsc*dp*ic*isc*ip*in*
alias:          usb:v1A86p7523d*dc*dsc*dp*ic*isc*ip*in*
depends:        usbserial
name:           ch34x
vermagic:       5.15.148-tegra SMP preempt mod_unload modversions aarch64
```

### 5.6 USB 허브 autosuspend 확인

```bash
for dev in 1-2 1-2.1 1-2.2 1-2.4 1-2.1.1; do
  echo "=== $dev: delay=$(cat /sys/bus/usb/devices/$dev/power/autosuspend_delay_ms 2>/dev/null) ctrl=$(cat /sys/bus/usb/devices/$dev/power/control 2>/dev/null) ==="
done
```
```
=== 1-2: delay=0 ctrl=auto ===
=== 1-2.1: delay=0 ctrl=auto ===
=== 1-2.2: delay=2000 ctrl=on ===
=== 1-2.4: delay=2000 ctrl=on ===
=== 1-2.1.1: delay=2000 ctrl=on ===
```
→ USB 허브 1-2, 1-2.1의 `autosuspend_delay_ms=0` (즉시 suspend) — 부팅 시 장치 초기화 중 전원 차단 가능성.

### 5.7 USB 허브 전원 사양

```bash
sudo lsusb -v -d 0bda:5489 2>&1 | grep -A2 "bNbrPorts\|MaxPower\|wHubCharacteristic\|bPwrOn2PwrGood"
```
```
    MaxPower                0mA
  wHubCharacteristic 0x00a9
    Per-port power switching
    Per-port overcurrent protection
  bPwrOn2PwrGood      250 * 2 milli seconds
  bHubContrCurrent    100 milli Ampere
```

### 5.8 Port 3 상태 확인

```bash
cat /sys/bus/usb/devices/1-2/1-2:1.0/1-2-port3/connect_type
```
```
unknown
```

```bash
cat /sys/bus/usb/devices/1-2/1-2:1.0/1-2-port3/over_current_count
```
```
0
```

### 5.9 USB 허브 재열거 시도

```bash
echo "1-2" | sudo tee /sys/bus/usb/drivers/usb/unbind
sleep 2
echo "1-2" | sudo tee /sys/bus/usb/drivers/usb/bind
sleep 3
lsusb | grep -i "1a86\|ch34"
```
→ 출력 없음 — CH340 여전히 미감지. USB 소프트웨어 리셋으로는 복구 불가.

### 5.10 분석 결과

| 요인 | 상태 | 영향 |
|:---|:---:|:---|
| **물리적 접촉** | **주 원인** | Port 3 `connect_type=unknown`, USB 열거 자체 실패 |
| USB 허브 autosuspend | 부 원인 | delay=0ms, 초기화 중 전원 차단 가능 |
| CH340 초기화 속도 | 기여 | FTDI/CP210x보다 느린 것으로 알려짐 |

**사용자 확인**: 물리적 접촉 문제가 맞았음 → 케이블 재접속 후 해결.

---

## 6. USB watchdog 시도 및 제거 (04-09)

### 6.1 시도한 것들

**a) USB 허브 autosuspend 비활성화 udev 규칙 (유지)**:
```
# 99-jupiter-devices.rules에 추가
ACTION=="add", SUBSYSTEM=="usb", ATTR{idVendor}=="0bda", ATTR{idProduct}=="5489", ATTR{power/autosuspend_delay_ms}="-1", ATTR{power/control}="on"
ACTION=="add", SUBSYSTEM=="usb", ATTR{idVendor}=="1a40", ATTR{idProduct}=="0101", ATTR{power/autosuspend_delay_ms}="-1", ATTR{power/control}="on"
```

**b) USB watchdog systemd 서비스 (제거됨)**:
5초 간격으로 CH340 존재 확인, 없으면 USB 허브 unbind/bind 재열거.

```bash
# /home/jetson/jupiter_ws_ros2/scripts/usb_watchdog.sh
#!/bin/bash
HUB_DEV="1-2"
CHECK_INTERVAL=5
RESCAN_COOLDOWN=30
LAST_RESCAN=0

log() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] $1"
    logger -t usb-watchdog "$1"
}

usb_rescan() {
    local now=$(date +%s)
    local elapsed=$((now - LAST_RESCAN))
    if [ $elapsed -lt $RESCAN_COOLDOWN ]; then
        log "Rescan cooldown (${elapsed}s < ${RESCAN_COOLDOWN}s), skipping"
        return 1
    fi

    log "Triggering USB hub $HUB_DEV unbind/bind..."

    if [ -e "/sys/bus/usb/drivers/usb/$HUB_DEV" ]; then
        echo "$HUB_DEV" > /sys/bus/usb/drivers/usb/unbind 2>/dev/null
        sleep 2
        echo "$HUB_DEV" > /sys/bus/usb/drivers/usb/bind 2>/dev/null
        sleep 3
        LAST_RESCAN=$(date +%s)
        log "USB hub re-enumeration complete"
        return 0
    fi

    if [ ! -e "/sys/bus/usb/drivers/usb/$HUB_DEV" ]; then
        echo "$HUB_DEV" > /sys/bus/usb/drivers/usb/bind 2>/dev/null
        sleep 3
        LAST_RESCAN=$(date +%s)
        log "USB hub bind-only complete"
        return 0
    fi

    return 1
}

log "USB watchdog started (hub=$HUB_DEV, interval=${CHECK_INTERVAL}s, cooldown=${RESCAN_COOLDOWN}s)"

while true; do
    missing=""

    if ! lsusb -d 1a86:7523 > /dev/null 2>&1; then
        missing="${missing} CH340(MCU)"
    fi

    if [ -n "$missing" ]; then
        log "Missing USB devices:${missing} - attempting rescan"
        usb_rescan
        sleep 3

        if lsusb -d 1a86:7523 > /dev/null 2>&1; then
            log "CH340(MCU) recovered after USB rescan!"
        else
            log "CH340(MCU) still missing after rescan - check physical connection"
        fi
    fi

    sleep $CHECK_INTERVAL
done
```

```bash
# /home/jetson/jupiter_ws_ros2/scripts/usb-watchdog.service
[Unit]
Description=USB Device Watchdog - monitors and recovers missing USB serial devices
After=local-fs.target
Wants=local-fs.target

[Service]
Type=simple
ExecStart=/home/jetson/jupiter_ws_ros2/scripts/usb_watchdog.sh
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
sudo cp /home/jetson/jupiter_ws_ros2/scripts/usb-watchdog.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable usb-watchdog.service
sudo systemctl start usb-watchdog.service
```

Watchdog 실행 결과:
```
Apr 08 12:03:10 yahboom usb_watchdog.sh[32395]: [2026-04-08 12:03:10] USB watchdog started (hub=1-2, interval=5s, cooldown=30s)
Apr 08 12:03:10 yahboom usb_watchdog.sh[32395]: [2026-04-08 12:03:10] Missing USB devices: CH340(MCU) - attempting rescan
Apr 08 12:03:10 yahboom usb_watchdog.sh[32395]: [2026-04-08 12:03:10] Triggering USB hub 1-2 unbind/bind...
Apr 08 12:03:15 yahboom usb_watchdog.sh[32395]: [2026-04-08 12:03:15] USB hub re-enumeration complete
Apr 08 12:03:18 yahboom usb_watchdog.sh[32395]: [2026-04-08 12:03:18] CH340(MCU) still missing after rescan - check physical connection
```

**c) Rosmaster_Lib.py에 USB 재스캔 로직 (제거됨)**:
`os.path.exists(self.__com)` 체크 + `subprocess.run(["sudo", "tee", unbind/bind])` 호출.

### 6.2 제거 이유
물리적 접촉 문제로 확인됨 → USB 소프트웨어 리셋으로는 해결 불가. 시스템 복잡도만 증가.

### 6.3 제거 절차

```bash
sudo systemctl stop usb-watchdog
sudo systemctl disable usb-watchdog
sudo rm /etc/systemd/system/usb-watchdog.service
sudo systemctl daemon-reload
```

```bash
rm /home/jetson/jupiter_ws_ros2/scripts/usb_watchdog.sh
rm /home/jetson/jupiter_ws_ros2/scripts/usb-watchdog.service
```

Rosmaster_Lib.py에서 제거:
- `import os`, `import subprocess`
- `__last_rescan_time` 변수
- `__trigger_usb_rescan()` 메서드 전체
- `__try_reconnect()` 내 `os.path.exists()` + USB rescan 로직
- `__init__`의 초기 연결 실패 허용 로직 (원본처럼 생성자에서 바로 `serial.Serial()`)
- `__receive_data()`의 `if not self.__serial_ok or self.ser is None:` 가드
- `set_car_motion()`의 `if not self.__serial_ok or self.ser is None: return` 가드

### 6.4 유지된 것 (간단하며 유용)
- `__try_reconnect()` — 단순 close → reopen (USB 재삽입 시 자동 복구)
- `__receive_data()`/`set_car_motion()`의 `SerialException` catch → reconnect
- udev autosuspend 비활성화 규칙 (2줄)

### 6.5 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_bringup zed_f9p_rtk --symlink-install
```
```
Starting >>> jupiter_bringup
Starting >>> zed_f9p_rtk
Finished <<< zed_f9p_rtk [5.65s]
Finished <<< jupiter_bringup [5.71s]

Summary: 2 packages finished [6.19s]
  1 package had stderr output: zed_f9p_rtk
```

---

## 7. NavSatStatus 분석 및 RTK 코드 주석 수정 (04-09)

### 7.1 NavSatStatus 메시지 정의 확인

```bash
ros2 interface show sensor_msgs/msg/NavSatStatus
```
```
# Navigation Satellite fix status for any Global Navigation Satellite System.
#
# Whether to output an augmented fix is determined by both the fix
# type and the last time differential corrections were received.  A
# fix is valid when status >= STATUS_FIX.

int8 STATUS_NO_FIX =  -1        # unable to fix position
int8 STATUS_FIX =      0        # unaugmented fix
int8 STATUS_SBAS_FIX = 1        # with satellite-based augmentation
int8 STATUS_GBAS_FIX = 2        # with ground-based augmentation

int8 status

# Bits defining which Global Navigation Satellite System signals were
# used by the receiver.

uint16 SERVICE_GPS =     1
uint16 SERVICE_GLONASS = 2
uint16 SERVICE_COMPASS = 4      # includes BeiDou.
uint16 SERVICE_GALILEO = 8

uint16 service
```

### 7.2 분석 결과

**NavSatStatus에는 -1, 0, 1, 2 네 값만 존재. 3은 없음.**

| 상수 | 값 | ROS 정의 의미 | RTK 재활용 |
|:---|:---:|:---|:---|
| `STATUS_NO_FIX` | -1 | 위치 결정 불가 | No Fix |
| `STATUS_FIX` | 0 | 비보강 일반 GPS | 3D Fix (qual=1,2) |
| `STATUS_SBAS_FIX` | 1 | 위성 기반 보강 (WAAS/EGNOS) | RTK Float (qual=5) |
| `STATUS_GBAS_FIX` | 2 | 지상 기반 보강 (DGPS) | RTK Fixed (qual=4) |

- GBAS/SBAS는 원래 DGPS/WAAS를 의미하지만, ROS 메시지에 RTK 전용 상수가 없음
- 정밀도 순서를 맞춰 재활용: `NO_FIX(-1) < FIX(0) < SBAS_FIX(1) < GBAS_FIX(2)`
- 현재 코드의 매핑 자체는 **정확하며 수정 불필요**

### 7.3 주석 보완

코드의 매핑 로직은 유지하고, 주석에 "3은 없음" 및 재활용 배경을 명시:

**파일**: `src/zed_f9p_rtk/zed_f9p_rtk/zed_f9p_rtk_node.py`
```python
# GGA quality → NavSatStatus 매핑 (ublox ZED-F9P 기준)
# NavSatStatus에는 -1, 0, 1, 2 네 값만 존재 (3은 없음)
#   qual=4 → RTK Fixed  → STATUS_GBAS_FIX (2)  σ²=0.0004 (2cm급)
#   qual=5 → RTK Float  → STATUS_SBAS_FIX (1)  σ²=0.25   (50cm급)
#   qual=1,2 → 3D Fix   → STATUS_FIX (0)       σ²=6.25   (2.5m급)
#   qual=0 → No Fix     → STATUS_NO_FIX (-1)
# 참고: GBAS/SBAS는 원래 DGPS/WAAS를 의미하나, ROS 메시지에
#       RTK 전용 상수가 없어 정밀도 순서에 맞춰 재활용함
```

**파일**: `src/jupiter_nav_VO/jupiter_nav_VO/gnss_quality_filter.py`
```python
# 품질 등급 (zed_f9p_rtk_node의 NavSatStatus 매핑):
# NavSatStatus에는 -1, 0, 1, 2 네 값만 존재 (3은 없음)
#   RTK Fixed : STATUS_GBAS_FIX (2), cov=0.0004  (cm급)
#   RTK Float : STATUS_SBAS_FIX (1), cov=0.25    (dm급)
#   3D Fix    : STATUS_FIX (0),      cov=6.25    (m급)
#   No Fix    : STATUS_NO_FIX (-1)                 → 게이트아웃
# 참고: GBAS/SBAS는 원래 DGPS/WAAS 의미이나, RTK 전용 상수가 없어 재활용
```

### 7.4 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select zed_f9p_rtk jupiter_nav_VO --symlink-install
```
```
Starting >>> zed_f9p_rtk
Starting >>> jupiter_nav_VO
Finished <<< jupiter_nav_VO [5.16s]
Finished <<< zed_f9p_rtk [5.23s]

Summary: 2 packages finished [5.65s]
  2 packages had stderr output: jupiter_nav_VO zed_f9p_rtk
```

---

## 8. 최종 udev 규칙

**파일**: `99-jupiter-devices.rules`
```
# === 시리얼 장치 symlink ===
KERNEL=="ttyUSB*", ATTRS{idVendor}=="1a86", ATTRS{idProduct}=="7523", MODE="0777", SYMLINK+="myserial"
KERNEL=="ttyUSB*", ATTRS{idVendor}=="0403", ATTRS{idProduct}=="6015", MODE="0777", SYMLINK+="rtk_gps"
KERNEL=="ttyUSB*", ATTRS{idVendor}=="10c4", ATTRS{idProduct}=="ea60", MODE="0777", SYMLINK+="lidar_port"

# === USB 허브 autosuspend 비활성화 (부팅 시 장치 인식 실패 방지) ===
# Realtek 4-Port USB 2.0 Hub
ACTION=="add", SUBSYSTEM=="usb", ATTR{idVendor}=="0bda", ATTR{idProduct}=="5489", ATTR{power/autosuspend_delay_ms}="-1", ATTR{power/control}="on"
# Terminus Technology 4-Port Hub
ACTION=="add", SUBSYSTEM=="usb", ATTR{idVendor}=="1a40", ATTR{idProduct}=="0101", ATTR{power/autosuspend_delay_ms}="-1", ATTR{power/control}="on"
```

USB 장치 맵:
| Vendor:Product | 칩 | 장치 | symlink | USB 경로 |
|:---|:---|:---|:---|:---|
| `1a86:7523` | CH340 | MCU 컨트롤러 | `/dev/myserial` | 1-2.3 |
| `0403:6015` | FTDI FT231X | RTK GPS (ZED-F9P) | `/dev/rtk_gps` | 1-2.2 |
| `10c4:ea60` | CP2102N | LiDAR (RPLidar S2L) | `/dev/lidar_port` | 1-2.1.1 |

---

## 9. 수정된 파일 목록 (04-08~09)

| 파일 | 변경 내용 |
|:---|:---|
| `99-jupiter-devices.rules` | 0403:6015 symlink `myserial` → `rtk_gps` 수정, USB 허브 autosuspend 비활성화 추가 |
| `src/jupiter_bringup/jupiter_bringup/Rosmaster_Lib.py` | USB 시리얼 자동 재연결 (`__try_reconnect`) 추가 [간소화 완료] |
| `src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` | 시리얼 헬스 모니터 타이머 추가 |
| `src/zed_f9p_rtk/zed_f9p_rtk/zed_f9p_rtk_node.py` | `sys.exit(1)` 제거, 시리얼 자동 재연결 추가, NavSatStatus 주석 보완 |
| `src/jupiter_nav_VO/jupiter_nav_VO/gnss_quality_filter.py` | NavSatStatus 주석 보완 (3은 없음 명시) |
| `src/jupiter_nav_VO/config/nav2_params_fused.yaml` | global_costmap obstacle_layer 제거 |
| `src/jupiter_nav_VO/jupiter_nav_VO/goal_pose_restamper.py` | rclpy.shutdown() 이중호출 크래시 수정 |
| `scripts/usb_watchdog.sh` | 생성 후 **제거** |
| `scripts/usb-watchdog.service` | 생성 후 **제거** |

---

## 10. 향후 작업

1. **왼쪽 바퀴 문제**: 동일 cmd_vel에 대해 왼쪽 모터가 일관된 결과를 내지 않음 (미동작이 제일 많음). Rosmaster_Lib.py 수정과 무관 → MCU 펌웨어 또는 ESC/모터 하드웨어 확인 필요.
2. **linear+angular 동시 동작 불량**: 전진하면서 회전이 안 됨 → MCU의 모터 PID 또는 ESC 데드존 문제 가능성.
3. **RTK 실테스트**: 실외에서 `/fix` 토픽의 status.status 값 확인 (0, 1, 2 중 하나여야 함).
