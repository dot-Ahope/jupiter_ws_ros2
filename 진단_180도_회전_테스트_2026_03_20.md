# 180° 제자리 회전 테스트 분석 보고서 (2026-03-20)

## 1. 테스트 개요

| 항목 | 값 |
|------|-----|
| 날짜 | 2026-03-20 12:41:32 |
| 테스트 내용 | NavigateToPose 180° 제자리 회전 |
| 결과 | **SUCCEEDED** (4.3초) |
| 데이터 | 312 rows CSV, 50Hz 수집 |
| CSV 경로 | `test_results/rotation_180_test_20260320_124132.csv` |

### 실행 중인 주요 파라미터

```
min_speed_theta      = 0.15   (0.30에서 변경 — 이번 세션)
max_vel_theta        = 1.0
vtheta_samples       = 20
sim_time             = 1.5
angular_dist_threshold = 0.35 (20°)
rotate_to_heading_angular_vel = 0.5
yaw_goal_tolerance   = 0.15 rad (8.6°)
controller_frequency = 10 Hz
velocity_smoother    = OPEN_LOOP, freq=20Hz
  max_accel[θ]       = 2.5
  max_decel[θ]       = -2.5
  deadband[θ]        = 0.05
```

---

## 2. 테스트 데이터 요약

### 2.1 회전 궤적

```
초기 odom yaw:  -18.3°
최종 odom yaw:   142.5°
odom 총 회전량:  160.9°
목표 (map 프레임): 161.7°

map→odom yaw offset: 18.6° (누적 드리프트)
실제 map 프레임 오차: ~0.5° (양호)
odom 프레임 오차:    19.1° (offset 포함)
```

### 2.2 타임라인

| 시간 (s) | 이벤트 | cmd_vel_nav_wz | odom_wz | error (odom) |
|--------:|--------|---------------|---------|-------------|
| 0.00 | 목표 전송 | 0.0 | 0.0 | 180° |
| 0.10 | DWB 첫 출력 | **1.0** | 0.0 | 180° |
| 0.14 | 스무더 가속 시작 | 1.0 | 0.0 | 180° |
| 0.50 | 스무더 1.0 도달 | 1.0 | 0.005 | 180° |
| 0.76 | **모터 응답 시작** (odom>0.1) | 1.0 | 0.1+ | 178° |
| 2.00 | 정상 회전 중 | 1.0 | 0.84 | 142° |
| 2.50 | 정상 회전 중 | 1.0 | 0.95 | 115° |
| 2.90 | **DWB 감속 시작** | 0.94 | 1.01 | 92° |
| 3.10 | 피크 odom 속도 | 0.81 | **1.12** | 80° |
| 3.50 | 급격한 지연 | 0.43 | 0.83 | 57° |
| 4.00 | 지연 지속 | 0.15 | 0.68 | 34° |
| 4.29 | **DWB 정지 명령** | **0.0** | **0.56** | 24° |
| 4.34 | 스무더→0 | 0.0 | 0.56 | 23° |
| 4.51 | **모터 정지** (odom<0.05) | 0.0 | 0.05 | 19° |
| 6.31 | 데이터 수집 종료 | 0.0 | 0.0 | 19.1° |

---

## 3. 발견된 문제

### 문제 P1: RotationShim 미활성화 (심각도: 높음)

**현상**: DWB가 `max_vel_theta=1.0`으로 직접 회전 (RotationShim의 0.5가 아닌 2배 속도)

**근거**:
- `cmd_vel_nav_wz`의 첫 출력이 **1.0** (RotationShim이라면 0.5여야 함)
- t=0.1~2.9까지 2.8초간 일정한 1.0 출력 (RotationShim 패턴)
- 하지만 `rotate_to_heading_angular_vel=0.5` → 출력이 일치하지 않음

**원인 분석**:
```
NavigateToPose → Planner → 경로 생성
            ↓
같은 위치 목표 → 경로 길이 0 → 거의 단일 점
            ↓
RotationShim: forward_sampling_distance(0.3m) 지점을 경로에서 찾을 수 없음
            ↓
RotationShim 비활성화 → DWB에 직접 전달
            ↓
DWB RotateToGoal: 180° 오차 → max_vel_theta=1.0 선택
```

**영향**:
- 회전 속도 2배 → 모터 추종 불가능 → P2, P3 악화
- DWB만으로 180° 회전 → RotationShim의 안정적인 속도 제어를 건너뜀

---

### 문제 P2: 심각한 모터 속도 추종 지연 (심각도: 높음)

**현상**: DWB가 감속 명령을 내려도 모터가 빠르게 따라가지 못함

**근거 데이터**:

```
시간     명령(cmd_wz)   실제(odom_wz)   지연
t=3.2    0.62            1.07          +0.45 rad/s (모터 72% 더 빠름)
t=3.8    0.29            0.77          +0.49 rad/s
t=4.0    0.15            0.68          +0.53 rad/s (모터 4.5배 더 빠름!)
t=4.3    0.00            0.56          +0.56 rad/s (정지 명령인데 0.56)
```

**정량 분석**:
- 최대 추종 지연: **1.0 rad/s** (시동 시)
- 감속 구간 지연: 0.45~0.56 rad/s
- 적분 추가 회전량: **94.6°** (시동 지연 포함)
- 모터 시동 지연: **0.62초** (cmd>0.1 → odom>0.1)

**원인 체인**:
```
cmd_vel → ANGULAR_SCALE_FACTOR(×0.401) → MCU serial → PID → LPF(α=0.3) → ESC → 모터
                                                ↕ PID 적분 누적
                                                ↕ LPF 77ms to 90%
                                                ↕ 기계적 관성
```

- **Motor LPF (α=0.3)**: 10ms 주기에서 90% 도달에 77ms, 99%에 153ms
- **MCU PID 지연**: 증분식 PID `Δpwm = Kp(e-e') + Ki·e + Kd(e-2e'+e'')` 로 100Hz 출력하나, 적분 누적 모멘텀 때문에 감속 시 과응답
- **기계적 관성**: 차동구동 양 바퀴의 회전 관성

---

### 문제 P3: 관성 코스팅 (심각도: 중간)

**현상**: Nav2 정지 명령 후 모터가 관성으로 추가 회전

**근거**:
```
t=4.29: cmd_vel=0 전송, odom_wz=0.556 rad/s (31.9°/s)
t=4.51: odom_wz < 0.05 (정지)
코스트 시간: 0.22초
코스트 추가 회전: 5.2° (odom 기준)
```

**영향**: DWB가 정확히 목표에 정지시키려 해도, 관성으로 5°+ 추가 회전 발생

---

### 문제 P4: PID 오버슈트 (심각도: 낮음)

**현상**: 정상상태 속도에서 모터가 명령보다 빠르게 회전

**근거**:
```
명령: 1.0 rad/s (cmd_vel_wz)
실제 피크: 1.118 rad/s (odom_wz, t=3.114s)
오버슈트: +11.8%
```

**원인**: MCU PID의 Ki 적분항이 시동 시 누적된 오차를 보상하면서 과보상

---

### 문제 P5: odom→map yaw 드리프트 (심각도: 참고)

**현상**: odom 프레임과 map 프레임 간 18.6° yaw 오프셋

**근거**:
```
map→odom transform yaw: 18.6°
(tf2 lookup으로 확인)
```

**비고**: 
- 이번 테스트에서 발생한 것이 아닌 **이전 주행에서 누적된 오프셋**
- 이번 회전 자체의 odom 정확도는 양호 (~0.5° map 오차)
- 하지만 테스트 스크립트가 odom 좌표로 목표를 설정하고 map 프레임으로 선언해서 160.9° 회전 (의도한 180°가 아님)

---

## 4. 문제 구조도

```
NavigateToPose (same position, 180° rotation)
    │
    ├─ P1: RotationShim 미활성화
    │   └─ DWB 직접 제어 → max_vel_theta=1.0 (의도한 것의 2배)
    │       │
    │       ├─ P2: 모터 속도 추종 불가
    │       │   ├─ Motor LPF α=0.3 (77ms to 90%)
    │       │   ├─ PID 적분 관성
    │       │   └─ acc/decel_lim_theta(0.8) > 모터 실제 능력(~0.6)
    │       │
    │       ├─ P3: 관성 코스팅 (5.2°)
    │       │   └─ cmd=0인데 odom_wz=0.56 → 0.22초 후 정지
    │       │
    │       └─ P4: PID 오버슈트 (+11.8%)
    │           └─ Ki 적분 누적 → 과보상
    │
    └─ P5: odom yaw 드리프트 (18.6°, 누적)
```

---

## 5. 해결 방안

### 방안 S1: max_vel_theta 1.0 → 0.5 감소 ★★★ (가장 효과적)

**대상 문제**: P1, P2, P3, P4 동시 완화

**변경**:
```yaml
# nav2_params_fused.yaml → FollowPath
max_vel_theta: 0.5    # 현재 1.0 → 0.5
```

**효과 분석**:
| 항목 | 현재 (1.0) | 변경 후 (0.5) |
|------|-----------|-------------|
| 최대 각속도 | 1.0 rad/s (57°/s) | 0.5 rad/s (28.6°/s) |
| 모터 추종성 | 불량 (0.56 lag) | 양호 (예상 0.1~0.2 lag) |
| 관성 코스팅 | 5.2° | 예상 1~2° |
| PID 오버슈트 | 11.8% | 예상 5% 이하 |
| 180° 회전 시간 | ~4.3초 | 예상 ~7초 |
| 속도 샘플 step | 0.1053 | 0.0526 (2배 정밀) |
| DWB dead zone | 없음 (6.7° < 8.6°) | 없음 (6.7° < 8.6°) ✓ |

**RotationShim과 통일**: `rotate_to_heading_angular_vel=0.5`와 동일 → RotationShim 활성/비활성 관계없이 일관된 속도

**Dead zone 검증**:
```
max_vel_theta=0.5, vtheta_samples=20 → step=0.0526
min_speed_theta=0.15 → 최소 유효 샘플: 0.1579
sim rotation: 0.1579 × 1.5 = 0.2369 rad = 13.6°
crossover: 6.7° < yaw_goal_tolerance(8.6°) → Dead zone 없음 ✓
```

---

### 방안 S2: decel_lim_theta를 모터 실제 능력에 맞춤 ★★

**대상 문제**: P2

**변경**:
```yaml
# nav2_params_fused.yaml → FollowPath
decel_lim_theta: -0.5   # 현재 -0.8 → -0.5
```

**근거**: 모터 시스템의 실제 감속률 ≈ 0.625 rad/s² (1.0→0 in 1.6s).
DWB가 -0.5로 시뮬레이션하면 모터가 따라올 수 있는 궤적을 계획.

**효과**: DWB 감속 시 명령-실제 간 지연 감소 → 더 예측 가능한 정지

---

### 방안 S3: velocity_smoother CLOSED_LOOP 전환 ★

**대상 문제**: P2, P3

**변경**:
```yaml
# nav2_params_fused.yaml → velocity_smoother
feedback: "CLOSED_LOOP"   # 현재 OPEN_LOOP → CLOSED_LOOP
```

**현재 문제**: OPEN_LOOP에서 스무더는 내부 가상 속도를 추적. 모터가 느려 실제 속도와 괴리.
- 스무더: "나는 이미 0으로 감속했다" → 0 출력
- 모터: "아직 0.56으로 돌고 있다" → 코스팅

**CLOSED_LOOP 효과**: 스무더가 odom 피드백으로 실제 속도를 추적. 모터가 실제로 감속할 때까지 적절한 감속 명령 유지.

**주의**: odom 노이즈가 클 경우 진동 가능. 테스트 후 판단.

---

### 방안 S4: velocity_smoother max_decel 감소 ★

**대상 문제**: P3

**변경**:
```yaml
# nav2_params_fused.yaml → velocity_smoother
max_decel: [-0.5, 0.0, -1.0]   # 현재 [-0.5, 0.0, -2.5] → θ: -2.5→-1.0
```

**근거**: 현재 angular max_decel=2.5는 모터 실제 능력(~0.6)의 4배. 
1.0으로 감소해도 정지 시간 증가는 미미하나 모터 추종성 향상.

---

## 6. 권장 적용 순서

### Phase 1 (즉시 적용 — 가장 안전한 단일 변경)

```yaml
max_vel_theta: 0.5    # S1
```

**검증**: 180° 회전 테스트 재실행, 모터 추종 지연 확인

### Phase 2 (Phase 1 테스트 후)

```yaml
decel_lim_theta: -0.5    # S2
```

**검증**: 감속 구간에서 cmd vs odom 추종 비교

### Phase 3 (선택적)

```yaml
feedback: "CLOSED_LOOP"              # S3
max_decel: [-0.5, 0.0, -1.0]        # S4
```

**검증**: 실내 자율주행 전체 경로 테스트

---

## 7. 데이터 해석 참고

### 실제 Nav2 성능 평가

이 테스트에서 Nav2는 **map 프레임** 기준으로 약 0.5° 오차로 목표에 성공적으로 도달했음. 
스크립트가 표시한 19.1° 오차는 odom→map yaw 오프셋(18.6°) 때문이며, Nav2의 실제 정확도가 아님.

```
Nav2 관점 (map 프레임):
  목표 heading: 161.7°
  최종 heading: ~161.2°
  오차: ~0.5° → yaw_goal_tolerance(8.6°) 내 → SUCCEEDED ✓
  
테스트 스크립트 관점 (odom 프레임):
  목표 heading: 161.7° (map 값을 odom으로 착각)
  최종 heading: 142.5°
  오차: 19.1° → 하지만 이는 odom→map offset
```

### min_speed_theta=0.15 효과 확인

이전의 DWB dead zone (8.6°~14.3° 영역에서 정지) 문제는 이번 테스트에서 **관찰되지 않음**.
DWB가 목표 근처에서 0.0842 rad/s까지 점진적으로 감속한 후 정지 → dead zone fix 유효 ✓

---

## 8. 첨부 파일

| 파일 | 설명 |
|------|------|
| `test_results/rotation_180_test_20260320_124132.csv` | 원본 데이터 |
| `scripts/test_rotation_180.py` | 테스트 스크립트 |
| `scripts/analyze_rotation_test.py` | 분석 스크립트 |
