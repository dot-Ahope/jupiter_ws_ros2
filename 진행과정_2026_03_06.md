# 진행과정 2026-03-06

## 1. 오늘 작업 목표
1. transform_tolerance 3.0 → 0.5 축소 (stamp=Time(0,0) + NTP 적용 후 불필요한 여유 제거)
2. 5차 실주행 테스트 및 로그/CSV 분석
3. 분석 결과 기반 파라미터 수정
4. 6차 테스트 검증 및 추가 수정

---

## 2. 수행 명령어 및 진단 과정 (시간순)

### 2.1 Phase 1: transform_tolerance 축소

#### 명령 1: nav2_params_fused.yaml 내 transform_tolerance 검색
```bash
grep -n 'transform_tolerance' /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml
```
- **목적**: 변경 대상 위치 파악
- **결과**: 5개 위치 확인 (controller_server DWB, planner_server, behavior_server, local_costmap, global_costmap)

#### 명령 2: transform_tolerance 일괄 변경 (5개 위치)
```
# 편집기에서 직접 수정 (replace_string_in_file × 5)
```
- **목적**: 3.0 → 0.5 축소
- **근거**: stamp=Time(0,0) + NTP chrony(<1ms) 적용 후, 4차 테스트에서 관측된 TF 최대 지연 ~40ms → 3.0s 여유 불필요

#### 명령 3: symlink 전파 확인
```bash
diff <(sed -n '140p' install/.../nav2_params_fused.yaml) <(sed -n '140p' src/.../nav2_params_fused.yaml)
```
- **목적**: colcon build --symlink-install로 빌드된 환경에서 src/ 수정이 install/에 자동 반영되는지 확인
- **결과**: 차이 없음 (symlink 정상) → 빌드 불필요

---

### 2.2 Phase 2: 5차 테스트 Nav2 로그 분석

> 사용자가 5차 실주행 Nav2 로그와 plot_data.csv 제공

#### 명령 4: 로그에서 goal 수신/완료/실패 이벤트 추출
```
# Nav2 로그 텍스트에서 수동 파싱 (사용자 제공 로그 직접 분석)
```
- **목적**: 각 goal의 성공/실패 판별, 시간 순서 파악
- **결과**:

| Goal | 좌표 | 결과 | 소요시간 | 비고 |
|------|------|------|----------|------|
| 1 | (0.74, -0.15) | SUCCESS ✅ | ~4s | 단거리 |
| **2** | **(0.33, -0.24)** | **FAIL ❌** | **~96s** | **5회 "Failed to make progress"** |
| 3 | (-0.21, -0.50) | SUCCESS ✅ | ~5s | Goal 2 preempt |
| 4 | (1.07, 0.01) | SUCCESS ✅ | ~10s | |
| 5 | (-0.41, -0.39) | SUCCESS ✅ | ~10.5s | |

---

### 2.3 Phase 3: CSV 센서 데이터 분석

#### 명령 5: CSV 기본 정보 확인
```bash
cd /home/jetson/jupiter_ws_ros2 && wc -l plot_data.csv && head -3 plot_data.csv
```
- **목적**: 데이터 규모 및 컬럼 구조 파악
- **결과**: 15430행, 컬럼 = elapsed_time, topic, x, y, z, qx, qy, qz, qw

#### 명령 6: 토픽별 샘플 수/주파수/시간범위 요약
```python
python3 -c "
import csv
from collections import defaultdict
# ... 토픽별 count, min_t, max_t 집계 후 Hz 계산
"
```
- **목적**: 각 센서의 동작 기간과 주파수 확인
- **결과**:

| Topic | 샘플수 | 주파수 | 시간범위 | 판정 |
|-------|--------|--------|----------|------|
| /odom (EKF) | 4843 | ~30Hz | 22.9~184.1s | ✅ 정상 |
| /odom_adapted | 8451 | ~49Hz | 11.9~184.1s | ✅ 정상 |
| /odom_rf2o_adapted | 1696 | ~9.9Hz | 11.9~184.1s | ✅ 전 세션 (respawn 작동) |
| /odom_vslam_adapted | **439** | **~5.5Hz** | **22.0~101.6s** | ⚠️ **43% only** |

#### 명령 7: VSLAM 갭/동결 분석
```python
python3 -c "
# /odom_vslam_adapted 토픽에서 연속 샘플 간 시간차 > 3s인 갭 검출
# 동일 qz값이 연속 반복(threshold=10)되는 구간 → frozen 판별
"
```
- **목적**: VSLAM이 101.6s에서 소실된 원인 단서 파악, frozen detection 검증
- **결과**:
  - 3회 갭 >3s 검출 (일시적 tracking loss)
  - 3회 단기 동결 (7, 5, 9 샘플) → threshold=10이므로 정상 차단됨
  - 101.6s 이후 완전 소실 — cuVSLAM tracking 영구 loss 추정

#### 명령 8: heading wrap 검출
```python
python3 -c "
# /odom (EKF) 토픽의 qz 값에서 연속 부호 반전 횟수 카운트
# sign(qz[i]) != sign(qz[i-1])인 지점 추출
"
```
- **목적**: 3/4에 빌드한 base.cpp heading normalization 수정이 실제 적용되었는지 확인
- **결과**: t=118.4부터 149회 부호 반전 → **base_node 미재시작 확인** (빌드는 됐으나 실행 중인 노드에 미반영)

---

### 2.4 Phase 4: Goal 2 실패 심층 분석

#### 명령 9: Goal 2 구간 EKF qz 시계열 추출 (t=40~140)
```python
python3 -c "
# /odom 토픽에서 t=40~140 구간의 qz, qw 추출
# yaw = 2 * atan2(qz, qw) 변환 후 5초 간격 출력
"
```
- **목적**: Goal 2 시도 중 로봇 heading이 실제로 얼마나 변했는지 정량 파악
- **결과**: t=40(yaw=-2.2°) → t=56(yaw=-22.7°) → t=72(-17.8°) → 목표 -160° 근처 未도달

#### 명령 10: 상세 EKF qz (t=40~58, 500+ 샘플)
```python
python3 -c "
# /odom 토픽에서 t=40~58 구간의 모든 샘플 추출
# 각 샘플의 yaw(deg) 출력
"
```
- **목적**: Goal 2 첫 시도에서 회전이 언제 시작되고 언제 멈추는지 정밀 관찰
- **결과**:
  - t=40~44: qz≈-0.019 (정지, yaw=-2.2°)
  - t=44.3: 회전 시작
  - t=48: yaw=-16° (감속)
  - t=50.2: 순간 burst 0.499 rad/s → yaw=-22°
  - **t=51+: 완전 정지 (yaw=-22.8° 고정)**
  - 달성 회전: 21° / 필요 158° = **13%**

#### 명령 11: 회전 속도 계산 (t=44~52)
```python
python3 -c "
# 연속 샘플 간 yaw 차이 / 시간 차이 → 즉시 각속도(rad/s) 계산
# 피크, 평균, 시간별 분포 출력
"
```
- **목적**: 명령 angular.z=0.5 대비 실제 달성 각속도 정량화
- **결과**:
  - **피크**: 0.499 rad/s (t=50.2, 1샘플 burst)
  - **지속 평균**: 0.04~0.18 rad/s (명령의 8~36%)
  - **결론**: 0.5 rad/s 명령으로는 정지마찰을 지속적으로 극복 불가

#### 명령 12: 리트라이 경계 heading 확인
```python
python3 -c "
# 각 리트라이 시작 시점(t≈42, 56, 72, 88, 103, 110, 128, 137)의 EKF qz→yaw 추출
"
```
- **목적**: 5회 리트라이 간 heading이 누적 진행되는지, 후퇴하는지 확인
- **결과**:
  ```
  t≈42:  yaw=-2.2°    (초기)
  t≈56:  yaw=-22.7°   (1차 시도 → 21° 진행)
  t≈72:  yaw=-17.8°   (오히려 후퇴 — backup 반동)
  t≈88:  yaw=-25.5°   (최대 진행)
  t≈103: yaw=-19.3°   (다시 후퇴)
  t≈110: yaw=-14.1°   (backup 후)
  t≈128: yaw=+177.1°  (heading wrap 발생)
  ```
  - 리트라이마다 backup 반동으로 heading이 후퇴 → 누적 진행 없음

---

### 2.5 Phase 5: 1차 수정 — rotate_to_heading_angular_vel 상향

#### 명령 13: nav2_params_fused.yaml 수정
```
# 편집기에서 직접 수정 (replace_string_in_file)
```

**수정 전**:
```yaml
rotate_to_heading_angular_vel: 0.5 # ⭐ 정지 회전 고정 속도 — 모터 데드존(~0.15) 확실히 초과
```

**수정 후**:
```yaml
rotate_to_heading_angular_vel: 0.8 # ⭐ 정지 회전 고정 속도 — 0.5에서 실패(5차 테스트: 실제 0.05~0.18만 달성). 0.8로 상향
```

- **수정 근거**:
  - 5차 테스트에서 0.5 명령 → 실제 0.05~0.18 rad/s만 달성 (10~36%)
  - 0.8로 올리면 예상 실제 ~0.3~0.5 → 정지마찰 확실히 지속 극복
  - `max_vel_theta=1.0` 범위 이내, `behavior_server.max_rotational_vel=0.8`과 동일하게 통일

#### 명령 14: symlink 전파 확인
```bash
diff <(sed -n '114,116p' install/.../nav2_params_fused.yaml) <(sed -n '114,116p' src/.../nav2_params_fused.yaml)
```
- **목적**: install/ 디렉토리에 변경 반영 확인
- **결과**: 차이 없음 → 빌드 불필요, Nav2 재시작만 필요

---

### 2.6 Phase 6: 6차 테스트 및 cmd_vel 모니터링

> Nav2 재시작 후 6차 실주행. 사용자가 cmd_vel echo와 Nav2 로그 동시 모니터링.

#### 6차 테스트 Nav2 로그 분석

| Goal | 좌표 | 결과 | 비고 |
|------|------|------|------|
| 1 | (0.21, -0.04) | **FAIL** → 복구 → SUCCESS | 시작 위치 맵 밖 → SLAM 맵 확장 후 자동 해결 |
| 2 | (0.21, 1.77) | PREEMPT | Goal 1 주행 중 preempt |
| 3 | (0.32, 2.08) | **FAIL ❌** | "Failed to make progress" × 2 + backup 실패 |
| 4 | (0.16, 0.57) | **FAIL** → 복구 → SUCCESS | "Failed to make progress" × 1 → recovery 후 성공 |
| 5 | (0.24, 1.35) | SUCCESS ✅ | ~7s |

**Goal 1 상세**: 로봇 X좌표(-0.03~-0.15)가 global_costmap 경계(-0.03) 밖 → "robot's start position is off the global costmap" → clearCostmap 후 StaticLayer Resizing으로 맵 확장 → 이후 정상 주행

**Goal 3 상세**:
- 1차: "Failed to make progress" (15s timeout)
- clearCostmap → 2차: 다시 "Failed to make progress"
- backup → "Exceeded time allowance before reaching the DriveOnHeading goal" → backup 실패
- → Goal failed (abort)

**Goal 4 상세**:
- "Failed to make progress" 1회 → backup 후 복구 진행 → Goal succeeded

#### 핵심 발견: cmd_vel angular.z가 0.8에 미도달

사용자가 cmd_vel echo에서 관찰한 실측 데이터 (Goal 3 실패 구간):
```
angular.z = 0.324 → 0.351 → 0.466 → 0.466 → 0.466
         → 0.477 → 0.477 → 0.507 → 0.507 → 0.549
         → 0.549 → 0.545 → 0.624 → 0.624 → 0.644
         → 0.644 → 0.644 → 0.658 → 0.742 → 0.742
         → 0.742
```
- **관찰**: 0.324에서 시작해 0.742까지 서서히 증가하지만 **목표 0.8에 도달하지 못함**
- **진단**: 가속도 리미터 + EKF 피드백 루프 문제

---

### 2.7 Phase 7: angular.z 미도달 근본 원인 분석

#### 분석: RotationShimController 가속도 리미터 메커니즘

RotationShimController 소스 코드의 속도 클램핑 로직:
```cpp
// velocity = 현재 EKF 오도메트리의 실측 각속도
max_feasible = velocity.angular.z + max_angular_accel * dt;
min_feasible = velocity.angular.z - max_angular_accel * dt;
cmd_vel.angular.z = clamp(target_vel, min_feasible, max_feasible);
```

`max_angular_accel=1.6`, `controller_frequency=10Hz (dt=0.1s)` → 매 틱 최대 **±0.16 rad/s** 증가

**데드록 피드백 루프**:

| 실제 EKF 각속도 | max_feasible (actual+0.16) | 출력 cmd | 0.8 도달? |
|-----------------|---------------------------|----------|-----------|
| 0.00 | 0.16 | 0.16 | ❌ |
| 0.10 | 0.26 | 0.26 | ❌ |
| 0.18 | **0.34** | **0.34** | ❌ ← 실측 0.324 일치! |
| 0.30 | **0.46** | **0.46** | ❌ ← 실측 0.466 일치! |
| 0.50 | **0.66** | **0.66** | ❌ ← 실측 0.658 일치! |
| 0.58 | **0.74** | **0.74** | ❌ ← 실측 0.742 일치! |
| **0.64** | **0.80** | **0.80** | ✅ |

**결론**: 모터 데드존(정지마찰)으로 실제 각속도가 낮음 → 리미터가 명령도 낮게 제한 → 실제 각속도 여전히 낮음 → **자기 강화 데드록**

---

### 2.8 Phase 8: 2차 수정 — max_angular_accel 상향

#### 명령 15: nav2_params_fused.yaml 수정
```
# 편집기에서 직접 수정 (replace_string_in_file)
```

**수정 전**:
```yaml
max_angular_accel: 1.6             # 회전 가속도 제한
```

**수정 후**:
```yaml
max_angular_accel: 5.0             # ⭐ 1.6→5.0: 가속 리미터=actual_vel+accel*dt, 1.6일 때 actual 0.3이면 cmd 0.46 한계→0.8 미도달. 5.0이면 actual 0.3→cmd 0.8 즉시 도달
```

- **수정 근거**:
  - accel=5.0, dt=0.1 → 매 틱 ±0.50 rad/s 증가 허용
  - actual=0.0 → cmd=min(0.50, 0.8)=0.50 (1틱)
  - actual=0.3 → cmd=min(0.80, 0.8)=**0.80** ✅ (즉시 도달)
  - `rotate_to_heading_angular_vel=0.8`이 상한이므로 과도한 명령 발생 없음

#### 명령 16: symlink 전파 확인
```bash
grep -n 'max_angular_accel\|rotate_to_heading' install/.../nav2_params_fused.yaml
```
- **목적**: install/ 반영 확인
- **결과**: line 114: 0.8, line 115: 5.0 확인 → 정상 반영

---

## 3. 전체 수정 내역 (수정 전/후 비교)

### 3.1 nav2_params_fused.yaml 변경 요약

| # | 파라미터 | 수정 전 | 수정 후 | 위치 | 근거 |
|---|----------|---------|---------|------|------|
| 1 | `transform_tolerance` (5곳) | `3.0` | `0.5` | controller DWB, planner, behavior, local/global costmap | stamp=Time(0,0)+NTP 적용 후 TF 최대지연 ~40ms, 3.0s 불필요. 5차 테스트 검증 완료 |
| 2 | `rotate_to_heading_angular_vel` | `0.5` | `0.8` | FollowPath (RotationShimController) | 5차 테스트: 0.5 명령 → 실제 0.05~0.18만 달성. 158° 회전에서 21°만 진행 후 정지 |
| 3 | `max_angular_accel` | `1.6` | `5.0` | FollowPath (RotationShimController) | 6차 테스트: 0.8 명령인데 cmd_vel 0.324~0.742만 출력. 가속 리미터+EKF 피드백 데드록 |

### 3.2 수정 파일 상세

**파일**: `src/jupiter_nav_VO/config/nav2_params_fused.yaml`

수정 후 RotationShimController 섹션 (최종):
```yaml
    FollowPath:
      plugin: "nav2_rotation_shim_controller::RotationShimController"
      primary_controller: "dwb_core::DWBLocalPlanner"
      angular_dist_threshold: 0.785      # 45° (변경 없음)
      forward_sampling_distance: 0.5     # (변경 없음)
      rotate_to_heading_angular_vel: 0.8 # 0.5→0.8
      max_angular_accel: 5.0             # 1.6→5.0
      simulate_ahead_time: 1.0           # (변경 없음)
```

---

## 4. 진단 방법론 요약

### 4.1 문제 발견 → 근본 원인 추적 흐름

```
Nav2 로그 "Failed to make progress"
  ↓ [명령 4] 로그에서 goal별 타임라인 파싱
Goal 2 실패 (158° 회전 필요)
  ↓ [명령 9] CSV에서 EKF qz 시계열 추출 (t=40~140)
실제 heading 21°만 진행
  ↓ [명령 10] 상세 샘플 (t=40~58, 500+ 점)
t=51에서 완전 정지
  ↓ [명령 11] 즉시 각속도 계산
실제 0.05~0.18 rad/s (명령 0.5의 10~36%)
  ↓ [진단] RotationShimController 0.5이 정지마찰 극복 부족
수정 1: rotate_to_heading_angular_vel 0.5→0.8
  ↓ [6차 테스트] cmd_vel echo 모니터링
angular.z = 0.324→0.742 (0.8 미도달!)
  ↓ [분석] 소스 코드 가속 리미터 로직 분석
cmd = clamp(0.8, actual-accel*dt, actual+accel*dt); accel=1.6, dt=0.1
  ↓ [진단] 실측 데이터와 이론값 대조 → 완전 일치
수정 2: max_angular_accel 1.6→5.0
```

### 4.2 CSV 기반 센서 진단 방법

| 진단 항목 | 사용 명령 | 판별 기준 |
|-----------|-----------|-----------|
| 센서 생존 기간 | 토픽별 min_t~max_t | 전체 세션 대비 커버리지 % |
| 센서 주파수 | count / (max_t - min_t) | 기대 주파수 대비 편차 |
| 갭·단절 | 연속 샘플 간 dt > 3s | 3s 이상 갭 = tracking loss 추정 |
| 동결 감지 | 동일 값 연속 반복 >= N | threshold 10 → frozen 구간 |
| Heading wrap | qz 부호 반전 빈도 | base_node 수정 적용 여부 |
| 실제 회전 속도 | Δyaw/Δt (인접 샘플) | 명령 대비 달성 비율 |

---

## 5. Phase 4: VSLAM 추적 소실 진단 및 수정

### 5.1 증상
- 5차 테스트에서 VSLAM 커버리지 43% (22~101.6초, 전체 184초 중)
- 나머지 57% 구간에서 VSLAM 데이터 없음 → EKF가 wheel+RF2O만으로 동작

### 5.2 진단 명령 및 결과

#### 명령 17: Docker 내 VSLAM 토픽 흐름 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 3 ros2 topic echo /visual_slam/tracking/odometry --once'
```
- **목적**: cuVSLAM이 odometry를 발행하고 있는지 확인
- **결과**: EXIT=124 (타임아웃) — 데이터 없음

#### 명령 18: Docker 내 노드 생존 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 node list'
```
- **목적**: visual_slam_node, camera/camera 노드가 살아있는지 확인
- **결과**: 두 노드 모두 ALIVE — 노드는 살아있으나 데이터 미발행

#### 명령 19: 카메라 스트림 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   timeout 3 ros2 topic echo /camera/infra1/image_rect_raw --once'
```
- **목적**: RealSense IR 이미지 스트림 확인
- **결과**: EXIT=124 — 카메라 스트림 중단 (노드 alive but 데이터 없음)

#### 명령 20: USB 하드웨어 확인
```bash
lsusb | grep -i "intel\|realsense"
```
- **목적**: RealSense D455F가 물리적으로 연결되어 있는지 확인
- **결과**: `Bus 002 Device 003: Intel(R) RealSense(TM) Depth Camera 455f` — 정상 연결

#### 명령 21: Docker 로그 분석
```bash
docker logs --tail 100 isaac_ros_dev-aarch64-container
```
- **목적**: cuVSLAM 초기화/에러 메시지, 카메라 에러 확인
- **결과**:
  - cuVSLAM 정상 초기화 (v12.6, GPU warmup 0.18s)
  - **24건 frame jitter 경고**: `Delta between current and previous frame [XX ms] is above threshold [22.000000 ms]`
  - 시작 5초 이내에 24건 밀집 → 카메라 프레임 전달 불안정
  - 이후 3건 산발적 (t=263, 343, 388)

#### 명령 22: 커널 USB 에러 확인
```bash
sudo dmesg | grep -iE "usb|uvc"
```
- **목적**: USB disconnect, firmware crash, UVC 에러 확인
- **결과**: 정상 (disconnect/error 없음)

### 5.3 근본 원인 분석

```
RealSense D455F: 640×360 @ 90fps (Stereo IR)
  → USB 3.2 대역폭: ~41.5 MB/s 지속 전송 (640×360×1byte × 2ch × 90fps)
  → Jetson Orin USB 3.2 컨트롤러 부하
  → 프레임 전달 지터 증가 (22ms 임계값 초과 24건)
  → 카메라 드라이버 내부 큐 포화
  → 스트리밍 파이프라인 hang (노드 alive, 데이터 0)
  → cuVSLAM 추적 소실
```

**핵심**: 90fps에서 USB 대역폭 압박 → 프레임 지터 → 스트리밍 hang → 추적 소실

### 5.4 수정 내용

#### 수정 3: 카메라 FPS 90→60 + Jitter 임계값 22→34ms

**파일**: `isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_visual_slam_realsense.launch.py`

**수정 전**:
```python
'depth_module.profile': '640x360x90',
# ...
'image_jitter_threshold_ms': 22.00,
```

**수정 후**:
```python
'depth_module.profile': '640x360x60',  # ⭐ 90→60fps: USB 대역폭 33% 감소
'image_jitter_threshold_ms': 34.00,    # ⭐ 22→34ms: 60fps(16.7ms)의 2x 임계값
```

**근거**:
| 항목 | 90fps | 60fps |
|------|-------|-------|
| 프레임 간격 | 11.1ms | 16.7ms |
| USB 대역폭 | ~41.5 MB/s | ~27.6 MB/s (**-33%**) |
| Jitter 임계값 | 22ms (2x) | 34ms (2x) |
| 시작 시 jitter 경고 | 24건 | **4건** (시작 직후만) |

#### 명령 23: Docker 내 빌드
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && cd /workspaces/isaac_ros-dev && \
   colcon build --packages-select isaac_ros_visual_slam --symlink-install'
```
- **결과**: 빌드 성공 (0.84s)

#### 명령 24: RealSense USB 리셋
```bash
sudo python3 /home/jetson/reset_realsense.py
```
- **목적**: 스트리밍 hang 상태의 USB 디바이스 리셋
- **결과**: Successfully reset /dev/bus/usb/002/003

#### 명령 25: VSLAM 재시작
```bash
docker exec -d isaac_ros_dev-aarch64-container bash -c \
  'source /opt/ros/humble/setup.bash && source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py'
```
- **목적**: 수정된 60fps 설정으로 cuVSLAM 재시작
- **결과**: cuVSLAM 초기화 OK, **60Hz 안정 발행 확인**, jitter 경고 4건(시작 직후만)

#### 명령 26: 호스트 VSLAM 수신 확인
```bash
timeout 5 ros2 topic echo /visual_slam/tracking/odometry --once --no-arr
```
- **결과**: 60Hz odometry 정상 수신 (Docker → Host DDS 통신 OK)

### 5.5 VSLAM 수정 검증 요약

| 지표 | 수정 전 (90fps) | 수정 후 (60fps) |
|------|-----------------|-----------------|
| 시작 시 jitter 경고 | 24건 | **4건** |
| 운영 중 jitter 경고 | 3건 (불규칙) | **0건** (10분+ 가동) |
| VSLAM 발행 주파수 | 5.5Hz (불안정) → hang | **60Hz** (안정) |
| USB 대역폭 | ~41.5 MB/s | ~27.6 MB/s |

### 5.6 DDS 통신 참고사항

Docker↔Host 간 DDS 통신 시, 아래 SHM 경고가 출력되지만 **동작에는 영향 없음**:
```
[RTPS_TRANSPORT_SHM Error] Failed init_port fastrtps_port7411: open_and_lock_file failed
```
- **원인**: SHM 포트 잠금 파일 충돌로 표시되는 경고. UDP fallback으로 자동 전환됨.
- **정상 동작**: `FASTRTPS_DEFAULT_PROFILES_FILE` 설정 없이도 Docker↔Host 통신 정상 동작 확인
- **주의**: VSLAM 프로세스를 `kill -9`로 강제 종료 시 DDS SHM 잠금 파일이 해제되지 않아
  후속 프로세스의 DDS discovery가 지연될 수 있음. 정상 종료(Ctrl+C) 사용 권장.

> **오진 정정**: 이전에 `FASTRTPS_DEFAULT_PROFILES_FILE`를 UDP 전용으로 설정해야 한다고 기록했으나,
> 이는 `kill -9`로 인한 DDS stale state가 원인이었으며 정상 시작/종료 시에는 불필요함.

---

## 6. 미해결 항목 (우선순위)

| # | 항목 | 상태 | 영향도 |
|---|------|------|--------|
| 1 | rotate_to_heading_angular_vel 0.5→0.8 | ✅ 완료 | HIGH |
| 2 | max_angular_accel 1.6→5.0 | ✅ 완료 | HIGH |
| 3 | transform_tolerance 3.0→0.5 | ✅ 검증 완료 | MED |
| 4 | VSLAM FPS 90→60, jitter 22→34ms | ✅ **완료** | **HIGH** |
| 5 | base_node 재시작 (heading wrap) | 📋 다음 테스트 | MED |
| 6 | RF2O 패치 (acos NaN 등) | 📋 미착수 | MED |
| 7 | 7차 테스트 (전체 수정 통합 검증) | 📋 다음 작업 | **CRITICAL** |
