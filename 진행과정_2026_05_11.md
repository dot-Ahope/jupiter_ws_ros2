# 진행과정 2026-05-11: 회전 / 주행 부드러움 종합 개선 (Stage 0~8 + PID Ki 환원)

## 1. 목표

### 1.1 사용자 요청 (시간순)

1. > "새로운 bag를 만들었다. 2번의 움직임을 명령했는데, 2번째 명령에서, 180도 회전 후 전진 후 목표지에서 다시 회전 명령을 내렸다. 그런데 처음 회전까지 시간이 오래 걸리고, 완전히 회전하기도 전에 전진해버리고, 그러다보니 경로를 벗어나서 움직이는 모습을 보여준다. 해당 문제의 원인을 분석하고, 해결할 방법을 찾아라. 그리고 전체 코드를 분석하여, 기계의 원할한 주행을 위해 무엇이 필요할 지 분석 및 계획을 세워라." (Plan mode 진입)
2. > "단계 1 진행하라" (Stage 1)
3. > "plan대로 진행하자" (Stage 2)
4. > "옵션 C를 진행한다" (Stage 5* — RUNNING_MIN 0.20 우선 적용)
5. > "옵션 2 진행" (Stage 3)
6. > "권장사항대로 진행한다" (Stage 5* update 2 — RUNNING_MIN 0.30)
7. > "권장사항대로 진행하라" (Stage 4)
8. > "권장사항대로 진행하라" (Stage 4 보강 — smoother / behavior hard cap)
9. > "권장사항을 진행하라" (Stage 6 — sign-flip 코드)
10. > "stage 7, 8, 9 전부 진행하도록 하자. 순서대로 진행해야 하는가. 한번에 진행해도 되는가."
11. > "옵션 A 진행" (Stage 7+8, Stage 9 보류)
12. > "1. 펌웨어 플래시 완료했다. 2. robot 재기동 및 stage 8 캘리브레이션 진행했다. 3. 무한루프에 걸려 강제종료했다." (Stage 8 캘리브레이션)
13. > "옵션 A 진행" (SCALE 0.93)
14. > "테스트를 진행하여 bag를 만들었다."
15. > "둘의 차이가 다른 이유 추론" (mapping vs localization)
16. > "ros2 launch /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py은 mapping 모드를 사용한다. 실제로 그래서 생긴 문제일 가능성이 크다고 본다."
17. > "Localization mode 로 재측정을 진행하여, bag를 만들었다."
18. > "운영 상태가 전혀 우수하지 않다. path를 제대로 따라가지 못하고, 이상움직임을 계속 보여준다. nav2 로그를 읽어보아라."
19. > "수행하라" (TF publish 0.1 → 0.05)
20. > "새로운 bag를 기록했다 확인해봐라. 그리고 spin을 이상하게 한다던가, spin을 이상한 상황에 한다던가, path를 따라가지 못하고 오른쪽으로 더 편향되어 전진한다던가 하는 문제가 발생한다. bag와 log등을 살펴보아라. 그리고 pid의 문제인지도 조사해봐라."
21. > "권장 옵션 진행" (TF 환원)
22. > "옵션 A 적용하라" (Ki 0.02 → 0.06 환원)
23. > "bag + nav2 log 기록 완료. path 추종 개선 보임, 편향 잔존하는 것 같음. 직접 기록을 보고 분석하라."
24. > "우선 오늘 한 것들을 정리해달라" (현재)

### 1.2 이전 세션 요약 / 연관 작업

- `/home/jetson/jupiter_ws_ros2/진행과정_2026_05_08.md`: SLAM strict + Path inflation 조정 + forced_recovery 비활성화 + firmware 보정 환원 (M3 mechanical 정상 확인)
- 본 세션은 사용자 보고 "180° 회전 후 전진 시 path 벗어남" 의 종합 해결. Plan mode 로 Stage 0~9 단계적 진행.

## 2. 대상 시스템 구성

### 2.1 변경 영향 layer

```
[운영]
ros2 launch /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py \
    slam_mode:=localization \
    slam_map:=/home/jetson/maps/jupiter_demo

[변경된 layer]
1. SLAM (slam_toolbox)              — slam_params.yaml (TF 0.05 시도→0.1 환원)
2. Nav2 controller (MPPI + Shim)    — nav2_params_fused.yaml (Stage 3,4,4 보강)
3. Nav2 velocity_smoother + behavior — nav2_params_fused.yaml (Stage 4 보강)
4. Driver (jupiter_driver_compensated.py) — Stage 1, 5*, 6, 8, A
5. MCU firmware                    — bsp_motor.c filter α (Stage 7, 사용자 Keil 플래시 완료)
```

### 2.2 cmd_vel pipeline (현재)

```
nav2 controller → /cmd_vel → collision_monitor → /cmd_vel_nav_filtered ──┐
joy → /cmd_vel_joy (bypass) ──────────────────────────────────────────┼→ twist_mux → /cmd_vel_safe → driver
forced_recovery (비활성) ─────────────────────────────────────────────┘
```

driver 의 cmd_vel 구독은 `/cmd_vel_safe` 로 remap (launch 명령: `-r /cmd_vel:=/cmd_vel_safe`).

## 3. 문제 분석

### 3.1 이슈 — 정지 회전 후 전진 시 path 벗어남 (사용자 핵심 보고)

- **증상**: 180° 회전 명령 → robot 이 처음 회전까지 시간 오래 걸림 → 완전 회전 전 전진 시작 → path 이탈
- **bag timeline 분석** (이전 세션):
  - 8초 무응답 (cmd ω=-0.125 → actual ω=0)
  - 빠른 회전 (-0.125 ramp up → -0.582)
  - Premature forward (yaw -119°, 목표 -180°)
  - Over-correction (cmd ω=+1.000 max → -169° → +1°)
- **수학적 / 코드상 원인 (다층)**:
  - Driver `ROT_KICK_CONFIRM_THRESHOLD=0.10` 너무 큼 → confirm 늦음
  - Driver `ROT_RUNNING_MIN_ANGULAR=0.0` → 작은 cmd 가 motor deadzone 미달
  - Nav2 `RotationShim.angular_dist_threshold=0.20` (11°) ↔ `yaw_goal_tolerance=0.32` (18°) gap 7°
  - Nav2 `MPPI.wz_max=1.0` + `velocity_smoother.max_velocity[z]=1.0` → panic ω 명령 통과
  - Driver brake 단계 inertia 잔류 — sign-flip 시 잔류 ω 잔존
  - Firmware `MOTOR_FILTER_ALPHA=0.3` — 응답 지연 230ms 의 일부
  - Driver `ANGULAR_SCALE_FACTOR=1.0` — 재캘리브 안 됨

### 3.2 이슈 — nav2 의 spin 이상 trigger (TF 환원 후 발견)

- **증상**: 정상 path 추종 중 갑자기 spin → 멈춤 → 따닥따닥 움직임
- **수학적 / 코드상 원인**:
  - Driver `Ki=0.02` (2026-03-19 변경)
  - cmd vx=0.2 → actual 도달까지 5초 ramp-up
  - nav2 progress_checker (5cm/0.5s 기준) 통과 못 함
  - BT 가 stuck 판정 → Spin recovery 발동 (잘못된 trigger)
- **근본 원인**: Ki=0.02 가 CAR_DIFFERENTIAL + ESC skip 환경 (2026-03-19) 전제. 현재 CAR_MECANUM 사용 중이라 가정 무관.
- **보조 원인**: balance correction reset 빈번, filter α 변경 후 PID 동작 변화.

### 3.3 이슈 — TF lookup 실패 (사용자 "이상 동작" 핵심 원인)

- **증상**: localization mode 운영 중 robot 의 따닥따닥 움직임, path 추종 끊김
- **로그 증거**:
  ```
  [ERROR] [controller_server]: Exception in transformPose: Lookup would require 
  extrapolation into the future. Requested time X but the latest data is at time Y, 
  when looking up transform from frame [map] to frame [odom]
  [ERROR] [controller_server]: Unable to transform goal pose into costmap frame
  [WARN] [controller_server]: [follow_path] [ActionServer] Aborting handle.
  ```
  132s 에 12회 abort + 21회 control loop missed
- **수학적/코드상 원인**: `slam_params.yaml:27 transform_publish_period=0.1` (10Hz) vs `nav2_params_fused.yaml:88 controller_frequency=20.0` (20Hz)
  - 매 50ms controller query, 100ms TF update
  - 매 두 번째 tick 이 future timestamp lookup → 실패
- **초기 의심**: 단순 SLAM 매칭 jitter
- **정정 후**: TF publish rate 와 controller rate mismatch 가 원인 (clean root cause)

### 3.4 이슈 — TF 0.05 변경 후 CPU 과부하 (시행착오)

- **증상**: TF 0.05 (20Hz) 변경 후 robot 이 더 이상 동작 — spin / 멈춤 빈발
- **로그 증거**:
  ```
  [WARN] [BehaviorTreeEngine]: Behavior Tree tick rate 100.00 was exceeded!
  [WARN] [bt_navigator]: Timed out while waiting for action server to acknowledge goal request for spin/backup/wait
  [ERROR] [bt_navigator]: Goal failed
  ```
  Control loop missed 48회 (이전 21회의 2배+)
- **근본 원인**: TF publish 2배 (10Hz→20Hz) 로 SLAM CPU 부하 ↑ → BT/action server 가 따라가지 못함
- **조치**: TF 0.1 환원. 옵션 B (controller 10Hz + model_dt 0.1 + time_steps 24) 는 MPPI 제약으로 동시 변경 필요 → 보류.

### 3.5 이슈 — 오른쪽 편향 (사용자 보고, 잔존)

- **증상**: 직진 명령에도 robot 이 오른쪽으로 휨
- **데이터**:
  - 직진 시 IMU yaw rate 평균 **-2.05°/s** (시계방향)
  - 좌/우 wheel diff +3.38% (좌측 |속도| 작음)
  - nav2 의 우회전 cmd 61.3% (보상 시도)
  - Path lateral drift: 70cm 직진에 15cm 측면 drift (12° 편향)
- **수학적/코드상 원인**:
  - Mechanical 좌/우 비대칭 (motor 또는 deadzone 차이)
  - `app_pid.c` 4 motor 동일 PID gain → 비대칭 보상 못 함
  - `app_mecanum.c` balance correction gain=0.02 → 보정 부족
- **잠정 근본 원인**: 좌측 motor sluggish + balance correction 약함. **Stage 9 (PID gain 좌/우 분화) 또는 balance correction gain 증가 필요**.

## 4. 적용한 변경

### 4.1 Plan 작성 (Plan mode)

`/home/jetson/.claude/plans/functional-sleeping-starfish.md`

- Phase 1: Explore agent 3개 병렬 (driver / nav2 / firmware)
- Phase 2: Plan agent 1개 — Stage 0~9 implementation 계획
- Phase 3: AskUserQuestion — firmware 플래시 상태, 진행 단위, A3.4 도입
- Phase 4: Plan file 작성
- Phase 5: ExitPlanMode → 사용자 승인

### 4.2 Stage 1 — driver kick confirm threshold

`/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

| 라인 | 파라미터 | 변경 전 | 변경 후 |
|---|---|---|---|
| 84 | `ROT_KICK_CONFIRM_THRESHOLD` | 0.10 | **0.06** |

**의도**: bsp_motor 의 filter (Stage 7 이전 α=0.3) 응답 230ms 안에서 0.06 도달 → kick 빠른 종료.
**결과**: Latency 1641ms → 13ms (median).

### 4.3 Stage 2 — kick startup 시간 (REVERTED)

| 라인 | 파라미터 | 변경 시도 | 결과 |
|---|---|---|---|
| 80 | `ROT_STARTUP_KICK_SEC` | 0.40 → 0.25 | Motion 안 시작 0/10 → 13/19 (68%!) — **0.40 환원** |

### 4.4 Stage 3 — nav2 RotationShim + PreferForward

`/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

| 라인 | 파라미터 | 변경 전 | 변경 후 |
|---|---|---|---|
| 136 | `RotationShim.angular_dist_threshold` | 0.20 | **0.30** |
| 209 | `PreferForwardCritic.cost_weight` | 5.0 | **3.0** |

**의도**: shim 작동 영역 11° → 17° 로 확장. yaw_goal_tolerance (18°) 와 gap 1.3°.

### 4.5 Stage 4 — MPPI wz_max + smoother accel

| 라인 | 파라미터 | 변경 전 | 변경 후 |
|---|---|---|---|
| 163 | `MPPI.wz_max` | 1.0 | **0.6** |
| 476 | `velocity_smoother.max_accel[z]` | 2.5 | **1.5** |
| 477 | `velocity_smoother.max_decel[z]` | -2.5 | **-1.5** |

**결과**: smoother accel 효과 명확 (decay 642ms → 356ms). 단 wz_max 0.6 단독 효과 미흡 (cmd 1.0 그대로 통과).

### 4.6 Stage 4 보강 — Hard cap 적용

| 라인 | 파라미터 | 변경 전 | 변경 후 |
|---|---|---|---|
| 329 | `behavior_server.max_rotational_vel` | 0.8 | **0.6** |
| 474 | `velocity_smoother.max_velocity[z]` | 1.0 | **0.6** |
| 475 | `velocity_smoother.min_velocity[z]` | -1.0 | **-0.6** |

**결과**: cmd ω hard cap 0.6 작동, panic 영역 차단. Premature forward 의 큰 actual ω 잔류 (>1.0) **0건**.

### 4.7 Stage 5* — Plan 우회 (RUNNING_MIN 0.20 → 0.30)

`/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

| 라인 | 파라미터 | 변경 전 | 변경 후 |
|---|---|---|---|
| 77 | `ROT_RUNNING_MIN_ANGULAR` | 0.0 | **0.30** |

**의도**: Plan 의 0.08 은 cmd 0.125 영역 boost 안 됨. motor deadzone 통과 임계 0.30 (측정값) 으로 강화.
**결과**: ≥3초 무응답 5회 → 3회 (Stage 5* update 1), 그 후 → motor 응답 보장.

### 4.8 Stage 6 — Driver sign-flip 0 cycle 코드

`/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

```python
# __init__ 에 추가:
self._last_cmd_omega = 0.0

# cmd_vel_callback 의 set_car_motion 직전:
if self._last_cmd_omega * angular_corrected < 0 and abs(self._last_cmd_omega) > 0.10:
    self.bot.set_car_motion(vx, vy, 0.0)
    self._last_cmd_omega = 0.0
    return
self._last_cmd_omega = angular_corrected
```

**결과**: Over-rotation max 6.41 → **3.61x** (44% 감소). Premature forward 8 → 5건.

### 4.9 Stage 7 — Firmware filter α (사용자 Keil 플래시 완료)

`/home/jetson/mcu/extbd/Source/BSP/bsp_motor.c`

| 라인 | 파라미터 | 변경 전 | 변경 후 |
|---|---|---|---|
| 15 | `MOTOR_FILTER_ALPHA` | 0.3f | **0.5f** |

**의도**: 응답 시간 90% 도달 77ms → 33ms.
**결과**: Decay 시간 198~492ms → **76ms** (localization mode).

### 4.10 Stage 8 — ANGULAR_SCALE 재캘리브

calibrate_angular.py 실행 시도:
- 사용자: cmd ω=0.5 rad/s for 360° goal
- **무한 루프 발생** — script 의 wraparound 처리 안 됨
- 로그 데이터에서 직접 분석 → 5 cycle 평균 11.64s/360° → ratio 1.079

`/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

| 라인 | 파라미터 | 변경 전 | 변경 후 |
|---|---|---|---|
| 30 | `ANGULAR_SCALE_FACTOR` | 1.0 | **0.93** |

**의도**: 8% over-rotation 보정.
**결과**: Localization mode 측정 ratio median 1.077 (예상 일치). 

### 4.11 Stage 9 (PID gain 좌/우 분화) — **Skip**

- 측정 변동성 큼: Bag 1 +3.61% / Bag 2 -1.87% / localization +0.97%
- 부호 변동 → 진짜 mechanical asymmetry 식별 어려움
- Plan 의 결정: skip

(편향 잔존 발견 후 옵션 B 로 재고려 됐으나 미적용)

### 4.12 SLAM TF publish period (시행착오)

`/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml`

| 라인 | 파라미터 | 시도 | 결과 |
|---|---|---|---|
| 27 | `transform_publish_period` | 0.1 → **0.05** (20Hz) | CPU 과부하 — BT tick 100Hz 못 따라감, action timeout → **0.1 환원** |

근본 해결 (옵션 B: controller 10Hz + model_dt 0.1 + time_steps 24) 는 MPPI 제약으로 동시 변경 필요. 보류.

### 4.13 옵션 A — PID Ki 환원

`/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py`

| 라인 | 파라미터 | 변경 전 | 변경 후 |
|---|---|---|---|
| 166 | `Ki` (declare_parameter default) | 0.02 | **0.06** (환원) |

**의도**: cmd vx 응답 ramp-up 시간 5초 → 1~2초. progress_checker 정상 통과 → BT Spin recovery 잘못 trigger 사라짐.
**결과**: Spin recovery 0회, 4/4 Goal succeeded, BT tick rate exceeded 1회만.

## 5. 빌드 및 검증

### 5.1 빌드 (각 변경 후)

```bash
cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_bringup --symlink-install
# Stage 1, 5*, 6, 8, A — 각 4.5~5.0s

cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_nav_VO --symlink-install
# Stage 3, 4, 4 보강 — 각 4.5~5.0s

cd /home/jetson/jupiter_ws_ros2 && colcon build --packages-select jupiter_nav --symlink-install
# TF publish 변경/환원 — 각 4.5~5.0s
```

모두 성공.

### 5.2 Firmware 빌드 / 플래시 (Stage 7)

- Jetson 에 ARM toolchain 없음 → Windows + Keil µVision 필요
- 사용자 작업: `bsp_motor.c` 수정본 → Keil 빌드 → STM32 플래시
- 사용자 보고: "펌웨어 플래시 완료" (Stage 7 적용됨)

### 5.3 검증 시나리오 (각 stage 후)

```bash
ros2 launch /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py \
    [slam_mode:=localization slam_map:=/home/jetson/maps/jupiter_demo]
```

사용자 시연:
- Stage 0~6: mapping mode (default)
- Stage 7+8 검증 일부: localization mode 도 사용
- 최종 검증: localization mode

### 5.4 검증 결과 (시점별)

| 측정 시점 | Spin 이상 | Over-rot max | Premature fwd | IMU drift | 좌/우 차 |
|---|---|---|---|---|---|
| 시작 baseline | 발생 | (큼) | 다수 | -3.28°/s | +3.61% |
| Stage 6 후 | — | **3.61x** | 5건 | — | — |
| Stage 7+8 mapping | — | 6.41~8.62x | 6~9건 | -3.28~+0.84°/s | -1.87~+3.61% |
| Stage 7+8 localization | — | **5.41x** | 8건 | **-0.79°/s** | **+0.97%** |
| TF 0.05 (시도) | **빈발** (CPU) | — | — | — | — |
| TF 환원 + Ki 0.02 | spin 1회 | — | — | -1.74°/s | +3.14% |
| **Ki 0.06 최종** | **0회** | — | — | **-2.05°/s** | +3.38% |

## 6. 최종 결론

### 6.1 오늘 해결된 항목

1. **8초 무응답** — Stage 5* update 2 (`ROT_RUNNING_MIN_ANGULAR=0.30`)
2. **Premature forward 의 큰 잔류 ω (>1.0)** — Stage 4 보강 (`velocity_smoother.max_velocity z=±0.6` hard cap)
3. **Over-correction oscillation (cmd ±1.000 panic)** — Stage 4 보강 (`max_rotational_vel=0.6`)
4. **회전을 이상하게 / 이상한 상황에 spin** — Ki 0.06 환원 (PID 응답 정상화)
5. **회전 정확도** — Stage 8 (ANGULAR_SCALE=0.93, 8% over 보정)
6. **Decay 시간** — Stage 7 (filter α 0.5) → 76ms
7. **forced_recovery ↔ nav2 충돌** — (이전 세션 이미 비활성화 유지)

### 6.2 오늘 새로 확인된 항목

1. **TF publish rate vs controller frequency mismatch** — `transform_publish_period=0.1` (10Hz) vs `controller_frequency=20.0` (20Hz) → 매 두 번째 controller tick 이 future timestamp lookup → follow_path Aborting. **132s 에 12회 abort + 21회 control loop missed**.
2. **localization vs mapping mode 변동성** — mapping mode 의 측정 결과는 path replanning + map matching jump 영향. localization 가 안정 측정.
3. **MPPI controller_period ≤ model_dt 제약** — controller_frequency 10 단독 변경 시 lifecycle configure 실패. model_dt 와 함께 변경 필요.
4. **calibrate_angular.py 의 wraparound bug** — 360° 이상 회전 측정 불가, 무한 루프.
5. **driver 가 `/cmd_vel_safe` 구독** — launch 의 `-r /cmd_vel:=/cmd_vel_safe` remap. bag 의 `/cmd_vel` 은 nav2 raw intent.
6. **stage 9 (PID 좌/우 분화) 측정 신뢰도** — Bag 별 부호 변동 (+3.61% ↔ -1.87% ↔ +0.97%) → 진짜 mechanical asymmetry 식별 어려움.

### 6.3 남은 제약 / 주의

1. **오른쪽 편향 잔존** — IMU drift -2.05°/s 평균, 70cm 직진에 15cm lateral drift. balance correction (gain 0.02) 으론 보정 부족.
2. **TF lookup abort 잔존** — 132s 에 5~7회. controller_frequency 변경 (옵션 B) 안 했으면 잔존. 운영 가능 수준이지만 path 추종 일부 끊김.
3. **localization mode 권장** — mapping mode 의 변동성 회피.
4. **사용자가 launch 시 slam_mode 미지정** — default mapping. 권장: `slam_mode:=localization slam_map:=/home/jetson/maps/jupiter_demo`.
5. **Stage 9 (PID 좌/우 분화) 미적용** — Plan 결정 skip. 편향 잔존 시 재고려.
6. **calibrate_angular.py 의 wraparound bug 미수정**.

### 6.4 다음 분석 우선순위

1. **오른쪽 편향 해결 (옵션 D, B, C 중 선택)**:
   - 옵션 D: Driver 단 yaw correction (Python, IMU drift 기반 보정, 즉시)
   - 옵션 B: Stage 9 PID gain 좌/우 분화 (Firmware Keil 필요)
   - 옵션 C: Balance correction gain 0.02 → 0.05 (Firmware Keil 필요)
2. **TF lookup abort 잔존 해결** — 옵션 B (controller 10Hz + model_dt 0.1 + time_steps 24) 동시 변경
3. **calibrate_angular.py wraparound fix** — 양방향 측정으로 정확한 SCALE 산출
4. **localization mode 운영 안정성 검증** — 장시간 운영 시 mapping 누적 없이 잘 작동하는지

## 7. 생성 / 수정 파일 목록

### 7.1 수정된 파일

#### ROS 패키지
- `/home/jetson/jupiter_ws_ros2/src/jupiter_bringup/jupiter_bringup/jupiter_driver_compensated.py` (Stage 1, 5*, 6, 8, A)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` (Stage 3, 4, 4 보강)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml` (TF publish 시행착오)

#### Firmware
- `/home/jetson/mcu/extbd/Source/BSP/bsp_motor.c` (Stage 7 — filter α 0.5, 사용자 Keil 플래시 완료)

#### Plan
- `/home/jetson/.claude/plans/functional-sleeping-starfish.md` (Plan 작성 + 실행 로그 누적)

### 7.2 생성된 문서

- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_05_11.md`
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_05_11.md` (이 파일)
