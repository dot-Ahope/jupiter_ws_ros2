# 진행과정 2026-03-18: 방안A 펌웨어 플래시 → Plan A → Goal2 데드존 재진단 → 3중 수정 → 무한회전 수정

> 이 문서는 2026-03-18 세션의 전체 작업 진행과정을 기록합니다.
> MCU 펌웨어(방안A)가 STM32에 플래시되었으며, ROS 워크어라운드를 정리(Plan A)한 뒤,
> test_deadzone_bypass.py Goal2에서 재발한 데드존+헌팅 문제를 진단하고 3가지 수정을 적용합니다.
> 이후 무한회전 문제를 진단하고 방안B에 감속 감지 로직을 추가합니다.

---

## 목차
1. [방안A 펌웨어 플래시 완료](#1-방안a-펌웨어-플래시-완료)
2. [Plan A 실행: ROS 워크어라운드 정리](#2-plan-a-실행-ros-워크어라운드-정리)
3. [Foxglove 수동 주행: 데드존 문제 없음](#3-foxglove-수동-주행-데드존-문제-없음)
4. [test_deadzone_bypass.py Goal2: 데드존+헌팅 재발](#4-test_deadzone_bypasspy-goal2-데드존헌팅-재발)
5. [근본 원인 진단: DWB 최소 샘플 + velocity_smoother 미설정 + forward_sampling_distance](#5-근본-원인-진단-dwb-최소-샘플--velocity_smoother-미설정--forward_sampling_distance)
6. [3중 수정 적용](#6-3중-수정-적용)
7. [무한회전 진단 및 수정: 방안B 감속 감지](#7-무한회전-진단-및-수정-방안b-감속-감지)
8. [현재 적용 상태 요약](#8-현재-적용-상태-요약)

---

## 1. 방안A 펌웨어 플래시 완료

### 1.1 플래시된 변경사항 (`bsp_motor.c`)

| 변경 ID | 내용 | 효과 |
|---------|------|------|
| **A-1** | `Motor_Ignore_Dead_Zone()` PID→ESC 비례매핑 | 0/72/144 이산값 → 연속 비례 출력 |
| **A-2** | `Motor_Set_Pwm()` M1/M3 ESC 직접 출력 | Differential 모드 A채널 정상화 |
| **A-3** | `MOTOR_FILTER_ALPHA` 0.1→0.3 | motor filter 응답 230ms→77ms |

### 1.2 펌웨어 효과 확인

- **헌팅(좌우진동) 소멸**: Foxglove로 수동 목표 전송 시 좌우 진동 없음 ✅
- **PID 윈드업 사이클 해소**: 비례매핑으로 0/72/144 양자화 제거됨 ✅
- **잔여 문제**: ESC 중립(108) 근처에서 물리적 토크 미발생 (Section 4 참조)

---

## 2. Plan A 실행: ROS 워크어라운드 정리

### 2.1 삭제: 방안B-2 (Anti-Hunting Filter) — 전체 제거

헌팅이 더 이상 발생하지 않으므로 완전 삭제.

| 삭제 항목 | 위치 | 줄 수 |
|-----------|------|-------|
| `self._ah_*` 상태변수 초기화 | `__init__()` | 5줄 + 주석 4줄 |
| `_anti_hunting_filter()` 메서드 | 클래스 본문 | ~55줄 |
| `self._anti_hunting_filter()` 호출 | `cmd_vel_callback()` | 1줄 + 주석 2줄 |

### 2.2 초기 방안B 조정: 0.25 → 0.15

펌웨어 비례매핑 적용으로 더 작은 값으로 충분하다고 판단, MIN_ANGULAR_DEADZONE = 0.15로 설정.

### 2.3 복원: Nav2 파라미터 — 헌팅 대책 해제

| 파라미터 | 방안B-2때 (03-17) | 복원값 (03-18) |
|----------|-------------------|----------------|
| `max_vel_theta` | 0.7 | **1.0** |
| `acc_lim_theta` | 0.5 | **0.8** |
| `decel_lim_theta` | -0.5 | **-0.8** |
| `Twirling.scale` | 15.0 | **5.0** |
| `Oscillation.scale` | 5.0 | **1.0** |

---

## 3. Foxglove 수동 주행: 데드존 문제 없음

드라이버 재시작 후 Foxglove에서 `/goal_pose_raw`로 좌표를 보내 Nav2 주행:
- **결과**: 데드존 문제 없이 정상 주행 ✅
- **이유**: RotationShimController가 180° 헤딩 차이를 감지 → `rotate_to_heading_angular_vel = 0.8 rad/s`로 일관된 방향 회전 → PID 누적 → ESC 활성화

---

## 4. test_deadzone_bypass.py Goal2: 데드존+헌팅 재발

### 4.1 테스트 실행 결과

```
--- Goal1: 0.5m forward (0.010, 0.535, 109.9) ---
Goal1: SUCCEEDED in 2.7s (VSLAM adapted: 0)

--- Goal2: Return to (0.180, 0.065, 109.9) ---
Goal2: TIMEOUT in 61.1s (VSLAM adapted: 0)
  Yaw span during Goal2: 3.4 deg
  Final distance to start: 0.484m
```

### 4.2 cmd_vel 관찰

Goal2 실행 중 `/cmd_vel`에서 관찰된 패턴:
```
angular.z = -0.052631578947368474   (14회 반복)
angular.z = +0.052631578947368474   (21회 반복)
angular.z = -0.052631578947368474   (10회 반복)
... (반복)
```

- **±0.0526 rad/s로만 교대** (정확히 `1/19 = 0.052631...`)
- **linear.x = 0.0** (순수 제자리 회전 시도)
- **로봇은 좌우로 움찔거릴 뿐 실제 회전 안 됨**
- **yaw_span = 3.4°** → 회전 통제됨 (이전 359°와 다름), 그러나 **목표 도달 불가**

### 4.3 Foxglove와의 차이

| | Foxglove 수동 목표 | test_deadzone_bypass.py |
|---|---|---|
| RotationShim 작동 | ✅ 0.8 rad/s 일관 방향 | ❌ DWB가 ±0.0526 교대 |
| 회전 성공 | ✅ 정상 | ❌ 실패 (헌팅) |
| 경로 차이 | 충분한 거리 → 명확한 방향 | **0.5m 복귀** → 짧은 경로 |

---

## 5. 근본 원인 진단: DWB 최소 샘플 + velocity_smoother 미설정 + forward_sampling_distance

### 5.1 angular.z = 1/19 = 0.0526의 출처

DWB controller의 각속도 샘플링 구조:
```
max_vel_theta = 1.0 rad/s
vtheta_samples = 20
→ 샘플 간격 = 1.0 / (20-1) = 1/19 = 0.05263 rad/s
→ 첫 non-zero 샘플 = ±0.05263 (DWB가 출력 가능한 최소 non-zero 각속도)
```

DWB 가속 제한:
```
acc_lim_theta = 0.8 rad/s²
controller_frequency = 10 Hz
→ 1 cycle 최대 변화 = 0.8 / 10 = 0.08 rad/s
→ 첫 샘플 0.0526 < 0.08 → OK (feasible)
→ 둘째 샘플 2/19 = 0.1053 > 0.08 → IMPOSSIBLE (정지 상태에서)
→ DWB는 0 또는 ±0.0526만 출력 가능
```

### 5.2 RotationShimController가 작동하지 않는 이유

```
forward_sampling_distance = 0.5m  (변경 전)
Goal2 거리 = 0.5m (0.5m 전진 후 복귀)
```

NavFn이 0.5m 직선 경로를 생성 → RotationShimController가 `forward_sampling_distance=0.5m` 앞을 보면 **경로 끝(목표 지점)**에 도달 → 방향이 모호 → RotationShim 작동 안 함 → DWB가 직접 처리 → ±0.0526 교대 헌팅

Foxglove에서는 일반적으로 더 먼 목표를 보내며, 경로가 길어서 RotationShim이 명확한 방향을 감지하고 `rotate_to_heading_angular_vel=0.8`로 정상 회전.

### 5.3 velocity_smoother 미설정 문제

Nav2 토픽 흐름 (navigation_launch.py에 하드코딩):
```
controller_server → /cmd_vel_nav → velocity_smoother → /cmd_vel → jupiter_driver → MCU
                    (cmd_vel remap)                     (cmd_vel_smoothed remap)
```

`nav2_params_fused.yaml`에 `velocity_smoother:` 섹션이 **없었음**:
- velocity_smoother가 **기본값**(max_accel:[2.5, 0.0, 3.2])으로 실행
- DWB `acc_lim_theta=0.8`이지만 smoother는 `3.2`까지 허용 → 불일치
- 로봇 max_vel_theta=1.0이지만 smoother 기본 max_velocity가 다를 수 있음

### 5.4 signal flow 전체 추적

```
[DWB 출력]
  angular.z = 0.0526 rad/s (첫 non-zero 샘플)

[velocity_smoother → /cmd_vel]
  angular.z = 0.0526 (기본 파라미터라 그대로 통과)

[jupiter_driver cmd_vel_callback]
  angular_corrected = 0.0526 × 0.353 = 0.01858 rad/s

[방안B (MIN_ANGULAR_DEADZONE=0.15)]
  0.005 < 0.01858 < 0.15 → 부스트: angular_corrected = 0.15

[MCU set_car_motion(0, 0, 0.15)]
  Differential_Ctrl: speed_spin = (0.15/1000) × 164.555mm = 0.0247 mm/s
  → PID 목표 ≈ 0.025 mm/s (극소)
  → PID 출력 ≈ 0~2 (매우 작음)
  → 방안A 비례매핑: esc_value = 108 + 2×36/3600 = 108.02
  → ESC 서보: 108~109 → 물리적 토크 미발생!

[1.5초 후 DWB 방향전환]
  angular.z = -0.0526 → 반대 방향으로 같은 과정 반복
  → 헌팅: 좌우 움찔거림만 발생
```

**핵심**: 방안B가 0.15로 부스트해도 MCU의 speed_spin이 **0.025 mm/s**로 PID 관점에서 거의 0에 가까움. 0.25로 올려야 ESC가 실제로 동작할 수 있는 수준.

---

## 6. 3중 수정 적용

### 6.1 수정 1: MIN_ANGULAR_DEADZONE 0.15 → 0.25

**파일**: `jupiter_driver_compensated.py` cmd_vel_callback()

```python
# ── 방안B 시작 ──
MIN_ANGULAR_DEADZONE = 0.25  # rad/s (scaled) — DWB 최소샘플 0.0526 대응
if abs(angular_corrected) > 0.005 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
    angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
# ── 방안B 끝 ──
```

**효과**:
```
angular_corrected = 0.25 (부스트)
→ MCU speed_spin = (0.25/1000) × 164.555 = 0.041 mm/s
→ 여전히 작지만, PID는 출력을 빠르게 올림 (incremental, Kp=0.8)
→ ESC가 활성화 영역에 도달할 수 있음
```

### 6.2 수정 2: velocity_smoother 설정 추가

**파일**: `nav2_params_fused.yaml` (신규 섹션)

```yaml
velocity_smoother:
  ros__parameters:
    use_sim_time: False
    smoothing_frequency: 20.0
    scale_velocities: false
    feedback: "OPEN_LOOP"
    max_velocity: [0.35, 0.0, 1.0]       # 로봇 최대 속도
    min_velocity: [-0.35, 0.0, -1.0]
    max_accel: [0.5, 0.0, 0.8]           # DWB acc_lim_x/theta와 일치
    max_decel: [-0.5, 0.0, -0.8]         # DWB decel_lim과 일치
    odom_topic: "odom"
    odom_duration: 0.1
    deadband_velocity: [0.0, 0.0, 0.0]   # 드라이버 방안B가 처리
    velocity_timeout: 1.0
```

**효과**: velocity_smoother의 가감속 제한이 DWB 파라미터와 일치 → 불필요한 smoothing/clipping 방지

### 6.3 수정 3: forward_sampling_distance 0.5 → 0.3

**파일**: `nav2_params_fused.yaml` RotationShimController 섹션

```yaml
forward_sampling_distance: 0.3     # ⭐ 0.5→0.3: 짧은 경로(0.5m 복귀)에서 RotationShim 활성화 보장
```

**효과**: 0.5m 복귀 경로에서 RotationShimController가 0.3m 앞을 보고 경로 방향(180°)을 감지 → `angular_dist_threshold=0.785(45°)` 초과 → `rotate_to_heading_angular_vel=0.8` 발행 → DWB의 ±0.0526 문제 우회

**이것이 가장 핵심적인 수정**: RotationShimController가 정상 작동하면 DWB의 최소 샘플 문제 자체가 발생하지 않음 (DWB는 RotationShim 이후 직진 제어만 담당).

### 6.4 빌드 결과

```
Starting >>> jupiter_bringup
Finished <<< jupiter_bringup [4.13s]
Starting >>> jupiter_nav_VO
Finished <<< jupiter_nav_VO [3.88s]
Summary: 2 packages finished [8.43s]
```

### 6.5 빌드 검증

```
=== 방안B ===
MIN_ANGULAR_DEADZONE = 0.25  # rad/s (scaled) — DWB 최소샘플 0.0526 대응

=== velocity_smoother ===
velocity_smoother:
  ros__parameters:
    use_sim_time: False

=== forward_sampling ===
forward_sampling_distance: 0.3     # ⭐ 0.5→0.3
```

---

## 7. 무한회전 진단 및 수정: 방안B 감속 감지

### 7.1 증상

3중 수정 적용 후 테스트에서 회전을 시작하면 **멈추지 못하고 무한히 회전**하는 현상 발생.

### 7.2 근본 원인: 방안B가 velocity_smoother 감속을 차단

**핵심**: 방안B는 scaled angular 값이 0.005~0.25 범위일 때 0.25로 부스트.
이것은 raw cmd_vel 기준 **0.014~0.708** 범위에 해당.

velocity_smoother가 0.8→0.0으로 감속할 때:

```
smoother 출력:     0.80  0.76  0.72  ...  0.60  0.40  0.20  0.08  0.00
×0.353(스케일):    0.282 0.268 0.254 ...  0.212 0.141 0.071 0.028 0.000
방안B 판정:         NO    NO    NO   ...  BOOST BOOST BOOST BOOST  NO
MCU 수신값:        0.282 0.268 0.254 ...  0.25  0.25  0.25  0.25  0.00
```

**raw 0.7 이하(≒ scaled 0.247 이하)의 모든 감속 중간값이 0.25로 부스트됨!**
- smoother가 0.8에서 0으로 감속하는 ~1초 중 **~0.87초**간 0.25 고정
- 로봇은 0.25 각속도로 계속 회전 → 목표 헤딩을 오버슈트
- RotationShimController가 오버슈트 감지 → 다시 0.8 명령 → 같은 방향 회전 반복
- **→ 무한회전 사이클 형성**

감속 사이클 상세:
```
1. RotationShim: +0.8 rad/s → smoother → driver (0.282) → MCU → 로봇 회전
2. 목표 헤딩 도달 → RotationShim 비활성화 → DWB 전환 → smoother 감속 시작
3. smoother 0.72→0.64→...→0.08: 모두 scaled<0.25 → 방안B 부스트 0.25
4. ~0.87초간 0.25 유지 → 로봇 계속 회전 → 목표 헤딩 오버슈트
5. 오버슈트 >45° → RotationShim 재활성화 → +0.8 → Step 1로 복귀
→ 무한 루프!
```

### 7.3 수정: 감속 감지 기반 방안B

**원리**: angular 명령의 크기가 이전 명령보다 감소 중(감속)이면 부스트를 적용하지 않음.

**`__init__`에 추가** (상태변수):
```python
# [방안B] 감속 감지용 이전 angular 명령 저장
self._prev_angular_cmd = 0.0
```

**`cmd_vel_callback` 방안B 블록** (변경 전):
```python
# ── 방안B 시작 ──
MIN_ANGULAR_DEADZONE = 0.25
if abs(angular_corrected) > 0.005 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
    angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
# ── 방안B 끝 ──
```

**변경 후** (감속 감지 추가):
```python
# ── 방안B 시작 ──
MIN_ANGULAR_DEADZONE = 0.25
is_decelerating = abs(angular) < abs(self._prev_angular_cmd) - 0.01
self._prev_angular_cmd = angular

if not is_decelerating:
    if abs(angular_corrected) > 0.005 and abs(angular_corrected) < MIN_ANGULAR_DEADZONE:
        angular_corrected = MIN_ANGULAR_DEADZONE if angular_corrected > 0 else -MIN_ANGULAR_DEADZONE
# ── 방안B 끝 ──
```

**동작 분석**:

| 시나리오 | prev | curr | 감소량 | is_decel | 부스트 | 결과 |
|----------|------|------|--------|----------|--------|------|
| DWB 정상 0.0526 유지 | 0.053 | 0.053 | 0.000 | ❌ | ✅ | 부스트→0.25 ✓ |
| smoother 감속 0.6→0.56 | 0.60 | 0.56 | 0.04 | ✅ | ❌ | 0.198 그대로 → 자연 감속 ✓ |
| smoother 감속 0.08→0.04 | 0.08 | 0.04 | 0.04 | ✅ | ❌ | 0.014 그대로 → 자연 감속 ✓ |
| 정지→가속 0→0.8 | 0.0 | 0.8 | -0.8 | ❌ | ✅ | 값>0.25이므로 무관 |
| DWB 방향전환 +0.053→-0.053 | 0.053 | -0.053 | 0.0* | ❌ | ✅ | 부스트→-0.25 ✓ |

\* 방향전환: abs(-0.053) = 0.053, abs(0.053) = 0.053 → 차이 0 → 감속 아님

---

## 8. 현재 적용 상태 요약

### 8.1 활성 변경사항

| 항목 | 값 | 파일 | 유형 |
|------|----|------|------|
| 방안A (펌웨어) | 비례매핑+ESC직접+α0.3 | bsp_motor.c (MCU) | ✅ 펌웨어 (영구) |
| 방안B (물리 데드존) | MIN_ANGULAR_DEADZONE=**0.25** + **감속감지** | jupiter_driver_compensated.py | 워크어라운드 |
| forward_sampling_distance | **0.3** (0.5에서) | nav2_params_fused.yaml | 튜닝 |
| velocity_smoother 설정 | **신규 추가** | nav2_params_fused.yaml | 설정 |
| ANGULAR_SCALE_FACTOR | 0.353 | jupiter_driver_compensated.py | 보정 (영구) |
| imu_gyro_scale | 1.025 | nav2_vslam_fused.launch.py | 보정 (영구) |
| frozen_threshold | 60 | nav2_vslam_fused.launch.py | 보정 (영구) |
| min_speed_theta | 0.30 | nav2_params_fused.yaml | 튜닝 (영구) |

### 8.2 제거된 항목

| 항목 | 제거일 | 이유 |
|------|--------|------|
| 방안B-2 (anti-hunting filter) | 2026-03-18 | 방안A 펌웨어로 코드 데드존 해소, DWB 샘플 문제도 별도 원인 |
| Nav2 θ 제한 (0.7/0.5/-0.5) | 2026-03-18 | 방안A 적용으로 원복 |
| Twirling 15.0 / Oscillation 5.0 | 2026-03-18 | 방안A 적용으로 원복 |

### 8.3 Nav2 토픽 흐름 (확인됨)

```
controller_server(DWB/RotationShim)
    │ publishes /cmd_vel_nav  (navigation_launch.py remap: cmd_vel → cmd_vel_nav)
    ▼
velocity_smoother
    │ subscribes /cmd_vel_nav  (remap: cmd_vel → cmd_vel_nav)
    │ publishes /cmd_vel       (remap: cmd_vel_smoothed → cmd_vel)
    ▼
jupiter_driver
    │ subscribes /cmd_vel
    │ 방안B: MIN_ANGULAR_DEADZONE=0.25 (감속 시 부스트 금지)
    │ × ANGULAR_SCALE_FACTOR=0.353
    ▼
MCU (STM32, 방안A 펌웨어)
```

이 흐름은 `/opt/ros/humble/share/nav2_bringup/launch/navigation_launch.py`에 하드코딩되어 있으며, `nav2_fused_navigation.launch.py`가 이를 include합니다.
