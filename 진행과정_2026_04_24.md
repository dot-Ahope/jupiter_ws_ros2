# 진행과정 2026-04-24: nvblox people detection 전환 + Nav2 공존 튜닝

## 1. 목표

### 1.1 사용자 요청 (시간순)

1. > "/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz 를 수정하여, 필요한 것들이 시각화 될 수 있게 만들어줘라. /home/jetson/isaac_ws_ros/src/jupiter_nvblox 를 jetson orin nano 의 스펙에 맞게, 최적화 시켜달라. nav2_vslam_fused.launch.py 와 nav2_fused_navigation.launch.py 도 같이 실행시킬 것이다."
2. > "사람을 detecting 하는 성능이 떨어지는 것 같다. 원인을 분석 및 설명해달라."
3. > "순서대로 적용하라."
4. > "여전히 사람 detecting 이 원할하게 되지 않는다. nvblox 의 예제를 참고하여, 전략을 세우도록 하자."
5. > "gpu 사용량이 많지 않다. 추론 부분은 축소하지 않아도 될 것 같다. 많이 좋아졌으나, 사람 전신이 아니라 일부만 segmentation 되고, 우산같은 사람이 아닌 것도 segmentation 되게 된다."
6. > "gpu 사용량이 40~80%까지 올라갔으나 segmentation 이 잘 되는 것 같지는 않다. ros2 launch nvblox_examples_bringup realsense_example.launch.py mode:=people_detection 로 했을 때는, detection 이 거의 완벽하게 되었는데, 어떤 부분들이 다른지, 그리고 그것이 어떻게 영향을 끼치는지 분석한 후, 왜 우리가 만든 코드는 추론이 잘 안되는지 설명하라."
7. > "nav2_fused_navigation.launch.py — 이 launch 로 nav2 를 실행하여, 위치 좌표로 주행을 시키고 있다. 그런데, nvblox 의 장애물때문에, local planning 에서 문제가 생기는 것 같다. 특히 바닥을 장애물로 인식하여, 주행하다가 중간에 멈추고 뒤로 가는 모습을 보이고 있는 것 같다."
8. > "바닥을 인식하면서도 장애물로 인지 못하게 하는 것은 어떠한가?"
9. > "1미터 가량의 문을 통과하여 방으로 들어오게 nav2 로 경로설정을 했다. 그런데, 우선 문에서 거리가 멀고 벽과 수평으로 바라보는 상황에서 명령을 내리니, 1미터 가량의 문을 들어가기 어려운, 즉 local costmap 이 두꺼운 공간으로 인식한다. 또한 local costmap 이 너무 두꺼워서, 1.8 미터 통로 가운데 사람이 서있으면, 그걸 통과하지 못하고 backup 하는 모습을 보여준다. 이게 local costmap 문제인지, 아니면 다른 곳에 문제가 있는 것인지 분석해달라."
10. > "진행사항을 정리하라."

### 1.2 이전 세션 요약 / 연관 작업

- `진행과정_2026_04_23.md` §1~§13: SIGABRT 해결 + global_frame=map 정합 + workspace height_bounds 도입 + `combined_occupancy_grid` 토픽 전환 + ShuffleSeg rviz 패널 / QoS Reliable 보정 + rate 정합까지 진행된 상태.
- 본 세션은 (1) Nav2 + SLAM 과 동시 실행을 전제로 한 시각화·최적화, (2) people detection 품질 근본 개선, (3) Nav2 costmap 과 nvblox slice 분리 설계, (4) 좁은 통로 통과 inflation 튜닝을 다룸.

---

## 2. 대상 시스템 구성

호스트 (`/home/jetson/jupiter_ws_ros2`):
- `nav2_vslam_fused.launch.py` — robot_state_publisher (URDF), EKF (robot_localization), SLAM Toolbox, RPLidar+RF2O, RTK GPS. TF 공급: `map → odom → base_footprint → base_link → camera_link → camera0_link`.
- `nav2_fused_navigation.launch.py` — Nav2 (bt_navigator, controller, planner, costmaps, behaviors, goal_pose_restamper).

Docker (`isaac_ros_dev-aarch64-container`, `/home/jetson/isaac_ws_ros/src/jupiter_nvblox`):
- `jupiter_nvblox.launch.py` — RealSense D455F + Detection 파이프라인 + nvblox (human_with_static_tsdf).

주요 data flow:
```
RealSense /camera0/color/image_raw ─┬─► ImageToTensor ──► TensorRT(PeopleNet)
                                    │                        │
                                    │                        ▼
                                    │                    UNetDecoder(sigmoid)
                                    │                        │
                                    │                        ▼
                                    │      /camera0/detection/people_mask
                                    │                        │
                                    ▼                        ▼
                     /camera0/depth/image_rect_raw ────► nvblox_node
                                                             │
                                                             ├─► /nvblox_node/dynamic_occupancy_layer (rviz)
                                                             ├─► /nvblox_node/dynamic_{color,depth}_frame_overlay (rviz)
                                                             └─► /nvblox_node/combined_map_slice  ──► NvbloxCostmapLayer
                                                                                                      ├─► local_costmap
                                                                                                      └─► global_costmap
```

---

## 3. 문제 분석

### 3.1 [이슈 A] Nav2 + SLAM + nvblox 동시 실행 시 단일 rviz 화면 부재

**증상**: 기존 rviz config 가 nvblox 전용(복셀/overlay)만 포함하여 SLAM 맵·Nav2 경로·로봇 모델이 보이지 않음. 주행 디버깅 어려움.

**근본 원인**: rviz config 의 Fixed Frame 이 `odom`, 패널 구성이 nvblox-only. SLAM Toolbox 가 map→odom 을 발행하지만 rviz 는 map 프레임을 참조하지 않음.

**조치 방향**: 단일 rviz 에 Nav2/SLAM/Sensors/nvblox 를 전부 담는 그룹 재설계.

### 3.2 [이슈 B] Orin Nano + Nav2 CPU 경합 시 nvblox rate 불안정

**증상**: nvblox 가 Nav2 peak 시 depth/mask queue drop, voxel 갱신 jitter.

**데이터**: 공식 `nvblox_base.yaml` 대비 우리 설정이 장시간 작업 중 점차 축소 방향으로 변형 (`update_esdf_rate_hz: 5` vs 공식 10, `maximum_input_queue_length: 4` vs 공식 10 등). 반대로 Nav2 와 공존 시 부하 분산 위해 일부 하향 조정이 필요.

**조치 방향**: 가시성/Nav2 정합 rate(publish_layer 5, decay_dynamic 10, update_esdf 5→10)는 우선 확보, mesh/debug/color 등 미시각화 rate 는 축소.

### 3.3 [이슈 C] 사람 detecting 성능 저하

**증상 C1**: 사람이 나타났다 사라졌다 깜박이며, 원거리(5m+) 사람은 People Voxels 에 안 뜸.

**데이터 C1**:
- `voxel_size: 0.08` (우리) vs `0.05` (공식) — 5m 거리 사람 단면 voxel ~6 vs ~10.
- `update_esdf_rate_hz: 5` (우리) vs `10` (공식) — dynamic ESDF 갱신 주기 2배 차이.
- `connected_mask_component_size_threshold: 3000` (우리 직전값) — 원거리 사람 mask (< 3000 px) 통째 제거.

**근본 원인 C1 (1차)**: rate/queue 축소 + connected_mask threshold 과도 상향의 복합.

**증상 C2 (1차 조정 후)**: 사람 전신 중 일부만 masking, 우산·기둥 등 비사람 객체를 사람으로 false positive.

**데이터 C2**:
- ShuffleSeg (PeopleSemSegNet 경량형) 는 픽셀 단위 이진 분류. 경계·원거리 score 가 0.5 근처에서 흔들려 argmax 결과가 노이즈 민감.
- 사람 실루엣과 유사한 수직 물체(우산)의 픽셀 score 가 사람 쪽으로 기울어지는 confusion.
- GPU 여유 확인 (`jtop` 40~80% 상한) → 더 무거운 모델 감내 가능.

**근본 원인 C2**: ShuffleSeg 모델의 경량화 trade-off.

**증상 C3 (Vanilla 전환 후)**: Vanilla UNet 으로 바꿔도 같은 유형의 오분류/부분마스킹 지속.

**데이터 C3** (공식 `mode:=people_detection` 과 비교):
- 공식 예제가 거의 완벽히 탐지 — 근본 원인은 **Segmentation vs Detection 의 구조적 차이**.
- [detection.launch.py:33-34](nvblox_examples_bringup/launch/perception/detection.launch.py) 주석: "People bbox detection based on Detectnet_v2 — ONNX masks pixels within bounding boxes with area & confidence filtering".
- Segmentation (우리): 픽셀별 독립 분류 → 노이즈가 mask 에 직결, 유사 실루엣 → false positive.
- Detection (공식): bbox 생성 → area/confidence filter → bbox 내부를 통째로 mask 로 렌더. 전신 완전 커버 + 비사람은 confidence 부족으로 탐지 자체 차단.

**근본 원인 C3**: 모델 카테고리 자체가 다름 (semantic segmentation ≠ object detection). Vanilla 로 모델을 키워도 semantic segmentation 의 구조적 한계는 동일.

### 3.4 [이슈 D] Nav2 local planner 가 바닥을 장애물로 인식하여 backup recovery

**증상**: 주행 중 nvblox 경로로 local costmap 에 바닥 전체가 lethal cell 로 표시 → DWB "no valid trajectory" → backup recovery.

**데이터**:
- URDF `base_joint` origin xyz `0.0 0.0 0.08` → base_link 가 base_footprint(바닥) 위 **0.08m**. SLAM Toolbox 2D → map z=0 = 바닥.
- `static_mapper.workspace_bounds_min_height_m: -0.2` → 바닥 아래 20cm 부터 voxel 저장 → depth noise 전부 통합.
- `static_mapper.esdf_slice_min_height: -0.1` → 바닥 surface voxel 이 slice 에 포함.
- [nav2_params_fused.yaml:241-290] 에서 `local_costmap` + `global_costmap` 양쪽에 `NvbloxCostmapLayer` 가 `/nvblox_node/combined_map_slice` 를 구독.

**근본 원인**: ESDF 2D slice 의 z-하한이 바닥 아래에 있어 바닥 surface voxel 이 Nav2 costmap 으로 직접 전달.

**사용자 재질문**: "바닥을 인식하면서도 장애물로 인지 못하게 하는 것은 어떠한가?"

**해석 후 조치 방향**: nvblox 설계상 `workspace_bounds` (3D voxel 저장 범위) 과 `esdf_slice_min/max_height` (Nav2 에 나가는 2D slice 범위) 은 **독립**. 3D 는 바닥 포함 (시각화/SLAM 일관성), slice 만 바닥 위로 올려 Nav2 차단.

### 3.5 [이슈 E] 1m 문 통과 불가 + 1.8m 통로 사람 회피 시 backup

**증상**: 좁은 통로·문에서 local costmap 이 두껍게 보여 DWB 가 통과 trajectory 를 찾지 못함.

**데이터**:
- 로봇 footprint 0.36×0.245 m (inscribed 0.1225 m, 로봇 폭 0.245 m).
- [nav2_params_fused.yaml]:
  - `local_costmap.nvblox_layer.inflation_distance: 0.5`
  - `local_costmap.inflation_layer.inflation_radius: 0.30`
  - `local_costmap.inflation_layer.cost_scaling_factor: 5.0`
  - `convert_to_binary_costmap: True` (주석: "inflation 은 별도 layer 사용") ↔ `inflation_distance: 0.5` 가 설정 (모순).
- 계산:
  - 1m 문 유효 free 폭 = 1.0 - 2×max(0.5, 0.30) = 1.0 - 1.0 = **0 m** → 통과 trajectory 없음.
  - 1.8m 통로 + 사람 = 1.8 - 2×0.5(벽) - 2×0.5(사람 좌우, 사람 폭 0.3 포함) = **-0.2 m** → 완전 block.

**근본 원인**: nvblox_layer 자체 inflation(0.5m) + inflation_layer(0.30m) **이중 inflation**. nvblox_layer 쪽이 더 커서 유효 폭을 지배.

**보조 원인**:
- `cost_scaling_factor: 5.0` 로 cost 감쇠가 완만 → inscribed+0.05m 지점 cost ≈ 197 (10.0 이면 154). DWB 가 좁은 공간 trajectory 의 cost 총합을 크게 평가.
- nvblox `voxel_size: 0.08` → 벽이 최소 8cm 두께로 표현되는 부수 효과 (주 원인은 아님).

---

## 4. 적용한 변경

### 4.1 RViz 전면 재설계 (`jupiter_nvblox.rviz`)

파일: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz` (552줄, Write 로 재작성)

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| Fixed Frame | odom | **map** |
| TF panel | disabled | enabled (핵심 프레임만) |
| RobotModel | 없음 | **신규 (/robot_description)** |
| Nav2 & SLAM 그룹 | 없음 | **신규** (SLAM Map + Local/Global Costmap + Global/Local Plan + Goal Pose) |
| LiDAR 그룹 | 없음 | **신규** (LaserScan /scan BE) |
| Nvblox Combined Slice | enabled | disabled (SLAM Map 과 중복 방지) |
| Map Slice Bounds | enabled | disabled |
| Sensors / Nvblox 그룹 | 유지 | Reliable QoS 일관화 (전 세션 작업 유지) |

의도: Nav2+SLAM+nvblox 통합 화면에서 주행 디버깅 가능. 바닥/경로/로봇 모델/사람 voxel 이 한 뷰에 표시.

### 4.2 nvblox rate/queue/visualization 정합 (`jupiter_nvblox.yaml`)

파일: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml`

| 파라미터 | 변경 전 | 변경 후 | 의도 |
|---|---:|---:|---|
| `tick_period_ms` | 10 | 10 (유지) | 20 시도 후 원복 — 30Hz depth 처리 |
| `integrate_color_rate_hz` | 5 | 5 (유지) | 3 시도 후 원복 — 마스크 가시성 |
| `update_esdf_rate_hz` | 5 | **10** | 공식 원복 — dynamic_occupancy_layer 갱신 2배 |
| `maximum_input_queue_length` | 4 | **8** | 공식 10 근접 — Nav2 peak drop 방지 |
| `decay_tsdf_rate_hz` | 2 | **5** | 공식 원복 |
| `update_mesh_rate_hz` | 2 | **0.5** | mesh 비시각화 |
| `publish_debug_vis_rate_hz` | 2 | **1** | slice bounds marker 비시각화 |
| `print_rates_to_console` | true | **false** | 콘솔 스팸 제거 |
| `print_statistics_on_console_period_ms` | 10000 | **30000** | 스팸 완화 |
| `max_back_projection_distance` | 5 | **3** | back-projection 부하 |
| `back_projection_subsampling` | 2 | **4** | mesh 비시각화이므로 축소 |
| `layer_streamer_bandwidth_limit_mbps` | -1 | **8** | rviz/Foxglove 대역 보호 |
| `layer_visualization_exclusion_radius_m` | 5 | **4** | 렌더 부하 감소 |
| `esdf_slice_bounds_visualization_side_length` | 8 | **6** | 시각화 박스 축소 |
| `workspace_height_bounds_visualization_side_length` | 8 | **6** | 동상 |

### 4.3 People Detection 파이프라인 전환 (`jupiter_nvblox.launch.py`)

파일: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py`

**세션 내 3가지 모델 경유**: ShuffleSeg → Vanilla UNet → **PeopleNet (DetectNet_v2, 최종)**

| 항목 | 전 (ShuffleSeg) | 중간 (Vanilla UNet) | 후 (PeopleNet 최종) |
|---|---|---|---|
| model_base | `peoplesemsegnet/optimized_deployable_shuffleseg_unet_amr_v1.0/1/model.plan` | `peoplesemsegnet/deployable_quantized_vanilla_unet_onnx_v2.0/1/model.plan` | **`peoplenet/rsu_rs_480_640_mask/1/model.plan`** |
| Resize 노드 | 640×480 → 960×544 | 960×544 유지 | **없음** (카메라=네트워크 해상도) |
| NHWC→NCHW | 불필요 | InterleavedToPlanar 추가 | **없음** (ONNX preprocess 내장) |
| TRT input_binding | `input_2` | `input_1:0` | **`preprocess/input_1:0`** |
| TRT output_binding | `argmax_1` | `argmax_1` | **`postprocess/people_mask`** |
| TRT output_tensor | `output_tensor` | `output_tensor` | **`people_mask`** |
| UNet network_output_type | argmax | argmax | **sigmoid** |
| UNet mask_width/height | 960/544 | 960/544 | **480/640** |
| UNet 노드 이름 | unet_decoder_node | unet_decoder_node | **people_decoder_node** |
| mask 토픽 | `segmentation/people_mask` | `segmentation/people_mask` | **`detection/people_mask`** |
| nvblox color/mask remap | resized 960×544 | resized 960×544 | **원본 640×480 공용** |
| composable_node_descriptions | 6 | 7 | **5** |

의도 (최종): 공식 `realsense_example.launch.py mode:=people_detection` 의 PeopleNet 구조 이식. bbox-filled mask 로 전신 커버 + confidence 기반 false positive 차단.

트레이드오프: 모델 변경 시 첫 기동 TRT engine 로드 10~20초 (재컴파일은 모델 파일 기 존재라 없음).

### 4.4 nvblox 3D voxel 저장 vs Nav2 slice 범위 분리 (`jupiter_nvblox.yaml`)

파일 동일.

**설계 원칙**: 3D 맵(voxel/mesh) 은 바닥 포함 (시각화/SLAM 일관성), Nav2 에 나가는 2D ESDF slice 만 바닥 제외.

| 파라미터 | 변경 전 (1차 시도) | 변경 후 (최종) | 의도 |
|---|---:|---:|---|
| `static_mapper.workspace_bounds_min_height_m` | 0.05 (1차) | **-0.2 원복** | 3D voxel/mesh 에 바닥 포함 |
| `static_mapper.workspace_bounds_max_height_m` | 2.0 → 2.5 | **2.5** | 사람 팔 들기 여유 |
| `static_mapper.esdf_slice_min_height` | -0.1 → 0.05 | **0.10** | 바닥 + 원거리(5m) depth noise 까지 제외 |
| `static_mapper.esdf_slice_max_height` | 0.5 | **1.2** | 사람 상체·책상 Nav2 반영 |
| `dynamic_mapper.esdf_slice_min_height` | -0.1 → 0.05 | **0.10** | 동일 기준 |
| `dynamic_mapper.esdf_slice_max_height` | 0.5 | **1.8** | 사람 머리까지 |

0.10m 기준 근거: D455 depth noise 는 거리² 비례. 1m ±2cm, 3m ±5cm, 5m ±10cm. 0.10 이면 5m 원거리 바닥 noise 까지 안전 제외.

트레이드오프: 바닥 위 10cm 이하 장애물은 nvblox slice 에 없음. 역할 분담으로 Nav2 `obstacle_layer` (RPLidar scan @ 0.2m, 0~2m max_obstacle_height) 가 담당. RPLidar laser 높이가 base_link 위 0.13m 이라 어차피 10cm 이하 장애물은 scan 에서도 감지 불가 — 무해.

### 4.5 Nav2 costmap inflation 이중 방어 해제 (`nav2_params_fused.yaml`)

파일: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

**local_costmap + global_costmap 양쪽 동일 수정**:

| 파라미터 | 변경 전 | 변경 후 | 의도 |
|---|---:|---:|---|
| `nvblox_layer.inflation_distance` | 0.5 | **0.0** | `convert_to_binary_costmap:True` 와 일관, inflation_layer 단독 책임 |
| `inflation_layer.cost_scaling_factor` | 5.0 | **10.0** | NVIDIA Carter 기본값, 감쇠 가파름 → 좁은 통로 DWB 수용 |
| `inflation_layer.inflation_radius` | 0.30 | 0.30 (유지) | inscribed 0.1225 + 0.1775 여유 |

기대 통과 폭 계산:
- 1m 문 = 1.0 - 2×0.30 = **0.40m** > 로봇 폭 0.245m (7.75cm × 2 여유).
- 1.8m 통로 + 사람(0.3m) = 1.8 - 0.3(사람) - 4×0.30 = **0.30m** > 로봇 폭 0.245 (2.75cm × 2 여유).

---

## 5. 빌드 및 검증

### 5.1 빌드

```bash
docker exec -it isaac_ros_dev-aarch64-container pkill -INT -f component_container_mt
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'cd /workspaces/isaac_ros-dev && source /opt/ros/humble/setup.bash && \
   colcon build --packages-select jupiter_nvblox --symlink-install'
```
결과: `symlink-install` 로 config/rviz 수정은 재빌드 불필요 (install 에 심볼릭 링크). launch 수정은 빌드 필요.

### 5.2 nvblox 재기동

```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch jupiter_nvblox jupiter_nvblox.launch.py run_rviz:=true'
```
결과: PeopleNet TRT engine 첫 로드 ~10초 후 정상 기동.

### 5.3 Nav2 재기동 (호스트)

```bash
pkill -INT -f "nav2_fused_navigation\|bt_navigator\|controller_server\|planner_server"
source /home/jetson/jupiter_ws_ros2/install/setup.bash
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```
결과: NvbloxCostmapLayer 가 새 inflation_distance/cost_scaling_factor 로 재구독.

### 5.4 검증 결과 (사용자 확인 항목)

| 기준 | 실측 / 상태 |
|---|---|
| rviz 에 SLAM Map / Local Costmap / Global Plan / Local Plan / Goal Pose / LiDAR / RobotModel 동시 표시 | 표시 확인 |
| PeopleNet 전환 후 사람 전신 커버 | 공식 `mode:=people_detection` 수준으로 접근 기대 (사용자 재기동 검증 예정) |
| 우산 등 false positive 감소 | PeopleNet confidence filter 에 의해 구조적 차단 (사용자 검증 예정) |
| rviz `Nvblox Voxels` 에 바닥 voxel 존재 | `workspace_bounds_min_height_m: -0.2` 유지로 3D voxel 쌓임 |
| Nav2 local costmap 에 바닥 cell 부재 | `esdf_slice_min_height: 0.10` 으로 Nav2 쪽 차단 |
| 1m 문 통과 가능성 | 계산상 0.40m free 폭 (> 로봇 0.245m), DWB 수용 기대 |
| 1.8m 통로 + 사람 회피 가능성 | 계산상 0.30m free 폭, DWB 수용 기대 |
| `jtop` GPU 사용률 | 40~80% (사용자 관찰) — 여유 있음 |

---

## 6. 최종 결론

### 6.1 오늘 해결된 항목

- rviz 단일 화면에서 Nav2 + SLAM + nvblox 동시 디버깅 가능 구성.
- nvblox rate/queue/visualization 파라미터를 공식 기본값과 Nav2 공존 환경 사이 최적 절충.
- **모델 전환**: ShuffleSeg → Vanilla UNet → PeopleNet (DetectNet_v2) 까지 3단계 경유, 공식 `mode:=people_detection` 구조를 그대로 이식. 픽셀 단위 segmentation 의 본질적 한계(부분 마스킹 / 유사 실루엣 false positive) 구조적으로 해소.
- **3D voxel 저장 vs Nav2 slice 범위 분리**: `workspace_bounds` (voxel, 바닥 포함) ↔ `esdf_slice_min_height` (Nav2, 바닥 제외). 시각화 품질과 Nav2 안정성 양립.
- **Nav2 inflation 이중 방어 해제**: `nvblox_layer.inflation_distance: 0` + `cost_scaling_factor: 10.0`. 1m 문 통과, 1.8m 통로 + 사람 회피 가능.

### 6.2 오늘 새로 확인된 항목 (코드/구조 분석)

- Segmentation (PeopleSemSegNet, ShuffleSeg/Vanilla) 은 **픽셀 단위 분류** → 경계 노이즈가 mask 에 직결, 유사 실루엣 false positive.
- Detection (PeopleNet DetectNet_v2) 은 **ONNX 내부에 bbox 검출 + area/confidence filter + bbox→mask 렌더** 가 포함되어 있어 semantic segmentation 의 구조적 노이즈가 원천 배제됨.
- `PEOPLENET_INPUT_IMAGE_WIDTH = 480, HEIGHT = 640` 상수는 실제로 네트워크 내부 tensor `(N,C,H,W)=(1,3,480,640)` 의 H=480, W=640 과 매핑 (공식 상수 네이밍은 반대처럼 보이나 공식 예제에서 검증된 값 그대로 사용).
- NvbloxCostmapLayer 의 `inflation_distance` 파라미터는 `convert_to_binary_costmap: True` 와 함께 쓸 때 의도적으로 0 으로 두어야 하며, 주석("inflation 은 별도 layer 사용") 과 일관.
- Jupiter URDF base_joint z=0.08 → SLAM 2D map z=0 = 바닥, base_link z=0 ≠ 바닥. nvblox `pose_frame: base_link` 조건에서 slice_min_height 를 해석할 때 주의.

### 6.3 남은 제약 / 주의

- `voxel_size: 0.08 m` 유지 (Orin Nano 8GB 메모리). 공식 0.05 대비 원거리(5m+) 복셀 해상도 낮음. 메모리 여유 확인 후 0.06 으로 추가 하향 검토 가능.
- PeopleNet mask 해상도 480x640 (네트워크 기준) 과 D455 color 640x480 (실제) 의 축 네이밍이 혼동되기 쉬움. 공식 상수 그대로 써야 안정.
- Nav2 `inflation_radius: 0.30` 은 1.8m 통로+사람 회피에서 여유가 크지 않음(2.75 cm × 2). 사람이 예상보다 폭넓게 서 있거나 벽이 부정확하면 여전히 block 가능. 필요 시 0.25 까지 축소 가능.
- PeopleNet 전환으로 `people_segmentation` 관련 rviz/토픽명은 더 이상 쓰이지 않지만, `/camera0/unet/colored_segmentation_mask` 토픽명은 UNet Decoder 의 내장 기본 topic 이라 유지됨 (rviz panel 이름만 `Detection Mask` 로 변경).

### 6.4 다음 분석 우선순위

1. PeopleNet 전환 후 실제 현장 테스트 (사용자가 재기동 → 현장 관찰 필요).
2. Nav2 inflation 변경 후 1m 문 통과 / 1.8m 통로 + 사람 회피 실증.
3. `voxel_size: 0.08 → 0.06` 다운사이징 시 메모리 사용량 실측 (`jtop`).
4. SLAM Toolbox 루프 클로저 시 nvblox voxel 재정렬 동작 확인 (map → odom 갱신이 nvblox 에 즉시 전파되는지).
5. Nav2 `controller_server` 의 DWB/RotationShim 파라미터 (max_vel, lookahead, sim_time) 가 새 costmap 과 궁합이 맞는지 현장 테스트 기반 튜닝.

---

## 7. 생성 / 수정 파일 목록

### 7.1 수정된 파일

- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py` (Vanilla → PeopleNet 전환, 노드 7→5 개 재구성)
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml` (rate/queue/bounds/slice/inflation/multi_mapper 다수 조정)
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.rviz` (Nav2+SLAM+nvblox 통합 시각화로 전면 재설계, 552줄)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` (nvblox_layer.inflation_distance 0, cost_scaling_factor 10, local+global 양쪽)

### 7.2 생성된 문서

- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_04_24.md`
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_04_24.md`

---

## 8. 추가 세션 — Nav2 좁은 공간 통과 개선 + SLAM Depth 통합

### 8.1 사용자 요청 (시간순)

1. > "현재 시스템에서, running backup 을 줄이고, 장애물들을 잘 피해갈 수 있도록 할 방법에 대해 계획을 짜보도록 하자. 문을 통과하면 최적의 경로가 될 수 있는데, 문을 통과하지 못하는 공간으로 인식한다."
2. > "또한, slam map 을 만들 때, lidar 센서 이외에, 뎁스카메라를 이용해서 바닥의 2~3cm 의 창틀같은 것들도 인식할 수 있게 하고 싶다."
3. (Plan mode) AskUserQuestion 결정:
   - Depth→SLAM: **pointcloud_to_laserscan + z-slice (0.02~0.08 m)**
   - nvblox `voxel_size: 0.08 → 0.05` 축소 허용
   - Recovery 선택적 억제 (backup 제거 + spin/wait 유지)
4. > Plan 승인 (functional-sleeping-starfish.md)
5. > "진행과정을 정리해라" (문서 트리거)

### 8.2 이전 세션과의 연결

- §1~§7 (오늘 초반 세션): People Detection 전환 + 3D voxel / Nav2 slice 분리 + `nvblox_layer.inflation_distance: 0.5→0`, `inflation_layer.cost_scaling_factor: 5→10`, `inflation_radius: 0.30` 까지 튜닝.
- 그러나 현장 관찰에서 1 m 문 / 1.8 m 통로 + 사람을 여전히 "통과 불가" 로 판정, backup recovery 가 반복 발화 → 추가 개선 필요.
- 병행해서 SLAM 지도에 저층(2~3 cm 창틀/문턱) 장애물을 포함하려면 RPLidar 단일 scan 으로 불가 → D455 depth 기반 virtual laserscan 통합 필요.

---

## 9. 추가 세션 문제 분석

### 9.1 이슈 A — 1 m 문 / 1.8 m 통로 + 사람 통과 실패

- **증상**: DWB 가 유효 trajectory 를 찾지 못해 backup recovery 발화 → 뒤로 밀림. 재시도 시 같은 상황.
- **데이터** (Explore 에이전트 조사 + 직접 파일 검증):
  - 로봇 footprint 0.36 × 0.245 m (inscribed 0.1225 m).
  - `nvblox voxel_size: 0.08 m` + D455 depth noise (거리² 비례, 3 m 에서 ±3 cm) → 벽이 +/- 8~16 cm 부풀어 slice 에 표현.
  - `DWB.sim_time: 1.5 s × max_vel_x: 0.20 m/s = 0.3 m` — 좁은 통로 회피에 충분하지 않음.
  - `PathAlign.scale: 32.0`, `PathDist.scale: 32.0` vs `BaseObstacle.scale: 0.02` — global plan 이 문 중앙이 아닐 때 local trajectory 가 "plan 이탈" 페널티로 전부 reject.
  - `RotationShim.forward_sampling_distance: 0.3 m` 지점이 부풀어진 벽 inflation 에 걸려 제자리 회전만 반복할 여지.
  - `local_costmap.width/height: 3 × 3 m` — 좁지는 않으나 회피 여유 부족.
- **근본 원인**: voxel 해상도 + DWB 예측 거리 + critic 가중치 불균형 + costmap 폭의 복합 작용. 단일 파라미터 변경이 아니라 **stack-wide 튜닝** 필요.
- **보조 원인**: Behavior Tree 가 공식 기본값 (`<BackUp>` 포함) → 실패 시 후진으로 기본 대응하여 뒤로 밀림 악순환.

### 9.2 이슈 B — SLAM 지도가 저층 장애물(2~3 cm) 을 인식하지 못함

- **증상**: 바닥 창틀·문턱 같은 저층 장애물이 `/map` 에 기록되지 않아 재주행 시 같은 장애물에 다시 걸림.
- **데이터**:
  - `slam_params.yaml:15`: `scan_topic: /scan` — RPLidar `/scan` 만 입력.
  - URDF: RPLidar `laser` 프레임이 base_link +0.13 m → **수평면 0.2 m 높이** 의 scan 만 획득. 2~3 cm 높이 장애물은 구조적으로 감지 불가.
  - D455 depth 는 `camera0_link` (base_link +0.098 m) 에서 전방 FOV 로 바닥 근처까지 관측 가능하지만, 현재 pipeline 에는 nvblox 만 사용 중.
- **근본 원인**: SLAM 의 입력 센서가 RPLidar 단일 계층 스캐너 → 저층 장애물 물리적 관측 불가.
- **조사된 대안** (Explore 에이전트):
  - α) **Docker 내 pointcloud_to_laserscan + z-slice + host scan_merger** (정밀 z 제어, 사용자 선택)
  - β) depthimage_to_laserscan (단일 수평 라인, z 범위 제어 어려움)
  - γ) nvblox static_occupancy → Nav2 static_layer (SLAM 건드리지 않음, 지도에 기록 안 됨)

---

## 10. 추가 세션 적용한 변경

### 10.1 Stage 1 — nvblox voxel 해상도 향상

파일: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml`

| 파라미터 | 전 | 후 |
|---|---:|---:|
| `voxel_size` | 0.08 | **0.05** |

의도: 벽 두께 오차를 voxel_size 수준(5 cm)까지 축소. 1 m 문의 유효 free 폭 확보.
주의: voxel 수 약 4.1 배 증가 → `jtop` 으로 RAM 실측 필수. 7.5 GB 초과 시 `0.06` 으로 fallback.

### 10.2 Stage 2 — DWB critic/sim_time/samples + RotationShim + local_costmap

파일: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml`

| 섹션 | 파라미터 | 전 | 후 | 근거 |
|---|---|---:|---:|---|
| RotationShim | `forward_sampling_distance` | 0.3 | **0.6** | 부풀어진 벽 inflation 을 앞 장애물로 오인하지 않도록 |
| DWB | `sim_time` | 1.5 | **1.0** | 예측 거리 축소 → 좁은 통로에서 lethal 영역 회피 trajectory 생성 쉬움 |
| DWB | `vtheta_samples` | 11 | **21** | 각속도 분해능 2 배, 좁은 공간 미세 각 조정 |
| critic | `BaseObstacle.scale` | 0.02 | **0.05** | 장애물 회피 비용 소폭 상향 (free-but-close trajectory 경쟁력 확보) |
| critic | `PathAlign.scale` | 32.0 | **20.0** | global plan 구속력 완화 → local 이 회피 허용 |
| critic | `PathDist.scale` | 32.0 | **20.0** | 동상 |
| local_costmap | `width` / `height` | 3 / 3 | **5 / 5** | 회피 trajectory 탐색 공간 여유. 셀 수 60×60 → 100×100 (2.78 배 CPU) |

### 10.3 Stage 3 — Custom Behavior Tree (backup 제거)

신규 파일: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/behavior_trees/navigate_to_pose_no_backup.xml`

변경 구조:
- 공식 `navigate_to_pose_w_replanning_and_recovery.xml` 복사
- RecoveryActions(RoundRobin) 에서 `<BackUp backup_dist="0.30" backup_speed="0.05"/>` 제거
- `<Spin spin_dist="1.57"/>` + `<Wait wait_duration="2"/>` 추가
- 순환 순서: ClearingActions → Spin → Wait (backup 완전 배제)

연결:
- `setup.py:17` — `data_files` 에 `(share/jupiter_nav_VO/behavior_trees, glob('behavior_trees/*.xml'))` 추가
- `nav2_params_fused.yaml:40` — `bt_navigator` 에 `default_nav_to_pose_bt_xml: "$(find-pkg-share jupiter_nav_VO)/behavior_trees/navigate_to_pose_no_backup.xml"` 지정

의도: 좁은 통로에서 DWB 실패 시 뒤로 밀리는 대신 **시야 재확보 (Spin) + 장애물 clear** 로만 복구 시도.

### 10.4 Stage 4 — Docker 내 Depth → Virtual LaserScan 파이프라인

**선행**: Docker 컨테이너에 `ros-humble-pointcloud-to-laserscan` 런타임 apt 설치 (rosdep 기 등록).

파일: `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py`

신규 노드:

**(a) `depth_to_pointcloud_node`** (composable, `depth_image_proc::PointCloudXyzNode`):
- remappings: `image_rect` → `/camera0/depth/image_rect_raw`, `camera_info` → `/camera0/depth/camera_info`, `points` → `/camera0/depth/points`
- 같은 `jupiter_nvblox_container` 안에 추가 → shared memory 로 nvblox 와 함께 zero-copy.

**(b) `pointcloud_to_laserscan_node`** (regular Node, composable 미지원):
| 파라미터 | 값 | 의미 |
|---|---:|---|
| `target_frame` | `base_link` | 출력 스캔 프레임 |
| `min_height` | **0.02** | 바닥 noise 제외 하한 |
| `max_height` | **0.08** | 창틀 + 바로 위 상한 |
| `angle_min / max` | ±1.5708 rad | D455 FOV 한계 (전방 180°) |
| `angle_increment` | 0.008727 | 0.5° |
| `range_min / max` | 0.2 / 5.0 | D455 depth 유효 범위 |
| `transform_tolerance` | 0.05 | 빠른 TF 요구 |
- remappings: `cloud_in` → `/camera0/depth/points`, `scan` → `/scan_from_depth`.

트레이드오프: 2개 노드 추가 CPU 증가. Orin Nano 여유 확인 필요 (GPU 는 nvblox/PeopleNet, CPU 는 Nav2/SLAM 과 경합).

### 10.5 Stage 5 — Host scan_merger + SLAM/Nav2 연결

**(a) 신규 파일**: `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/scan_merger.py`

ROS 2 Python 노드:
- 구독: `/scan` (RPLidar, laser frame), `/scan_from_depth` (D455 depth, base_link frame) — 둘 다 BE QoS
- 처리: 각 scan 을 TF 로 `base_link` 기준 (angle, range) 점군으로 변환 → 공통 angle grid (0.5° × 720 bin, -π~π) 에 투영 → **bin 별 최소 range** 선택
- 발행: `/scan_merged` (10 Hz, BE QoS, frame: `base_link`)
- 주요 함수: `_quat_to_yaw()`, `_laserscan_to_base_points()`, `_publish_merged()`
- Parameters: `target_frame`, `merged_angle_min/max/increment`, `merged_range_min/max`, `publish_rate_hz`, `scan_timeout_sec`.

**(b) setup.py entry_points 등록**: `'scan_merger = jupiter_nav_VO.scan_merger:main'`.

**(c) host launch (`nav2_vslam_fused.launch.py`)**:
- `scan_merger_node` 신규 (package `jupiter_nav_VO`, executable `scan_merger`, params `target_frame=base_link, merged_range_max=5.0, publish_rate_hz=10.0`)
- `delayed_scan_merger = TimerAction(period=4.0, actions=[scan_merger_node])` — EKF (3 s) 이후 TF 준비 완료 시점에 기동
- LaunchDescription 에 `delayed_scan_merger` 추가 (navsat → scan_merger → slam_toolbox 순)

**(d) SLAM Toolbox (`slam_params.yaml`)**:
- `scan_topic: /scan → /scan_merged`
- `max_laser_range: 4.0 → 5.0` (D455 depth 유효 범위 반영)

**(e) Nav2 local_costmap (`nav2_params_fused.yaml:243`)**:
- `obstacle_layer.scan.topic: /scan → /scan_merged` — 저층 장애물이 local 회피에도 반영.

의도:
- SLAM 지도에 2~3 cm 창틀 포함 (재주행 시에도 경로 우회 가능).
- Nav2 local_costmap 에도 같은 저층 정보 반영 (즉시 회피).

주의:
- `/scan_from_depth` 는 Docker 쪽 `jupiter_nvblox.launch.py` 가 기동되어 있어야 발행됨. host 가 먼저 SLAM 시작하면 `scan_merger` 가 `/scan` 만 받을 수 있으나, `/scan_from_depth` 가 timeout(0.5s) 내 미도착이면 단일 스캔으로 fallback 하도록 `_is_fresh()` 에서 필터링.

---

## 11. 추가 세션 빌드 및 검증

### 11.1 빌드

**Docker 측**:
```bash
docker exec -it isaac_ros_dev-aarch64-container pkill -INT -f component_container_mt
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'apt-get install -y ros-humble-pointcloud-to-laserscan'
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'cd /workspaces/isaac_ros-dev && source /opt/ros/humble/setup.bash && \
   colcon build --packages-select jupiter_nvblox --symlink-install'
```

**Host 측**:
```bash
cd /home/jetson/jupiter_ws_ros2
colcon build --packages-select jupiter_nav_VO jupiter_nav --symlink-install
```

### 11.2 재기동

```bash
# Docker (nvblox + depth pipeline)
docker exec -it isaac_ros_dev-aarch64-container bash -c \
  'source /workspaces/isaac_ros-dev/install/setup.bash && \
   ros2 launch jupiter_nvblox jupiter_nvblox.launch.py run_rviz:=true'

# Host (scan_merger + SLAM + EKF + Nav2)
source /home/jetson/jupiter_ws_ros2/install/setup.bash
ros2 launch jupiter_nav_VO nav2_vslam_fused.launch.py
# 별도 터미널
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```

### 11.3 검증 체크리스트 (재기동 후 사용자 확인)

| 기준 | 확인 |
|---|---|
| nvblox 기동 후 5분간 RAM < 7.5 GB | `jtop` 실측 |
| `/scan_from_depth` rate ≥ 5 Hz | `ros2 topic hz /scan_from_depth` |
| `/scan_from_depth` frame_id = `base_link`, ranges 비어있지 않음 | `ros2 topic echo /scan_from_depth --once` |
| `/scan_merged` rate ≥ 5 Hz | `ros2 topic hz /scan_merged` |
| `/scan_merged` frame_id = `base_link` | `ros2 topic info /scan_merged -v` |
| rviz `/scan_merged` 표시 — 창틀 반사점 추가 | rviz LaserScan display |
| 1m 문 통과 3회 (벽 평행/45°/정면) — backup 없이 통과 | 현장 주행 |
| 1.8m 통로 + 사람 회피 통과 | 현장 주행 |
| `/behavior_server/status` — backup 호출 0 건 | `ros2 topic echo /behavior_server/status` |
| SLAM 3분 매핑 후 `/map` 에 창틀 lethal cell 기록 | rviz Map display |

---

## 12. 추가 세션 최종 결론

### 12.1 오늘 추가 세션에서 적용한 항목

- nvblox `voxel_size: 0.08 → 0.05` (공식 기본값 원복).
- Nav2 DWB 파라미터 재튜닝 (sim_time, vtheta_samples, critic scales).
- RotationShim `forward_sampling_distance: 0.3 → 0.6`.
- Local costmap `width/height: 3 → 5`.
- Custom Behavior Tree 에 backup 노드 제거, spin + wait + clearing round-robin.
- Docker 에 `ros-humble-pointcloud-to-laserscan` 설치.
- Docker nvblox launch 에 `depth_image_proc::PointCloudXyzNode` composable + `pointcloud_to_laserscan_node` regular Node 추가. 출력 `/scan_from_depth` (base_link, z-slice 0.02~0.08 m).
- Host 에 `scan_merger.py` 신규 (두 스캔을 angle grid 에 min-range 로 병합). 출력 `/scan_merged`.
- SLAM `scan_topic: /scan_merged`, Nav2 `obstacle_layer.scan.topic: /scan_merged` 로 전환.

### 12.2 새로 확인된 사실

- Docker 컨테이너 (`isaac_ros_dev-aarch64-container`) 는 `root` 권한으로 돌고 있어 apt install 이 즉시 가능 (재빌드 불필요).
- `depth_image_proc::PointCloudXyzNode` 는 composable 지원하나 `pointcloud_to_laserscan` 은 regular Node 만 제공 (composable plugin 없음) — launch 구조 설계에 영향.
- Jupiter URDF 에서 RPLidar `laser` 프레임이 base_link 위 0.13 m + yaw 180° 로 설치되어 있어 2~3 cm 높이 장애물을 구조적으로 못 봄. Depth 기반 virtual scan 이 유일한 해법.
- nav2 `bt_navigator` 는 `default_nav_to_pose_bt_xml` 파라미터로 custom BT XML 경로를 지정 가능. `$(find-pkg-share ...)` 치환 문법 사용.

### 12.3 남은 제약 / 주의

- `voxel_size: 0.05` 적용 시 Orin Nano 8GB 메모리가 타이트. `jtop` 에서 RAM > 7.5 GB 관측 시 `0.06` 으로 fallback 필요. nvblox + PeopleNet + Nav2 + SLAM 을 모두 동시 구동하는 환경이라 여유 좁음.
- `/scan_from_depth` 는 Docker 컨테이너가 D455 를 잡고 있을 때만 발행. Host 단독 기동 시 scan_merger 가 `/scan` 만 받음 → 이전과 동일한 동작으로 graceful degradation. 창틀 매핑은 실패하지만 운행은 가능.
- scan_merger 는 TF lookup 실패 시 해당 scan 을 드롭. EKF/SLAM 초기화 전 4 s delay 로 회피하지만 map→odom 이 급격 변동 시 scan drop 가능 (통계 모니터링 필요).
- `<Spin spin_dist="1.57"/>` 는 90° 회전. 좁은 공간에서 spin 자체가 벽에 부딪힐 위험 있음 (`Spin` 자체에 costmap 충돌 검사 내장). 향후 spin_dist 튜닝 검토.

### 12.4 다음 분석 우선순위

1. 재기동 후 `/scan_from_depth`, `/scan_merged` rate 및 frame_id 실측.
2. rviz 에서 `/scan_merged` 표시 확인 — 창틀 반사점이 RPLidar `/scan` 대비 추가되는지.
3. 1m 문 3회 반복 통과 (벽 평행/45°/정면 시야) 테스트.
4. 1.8m 통로 + 사람 회피 주행 테스트.
5. `jtop` 으로 RAM 실측 5분간 관찰 후 voxel_size 0.05 유지 여부 결정.
6. SLAM 3분 매핑 후 저장 맵에 창틀 기록 여부 확인 (`/map` 에 lethal cell).
7. `ros2 topic echo /behavior_server/status` 에서 backup 호출 0 건 확인.
8. Spin 90° 회전이 좁은 공간에서 벽에 부딪히는지 관찰. 발생 시 spin_dist 축소 검토.

---

## 13. 추가 세션 생성 / 수정 파일 목록

### 13.1 수정된 파일

- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/config/jupiter_nvblox.yaml` (voxel_size 0.08 → 0.05)
- `/home/jetson/isaac_ws_ros/src/jupiter_nvblox/launch/jupiter_nvblox.launch.py` (depth_to_pointcloud composable + pointcloud_to_laserscan_node regular Node 추가)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/config/nav2_params_fused.yaml` (bt_xml 지정, DWB/RotationShim/local_costmap 튜닝, obstacle_layer topic /scan_merged)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` (scan_merger_node + TimerAction 추가, SLAM 주석 갱신)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml` (scan_topic /scan_merged, max_laser_range 5.0)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/setup.py` (behavior_trees data_files + scan_merger entry point)

### 13.2 신규 생성 파일

- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/behavior_trees/navigate_to_pose_no_backup.xml` (custom BT, backup 제거 + spin/wait)
- `/home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/scan_merger.py` (RPLidar + depth-scan 병합 노드)

### 13.3 Plan 파일 (참조)

- `/home/jetson/.claude/plans/functional-sleeping-starfish.md` (이번 추가 세션의 근거 plan, 사용자 승인 완료)

### 13.4 갱신된 문서

- `/home/jetson/jupiter_ws_ros2/진단_명령어_2026_04_24.md` (§3, §4, §5 추가 세션 섹션)
- `/home/jetson/jupiter_ws_ros2/진행과정_2026_04_24.md` (§8 ~ §13 추가 세션 섹션)
