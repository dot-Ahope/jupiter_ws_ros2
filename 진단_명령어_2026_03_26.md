# 진단 명령어 2026-03-26: nvblox 3D 복셀 토픽 미표시 + Docker↔Host 통신 끊김 분석

## Phase 1: Docker 로그 분석

### 1.1 로그 파일 크기 확인
```bash
wc -l /home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/exemple_test.txt
wc -l /home/jetson/jupiter_ws_ros2/plot_data_position_x.csv
```
**목적**: Docker 실행 로그 및 Foxglove CSV 데이터 파일 규모 파악
**결과**:
```
1023 /home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/exemple_test.txt
6118 /home/jetson/jupiter_ws_ros2/plot_data_position_x.csv
```

### 1.2 Docker 로그 — nvblox 초기화 확인 (1~100행)
```bash
# read_file: exemple_test.txt lines 1-100
```
**목적**: nvblox 노드 초기화 및 RealSense D455F 인식 확인
**결과**:
- RealSense D455F (시리얼 253822300207) 정상 인식
- nvblox layers 생성됨: tsdf, freespace, mesh, esdf, color
- RealsenseSplitterNode 생성
- cuVSLAM v12.6 초기화

### 1.3 Docker 로그 — nvblox mapper 설정 (100~500행)
```bash
# read_file: exemple_test.txt lines 100-300, 300-500
```
**목적**: nvblox 전체 파라미터 덤프 확인
**결과**:
```
mapping_type: static_tsdf
voxel_size: 0.05
global_frame: odom
pose_frame: base_link
esdf_mode: 2d
esdf_2d_min_height: 0.0, max_height: 1.0
publish_esdf_distance_slice: true
publish_layer_rate_hz: 5.0
update_mesh_every_n_indices: 5
```

### 1.4 Docker 로그 — 카메라 스트림 설정 (500~700행)
```bash
# read_file: exemple_test.txt lines 500-700
```
**목적**: RealSense 카메라 스트림 프로파일 확인
**결과**:
```
Depth:  Z16, 640x480, 60 FPS
Infra1: Y8,  640x480, 60 FPS
Infra2: Y8,  640x480, 60 FPS
Color:  RGB8, 640x480, 30 FPS
emitter_on_off 모드 -> splitter 기반 VSLAM(IR) + nvblox(depth+color)
```

### 1.5 Docker 로그 — nvblox/VSLAM 런타임 통계 (700~1023행)
```bash
# read_file: exemple_test.txt lines 700-1023
```
**목적**: nvblox 처리 속도, VSLAM 프레임 경고 확인
**결과**:
```
nvblox 통계 (10초 주기):
  depth_integration: 20-25 Hz, 지연 ~0.025s
  color_integration: 4.8 Hz
  esdf_integration: 9.5-9.6 Hz
  tick: 89-101 Hz

cuVSLAM 경고:
  "Delta between current and previous frame [~100ms] is above threshold [68ms]"
  → image_jitter_threshold_ms=68, emitter_on_off 30Hz에서 간헐적 발생
```
- nvblox: 정상 동작 (ERROR/FATAL 없음)
- cuVSLAM: 프레임 간격 경고만 있고 tracking_lost 없음

### 1.6 Docker 로그 — TF 에러 및 심각한 오류 검색
```bash
grep -i "TF\|ERROR\|FATAL\|mesh\|esdf" /home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/exemple_test.txt | grep -v "frame delta\|Delta between" | head -30
```
**목적**: TF tree 오류, nvblox mesh/esdf 발행 실패 여부 확인
**결과**:
```
(TF 에러 없음, ERROR/FATAL 없음)
nvblox esdf/mesh 관련 로그는 정상 설정 출력만 존재
```

### 1.7 nvblox 퍼블리시 설정 확인
```bash
grep -i "publish\|global_frame\|pose_frame\|mapping_type\|update_mesh\|layer_rate" /home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/exemple_test.txt | head -20
```
**목적**: nvblox가 mesh/esdf를 실제로 발행하도록 설정되어 있는지 확인
**결과**:
```
global_frame: odom
pose_frame: base_link
mapping_type: static_tsdf
publish_esdf_distance_slice: true
publish_layer_rate_hz: 5.0
update_mesh_every_n_indices: 5
publish_debug_vis_rate_hz: 2.0
```
- 모든 발행 설정이 활성화됨 → Docker 내부에서는 nvblox 토픽 정상 발행

---

## Phase 2: CSV 데이터 분석 (Foxglove 기록)

### 2.1 CSV 구조 확인
```bash
head -5 /home/jetson/jupiter_ws_ros2/plot_data_position_x.csv
tail -5 /home/jetson/jupiter_ws_ros2/plot_data_position_x.csv
```
**목적**: CSV 데이터 형식 및 시간 범위 파악
**결과**:
```
헤더: elapsed_time,timestamp,topic,value
시작: t=0.0, 토픽 /odom_adapted, value=0.0
끝:   t=206.8, 토픽 /odom, value=0.277...
총 시간 범위: ~207초
```

### 2.2 토픽별 데이터 포인트 수
```bash
awk -F',' 'NR>1 {print $3}' /home/jetson/jupiter_ws_ros2/plot_data_position_x.csv | sort | uniq -c | sort -rn
```
**목적**: 각 토픽의 데이터 양 비교
**결과**:
```
3218 /odom_adapted
1913 /odom
 646 /odom_rf2o_adapted
 340 /visual_slam/tracking/odometry_adapted
```
- VSLAM 토픽이 340개로 가장 적음 → 통신 문제 가능성

### 2.3 VSLAM 토픽 시간 갭 분석
```bash
awk -F',' '$3=="/visual_slam/tracking/odometry_adapted" {if(prev!="") {gap=$1-prev; if(gap>3) printf "GAP: t=%.1f → t=%.1f (%.1fs)\n", prev, $1, gap} prev=$1}' /home/jetson/jupiter_ws_ros2/plot_data_position_x.csv
```
**목적**: VSLAM 오도메트리 데이터에 큰 갭이 있는지 확인
**결과**:
```
GAP: t=10.1 → t=13.5 (3.4s)
GAP: t=26.2 → t=31.6 (5.4s)
GAP: t=41.1 → t=51.0 (9.9s)
GAP: t=51.0 → t=55.0 (4.0s)
GAP: t=55.0 → t=80.4 (25.4s)
→ 갭이 점진적으로 커짐. t=55초 이후 거대한 갭 발생
```

### 2.4 VSLAM 토픽 프리징 탐지
```bash
awk -F',' '$3=="/visual_slam/tracking/odometry_adapted" {
  if(prev_val==$4 && prev_val!="") {
    count++; last_t=$1
  } else {
    if(count>5) printf "FROZEN: t=%.1f→%.1f (%d samples) value=%s\n", first_t, last_t, count, prev_val
    count=1; first_t=$1
  }
  prev_val=$4; prev_t=$1
} END {
  if(count>5) printf "FROZEN: t=%.1f→%.1f (%d samples) value=%s\n", first_t, last_t, count, prev_val
}' /home/jetson/jupiter_ws_ros2/plot_data_position_x.csv
```
**목적**: VSLAM 값이 고정(프리즈)되어 반복 출력되는지 탐지
**결과**:
```
FROZEN: t=55.0→206.8 (242 samples) value=0.27955493330955505
```
- **t=55초부터 실험 종료(t=207초)까지 152초간 VSLAM position_x가 동일 값으로 고정**
- 242개 샘플이 모두 값 0.27955493330955505 → VSLAM 출력이 완전히 멈춤

### 2.5 /odom 토픽 후반부 갭 분석
```bash
awk -F',' '$3=="/odom" {if(prev!="") {gap=$1-prev; if(gap>3) printf "GAP: t=%.1f → t=%.1f (%.1fs)\n", prev, $1, gap} prev=$1}' /home/jetson/jupiter_ws_ros2/plot_data_position_x.csv | tail -10
```
**목적**: EKF 출력(/odom)도 후반부에서 갭이 커지는지 확인
**결과**:
```
GAP: t=157.8 → t=167.1 (9.3s)
GAP: t=167.1 → t=177.2 (10.1s)
GAP: t=177.2 → t=181.5 (4.3s)
GAP: t=190.5 → t=199.5 (9.1s)
```
- /odom 갭도 후반부로 갈수록 점진적 악화 (초반 < 1초 → 후반 9~10초)

---

## Phase 3: Launch 파일 분석

### 3.1 Docker launch 파일 확인 — RealSense + splitter + VSLAM + nvblox
```bash
# read_file: isaac_ros_vslam_nvblox_realsense.launch.py lines 1-100, 100-280
```
**목적**: Docker 측 RealSense, VSLAM, nvblox 노드 구성 확인
**결과**:
```python
# RealSense: emitter_on_off mode
# depth 640x480@60, infra 640x480@60, color 640x480@30
# splitter → VSLAM(infra1/2) + nvblox(depth+color)
# VSLAM: publish_odom_to_base_tf = PythonExpression (standalone 의존)
# nvblox: config from nvblox_base.yaml + nvblox_realsense.yaml (get_package_share_directory)
```

### 3.2 Host launch 파일 — Foxglove bridge 토픽 화이트리스트
```bash
grep "topic_whitelist" -A 30 /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py | head -35
```
**목적**: Foxglove bridge가 nvblox 토픽을 화이트리스트에 포함하는지 확인
**결과**:
```python
'topic_whitelist': [
    '/odom', '/map', '/scan', '/tf', '/tf_static', '/cmd_vel',
    '/diagnostics', '/robot_description',
    '/odom_adapted', '/odom_rf2o_adapted',
    '/visual_slam/tracking/odometry_adapted',
    '/imu/data_calibrated', '/jupiter/imu',
    # nvblox 관련
    '/nvblox_node/mesh',
    '/nvblox_node/static_esdf_pointcloud',
    '/nvblox_node/combined_esdf_pointcloud',
    '/nvblox_node/.*_layer',
    '/nvblox_node/esdf_slice_bounds',
    '/nvblox_node/human_voxels',
    # Nav2 관련
    '/local_costmap/costmap', '/global_costmap/costmap',
    '/plan', '/local_plan',
],
'send_buffer_limit': 10000000,  # 10MB
'num_threads': 2,
'max_qos_depth': 5,
```
- nvblox 토픽이 화이트리스트에 포함됨 → Foxglove bridge 설정은 정상

### 3.3 Host launch 파일 — 카메라 토픽 구독 여부
```bash
grep -rn "camera\|image_raw\|depth\|infra" /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py | head -20
```
**목적**: 호스트 측 노드가 카메라 이미지 토픽을 직접 구독하는지 확인
**결과**:
```
120: # Static TF: base_link → camera_link
124: tf_base_to_camera = Node(...)
```
- 호스트에서 카메라 이미지를 직접 구독하는 노드 없음
- 카메라 데이터는 Docker 내부에서만 소비

---

## Phase 4: RMW 및 DDS 설정 확인

### 4.1 Docker RMW 확인
```bash
grep "RMW\|rmw" /home/jetson/isaac_ws_ros/src/isaac_ros_common/docker/Dockerfile.* 2>/dev/null | head -10
```
**목적**: Docker 내부 DDS 구현체 확인
**결과**:
```
RMW_IMPLEMENTATION=rmw_fastrtps_cpp
```

### 4.2 Host RMW 확인
```bash
dpkg -l | grep "rmw-\|rmw_" | grep -i "fastrtps\|cyclone"
```
**목적**: 호스트 DDS 구현체 확인
**결과**:
```
ros-humble-rmw-fastrtps-cpp (설치됨)
ros-humble-rmw-cyclonedds-cpp (미설치)
```
- Docker와 Host 모두 `rmw_fastrtps_cpp` 사용 → DDS 호환성 문제 없음

### 4.3 현재 RTPS 프로필 확인
```bash
cat /home/jetson/isaac_ws_ros/src/isaac_ros_common/docker/middleware_profiles/rtps_udp_profile.xml
```
**목적**: Docker에서 사용하는 FastRTPS UDP 프로필 확인
**결과**:
```xml
<transport_descriptor>
    <transport_id>UdpTransport</transport_id>
    <type>UDPv4</type>
    <maxInitialPeersRange>400</maxInitialPeersRange>
</transport_descriptor>
<participant profile_name="udp_transport_profile" is_default_profile="true">
    <rtps>
        <useBuiltinTransports>false</useBuiltinTransports>
    </rtps>
</participant>
```

---

## Phase 5: nvblox_msgs 호스트 타입 해석 진단

### 5.1 호스트 nvblox_msgs 패키지 인식 확인
```bash
ros2 pkg list 2>/dev/null | grep nvblox
```
**목적**: 호스트에서 nvblox 패키지가 인식되는지 확인
**결과**:
```
nvblox_msgs
nvblox_nav2
nvblox_ros_python_utils
nvblox_test
```
- 패키지 목록에는 표시됨

### 5.2 nvblox_msgs 인터페이스 해석 시도
```bash
ros2 interface list | grep nvblox
```
**목적**: nvblox_msgs 메시지 타입이 실제 해석 가능한지 확인
**결과**:
```
Traceback (most recent call last):
  ...
LookupError: Unknown package 'nvblox_msgs'
```
- **패키지는 인식되나 메시지 인터페이스 해석 불가** → 핵심 원인 발견

### 5.3 nvblox_msgs 설치 경로 추적
```bash
echo $AMENT_PREFIX_PATH | tr ':' '\n' | grep nvblox
```
**목적**: nvblox_msgs가 어디서 로드되는지 확인
**결과**:
```
/home/jetson/isaac_ws_ros/install/nvblox_nav2
/home/jetson/isaac_ws_ros/install/nvblox_ros_python_utils
/home/jetson/isaac_ws_ros/install/nvblox_msgs  ← 여기
```

### 5.4 nvblox_msgs install 디렉토리 심볼릭 링크 상태 확인
```bash
ls -la /home/jetson/isaac_ws_ros/install/nvblox_msgs/share/nvblox_msgs/msg/
```
**목적**: .msg 파일이 실제 파일인지 심볼릭 링크인지 확인
**결과**:
```
Mesh.msg          → /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs/msg/Mesh.msg
MeshBlock.msg     → /workspaces/isaac_ros-dev/build/nvblox_msgs/rosidl_adapter/nvblox_msgs/msg/MeshBlock.idl
DistanceMapSlice.msg → /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs/msg/DistanceMapSlice.msg
Index3D.msg       → /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs/msg/Index3D.msg
VoxelBlock.msg    → /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs/msg/VoxelBlock.msg
VoxelBlockLayer.msg → /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs/msg/VoxelBlockLayer.msg
SemanticLabelsStamped.msg → /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs/msg/SemanticLabelsStamped.msg
```
- **모든 .msg 파일이 Docker 경로(/workspaces/isaac_ros-dev/...)를 가리키는 심볼릭 링크**
- 호스트에 /workspaces/isaac_ros-dev/ 경로가 없음 → **모든 심볼릭 링크 깨짐**

### 5.5 nvblox_msgs .so 라이브러리 상태 확인
```bash
ls -la /home/jetson/isaac_ws_ros/install/nvblox_msgs/lib/
```
**목적**: 실행시간 라이브러리(.so)는 실제 파일인지 확인
**결과**:
```
libnvblox_msgs__rosidl_generator_c.so                    (71128 bytes, 실제 파일)
libnvblox_msgs__rosidl_generator_py.so                   (46784 bytes, 실제 파일)
libnvblox_msgs__rosidl_typesupport_cpp.so                (34632 bytes, 실제 파일)
libnvblox_msgs__rosidl_typesupport_c.so                  (32144 bytes, 실제 파일)
libnvblox_msgs__rosidl_typesupport_fastrtps_cpp.so       (90720 bytes, 실제 파일)
libnvblox_msgs__rosidl_typesupport_fastrtps_c.so         (61512 bytes, 실제 파일)
libnvblox_msgs__rosidl_typesupport_introspection_cpp.so  (99752 bytes, 실제 파일)
```
- .so 파일은 실제 파일 → DDS 통신 시 typesupport는 동작 가능
- 문제는 .msg/.idl 정의 파일 + cmake/환경 설정 파일이 깨진 심볼릭 링크

### 5.6 package.xml 심볼릭 링크 상태
```bash
file /home/jetson/isaac_ws_ros/install/nvblox_msgs/share/nvblox_msgs/package.xml
```
**목적**: package.xml도 깨진 심볼릭 링크인지 확인
**결과**:
```
broken symbolic link to /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs/package.xml
```
- package.xml도 깨짐 → `ros2 interface`가 패키지 메타데이터를 읽지 못함

### 5.7 전체 깨진 심볼릭 링크 수 확인 (nvblox_msgs)
```bash
find /home/jetson/isaac_ws_ros/install/nvblox_msgs -xtype l -print 2>/dev/null | wc -l
```
**목적**: nvblox_msgs 패키지 내 깨진 심볼릭 링크 총 수
**결과**: (대량 — msg, srv, cmake, include, environment, python egg-info 등)

### 5.8 전체 isaac_ws_ros/install 깨진 심볼릭 링크 수
```bash
find /home/jetson/isaac_ws_ros/install -xtype l 2>/dev/null | wc -l
```
**목적**: 전체 isaac 워크스페이스에서 깨진 심볼릭 링크 규모 파악
**결과**:
```
1684
```
- 1684개 전체가 Docker 빌드 시 `/workspaces/isaac_ros-dev/...` 경로로 생성

---

## Phase 6: 수정 시도 1 — 심볼릭 링크 경로 치환 (실패)

### 6.1 경로 치환 전 검증
```bash
BROKEN=$(readlink /home/jetson/isaac_ws_ros/install/nvblox_msgs/share/nvblox_msgs/msg/Mesh.msg)
echo "원본: $BROKEN"
NEW=$(echo "$BROKEN" | sed 's|/workspaces/isaac_ros-dev/|/home/jetson/isaac_ws_ros/|')
echo "변환: $NEW"
test -f "$NEW" && echo "EXISTS" || echo "MISSING"
```
**목적**: 심볼릭 링크 경로를 Docker→Host로 치환했을 때 대상 파일이 존재하는지 검증
**결과**:
```
원본: /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs/msg/Mesh.msg
변환: /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_msgs/msg/Mesh.msg
EXISTS
```

### 6.2 깨진 심볼릭 링크 목록 생성
```bash
find /home/jetson/isaac_ws_ros/install -xtype l -print 2>/dev/null > /tmp/broken_links.txt
wc -l /tmp/broken_links.txt
```
**목적**: 수정 대상 심볼릭 링크 목록 파일 생성
**결과**:
```
1684 /tmp/broken_links.txt
```

### 6.3 전체 심볼릭 링크 일괄 치환 (Docker→Host 경로)
```bash
FIXED=0; SKIPPED=0
while IFS= read -r link; do
  target=$(readlink "$link")
  new_target=$(echo "$target" | sed 's|/workspaces/isaac_ros-dev/|/home/jetson/isaac_ws_ros/|')
  if [ -e "$new_target" ]; then
    rm "$link"
    ln -s "$new_target" "$link"
    FIXED=$((FIXED+1))
  else
    SKIPPED=$((SKIPPED+1))
  fi
done < /tmp/broken_links.txt
echo "FIXED=$FIXED SKIPPED=$SKIPPED"
```
**목적**: /workspaces/isaac_ros-dev/ → /home/jetson/isaac_ws_ros/ 경로 치환
**결과**:
```
FIXED=1670 SKIPPED=14
```
- 1670개 수정, 14개 스킵 (Docker 전용 바이너리)

### 6.4 수정 후 호스트 interface 검증
```bash
source /home/jetson/isaac_ws_ros/install/setup.bash
ros2 interface show nvblox_msgs/msg/Mesh
ros2 interface list | grep nvblox
```
**목적**: 호스트에서 nvblox_msgs 메시지 타입이 해석되는지 확인
**결과**:
```
ros2 interface show → Mesh 구조 정상 출력 (header, block_size_m, block_indices, blocks, clear)
ros2 interface list → 7개 msg + 2개 srv 모두 인식:
  nvblox_msgs/msg/DistanceMapSlice
  nvblox_msgs/msg/Index3D
  nvblox_msgs/msg/Mesh
  nvblox_msgs/msg/MeshBlock
  nvblox_msgs/msg/SemanticLabelsStamped
  nvblox_msgs/msg/VoxelBlock
  nvblox_msgs/msg/VoxelBlockLayer
  nvblox_msgs/srv/EsdfAndGradients
  nvblox_msgs/srv/FilePath
```
- **호스트 메시지 해석 성공!**

### 6.5 Docker 내부 launch 실행 → 실패
```
# Docker 내부에서 실행 결과 (사용자 보고)
ros2 launch isaac_ros_vslam_nvblox_realsense.launch.py
→ PackageNotFoundError: "package 'nvblox_examples_bringup' not found"
```

```
# Host에서 직접 실행 결과 (사용자 보고)
ros2 launch /home/jetson/isaac_ws_ros/src/.../isaac_ros_vslam_nvblox_realsense.launch.py
→ dlopen error: libnegotiated.so: cannot open shared object file
→ dlopen error: libisaac_ros_nitros_image_type.so: cannot open shared object file
```
**원인**: 심볼릭 링크를 `/home/jetson/isaac_ws_ros/...`로 변경 → Docker 내부에서는 해당 경로가 존재하지 않음 → 패키지 resolving 전부 실패
**결론**: **이 방식은 Docker를 깨뜨림. 원복 필요.**

---

## Phase 7: 수정 시도 1 원복 + 수정 시도 2 — 호환 심볼릭 링크

### 7.1 심볼릭 링크 원복 (Host→Docker 경로)
```bash
find /home/jetson/isaac_ws_ros/install -type l -lname '/home/jetson/isaac_ws_ros/*' > /tmp/host_links.txt
wc -l /tmp/host_links.txt
```
**목적**: Host 경로를 가리키는 심볼릭 링크 수 확인 (원복 대상)
**결과**:
```
1472 /tmp/host_links.txt
```

```bash
while IFS= read -r link; do
  target=$(readlink "$link")
  old_target=$(echo "$target" | sed 's|/home/jetson/isaac_ws_ros/|/workspaces/isaac_ros-dev/|')
  rm "$link"
  ln -s "$old_target" "$link"
done < /tmp/host_links.txt
```
**목적**: 모든 심볼릭 링크를 Docker 원래 경로(`/workspaces/isaac_ros-dev/...`)로 복원
**결과**:
```
REVERT=0 (성공)
```

### 7.2 원복 확인
```bash
readlink /home/jetson/isaac_ws_ros/install/nvblox_msgs/share/nvblox_msgs/msg/Mesh.msg
find /home/jetson/isaac_ws_ros/install -type l -lname '/home/jetson/isaac_ws_ros/*' | wc -l
```
**목적**: 모든 심볼릭 링크가 Docker 경로로 복원되었는지 확인
**결과**:
```
/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs/msg/Mesh.msg
Host 경로 심볼릭 링크 수: 0
```

### 7.3 호스트에 호환 심볼릭 링크 생성
```bash
ls -la /workspaces/ 2>/dev/null || echo "NO /workspaces dir"
sudo mkdir -p /workspaces
sudo ln -s /home/jetson/isaac_ws_ros /workspaces/isaac_ros-dev
ls -la /workspaces/isaac_ros-dev/
```
**목적**: 호스트에 `/workspaces/isaac_ros-dev` → `/home/jetson/isaac_ws_ros` 심볼릭 링크를 생성하여, Docker 경로 심볼릭 링크가 호스트에서도 유효하게 만듦
**결과**:
```
/workspaces/isaac_ros-dev → /home/jetson/isaac_ws_ros (심볼릭 링크 생성 완료)
install, src, build 등 모든 하위 디렉토리 접근 가능
```

### 7.4 호환 심볼릭 링크 후 호스트 nvblox_msgs 검증
```bash
file /home/jetson/isaac_ws_ros/install/nvblox_msgs/share/nvblox_msgs/msg/Mesh.msg
cat /home/jetson/isaac_ws_ros/install/nvblox_msgs/share/nvblox_msgs/msg/Mesh.msg
```
**목적**: Docker 경로 심볼릭 링크가 호환 심볼릭 링크를 통해 호스트에서 해석되는지 확인
**결과**:
```
심볼릭 링크: → /workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs/msg/Mesh.msg
→ /workspaces/isaac_ros-dev → /home/jetson/isaac_ws_ros (호환 링크)
→ /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_msgs/msg/Mesh.msg (실제 파일)
내용 정상 출력 (std_msgs/Header header, float32 block_size_m, ...)
```

### 7.5 호스트 ros2 interface 검증
```bash
ros2 interface show nvblox_msgs/msg/Mesh 2>&1 | head -5
```
**목적**: 호스트에서 nvblox_msgs 메시지 타입 해석이 여전히 동작하는지 확인
**결과**:
```
std_msgs/Header header
        builtin_interfaces/Time stamp
                int32 sec
                uint32 nanosec
        string frame_id
```

### 7.6 Docker 내부 nvblox_msgs 검증
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  "source /workspaces/isaac_ros-dev/install/setup.bash && ros2 interface show nvblox_msgs/msg/Mesh 2>&1 | head -3"
```
**목적**: Docker 내부에서도 심볼릭 링크가 정상인지 확인
**결과**:
```
std_msgs/Header header
        builtin_interfaces/Time stamp
                int32 sec
```

### 7.7 Docker 내부 nvblox_examples_bringup 패키지 검증
```bash
docker exec isaac_ros_dev-aarch64-container bash -c \
  "source /workspaces/isaac_ros-dev/install/setup.bash && python3 -c \"from ament_index_python.packages import get_package_share_directory; print(get_package_share_directory('nvblox_examples_bringup'))\""
```
**목적**: Docker 내부에서 launch에 필요한 패키지가 정상 인식되는지 확인
**결과**:
```
/workspaces/isaac_ros-dev/install/nvblox_examples_bringup/share/nvblox_examples_bringup
```
- Docker 측 패키지 resolving 정상 복구

### 7.8 최종 깨진 심볼릭 링크 확인
```bash
find /home/jetson/isaac_ws_ros/install -xtype l 2>/dev/null
```
**목적**: 최종적으로 남은 깨진 심볼릭 링크 확인
**결과**:
```
/home/jetson/isaac_ws_ros/install/isaac_ros_visual_slam/lib/isaac_ros_visual_slam/cuvslam_api_launcher
/home/jetson/isaac_ws_ros/install/isaac_ros_visual_slam/lib/libcuvslam.so
```
- 2개만 남음: Docker 전용 CUDA 바이너리 (`/opt/ros/humble/share/isaac_ros_nitros/cuvslam/lib/...`)
- nvblox 토픽 시각화와 무관

---

## Phase 8: Docker 컨테이너 상태 확인

### 8.1 Docker 컨테이너 실행 상태
```bash
docker ps --filter "name=isaac" --format "{{.Names}} {{.Status}}"
```
**목적**: Docker 컨테이너가 실행 중인지 확인
**결과**:
```
isaac_ros_dev-aarch64-container Up 4 hours
```

---

## Phase 9: Foxglove 시각화 느려짐 진단 및 해결 (2026-03-27)

### 문제 상황
nvblox_msgs 심볼릭 링크 수정 후, Foxglove에서 nvblox 토픽이 정상 표시됨.
그러나 **Docker launch 실행 시 Foxglove가 극도로 느려지는** 새로운 문제 발생.

### 9.1 현재 활성 토픽/노드 확인
```bash
ros2 topic list
ros2 node list
```
**목적**: Docker/Host launch 비실행 상태에서 기본 토픽/노드 확인
**결과**:
```
/joy, /joy/set_feedback  (조이스틱만)
/joy_node, /jupiter_joy  (조이스틱 노드만)
```

### 9.2 Docker launch 파일 분석 (nvblox 퍼블리시 설정)
```bash
# Docker launch 파일 전체 읽기 (280행)
cat /home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py
```
**목적**: nvblox_node 매개변수, 퍼블리시 설정, 카메라 구성 확인
**결과**: nvblox_base.yaml에서 기본값 로딩 → `publish_layer_rate_hz: 5.0`, `publish_debug_vis_rate_hz: 2.0`

### 9.3 Host launch 파일 Foxglove whitelist 분석
```bash
grep -n 'topic_whitelist' -A 50 /home/jetson/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
```
**목적**: Foxglove bridge가 구독하는 nvblox 토픽 목록 확인
**결과** (수정 전 whitelist에 포함된 nvblox 항목):
```python
'/nvblox_node/mesh',
'/nvblox_node/static_esdf_pointcloud',
'/nvblox_node/combined_esdf_pointcloud',
'/nvblox_node/esdf_slice_bounds',
'/nvblox_node/.*_layer',      # ← regex: 4개 토픽 매칭!
'/nvblox_node/human_voxels',
```

### 9.4 nvblox 기본 퍼블리시 설정 확인
```bash
grep -n 'publish_layer_rate_hz\|publish_debug_vis_rate_hz\|update_mesh_rate_hz\|layer_streamer_bandwidth' \
  /home/jetson/isaac_ws_ros/install/nvblox_examples_bringup/share/nvblox_examples_bringup/config/nvblox/nvblox_base.yaml
```
**목적**: nvblox 기본 퍼블리시 빈도 확인
**결과**:
```
publish_layer_rate_hz: 5.0
publish_debug_vis_rate_hz: 2.0
layer_streamer_bandwidth_limit_mbps: -1.0  # 무제한!
```

### 9.5 nvblox 토픽별 대역폭 분석 (계산)

| 토픽 | 메시지 크기 | 주파수 | 대역폭 | 비고 |
|------|------------|--------|--------|------|
| `/nvblox_node/mesh` | 374 KB | 9.5 Hz | **3.5 MB/s** | nvblox_msgs/Mesh |
| `/nvblox_node/color_layer` | 372 KB | 5 Hz | 1.9 MB/s | `.*_layer` 매칭 |
| `/nvblox_node/tsdf_layer` | 367 KB | 5 Hz | 1.8 MB/s | `.*_layer` 매칭 |
| `/nvblox_node/color_layer_marker` | ~600 KB | 5 Hz | 3.0 MB/s | `.*_layer` 매칭 |
| `/nvblox_node/tsdf_layer_marker` | ~600 KB | 5 Hz | 3.0 MB/s | `.*_layer` 매칭 |
| `/nvblox_node/static_esdf_pointcloud` | 58 KB | 2 Hz | **0.1 MB/s** | PointCloud2 |
| `/nvblox_node/esdf_slice_bounds` | 0.5 KB | 2 Hz | **≈0** | Marker |
| **합계 (Foxglove 경유)** | | | **~21.7 MB/s** | send_buffer_limit 10MB 초과 |

**핵심 발견**: `/nvblox_node/.*_layer` regex가 4개 토픽 매칭 → 16 MB/s 기여

### 9.6 Foxglove whitelist 수정 (Host launch)
```bash
# 파일: jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py
# 변경: nvblox 관련 whitelist 6개 항목 → 2개로 축소
```
**수정 전**:
```python
'/nvblox_node/mesh',
'/nvblox_node/static_esdf_pointcloud',
'/nvblox_node/combined_esdf_pointcloud',
'/nvblox_node/esdf_slice_bounds',
'/nvblox_node/.*_layer',
'/nvblox_node/human_voxels',
```
**수정 후**:
```python
# nvblox 관련 — 대역폭 제한 필수!
# ⚠️ 전체 nvblox 토픽 ~21.7 MB/s → WebSocket 포화 → Foxglove 멈춤
# mesh(374KB×9.5Hz=3.5MB/s), .*_layer(4토픽 매칭, 16MB/s) 등은 제거
# 경량 토픽만 유지:
'/nvblox_node/static_esdf_pointcloud',  # 2D ESDF 포인트클라우드 (58KB×2Hz=0.1MB/s)
'/nvblox_node/esdf_slice_bounds',       # ESDF 범위 마커 (0.5KB×2Hz≈0)
# ❌ 제거됨 (대역폭 과다):
# '/nvblox_node/mesh',               # 374KB×9.5Hz=3.5MB/s
# '/nvblox_node/combined_esdf_pointcloud', # 미발행 (static_tsdf 모드)
# '/nvblox_node/.*_layer',            # 4토픽 매칭 → 16MB/s!
# '/nvblox_node/human_voxels',        # static_tsdf 모드 미발행
```
**효과**: Foxglove 경유 nvblox 대역폭 ~21.7 MB/s → ~0.1 MB/s

### 9.7 nvblox 퍼블리시 빈도 오버라이드 (Docker launch)
```bash
# 파일: isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py
# nvblox_node parameters에 추가
```
**추가된 매개변수**:
```python
# --- DDS 대역폭 최적화 ---
'publish_layer_rate_hz': 0.0,       # layer 토픽 발행 중지 (Host 미구독)
'publish_debug_vis_rate_hz': 2.0,    # ESDF pointcloud 발행 유지 (2Hz)
'update_mesh_rate_hz': 5.0,          # mesh 내부 갱신은 유지 (costmap용)
```
**효과**: DDS 네트워크에 layer 토픽 자체를 미발행 → 추가 대역폭 절감

### 9.8 Host 패키지 빌드
```bash
cd /home/jetson/jupiter_ws_ros2
source install/setup.bash
colcon build --packages-select jupiter_nav_VO --symlink-install
```
**목적**: Foxglove whitelist 변경 반영
**결과**: `Finished <<< jupiter_nav_VO [5.65s]` 성공

### 9.9 검증 대기
- Docker launch 재시작 필요 (`publish_layer_rate_hz: 0.0` 반영)
- Host launch 재시작 필요 (trimmed whitelist 반영)
- Foxglove에서 시각화 응답성 확인 예정
