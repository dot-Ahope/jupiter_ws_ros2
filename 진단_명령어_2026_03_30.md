# 진단 명령어 2026-03-30: nvblox ↔ Nav2 연동 + cuVSLAM 진단 + 설정 테스트

## 배경
- 2026-03-27에 nvblox Foxglove extension 기반 3D 시각화 완성
- 2026-03-30에 nvblox → Nav2 costmap 연동 검증 및 TF 오류 수정

---

## Phase 1: nvblox costmap 플러그인 사전 조건 확인

### 1.1 static_map_slice 토픽 발행 주파수 확인
```bash
ros2 topic hz /nvblox_node/static_map_slice --window 5
```
**목적**: Docker nvblox_node가 DistanceMapSlice를 Host로 발행 중인지 확인
**결과**:
```
average rate: 9.126
  min: 0.099s max: 0.121s std dev: 0.00815s window: 5
```
- ~9.1Hz로 정상 발행 중

### 1.2 static_map_slice 토픽 상세 정보
```bash
ros2 topic info /nvblox_node/static_map_slice --verbose
```
**목적**: 타입, QoS, Publisher/Subscriber 확인
**결과**:
```
Type: nvblox_msgs/msg/DistanceMapSlice
Publisher count: 1 (nvblox_node, Docker)
Subscription count: 0 (Nav2 미실행 상태)
QoS: RELIABLE / VOLATILE
```

### 1.3 static_map_slice 메시지 내용 확인
```bash
ros2 topic echo /nvblox_node/static_map_slice --once --no-arr
```
**목적**: 슬라이스 크기, 해상도, 좌표계 확인
**결과**:
```yaml
header:
  frame_id: odom
origin:
  position: {x: -3.45, y: -3.25}
resolution: 0.05000000074505806
width: 136
height: 128
unknown_value: -1000.0
data: '<sequence type: float32, length: 17408>'
```
- 136×128 cells, resolution 0.05m, frame_id=odom, 17408 float values

### 1.4 nvblox_nav2 플러그인 라이브러리 확인
```bash
ls -la /home/jetson/isaac_ws_ros/install/nvblox_nav2/lib/libnvblox_nav2.so
```
**목적**: shared library 존재 및 크기 확인
**결과**:
```
-rwxr-xr-x 1 root root 926504 Mar 23 15:42 libnvblox_nav2.so
```
- 926KB, Mar 23 빌드 (root 소유 — Docker에서 빌드됨)

### 1.5 DistanceMapSlice 메시지 타입 Host 가용성
```bash
ros2 interface show nvblox_msgs/msg/DistanceMapSlice
```
**목적**: Host에서 메시지 타입 해석 가능한지 확인
**결과**: 성공 — 메시지 구조 정상 출력 (이전 세션 심볼릭 링크 수정 덕분)

### 1.6 AMENT_PREFIX_PATH에 nvblox_nav2 포함 확인
```bash
echo $AMENT_PREFIX_PATH | tr ':' '\n' | grep nvblox
```
**목적**: colcon install 경로에 nvblox_nav2가 포함되어 있는지 확인
**결과**:
```
/home/jetson/isaac_ws_ros/install/nvblox_nav2
```
- 3회 등장 (여러 setup.bash 중첩) — 로드 가능

---

## Phase 2: TF Chain 확인

### 2.1 map → base_footprint TF 확인
```bash
ros2 run tf2_ros tf2_echo map base_footprint
```
**목적**: SLAM Toolbox → EKF 전체 TF chain 정상 여부 확인
**결과**:
```
Translation: [0.013, 0.001, 0.000]
Rotation RPY: [-0.000, 0.000, -37.136°]
```
- map → base_footprint TF 정상 (SLAM Toolbox + EKF)

### 2.2 odom → base_footprint TF 확인
```bash
timeout 5 ros2 run tf2_ros tf2_echo odom base_footprint
```
**목적**: local_costmap이 사용하는 odom→base_footprint TF 확인
**결과**:
```
Translation: [0.007, 0.000, -0.000]
Rotation RPY: [0.000, 0.000, -31.948°]
```
- odom → base_footprint TF 정상 (EKF)

---

## Phase 3: Nav2 navigation stack 실행 및 노드 확인

### 3.1 Nav2 실행
```bash
cd /home/jetson/jupiter_ws_ros2
source install/setup.bash
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```
**목적**: Nav2 navigation stack 실행 (nav2_params_fused.yaml 기반)
**결과**: 12개 Nav2 노드 기동 완료

### 3.2 Nav2 노드 목록 확인
```bash
ros2 node list | grep -E 'controller|planner|costmap|behavior|bt_navigator|smoother|waypoint|velocity'
```
**목적**: Nav2 관련 노드 모두 실행 중인지 확인
**결과**:
```
/behavior_server
/bt_navigator
/bt_navigator_navigate_through_poses_rclcpp_node
/bt_navigator_navigate_to_pose_rclcpp_node
/controller_server
/global_costmap/global_costmap
/goal_pose_restamper
/local_costmap/local_costmap
/planner_server
/smoother_server
/velocity_smoother
/waypoint_follower
```
- 12개 노드 모두 실행 중

### 3.3 lifecycle 상태 확인
```bash
ros2 lifecycle get /local_costmap/local_costmap
ros2 lifecycle get /global_costmap/global_costmap
ros2 lifecycle get /controller_server
ros2 lifecycle get /planner_server
ros2 lifecycle get /bt_navigator
```
**목적**: 모든 Nav2 노드가 `active` 상태인지 확인
**결과**: 모두 `active [3]`

---

## Phase 4: nvblox_layer 로드 및 구독 확인

### 4.1 costmap 플러그인 목록 확인
```bash
ros2 param get /local_costmap/local_costmap plugins
ros2 param get /global_costmap/global_costmap plugins
```
**목적**: nvblox_layer가 실제 로드되었는지 확인
**결과**:
```
local_costmap: ['obstacle_layer', 'nvblox_layer', 'inflation_layer']
global_costmap: ['static_layer', 'obstacle_layer', 'nvblox_layer', 'inflation_layer']
```
- 양쪽 costmap 모두 nvblox_layer 로드 성공

### 4.2 static_map_slice 구독 수 확인
```bash
ros2 topic info /nvblox_node/static_map_slice
```
**목적**: Nav2 실행 후 구독자 수 변화 확인
**결과**:
```
Type: nvblox_msgs/msg/DistanceMapSlice
Publisher count: 1
Subscription count: 2
```
- Subscription 0→2 (local_costmap + global_costmap이 모두 구독)

### 4.3 nvblox_layer 파라미터 확인
```bash
ros2 param list /global_costmap/global_costmap | grep nvblox
```
**목적**: nvblox_layer 런타임 파라미터 확인
**결과**:
```
nvblox_layer.convert_to_binary_costmap
nvblox_layer.enabled (= True)
nvblox_layer.inflation_distance
nvblox_layer.max_cost_value
nvblox_layer.max_obstacle_distance
nvblox_layer.nav2_costmap_global_frame
nvblox_layer.nvblox_map_slice_topic
nvblox_layer.plugin
```

---

## Phase 5: costmap 발행 상태 진단

### 5.1 local_costmap 업데이트 주파수
```bash
timeout 20 ros2 topic hz /local_costmap/costmap_updates --window 5
```
**목적**: local_costmap 업데이트 발행 주파수 확인
**결과** (TF 수정 전):
```
average rate: 1.666
  min: 0.600s max: 0.601s std dev: 0.00040s window: 3
```
- ~1.67Hz로 발행 중

### 5.2 global_costmap 업데이트 주파수
```bash
timeout 15 ros2 topic hz /global_costmap/costmap_updates --window 5
```
**목적**: global_costmap 업데이트 발행 확인
**결과** (TF 수정 전):
```
average rate: 0.423
  min: 1.730s max: 3.000s std dev: 0.63505s window: 2
```
- ~0.4Hz로 발행 중, 편차가 크고 간헐적

### 5.3 costmap 파라미터 확인
```bash
ros2 param get /local_costmap/local_costmap publish_frequency    # → 2.0
ros2 param get /local_costmap/local_costmap always_send_full_costmap  # → False
ros2 param get /local_costmap/local_costmap update_frequency     # → 5.0
ros2 param get /global_costmap/global_costmap publish_frequency  # → 0.5
ros2 param get /global_costmap/global_costmap always_send_full_costmap  # → False
ros2 param get /global_costmap/global_costmap update_frequency   # → 1.0
ros2 param get /global_costmap/global_costmap transform_tolerance  # → 0.5
```
**목적**: costmap 발행 주기 및 TF 허용오차 확인

### 5.4 published_footprint으로 costmap 내부 활동 확인
```bash
timeout 30 ros2 topic hz /local_costmap/published_footprint --window 3
```
**목적**: costmap이 내부적으로 업데이트 중인지 간접 확인
**결과**:
```
average rate: 5.000 (min: 0.200s max: 0.200s)
```
- costmap 내부 루프 정상 동작 (5Hz = update_frequency)

### 5.5 costmap_raw 주파수 확인
```bash
timeout 10 ros2 topic hz /local_costmap/costmap_raw --window 3
timeout 15 ros2 topic hz /global_costmap/costmap_raw --window 3
```
**목적**: costmap_raw (nav2_msgs/Costmap) 직접 발행 확인
**결과**:
```
local_costmap_raw: ~1.7 Hz
global_costmap_raw: ~0.4 Hz
```

---

## Phase 6: TF 오류 진단

### 6.1 Nav2 실행 로그 확인
```bash
# Nav2 launch의 background terminal 출력 확인
```
**목적**: NvbloxCostmapLayer TF 경고 확인
**결과**:
```
[WARN] [global_costmap.global_costmap]: [NvbloxCostmapLayer] Can't transform: map to odom.
Error: Lookup would require extrapolation into the future.
Requested time 1774848199.928240 but the latest data is at time 1774848199.921837
```
- 약 6~30ms 시간차 → Docker nvblox timestamp > Host SLAM Toolbox TF timestamp
- 경고 ~30초 간격으로 반복 발생

### 6.2 NvbloxCostmapLayer 소스 코드 분석
```bash
grep -n "transform\|lookupTransform\|tolerance\|Can't transform" \
    /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_nav2/src/nvblox_costmap_layer.cpp
```
**목적**: TF lookup 로직 확인
**결과**: L250에서 `lookupTransform(... timestamp)` — slice 메시지의 정확한 시간으로 TF 요청

### 6.3 해당 코드 라인 확인
```bash
# nvblox_costmap_layer.cpp L237-270
```
```cpp
rclcpp::Time timestamp = slice->header.stamp;
// ...
T_G_S_msg = tf_buffer_->lookupTransform(
    nav2_costmap_global_frame_, slice_frame, timestamp).transform;
```
- slice→header.stamp(Docker 시간)으로 TF 요청 → Host TF에 아직 없는 미래 시간

---

## Phase 7: TF 오류 수정 및 재빌드

### 7.1 소스 코드 수정
```bash
# /home/jetson/isaac_ws_ros/src/isaac_ros_nvblox/nvblox_nav2/src/nvblox_costmap_layer.cpp
# L250: timestamp → tf2::TimePointZero
```
**변경 전**:
```cpp
tf_buffer_->lookupTransform(nav2_costmap_global_frame_, slice_frame, timestamp).transform;
```
**변경 후**:
```cpp
tf_buffer_->lookupTransform(nav2_costmap_global_frame_, slice_frame,
    tf2::TimePointZero).transform;
```
**목적**: 최신 가용 TF 사용 → 미래 시간 extrapolation 오류 방지

### 7.2 빌드 디렉토리 권한 수정
```bash
sudo chown -R jetson:jetson /home/jetson/isaac_ws_ros/install/nvblox_nav2/ \
    /home/jetson/isaac_ws_ros/build/nvblox_nav2/
```
**목적**: Docker에서 root로 빌드된 디렉토리 → Host 사용자 소유로 변경
**결과**: 권한 변경 성공

### 7.3 nvblox_nav2 패키지 재빌드
```bash
cd /home/jetson/isaac_ws_ros
colcon build --packages-select nvblox_nav2 --symlink-install
```
**목적**: 수정된 NvbloxCostmapLayer 빌드
**결과**:
```
Finished <<< nvblox_nav2 [36.4s]
Summary: 1 package finished [36.9s]
```
- symlink: `install/.../libnvblox_nav2.so` → `build/.../libnvblox_nav2.so`

---

## Phase 8: Nav2 재시작 및 연동 검증

### 8.1 Nav2 프로세스 종료
```bash
pkill -f "nav2_fused_navigation.launch.py"
sleep 2
pkill -9 -f "controller_server|planner_server|behavior_server|bt_navigator|smoother_server|waypoint_follower|velocity_smoother|lifecycle_manager_navigation"
sleep 3
ros2 daemon stop; sleep 2; ros2 daemon start
```
**목적**: 모든 Nav2 노드 중지 후 ROS 2 daemon 초기화

### 8.2 Nav2 재시작 (수정된 라이브러리 사용)
```bash
cd /home/jetson/jupiter_ws_ros2
source install/setup.bash
source /home/jetson/isaac_ws_ros/install/setup.bash
ros2 launch jupiter_nav_VO nav2_fused_navigation.launch.py
```
**목적**: 수정된 libnvblox_nav2.so 로드하여 Nav2 재시작
**결과**: 모든 노드 `active [3]`, TF 경고 **0건**

### 8.3 수정 후 TF 경고 확인
```bash
# Nav2 background terminal 출력 확인
```
**목적**: TF extrapolation 경고 소멸 확인
**결과**: **경고 없음** — `tf2::TimePointZero` 수정으로 완전 해결

### 8.4 costmap 발행 확인 (수정 후)
```bash
timeout 10 ros2 topic hz /local_costmap/costmap_raw --window 3
timeout 15 ros2 topic hz /global_costmap/costmap_raw --window 3
```
**결과**:
```
local_costmap_raw: ~1.7 Hz
global_costmap_raw: ~0.4 Hz
```
- 양쪽 costmap 모두 정상 발행

---

## Phase 9: costmap 장애물 데이터 검증

### 9.1 local_costmap_raw 장애물 분석
```bash
timeout 8 ros2 topic echo /local_costmap/costmap_raw --once | grep -E "^- [0-9]" | \
    python3 -c "
import sys
costs = [int(line.strip()[2:]) for line in sys.stdin]
print(f'Cells: {len(costs)}, nonzero: {sum(1 for c in costs if c>0)}, lethal: {sum(1 for c in costs if c>=253)}')
print(f'Values: {sorted(set(costs))}')
"
```
**목적**: costmap_raw에 실제 장애물 데이터가 있는지 확인
**결과**:
```
Cells: 128, nonzero: 81, lethal(>=253): 50, inflated(1-252): 31
Values: [0, 109, 113, 118, 136, 140, 159, 169, 174, 179, 198, 221, 230, 241, 253, 254]
```
- lethal(벽/장애물) + inflated(경계) 셀 존재 → **nvblox + LiDAR 데이터 반영 확인**

### 9.2 global_costmap_raw 장애물 분석
```bash
timeout 15 ros2 topic echo /global_costmap/costmap_raw --once | grep -E "^- [0-9]" | \
    python3 -c "..." # (동일 스크립트)
```
**결과**:
```
Cells: 128, nonzero: 128, lethal(>=253): 124, inflated(1-252): 4
Values: [198, 221, 230, 241, 253, 254]
```
- 대부분 lethal → SLAM map(static_layer) + obstacle + nvblox 합산

### 9.3 Python 직접 구독 검증
```python
python3 -c "
import rclpy
from rclpy.node import Node
from rclpy.qos import QoSProfile, DurabilityPolicy, ReliabilityPolicy
from nav2_msgs.msg import Costmap
import time

rclpy.init()
node = Node('costmap_test')
qos = QoSProfile(depth=5, reliability=ReliabilityPolicy.RELIABLE, durability=DurabilityPolicy.VOLATILE)
local_count = [0]; global_count = [0]

def local_cb(msg):
    local_count[0] += 1
    if local_count[0] == 1:
        print(f'  local: {msg.metadata.size_x}x{msg.metadata.size_y}, res={msg.metadata.resolution:.3f}')

def global_cb(msg):
    global_count[0] += 1
    if global_count[0] == 1:
        print(f'  global: {msg.metadata.size_x}x{msg.metadata.size_y}, res={msg.metadata.resolution:.3f}')

node.create_subscription(Costmap, '/local_costmap/costmap_raw', local_cb, qos)
node.create_subscription(Costmap, '/global_costmap/costmap_raw', global_cb, qos)

start = time.time()
while time.time() - start < 15:
    rclpy.spin_once(node, timeout_sec=0.1)
elapsed = time.time() - start
print(f'In {elapsed:.1f}s: local={local_count[0]} ({local_count[0]/elapsed:.2f}Hz), global={global_count[0]} ({global_count[0]/elapsed:.2f}Hz)')
rclpy.shutdown()
"
```
**목적**: Python rclpy로 costmap_raw 직접 수신 확인
**결과**:
```
local: 60x60, res=0.050, 26 msgs (1.72Hz)
global: 25x34, res=0.050, 5 msgs (0.33Hz)
```
- 양쪽 costmap 모두 정상 수신, 장애물 데이터 포함

---

## Phase 10: QoS 참고 사항

### 10.1 costmap 토픽 QoS 정리
```bash
ros2 topic info /local_costmap/costmap_updates --verbose
ros2 topic info /local_costmap/costmap_raw --verbose
ros2 topic info /global_costmap/costmap_raw --verbose
```

| 토픽 | 타입 | Reliability | Durability |
|------|------|-------------|------------|
| `costmap_updates` | map_msgs/OccupancyGridUpdate | RELIABLE | TRANSIENT_LOCAL |
| `costmap_raw` | nav2_msgs/Costmap | RELIABLE | VOLATILE |
| `costmap` (full) | nav_msgs/OccupancyGrid | RELIABLE | TRANSIENT_LOCAL |
| `published_footprint` | geometry_msgs/PolygonStamped | RELIABLE | VOLATILE |

**참고**: `costmap_updates`가 TRANSIENT_LOCAL이므로 `ros2 topic hz` 기본 QoS(VOLATILE)로는
측정이 불안정할 수 있음. `costmap_raw`(VOLATILE)로 측정하는 것이 더 정확함.

### 10.2 /map 토픽 QoS
```bash
ros2 topic info /map --verbose
```
**결과**:
- Publisher (slam_toolbox): RELIABLE / TRANSIENT_LOCAL
- Subscriber (global_costmap): RELIABLE / TRANSIENT_LOCAL → 호환 ✅

---
---

# Part 2: cuVSLAM 오도메트리 문제 진단 명령어

## Phase 11: CSV 데이터 분석

### 11.1 Foxglove CSV 데이터 다운로드
```
Foxglove에서 Plot 패널 → Export CSV
- plot_data_position_x.csv: position.x 데이터 (4개 토픽)
- plot_data.csv: orientation.z 데이터 (4개 토픽)
```
**목적**: wheel, RF2O, odom_adapted, VSLAM 오도메트리 비교 데이터 확보
**결과**: 15648 lines, 0~155초, 4개 토픽 데이터

### 11.2 CSV 위치 데이터 분석
```python
python3 -c "
import csv
from collections import defaultdict
data = defaultdict(list)
with open('plot_data_position_x.csv') as f:
    reader = csv.DictReader(f)
    for row in reader:
        for key in row:
            if key != 'elapsed time':
                val = row[key]
                if val:
                    data[key].append((float(row['elapsed time']), float(val)))

for topic, points in sorted(data.items()):
    if points:
        print(f'{topic}:')
        print(f'  count={len(points)}, t=[{points[0][0]:.1f}~{points[-1][0]:.1f}]s')
        print(f'  x=[{points[0][1]:.3f} → {points[-1][1]:.3f}], range={max(p[1] for p in points)-min(p[1] for p in points):.3f}m')
"
```
**목적**: 각 오도메트리 토픽의 position.x 범위와 데이터 수 비교
**결과**:
```
/odom.pose.pose.position.x (wheel):
  count=3872, t=[2.4~155.6]s, x=[0.000 → 0.152], range=1.607m
/rf2o_laser_odometry/odom.pose.pose.position.x (RF2O):
  count=3872, t=[2.4~155.6]s, x=[-0.004 → 0.180]
/visual_slam/tracking/odometry_adapted.pose.pose.position.x (VSLAM):
  count=3872, t=[2.4~155.6]s, x=[NaN → -1.790], range=1.823m
/visual_slam/tracking/odometry.pose.pose.position.x (VSLAM raw):
  count=3832  # 실제 VSLAM 데이터
```

### 11.3 VSLAM 타임라인 상세 분석
```python
python3 -c "
import csv
from collections import defaultdict
data = defaultdict(list)
with open('plot_data_position_x.csv') as f:
    reader = csv.DictReader(f)
    cols = None
    for row in reader:
        if cols is None:
            cols = [k for k in row.keys() if 'visual_slam/tracking/odometry_adapted' in k]
        t = float(row['elapsed time'])
        for c in cols:
            v = row[c]
            if v:
                data['vslam'].append((t, float(v)))

points = data['vslam']
print(f'Total VSLAM points: {len(points)}')
print(f'Time range: {points[0][0]:.1f} ~ {points[-1][0]:.1f}s')

# Find frozen periods (same value repeated)
frozen_start = None
for i in range(1, len(points)):
    if abs(points[i][1] - points[i-1][1]) < 1e-7:
        if frozen_start is None:
            frozen_start = i-1
    else:
        if frozen_start is not None:
            duration = points[i-1][0] - points[frozen_start][0]
            count = i - frozen_start
            if count > 5:
                print(f'  Frozen: t={points[frozen_start][0]:.1f}~{points[i-1][0]:.1f} ({duration:.1f}s, {count} samples, x={points[frozen_start][1]:.6f})')
            frozen_start = None

# Find big jumps
for i in range(1, len(points)):
    dx = abs(points[i][1] - points[i-1][1])
    dt = points[i][0] - points[i-1][0]
    if dx > 0.05:
        print(f'  Jump: t={points[i][0]:.1f}, dx={points[i][1]-points[i-1][1]:+.3f}m (dt={dt:.1f}s)')
"
```
**목적**: VSLAM tracking frozen 기간과 큰 점프 이벤트 식별
**결과**:
- Frozen: t=44.7~50.0 (106 samples, 5.3초), t=111.8~112.0, t=114.5~115.2
- Jump: t=64.2 dx=-1.446m, t=110 ±0.08m, t=117 ±0.09m, t=122 ±0.30m, t=124.3→139.2 gap 후 -0.76m

---

# Part 3: cuVSLAM 설정 테스트 명령어

## Phase 12: Test A — VSLAM-only (emitter OFF, 60Hz)

### 12.1 기존 Docker 프로세스 종료
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
  pkill -f 'isaac_ros_vslam_nvblox_realsense' 2>/dev/null
  sleep 1
  pkill -f 'component_container' 2>/dev/null
  pkill -f 'realsense2_camera_node' 2>/dev/null
"
sleep 3
```
**목적**: 기존 VSLAM+nvblox 프로세스 정리
**결과**: 프로세스 종료 확인

### 12.2 RealSense 카메라 USB 리셋
```bash
python3 /home/jetson/reset_realsense.py
sleep 5
```
**목적**: 카메라 상태 초기화 (이전 emitter_on_off 설정 잔존 방지)
**결과**: USB 리셋 성공

### 12.3 Test A 실행 (VSLAM-only, emitter OFF, 60Hz)
```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c "
  source /workspaces/isaac_ros-dev/install/setup.bash &&
  ros2 launch isaac_ros_visual_slam isaac_ros_visual_slam_realsense.launch.py
" 2>&1 | tee /home/jetson/isaac_ws_ros/test_A_vslam_only.log
```
**목적**: emitter_on_off 없이 순수 IR 60Hz에서 cuVSLAM 추적 성능 검증
**결과**: cuVSLAM 정상 기동, tracker 생성 ~9.8초

### 12.4 Test A 오도메트리 수집 (10초)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
python3 -c \"
import rclpy
from rclpy.node import Node
from nav_msgs.msg import Odometry
import time, math

rclpy.init()
node = Node('test_a')
msgs = []
first_pos = None

def cb(msg):
    global first_pos
    p = msg.pose.pose.position
    if first_pos is None:
        first_pos = (p.x, p.y, p.z)
    msgs.append((time.time(), p.x, p.y, p.z))

node.create_subscription(Odometry, '/visual_slam/tracking/odometry', cb, 10)
start = time.time()
while time.time() - start < 10:
    rclpy.spin_once(node, timeout_sec=0.05)
elapsed = time.time() - start

if msgs:
    rate = len(msgs) / elapsed
    dx = msgs[-1][1] - msgs[0][1]
    dy = msgs[-1][2] - msgs[0][2]
    dz = msgs[-1][3] - msgs[0][3]
    disp = math.sqrt(dx*dx + dy*dy + dz*dz)
    frozen = sum(1 for i in range(1, len(msgs))
                 if abs(msgs[i][1]-msgs[i-1][1]) < 1e-6
                 and abs(msgs[i][2]-msgs[i-1][2]) < 1e-6)
    print(f'Msgs: {len(msgs)}, Rate: {rate:.1f} Hz, Displacement: {disp:.6f}m')
    print(f'Frozen frames: {frozen}/{len(msgs)-1}')
    print(f'First: ({msgs[0][1]:.6f}, {msgs[0][2]:.6f}, {msgs[0][3]:.6f})')
    print(f'Last:  ({msgs[-1][1]:.6f}, {msgs[-1][2]:.6f}, {msgs[-1][3]:.6f})')
else:
    print('No messages received')
rclpy.shutdown()
\"
"
```
**목적**: 정지 상태에서 VSLAM 발행 주파수, 변위, frozen 비율 측정
**결과**:
```
Msgs: 540, Rate: 54.0 Hz, Displacement: 0.000040m
Frozen frames: 0/539
First: (0.000031, -0.000027, -0.000006)
Last:  (0.000061, -0.000046, 0.000009)
```

### 12.5 Test A Jitter 경고 확인
```bash
grep -c "Delta between" /home/jetson/isaac_ws_ros/test_A_vslam_only.log
```
**목적**: 프레임 간격 jitter 경고 수 확인
**결과**: 71건

```bash
grep "Delta between" /home/jetson/isaac_ws_ros/test_A_vslam_only.log | \
  grep -oP 'Delta between current and previous frame \[\K[0-9.]+' | sort -rn | head -5
```
**목적**: 최대 jitter 값 확인
**결과**: 351ms, 100ms, 66ms, 50ms, 50ms (시작 시 큰 값, 이후 안정)

### 12.6 Test A IMU 에러 확인
```bash
grep -c "force pause" /home/jetson/isaac_ws_ros/test_A_vslam_only.log
```
**목적**: RealSense IMU Hardware Error 발생 수 확인
**결과**: 2건 (시작 시 "Motion Module force pause", 비치명적)

---

## Phase 13: Test B — base_frame='base_link'

### 13.1 Launch 파일 수정
```bash
# /home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py
# 변경: 'base_frame': 'camera_link' → 'base_frame': 'base_link'
```
**목적**: cuVSLAM base_frame을 robot body frame으로 변경

### 13.2 Test A 프로세스 종료
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
  pkill -f 'isaac_ros_visual_slam_realsense' 2>/dev/null
  sleep 1
  pkill -f 'component_container' 2>/dev/null
  pkill -f 'realsense2_camera_node' 2>/dev/null
"
sleep 3
```
**목적**: Test A 프로세스 정리
**결과**: 종료 확인

### 13.3 RealSense 리셋
```bash
python3 /home/jetson/reset_realsense.py
sleep 5
```
**목적**: 카메라 상태 초기화
**결과**: 리셋 성공

### 13.4 Test B 실행
```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c "
  source /workspaces/isaac_ros-dev/install/setup.bash &&
  ros2 launch isaac_ros_visual_slam isaac_ros_vslam_nvblox_realsense.launch.py
" 2>&1 | tee /home/jetson/isaac_ws_ros/test_B_base_link.log
```
**목적**: base_frame='base_link' 설정에서 cuVSLAM+nvblox 동작 확인
**결과**: tracker 생성 0.33초 (빠름), 하지만 오도메트리 미발행

### 13.5 Test B 오도메트리 수집 시도 (10초)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
python3 -c \"
import rclpy
from rclpy.node import Node
from nav_msgs.msg import Odometry
import time, math

rclpy.init()
node = Node('test_b')
msgs = []
first_pos = None

def cb(msg):
    global first_pos
    p = msg.pose.pose.position
    if first_pos is None:
        first_pos = (p.x, p.y, p.z)
    msgs.append((time.time(), p.x, p.y, p.z))

node.create_subscription(Odometry, '/visual_slam/tracking/odometry', cb, 10)
start = time.time()
while time.time() - start < 10:
    rclpy.spin_once(node, timeout_sec=0.05)
elapsed = time.time() - start

if msgs:
    rate = len(msgs) / elapsed
    dx = msgs[-1][1] - msgs[0][1]
    dy = msgs[-1][2] - msgs[0][2]
    dz = msgs[-1][3] - msgs[0][3]
    disp = math.sqrt(dx*dx + dy*dy + dz*dz)
    frozen = sum(1 for i in range(1, len(msgs))
                 if abs(msgs[i][1]-msgs[i-1][1]) < 1e-6
                 and abs(msgs[i][2]-msgs[i-1][2]) < 1e-6)
    print(f'Msgs: {len(msgs)}, Rate: {rate:.1f} Hz, Displacement: {disp:.6f}m')
    print(f'Frozen frames: {frozen}/{len(msgs)-1}')
else:
    print('No messages received')
rclpy.shutdown()
\"
"
```
**목적**: base_frame='base_link' 설정에서 오도메트리 발행 여부 확인
**결과**: `No messages received` — 오도메트리 미발행

### 13.6 Test B nvblox 에러 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
  grep -i 'error\|failed\|camera_link' /home/jetson/isaac_ws_ros/test_B_base_link.log | head -10
"
```
**목적**: nvblox TF lookup 실패 메시지 확인
**결과**:
```
[nvblox_node]: Transformer - lookTransform failed from frame 'camera_link'
  "camera_link" passed to lookupTransform argument target_frame does not exist.
```

### 13.7 Test B Static TF 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
  ros2 topic echo /tf_static --once --no-arr 2>/dev/null | head -30
"
```
**목적**: base_link→camera_link static TF 존재 여부 확인
**결과**: `base_link → camera_link: translation(0.274, 0, 0.055)` — static TF 존재하지만 odom→base_link dynamic TF가 없어 chain 단절

---

## Phase 14: Test C — image_jitter_threshold_ms=100

### 14.1 Launch 파일 수정 (2건)
```bash
# /home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py
# 1. 복원: 'base_frame': 'base_link' → 'base_frame': 'camera_link'
# 2. 변경: 'image_jitter_threshold_ms': 68.00 → 'image_jitter_threshold_ms': 100.00
```
**목적**: base_frame 원복 + jitter threshold를 68→100ms로 완화

### 14.2 Test B 프로세스 종료
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
  pkill -f 'isaac_ros_vslam_nvblox_realsense' 2>/dev/null
  sleep 1
  pkill -f 'component_container' 2>/dev/null
  pkill -f 'realsense2_camera_node' 2>/dev/null
"
sleep 3
```
**목적**: Test B 프로세스 정리
**결과**: 종료 확인

### 14.3 RealSense 리셋
```bash
python3 /home/jetson/reset_realsense.py
sleep 5
```
**목적**: 카메라 상태 초기화
**결과**: 리셋 성공

### 14.4 Test C 실행
```bash
docker exec -it isaac_ros_dev-aarch64-container bash -c "
  source /workspaces/isaac_ros-dev/install/setup.bash &&
  ros2 launch isaac_ros_visual_slam isaac_ros_vslam_nvblox_realsense.launch.py
" 2>&1 | tee /tmp/test_C_jitter100.log
```
**목적**: image_jitter_threshold_ms=100ms에서 cuVSLAM+nvblox 추적 성능 확인
**결과**: cuVSLAM 기동, 발행 시작

### 14.5 Test C 오도메트리 수집 (10초)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
python3 -c \"
import rclpy
from rclpy.node import Node
from nav_msgs.msg import Odometry
import time, math

rclpy.init()
node = Node('test_c')
msgs = []
first_pos = None

def cb(msg):
    global first_pos
    p = msg.pose.pose.position
    if first_pos is None:
        first_pos = (p.x, p.y, p.z)
    msgs.append((time.time(), p.x, p.y, p.z))

node.create_subscription(Odometry, '/visual_slam/tracking/odometry', cb, 10)
start = time.time()
while time.time() - start < 10:
    rclpy.spin_once(node, timeout_sec=0.05)
elapsed = time.time() - start

if msgs:
    rate = len(msgs) / elapsed
    dx = msgs[-1][1] - msgs[0][1]
    dy = msgs[-1][2] - msgs[0][2]
    dz = msgs[-1][3] - msgs[0][3]
    disp = math.sqrt(dx*dx + dy*dy + dz*dz)
    frozen = sum(1 for i in range(1, len(msgs))
                 if abs(msgs[i][1]-msgs[i-1][1]) < 1e-6
                 and abs(msgs[i][2]-msgs[i-1][2]) < 1e-6)
    max_jump = max(
        math.sqrt((msgs[i][1]-msgs[i-1][1])**2 + (msgs[i][2]-msgs[i-1][2])**2)
        for i in range(1, len(msgs))
    )
    print(f'Msgs: {len(msgs)}, Rate: {rate:.1f} Hz, Displacement: {disp:.6f}m')
    print(f'Frozen frames: {frozen}/{len(msgs)-1}')
    print(f'Max inter-frame jump: {max_jump:.6f}m')
    print(f'First: ({msgs[0][1]:.6f}, {msgs[0][2]:.6f}, {msgs[0][3]:.6f})')
    print(f'Last:  ({msgs[-1][1]:.6f}, {msgs[-1][2]:.6f}, {msgs[-1][3]:.6f})')
else:
    print('No messages received')
rclpy.shutdown()
\"
"
```
**목적**: jitter threshold=100ms에서 발행 주파수, frozen 비율, 변위 측정
**결과**:
```
Msgs: 247, Rate: 25.2 Hz, Displacement: 0.000509m
Frozen frames: 158/246
Max inter-frame jump: 0.000285m
```

### 14.6 Test C Jitter 경고 수
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
  grep -c 'Delta between' /workspaces/isaac_ros-dev/test_C_jitter100.log
"
```
**목적**: 100ms 임계값에서 jitter 경고 수 확인
**결과**: 114건

### 14.7 Test C 최대 Jitter 값 (Top 5)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
  grep -oP 'Delta between current and previous frame \[\K[0-9.]+' \
    /workspaces/isaac_ros-dev/test_C_jitter100.log | sort -rn | head -5
"
```
**목적**: 최대 프레임 간격 jitter 분석
**결과**: 354.854144ms, 233.677312ms, 206.473216ms, 200.528896ms, 200.491776ms

### 14.8 Test C 최소 Jitter 값 (Bottom 5)
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
  grep -oP 'Delta between current and previous frame \[\K[0-9.]+' \
    /workspaces/isaac_ros-dev/test_C_jitter100.log | sort -n | head -5
"
```
**목적**: jitter 경고의 하한값 확인 (100ms 초과만 기록)
**결과**: 100.144896ms, 100.149248ms, 100.149504ms, 100.150272ms, 100.155648ms

### 14.9 Test C IMU 에러 확인
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
  grep -c 'force pause' /workspaces/isaac_ros-dev/test_C_jitter100.log
"
```
**목적**: IMU Hardware Error 발생 수 확인
**결과**: 2건 (시작 시 발생, 비치명적)

### 14.10 Test C 로그 저장
```bash
docker exec isaac_ros_dev-aarch64-container bash -c "
  cp /tmp/test_C_jitter100.log /workspaces/isaac_ros-dev/
"
```
**목적**: Docker 로그 영구 저장
**결과**: `/home/jetson/isaac_ws_ros/test_C_jitter100.log` 저장

---

## Phase 15: Launch 파일 원상복구

### 15.1 설정 원복
```bash
# /home/jetson/isaac_ws_ros/src/isaac_ros_visual_slam/isaac_ros_visual_slam/launch/isaac_ros_vslam_nvblox_realsense.launch.py
# 복원: 'image_jitter_threshold_ms': 100.00 → 'image_jitter_threshold_ms': 68.00
# 확인: 'base_frame': 'camera_link' (이미 원복)
```
**목적**: 3개 테스트 후 프로덕션 설정으로 원복
**결과**: 원복 완료

---

## Phase 16: 테스트 결과 요약

### 16.1 종합 비교표

| 테스트 | 구성 | 발행 주파수 | Frozen | Jitter 경고 | 최대 Jitter | 결과 |
|--------|------|-----------|--------|------------|------------|------|
| A: VSLAM-only | emitter OFF, 60Hz, threshold=34ms | 54 Hz | 0% | 71건 | 351ms | ✅ 정상 |
| B: base_link | emitter_on_off, threshold=68ms | 0 Hz | N/A | 0건 | N/A | ❌ 실패 |
| C: jitter=100 | emitter_on_off, threshold=100ms | 25 Hz | 64% | 114건 | 354ms | ⚠️ 열화 |

### 16.2 핵심 결론
- **emitter_on_off 모드가 cuVSLAM 추적 실패의 근본 원인**
- emitter OFF 60Hz → 54Hz 정상 추적 (Test A)
- emitter_on_off → 25Hz, 64% frozen (Test C)
- base_frame='base_link' + publish_odom_to_base_tf=False → TF 순환 의존 (Test B)
